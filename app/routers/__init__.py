"""HTTP routers package.

Phase 1 Plan 03 lands ``csp_report`` here. Plan 07 (auth) and Plan 08
(debug) add ``auth.py`` and ``debug.py``. Each router module is imported
and mounted by ``app/main.py`` (Plan 09) — adding a router does not require
modifying this ``__init__.py``.
"""
