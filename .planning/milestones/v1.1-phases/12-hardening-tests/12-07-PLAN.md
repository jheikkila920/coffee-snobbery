---
phase: 12-hardening-tests
plan: 07
type: execute
wave: 5
depends_on: [12-01, 12-02, 12-05]
files_modified:
  - .github/workflows/ci.yml
  - README.md
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]
user_setup: []
must_haves:
  truths:
    - "On push and pull_request, GitHub Actions runs ruff format --check, ruff check, the grep tests, and the full pytest suite against a Postgres 16 service container"
    - "The CI pytest run sets SNOB_CI=1 so unexpected skips fail the gate, and excludes tests/e2e (Playwright is local-only, D-06)"
    - "CI sets DATABASE_URL/POSTGRES_* so the conftest <db>_test forcing resolves to the service container; no separate migration step is needed"
    - "README documents the iOS Wake-Lock fallback caveat and the one-time G-01 chown deploy step, and the single-worker/restore/sw.js notes are confirmed present"
    - "The existing README doc tests (NGINX block, env-example hints) still pass after the gap-fill"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "D-04 push/PR regression gate"
      contains: "postgres:16"
    - path: "README.md"
      provides: "D-08 publishable gap-fill"
      contains: "Wake Lock"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/ (excl. e2e) + ruff + grep"
      via: "pytest -rs --ignore=tests/e2e with SNOB_CI=1"
      pattern: "SNOB_CI"
    - from: ".github/workflows/ci.yml"
      to: "postgres:16-alpine service container"
      via: "services.postgres + DATABASE_URL env"
      pattern: "services"
---

<objective>
Close the phase with the regression net and the publishable docs: a GitHub Actions CI workflow (D-04) that runs ruff + the grep tests + the full pytest suite against a Postgres 16 service container on every push/PR, and a targeted README gap-fill (D-08) adding the named criterion-#5 gaps (iOS Wake-Lock caveat + the one-time G-01 chown deploy note) and confirming the single-worker restatement, restore runbook, and /sw.js Cache-Control note are present — without rewriting the already-tested README prose.

Purpose: Criterion #1 reproducibility (CI net, not a deploy path) and criterion #5 (publishable README). This is the final ship gate.
Output: `.github/workflows/ci.yml`, edited `README.md`.

Depends on: Plan 12-01 (SNOB_CI gate semantics + addopts), Plan 12-02 (the grep tests CI runs), Plan 12-05 (requirements-dev.txt with playwright — CI installs from it; the Playwright browser is NOT installed in CI since tests/e2e is excluded).

Verify the GitHub Actions Postgres service-container + actions/checkout + actions/setup-python pip-cache wiring via Context7/ctx7 before writing the workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-hardening-tests/12-CONTEXT.md
@.planning/phases/12-hardening-tests/12-RESEARCH.md
@.planning/phases/12-hardening-tests/12-PATTERNS.md

<interfaces>
<!-- Verified facts for the CI workflow + README gap-fill. -->

conftest DB forcing (verified): when DATABASE_URL points at a db WITHOUT "test" in the name, conftest rewrites it to `<db>_test`, then `_provision_test_db` runs `alembic upgrade head` against `<db>_test`. So CI sets DATABASE_URL=.../snobbery and conftest handles the rename + migration — NO explicit alembic step needed.

alembic.ini (verified): `sqlalchemy.url =` is INTENTIONALLY EMPTY; app/migrations/env.py sets it from `settings.DATABASE_URL` at runtime. So the only CI requirement is that DATABASE_URL/POSTGRES_* env vars are set — no alembic.ini patch (resolves RESEARCH Pitfall 3 / Open Question 2).

Required CI env: DATABASE_URL, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, APP_SECRET_KEY (>=32 chars), APP_ENCRYPTION_KEY (valid Fernet shape), SNOB_CI=1.
CI command: `python -m pytest tests/ -rs --tb=short --ignore=tests/e2e` (e2e excluded per D-06; the grep tests under tests/ci/ run as part of this).
ruff is in requirements-dev.txt (>=0.15.13,<0.16); CLAUDE.md: warnings-as-errors → `ruff format --check .` then `ruff check .`.

README (verified existing — DO NOT rewrite these, only add the gaps):
  - "### Single uvicorn worker — DO NOT change" (line 63) — single-worker rule ALREADY present (3-places audit). D-08 restatement already satisfied; confirm + leave.
  - "## Restore from backup" (line 199) — restore runbook ALREADY present (psql < dump.sql, photos tar). Confirm it matches CLAUDE.md; extend only if a step is missing.
  - "### NGINX server-block snippet" /sw.js `Cache-Control: no-cache` note (lines 106-120) — ALREADY present. Confirm + leave.
  - GENUINE GAPS (grep-confirmed absent): iOS Wake-Lock fallback caveat (zero mentions of wake-lock/NoSleep/iOS), and the G-01 chown deploy note (zero mentions of chown/app:app/root-owned).

README doc tests that MUST stay green: tests/docs/test_readme_nginx.py (asserts literals: Strict-Transport-Security, max-age=63072000, proxy_set_header X-Forwarded-Proto $scheme, proxy_buffering off), tests/test_env_example.py.

Deploy stays git pull + docker build on the VPS (CONTEXT: CI is a net, NOT a deploy pipeline). G-01 chown is a deploy-time OPS note only — DO NOT implement infra changes here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI workflow (D-04)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - requirements-dev.txt (post-Plan-12-05 — confirm ruff + pytest + playwright pins; CI installs from this file)
    - tests/conftest.py (the DATABASE_URL `<db>_test` forcing + `_provision_test_db` so the workflow needs no manual migration step; the SNOB_CI helper from Plan 12-01)
    - alembic.ini + app/migrations/env.py (confirm the URL is env-driven, no hardcoded string — so CI only needs env vars)
    - pyproject.toml (post-Plan-12-01 addopts without `-x`)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the ci.yml structure, lines 524-572; the conftest integration note)
    - .planning/phases/12-hardening-tests/12-RESEARCH.md (Pattern 4 + the critical conftest integration note, lines 306-366)
    - Context7/ctx7: query "GitHub Actions postgres service container actions/setup-python pip cache" to confirm current action major versions BEFORE writing the YAML
  </read_first>
  <action>
    Create .github/workflows/ci.yml. `name: CI`; `on: [push, pull_request]`; one job `test` on `ubuntu-latest`. Add a `services.postgres` using `postgres:16-alpine` with env POSTGRES_USER/PASSWORD/DB and a `pg_isready` healthcheck (health-interval 5s, retries 10) and `ports: ["5432:5432"]`. Steps in order: (1) `actions/checkout` (confirm current major via Context7); (2) `actions/setup-python` with `python-version: "3.12"` and `cache: pip` (confirm current major); (3) `pip install -r requirements-dev.txt`; (4) `ruff format --check .`; (5) `ruff check .`; (6) the pytest step with env DATABASE_URL=`postgresql+psycopg://<user>:<pass>@localhost:5432/snobbery`, POSTGRES_USER/PASSWORD/DB matching the service, APP_SECRET_KEY (a GitHub secret with a 64-char fallback literal), APP_ENCRYPTION_KEY (a secret with a valid Fernet-shaped fallback), `SNOB_CI: "1"`, running `python -m pytest tests/ -rs --tb=short --ignore=tests/e2e`. Do NOT install the Playwright browser and do NOT run tests/e2e (D-06 — Playwright is local-only). Do NOT add a deploy/build-push step (CI is a regression net only). Rely on conftest to rewrite snobbery→snobbery_test and run alembic — add NO manual migration step. Order the lint steps before pytest so a format/lint failure fails fast.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['test']; assert 'postgres' in str(j['services']); s=str(j['steps']); assert 'SNOB_CI' in s and '--ignore=tests/e2e' in s and 'ruff check' in s and 'ruff format' in s; print('ci.yml structure OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml defines a `postgres:16-alpine` service container with a pg_isready healthcheck
    - The workflow runs `ruff format --check .`, `ruff check .`, then `python -m pytest tests/ -rs --tb=short --ignore=tests/e2e`
    - The pytest step sets `SNOB_CI: "1"` and the required DATABASE_URL/POSTGRES_*/APP_SECRET_KEY/APP_ENCRYPTION_KEY env vars
    - tests/e2e is excluded; no Playwright browser install; no deploy/build-push step
    - No manual alembic step (conftest provisions the test DB)
    - The YAML parses (the verify command exits 0)
  </acceptance_criteria>
  <done>ci.yml runs ruff + grep + full pytest (excl. e2e) against Postgres 16 with SNOB_CI=1 on push/PR; no deploy path; YAML validates.</done>
</task>

<task type="auto">
  <name>Task 2: README gap-fill (D-08)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the WHOLE file — locate the existing "### Single uvicorn worker", "## Restore from backup", "## Deployment", and NGINX `/sw.js` sections so additions slot in WITHOUT touching tested prose)
    - tests/docs/test_readme_nginx.py (the exact literals that must remain: Strict-Transport-Security, max-age=63072000, `proxy_set_header X-Forwarded-Proto $scheme`, `proxy_buffering off`)
    - tests/test_env_example.py (confirm what it asserts so the gap-fill does not disturb env-example coverage)
    - CLAUDE.md "Restore from backup" section (the canonical restore runbook to reconcile against the README's existing one)
    - .planning/phases/11-pwa-mobile-polish/11-CONTEXT.md (the iOS Wake-Lock fallback caveat wording — silent-audio-loop / NoSleep.js, incomplete iOS Wake Lock API support, re-acquire on visibilitychange)
    - .planning/STATE.md Deferred Items (the exact G-01 chown command + rationale)
  </read_first>
  <action>
    Make SURGICAL additions only — do not rewrite tested prose. (1) iOS Wake-Lock caveat (GENUINE GAP): add a short subsection (under the deployment/troubleshooting area or a new "## Known caveats") documenting that Guided Brew Mode requests the Wake Lock API, that iOS Safari has incomplete Wake Lock support so the app falls back to a silent-audio-loop / NoSleep.js, and that the lock is re-acquired on `visibilitychange` — lift the wording from 11-CONTEXT. (2) G-01 chown deploy note (GENUINE GAP, deferred-ops): in the deployment runbook, add a one-time step that PRE-EXISTING VPS volumes (root-owned, predating the Dockerfile fix) need `docker compose run --rm -u root coffee-snobbery chown -R app:app /app/data` before backups + photo writes work; state that fresh volumes do not need it. Do NOT implement any infra change — this is a documentation note only. (3) Confirm (do NOT rewrite) that the single-worker restatement, the restore-from-backup runbook (psql < dump.sql + photos tar), and the NGINX `/sw.js Cache-Control: no-cache` note are already present; if the restore runbook is missing any step relative to CLAUDE.md, add only that step. Keep the four NGINX literals the doc test asserts intact. After editing, run the README doc tests to confirm they stay green.
  </action>
  <verify>
    <automated>python -m pytest tests/docs/test_readme_nginx.py tests/test_env_example.py -rs --tb=short -p no:cacheprovider 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains an iOS Wake-Lock fallback caveat (mentions Wake Lock + iOS + the silent-audio/NoSleep fallback)
    - README.md contains the one-time G-01 chown deploy step (`chown -R app:app /app/data`) flagged as needed only for pre-existing root-owned VPS volumes
    - The single-worker rule, restore runbook, and `/sw.js Cache-Control: no-cache` note remain present
    - `pytest tests/docs/test_readme_nginx.py tests/test_env_example.py -rs` exits 0 (the four NGINX literals + env-example hints intact)
    - No tested prose was rewritten (additions only)
  </acceptance_criteria>
  <done>README has the iOS Wake-Lock caveat and the G-01 chown note added surgically; the single-worker/restore/sw.js notes confirmed present; README doc tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| push/PR → main (regression) | CI is the automated boundary that catches a regression (failed test, lint error, missing nonce, leaked credential dump) before it reaches the deployable branch |
| operator → VPS deploy (docs) | the README is the artifact an operator trusts; a missing chown/wake-lock note crosses this boundary as an operational surprise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15 | Repudiation | CI gate (hollow green) | mitigate | The CI pytest step sets SNOB_CI=1, so unexpected skips (including the security grep tests from Plan 12-02 and the load-bearing service tests) fail the build, not pass silently |
| T-12-16 | Information Disclosure | CI secrets | mitigate | APP_SECRET_KEY / APP_ENCRYPTION_KEY come from GitHub secrets (with non-production fallback literals for the throwaway test DB only); no real production secret is committed to ci.yml |
| T-12-17 | (Operational, non-security) | deploy docs gaps | mitigate | D-08 adds the chown + wake-lock notes so a deploy does not silently fail on root-owned volumes or surprise the operator on iOS wake-lock behavior |
</threat_model>

<verification>
- `.github/workflows/ci.yml` parses and contains the postgres service, ruff steps, the SNOB_CI=1 pytest step, and the `--ignore=tests/e2e` exclusion.
- `pytest tests/docs/test_readme_nginx.py tests/test_env_example.py -rs` stays green after the README edits.
- README contains the iOS Wake-Lock caveat and the G-01 chown note.
</verification>

<success_criteria>
- D-04 closed: push/PR CI gate (ruff + grep + full pytest against Postgres 16, SNOB_CI=1, e2e excluded, no deploy path).
- D-08 closed: README gap-fill (Wake-Lock caveat + chown note added; single-worker/restore/sw.js confirmed) with doc tests still green.
- Phase ship gate complete: full suite + grep + lint gate in CI; Playwright pre-deploy smoke documented (Plan 12-06).
</success_criteria>

<output>
After completion, create `.planning/phases/12-hardening-tests/12-07-SUMMARY.md`
</output>
