---
phase: 12-hardening-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ci/test_csp_nonce.py
  - tests/ci/test_no_credential_dump.py
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "Every <script> and <style> tag under app/templates/ carries a nonce attribute"
    - "No 'unsafe-eval' or 'unsafe-inline' appears in app/templates/ outside the documented docs/decisions trade-off"
    - "model_dump() is never called in any app/ Python file that references ApiCredential"
    - "Both grep tests run with zero Postgres dependency and gate on every pytest invocation"
  artifacts:
    - path: "tests/ci/test_csp_nonce.py"
      provides: "D-07a CSP nonce/unsafe-* template grep"
      contains: "SCRIPT_WITHOUT_NONCE"
    - path: "tests/ci/test_no_credential_dump.py"
      provides: "D-07b SEC-6 model_dump-on-ApiCredential grep"
      contains: "model_dump"
  key_links:
    - from: "tests/ci/test_csp_nonce.py"
      to: "app/templates/**/*.html"
      via: "rglob + regex scan after comment strip"
      pattern: "TEMPLATES_DIR.rglob"
    - from: "tests/ci/test_no_credential_dump.py"
      to: "app/**/*.py referencing ApiCredential"
      via: "rglob + early-return guard + model_dump regex"
      pattern: "model_dump"
---

<objective>
Add the two permanent security grep tests (D-07): a CSP nonce/unsafe-directive scan of `app/templates/` (D-07a) and a SEC-6 scan forbidding `model_dump()` in any file that references `ApiCredential` (D-07b). Both clone the existing `tests/ci/test_no_unsafe_jinja.py` idiom and run on every pytest invocation (no Postgres needed) so the SEC-02 (CSP) and SEC-6 (decrypted-key non-leak) invariants are gated permanently.

Purpose: Criterion #4 of the phase goal (CSP audit becomes scripted) and the Phase 3 SEC-6 promise (model_dump grep) that was never written. These are net-new ship-gate tests.
Output: `tests/ci/test_csp_nonce.py` and `tests/ci/test_no_credential_dump.py`.

CRITICAL: These greps MUST pass clean. If either finds a real violation, the fix is in the SOURCE (a missing nonce in a template, or a model_dump on a credential), NEVER in the test. CLAUDE.md: never disable CSP, never log keys. A failing grep is a real finding to fix, not a test to loosen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-hardening-tests/12-CONTEXT.md
@.planning/phases/12-hardening-tests/12-RESEARCH.md
@.planning/phases/12-hardening-tests/12-PATTERNS.md

<interfaces>
<!-- The grep-test idiom to clone. Copy these helpers VERBATIM. -->

tests/ci/test_no_unsafe_jinja.py (the analog):
  - `TEMPLATES_DIR = Path("app/templates")`
  - `_JINJA_COMMENT = re.compile(r"\{#.*?#\}", re.DOTALL)`
  - `_HTML_COMMENT = re.compile(r"<!--.*?-->", re.DOTALL)`
  - `_strip_comments(source) -> str`  (strips both comment kinds before scanning)
  - `@pytest.mark.parametrize("template_path", list(TEMPLATES_DIR.rglob("*.html")) if TEMPLATES_DIR.exists() else [])`
  - assertion message format: `f"{template_path}: {message} (matched: {match.group(0)!r})"`

app/models/api_credential.py: the `last_four` column exists SO THAT the decrypted key never enters a model_dump-able Pydantic schema (SEC-6 invariant the D-07b grep enforces).
app/services/credentials.py: returns a frozen `@dataclass ProviderCredential` (not a Pydantic model) holding the decrypted key — confirm no `model_dump` exists there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSP nonce + unsafe-directive template grep (D-07a)</name>
  <files>tests/ci/test_csp_nonce.py</files>
  <read_first>
    - tests/ci/test_no_unsafe_jinja.py (the ENTIRE file — clone its structure: imports, `_strip_comments`, the parametrize-over-rglob guard, the assertion message format)
    - app/templates/base.html (confirm the actual nonce attribute pattern, e.g. `nonce="{{ csp_nonce }}"`, so the negative-lookahead regex matches real usage and does not false-positive)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the CSP-specific regex block, lines 283-296)
    - .planning/phases/12-hardening-tests/12-RESEARCH.md (Pattern 6 CSP grep, lines 451-495; the "do not soften the test" anti-pattern, line 537)
  </read_first>
  <action>
    Create tests/ci/test_csp_nonce.py cloning the test_no_unsafe_jinja.py structure. Copy `_JINJA_COMMENT`, `_HTML_COMMENT`, and `_strip_comments` VERBATIM. Define two checks over `TEMPLATES_DIR.rglob("*.html")`: (1) `SCRIPT_WITHOUT_NONCE = re.compile(r'<(script|style)(?![^>]*\bnonce\s*=)[^>]*>', re.IGNORECASE)` — a `<script>`/`<style>` open tag with no `nonce=` before the `>` is a violation. Exclude self-closing/void cases as needed and verify against the real base.html nonce form first. (2) `UNSAFE_DIRECTIVES = re.compile(r"'unsafe-eval'|'unsafe-inline'")` — these must not appear in any template (the documented trade-off lives in docs/decisions/, not in templates). Use the same parametrize-over-rglob-with-exists-guard pattern as the analog so an empty template tree yields zero collected cases rather than an error. Assertion message must name the file and the matched fragment, matching the analog's format. Add a module docstring referencing D-07a / SEC-02 and stating the "fix the source, never loosen the test" rule. The grep must PASS clean against the current templates (the app uses Alpine CSP build + nonce'd scripts per docs/decisions/0001) — if it finds a real missing nonce, STOP and report it as a finding for a source fix; do not adjust the regex to pass.
  </action>
  <verify>
    <automated>python -m pytest tests/ci/test_csp_nonce.py -rs --tb=short -p no:cacheprovider 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/ci/test_csp_nonce.py exists and is parametrized over `app/templates/**/*.html`
    - It asserts no `<script>`/`<style>` tag lacks a `nonce` attribute (after comment-stripping)
    - It asserts no `'unsafe-eval'` / `'unsafe-inline'` literal in any template
    - `pytest tests/ci/test_csp_nonce.py -rs` exits 0 (passes clean) against the current templates
    - The test requires no Postgres (runs in the fast `tests/ci/` subset)
  </acceptance_criteria>
  <done>`pytest tests/ci/test_csp_nonce.py -rs` is green; every script/style tag has a nonce; no unsafe-* directive in templates. If a violation surfaced, it was fixed in the template source (reported in the SUMMARY), not by relaxing the regex.</done>
</task>

<task type="auto">
  <name>Task 2: model_dump-on-ApiCredential grep (D-07b / SEC-6)</name>
  <files>tests/ci/test_no_credential_dump.py</files>
  <read_first>
    - tests/ci/test_no_unsafe_jinja.py (structure to clone — but scan Python, not HTML)
    - app/models/api_credential.py (confirm the class name `ApiCredential` and the last_four/SEC-6 rationale)
    - app/services/credentials.py (confirm it uses a frozen dataclass `ProviderCredential`, NOT a Pydantic model, and contains no `model_dump`)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the test_no_credential_dump.py pattern, lines 300-341)
  </read_first>
  <action>
    Create tests/ci/test_no_credential_dump.py. Set `APP_DIR = Path("app")` and parametrize over `[p for p in APP_DIR.rglob("*.py") if p.exists()]` with the same exists-guard idiom. Define `MODEL_DUMP_PATTERN = re.compile(r"\bmodel_dump\s*\(")`. In each parametrized test: read the source; EARLY-RETURN (skip the file) if neither `"ApiCredential"` nor `"api_credential"` appears (case-insensitive for the snake form) — this avoids false-positives on unrelated files that legitimately call model_dump elsewhere. If the file references credentials AND contains a `model_dump(` call, FAIL with a message naming the file and the 1-based line number (`src[:match.start()].count(chr(10)) + 1`). No `_strip_comments` needed (Python comments do not embed the forbidden token in a way that matters here). Module docstring references SEC-6 / D-07b and the "decrypted key must never enter a serializable dict" invariant. The grep must PASS clean — if it finds a real `model_dump` on a credential-referencing file, STOP and report for a source fix (move the dump off the credential path), do not loosen the guard.
  </action>
  <verify>
    <automated>python -m pytest tests/ci/test_no_credential_dump.py -rs --tb=short -p no:cacheprovider 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/ci/test_no_credential_dump.py exists and is parametrized over `app/**/*.py`
    - It early-returns for files that do not reference ApiCredential / api_credential
    - It fails (with file + line number) if `model_dump(` appears in a credential-referencing file
    - `pytest tests/ci/test_no_credential_dump.py -rs` exits 0 (passes clean) against current app code
    - The test requires no Postgres
  </acceptance_criteria>
  <done>`pytest tests/ci/test_no_credential_dump.py -rs` is green; no model_dump on any ApiCredential-referencing file. Any real finding was fixed in source (reported in SUMMARY), not by softening the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template render → browser (CSP) | an un-nonce'd inline `<script>`/`<style>` would execute under a relaxed CSP, opening an XSS/tampering vector |
| credential service → logs/JSON serialization | a `model_dump()` on a credential-bearing object could serialize the decrypted API key into a log line or response body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering / XSS | app/templates inline scripts/styles | mitigate | D-07a grep fails the build on any `<script>`/`<style>` without a nonce or any `unsafe-*` literal in a template; runs on every pytest + in CI |
| T-12-04 | Information Disclosure | app code referencing ApiCredential | mitigate | D-07b grep fails the build if `model_dump()` is called in a file that references ApiCredential, keeping the decrypted key out of any serializable dict |
</threat_model>

<verification>
- `pytest tests/ci/ -rs` (the full fast grep subset, now including these two + the existing |safe test) is green.
- Both new tests collect at least one parametrized case (templates and app/*.py exist).
</verification>

<success_criteria>
- D-07a and D-07b grep tests exist under `tests/ci/`, clone the established idiom, and pass clean.
- No Postgres dependency; both run in the fast subset and will gate in CI (Plan 07).
- Any violation discovered was fixed in source, not by loosening a test.
</success_criteria>

<output>
After completion, create `.planning/phases/12-hardening-tests/12-02-SUMMARY.md`
</output>
