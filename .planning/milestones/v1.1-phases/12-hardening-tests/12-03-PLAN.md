---
phase: 12-hardening-tests
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - tests/test_happy_path_smoke.py
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "A single test drives setup → create coffee → create equipment → create recipe → log a brew session → GET / returns 200"
    - "The home page response after the chain renders the expected analytics/cold-start sections"
    - "The smoke hard-requires Postgres under SNOB_CI (it FAILS, not skips, if the DB is unreachable in CI mode)"
    - "Every state-changing POST in the chain uses the CSRF triple-send idiom and carries the session forward"
  artifacts:
    - path: "tests/test_happy_path_smoke.py"
      provides: "TEST-01 end-to-end acceptance-criteria smoke"
      min_lines: 60
      contains: "test_happy_path"
  key_links:
    - from: "tests/test_happy_path_smoke.py"
      to: "/setup, /roasters, /coffees, /equipment, /recipes, /brew, /"
      via: "sequential authenticated POST/GET with CSRF + session cookies"
      pattern: "client\\.post"
    - from: "tests/test_happy_path_smoke.py"
      to: "SNOB_CI skip-to-fail gate"
      via: "_require_wired / _require_postgres hard-require"
      pattern: "SNOB_CI"
---

<objective>
Build the net-new TEST-01 happy-path smoke (`tests/test_happy_path_smoke.py`) covering the acceptance-criteria chain end to end: setup → create coffee → create equipment → create recipe → log a brew session → GET / renders all sections (including the AI cold-start state where applicable). It extends the working `test_phase02_smoke.py` setup+CSRF+session idiom and is a HARD test (requires Postgres per D-02), not a skip-on-missing-DB test.

Purpose: Criterion #1 of the phase goal. The existing `test_phase02_smoke.py` only covers setup → login → home; the full catalog→brew→home chain does not exist as one smoke.
Output: `tests/test_happy_path_smoke.py`.

Depends on Plan 12-01: the catalog teardown must land first so the smoke's created entities do not collide with phase_04 fixtures, and the SNOB_CI gate must exist so this smoke can be a hard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-hardening-tests/12-CONTEXT.md
@.planning/phases/12-hardening-tests/12-PATTERNS.md
@.planning/phases/12-hardening-tests/12-RESEARCH.md

<interfaces>
<!-- Verified route prefixes (catalog routers are TOP-LEVEL, not under /catalog). -->

Catalog + brew create routes (all POST, CSRF-protected, require auth session):
  - POST /roasters          (app/routers/roasters.py:203)   — create a roaster
  - POST /coffees           (app/routers/coffees.py:353)     — create a coffee
  - POST /equipment         (app/routers/equipment.py:178)   — create an equipment item (type enum: brewer/grinder/kettle/scale/water_filter/other)
  - POST /recipes           (app/routers/recipes.py:239)     — create a recipe (JSONB steps)
  - POST /brew              (app/routers/brew.py:771)         — create a brew session (success → 204 + HX-Redirect to /brew)
  - GET  /                  (app/routers/home.py)            — home page (analytics shell + lazy fragments + cold-start gate)

CSRF triple-send idiom (from test_phase02_smoke.py — apply to EVERY POST):
  client.post(url,
      data={"X-CSRF-Token": token, ...form_fields...},
      headers={"X-CSRF-Token": token},
      cookies={"csrftoken": token, "session_id": session_signed},
      follow_redirects=False)
  Refresh `token` from each response's csrftoken cookie in case rotation kicked in.

Setup bootstrap (test_phase02_smoke.py lines 57-96):
  GET /setup → capture csrftoken cookie → POST /setup (303) → extract session_id from set-cookie
  → carry session_id + csrftoken into all subsequent requests.

conftest fixtures available: `client` (sync TestClient), `_require_postgres` (Plan 12-01 helper), `_CI_MODE`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEST-01 end-to-end happy-path smoke</name>
  <files>tests/test_happy_path_smoke.py</files>
  <read_first>
    - tests/test_phase02_smoke.py (the ENTIRE file — the setup → CSRF → session → authenticated-GET pattern and the `_require_phase02_wired` guard you are extending)
    - tests/conftest.py (the `client` fixture, the new `_require_postgres`/`_CI_MODE` helper from Plan 12-01, the `fresh_db` autouse reset)
    - app/routers/roasters.py POST handler (lines ~203-296 — the exact form field names the create endpoint expects)
    - app/routers/coffees.py POST handler (lines ~353-426 — required fields incl. roaster FK, country, process; advertised_flavor_note_ids handling)
    - app/routers/equipment.py POST handler (lines ~178-236 — the type enum + brand/model fields)
    - app/routers/recipes.py POST handler (lines ~239-323 — the steps JSON shape + dose/water/temp/grind fields)
    - app/routers/brew.py POST handler (lines ~771-820 — required brew fields: coffee_id, dose/water actual, temp, grind, brewed_at; success is 204 + HX-Redirect, not 200)
    - app/routers/home.py (what sections/fragments the GET / response shell contains so the assertion targets real markers)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the "Extended smoke pattern" + CSRF triple-send idiom, lines 110-111 and 604-616)
  </read_first>
  <action>
    Create tests/test_happy_path_smoke.py with one end-to-end test (e.g. `test_happy_path_full_chain`). Add a `_require_wired(label)` guard at the top that imports the brew + catalog routers and, when `_CI_MODE` (SNOB_CI=1), calls `pytest.fail(...)` on ImportError instead of `pytest.skip` — mirror the Plan 12-01 SNOB_CI pattern so this smoke is HARD in CI. Use the `client` fixture. Step 1: bootstrap via /setup exactly as test_phase02_smoke.py does (GET /setup → csrftoken → POST /setup → 303 → extract session_id). Step 2: with the session+csrftoken carried forward and the triple-send idiom, POST /roasters to create a roaster; capture its id from the response (parse the redirect/fragment or re-GET the list). Step 3: POST /coffees referencing the roaster (supply the real required fields read from the handler). Step 4: POST /equipment for at least one brewer (type=brewer) and ideally a grinder + kettle so the brew form FKs resolve. Step 5: POST /recipes with a minimal valid steps array. Step 6: POST /brew with the created coffee_id + equipment ids + a dose/water/temp/grind + brewed_at; assert the create succeeds (expect 204 + an HX-Redirect header to /brew, NOT 200 — confirm against the handler). Step 7: GET / with the session cookie; assert 200 AND that the home shell renders the expected section markers (read home.py / the home template to choose stable assertion strings — e.g. the recent-brews region, the analytics card containers, or the AI cold-start meter copy). Refresh the CSRF token from each response before the next POST. Keep field values minimal-but-valid (respect Pydantic numeric ranges: rating 0-5 step 0.25, temp 0-100). Do NOT assert on AI prose (provider not configured in test) — assert the cold-start / "AI not configured" state renders gracefully. This is one cohesive smoke; if a route's contract differs from the interface notes, follow the actual handler and note the deviation in the SUMMARY.
  </action>
  <verify>
    <automated>SNOB_CI=1 python -m pytest tests/test_happy_path_smoke.py -rs -x --tb=short -p no:cacheprovider 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `SNOB_CI=1 pytest tests/test_happy_path_smoke.py -rs -x` exits 0
    - The test creates a roaster, coffee, equipment, recipe, and brew session via authenticated CSRF-protected POSTs, then GETs / and asserts 200
    - The home-page assertion checks at least one real section marker from the home template (recent brews / analytics card / cold-start meter), not just status 200
    - The brew-create assertion matches the real handler contract (204 + HX-Redirect, not a naive 200)
    - Under SNOB_CI=1 the test FAILS (does not skip) if the brew/catalog routers are unimportable or Postgres is unreachable
    - Running it twice consecutively stays green (relies on the Plan 12-01 catalog teardown)
  </acceptance_criteria>
  <done>The happy-path smoke drives the full setup→coffee→equipment→recipe→session→home chain green under SNOB_CI=1, asserts real home sections render, and is a hard (non-skipping) test in CI mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke verdict → ship decision | the happy-path smoke is the single end-to-end "the core flow works" signal a human relies on before deploy; a smoke that silently skips would cross this boundary as a false-positive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Repudiation | TEST-01 smoke (skip-as-green) | mitigate | The smoke's `_require_wired` + `_require_postgres` hard-fail under SNOB_CI=1 (Plan 12-01), so a missing router or unreachable DB fails the gate instead of silently passing |
| T-12-06 | Tampering | CSRF on state-changing POSTs | accept | The smoke exercises (does not bypass) the production CSRF triple-send path; CSRF enforcement itself is verified by TEST-05 (Plan 04). No new endpoint is added here. |
</threat_model>

<verification>
- `SNOB_CI=1 pytest tests/test_happy_path_smoke.py -rs -x` is green.
- The full suite (`SNOB_CI=1 pytest tests/ --ignore=tests/e2e -rs`) remains green with the smoke added (no cross-module pollution, thanks to Plan 12-01).
</verification>

<success_criteria>
- TEST-01 satisfied: one smoke covers the acceptance-criteria happy path end to end.
- Hard test under SNOB_CI; uses the production CSRF + session path; asserts real home sections.
</success_criteria>

<output>
After completion, create `.planning/phases/12-hardening-tests/12-03-SUMMARY.md`
</output>
