---
phase: 12-hardening-tests
plan: 06
type: execute
wave: 4
depends_on: [12-05]
files_modified:
  - tests/e2e/__init__.py
  - tests/e2e/conftest.py
  - tests/e2e/test_responsive_smoke.py
autonomous: false
requirements: [TEST-06]
user_setup: []
must_haves:
  truths:
    - "A Playwright chromium smoke runs at BOTH 375x667 and 390x844 viewports"
    - "It asserts the bottom nav is present and visible at mobile widths"
    - "It asserts the brew session form renders with no horizontal scroll"
    - "It asserts the photo upload control is present on the relevant page"
    - "It asserts the home analytics cards stack vertically and remain readable"
    - "It asserts computed font-size >= 16px on every input/select/textarea (no iOS focus zoom, MX-1)"
    - "The suite authenticates first (programmatic setup/login) before asserting layout"
    - "tests/e2e is excluded from the default/CI pytest run and invoked as a separate local/pre-deploy step (D-06)"
  artifacts:
    - path: "tests/e2e/conftest.py"
      provides: "session browser fixture + parametrized viewport page + auth-seeded base_url"
      contains: "page_at_viewport"
    - path: "tests/e2e/test_responsive_smoke.py"
      provides: "TEST-06 responsive assertions"
      contains: "font-size"
  key_links:
    - from: "tests/e2e/test_responsive_smoke.py"
      to: "running app (compose test stack)"
      via: "page.goto(base_url + route)"
      pattern: "page.*goto"
    - from: "tests/e2e/conftest.py page_at_viewport"
      to: "375x667 + 390x844"
      via: "pytest params"
      pattern: "375|390"
---

<objective>
Build the net-new Playwright responsive smoke (D-05/TEST-06): a chromium-headless suite under `tests/e2e/` that runs at 375x667 and 390x844 against the running app and asserts the criterion-#3 set — bottom nav present+functional, brew form usable with no horizontal scroll, photo upload control present, home analytics cards stack vertically, and computed font-size >= 16px on every input/select/textarea (MX-1, no iOS focus zoom). This is a LOCAL / pre-deploy ship-smoke only — NOT in GitHub Actions (D-06).

Purpose: Criterion #3 of the phase goal. Real layout + computed-style truth that static checks cannot provide.
Output: `tests/e2e/__init__.py`, `tests/e2e/conftest.py`, `tests/e2e/test_responsive_smoke.py`.

This plan has a human-verify checkpoint because the Playwright smoke must be run once against the live compose stack to confirm it passes before deploy (D-06 designates it the pre-deploy ship step). Depends on Plan 12-05 (the dev image with chromium baked + the compose `test` profile).

Verify the Playwright Python sync API (computed-style via evaluate, bounding_box, parametrized viewport context) via Context7/ctx7 before writing the fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-hardening-tests/12-CONTEXT.md
@.planning/phases/12-hardening-tests/12-RESEARCH.md
@.planning/phases/12-hardening-tests/12-PATTERNS.md

<interfaces>
<!-- Verified selectors + routes for the responsive assertions. -->

Bottom nav (app/templates/base.html line 236): `<nav x-data="navBar" class="fixed bottom-0 left-0 right-0 z-40 h-16 ... md:hidden">`
  → selectable as `nav.fixed.bottom-0` or `nav[x-data="navBar"]`; it is `md:hidden` so it is VISIBLE at 375/390 and HIDDEN >=768. No data-testid exists — CONFIRM the selector by re-reading base.html before hardcoding.

Routes to assert against (require an authenticated session):
  - `/`         → home shell with lazy analytics cards + cold-start meter
  - `/brew/new` → brew session form (the 56x56px star control, 16px-baseline inputs)
  - `/coffees/{id}` (coffee detail) or the bag form → the photo upload `<input capture="environment">` control (read app/templates to find the exact page that mounts it)

Auth seeding: mirror test_phase02_smoke.py — POST /setup then carry the session_id cookie into the Playwright browser context (set via context.add_cookies or page.context). The app has NO public registration; /setup only works at zero users (the compose test DB is fresh per the conftest provisioning).

base_url: the compose `test` service reaches the web app at http://coffee-snobbery:8000 on the bridge network; a local host run reaches it at http://127.0.0.1:8080. Make base_url a fixture/env-driven value (default to the host-bind 127.0.0.1:8080, override via an env var for the in-network container run).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright e2e fixtures (session browser + parametrized viewport + auth + base_url)</name>
  <files>tests/e2e/__init__.py, tests/e2e/conftest.py</files>
  <read_first>
    - tests/conftest.py (the session-scoped fixture style + the setup/CSRF bootstrap to mirror for auth seeding)
    - tests/test_phase02_smoke.py (the /setup → session_id extraction flow to replicate for Playwright auth)
    - app/templates/base.html (CONFIRM the bottom-nav selector before the e2e test in Task 2 relies on it)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the e2e conftest pattern, lines 345-379)
    - .planning/phases/12-hardening-tests/12-RESEARCH.md (Pattern 5 fixtures + auth-seeding note, lines 368-447; Pitfall 4 about computed-style on un-rendered elements)
    - Context7/ctx7: query "Playwright Python sync_api new_context viewport add_cookies evaluate getComputedStyle" to confirm the 1.59 API surface BEFORE writing fixtures
  </read_first>
  <action>
    Create tests/e2e/__init__.py (empty package marker). Create tests/e2e/conftest.py with: (1) a `base_url` fixture reading an env var (e.g. `SNOB_E2E_BASE_URL`) defaulting to `http://127.0.0.1:8080`; (2) a session-scoped `browser` fixture using `sync_playwright().chromium.launch(headless=True)` (yield, then close); (3) a function-scoped `page_at_viewport` fixture parametrized `params=[(375, 667), (390, 844)], ids=["375x667", "390x844"]` that creates `browser.new_context(viewport={"width": w, "height": h})`, opens a page, yields it, then closes the context — accept `request` for `request.param`; (4) an auth-seeding helper/fixture that, against `base_url`, performs the /setup bootstrap (via `page.request` or a small httpx call) to create the first user and obtain the `session_id` cookie, then injects that cookie into the Playwright context with `context.add_cookies([...])` so navigations are authenticated. Guard the whole module so that if Playwright is not installed it skips cleanly when run on a host without the dev image (these tests are local-only; do NOT make them fail under SNOB_CI since CI excludes tests/e2e per D-06). Keep auth seeding idempotent against an already-set-up DB (if /setup returns the post-setup redirect, fall back to /login).
  </action>
  <verify>
    <automated>python -m pytest tests/e2e/ --collect-only -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/__init__.py exists (package marker)
    - tests/e2e/conftest.py defines `browser` (session), `page_at_viewport` (params 375x667 + 390x844), `base_url` (env-overridable), and an auth-seeding mechanism that injects the session cookie into the context
    - `pytest tests/e2e/ --collect-only` succeeds (or skips cleanly if Playwright absent on the host) — it does NOT error at collection
    - The fixtures use the Playwright sync API confirmed via Context7
  </acceptance_criteria>
  <done>e2e fixtures collect cleanly; two viewport params present; auth seeding injects a session cookie; base_url is env-overridable for the in-container vs host run.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive smoke assertions (TEST-06)</name>
  <files>tests/e2e/test_responsive_smoke.py</files>
  <read_first>
    - tests/e2e/conftest.py (the fixtures from Task 1 — base_url, page_at_viewport, auth)
    - app/templates/base.html (CONFIRM the bottom-nav selector `nav.fixed.bottom-0` / `nav[x-data="navBar"]` and that it is md:hidden)
    - app/templates for the brew form (/brew/new) and the page that mounts the photo upload `<input capture>` control — confirm the exact route + a stable selector
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the computed-style + no-scroll patterns, lines 387-415)
    - .planning/phases/12-hardening-tests/12-RESEARCH.md (Pattern 5 assertions, lines 394-445; Pitfall 4)
  </read_first>
  <action>
    Create tests/e2e/test_responsive_smoke.py with tests that consume `page_at_viewport` (so each runs at both 375x667 and 390x844) and the auth-seeded context. (1) `test_input_font_size_no_ios_zoom`: goto base_url + "/brew/new"; `page.wait_for_selector("input, select, textarea")` first (Pitfall 4 — computed style is empty for un-rendered elements); then `page.evaluate` a JS snippet that walks all input/select/textarea, parses `getComputedStyle(el).fontSize`, and collects any element with font-size < 16; assert the violations list is empty (message lists the offenders). (2) `test_brew_form_no_horizontal_scroll`: goto /brew/new; assert `document.documentElement.scrollWidth <= document.documentElement.clientWidth`. (3) `test_bottom_nav_present`: goto "/"; locate the bottom nav by the confirmed selector; assert it is visible and its bounding_box y is in the lower ~30% of the viewport height. (4) `test_photo_upload_control_present`: goto the page that mounts the photo upload input (confirmed in read_first); assert the `input[type=file]` / `input[capture]` control is present. (5) `test_home_cards_stack_vertically`: goto "/"; assert the analytics card containers stack (e.g. each card's bounding_box width ~= the viewport content width, or they share an x and differ in y — pick the assertion that matches the real markup) and that the page has no horizontal scroll. Use the auth-seeded context for all navigations. Keep selectors resilient (prefer roles/aria-labels/structural classes confirmed from the templates over brittle deep CSS). Mark/keep these local-only (excluded from CI via the Plan 07 `--ignore=tests/e2e`).
  </action>
  <verify>
    <automated>python -m pytest tests/e2e/ --collect-only -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/test_responsive_smoke.py defines tests for: input font-size >=16px, brew-form no horizontal scroll, bottom nav present+positioned, photo upload control present, home cards stack vertically
    - Every test consumes `page_at_viewport` so it runs at BOTH 375x667 and 390x844 (collect-only shows the `[375x667]`/`[390x844]` parametrize ids)
    - The font-size test calls `wait_for_selector` before `getComputedStyle` (Pitfall 4)
    - Navigations use the auth-seeded session
    - `pytest tests/e2e/ --collect-only` enumerates the tests x 2 viewports without error
  </acceptance_criteria>
  <done>The responsive smoke defines all five criterion-#3 assertions, parametrized across both mobile viewports, against the authenticated app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Playwright responsive smoke suite (tests/e2e/) running against the live compose stack via the Plan 12-05 dev image + test profile. D-06 designates this the required pre-deploy ship step (it is intentionally NOT in CI).</what-built>
  <how-to-verify>
    1. Bring up the stack: `docker compose up -d` (web + db healthy).
    2. Run the e2e smoke through the test profile against the in-network app:
       `docker compose --profile test run --rm -e SNOB_E2E_BASE_URL=http://coffee-snobbery:8000 coffee-snobbery-test tests/e2e/ -rs --tb=short`
       (or locally against the host bind: `python -m pytest tests/e2e/ -rs` with the stack on 127.0.0.1:8080 and Playwright installed).
    3. Expected: all responsive tests PASS at BOTH 375x667 and 390x844 — bottom nav visible, no horizontal scroll on /brew/new, photo upload control present, home cards stacked, and ZERO font-size<16px violations.
    4. If a font-size violation appears, that is a real MX-1 regression — fix the offending input's CSS (the 16px baseline lives in app/static/css/tailwind.src.css @layer base), do NOT relax the assertion.
    Note: the service worker stale-cache can confound UI checks (memory sw-stale-cache-confounds-ui-verify) — if results look stale, "Clear site data" / use a fresh incognito context (Playwright's fresh context per test sidesteps this).
  </how-to-verify>
  <resume-signal>Type "approved" once the e2e smoke passes at both viewports, or describe the failing assertion(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile browser rendering → user (iOS focus zoom) | an input with computed font-size < 16px triggers iOS Safari auto-zoom, degrading the mobile-first UX guarantee; the e2e font-size assertion guards this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | (UX regression, non-security) | mobile input font-size | mitigate | The Playwright computed-style assertion fails the pre-deploy smoke on any input below 16px; the fix is in the source CSS baseline, never the test |
| T-12-14 | Repudiation | e2e smoke skipped before deploy | accept | D-06 makes this a manual pre-deploy step (not CI) by design; the human-verify checkpoint + README runbook (Plan 07/D-08) make skipping it a conscious choice, not a silent one |
</threat_model>

<verification>
- `pytest tests/e2e/ --collect-only` enumerates all responsive tests across both viewports without collection error.
- The human-verify checkpoint confirms a live pass at 375x667 and 390x844 with zero font-size violations.
</verification>

<success_criteria>
- TEST-06 satisfied: Playwright responsive smoke at 375x667 + 390x844 asserting nav, no-scroll, photo control, card stacking, and font-size>=16px.
- Excluded from CI (D-06); runnable as the pre-deploy ship step via the compose test profile.
- Live pass confirmed at the human-verify checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/12-hardening-tests/12-06-SUMMARY.md`
</output>
