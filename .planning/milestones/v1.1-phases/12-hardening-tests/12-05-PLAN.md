---
phase: 12-hardening-tests
plan: 05
type: execute
wave: 3
depends_on: [12-01]
files_modified:
  - Dockerfile
  - docker-compose.yml
  - requirements-dev.txt
autonomous: true
requirements: [TEST-06]
user_setup: []
must_haves:
  truths:
    - "A `dev` Dockerfile stage extends `runtime` and installs requirements-dev.txt plus the Playwright chromium browser"
    - "The production runtime stage still installs NO pytest/test tooling"
    - "`docker compose run --rm coffee-snobbery-test` runs the full gate (pytest -rs, ignoring tests/e2e) in one command"
    - "The test compose service sets SNOB_CI=1 and never starts under a plain `docker compose up -d` (gated behind a profile)"
    - "requirements-dev.txt pins playwright>=1.59,<2"
  artifacts:
    - path: "Dockerfile"
      provides: "dev/test multi-stage target (FROM runtime AS dev)"
      contains: "AS dev"
    - path: "docker-compose.yml"
      provides: "coffee-snobbery-test service behind profiles: [test]"
      contains: "profiles"
    - path: "requirements-dev.txt"
      provides: "playwright pin"
      contains: "playwright"
  key_links:
    - from: "docker-compose.yml coffee-snobbery-test"
      to: "Dockerfile target: dev"
      via: "build.target"
      pattern: "target:\\s*dev"
    - from: "Dockerfile dev stage"
      to: "compiled Tailwind CSS"
      via: "inherited COPY --from=tailwind-builder in runtime"
      pattern: "FROM runtime AS dev"
---

<objective>
Stand up the reproducible test runtime (D-03): a `dev`/`test` multi-stage Dockerfile target that extends the existing `runtime` stage with `requirements-dev.txt` + the Playwright chromium browser, a `docker-compose.yml` `test` profile so `docker compose run --rm coffee-snobbery-test` runs the whole gate in one command, and the `playwright>=1.59,<2` pin in `requirements-dev.txt`. The prod runtime image stays pytest-free.

Purpose: Retires the brittle manual `docker compose exec ... pip install --user pytest ...` + `docker compose cp` flow documented in CLAUDE.md, and provides the image both the local gate and the GitHub Actions CI (Plan 07) share. Prerequisite for the Playwright e2e suite (Plan 06) which needs the baked browser.
Output: modified `Dockerfile`, `docker-compose.yml`, `requirements-dev.txt`.

Verify the exact Playwright-in-Docker install invocation via Context7/ctx7 before writing the Dockerfile stage (the `--with-deps` flag and browser-binary step are version-sensitive).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-hardening-tests/12-CONTEXT.md
@.planning/phases/12-hardening-tests/12-RESEARCH.md
@.planning/phases/12-hardening-tests/12-PATTERNS.md

<interfaces>
<!-- Verified Dockerfile + compose structure to extend. -->

Dockerfile stages (verified):
  - Stage 1: `FROM debian:bookworm-slim AS tailwind-builder` (compiles tailwind.<hash>.css)
  - Stage 2: `FROM python:3.12-slim AS runtime` — installs requirements.txt, COPY --chown=app:app . ., COPY --from=tailwind-builder the compiled CSS, creates user `app` (uid 1000), ENTRYPOINT ["./entrypoint.sh"]
  - The runtime stage already has the compiled Tailwind CSS (so a dev stage FROM runtime inherits it — avoids the conftest "Tailwind CSS missing" skip, Pitfall 2)

docker-compose.yml (verified):
  - name: coffee-snobbery
  - services: coffee-snobbery-db (postgres:16-alpine, healthcheck), coffee-snobbery (build context ., port 127.0.0.1:8080:8000)
  - DATABASE_URL computed inline: postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@coffee-snobbery-db:5432/${POSTGRES_DB}
  - networks: coffee-snobbery-net ; volumes: postgres_data/photos/backups (named)

requirements-dev.txt (verified current):
  -r requirements.txt ; ruff>=0.15.13,<0.16 ; mypy>=1.13,<2 ; pytest>=9.0,<10 ; pytest-asyncio ; pytest-cov ; respx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add playwright pin + Dockerfile dev/test stage (D-03)</name>
  <files>requirements-dev.txt, Dockerfile</files>
  <read_first>
    - requirements-dev.txt (the whole file — you append one pin)
    - Dockerfile (the WHOLE file — study the runtime stage's COPY --from=tailwind-builder line and the `USER app` boundary; the dev stage extends runtime)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the Dockerfile dev stage pattern, lines 419-461; Pitfall 2 about inheriting compiled Tailwind)
    - .planning/phases/12-hardening-tests/12-RESEARCH.md (Pattern 3 + the playwright install note, lines 102 and 260-280; Assumption A1)
    - Context7/ctx7: query "Playwright Python install chromium in Docker --with-deps" to confirm the exact 1.59 install invocation BEFORE writing the RUN lines
  </read_first>
  <action>
    Append `playwright>=1.59,<2` to requirements-dev.txt (one line; keep the existing comment about httpx). In the Dockerfile, add a NEW stage AFTER the runtime stage: `FROM runtime AS dev`. In it: switch to `USER root`; `COPY requirements-dev.txt ./` then `RUN pip install -r requirements-dev.txt` (this installs pytest, ruff, mypy, respx, playwright); then install the browser binary + OS deps with the exact invocation confirmed via Context7 (expected: `RUN playwright install chromium --with-deps` — the `--with-deps` flag pulls the libglib/libfontconfig/etc. that bookworm-slim lacks; confirm flag name/spelling against the docs). Switch back to `USER app`. Override the entrypoint for test runs: `ENTRYPOINT ["python", "-m", "pytest"]` and `CMD ["tests/", "-rs", "--tb=short", "--ignore=tests/e2e"]` so `docker compose run --rm coffee-snobbery-test` runs the full non-e2e gate by default and an explicit `... coffee-snobbery-test tests/e2e/ -rs` runs Playwright. The dev stage MUST inherit the compiled Tailwind CSS — because it is FROM runtime (which already did `COPY --from=tailwind-builder`), this is automatic; do NOT re-copy or rebuild Tailwind, just confirm the inheritance so the conftest `app` fixture does not skip with "Tailwind CSS missing" (Pitfall 2). Do NOT touch the runtime stage — the prod image must stay pytest-free (CLAUDE.md invariant). Build the dev stage to prove it succeeds.
  </action>
  <verify>
    <automated>docker build --target dev -t coffee-snobbery:dev . 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - requirements-dev.txt contains `playwright>=1.59,<2`
    - Dockerfile contains a `FROM runtime AS dev` stage that installs requirements-dev.txt and the Playwright chromium browser
    - The Dockerfile `runtime` (prod) stage still does NOT install pytest/ruff/playwright (grep `runtime` block: no `requirements-dev` install)
    - The dev stage ENTRYPOINT/CMD default to `python -m pytest tests/ -rs --tb=short --ignore=tests/e2e`
    - `docker build --target dev` completes successfully (chromium + deps install without error)
  </acceptance_criteria>
  <done>`docker build --target dev` succeeds; dev stage carries pytest+playwright+chromium and inherits the compiled Tailwind CSS; prod runtime stage remains test-free.</done>
</task>

<task type="auto">
  <name>Task 2: Add the docker-compose test profile (D-03)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (the WHOLE file — mirror the coffee-snobbery service block's env_file/networks/depends_on; note the db healthcheck gate)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the test service block, lines 494-516; the "key differences from prod service" notes)
    - .planning/phases/12-hardening-tests/12-RESEARCH.md (Pattern 3 compose profile, lines 282-304)
  </read_first>
  <action>
    Add a `coffee-snobbery-test` service under `services:` in docker-compose.yml. It: builds from `context: .`, `dockerfile: Dockerfile`, `target: dev`; sets `profiles: [test]` (so `docker compose up -d` with no profile NEVER starts it); `depends_on: coffee-snobbery-db: condition: service_healthy`; `env_file: [.env]`; environment: the same inline `DATABASE_URL: postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@coffee-snobbery-db:5432/${POSTGRES_DB}` as the prod service AND `SNOB_CI: "1"` (so the skip gate is live in the container gate); `networks: [coffee-snobbery-net]`; a source bind-mount `volumes: [- .:/app]` so a test run picks up local changes without a rebuild (the prod service has no code bind-mount). Do NOT give it a `container_name` (allows parallel `run` invocations), no `restart`, no `ports`, no photo/backup volumes. Leave the existing two services and all named volumes/network untouched. Note: conftest forces a `<db>_test` sibling DB and provisions+migrates it, so no separate test-DB service is needed — it shares coffee-snobbery-db. Validate the compose file parses.
  </action>
  <verify>
    <automated>docker compose --profile test config 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml defines `coffee-snobbery-test` with `build.target: dev` and `profiles: [test]`
    - The test service sets `SNOB_CI: "1"` and the inline DATABASE_URL pointing at coffee-snobbery-db
    - The test service depends_on coffee-snobbery-db with `condition: service_healthy`
    - The test service has a `- .:/app` bind-mount and NO ports/container_name/restart
    - `docker compose --profile test config` parses without error and shows coffee-snobbery-test
    - `docker compose config` (no profile) does NOT list coffee-snobbery-test
    - The two existing services, named volumes, and network are unchanged
  </acceptance_criteria>
  <done>`docker compose --profile test config` shows the test service with target dev + SNOB_CI=1; a no-profile config omits it; `docker compose run --rm coffee-snobbery-test` is the one-command gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod image build → deployed container | test tooling (pytest, browsers) leaking into the prod runtime stage would enlarge attack surface and image size |
| test container → database | the gate container must only ever touch the `<db>_test` sibling, never the live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Elevation / surface | prod runtime image | mitigate | Dev tooling lives only in the `dev` stage; the prod `runtime` stage is untouched and stays pytest/browser-free (CLAUDE.md invariant) |
| T-12-11 | Tampering | test container DB target | mitigate | The conftest `<db>_test` forcing + safety interlock (Plan 12-01) means even the SNOB_CI=1 container only mutates the test DB; the compose service shares coffee-snobbery-db but never the live `<db>` name |
| T-12-12 | Repudiation | container gate hollow-green | mitigate | The test service exports `SNOB_CI=1`, so the in-container gate fails on unexpected skips |
</threat_model>

<verification>
- `docker build --target dev -t coffee-snobbery:dev .` succeeds.
- `docker compose --profile test config` validates and lists coffee-snobbery-test; `docker compose config` (no profile) omits it.
- Optional end-to-end (if the stack is up): `docker compose --profile test run --rm coffee-snobbery-test` runs the non-e2e gate green.
</verification>

<success_criteria>
- D-03 closed: dev/test image + compose test profile + playwright pin; prod image stays test-free.
- One-command gate `docker compose run --rm coffee-snobbery-test` works; SNOB_CI=1 live in the container.
- Image/deps source the Plan 07 CI workflow will share.
</success_criteria>

<output>
After completion, create `.planning/phases/12-hardening-tests/12-05-SUMMARY.md`
</output>
