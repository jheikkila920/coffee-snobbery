---
phase: 12-hardening-tests
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - tests/services/test_ai_service.py
  - tests/services/test_encryption.py
  - tests/services/test_analytics.py
  - tests/middleware/test_csrf.py
  - tests/middleware/test_csrf_form_shim.py
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04, TEST-05]
must_haves:
  truths:
    - "ai_service tests cover signature computation, provider fallback under respx, citation-block projection, and manual-refresh throttle"
    - "encryption tests cover encrypt→decrypt round-trip AND MultiFernet key rotation"
    - "analytics tests cover top coffees, preference profile, sweet spots, and roast freshness against a seeded DB"
    - "CSRF tests cover both a positive (valid token passes) and a negative (missing/invalid token → 403) path"
    - "Any named coverage gap is closed by ADDING a focused test, never by rewriting an existing passing test"
  artifacts:
    - path: "tests/services/test_ai_service.py"
      provides: "verified TEST-02 coverage (signature + fallback + projection + throttle)"
    - path: "tests/services/test_encryption.py"
      provides: "verified TEST-03 coverage (round-trip + rotation)"
    - path: "tests/services/test_analytics.py"
      provides: "verified TEST-04-adjacent analytics coverage (this plan owns the analytics-test verify; analytics REQ is TEST-04, claimed here for verify-and-extend)"
    - path: "tests/middleware/test_csrf.py"
      provides: "verified TEST-05 positive+negative CSRF coverage"
  key_links:
    - from: "tests/services/test_ai_service.py"
      to: "respx-mocked provider HTTP + ai_service signature/fallback"
      via: "respx fixtures"
      pattern: "respx"
    - from: "tests/services/test_encryption.py"
      to: "MultiFernet rotation"
      via: "monkeypatched_app_encryption_key fixture"
      pattern: "rotation"
---

<objective>
VERIFY-AND-EXTEND the already-shipped unit tests for the load-bearing services and CSRF middleware (TEST-02, TEST-03, TEST-05, and the analytics portion of TEST-04). Phases 0-11 already shipped substantial coverage. This plan reads each existing test file, confirms it covers the criterion's named cases, and adds ONLY the missing cases. It does NOT rebuild existing coverage — re-creating passing tests is the single biggest waste-risk in this phase.

Purpose: Criterion #2 of the phase goal — prove the load-bearing service + CSRF tests actually cover the named behaviors, closing any gap with a surgical addition.
Output: Verified (and, only where a gap exists, minimally extended) test files. If a file already fully covers its criterion, the deliverable is a SUMMARY note documenting the verified mapping with NO code change to that file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-hardening-tests/12-CONTEXT.md
@.planning/phases/12-hardening-tests/12-RESEARCH.md

<interfaces>
<!-- Existing coverage inventory (verified by grep). VERIFY these map to the criteria; ADD only gaps. -->

tests/services/test_ai_service.py (44 tests) — already includes:
  signature: test_advisory_key_stable, test_sig_skip, test_force_regenerates, test_is_stale_true_when_sig_changed
  fallback: test_fallback_predicate_non_retryable, test_provider_fallback_anthropic_to_openai, test_three_tier_fallback
  projection: test_citation_projector, test_projector_no_match_raises
  throttle: test_throttle_eviction
  validation: test_pydantic_validation_error_try_again, test_all_top_level_schemas_have_summary_prose

tests/services/test_encryption.py (6 tests) — already includes:
  round-trip: test_encrypt_decrypt_roundtrip
  rotation: test_rotation_decrypts_old_token, test_unknown_key_raises_invalid_token
  startup: test_startup_check_fails_loudly_on_empty_key, test_startup_check_emits_ok_event, test_fingerprint_stable_and_hex

tests/services/test_analytics.py (13 tests) — already includes:
  test_top_coffees, test_preference_profile, test_flavor_descriptors, test_roast_freshness_buckets,
  test_sweet_spots, test_recent_brews, test_unrated_coffees, test_cold_start_counts, test_signature_* (4)
  (test_analytics_perf.py covers the 1000-session <50ms perf check — owned by TEST-04, verified in Plan note only)

tests/middleware/test_csrf.py (4) — test_missing_token (negative), test_valid_token (positive), test_no_rotation, test_csp_report_exempt
tests/middleware/test_csrf_form_shim.py (5) — test_get_passthrough, test_header_passthrough, test_form_field_hoisted, test_multipart_body_preserved, test_json_passthrough

Reusable fixtures: monkeypatched_app_encryption_key (Fernet reload), fernet_key/fernet_key_str, sync_db, respx (dev dep), the analytics seeding fixture `clean_analytics`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify-and-extend ai_service + encryption coverage (TEST-02, TEST-03)</name>
  <files>tests/services/test_ai_service.py, tests/services/test_encryption.py</files>
  <read_first>
    - tests/services/test_ai_service.py (the ENTIRE file — map each existing test to the TEST-02 named cases: signature computation, provider fallback under respx, citation-block projection, manual-refresh throttle)
    - tests/services/test_encryption.py (the ENTIRE file — map to TEST-03: encrypt→decrypt round-trip AND MultiFernet key rotation)
    - app/services/ai_service.py (the signature, fallback predicate, projector, and throttle surfaces, to confirm the tests exercise the real public functions and to spot any named behavior with no test)
    - app/services/encryption.py (MultiFernet construction + rotation semantics)
    - tests/conftest.py (monkeypatched_app_encryption_key, fernet_key fixtures)
  </read_first>
  <action>
    For test_ai_service.py: build a written mapping (TEST-02 named case → existing test name) and confirm all four named areas (signature computation, provider fallback under respx, citation-block projection, manual-refresh throttle) are covered. The inventory above shows all four ARE present — so the expected outcome is a verification with NO new test unless you find a genuine hole (e.g. the throttle only tests eviction but not the 429/within-window rejection, or fallback only tests the predicate but not a respx-driven Anthropic→OpenAI HTTP swap). If a real gap exists, add ONE focused test using the existing respx + fixture patterns in the file; do not restructure or rewrite existing tests. For test_encryption.py: confirm both the round-trip (test_encrypt_decrypt_roundtrip) and the rotation (test_rotation_decrypts_old_token: encrypt under primary, rotate so old primary becomes secondary, still decrypts) are present and correct — they are, so expect a verify-only outcome. Document the mapping in the SUMMARY. Touch the file ONLY to add a missing case. Run the files with `-rs` to confirm zero skips/zero failures.
  </action>
  <verify>
    <automated>SNOB_CI=1 python -m pytest tests/services/test_ai_service.py tests/services/test_encryption.py -rs --tb=short -p no:cacheprovider 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - A TEST-02 case→test mapping is recorded in the SUMMARY covering signature, fallback(respx), projection, throttle
    - A TEST-03 case→test mapping is recorded covering round-trip AND rotation
    - `SNOB_CI=1 pytest tests/services/test_ai_service.py tests/services/test_encryption.py -rs` exits 0 with zero unexpected skips
    - No existing passing test was rewritten; any change is a strictly additive new test for a documented gap
  </acceptance_criteria>
  <done>TEST-02 and TEST-03 verified covered (mapping in SUMMARY); both files green under SNOB_CI with no unexpected skips; any addition was a single targeted gap-closer, not a rebuild.</done>
</task>

<task type="auto">
  <name>Task 2: Verify-and-extend analytics + CSRF coverage (TEST-04 analytics portion, TEST-05)</name>
  <files>tests/services/test_analytics.py, tests/middleware/test_csrf.py, tests/middleware/test_csrf_form_shim.py</files>
  <read_first>
    - tests/services/test_analytics.py (the ENTIRE file — map to TEST-04 named cases: top coffees, preference profile, sweet spots, roast freshness; confirm seeded-DB usage via `clean_analytics`)
    - tests/services/test_analytics_perf.py (confirm the 1000-session perf check exists — verify only, do not modify)
    - tests/middleware/test_csrf.py (the ENTIRE file — confirm a positive path: valid token → request succeeds, and a negative path: missing/invalid token → 403)
    - tests/middleware/test_csrf_form_shim.py (the ENTIRE file — the form-field hoist shim behaviors)
    - app/services/analytics.py (the four named derivations to confirm tests hit the real query functions)
  </read_first>
  <action>
    For test_analytics.py: map the four TEST-04 named cases (top coffees → test_top_coffees, preference profile → test_preference_profile, sweet spots → test_sweet_spots, roast freshness → test_roast_freshness_buckets) and confirm each runs against the seeded test DB. All four are present per inventory — expect verify-only. Add a focused test ONLY if a named case is genuinely missing or asserts something trivial. For CSRF (TEST-05): confirm test_csrf.py has BOTH a positive (test_valid_token: a correctly-tokened state-changing request succeeds) and a negative (test_missing_token: a POST without a valid double-submit token returns 403). Both are present — expect verify-only. If the negative path only tests "missing" but not "present-but-wrong" token, add one focused negative case for a mismatched/forged token → 403, reusing the existing client fixture and triple-send idiom. Record the TEST-04(analytics)/TEST-05 mapping in the SUMMARY. Touch files only to add a documented gap-closer.
  </action>
  <verify>
    <automated>SNOB_CI=1 python -m pytest tests/services/test_analytics.py tests/services/test_analytics_perf.py tests/middleware/test_csrf.py tests/middleware/test_csrf_form_shim.py -rs --tb=short -p no:cacheprovider 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - A TEST-04 analytics case→test mapping is recorded covering top coffees, preference profile, sweet spots, roast freshness, all against a seeded DB
    - A TEST-05 mapping is recorded with BOTH a positive and a negative (403) CSRF case
    - `SNOB_CI=1 pytest <the four files> -rs` exits 0 with zero unexpected skips
    - No existing passing test rewritten; any change is strictly additive
  </acceptance_criteria>
  <done>TEST-04 (analytics) and TEST-05 verified covered (mapping in SUMMARY); the four files green under SNOB_CI with no unexpected skips; additions, if any, were targeted gap-closers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → CSRF-protected endpoint | a forged/absent double-submit token must be rejected (403); TEST-05 verifies this boundary |
| stored ciphertext → decrypted key (rotation) | TEST-03 verifies a rotated key still decrypts old tokens, so a key rotation never orphans stored API keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | CSRF middleware | mitigate | TEST-05 verify-and-extend confirms a positive pass AND a negative 403 (missing and, if a gap, forged token) |
| T-12-08 | Information Disclosure / Crypto | encryption MultiFernet rotation | mitigate | TEST-03 verify confirms round-trip + rotation; key rotation does not orphan or leak stored credentials |
| T-12-09 | Repudiation | service tests run hollow (skip) | mitigate | All verify commands run under SNOB_CI=1 so any skip on the critical service tests becomes a failure (Plan 12-01) |
</threat_model>

<verification>
- `SNOB_CI=1 pytest tests/services/test_ai_service.py tests/services/test_encryption.py tests/services/test_analytics.py tests/services/test_analytics_perf.py tests/middleware/test_csrf.py tests/middleware/test_csrf_form_shim.py -rs` is green with no unexpected skips.
- The SUMMARY contains an explicit criterion→test mapping for TEST-02, TEST-03, TEST-04(analytics), TEST-05.
</verification>

<success_criteria>
- TEST-02, TEST-03, TEST-05 (and the analytics portion of TEST-04) verified covered with a documented mapping.
- Zero existing passing tests rewritten; only targeted gap-closers added where a named case was genuinely missing.
</success_criteria>

<output>
After completion, create `.planning/phases/12-hardening-tests/12-04-SUMMARY.md`
</output>
