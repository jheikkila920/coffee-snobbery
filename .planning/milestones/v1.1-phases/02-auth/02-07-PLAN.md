---
phase: 02-auth
plan: 07
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-05
  - 02-06
files_modified:
  - app/schemas/auth.py
  - app/routers/auth.py
  - app/templates/pages/setup.html
  - app/templates/pages/login.html
  - tests/routers/test_auth.py
  - tests/routers/test_auth_stub.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-06
  - AUTH-07
tags:
  - wave-4
  - routers
  - templates
must_haves:
  truths:
    - "GET /setup renders pages/setup.html when app_settings.setup_completed='false'; returns 303 to /login when 'true' (D-01)"
    - "POST /setup with valid form on a clean DB: calls create_first_admin, on success regenerates session, sets session_id cookie via build_session_cookie, returns 303 to / (auto-login per D-03)"
    - "POST /setup after setup_completed='true' returns 303 to /login with NO argon2 cost incurred (explicit guard SELECT BEFORE the service call, per RESEARCH Open Q5)"
    - "GET /login renders pages/login.html with empty error and empty username"
    - "POST /login happy path: argon2 verify_password passes → regenerate_session → set cookie → 303 → /; emits auth.login_succeeded log with user_id"
    - "POST /login wrong password: 200 + re-renders pages/login.html with 'Invalid username or password.' + repopulated username (D-07); emits auth.login_failed log with reason=bad_password + user_id"
    - "POST /login unknown user: 200 + same re-render; calls dummy_verify to consume symmetric wall-clock time; emits auth.login_failed log with reason=user_not_found + NO user_id + NO attempted_username (D-15 Phase 1 carried)"
    - "POST /login inactive user: same generic error path as wrong password; emits auth.login_failed log with reason=inactive + user_id"
    - "POST /logout: deletes session row via delete_session, clears cookie via build_session_clear_cookie, returns 303 to /login; emits auth.logout log with user_id (or None if no session)"
    - "Session cookie attributes match build_session_cookie: HttpOnly, Secure, SameSite=Lax, Max-Age=2592000, Path=/"
    - "setup.html + login.html include the hidden <input type=hidden name=X-CSRF-Token value={{ request.cookies.get('csrftoken', '') }}> per D-15"
    - "tests/routers/test_auth_stub.py is DELETED; tests/routers/test_auth.py is the replacement"
  artifacts:
    - path: "app/schemas/auth.py"
      provides: "Pydantic SetupForm + LoginForm with username regex, EmailStr (setup only), password min length"
      contains: "EmailStr"
    - path: "app/routers/auth.py"
      provides: "Replaced bodies for /login + /setup; new /logout; consumes get_async_session, hash_password, verify_password, dummy_verify, create_first_admin, regenerate_session, sign_session_id, build_session_cookie/clear, delete_session, templates"
      contains: "create_first_admin"
    - path: "app/templates/pages/setup.html"
      provides: "First-time setup form with CSRF hidden field, username/email/password inputs, error display"
      contains: "X-CSRF-Token"
    - path: "app/templates/pages/login.html"
      provides: "Sign-in form with CSRF hidden field, username (repopulated)/password inputs, error display"
      contains: "X-CSRF-Token"
    - path: "tests/routers/test_auth.py"
      provides: "Integration tests: setup_happy_path, setup_blocked_after_completion, setup_concurrent_race, no_register_route, login_happy_path, login_wrong_password, session_cookie_attributes, session_fixation_defense, preset_cookie_does_not_inherit, logout_clears_session, login_csrf_blocked, logout_csrf_blocked"
      contains: "test_setup_concurrent_race"
  key_links:
    - from: "POST /setup happy path"
      to: "create_first_admin + regenerate_session + sign_session_id + build_session_cookie"
      via: "303 RedirectResponse with Set-Cookie header"
      pattern: "RedirectResponse.*303"
    - from: "POST /login wrong-user branch"
      to: "dummy_verify"
      via: "called BEFORE rendering the generic error response so wall-clock time matches the wrong-password branch"
      pattern: "dummy_verify"
    - from: "POST /logout"
      to: "delete_session + build_session_clear_cookie"
      via: "303 RedirectResponse to /login with clear-cookie header"
      pattern: "build_session_clear_cookie"
---

<objective>
The Phase 2 routing meat. Replace the Phase 1 stub bodies in `app/routers/auth.py` with the real `/setup` + `/login` + new `/logout` handlers. Ship the two new templates (`pages/setup.html`, `pages/login.html`). Ship the Pydantic form schemas. Delete the obsolete `tests/routers/test_auth_stub.py` and add the comprehensive `tests/routers/test_auth.py` covering every AUTH-01 / AUTH-02 / AUTH-03 / AUTH-06 / AUTH-07 scenario plus D-07 generic-error re-render. (Real-handler D-15 logging assertions are deferred to Plan 02-10 Task 4 alongside the smoke test.)

Purpose: This plan is the single largest carrier of Phase 2 requirements. It's intentionally one plan (3 files of code + 1 schema + 2 templates + 1 new test file + 1 extended test file) because the route + service + template + test cycle for `/setup`, `/login`, `/logout` is tightly coupled — splitting it across plans would force re-reading the same Phase 1 stub context twice.

Output: 7 files modified. Three commits acceptable (one per concern: schemas+routes, templates, tests) OR one squashed commit — executor decides per ergonomics.

Wave 4 placement: blocked by Plan 06 (SessionMiddleware D-09 must land first so `/login`'s `regenerate_session` happy path can rely on `request.state.session` being the right shape, and so the auto-login on `/setup` mints a session whose downstream consumer sees the User row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth/02-CONTEXT.md
@.planning/phases/02-auth/02-RESEARCH.md
@.planning/phases/02-auth/02-VALIDATION.md
@.planning/phases/02-auth/02-PATTERNS.md
@CLAUDE.md
@app/routers/auth.py
@app/services/auth.py
@app/services/setup.py
@app/services/sessions.py
@app/dependencies/auth.py
@app/dependencies/db.py
@app/signing.py
@app/csrf.py
@app/events.py
@app/rate_limit.py
@app/templates/base.html
@app/templates_setup.py
@tests/routers/test_auth_stub.py
@tests/middleware/test_csrf.py
</context>

<files_to_read>
- `.planning/phases/02-auth/02-CONTEXT.md` D-01..D-08 (login/setup behavior + 303 / 200 status codes + D-07 generic error), D-15 (form-field CSRF via shim), Claude's Discretion (password floor + template ergonomics + sign-out footer location).
- `.planning/phases/02-auth/02-RESEARCH.md` §"Pattern 1: `/login` happy + sad paths" (lines 547-671) — verbatim canonical body for the entire `/login` handler.
- `.planning/phases/02-auth/02-RESEARCH.md` §"Pattern 2: `/setup` with race protection + auto-login" (lines 673-730) — verbatim canonical body for both `/setup` handlers.
- `.planning/phases/02-auth/02-RESEARCH.md` §"FastAPI — RedirectResponse(status_code=303) + Set-Cookie" (lines 375-405).
- `.planning/phases/02-auth/02-RESEARCH.md` §"Open Questions for Planner" Q4 (Pydantic Form schema), Q5 (explicit guard before service call), Q7 (CSRF cookie on the post-setup 303).
- `.planning/phases/02-auth/02-PATTERNS.md` §"`app/routers/auth.py` (router REPLACEMENT)" (lines 378-435) — preserve decorator shape + slowapi + logging.
- `.planning/phases/02-auth/02-PATTERNS.md` §"`app/templates/pages/setup.html` and `pages/login.html`" (lines 655-715) — verbatim template scaffolds.
- `app/routers/auth.py` (entire current file, 53 lines) — stub being replaced.
- `app/services/sessions.py` lines 152-182 — `build_session_cookie` + `build_session_clear_cookie` value contracts.
- `app/templates/base.html` line 10 — the CSRF token meta surface pattern.
- `app/csrf.py` line 41 — `CSRF_HEADER_NAME = "X-CSRF-Token"` (the exact name the templates' hidden input uses).
- `tests/routers/test_auth_stub.py` — DELETE in the SAME commit that creates `tests/routers/test_auth.py`.
- `tests/middleware/test_csrf.py` lines 50-73 — `_csrf_pair` helper to mirror in `tests/routers/test_auth.py`.
</files_to_read>

<interfaces>
```python
# app/schemas/auth.py
from pydantic import BaseModel, EmailStr, Field

class SetupForm(BaseModel):
    username: str = Field(..., pattern=r"^[A-Za-z0-9_-]{3,32}$")
    email: EmailStr
    password: str = Field(..., min_length=12)

class LoginForm(BaseModel):
    username: str = Field(..., min_length=1, max_length=32)
    password: str = Field(..., min_length=1)
```

NOTE on EmailStr: requires `pydantic[email]` extra. Check `requirements.txt` — if `email-validator` is NOT pinned, the executor adds `email-validator>=2,<3` to requirements.txt in this plan. (Quick grep: `grep email-validator requirements.txt`.)

```python
# app/routers/auth.py — full body replacement; module-level structure preserved

router = APIRouter()

@router.get("/setup", response_class=HTMLResponse) ...
@router.post("/setup", response_class=HTMLResponse) ...
@router.get("/login", response_class=HTMLResponse) ...
@router.post("/login", response_class=HTMLResponse) ...
@router.post("/logout") ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/routers/test_auth.py (delete tests/routers/test_auth_stub.py in same task)</name>
  <files>tests/routers/test_auth.py, tests/routers/test_auth_stub.py</files>
  <behavior>
    AUTH-01:
    - test_setup_happy_path: clean DB → GET /setup returns 200 + form; POST /setup with valid creds + CSRF pair → 303 to /, Set-Cookie session_id present
    - test_setup_blocked_after_completion: pre-flip setup_completed='true' → GET /setup returns 303 to /login; POST /setup returns 303 to /login (no argon2 cost)
    AUTH-02:
    - test_setup_concurrent_race: asyncio.gather two POST /setups with primed CSRF cookies → exactly ONE returns 303→/, the other 303→/login; users table has exactly 1 row
    AUTH-03:
    - test_no_register_route: GET /register and POST /register both return 404
    - test_login_happy_path: seed user via fixture → POST /login with creds → 303 to /, Set-Cookie session_id present
    - test_login_wrong_password: seed user → POST /login with wrong password → 200, body contains "Invalid username or password."
    AUTH-06:
    - test_session_cookie_attributes: after happy login, parse Set-Cookie header; assert "HttpOnly", "Secure", "SameSite=Lax", "Max-Age=2592000", "Path=/"
    AUTH-07:
    - test_session_fixation_defense: seed user with one session → POST /login → assert OLD session row no longer exists AND new session row exists with same user_id but different session_id; cookie value changed
    - test_preset_cookie_does_not_inherit: client sets a fake session_id cookie BEFORE login → POST /login → response Set-Cookie has a DIFFERENT session_id (attacker's pre-set cookie does not "transfer" the new session)
    - test_logout_clears_session: seed user with session → POST /logout with CSRF pair → 303 to /login, session row DELETEd, Set-Cookie has Max-Age=0
    CSRF:
    - test_login_csrf_blocked: POST /login with NO CSRF cookie+header pair → 403 (after Phase 2 properly enforces; may need xfail if Plan 02-10 isn't yet wired — write the assertion correctly and let it pass when Plan 02-10 ships)
    - test_logout_csrf_blocked: POST /logout with NO CSRF pair → 403
  </behavior>
  <action>
DELETE `tests/routers/test_auth_stub.py` in this same task — `git rm tests/routers/test_auth_stub.py` is the explicit step. Per VALIDATION line 91: "replaces tests/routers/test_auth_stub.py (delete the stub file in the same plan that creates this one)".

CREATE `tests/routers/test_auth.py` with the test set above. Use these fixtures from Plan 01: `client` (existing TestClient), `fresh_db` (autouse), `seeded_admin_user`, `seeded_regular_user`, `async_client` (for the AUTH-02 concurrent-race test).

Common helpers at top of file:

```
import pytest, uuid
from typing import Any

def _require_auth_router() -> None:
    try:
        from app.routers.auth import router  # noqa: F401
    except ImportError:
        pytest.skip("Wave 4 dep: app.routers.auth (Plan 02-07)")

def _csrf_pair(client) -> tuple[str, dict[str, str], dict[str, str]]:
    """Get a primed CSRF token from a GET; return (token, headers, cookies)."""
    primer = client.get("/")
    token = primer.cookies.get("csrftoken")
    if not token:
        pytest.skip("CSRF cookie 'csrftoken' not yet set by middleware (Plan 02-10 wiring)")
    return token, {"X-CSRF-Token": token}, {"csrftoken": token}
```

AUTH-01 happy path:
```
def test_setup_happy_path(client, fresh_db) -> None:
    _require_auth_router()
    # GET /setup renders the form
    r = client.get("/setup")
    assert r.status_code == 200
    assert "First-time setup" in r.text or "<input" in r.text
    # POST /setup with valid form
    token, headers, cookies = _csrf_pair(client)
    r2 = client.post(
        "/setup",
        data={
            "X-CSRF-Token": token,
            "username": "admin",
            "email": "admin@example.com",
            "password": "twelve-chars-min-password",
        },
        headers=headers,
        cookies=cookies,
        follow_redirects=False,
    )
    assert r2.status_code == 303, f"expected 303 to /, got {r2.status_code}: {r2.text[:200]}"
    assert r2.headers["location"] == "/"
    set_cookie = r2.headers.get("set-cookie", "")
    assert "session_id=" in set_cookie
```

AUTH-02 concurrent race — uses async_client + asyncio.gather:
```
import asyncio, pytest_asyncio

@pytest.mark.asyncio
async def test_setup_concurrent_race(async_client, fresh_db) -> None:
    _require_auth_router()
    # Prime CSRF cookie via async GET
    primer = await async_client.get("/setup")
    token = primer.cookies.get("csrftoken")
    if not token:
        pytest.skip("CSRF cookie not primed by GET /setup")
    headers = {"X-CSRF-Token": token}
    cookies = {"csrftoken": token}
    body = {"X-CSRF-Token": token, "username": "racer", "email": "r@b.c", "password": "twelve-chars-min"}
    r1, r2 = await asyncio.gather(
        async_client.post("/setup", data=body, headers=headers, cookies=cookies),
        async_client.post("/setup", data=body, headers=headers, cookies=cookies),
    )
    # One winner (303→/), one loser (303→/login). Both 303 in HTTP — distinguish by Location.
    locations = sorted([r1.headers.get("location", ""), r2.headers.get("location", "")])
    assert locations == ["/", "/login"], (
        f"AUTH-02: expected one 303→/ + one 303→/login, got locations={locations}"
    )
    # Exactly one user row
    from app.main import async_session_factory
    from app.models.user import User
    from sqlalchemy import select
    async with async_session_factory() as db:
        users = (await db.execute(select(User))).scalars().all()
        assert len(users) == 1, f"AUTH-02: exactly one user expected, got {len(users)}"
```

AUTH-03 no register:
```
def test_no_register_route(client) -> None:
    r1 = client.get("/register")
    r2 = client.post("/register", data={})
    assert r1.status_code == 404
    assert r2.status_code in (404, 405)
```

AUTH-03 login happy path:
```
def test_login_happy_path(client, seeded_regular_user) -> None:
    _require_auth_router()
    token, headers, cookies = _csrf_pair(client)
    r = client.post(
        "/login",
        data={
            "X-CSRF-Token": token,
            "username": seeded_regular_user["user"].username,
            "password": "twelve-chars-min-password",  # the literal fixture uses
        },
        headers=headers, cookies=cookies, follow_redirects=False,
    )
    assert r.status_code == 303
    assert r.headers["location"] == "/"
    assert "session_id=" in r.headers.get("set-cookie", "")
```

AUTH-03 wrong password:
```
def test_login_wrong_password(client, seeded_regular_user) -> None:
    _require_auth_router()
    token, headers, cookies = _csrf_pair(client)
    r = client.post(
        "/login",
        data={
            "X-CSRF-Token": token,
            "username": seeded_regular_user["user"].username,
            "password": "WRONG-password-here",
        },
        headers=headers, cookies=cookies, follow_redirects=False,
    )
    assert r.status_code == 200, f"D-07: wrong password returns 200 + re-render, got {r.status_code}"
    assert "Invalid username or password." in r.text
```

AUTH-06 cookie attributes:
```
def test_session_cookie_attributes(client, seeded_regular_user) -> None:
    _require_auth_router()
    token, headers, cookies = _csrf_pair(client)
    r = client.post(
        "/login",
        data={"X-CSRF-Token": token, "username": seeded_regular_user["user"].username, "password": "twelve-chars-min-password"},
        headers=headers, cookies=cookies, follow_redirects=False,
    )
    set_cookies = r.headers.get_list("set-cookie") if hasattr(r.headers, "get_list") else [r.headers.get("set-cookie", "")]
    session_cookie = next((c for c in set_cookies if c.startswith("session_id=")), "")
    assert "HttpOnly" in session_cookie
    assert "Secure" in session_cookie
    assert "SameSite=Lax" in session_cookie
    assert "Max-Age=2592000" in session_cookie
    assert "Path=/" in session_cookie
```

AUTH-07 fixation defense — capture old session_id before login, then assert new session_id differs AND old row is gone:
```
def test_session_fixation_defense(client, seeded_regular_user) -> None:
    _require_auth_router()
    old_sid = seeded_regular_user["session_id"]
    old_signed = seeded_regular_user["signed_cookie"]
    token, headers, cookies = _csrf_pair(client)
    cookies["session_id"] = old_signed
    r = client.post(
        "/login",
        data={"X-CSRF-Token": token, "username": seeded_regular_user["user"].username, "password": "twelve-chars-min-password"},
        headers=headers, cookies=cookies, follow_redirects=False,
    )
    assert r.status_code == 303
    new_set_cookie = r.headers.get("set-cookie", "")
    # New session_id value differs from the pre-set one
    assert old_signed not in new_set_cookie, "AUTH-07: new session cookie must differ from pre-set"
    # Old session row no longer exists in DB
    import asyncio
    from app.main import async_session_factory
    from app.models.session import Session
    from sqlalchemy import select
    async def _check():
        async with async_session_factory() as db:
            row = (await db.execute(select(Session).where(Session.session_id == old_sid))).scalar_one_or_none()
            return row
    assert asyncio.run(_check()) is None, "AUTH-07: old session row must be DELETEd on regeneration"
```

AUTH-07 preset-cookie-does-not-inherit: similar; assert that an attacker-pre-set cookie value never appears as the new authenticated session cookie.

AUTH-07 logout:
```
def test_logout_clears_session(client, seeded_regular_user) -> None:
    _require_auth_router()
    sid = seeded_regular_user["session_id"]
    signed = seeded_regular_user["signed_cookie"]
    token, headers, cookies = _csrf_pair(client)
    cookies["session_id"] = signed
    r = client.post(
        "/logout",
        data={"X-CSRF-Token": token},
        headers=headers, cookies=cookies, follow_redirects=False,
    )
    assert r.status_code == 303
    assert r.headers["location"] == "/login"
    set_cookie = r.headers.get("set-cookie", "")
    assert "session_id=" in set_cookie and ("Max-Age=0" in set_cookie or "max-age=0" in set_cookie)
    # DB row deleted
    import asyncio
    from app.main import async_session_factory
    from app.models.session import Session
    from sqlalchemy import select
    async def _check():
        async with async_session_factory() as db:
            return (await db.execute(select(Session).where(Session.session_id == sid))).scalar_one_or_none()
    assert asyncio.run(_check()) is None
```

CSRF blocked tests:
```
def test_login_csrf_blocked(client) -> None:
    _require_auth_router()
    r = client.post(
        "/login",
        data={"username": "x", "password": "y"},
    )
    # When CSRFMiddleware is wired (Plan 02-10) and the CSRFFormFieldShim is mounted,
    # a POST with no cookie + no header + no form-field returns 403.
    # Before Plan 02-10, this may pass through; xfail with note for that interim.
    if r.status_code != 403:
        pytest.xfail(
            f"CSRF enforcement expected but got {r.status_code} — "
            "plan 02-10 wires the shim into app.main; this test turns green then."
        )
    assert r.status_code == 403
```

CRITICAL — for the AUTH-07 tests that use the seeded_regular_user fixture: the password "twelve-chars-min-password" is hard-coded into the `_seed_user` helper in `tests/conftest.py` (Plan 01). Tests in this plan use that literal. If a future change makes the literal configurable, this contract changes.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery pytest -x tests/routers/test_auth.py</automated>
  </verify>
  <done>tests/routers/test_auth_stub.py is removed; tests/routers/test_auth.py exists with all tests. Before Tasks 2-5 in this plan, most tests skip cleanly. After Tasks 2-5 + Plan 02-10 wiring, all pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create app/schemas/auth.py — SetupForm + LoginForm Pydantic models</name>
  <files>app/schemas/auth.py</files>
  <action>
Create `app/schemas/auth.py` (the `app/schemas/` package already exists — verify by listing; if not, create `app/schemas/__init__.py` as a one-line package marker).

First: check whether `email-validator` is in `requirements.txt`. If not, add it:
```
email-validator>=2,<3
```
to `requirements.txt` (after the `argon2-cffi` line). This is a dependency of `pydantic[email]` / `EmailStr`.

File body:
```
"""Pydantic v2 form schemas for /setup and /login.

Validation rules (CONTEXT 'Claude's Discretion / Password policy floor'):

* Username: 3-32 chars, ``[A-Za-z0-9_-]`` regex.
* Email: ``EmailStr`` validation (RFC-aware via email-validator).
* Password: 12 char minimum (soft floor; no complexity rules per CONTEXT).

The forms are parsed by FastAPI's ``Form(...)`` parameter mechanism at the
route level; this module ships the validation contract. Failed validation
raises ``pydantic.ValidationError`` which FastAPI converts to a 422 — the
route handler in :mod:`app.routers.auth` catches the error type via the
parameter-level validation and re-renders the template with a generic
'Invalid input' message instead of returning the default 422 JSON.
"""

from __future__ import annotations

from pydantic import BaseModel, EmailStr, Field


class SetupForm(BaseModel):
    """First-admin setup form. D-02 requires email; soft 12-char password floor."""

    username: str = Field(..., pattern=r"^[A-Za-z0-9_-]{3,32}$",
                          description="3-32 chars, alphanumeric + - and _")
    email: EmailStr = Field(..., description="Required for first admin per D-02")
    password: str = Field(..., min_length=12,
                          description="Minimum 12 characters")


class LoginForm(BaseModel):
    """Login form. Loose validation — the wrong-credentials path uses the
    constant-time argon2 verify, not a Pydantic shape check, to avoid the
    'your username doesn't match the regex' enumeration leak.
    """

    username: str = Field(..., min_length=1, max_length=32)
    password: str = Field(..., min_length=1)


__all__ = ["SetupForm", "LoginForm"]
```

Note on Pydantic Form binding: in FastAPI 0.115+ the `Annotated[SetupForm, Form()]` pattern works. In the route signature use individual `Form(...)` parameters and construct the BaseModel manually inside the handler — that's the simplest and most compatible approach:
```python
@router.post("/setup")
async def setup_submit(
    request: Request,
    username: str = Form(...),
    email: str = Form(...),
    password: str = Form(...),
    db: AsyncSession = Depends(get_async_session),
) -> Response:
    try:
        form = SetupForm(username=username, email=email, password=password)
    except ValidationError as e:
        # Re-render with a generic "Invalid input" error
        return templates.TemplateResponse(...)
    # Continue with form.username, form.email, form.password
```
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -c "from app.schemas.auth import SetupForm, LoginForm; SetupForm(username='admin', email='a@b.c', password='twelve-chars-min'); LoginForm(username='admin', password='x'); print('ok')"</automated>
  </verify>
  <done>app/schemas/auth.py exists; both models import and validate sample inputs without error.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace app/routers/auth.py — real /setup + /login + /logout</name>
  <files>app/routers/auth.py</files>
  <action>
REPLACE the entire body of `app/routers/auth.py`. Compose verbatim from RESEARCH §"Pattern 1" (lines 547-671) for /login + /logout and RESEARCH §"Pattern 2" (lines 673-730) for /setup, integrating:

- The Pydantic SetupForm + LoginForm from Plan-Task 2 (use individual `Form(...)` params + manual `try: SetupForm(...) except ValidationError`).
- The `get_async_session` dependency from Plan 03.
- The `create_first_admin` service from Plan 05.
- The argon2 service from Plan 02 (`hash_password`, `verify_password`, `dummy_verify`).
- The session helpers from Phase 1 (`regenerate_session`, `delete_session`, `build_session_cookie`, `build_session_clear_cookie`, `sign_session_id`).
- The slowapi decorators preserved from the stub (`@limiter.limit(LOGIN_LIMIT)`, `@limiter.limit(SETUP_LIMIT)`).
- Event constants from `app.events` (`AUTH_LOGIN_SUCCEEDED`, `AUTH_LOGIN_FAILED`, `AUTH_LOGOUT`, `ADMIN_USER_CREATED`).
- D-15 logging policy per route (see PATTERNS lines 416-421).

Module docstring:
```
"""Phase 2 real auth routes: /setup, /login, /logout.

Replaces the Phase 1 stubs (no parallel file). Preserves the slowapi
decorators and the `Request: Request` parameter (slowapi requires it).

Locked behaviors per CONTEXT:
* D-01: post-setup GET /setup AND POST /setup → 303 → /login
* D-03: setup happy path → auto-login (303 → /) — no /setup → /login → re-type
* D-05: classic form POST → 303 (no HTMX on auth surfaces)
* D-07: wrong/unknown credentials → 200 + re-render with "Invalid username or password."
* D-12: /logout is POST-only with CSRF
* D-15: form-field CSRF via CSRFFormFieldShim (Plan 02-04); shim hoists to header

D-15 Phase 1 logging policy:
* auth.login_succeeded → log {user_id, ip, request_id}
* auth.login_failed reason=bad_password → log {user_id, ip, request_id, reason}
* auth.login_failed reason=user_not_found → log {ip, request_id, reason} (NO user_id, NO attempted_username)
* auth.login_failed reason=inactive → log {user_id, ip, request_id, reason}
* auth.logout → log {user_id (or None), ip, request_id}
* admin.user_created → log {user_id, ip, request_id}
"""
```

Imports block:
```
from __future__ import annotations

import structlog
from fastapi import APIRouter, Depends, Form, Request
from fastapi.responses import HTMLResponse, RedirectResponse, Response
from pydantic import ValidationError
from sqlalchemy import select, text
from sqlalchemy.ext.asyncio import AsyncSession

from app.dependencies.db import get_async_session
from app.events import (
    ADMIN_USER_CREATED,
    AUTH_LOGIN_FAILED,
    AUTH_LOGIN_SUCCEEDED,
    AUTH_LOGOUT,
)
from app.models.user import User
from app.rate_limit import LOGIN_LIMIT, SETUP_LIMIT, limiter
from app.schemas.auth import LoginForm, SetupForm
from app.services.auth import dummy_verify, verify_password
from app.services.sessions import (
    build_session_clear_cookie,
    build_session_cookie,
    delete_session,
    regenerate_session,
)
from app.services.setup import create_first_admin
from app.signing import sign_session_id
from app.templates_setup import templates

log = structlog.get_logger()
router = APIRouter()


def _ip(request: Request) -> str:
    return request.client.host if request.client else "unknown"


def _rid(request: Request) -> str:
    return getattr(request.state, "request_id", "unknown")
```

GET /setup — explicit guard before render (per RESEARCH Open Q5):
```
@router.get("/setup", response_class=HTMLResponse)
async def setup_form(
    request: Request,
    db: AsyncSession = Depends(get_async_session),
) -> Response:
    """Render setup form when setup_completed='false'; else 303 → /login."""
    row = await db.execute(
        text("SELECT value FROM app_settings WHERE key = 'setup_completed'")
    )
    if row.scalar() == "true":
        return RedirectResponse(url="/login", status_code=303)
    return templates.TemplateResponse(
        request=request, name="pages/setup.html",
        context={"error": None},
    )
```

POST /setup — with explicit guard BEFORE argon2 cost (RESEARCH Open Q5):
```
@router.post("/setup", response_class=HTMLResponse)
@limiter.limit(SETUP_LIMIT)
async def setup_submit(
    request: Request,
    username: str = Form(...),
    email: str = Form(...),
    password: str = Form(...),
    db: AsyncSession = Depends(get_async_session),
) -> Response:
    """First-admin POST. Guard → form validation → create_first_admin → auto-login."""
    # Guard 1: setup already done → 303 → /login BEFORE incurring any cost (Open Q5).
    row = await db.execute(
        text("SELECT value FROM app_settings WHERE key = 'setup_completed'")
    )
    if row.scalar() == "true":
        return RedirectResponse(url="/login", status_code=303)

    # Guard 2: validate the form shape
    try:
        form = SetupForm(username=username, email=email, password=password)
    except ValidationError:
        return templates.TemplateResponse(
            request=request, name="pages/setup.html",
            context={"error": "Please check the form values."},
            status_code=200,
        )

    new_user = await create_first_admin(
        db,
        username=form.username,
        email=form.email,
        plaintext_password=form.password,
    )
    if new_user is None:
        # Race lost during the FOR UPDATE wait
        return RedirectResponse(url="/login", status_code=303)

    log.info(
        ADMIN_USER_CREATED,
        user_id=new_user.id, ip=_ip(request), request_id=_rid(request),
    )

    # D-03: auto-login on successful setup.
    new_session_id = await regenerate_session(db, None, new_user.id)
    log.info(
        AUTH_LOGIN_SUCCEEDED,
        user_id=new_user.id, ip=_ip(request), request_id=_rid(request),
    )
    response = RedirectResponse(url="/", status_code=303)
    response.headers.append(
        "Set-Cookie", build_session_cookie(sign_session_id(new_session_id))
    )
    return response
```

GET /login:
```
@router.get("/login", response_class=HTMLResponse)
async def login_form(request: Request) -> Response:
    return templates.TemplateResponse(
        request=request, name="pages/login.html",
        context={"error": None, "username": ""},
    )
```

POST /login — verbatim per RESEARCH §"Pattern 1" lines 589-648, with the D-15 logging-policy reason fields:
```
@router.post("/login", response_class=HTMLResponse)
@limiter.limit(LOGIN_LIMIT)
async def login_submit(
    request: Request,
    username: str = Form(...),
    password: str = Form(...),
    db: AsyncSession = Depends(get_async_session),
) -> Response:
    # Loose validation — wrong shape is treated as wrong credentials (no enumeration)
    try:
        form = LoginForm(username=username, password=password)
    except ValidationError:
        return templates.TemplateResponse(
            request=request, name="pages/login.html",
            context={"error": "Invalid username or password.", "username": username},
            status_code=200,
        )

    result = await db.execute(select(User).where(User.username == form.username))
    user = result.scalar_one_or_none()

    if user is None:
        # D-15: user_not_found branch — dummy_verify symmetry, no user_id, no attempted_username
        dummy_verify(form.password)
        log.info(
            AUTH_LOGIN_FAILED,
            ip=_ip(request), request_id=_rid(request), reason="user_not_found",
        )
        return templates.TemplateResponse(
            request=request, name="pages/login.html",
            context={"error": "Invalid username or password.", "username": form.username},
            status_code=200,
        )

    if not user.is_active:
        # Inactive: symmetric cost + user_id logged (no attempted_username)
        dummy_verify(form.password)
        log.info(
            AUTH_LOGIN_FAILED,
            ip=_ip(request), request_id=_rid(request), user_id=user.id, reason="inactive",
        )
        return templates.TemplateResponse(
            request=request, name="pages/login.html",
            context={"error": "Invalid username or password.", "username": form.username},
            status_code=200,
        )

    if not verify_password(user.password_hash, form.password):
        log.info(
            AUTH_LOGIN_FAILED,
            ip=_ip(request), request_id=_rid(request), user_id=user.id, reason="bad_password",
        )
        return templates.TemplateResponse(
            request=request, name="pages/login.html",
            context={"error": "Invalid username or password.", "username": form.username},
            status_code=200,
        )

    # Happy path
    prior_session = getattr(request.state, "session", None)
    prior_session_id = prior_session.session_id if prior_session else None
    new_session_id = await regenerate_session(db, prior_session_id, user.id)
    log.info(AUTH_LOGIN_SUCCEEDED, user_id=user.id, ip=_ip(request), request_id=_rid(request))
    response = RedirectResponse(url="/", status_code=303)
    response.headers.append(
        "Set-Cookie", build_session_cookie(sign_session_id(new_session_id))
    )
    return response
```

POST /logout — D-12 (POST-only, CSRF-required):
```
@router.post("/logout")
async def logout_submit(
    request: Request,
    db: AsyncSession = Depends(get_async_session),
) -> Response:
    session = getattr(request.state, "session", None)
    user_id = session.user_id if session else None
    if session is not None:
        await delete_session(db, session.session_id)
    log.info(
        AUTH_LOGOUT,
        user_id=user_id, ip=_ip(request), request_id=_rid(request),
    )
    response = RedirectResponse(url="/login", status_code=303)
    response.headers.append("Set-Cookie", build_session_clear_cookie())
    return response
```

ABSOLUTELY DO NOT include this entire body inline above as fenced Python — emit it as the actual file content. The `<action>` here is descriptive; the executor implements the file by composing the verbatim RESEARCH excerpts referenced.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery pytest -x tests/routers/test_auth.py -k "happy_path or no_register or wrong_password or session_cookie or fixation or logout_clears"</automated>
  </verify>
  <done>The non-CSRF-gated tests in `tests/routers/test_auth.py` pass. CSRF-gated tests (`test_login_csrf_blocked`, `test_logout_csrf_blocked`) may xfail until Plan 02-10 wires the shim — that's expected at this point.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create app/templates/pages/setup.html + app/templates/pages/login.html</name>
  <files>app/templates/pages/setup.html, app/templates/pages/login.html</files>
  <action>
Create both templates per 02-PATTERNS.md §"`app/templates/pages/setup.html` and `pages/login.html`" lines 655-715.

`app/templates/pages/setup.html`:
```html
{% extends "base.html" %}
{% block page_title %}Setup{% endblock %}
{% block content %}
  <main class="mx-auto max-w-prose px-6 py-12">
    <h1 class="text-2xl font-semibold">First-time setup</h1>
    <p class="mt-2 text-sm">This creates the household admin account.</p>
    {% if error %}<p class="mt-4 text-red-700">{{ error }}</p>{% endif %}
    <form method="post" action="/setup" class="mt-6 flex flex-col gap-4">
      {# D-15: CSRFFormFieldShim hoists this field into the X-CSRF-Token header #}
      <input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken', '') }}">
      <label class="flex flex-col">Username
        <input name="username" required minlength="3" maxlength="32" pattern="[A-Za-z0-9_\-]+"
               class="rounded border px-2 py-1">
      </label>
      <label class="flex flex-col">Email
        <input name="email" type="email" required class="rounded border px-2 py-1">
      </label>
      <label class="flex flex-col">Password
        <input name="password" type="password" required minlength="12"
               class="rounded border px-2 py-1">
      </label>
      <button type="submit" class="mt-2 self-start rounded bg-espresso-900 px-4 py-2 text-cream-50">
        Create admin
      </button>
    </form>
  </main>
{% endblock %}
```

`app/templates/pages/login.html`:
```html
{% extends "base.html" %}
{% block page_title %}Sign in{% endblock %}
{% block content %}
  <main class="mx-auto max-w-prose px-6 py-12">
    <h1 class="text-2xl font-semibold">Sign in to Snobbery</h1>
    {% if error %}<p class="mt-4 text-red-700">{{ error }}</p>{% endif %}
    <form method="post" action="/login" class="mt-6 flex flex-col gap-4">
      <input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken', '') }}">
      <label class="flex flex-col">Username
        <input name="username" required value="{{ username or '' }}" class="rounded border px-2 py-1">
      </label>
      <label class="flex flex-col">Password
        <input name="password" type="password" required class="rounded border px-2 py-1">
      </label>
      <button type="submit" class="mt-2 self-start rounded bg-espresso-900 px-4 py-2 text-cream-50">
        Sign in
      </button>
    </form>
  </main>
{% endblock %}
```

Constraints:
- 2-space indent for HTML/Jinja per CLAUDE.md "Code conventions".
- snake_case template variables.
- NO `|safe` anywhere — the CI grep at `tests/ci/test_no_unsafe_jinja.py` will fail the build.
- Tailwind utility classes only; the cream/espresso palette comes from Phase 0's `tailwind.config.js`.
- Mobile-first: `max-w-prose px-6 py-12` works at 375px without horizontal scroll (manual verification per CLAUDE.md mobile-first invariant — see VALIDATION § "Manual-Only Verifications").
- The hidden CSRF input field name MUST be exactly `X-CSRF-Token` (matches `app/csrf.py:CSRF_HEADER_NAME` line 41 and the shim's needle `name="X-CSRF-Token"`).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery pytest -x tests/ci/test_no_unsafe_jinja.py tests/routers/test_auth.py::test_setup_happy_path</automated>
  </verify>
  <done>Both templates exist; no |safe / hx-on / hx-vals='js:' / hx-headers='js:' patterns; setup_happy_path test passes (which renders pages/setup.html via TestClient).</done>
</task>

</tasks>

<!-- NOTE: D-15 logging assertions (formerly Task 5 here) were lifted into Plan 02-10
     during plan-checker revision (mechanical 5-task split). The new handlers in
     this plan emit auth.login_failed with reason="bad_password" (carries user_id) /
     reason="user_not_found" (no user_id, no attempted_username) per Phase 1 D-15 —
     Plan 02-10 Task 4 asserts that contract via structlog capsys capture. -->


<verification>
Maps to 02-VALIDATION.md Per-Task Verification Map rows:
- AUTH-01 happy + blocked_after_completion → `test_setup_happy_path`, `test_setup_blocked_after_completion`
- AUTH-02 concurrent race → `test_setup_concurrent_race`
- AUTH-03 happy + wrong_password + no_register → `test_login_happy_path`, `test_login_wrong_password`, `test_no_register_route`
- AUTH-06 cookie attributes → `test_session_cookie_attributes`
- AUTH-07 fixation + preset + logout → `test_session_fixation_defense`, `test_preset_cookie_does_not_inherit`, `test_logout_clears_session`
- CSRF block on /login + /logout → `test_login_csrf_blocked`, `test_logout_csrf_blocked` (may xfail until Plan 10)
- D-15 logging coverage moved to Plan 02-10 Task 4

Full plan run:
```
docker compose exec coffee-snobbery pytest -x tests/routers/test_auth.py tests/ci/test_no_unsafe_jinja.py
```

Important post-condition: `tests/routers/test_auth_stub.py` is DELETED. Verify:
```
test -f tests/routers/test_auth_stub.py && echo "STILL PRESENT — DELETE IT" || echo "ok deleted"
```
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → /setup, /login, /logout | Untrusted plaintext form fields; Pydantic schemas validate; argon2 verify constant-time. |
| login form → server response | Wrong-credentials response must NOT leak which leg failed (user vs password). |
| Set-Cookie response → browser | session_id cookie must carry HttpOnly + Secure + SameSite=Lax + signed value. |
| /logout → session row | Must DELETE the row server-side, not just clear the cookie. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Information Disclosure | User-enumeration via login response timing | mitigate | `dummy_verify` on user-not-found branch (called BEFORE rendering response); proven by tests/services/test_auth.py::test_dummy_verify_timing + the user-not-found test asserts no user_id leaked. ASVS V2.2.5. |
| T-02-07-02 | Spoofing | Session fixation: attacker pre-sets session_id cookie; victim logs in; attacker uses old cookie | mitigate | `regenerate_session(db, prior_session_id, user.id)` deletes the old row + mints a fresh UUID + signs it. `test_preset_cookie_does_not_inherit` regression-tests. ASVS V3.2.3. |
| T-02-07-03 | Elevation of Privilege | Concurrent /setup POSTs both create admin | mitigate | `create_first_admin` (Plan 05) holds SELECT FOR UPDATE; race test `test_setup_concurrent_race` proves exactly-one-user-row outcome. ASVS V14.4.6. |
| T-02-07-04 | Tampering | CSRF: cross-site POST to /login or /logout from a malicious page | mitigate | starlette-csrf 3.0 (Phase 1 wired) + CSRFFormFieldShim (Plan 04, wired by Plan 02-10). Tests `test_login_csrf_blocked` + `test_logout_csrf_blocked`. ASVS V4.2.1. |
| T-02-07-05 | Denial of Service | Argon2 amplification via /login flood | mitigate | slowapi LOGIN_LIMIT="5/15minutes" per IP, decorator-preserved from stub. ASVS V14.4.3. |
| T-02-07-06 | Information Disclosure | Failed-login response includes the attempted username in the re-render input — visible to anyone who can see the response | accept | The username field is re-populated as a UX courtesy for real users mistyping their password; the password field is NOT re-populated. Same as every login form on the web. The username is also already visible in URL bar after redirect / in browser history. |
| T-02-07-07 | Information Disclosure | `auth.login_failed` log line accidentally includes the attempted password | mitigate | Code never passes `form.password` to `log.info(...)`; structlog `_redact_sensitive_keys` processor (Phase 1) defensively scrubs "password" / "token" keys if accidentally added. D-15 tests assert exact field set. ASVS V7.1.1. |
| T-02-07-08 | Tampering | Race: SQL Pydantic validation passes but `create_first_admin` returns None (race lost). The route handler should NOT proceed to auto-login | mitigate | Explicit `if new_user is None: return RedirectResponse("/login", 303)` guard. Tested via `test_setup_concurrent_race`. |
| T-02-07-09 | Denial of Service | POST /setup after setup_completed='true' incurs argon2 cost (100ms per request) before noticing the redirect path | mitigate | Explicit `text("SELECT value FROM app_settings WHERE key='setup_completed'")` guard BEFORE the create_first_admin service call. RESEARCH Open Q5 lock-in. |
| T-02-07-10 | Spoofing | /logout works without CSRF → drive-by logout via `<img src="/logout">` | mitigate | /logout is POST-only with CSRF. Test `test_logout_csrf_blocked`. ASVS V4.2.1. |
</threat_model>

<success_criteria>
- `app/schemas/auth.py`, `app/routers/auth.py` (replaced body), `app/templates/pages/setup.html`, `app/templates/pages/login.html` all exist with content per the task descriptions.
- `tests/routers/test_auth_stub.py` is DELETED.
- `tests/routers/test_auth.py` exists; all non-CSRF-gated tests pass.
- `tests/ci/test_no_unsafe_jinja.py` passes (no |safe in templates/pages/).
- CSRF-gated tests (`test_login_csrf_blocked`, `test_logout_csrf_blocked`) either pass or xfail with a note pointing to Plan 10.
- `email-validator>=2,<3` is in `requirements.txt` (added if missing).
- `ruff check app/` clean.
- Commit (or 2 atomic commits):
  - `feat(02-07): real /setup + /login + /logout handlers with Pydantic schemas + templates`
  - `test(02-07): comprehensive auth route tests; replaces test_auth_stub.py`
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-07-SUMMARY.md`.
</output>

<commit>
feat(02-07): real /setup + /login + /logout handlers, templates, and Pydantic schemas
</commit>

## PLAN COMPLETE
