---
phase: 02-auth
plan: 11
type: execute
wave: 6
depends_on:
  - 02-10
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-07
tags:
  - wave-6
  - docs
must_haves:
  truths:
    - "ROADMAP.md Phase 2 success criterion #1 says '302 → /login' (not 'the route 404s') per D-01"
    - "ROADMAP.md Phase 2 success criterion #5 says 'cold container → /setup → auto-login → see /' (not 'setup → login → see /') per D-03"
    - "No other Phase 2 ROADMAP wording changes — only the two locked-decision amendments"
    - "Phase 2 'Plans:' line is updated to '11 plans' with the 02-NN-PLAN.md list (the ROADMAP currently says 'Plans: TBD')"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Amended success criteria #1 + #5 + populated Plans block for Phase 2"
      contains: "302 → /login"
  key_links:
    - from: "ROADMAP.md Phase 2 success criterion #1"
      to: "CONTEXT D-01 (post-setup behavior)"
      via: "wording correction from '404s' to '302 → /login'"
      pattern: "302 → /login"
    - from: "ROADMAP.md Phase 2 success criterion #5"
      to: "CONTEXT D-03 (auto-login on successful setup)"
      via: "wording correction from 'login' to 'auto-login'"
      pattern: "auto-login"
---

<objective>
Document-only update. The ROADMAP was written before CONTEXT D-01 / D-03 / D-15 locked the actual Phase 2 behavior; the wording disagreement between the ROADMAP and CONTEXT was explicitly called out by CONTEXT as a plan-phase task ("Update ROADMAP success criterion #1 wording during plan-phase to match" — D-01; "ROADMAP success criterion #5 is amended at plan-phase to reflect the auto-login flow" — D-03).

Purpose: A ROADMAP that contradicts the CONTEXT is a future-execution trap — a checker or executor reading only the ROADMAP would expect the wrong behavior. This plan closes the loop. It also fills in the `Plans: TBD` placeholder for Phase 2 with the actual 11-plan list, matching the format used in Phases 0 + 1.

Output: One file modified (`.planning/ROADMAP.md`); one commit. No code, no tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-auth/02-CONTEXT.md
</context>

<files_to_read>
- `.planning/ROADMAP.md` lines 94-105 — Phase 2 section as currently written.
- `.planning/phases/02-auth/02-CONTEXT.md` §D-01 ("Update ROADMAP success criterion #1 wording during plan-phase to match"), §D-03 ("ROADMAP success criterion #5 ('setup → login → see /') is amended at plan-phase to reflect the auto-login flow").
- The Phase 1 entry in ROADMAP (lines 69-92) — as a format reference for the populated "Plans:" block.
</files_to_read>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Amend ROADMAP Phase 2 success criteria #1 and #5; populate Plans list</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Three edits inside the `### Phase 2: Auth` block.

EDIT 1 — Success criterion #1 (line 99 of current ROADMAP):

BEFORE:
```
  1. With zero users, `/setup` accepts a username + password and creates an admin user; concurrent `/setup` POSTs to a brand-new instance produce exactly one user row because the route holds a `SELECT … FOR UPDATE` on an `app_settings.setup_completed` row; once true, the route 404s.
```

AFTER:
```
  1. With zero users, `/setup` accepts a username + password and creates an admin user; concurrent `/setup` POSTs to a brand-new instance produce exactly one user row because the route holds a `SELECT … FOR UPDATE` on an `app_settings.setup_completed` row; once true, the route responds `302 → /login` (per CONTEXT D-01: friendlier than 404, zero info-leak since the route is read-only post-setup).
```

EDIT 2 — Success criterion #5 (line 103 of current ROADMAP):

BEFORE:
```
  5. Smoke pass: cold container → `/setup` → `/login` → see a stub `/` page that prints the logged-in username.
```

AFTER:
```
  5. Smoke pass: cold container → `/setup` → auto-login → see a stub `/` page that prints "Signed in as <username>" in the footer (per CONTEXT D-03: setup's happy path auto-logs in via session-ID regen + 303 → /, skipping a separate /login step).
```

EDIT 3 — Populate the "Plans:" placeholder (line 104 of current ROADMAP says `**Plans:** TBD`):

BEFORE:
```
**Plans:** TBD
```

AFTER:
```
**Plans:** 11 plans
Plans:
- [ ] 02-01-PLAN.md — Wave 0 conftest fixtures (async_client, fresh_db, seeded_admin/regular_user) + tests/dependencies package marker
- [ ] 02-02-PLAN.md — app/services/auth.py: PasswordHasher singleton + hash_password + verify_password + dummy_verify (AUTH-04 + AUTH-03 user-not-found timing defense)
- [ ] 02-03-PLAN.md — app/dependencies package: get_async_session + require_user + require_admin (AUTH-09 module location)
- [ ] 02-04-PLAN.md — app/csrf.py: CSRFFormFieldShim ASGI middleware (D-15) + 5 test cases
- [ ] 02-05-PLAN.md — app/services/setup.py: create_first_admin FOR UPDATE transaction (AUTH-02)
- [ ] 02-06-PLAN.md — app/middleware/session.py: D-09 User-row load + D-10 fail-closed
- [ ] 02-07-PLAN.md — app/routers/auth.py real /setup + /login + /logout + templates + schemas + replaces test_auth_stub.py (AUTH-01, AUTH-03, AUTH-06, AUTH-07)
- [ ] 02-08-PLAN.md — app/routers/admin.py + /admin template (D-13, AUTH-09)
- [ ] 02-09-PLAN.md — /debug/proxy admin-gate wrap + test_debug_proxy extension (D-14, AUTH-09)
- [ ] 02-10-PLAN.md — app/main.py wire CSRFFormFieldShim + admin router; pages/index.html footer; tests/test_phase02_smoke.py; D-15 logging assertions on real /login handler
- [ ] 02-11-PLAN.md — ROADMAP doc amendments per D-01 / D-03 + populate Plans list (this plan)
```

CRITICAL — preserve the rest of the Phase 2 block:
- The Goal line at the top (line 95) — unchanged.
- Success criteria #2, #3, #4 — unchanged.
- The `**Notes:**` line at the bottom (line 105) — unchanged.

NO other phases in ROADMAP.md are touched. This is a surgical edit to Phase 2 only.

Sanity-check: after the edit, the Phase 2 success-criteria block should still have exactly 5 numbered items, and the file's overall structure (Phase 0 through Phase 12) is intact.
  </action>
  <verify>
    <automated>grep -c "302 → /login" .planning/ROADMAP.md && grep -c "auto-login → see" .planning/ROADMAP.md && grep -c "02-01-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>The three grep counts are all ≥ 1. The "402 → /login" string appears in success criterion #1; "auto-login → see" appears in #5; "02-01-PLAN.md" appears in the populated Plans list.</done>
</task>

</tasks>

<verification>
This plan does NOT introduce new tests or code; the verification is purely text-based:

```
# All three strings present in ROADMAP after edit
grep -n "302 → /login" .planning/ROADMAP.md
grep -n "auto-login → see" .planning/ROADMAP.md
grep -n "02-01-PLAN.md" .planning/ROADMAP.md
```

Cross-check against CONTEXT:
- D-01 → ROADMAP SC #1 wording matches "302 → /login".
- D-03 → ROADMAP SC #5 wording matches "auto-login → see /".

No test row in 02-VALIDATION.md maps here — this is a documentation amendment requested directly by CONTEXT D-01 and D-03.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROADMAP wording → future executor's expectations | A stale ROADMAP would mislead future executors about the locked Phase 2 behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11-01 | Information Disclosure | (N/A — doc change only; no security threat) | accept | Documentation amendment carries no STRIDE risk. The amendment SURFACES the locked behavior more clearly, reducing the risk of future misimplementation. |
</threat_model>

<success_criteria>
- `.planning/ROADMAP.md` Phase 2 block edited: SC #1 says "302 → /login" (not "404s"); SC #5 says "auto-login → see /" (not "setup → login → see /"); Plans block populated with the 11 plans.
- The rest of `.planning/ROADMAP.md` is BYTE-IDENTICAL outside the Phase 2 success-criteria + Plans paragraphs.
- Commit: `docs(02-11): amend Phase 2 success criteria #1 + #5 per D-01/D-03; populate Plans list`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-11-SUMMARY.md`.
</output>

<commit>
docs(02-11): amend Phase 2 success criteria per D-01/D-03; populate Plans list
</commit>

## PLAN COMPLETE
