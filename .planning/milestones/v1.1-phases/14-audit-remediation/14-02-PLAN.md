---
phase: 14-audit-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/ai_service.py
  - tests/services/test_ai_service.py
autonomous: true
requirements: [S1]
must_haves:
  truths:
    - "Private IPv4 hosts (10/8, 172.16/12, 192.168/16) are refused pre-connect — D-03, D-04"
    - "Loopback hosts (127.0.0.1, ::1, localhost) are refused — D-04"
    - "Link-local / cloud-metadata host 169.254.169.254 is refused — D-04"
    - "IPv4-mapped IPv6 (::ffff:169.254.169.254) is normalized then refused — D-04"
    - "Public URLs still pass validation (no false-block) — D-04"
    - "_fetch_page_text (paste-and-rank, any authenticated user) enforces the same gate — D-04"
    - "DNS failure (OSError) is treated as a rejection, not a crash — D-03"
    - "Existing scheme + redirect SSRF defenses still pass — D-04"
    - "Validation is pre-resolve on ALL resolved IPs, with an accepted sub-ms TOCTOU window (graded against the amended criterion) — D-05"
    - "S1 (security) lands on a feature branch (not direct-to-main) — D-10"
  artifacts:
    - path: "app/services/ai_service.py"
      provides: "Shared _assert_public_host SSRF gate reused by both fetchers"
      contains: "def _assert_public_host"
    - path: "tests/services/test_ai_service.py"
      provides: "SSRF private/loopback/link-local/mapped/public/dns/fetch tests"
      contains: "ssrf"
  key_links:
    - from: "app/services/ai_service.py::_verify_buy_url"
      to: "_assert_public_host(url)"
      via: "guard between scheme check and client.get"
      pattern: "_assert_public_host"
    - from: "app/services/ai_service.py::_fetch_page_text"
      to: "_assert_public_host(url)"
      via: "guard between scheme check and client.get"
      pattern: "_assert_public_host"
    - from: "app/services/ai_service.py::_assert_public_host"
      to: "socket.getaddrinfo + ipaddress classification"
      via: "resolve-validate"
      pattern: "getaddrinfo"
---

<objective>
Fix S1 (HIGH) — SSRF: `_verify_buy_url` (`ai_service.py:157`) and `_fetch_page_text` (`:1466`) currently accept any `https://` URL whose host resolves to a private/internal address. `_fetch_page_text` is reachable by any authenticated user via paste-and-rank. Add a resolve-validate gate per D-03/D-04/D-05: resolve the host with `socket.getaddrinfo`, classify every resolved IP with stdlib `ipaddress`, and reject if any is private/loopback/link-local/reserved — handling IPv4-mapped IPv6 by normalizing first.

Purpose: Block SSRF to private subnets and the cloud-metadata endpoint (169.254.169.254) before any network connection, while extending — not rewriting — the existing scheme-allowlist / no-redirect / 5s-timeout / Range-cap scaffolding.
Output: A single DRY helper `_assert_public_host(url) -> bool` called by both fetchers, plus the first private-IP/DNS SSRF test suite.

Grade against the AMENDED criterion (D-05): "block on pre-resolve validation of all resolved IPs," accepted sub-ms TOCTOU window at household scale — NOT the literal "pinned IP" wording in ROADMAP success criterion #2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-remediation/14-CONTEXT.md
@.planning/phases/14-audit-remediation/14-RESEARCH.md
@.planning/phases/14-audit-remediation/14-PATTERNS.md
@.planning/phases/14-audit-remediation/14-VALIDATION.md

<interfaces>
<!-- Existing fetchers — extend, do NOT rewrite -->
<!-- ai_service.py:157  async def _verify_buy_url(url, roaster_name, coffee_name) -> bool  (returns False on guard fail) -->
<!-- ai_service.py:1466 async def _fetch_page_text(url) -> str                            (returns "" on guard fail) -->
<!-- Both: scheme check (startswith "https://") -> httpx.AsyncClient(follow_redirects=False, timeout=5.0) -> client.get(..., headers Range) -->

<!-- stdlib classification (Python 3.12): -->
<!-- ipaddress.is_private covers 10/8, 172.16/12, 192.168/16, fc00::/7 -->
<!-- ipaddress.is_loopback covers 127/8, ::1 ; is_link_local covers 169.254/16, fe80::/10 ; is_reserved covers special ranges -->
<!-- IPv6Address.ipv4_mapped returns an IPv4Address for ::ffff:... else None -->

<!-- Imports currently present at ai_service.py top (NO socket/ipaddress/urlparse yet) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _assert_public_host helper and wire both fetchers (S1)</name>
  <files>app/services/ai_service.py</files>
  <read_first>
    - app/services/ai_service.py top-of-file imports (confirm socket/ipaddress/urlparse are absent — they must be added)
    - app/services/ai_service.py:157-202 (_verify_buy_url scaffolding + insertion point after scheme check)
    - app/services/ai_service.py:1466-1505 (_fetch_page_text scaffolding + insertion point after scheme check)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "S1 — Add _assert_public_host helper + insert calls" (verbatim helper + exact insertion lines + failure-return convention)
    - .planning/phases/14-audit-remediation/14-RESEARCH.md § "S1: SSRF resolve-validate pattern" + Pitfall 2 (IPv4-mapped) + Pitfall 7 (getaddrinfo OSError)
  </read_first>
  <action>
    Add `import ipaddress`, `import socket`, and `from urllib.parse import urlparse` to the existing top-of-file import block (alphabetize within the stdlib group per ruff). Add a module-level `def _assert_public_host(url: str) -> bool` (sync — runs before any await) near the SSRF helpers (after `_verify_buy_url` is cleanest). It must: parse the host via `urlparse(url).hostname` (return False if falsy); call `socket.getaddrinfo(host, None)` inside `try/except OSError: return False` (DNS failure → reject, Pitfall 7); for every `(family, type, proto, canon, sockaddr)` entry, build `ipaddress.ip_address(sockaddr[0])` (return False on `ValueError`), normalize IPv4-mapped IPv6 (`if isinstance(addr, ipaddress.IPv6Address) and addr.ipv4_mapped is not None: addr = addr.ipv4_mapped` — Pitfall 2, critical for `::ffff:169.254.169.254`), then return False if `addr.is_private or addr.is_loopback or addr.is_link_local or addr.is_reserved`; return True only if every resolved IP is public.
    Wire it into BOTH fetchers between the existing scheme check and the `httpx.AsyncClient`/`client.get` block, using each function's existing failure-return convention: in `_verify_buy_url` add `if not _assert_public_host(url): return False`; in `_fetch_page_text` add `if not _assert_public_host(url): return ""`. Do NOT touch the scheme allowlist, `follow_redirects=False`, the 5s timeout, the Range cap, or the broad `except (httpx.TimeoutException, httpx.RequestError)` — extend only.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/services/test_ai_service.py" -k "ssrf_redirect or scheme_rejected" -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/ai_service.py` contains `def _assert_public_host(`, `getaddrinfo`, and `ipv4_mapped`.
    - `app/services/ai_service.py` contains `import socket`, `import ipaddress`, and `from urllib.parse import urlparse`.
    - `_assert_public_host` is called inside both `_verify_buy_url` (returns False) and `_fetch_page_text` (returns "") — `grep -c "_assert_public_host" app/services/ai_service.py` returns >= 3 (1 def + 2 calls).
    - The existing scheme + redirect SSRF tests still pass: `pytest tests/services/test_ai_service.py -k "ssrf_redirect or scheme_rejected" -x` exits 0.
    - `ruff check app/services/ai_service.py` exits 0 (imports used, alphabetized).
  </acceptance_criteria>
  <done>One DRY `_assert_public_host` gate added and wired into both fetchers ahead of the network call; existing SSRF defenses still green; imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author Wave 0 SSRF private-IP test suite (S1)</name>
  <files>tests/services/test_ai_service.py</files>
  <read_first>
    - tests/services/test_ai_service.py:192-216 (existing scheme + redirect test shapes; @pytest.mark.asyncio; @respx.mock)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "tests/services/test_ai_service.py — S1 SSRF private-IP tests" (unittest.mock.patch("socket.getaddrinfo") shapes — no respx needed because the gate fires before httpx)
    - .planning/phases/14-audit-remediation/14-VALIDATION.md (the S1 test rows + Wave 0 list mapping each named test to a -k token)
  </read_first>
  <action>
    Add the SSRF private-IP suite to `tests/services/test_ai_service.py`, mocking `socket.getaddrinfo` via `unittest.mock.patch("socket.getaddrinfo", return_value=...)` (respx is NOT needed — the gate runs before any httpx call; do not add respx mocks to these). Each test calls `_verify_buy_url("https://example.com/coffee", "Roaster", "Coffee")` (or `_fetch_page_text` for the fetch test) under a mocked resolution and asserts the rejection sentinel. Author exactly (names must match the VALIDATION -k tokens):
    - `test_ssrf_private_ipv4_blocked` — getaddrinfo → 10.0.0.1 (and/or 172.16.0.1, 192.168.0.1); assert False.
    - `test_ssrf_loopback_blocked` — 127.0.0.1 and ::1; assert False. (Optionally a real-DNS `test_ssrf_localhost_blocked` resolving "localhost"; if flaky in the container, keep only the mocked loopback test — VALIDATION accepts either `ssrf_loopback or ssrf_localhost`.)
    - `test_ssrf_link_local_blocked` — 169.254.169.254 (metadata endpoint); assert False. (This also satisfies the `ssrf_metadata` token.)
    - `test_ssrf_ipv4_mapped_ipv6_blocked` — AF_INET6 sockaddr `('::ffff:169.254.169.254', 443, 0, 0)`; assert False (proves normalization).
    - `test_ssrf_public_url_allowed` — getaddrinfo → a public IP (e.g. 93.184.216.34); for `_verify_buy_url`, mock the downstream httpx GET with respx so the call can proceed past the gate, OR assert via `_assert_public_host(...) is True` directly to prove no false-block. Choose the assertion that keeps the test deterministic and document the choice in the test docstring.
    - `test_fetch_page_ssrf_private_blocked` — same gate proven on `_fetch_page_text` (assert `== ""`).
    - `test_ssrf_dns_failure_blocked` — patch getaddrinfo to raise `socket.gaierror`; assert the rejection sentinel (OSError → reject, no crash).
    Author the tests so they FAIL against pre-Task-1 code where feasible (RED), then pass after Task 1 (GREEN). Do NOT modify the existing scheme/redirect tests.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/services/test_ai_service.py" -k "ssrf_private or ssrf_loopback or ssrf_link_local or ssrf_metadata or ssrf_ipv4_mapped or ssrf_public_allowed or fetch_page_ssrf or ssrf_dns_fail" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/services/test_ai_service.py` contains `test_ssrf_private_ipv4_blocked`, `test_ssrf_loopback_blocked`, `test_ssrf_link_local_blocked`, `test_ssrf_ipv4_mapped_ipv6_blocked`, `test_ssrf_public_url_allowed`, `test_fetch_page_ssrf_private_blocked`, and `test_ssrf_dns_failure_blocked`.
    - `pytest tests/services/test_ai_service.py -k "ssrf_link_local or ssrf_metadata" -x` exits 0.
    - `pytest tests/services/test_ai_service.py -k "ssrf_ipv4_mapped" -x` exits 0.
    - `pytest tests/services/test_ai_service.py -k "fetch_page_ssrf" -x` exits 0.
    - `pytest tests/services/test_ai_service.py -k "ssrf_public_allowed" -x` exits 0 (no false-block).
    - `ruff check tests/services/test_ai_service.py` exits 0 AND `ruff format --check tests/services/test_ai_service.py` exits 0.
  </acceptance_criteria>
  <done>Seven SSRF tests cover private/loopback/link-local+metadata/IPv4-mapped/public/fetch-page/DNS-failure, all green against the Task-1 gate; existing SSRF tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → paste-and-rank (`_fetch_page_text`) | Any logged-in household user supplies an arbitrary URL the server fetches server-side |
| AI-recommended buy URL → `_verify_buy_url` | An LLM-produced URL is fetched server-side to verify the product |
| server → resolved IP (egress) | The server's outbound request can reach private subnets / cloud-metadata if the host resolves internally |

## STRIDE Threat Register (ASVS V10 Web Services — SSRF; block on HIGH)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SSRF-priv | Information Disclosure | `_assert_public_host` over both fetchers | mitigate | `ipaddress.is_private` rejects 10/8, 172.16/12, 192.168/16, fc00::/7 pre-connect. Tested by `test_ssrf_private_ipv4_blocked` (VALIDATION S1 private). |
| T-SSRF-lo | Information Disclosure | both fetchers | mitigate | `ipaddress.is_loopback` rejects 127/8 and ::1. Tested by `test_ssrf_loopback_blocked` (VALIDATION S1 loopback). |
| T-SSRF-meta | Elevation of Privilege | both fetchers | mitigate (HIGH) | `ipaddress.is_link_local` rejects 169.254.0.0/16 incl. the cloud-metadata endpoint 169.254.169.254. Tested by `test_ssrf_link_local_blocked` (VALIDATION S1 link_local/metadata). |
| T-SSRF-mapped | Elevation of Privilege | `_assert_public_host` normalization | mitigate (HIGH) | `IPv6Address.ipv4_mapped` normalization reclassifies `::ffff:169.254.169.254` before checking — without it the metadata endpoint bypasses as "global" IPv6. Tested by `test_ssrf_ipv4_mapped_ipv6_blocked` (VALIDATION S1 ipv4_mapped). |
| T-SSRF-fetch | Information Disclosure | `_fetch_page_text` (auth-reachable) | mitigate | Same gate applied to the paste-and-rank fetcher reachable by any authenticated user. Tested by `test_fetch_page_ssrf_private_blocked` (VALIDATION S1 fetch). |
| T-SSRF-dns | Denial of Service / crash | `_assert_public_host` OSError handling | mitigate | `try/except OSError` on `getaddrinfo` treats unresolvable hosts as rejection instead of letting the exception escape the fetcher. Tested by `test_ssrf_dns_failure_blocked` (VALIDATION S1 dns_fail). |
| T-SSRF-pub | Availability (false-block) | `_assert_public_host` | mitigate | Public hosts must still pass — `test_ssrf_public_url_allowed` guards against over-blocking (VALIDATION S1 public). |
| T-SSRF-toctou | TOCTOU / DNS-rebinding | resolve-vs-connect gap | accept | D-05: pre-resolve validation only; sub-ms TOCTOU / DNS-rebinding window accepted at household scale (both fetchers are authenticated-only; 2-person household). Documented in `_assert_public_host` docstring. |
| T-SSRF-base | (regression) | existing scheme + no-redirect defenses | mitigate | Existing non-https rejection and `follow_redirects=False` retained; `test_ssrf_redirect`/`scheme_rejected` still pass (VALIDATION S1 base). |
</threat_model>

<verification>
- Quick run (the 4 phase-touched test files) green after the plan:
  `docker compose exec coffee-snobbery python -m pytest tests/test_scheduler.py tests/phase_09/test_admin_users.py tests/services/test_ai_service.py tests/test_search.py -q`
- `ruff check app/ tests/` exits 0 AND `ruff format --check app/ tests/` exits 0 (CI gates BOTH).
- No new dependency added (stdlib socket/ipaddress/urlparse only); httpx scaffolding unchanged.
</verification>

<success_criteria>
- ROADMAP Phase 14 success criterion #2 — as AMENDED by D-05 — is satisfied: both fetchers reject hosts resolving to private/loopback/link-local/reserved addresses (incl. 169.254.169.254 and its IPv4-mapped form) via pre-resolve validation; public URLs still work; the existing scheme + no-redirect defenses remain.
- `ruff check` and `ruff format --check` both clean on `app/` and `tests/`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-remediation/14-02-SUMMARY.md`. Note the D-05 amended-criterion grading and the accepted-TOCTOU caveat documented in the helper docstring; record how `test_ssrf_public_url_allowed` was made deterministic.
</output>
