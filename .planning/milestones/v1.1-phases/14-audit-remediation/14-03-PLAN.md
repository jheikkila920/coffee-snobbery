---
phase: 14-audit-remediation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/scheduler.py
  - tests/test_scheduler.py
autonomous: true
requirements: [B2]
must_haves:
  truths:
    - "A nightly APScheduler job sweeps expired sessions: DELETE FROM sessions WHERE expires_at < now() — D-06"
    - "The sweep job is registered with stable id nightly_session_sweep, replace_existing=True (idempotent) — D-06"
    - "The sweep runs at 03:00 APP_TIMEZONE (after 02:00 backup) — D-06"
    - "After registration there are exactly 3 jobs (ai_refresh, backup, session_sweep) — D-06"
    - "Expired session rows are deleted; unexpired rows are retained — D-06"
    - "The deferred TODO at sessions.py:182-185 is conceptually closed (scheduler-only, no admin button) — D-06"
  artifacts:
    - path: "app/services/scheduler.py"
      provides: "run_nightly_session_sweep job + registration in register_jobs"
      contains: "nightly_session_sweep"
    - path: "tests/test_scheduler.py"
      provides: "sweep deletes-expired/retains-unexpired tests + updated idempotency assertion (3 jobs)"
      contains: "session_sweep"
  key_links:
    - from: "app/services/scheduler.py::register_jobs"
      to: "run_nightly_session_sweep via CronTrigger(hour=3)"
      via: "target.add_job(..., id='nightly_session_sweep', replace_existing=True)"
      pattern: "nightly_session_sweep"
    - from: "app/services/scheduler.py::run_nightly_session_sweep"
      to: "sessions table DELETE WHERE expires_at < now()"
      via: "sql_delete(SessionModel).where(SessionModel.expires_at < func.now())"
      pattern: "expires_at"
---

<objective>
Fix B2 (MEDIUM) — there is no nightly expired-session sweep (deferred TODO at `sessions.py:182-185`). Add one nightly APScheduler job in `app/services/scheduler.py` running `DELETE FROM sessions WHERE expires_at < now()`, scheduled at 03:00 `APP_TIMEZONE` (clean low-traffic slot after the 02:00 backup), with stable id `nightly_session_sweep` and `replace_existing=True` (idempotent like the existing two jobs → 3 total). Scheduler-only — no admin "Sweep now" button (D-06; the button is an explicitly deferred idea).

Purpose: Stop expired session rows from accumulating indefinitely; the `expires_at` btree index (migration `p1_sessions`) makes the DELETE an index scan.
Output: A new `run_nightly_session_sweep` job body + its registration, plus sweep tests and an updated idempotency assertion (2→3 jobs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-remediation/14-CONTEXT.md
@.planning/phases/14-audit-remediation/14-RESEARCH.md
@.planning/phases/14-audit-remediation/14-PATTERNS.md
@.planning/phases/14-audit-remediation/14-VALIDATION.md

<interfaces>
<!-- scheduler.py: func already imported (line 42 `from sqlalchemy import func, select, text`); SessionLocal at line 46. -->
<!-- SessionModel NOT imported in scheduler.py — lazy-import inside the job body (mirror run_nightly_backup's lazy `from app.services.backup import run_backup`). -->
<!-- register_jobs at line 103; run_nightly_backup at line 340 (the lazy-import + log start/success/error pattern to clone). -->
<!-- Existing add_job pattern: target.add_job(fn, CronTrigger(hour=, minute=, timezone=settings.APP_TIMEZONE), id=, replace_existing=True). -->
<!-- Event constants: SCHEDULER_JOB_START / SCHEDULER_JOB_SUCCESS / SCHEDULER_JOB_ERROR already used by the other jobs. -->
<!-- Session model: app/models/session.py — expires_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, index=True). -->
<!-- Existing idempotency test: tests/test_scheduler.py::test_idempotent_job_registration — asserts len(jobs)==2 (line 49) and the 2-id set (line 51). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run_nightly_session_sweep job + register it (B2)</name>
  <files>app/services/scheduler.py</files>
  <read_first>
    - app/services/scheduler.py:42-46 (imports — func + SessionLocal already present; confirm)
    - app/services/scheduler.py:103-127 (register_jobs — the two existing add_job calls)
    - app/services/scheduler.py:340-360 (run_nightly_backup — lazy-import + log start/success/error pattern to clone)
    - app/models/session.py (Session model + expires_at column)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "B2 — Add nightly_session_sweep job" (verbatim job body + add_job snippet)
    - .planning/phases/14-audit-remediation/14-RESEARCH.md § "B2" + Pitfall 5 (use `<` not `<=`, use `expires_at` not `last_seen`)
  </read_first>
  <action>
    Add a module-level `def run_nightly_session_sweep() -> None` (plain sync def — runs in the threadpool like the other jobs), placed after `run_nightly_backup`. It must: log `SCHEDULER_JOB_START` with `job_id="nightly_session_sweep"`; lazy-import `from sqlalchemy import delete as sql_delete` and `from app.models.session import Session as SessionModel` inside the body (mirror the backup lazy-import to avoid any import-cycle risk); open `with SessionLocal() as db:` and execute `sql_delete(SessionModel).where(SessionModel.expires_at < func.now())` then `db.commit()` (use strict `<` and the `expires_at` column — Pitfall 5); log `SCHEDULER_JOB_SUCCESS`; on `except Exception as exc` log `SCHEDULER_JOB_ERROR` with `job_id`, `error_class=type(exc).__name__`, `error_msg=str(exc)` and `raise`.
    In `register_jobs`, after the existing two `add_job` calls, add a third: `target.add_job(run_nightly_session_sweep, CronTrigger(hour=3, minute=0, timezone=settings.APP_TIMEZONE), id="nightly_session_sweep", replace_existing=True)`. Do NOT alter the existing two jobs or their schedules. Do NOT add any admin endpoint/button (D-06 — deferred).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/test_scheduler.py::test_idempotent_job_registration" -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/scheduler.py` contains `def run_nightly_session_sweep(` and `id="nightly_session_sweep"` and `replace_existing=True` on the new add_job.
    - `app/services/scheduler.py` contains `CronTrigger(hour=3, minute=0` for the sweep.
    - The sweep uses `SessionModel.expires_at < func.now()` (strict `<`, `expires_at` column) — `grep -c "expires_at" app/services/scheduler.py` returns >= 1.
    - `ruff check app/services/scheduler.py` exits 0 (lazy imports used, no unused module-level imports introduced).
  </acceptance_criteria>
  <done>`run_nightly_session_sweep` job added and registered at 03:00 with a stable idempotent id; existing jobs untouched; no admin button.</done>
</task>

<task type="auto">
  <name>Task 2: Update idempotency assertion + author sweep tests (B2)</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - tests/test_scheduler.py:22-53 (test_idempotent_job_registration — assertions at lines 49 + 51 to update)
    - tests/test_scheduler.py:88-159 (test_eligibility_filter — the sync_db seed/call/assert shape to mirror)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "tests/test_scheduler.py — B2 job count update + sweep tests" (note: the job opens its own SessionLocal, so seed+commit, call job, then query in a new session)
    - .planning/phases/14-audit-remediation/14-VALIDATION.md (B2 rows + Wave 0 list)
  </read_first>
  <action>
    Update `test_idempotent_job_registration`: change `assert len(jobs) == 2` to `assert len(jobs) == 3` and the expected set to `{"nightly_ai_refresh", "nightly_backup", "nightly_session_sweep"}` (Pitfall 3 — both must change together).
    Add two sweep tests mirroring the `test_eligibility_filter` shape (skip cleanly when `sync_db is None`):
    - `test_session_sweep_deletes_expired` — seed one expired Session (`expires_at` in the past) + one unexpired (`expires_at` in the future) via `sync_db`, commit, call `run_nightly_session_sweep()` (it opens its OWN `SessionLocal()` internally — do NOT rely on the fixture's open transaction for the deleted rows), then query in a fresh session and assert the expired row is gone.
    - `test_session_sweep_retains_unexpired` — assert the unexpired row survived the sweep (can be a sibling assertion in the same seed or a separate test).
    Seed Session rows with all required NOT NULL columns (read `app/models/session.py` for the schema — `user_id`, `session_id`/token, `expires_at`, etc.); a seeded user may be required as the FK target (reuse the existing user-seeding helper/fixture pattern in the test module or conftest).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/test_scheduler.py" -k "session_sweep or idempotent_job_registration" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_scheduler.py::test_idempotent_job_registration` asserts `len(jobs) == 3` and the expected set includes `"nightly_session_sweep"`.
    - `tests/test_scheduler.py` contains `test_session_sweep_deletes_expired` and `test_session_sweep_retains_unexpired`.
    - `pytest tests/test_scheduler.py::test_idempotent_job_registration -x` exits 0 (3 jobs).
    - `pytest tests/test_scheduler.py -k "session_sweep" -x` exits 0.
    - `ruff check tests/test_scheduler.py` exits 0 AND `ruff format --check tests/test_scheduler.py` exits 0.
  </acceptance_criteria>
  <done>Idempotency assertion updated to 3 jobs; sweep tests prove expired rows deleted and unexpired rows retained; all scheduler tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale session rows → sessions table | Expired but undeleted session rows accumulate; an authoritative expiry must be enforced server-side over time |

## STRIDE Threat Register (ASVS V3 Session Management)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V3-01 | Information Disclosure / hygiene | sessions table growth | mitigate | Nightly `DELETE FROM sessions WHERE expires_at < now()` removes expired rows on a schedule, closing the deferred TODO. `expires_at` is the authoritative expiry (not `last_seen`); strict `<` avoids deleting just-expiring rows. Tested by `test_session_sweep_deletes_expired` (VALIDATION B2 deleted) and `test_session_sweep_retains_unexpired` (VALIDATION B2 retained). |
| T-V3-02 | Tampering (job dup) | scheduler job registration | mitigate | Stable id `nightly_session_sweep` + `replace_existing=True` keeps registration idempotent across container restarts (3 jobs total, never duplicated). Tested by `test_idempotent_job_registration` (VALIDATION B2 3-jobs). |
| T-V3-03 | (excluded scope creep) | admin "Sweep now" button | accept | Deferred per D-06 — stale sessions are harmless until swept; a manual endpoint would add CSRF + UI surface for no household-scale benefit. |
</threat_model>

<verification>
- Quick run (the 4 phase-touched test files) green after the plan:
  `docker compose exec coffee-snobbery python -m pytest tests/test_scheduler.py tests/phase_09/test_admin_users.py tests/services/test_ai_service.py tests/test_search.py -q`
- `ruff check app/ tests/` exits 0 AND `ruff format --check app/ tests/` exits 0 (CI gates BOTH).
- No schema migration introduced (the `expires_at` index already exists); no AI-scheduling logic changed (only a new independent job added).
</verification>

<success_criteria>
- ROADMAP Phase 14 success criterion #3 (nightly sweep job; stable idempotent id; 3 jobs; expired rows deleted, unexpired retained; TODO closed) is satisfied.
- `ruff check` and `ruff format --check` both clean on `app/` and `tests/`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-remediation/14-03-SUMMARY.md`. Note the lazy-import choice for `SessionModel`/`sql_delete` and that the job opens its own `SessionLocal` (test must query in a fresh session). Optionally note the `sessions.py:182-185` TODO can be marked closed/removed if touched.
</output>
