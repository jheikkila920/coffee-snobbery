---
phase: 06-analytics-home-page
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - app/routers/home.py
  - app/templates/fragments/home/top_coffees.html
  - app/templates/fragments/home/preference_profile.html
  - app/templates/fragments/home/flavor_descriptors.html
  - app/templates/fragments/home/roast_freshness.html
  - app/templates/fragments/home/sweet_spots.html
  - app/templates/fragments/home/_card_sparse.html
  - tests/routers/test_home.py
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-09]
must_haves:
  truths:
    - "The five aggregate-card fragment endpoints (top-coffees, preference-profile, flavor-descriptors, roast-freshness, sweet-spots) render their SQL-derived card bodies"
    - "Every aggregate card renders-with-hint when its query returns no rows — the card stays in place (D-04)"
    - "Rating-dependent cards show a distinct 'rate your brews' nudge when the user has sessions but none rated (D-05)"
    - "Each fragment endpoint is gated by require_user and scoped to request.state.user.id"
    - "HOME-04 displays buckets derived from bags.roast_date; HOME-05 displays the (origin×process×brewer×recipe) sweet spots"
  artifacts:
    - path: "app/templates/fragments/home/sweet_spots.html"
      provides: "HOME-05 sweet-spots card body + sparse hint"
    - path: "app/templates/fragments/home/_card_sparse.html"
      provides: "Shared sparse/all-unrated hint partial (D-04/D-05)"
    - path: "app/routers/home.py"
      provides: "Five aggregate-card fragment endpoints (extends Plan 06-02's router)"
      contains: "card_sweet_spots"
  key_links:
    - from: "app/routers/home.py"
      to: "app/services/analytics.py"
      via: "per-card query call scoped to user.id"
      pattern: "analytics\\.get_(top_coffees|sweet_spots|flavor_descriptors)"
    - from: "aggregate card templates"
      to: "_card_sparse.html"
      via: "include when rows empty, passing hint_type"
      pattern: "_card_sparse.html"
---

<objective>
Implement the five aggregate-card fragment endpoints in `app/routers/home.py` (extending the router Plan 06-02 created) and their Jinja2 card templates, plus the shared sparse-card partial. These are the lazy-loaded slots the shell wired in Plan 06-02: top coffees (HOME-01), preference profile (HOME-02), flavor descriptors (HOME-03), roast freshness (HOME-04), sweet spots (HOME-05). Each card renders-with-hint when empty (D-04), and rating-dependent cards distinguish the all-unrated case with a "rate your brews" nudge (D-05).

Purpose: Complete the analytics surface. Plan 06-01 built the queries; Plan 06-02 built the shell + gate + the two always-on cards; this plan wires the five gated aggregate cards behind the lazy slots and renders their bodies + empty states.

Output: five fragment endpoints, five card templates, `_card_sparse.html`, and additional router smoke tests appended to `tests/routers/test_home.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-home-page/06-CONTEXT.md
@.planning/phases/06-analytics-home-page/06-RESEARCH.md
@.planning/phases/06-analytics-home-page/06-PATTERNS.md
@.planning/phases/06-analytics-home-page/06-UI-SPEC.md
@app/services/analytics.py
@app/routers/home.py

<interfaces>
<!-- The analytics service these endpoints consume (from Plan 06-01). -->
analytics.get_top_coffees(db, user_id) -> list[Row]            # (id, name, avg_rating, session_count)
analytics.get_preference_profile(db, user_id) -> dict          # {origin:[...], process:[...], roaster:[...], roast_level:[...]}, each row (label, avg_rating, session_count)
analytics.get_flavor_descriptors(db, user_id) -> list[Row]     # (id, name, session_count) from rated 4.0+ sessions
analytics.get_roast_freshness_buckets(db, user_id) -> list[Row]# (freshness_bucket, avg_rating, session_count, bucket_order)
analytics.get_sweet_spots(db, user_id) -> list[Row]            # (origin, process, brewer_name, recipe_name, avg_rating, session_count)
analytics.get_cold_start_counts(db, user_id) -> dict           # used to detect the all-unrated case (sessions>0)

<!-- Router plumbing already established in Plan 06-02's home.py: -->
# from app.dependencies.auth import require_user
# from app.dependencies.db import get_session
# from app.services import analytics
# from app.templates_setup import templates
# from app.models.user import User
# FragmentCacheHeadersMiddleware applies no-store + Vary: HX-Request automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five aggregate-card fragment endpoints + all-unrated detection</name>
  <files>app/routers/home.py</files>
  <read_first>
    - app/routers/home.py (Plan 06-02 — the existing `card_recent_brews`/`card_unrated_coffees` handler shape to mirror; the imports already present)
    - app/services/analytics.py (Plan 06-01 — the exact return shapes/labels of the five functions + get_cold_start_counts)
    - .planning/phases/06-analytics-home-page/06-PATTERNS.md §"app/routers/home.py" (the `card_top_coffees` example handler) + §"Shared Patterns"
    - .planning/phases/06-analytics-home-page/06-UI-SPEC.md §"HTMX Fragment Endpoints" (route slugs) + §"Copywriting Contract" (the D-04/D-05 hint matrix)
    - .planning/phases/06-analytics-home-page/06-CONTEXT.md D-04/D-05 (render-with-hint + all-unrated nudge)
  </read_first>
  <action>
    Add five handlers to app/routers/home.py, each mirroring the Plan 06-02 fragment shape (`Depends(require_user)` + `Depends(get_session)` both `# noqa: B008`, `templates.TemplateResponse(request=request, ...)`), scoped to `user.id`:
      - `@router.get("/home/cards/top-coffees")` → `analytics.get_top_coffees(db, user.id)` → `fragments/home/top_coffees.html`.
      - `@router.get("/home/cards/preference-profile")` → `analytics.get_preference_profile(db, user.id)` → `fragments/home/preference_profile.html` (pass the dict as `profile`).
      - `@router.get("/home/cards/flavor-descriptors")` → `analytics.get_flavor_descriptors(db, user.id)` → `fragments/home/flavor_descriptors.html`.
      - `@router.get("/home/cards/roast-freshness")` → `analytics.get_roast_freshness_buckets(db, user.id)` → `fragments/home/roast_freshness.html`.
      - `@router.get("/home/cards/sweet-spots")` → `analytics.get_sweet_spots(db, user.id)` → `fragments/home/sweet_spots.html`.

    For the four RATING-DEPENDENT cards (top-coffees, preference-profile, flavor-descriptors, sweet-spots), detect the all-unrated case (D-05): when the card's query returns no qualifying rows AND the user has logged sessions, the empty card should show the "rate your brews" nudge instead of the generic sparse hint. Compute this cheaply — pass `all_unrated` into the template context as `True` when the query is empty and `analytics.get_cold_start_counts(db, user.id)["sessions"] > 0` and there are zero rated sessions. (Simplest: add a tiny helper in analytics OR reuse get_cold_start_counts plus a rated-count check. If a rated-count is needed, prefer a single `db.scalar(select(func.count()).where(user_id==..., rating.is_not(None)))` in the handler; do NOT loop.) Roast-freshness is NOT rating-gated for the nudge (its hint is always the generic "add roast dates" copy per UI-SPEC), so it never sets `all_unrated`.

    Each template receives the rows (or dict) plus `all_unrated: bool` so the template can pick the correct hint. Do not add audit logging.
  </action>
  <verify>
    <automated>docker compose cp app/ coffee-snobbery:/app/app/ && docker compose exec coffee-snobbery python -c "from app.main import create_app; app=create_app(); p={r.path for r in app.routes}; need={'/home/cards/top-coffees','/home/cards/preference-profile','/home/cards/flavor-descriptors','/home/cards/roast-freshness','/home/cards/sweet-spots'}; assert need<=p, need-p; print('endpoints ok')" && docker compose exec coffee-snobbery ruff check app/routers/home.py</automated>
  </verify>
  <acceptance_criteria>
    - app/routers/home.py defines handlers for all five aggregate-card routes, each with `Depends(require_user)` and `Depends(get_session)`
    - the four rating-dependent handlers pass an `all_unrated` boolean into their template context
    - the route-probe prints `endpoints ok`
    - `ruff check app/routers/home.py` exits 0
  </acceptance_criteria>
  <done>All five aggregate-card fragment endpoints resolve, are auth-gated and user-scoped, and pass all-unrated detection to rating-dependent templates.</done>
</task>

<task type="auto">
  <name>Task 2: Five card templates + shared sparse partial</name>
  <files>app/templates/fragments/home/top_coffees.html, app/templates/fragments/home/preference_profile.html, app/templates/fragments/home/flavor_descriptors.html, app/templates/fragments/home/roast_freshness.html, app/templates/fragments/home/sweet_spots.html, app/templates/fragments/home/_card_sparse.html</files>
  <read_first>
    - app/templates/fragments/home/recent_brews.html + unrated_coffees.html (Plan 06-02 — established card-body list shape, autoescape, `★` aria-hidden, tabular-nums)
    - app/templates/fragments/empty.html (analog for the shared sparse partial)
    - app/templates/fragments/session_list.html lines 29-84 (list/empty branching) + session_row.html (card div class, `★` pattern)
    - .planning/phases/06-analytics-home-page/06-UI-SPEC.md §"Component Inventory" (stat row, pill/badge), §"Copywriting Contract" (the EXACT generic + all-unrated hint strings per card), §"Accessibility Contract", §"Mobile-First Constraints" (preference profile = `md:grid-cols-2`)
    - .planning/phases/06-analytics-home-page/06-PATTERNS.md §"fragments/home/*" + §"_card_sparse.html"
  </read_first>
  <action>
    _card_sparse.html: a shared partial that takes `hint_type` ("sparse" | "unrated") and a `hint_text`/`unrated_text` (or per-card strings passed in). Render `<p class="text-sm text-espresso-600 dark:text-cream-300">{the chosen hint}</p>`. Keep it dumb — the caller decides which copy to pass based on `all_unrated`. (Alternatively the partial can branch on `hint_type` and the caller passes both strings; pick one approach and use it consistently across all five cards.)

    Each card template renders ONLY the card BODY (the outer `<section>` + `<h2>` heading already live in the shell from Plan 06-02 — these fragments swap into the inner `<div hx-swap="innerHTML">`). For each:
      - top_coffees.html: if `rows`, an `<ul class="space-y-2">` of stat-rows (coffee name + a pill/badge with session_count + `tabular-nums` avg rating with `★` aria-hidden). If empty: include `_card_sparse.html` with generic "No coffees with 2+ sessions yet — keep logging." or, when `all_unrated`, "Rate some brews to see your top coffees."
      - preference_profile.html: render the four dimensions (origin, process, roaster, roast level) as a `grid grid-cols-1 md:grid-cols-2 gap-4`; each dimension is a sub-block listing (label, avg rating, session_count) stat-rows. If the whole `profile` dict is empty across all dimensions: generic "Not enough sessions per dimension yet — keep logging." or all-unrated "Rate some brews to see your preference profile."
      - flavor_descriptors.html: if `rows`, render descriptor names as pills/badges (UI-SPEC pill) with their counts. If empty: "No flavor notes in 4.0+ rated sessions yet." or all-unrated "Rate some brews 4.0+ and add observed flavor notes to see top descriptors."
      - roast_freshness.html: if `rows`, a stat-row list of bucket label → avg rating (`tabular-nums`) + session_count, in bucket order. If empty: ALWAYS the generic "No sessions with a bag roast date yet — add roast dates to your bags." (no all-unrated variant — UI-SPEC marks it n/a).
      - sweet_spots.html: if `rows`, list the top 3 as stat-rows showing "{origin} · {process} · {brewer_name} · {recipe_name}" + avg rating + session_count. If empty: "Not enough sessions per combination yet (need 3 per match)." or all-unrated "Rate some brews to find your sweet spots." Per UI-SPEC §"AI Prose Placeholder", the card ends after the SQL-derived list — NO "AI insights coming soon" placeholder (HOME-06 is Phase 7).

    ALL templates: 2-space indent, snake_case vars, NO `|safe`, NO `hx-on:`, NO `hx-vals='js:'`, autoescape every value, `tabular-nums` on numerics, `★` decorative chars carry `aria-hidden="true"` with the numeric value adjacent. Single column at 375px (only preference profile uses `md:grid-cols-2`). Use the exact UI-SPEC copy strings verbatim.
  </action>
  <verify>
    <automated>docker compose cp app/ coffee-snobbery:/app/app/ && docker compose exec coffee-snobbery python -c "import re,glob; files=glob.glob('app/templates/fragments/home/*.html'); bad=[f for f in files if re.search(r'\|\s*safe|hx-on:|hx-vals=.js:', open(f,encoding='utf-8').read())]; assert not bad, bad; import os; need={'top_coffees.html','preference_profile.html','flavor_descriptors.html','roast_freshness.html','sweet_spots.html','_card_sparse.html'}; have={os.path.basename(f) for f in files}; assert need<=have, need-have; print('cards ok')"</automated>
  </verify>
  <acceptance_criteria>
    - all six template files exist under app/templates/fragments/home/
    - each of the five card templates includes `_card_sparse.html` (or renders an equivalent hint) on the empty branch
    - sweet_spots.html contains NO "AI" / "coming soon" placeholder text
    - roast_freshness.html's empty hint references adding roast dates and has no all-unrated variant
    - preference_profile.html uses `md:grid-cols-2`
    - the CSP/files probe prints `cards ok`
  </acceptance_criteria>
  <done>Five card bodies render their SQL-derived data; every card renders-with-hint when empty (D-04); rating-dependent cards distinguish all-unrated (D-05); CSP-clean; no AI placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: Fragment smoke tests — render + headers + sparse/all-unrated branches</name>
  <files>tests/routers/test_home.py</files>
  <read_first>
    - tests/routers/test_home.py (Plan 06-02 — existing skip gates, authed client, fragment-header assertion shape)
    - tests/services/test_analytics.py (Plan 06-01 — `_seed_analytics_scenario` and `_seed_all_unrated` helpers to reuse for seeding)
    - app/routers/home.py (the five endpoints under test)
    - .planning/phases/06-analytics-home-page/06-UI-SPEC.md §"Copywriting Contract" (the hint strings to assert)
  </read_first>
  <action>
    Append tests to tests/routers/test_home.py (reuse the existing authed-client fixture + skip gates; import/duplicate the seed helpers from the service test module or add a small local seeder):
      - For each of the five aggregate routes: `test_{slug}_fragment_headers` — GET with `headers={"HX-Request":"true"}` (authed) → 200, `no-store` in cache-control, `HX-Request` in vary.
      - `test_{slug}_requires_auth` for at least sweet-spots and top-coffees — UN-authed GET → 401.
      - `test_sweet_spots_sparse_hint`: with a regular user that cleared the gate but has no qualifying combination, GET "/home/cards/sweet-spots" → body contains the generic "need 3 per match" hint.
      - `test_top_coffees_all_unrated_nudge`: seed an all-unrated gate-cleared user (reuse `_seed_all_unrated`), GET "/home/cards/top-coffees" → body contains "Rate some brews to see your top coffees." (proves D-05).
      - `test_sweet_spots_no_ai_placeholder`: GET "/home/cards/sweet-spots" (any state) → body does NOT contain "AI" coming-soon placeholder text (HOME-06 scope guard).
    Keep tests self-contained with unique usernames; clean up created rows. Pass `HX-Request: true` on every fragment request.
  </action>
  <verify>
    <automated>docker compose cp tests/ coffee-snobbery:/app/tests/ && docker compose exec coffee-snobbery python -m pytest tests/routers/test_home.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/routers/test_home.py -q` exits 0 (or skips cleanly only when Postgres is unreachable)
    - a fragment-header test asserts `no-store` in cache-control and `HX-Request` in vary for at least one aggregate card
    - `test_top_coffees_all_unrated_nudge` asserts the exact "Rate some brews to see your top coffees." string
    - `test_sweet_spots_requires_auth` asserts 401 unauthenticated
    - `test_sweet_spots_no_ai_placeholder` passes (no AI placeholder in the card)
  </acceptance_criteria>
  <done>All five aggregate fragments verified for render, cache headers, auth gate, sparse hint, all-unrated nudge, and the no-AI-placeholder scope guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → aggregate fragment endpoints | each `/home/cards/{aggregate}` is independently reachable and must enforce auth + per-user scoping |
| query result → card template | coffee/roaster/brewer/recipe names + flavor descriptors render into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure | five aggregate fragment endpoints | mitigate | Every handler declares `Depends(require_user)` → 401 unauthenticated (proven by `test_*_requires_auth`). |
| T-06-09 | Elevation of Privilege (IDOR) | aggregate queries | mitigate | Handlers pass `user.id` from `request.state.user`; the underlying analytics queries are user-scoped (Plan 06-01); no user_id query param accepted. |
| T-06-10 | Tampering (XSS) | five card templates + _card_sparse | mitigate | No `|safe`, no `hx-on:`, no `hx-vals='js:'`; coffee/roaster/descriptor names autoescaped; CSP-clean probe in Task 2. |
| T-06-11 | Scope leak (HOME-06) | sweet_spots.html | accept→guard | No AI prose / "coming soon" placeholder rendered in Phase 6; enforced by `test_sweet_spots_no_ai_placeholder`. |
</threat_model>

<verification>
- `pytest tests/routers/test_home.py -q` green (covers Plan 06-02 + Plan 06-03 tests in one file).
- `ruff check app/routers/home.py` exits 0; CSP/files probe prints `cards ok`.
- create_app exposes all five `/home/cards/{aggregate}` routes.
</verification>

<success_criteria>
- The five aggregate cards render their SQL-derived bodies and lazy-load into the shell slots (HOME-01..05, HOME-09).
- Every card renders-with-hint when empty (D-04); rating-dependent cards show the all-unrated nudge (D-05); roast-freshness uses the generic add-roast-date hint.
- No AI placeholder in the sweet-spots card (HOME-06 stays Phase 7).
- All fragments auth-gated, user-scoped, and CSP-clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-home-page/06-03-SUMMARY.md`
</output>
