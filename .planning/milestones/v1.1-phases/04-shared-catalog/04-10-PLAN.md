---
phase: 04-shared-catalog
plan: 10
type: execute
wave: 8
depends_on:
  - "04-01"
  - "04-03"
files_modified:
  - app/routers/photos.py
  - app/main.py
  - tests/phase_04/test_routers_photos.py
autonomous: true
requirements:
  - SEC-07
tags:
  - photos
  - serving
  - auth-gate
  - cache-headers
  - path-traversal

must_haves:
  truths:
    - "GET `/photos/{filename}` requires an authenticated session; anonymous requests return **404 (NOT 403)** per D-06 — no existence-leak."
    - "Filename validation: only `^[0-9a-f]{32}(-thumb)?\\.jpg$` matches; anything else (path traversal `../etc/passwd`, wrong extension, malformed UUID) returns 404."
    - "Authenticated successful request returns the file via `FileResponse` with EXPLICIT `Content-Type: image/jpeg`, `Cache-Control: private, max-age=31536000, immutable`, `X-Content-Type-Options: nosniff`, `Content-Disposition: inline`."
    - "Route is NOT a `StaticFiles` mount (D-06)."
    - "Phase 1's `FragmentCacheHeadersMiddleware` does not overwrite this route's `Cache-Control` (Phase 1 D-12 'do not overwrite if already set by the route' is honored — test verifies)."
    - "Plan 04-09 bag photo upload + plan 04-11 autocomplete-mini-modal both reference `/photos/{filename}` for serving thumbnails and full images."
  artifacts:
    - path: "app/routers/photos.py"
      provides: "Auth-gated photo serving route with D-06 header contract."
      contains: "FileResponse"
    - path: "app/main.py"
      provides: "Includes the new photos router."
      contains: "photos_router"
  key_links:
    - from: "app/routers/photos.py"
      to: "app/services/photos.py::_is_safe_photo_filename"
      via: "import + filename gate before FileResponse"
      pattern: "_is_safe_photo_filename"
    - from: "app/routers/photos.py serve_photo"
      to: "request.state.user"
      via: "Read directly to translate anonymous → 404 (not require_user 401)"
      pattern: "request.state.user"
---

<objective>
Ship the auth-gated photo serving route. This is a single-file plan but critical: the threat model concentrates here (T-04-AUTH — unauthenticated enumeration, path traversal, mis-typed Content-Type). The cache headers are load-bearing for client-side performance (`max-age=31536000, immutable` means the browser caches photos for a year + never revalidates because UUID filenames are unique-per-upload).

Purpose: SEC-07 / D-06. Phase 4-09 uploads + saves photos; this plan serves them. Phase 5 brew-session UI will surface bag photos in the session form; Phase 6 home page may render the latest bag thumbnail. Every consumer hits this route — get the auth + cache + nosniff contract right once.

Output:
  - `app/routers/photos.py` with `serve_photo` handler.
  - `app/main.py` includes the router.
  - Real test coverage in `test_routers_photos.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-shared-catalog/04-CONTEXT.md
@.planning/phases/04-shared-catalog/04-RESEARCH.md
@.planning/phases/04-shared-catalog/04-PATTERNS.md
@.planning/phases/04-shared-catalog/04-UI-SPEC.md
@.planning/phases/04-shared-catalog/04-VALIDATION.md
@.planning/phases/04-shared-catalog/04-01-SUMMARY.md
@app/services/photos.py
@app/routers/admin.py
@app/dependencies/auth.py
@app/main.py
</context>

<interfaces>
From plan 04-01 (`app/services/photos.py`):
  PHOTOS_DIR: Path
  _SAFE_FILENAME_RE: re.Pattern  # ^[0-9a-f]{32}(-thumb)?\.jpg$
  def _is_safe_photo_filename(name: str) -> bool

From Phase 2 (`app/dependencies/auth.py`):
  def require_user(request: Request) -> User  # raises 401 on no session

Phase 1 D-12 contract (`FragmentCacheHeadersMiddleware`):
  Sets `Cache-Control: no-store + Vary: HX-Request` on HX-Request responses.
  Sets `Cache-Control: private, no-cache, must-revalidate` on full-page responses.
  RESPECTS routes that set their own Cache-Control header — "do not overwrite if already set."

D-06 photo response header contract (verbatim from CONTEXT):
  Content-Type: image/jpeg|png|webp (explicit, never sniffed)
  Cache-Control: private, max-age=31536000, immutable
  X-Content-Type-Options: nosniff
  Content-Disposition: inline

D-06 anonymous handling: returns 404, NOT 403 (don't leak existence). So the handler reads request.state.user directly instead of using Depends(require_user) (which would return 401).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement app/routers/photos.py with auth gate + filename validation + explicit response headers; register in main.py</name>
  <files>
    app/routers/photos.py,
    app/main.py
  </files>
  <read_first>
    app/services/photos.py,
    app/routers/admin.py,
    app/dependencies/auth.py,
    app/csrf.py,
    app/main.py,
    .planning/phases/04-shared-catalog/04-PATTERNS.md,
    .planning/phases/04-shared-catalog/04-RESEARCH.md,
    .planning/phases/04-shared-catalog/04-CONTEXT.md
  </read_first>
  <action>
**A. `app/routers/photos.py`** (NEW) — Net-new in repo (no `FileResponse` consumer in any prior router; closest analog is `app/routers/admin.py` for the require-style gate).

Module docstring: "Auth-gated photo serving route — D-06. Anonymous → 404 (not 403); only well-formed UUID filenames accepted; explicit Content-Type + nosniff + immutable cache. NOT a StaticFiles mount per CONTEXT canonical_refs + Phase 0 invariant."

Imports:

```
from __future__ import annotations
from fastapi import APIRouter, HTTPException, Request, status
from fastapi.responses import FileResponse
from app.services.photos import PHOTOS_DIR, _is_safe_photo_filename
```

Note: import `_is_safe_photo_filename` from plan 04-01 — it's exposed as a module-level helper. If plan 04-01 didn't export it, this plan adds an `__all__` entry to `app/services/photos.py` OR duplicates the regex here. Recommendation: export from photos.py (single source of truth).

`router = APIRouter(prefix="/photos", tags=["photos"])`.

Handler:

```
@router.get("/{filename}")
def serve_photo(filename: str, request: Request) -> FileResponse:
    """D-06: auth-gated photo serve. Anonymous → 404, NOT 403."""
    # 1. Auth gate (read state.user directly — Depends(require_user) returns 401,
    #    but D-06 mandates 404 to avoid leaking existence).
    user = getattr(request.state, "user", None)
    if user is None:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND)

    # 2. Filename validation — defends against path traversal + arbitrary file access.
    if not _is_safe_photo_filename(filename):
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND)

    # 3. Resolve full path; resolve to absolute and re-check it stays inside PHOTOS_DIR
    #    (belt-and-braces defense — the regex above already restricts to a flat filename).
    photo_path = (PHOTOS_DIR / filename).resolve()
    try:
        photo_path.relative_to(PHOTOS_DIR.resolve())
    except ValueError:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND)

    if not photo_path.is_file():
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND)

    # 4. Serve with explicit headers per D-06.
    return FileResponse(
        photo_path,
        media_type="image/jpeg",  # All saved files are JPEG (plan 04-01 always re-encodes to JPEG)
        headers={
            "Cache-Control": "private, max-age=31536000, immutable",
            "X-Content-Type-Options": "nosniff",
            "Content-Disposition": "inline",
        },
    )
```

(Literal block fixes shape; the executor writes the actual file. No docstring fences inside the file.)

Note: Per plan 04-01, the photo pipeline always re-encodes to JPEG via `Image.save(..., "JPEG", quality=85)` regardless of upload format (JPEG/PNG/WebP). So `media_type="image/jpeg"` is correct for ALL saved files. If a future Phase 4+ revision allows PNG/WebP storage, the route extends to switch on extension.

**B. `app/main.py` (MODIFY)** — add `from app.routers import photos as photos_router` to imports + `app.include_router(photos_router.router)` after `bags_router`.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -c "from app.routers.photos import router, serve_photo; print(router.prefix)"</automated>
  </verify>
  <done>
    Smoke prints `/photos`. `grep -c 'FileResponse' app/routers/photos.py` returns 1. `grep -c 'Cache-Control.*max-age=31536000.*immutable' app/routers/photos.py` returns 1. `grep -c 'X-Content-Type-Options.*nosniff' app/routers/photos.py` returns 1. `grep -c 'Content-Disposition.*inline' app/routers/photos.py` returns 1. `grep -c 'include_router.photos_router' app/main.py` returns 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author tests/phase_04/test_routers_photos.py — integration tests for auth gate + filename validation + cache headers</name>
  <files>
    tests/phase_04/test_routers_photos.py
  </files>
  <read_first>
    tests/phase_04/conftest.py,
    tests/phase_04/test_services_photos.py,
    app/routers/photos.py,
    .planning/phases/04-shared-catalog/04-VALIDATION.md
  </read_first>
  <action>
Author real tests replacing the Wave-0 stub. Cases per 04-VALIDATION.md row 04-10-NN (D-06 / T-04-AUTH):

Setup: each test needs a file present in the (monkeypatched) `photo_volume`. Use the `photo_volume` fixture to set PHOTOS_DIR + write a synthetic JPEG to a known UUID-style filename.

Cases:

1. `test_anonymous_returns_404` — write a photo `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.jpg` to `photo_volume`. Use an unauth'd TestClient (NOT the `authed_client` fixture — make a fresh one without a session cookie). GET `/photos/aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.jpg`. Status MUST be 404 (NOT 401, NOT 403 — D-06 lock).

2. `test_authed_returns_photo` — write the same photo + use `authed_client`. GET `/photos/{uuid}.jpg`. Status 200. `response.headers["content-type"] == "image/jpeg"`. `response.content` length > 0.

3. `test_authed_response_has_d06_cache_headers` — same as test 2; assert `response.headers["cache-control"]` contains `private`, `max-age=31536000`, `immutable` (case-insensitive contains; HTTP headers are case-insensitive but Python http.client returns lowercased keys).

4. `test_authed_response_has_nosniff_header` — `response.headers["x-content-type-options"] == "nosniff"`.

5. `test_authed_response_has_content_disposition_inline` — `response.headers["content-disposition"] == "inline"`.

6. `test_authed_path_traversal_returns_404` — GET `/photos/../etc/passwd` (URL-encoded if necessary: `/photos/..%2Fetc%2Fpasswd`); FastAPI's URL parser should strip OR the regex gate rejects. Status 404.

7. `test_authed_wrong_extension_returns_404` — GET `/photos/aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.png` (uuid hex + .png — fails regex); 404.

8. `test_authed_malformed_uuid_returns_404` — GET `/photos/short.jpg`; 404 (fails regex).

9. `test_authed_existing_uuid_but_missing_file_returns_404` — GET `/photos/bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb.jpg` where the regex passes but the file doesn't exist in `photo_volume`; 404 (`photo_path.is_file()` false).

10. `test_authed_thumb_variant_serves` — write `aaaa…aaaa-thumb.jpg`; GET `/photos/aaaa…aaaa-thumb.jpg`; status 200.

11. `test_cache_control_not_overwritten_by_middleware` — the route's `Cache-Control: private, max-age=31536000, immutable` MUST survive the `FragmentCacheHeadersMiddleware` (Phase 1 D-12 "do not overwrite if already set"). Assert the response's final Cache-Control value matches what the route emits — NOT `no-store` or `private, no-cache, must-revalidate`.

12. `test_hx_request_does_not_force_no_store` — GET with `HX-Request: true` header; the photo route's Cache-Control should still be `private, max-age=31536000, immutable` (not the HX-Request-fragment override). This is an EXTRA-defensive test of Phase 1 D-12.

13. `test_anonymous_with_existing_file_still_404` — write the photo + use unauth'd client; status MUST be 404 even though the file is there (D-06 existence-leak defense).

14. `test_route_is_not_static_files_mount` — assert that the response is NOT served by Starlette's StaticFiles (negative test of D-06). Check via the response headers `Server` or via the body shape — alternatively, just confirm the headers above are present, which StaticFiles wouldn't emit. (This is a soft test; the strong assertion is the header set.)

For the unauth'd TestClient, create a fresh `TestClient(fastapi_app)` instance in the test without cookies. Or document via the `csrf_client` style — but csrf_client has a session, just bad CSRF. Create a new `anon_client` fixture in conftest.py if convenient: `TestClient(fastapi_app)` without any session cookie / pre-loaded headers.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery pytest -q tests/phase_04/test_routers_photos.py -x</automated>
  </verify>
  <done>
    `pytest tests/phase_04/test_routers_photos.py -x` exits 0 with ≥ 12 tests passing. The four D-06 header assertions are all present (cache-control, x-content-type-options, content-disposition, content-type). The anonymous-returns-404 test passes (locks the existence-leak defense). The Cache-Control-not-overwritten test passes (locks the Phase 1 D-12 contract).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /photos/{filename} | Untrusted filename input crosses the boundary; regex gate + path-resolve check defend against path traversal. |
| anonymous browser → GET /photos | Returns 404 to defeat existence enumeration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-AUTH | Information Disclosure | GET /photos/{filename} anonymous | mitigate | Read `request.state.user` directly + return 404 (not 401/403) if None. Test `test_anonymous_returns_404` locks the behavior. |
| (path traversal) | Tampering | GET /photos/{filename} filename | mitigate | Regex `^[0-9a-f]{32}(-thumb)?\.jpg$` rejects anything but flat filenames; belt-and-braces `Path.resolve().relative_to(PHOTOS_DIR.resolve())` re-check. Tests `test_authed_path_traversal_returns_404`, `test_authed_malformed_uuid_returns_404`. |
| T-04-POLY | Tampering / Spoofing | GET /photos/{filename} serving | mitigate | `Content-Type: image/jpeg` explicit (never sniffed) + `X-Content-Type-Options: nosniff` defeats MIME-sniff polyglot exploits. `Content-Disposition: inline` (NOT attachment) prevents browser from offering to "open" the file as another type. |
| (cache leak) | Information Disclosure | Response Cache-Control | mitigate | `private` keeps photos out of shared caches (CDN, corporate proxy); `max-age=31536000, immutable` means the browser caches but never revalidates — safe because UUID filenames are write-once. |
| (CSP report bypass) | n/a | n/a | n/a | Photo route is auth-gated; no XSS surface. |
</threat_model>

<verification>
- D-06 header contract enforced on every successful response.
- Anonymous returns 404 (existence-leak defense).
- Filename validation rejects path traversal + malformed UUIDs + wrong extensions.
- Phase 1 D-12 "do not overwrite Cache-Control" contract verified end-to-end.
- Route is not a StaticFiles mount (header set proves it).
</verification>

<success_criteria>
- ROADMAP success criterion #4 satisfied at the serving layer: photos served via `routers/photos.py` (not a `StaticFiles` mount) with `Content-Type` set and `Cache-Control: private, max-age=31536000, immutable`.
- Plan 04-09 (bag photo upload) has a working /photos endpoint to reference in `<img src>` tags.
- Phase 5+ consumers can render `<img src="/photos/{uuid}.jpg">` and `<img src="/photos/{uuid}-thumb.jpg">` knowing the auth gate + cache + nosniff are all correct.
</success_criteria>

<output>
Create `.planning/phases/04-shared-catalog/04-10-SUMMARY.md`: the auth-gate-via-state.user rationale (vs require_user 401), the regex + path-resolve belt-and-braces, the Phase 1 D-12 verification result, the route's contract for plan 04-09 and future consumers.
</output>
