---
phase: 04-shared-catalog
plan: 07
type: execute
wave: 7
depends_on:
  - "04-02"
  - "04-03"
  - "04-04"
  - "04-05"
files_modified:
  - app/services/coffees.py
  - app/routers/coffees.py
  - app/templates/pages/coffees.html
  - app/templates/pages/coffee_detail.html
  - app/templates/fragments/coffee_row.html
  - app/templates/fragments/coffee_form.html
  - app/templates/fragments/coffee_list.html
  - app/templates/fragments/coffee_filters_panel.html
  - app/main.py
  - tests/phase_04/test_routers_coffees.py
  - tests/phase_04/test_coffee_filters.py
autonomous: true
requirements:
  - CAT-03
  - CAT-07
tags:
  - coffees
  - crud
  - htmx
  - filters
  - hx-push-url
  - mobile-responsive

must_haves:
  truths:
    - "A logged-in user can navigate to `/coffees`, click 'Add coffee', fill in name + roaster + country + process + roast_level + advertised flavor notes + notes, save via HTMX, and see the new coffee in the list."
    - "Coffees list renders as a desktop table at ≥768px and collapses to a card list at <768px per CAT-07 / UI-SPEC §Coffees."
    - "Four filter dimensions work (roaster, country, process, archived). Each filter change fires `hx-get` against `/coffees?roaster=...&...` AND updates the browser URL via `hx-push-url=\"true\"` (D-03)."
    - "Browser back/forward replays the filter state (D-03)."
    - "Roaster + flavor-note inputs in the coffee form use HTMX autocomplete: `hx-trigger=\"input changed delay:350ms[target.value.length >= 2]\"` + `hx-sync=\"this:replace\"` (D-13, HX-4 pitfall mitigation). Plan 04-11 adds the `focus once` re-fetch + the mini-modal pre-select Alpine listener."
    - "Coffee detail page `/coffees/{id}` renders the coffee + its bags + an 'Open new bag' affordance (the affordance launches a bag form whose CRUD is implemented in plan 04-09)."
    - "Pydantic ValidationError → 200 + form fragment with field errors."
    - "Audit events: `CATALOG_COFFEE_CREATED`, `CATALOG_COFFEE_UPDATED`, `CATALOG_COFFEE_ARCHIVED`."
    - "GET /coffees with HX-Request header returns the list-only fragment (`fragments/coffee_list.html`); without it returns the full page."
  artifacts:
    - path: "app/services/coffees.py"
      provides: "CRUD + filter list query (roaster_id, country, process, archived); kwargs API + audit emit."
      contains: "def list_coffees"
    - path: "app/routers/coffees.py"
      provides: "GET / (page + fragment) + GET /new + POST + GET /{id} (detail) + GET /{id}/edit + POST /{id} + POST /{id}/archive + GET /filters-panel (HTMX-fetched filter dropdowns)."
      contains: "hx_push_url\\|HX-Request"
    - path: "app/templates/pages/coffees.html"
      provides: "List page with filter bar + desktop table / mobile card list."
    - path: "app/templates/pages/coffee_detail.html"
      provides: "Coffee detail with bags section + Open new bag affordance."
    - path: "app/templates/fragments/coffee_filters_panel.html"
      provides: "HTMX-fetched filter dropdown panel (roaster list, country list, process list, archived toggle)."
  key_links:
    - from: "app/templates/pages/coffees.html filter bar"
      to: "GET /coffees"
      via: "hx-get /coffees?roaster=...&country=... + hx-push-url=true + hx-target=#coffee-list"
      pattern: "hx-push-url=.true"
    - from: "app/templates/fragments/coffee_form.html roaster input"
      to: "GET /roasters/list"
      via: "hx-get + hx-trigger debounce + hx-sync=this:replace"
      pattern: "hx-trigger=.input changed delay:350ms"
    - from: "app/routers/coffees.py GET /"
      to: "HX-Request fragment branch"
      via: "if request.headers.get('HX-Request') == 'true': fragment else page"
      pattern: "HX-Request"
---

<objective>
Ship CAT-03 (coffees CRUD with `ARRAY(BigInteger) advertised_flavor_note_ids`) + CAT-07 (mobile card-list collapse at <768px + 4-dimension filter with `hx-push-url`). This is the most complex CRUD surface in Phase 4 because of (a) the array column round-trip, (b) the filter bar with URL state, (c) the responsive table↔card layout, (d) the autocomplete fields driven by /roasters/list and /flavor-notes/datalist from plans 04-04 + 04-05.

Purpose: Coffees are the central catalog entity — every brew session (Phase 5) and AI recommendation (Phase 7) references a coffee. The filter UX is critical for usability at scale; without `hx-push-url` users can't share URLs of filtered views.

Output:
  - `app/services/coffees.py` with filtered list query.
  - `app/routers/coffees.py` with 8 endpoints.
  - 2 pages (list + detail) + 4 fragments.
  - `app/main.py` includes the router.
  - Real test coverage in `test_routers_coffees.py` + filter-state integration tests in `test_coffee_filters.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-shared-catalog/04-CONTEXT.md
@.planning/phases/04-shared-catalog/04-RESEARCH.md
@.planning/phases/04-shared-catalog/04-PATTERNS.md
@.planning/phases/04-shared-catalog/04-UI-SPEC.md
@.planning/phases/04-shared-catalog/04-VALIDATION.md
@.planning/phases/04-shared-catalog/04-04-SUMMARY.md
@.planning/phases/04-shared-catalog/04-05-SUMMARY.md
@app/services/roasters.py
@app/services/flavor_notes.py
@app/routers/roasters.py
@app/templates/pages/roasters.html
@app/templates/fragments/roaster_form.html
@app/templates/fragments/autocomplete_list.html
@app/csrf.py
@app/main.py
</context>

<interfaces>
From app/schemas/coffee.py (plan 04-02):
  class CoffeeCreate(BaseModel):
    name: str (min_length=1, max_length=200)
    roaster_id: int | None (ge=1)
    country: str | None (max_length=80)
    origin: str | None (max_length=120)
    process: str | None (regex)
    roast_level: str | None (regex)
    varietal: str | None (max_length=120)
    notes: str (max_length=2000)
    advertised_flavor_note_ids: list[int]
    model_config = ConfigDict(extra="forbid")
  class CoffeeUpdate(CoffeeCreate): pass

From app/models/coffee.py (plan 04-03):
  class Coffee(Base):
    id, name, roaster_id (FK SET NULL), country, origin, process, roast_level, varietal,
    notes, advertised_flavor_note_ids (ARRAY(BigInteger)), archived, created_at, updated_at

Plans 04-04/04-05 provide the autocomplete endpoints:
  GET /roasters/list?q=...  → fragments/autocomplete_list.html
  GET /flavor-notes/datalist?q=... → fragments/autocomplete_list.html

Audit events (plan 04-01):
  CATALOG_COFFEE_CREATED, CATALOG_COFFEE_UPDATED, CATALOG_COFFEE_ARCHIVED

CRUD template: plan 04-04 SUMMARY (roasters).
Mobile responsiveness contract: UI-SPEC §"List-vs-Card Layout per Entity → Coffees" + §"Filter Bar (coffees only — CAT-07)".

Filter dimensions (D-03 + CAT-07): roaster_id (int | None), country (str | None — free-form ILIKE), process (str | None — enum value), archived (bool, default False).

Note on `advertised_flavor_note_ids` form field parsing: HTML <select multiple> with hidden inputs OR a comma-separated string OR repeated `<input name="advertised_flavor_note_ids" type="hidden" value="X">` entries. FastAPI's `Form()` with `list[int]` collects repeated values. Recommendation: render selected flavor notes as chips with hidden inputs (one per id) — the chip widget is a tiny Alpine-driven affordance; for Phase 4 we ship the static rendering (chips can be removed by an `x-on:click` that splices the hidden inputs).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement app/services/coffees.py + app/routers/coffees.py + register in app/main.py</name>
  <files>
    app/services/coffees.py,
    app/routers/coffees.py,
    app/main.py
  </files>
  <read_first>
    app/services/roasters.py,
    app/routers/roasters.py,
    app/schemas/coffee.py,
    app/models/coffee.py,
    .planning/phases/04-shared-catalog/04-PATTERNS.md,
    .planning/phases/04-shared-catalog/04-RESEARCH.md,
    .planning/phases/04-shared-catalog/04-04-SUMMARY.md,
    app/main.py
  </read_first>
  <action>
**A. `app/services/coffees.py`**:

Functions:

1. `create_coffee(db, *, name, roaster_id, country, origin, process, roast_level, varietal, notes, advertised_flavor_note_ids, by_user_id) -> Coffee` — INSERT, commit, `log.info(CATALOG_COFFEE_CREATED, coffee_id=..., roaster_id=roaster_id, user_id=by_user_id)`. SQLAlchemy 2.0 handles the ARRAY mapping natively — pass `advertised_flavor_note_ids=advertised_flavor_note_ids` directly.

2. `get_coffee(db, *, coffee_id) -> Coffee | None`.

3. `list_coffees(db, *, roaster_id=None, country=None, process=None, archived=False) -> list[Coffee]` — filter builder:
   - Base: `stmt = select(Coffee)`.
   - If `not archived`: `stmt = stmt.where(Coffee.archived.is_(False))`. If `archived=True`: include archived AND non-archived (or only archived? CONTEXT says it's a filter, so the spec is "show archived ONLY when toggled on" — recommendation: when `archived=True`, return only archived rows; when `archived=False` (default), return only non-archived). UI-SPEC §"Filter Bar" lock: `☐ Show archived` toggles between "non-archived only" and "archived only" — not a union. Confirm during implementation and document in SUMMARY.
   - If `roaster_id is not None`: `stmt = stmt.where(Coffee.roaster_id == roaster_id)`.
   - If `country`: `stmt = stmt.where(Coffee.country.ilike(f"%{country}%"))` (partial match — UI uses a dropdown of distinct countries, so exact match is also fine; planner picks; recommendation: dropdown of distinct values + exact match for performance).
   - If `process`: `stmt = stmt.where(Coffee.process == process)`.
   - Order: `Coffee.name` (CITEXT case-insensitive natural order).
   - Return `list(db.execute(stmt).scalars())`.

4. `update_coffee(db, *, coffee_id, ..., by_user_id) -> Coffee` — same signature as create but with `coffee_id` instead of skipping it. Use `update(Coffee)...values(...)` with explicit `updated_at=func.now()`. Re-fetch row.

5. `archive_coffee(db, *, coffee_id, by_user_id) -> None`.

6. `list_distinct_countries(db) -> list[str]` — for the filter-panel dropdown: `select(Coffee.country.distinct()).where(Coffee.country.isnot(None)).order_by(Coffee.country)`. Returns flat list.

7. `list_distinct_processes() -> list[str]` — return the 6 enum values (hardcoded module-level constant tuple `("washed", "natural", "honey", "anaerobic", "experimental", "unknown")`).

8. `get_coffee_with_bags(db, *, coffee_id) -> tuple[Coffee, list[Bag]]` — fetches the coffee + all its bags ordered by `Bag.opened_at DESC NULLS LAST, Bag.created_at DESC`. Used by the detail page.

**B. `app/routers/coffees.py`**:

`router = APIRouter(prefix="/coffees")`.

Endpoints:

1. `GET /` → list page or fragment based on HX-Request. Query params: `roaster_id: int | None = None`, `country: str | None = None`, `process: str | None = None`, `archived: bool = False`. Service call: `coffees_service.list_coffees(db, roaster_id=..., country=..., process=..., archived=archived)`. Two paths:
   - HX-Request: return `fragments/coffee_list.html` with context `{"coffees": rows, "filters": {...}}`.
   - Else: return `pages/coffees.html` with context including `roasters` (for the filter dropdown), `countries` (`list_distinct_countries`), `processes` (the 6 enum values), and the current filters echoed back.

2. `GET /new` → empty `fragments/coffee_form.html` with `mode="create"`, `values={}`, `errors={}`. Service call: fetch the lists of roasters (`roasters_service.list_roasters(db)`) and flavor_notes (`flavor_notes_service.list_flavor_notes(db)`) so the form template can render the autocomplete fallback datalists + the chip-builder. (Note: per D-14 + UI-SPEC §"Autocomplete Dropdown", the form RELIES on `hx-trigger="focus once from:closest .field"` to fetch fresh lists; passing the lists at form-render time is the SEED, not the source of truth.)

3. `POST /`:
   - Form params: `name: str = Form(...)`, `roaster_id: int | None = Form(None)`, `country: str | None = Form(None)`, `origin: str | None = Form(None)`, `process: str | None = Form(None)`, `roast_level: str | None = Form(None)`, `varietal: str | None = Form(None)`, `notes: str = Form("")`, `advertised_flavor_note_ids: list[int] = Form(default_factory=list)`.
   - Build `raw = {...}`; `try: form = CoffeeCreate(**raw) except ValidationError as exc:` → 200 + `fragments/coffee_form.html` with `values=raw` + `errors=errors_by_field(exc)`.
   - On success: `coffee = coffees_service.create_coffee(db, **form.model_dump(), by_user_id=user.id)`.
   - Return `fragments/coffee_row.html` with `include_oob_form_clear=True`.

4. `GET /{coffee_id}` → coffee detail page. Use `get_coffee_with_bags` to fetch row + bags. Return `pages/coffee_detail.html`.

5. `GET /{coffee_id}/edit` → form fragment pre-populated with the coffee's fields + `mode="edit"`.

6. `POST /{coffee_id}` → update. Validation re-render path same as create. On success: row fragment.

7. `POST /{coffee_id}/archive` → archive + return updated row.

8. `GET /filters-panel` → `fragments/coffee_filters_panel.html` populated with the current roaster list + distinct countries + the 6 process values. Recommended by 04-RESEARCH.md Open Question 2 ("HTMX-fetched filter panel — server is the single source of truth for available filter values").

9. `GET /empty-form` → `fragments/empty.html`.

**C. `app/main.py` (MODIFY)** — add `from app.routers import coffees as coffees_router` + `app.include_router(coffees_router.router)` after equipment.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -c "from app.routers.coffees import router; from app.services.coffees import create_coffee, list_coffees, list_distinct_countries, get_coffee_with_bags; print(router.prefix, 'ok')"</automated>
  </verify>
  <done>
    Smoke prints `/coffees ok`. `grep -c 'CATALOG_COFFEE' app/services/coffees.py` returns ≥ 3. `grep -c 'advertised_flavor_note_ids' app/services/coffees.py` returns ≥ 2 (create + update). `grep -c 'list_distinct_countries' app/services/coffees.py` returns ≥ 1. `grep -c 'include_router.coffees_router' app/main.py` returns 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 2 pages + 4 fragments with desktop/mobile responsive layout + filter bar with hx-push-url</name>
  <files>
    app/templates/pages/coffees.html,
    app/templates/pages/coffee_detail.html,
    app/templates/fragments/coffee_row.html,
    app/templates/fragments/coffee_form.html,
    app/templates/fragments/coffee_list.html,
    app/templates/fragments/coffee_filters_panel.html
  </files>
  <read_first>
    app/templates/pages/roasters.html,
    app/templates/fragments/roaster_row.html,
    app/templates/fragments/roaster_form.html,
    app/templates/fragments/roaster_list.html,
    app/templates/fragments/autocomplete_list.html,
    .planning/phases/04-shared-catalog/04-UI-SPEC.md,
    .planning/phases/04-shared-catalog/04-VALIDATION.md
  </read_first>
  <action>
**A. `app/templates/pages/coffees.html`** — extends base.html. Block content:

- `<main class="mx-auto max-w-6xl px-6 py-12">` (wider for the data-dense coffees table).
- `<header class="flex items-center justify-between mb-6">`:
  - `<h1 class="text-2xl font-semibold">Coffees</h1>`.
  - "Add coffee" button: `<button hx-get="/coffees/new" hx-target="#coffee-form-mount" hx-swap="innerHTML" class="rounded bg-espresso-700 px-4 py-2 text-cream-50 hover:bg-espresso-800">Add coffee</button>`.

- **Filter bar** — per UI-SPEC §"Filter Bar (coffees only — CAT-07)":
  ```
  <form id="coffee-filters" class="mb-4 flex flex-wrap gap-3 items-end"
        hx-get="/coffees"
        hx-trigger="change from:select, change from:input[type='checkbox']"
        hx-push-url="true"
        hx-target="#coffee-list"
        hx-swap="innerHTML"
        hx-include="this">
    <label class="flex flex-col gap-1">Roaster
      <select name="roaster_id" class="rounded border px-2 py-2 text-base">
        <option value="">All</option>
        {% for r in roasters %}<option value="{{ r.id }}" {% if filters.roaster_id == r.id %}selected{% endif %}>{{ r.name }}</option>{% endfor %}
      </select>
    </label>
    <label class="flex flex-col gap-1">Country
      <select name="country" class="rounded border px-2 py-2 text-base">
        <option value="">All</option>
        {% for c in countries %}<option value="{{ c }}" {% if filters.country == c %}selected{% endif %}>{{ c }}</option>{% endfor %}
      </select>
    </label>
    <label class="flex flex-col gap-1">Process
      <select name="process" class="rounded border px-2 py-2 text-base">
        <option value="">All</option>
        {% for p in processes %}<option value="{{ p }}" {% if filters.process == p %}selected{% endif %}>{{ p }}</option>{% endfor %}
      </select>
    </label>
    <label class="flex items-center gap-2 mt-6">
      <input type="checkbox" name="archived" value="true" {% if filters.archived %}checked{% endif %}> Show archived
    </label>
  </form>
  ```
  (Hint: I'm describing the structure; the executor implements with the right details. Do not include the literal fenced block in the implementation file's source — translate to actual Jinja syntax. The form is a NON-submitting form: HTMX intercepts the change events and fires GET requests with all named fields included.)

- `<div id="coffee-form-mount" class="mb-4"></div>` — inline-expand form mounts here.
- `<div id="coffee-list">{% include "fragments/coffee_list.html" %}</div>`.

**B. `app/templates/pages/coffee_detail.html`** — extends base. `{% block page_title %}{{ coffee.name }} — Snobbery{% endblock %}`.

Layout: `<main class="mx-auto max-w-3xl px-6 py-12">`:
- Header with coffee name (`<h1 class="text-2xl font-semibold">`), roaster link, origin/process/roast_level/varietal lines.
- Notes block.
- `<h2 class="text-lg font-semibold mt-8">Bags</h2>` section: list of bags (rendered via `fragments/bag_row.html` from plan 04-09 — for now, render placeholder `<p>No bags yet.</p>` if empty, since plan 04-09 ships the bag CRUD).
- "Open new bag" button: per CONTEXT discretion + D-02 recommendation, this is an inline-expand affordance. Button `hx-get="/coffees/{{ coffee.id }}/bags/new"` `hx-target="#bag-form-mount"` `hx-swap="innerHTML"`. Mount div: `<div id="bag-form-mount"></div>`. The bag form fragment + the endpoint are owned by plan 04-09; for now ship the button + mount div + a placeholder; document the contract in the SUMMARY for 04-09 to consume.
- Edit/Archive coffee buttons in the header.

**C. `app/templates/fragments/coffee_list.html`** — desktop table + mobile cards. Per UI-SPEC §"Coffees" + §"List-vs-Card Layout":

- Desktop (`<table class="hidden md:table w-full text-base">`): thead with Name · Roaster · Origin · Process · Roast level · Flavor notes · Actions (use `text-sm font-semibold` per UI-SPEC).
- Mobile (`<div class="md:hidden space-y-3">`): cards rendered via `{% include "fragments/coffee_row.html" with context %}` with `mode="card"` flag.

For both layouts: iterate `coffees`. Empty state: `<p class="text-espresso-600 italic">No coffees yet. Click "Add coffee" to begin building the catalog.</p>` (UX-04 snobbery-tone copy).

**D. `app/templates/fragments/coffee_row.html`** — dual-mode (row vs card). Per UI-SPEC §"Coffees":

- Desktop row: `<tr id="coffee-{{ coffee.id }}" data-row>`. Cells:
  - Name (link to `/coffees/{{ coffee.id }}`).
  - Roaster name (from `coffee.roaster.name` — service must `selectinload(Coffee.roaster)` to avoid N+1; OR pass a `roaster_by_id` dict from the router context). Recommendation: add a SQLAlchemy `relationship` on Coffee + `selectinload`; this is small. If the executor opts not to, document in SUMMARY.
  - Country + Origin combined as `{{ coffee.country }} · {{ coffee.origin }}` (filter out None halves).
  - Process pill.
  - Roast level pill.
  - Advertised flavor-note pills (up to 3, then "+N more" — per UI-SPEC). Each pill: `<span class="inline-flex px-2 py-1 rounded text-sm bg-cream-200 text-espresso-900">{{ name }}</span>` — need to JOIN flavor_notes to resolve ids → names. Recommendation: pre-fetch in the service via a single query and pass an `id_to_flavor_note_name: dict[int, str]` to the template, OR add a relationship + `selectinload`.
  - Actions: Edit / Archive.

- Mobile card: `<div data-row id="coffee-{{ coffee.id }}" class="rounded-lg border border-espresso-200 bg-cream-100 p-4">` containing name strong + meta line + flavor pills + action row. Per UI-SPEC §Coffees mobile-card mockup.

`include_oob_form_clear` handling identical to roaster_row.html.

Archived rows: `text-espresso-600 italic` + small "Archived" pill at the end.

**E. `app/templates/fragments/coffee_form.html`** — the inline-expand form. Same root container as `roaster_form.html`. CSRF hidden input verbatim.

Fields:
- `name` (text input, required).
- `roaster` autocomplete: a wrapper `<div class="relative">` containing:
  - `<input type="text" name="roaster_query" value="{{ values.roaster_name or '' }}" hx-get="/roasters/list" hx-trigger="input changed delay:350ms[target.value.length >= 2]" hx-target="#roaster-dropdown" hx-swap="innerHTML" hx-sync="this:replace" autocomplete="off" class="...text-base...">` per D-13 + HX-4.
  - `<input type="hidden" name="roaster_id" value="{{ values.roaster_id or '' }}">` (the actual submitted id).
  - `<div id="roaster-dropdown" class="absolute top-full left-0 right-0 z-10"></div>` — populated by `/roasters/list` HX-GET.

  Plan 04-11 wires the Alpine `autocomplete` component + the `focus once` re-fetch + the `HX-Trigger` listener for pre-select. This plan ships the HTML shell.

- `country` (text input, max 80).
- `origin` (text input, max 120).
- `process` (native `<select>` with 6 enum values + "All / unset" None option).
- `roast_level` (native `<select>` with 6 enum values + None).
- `varietal` (text input, max 120).
- `advertised_flavor_note_ids`: chip-builder UI. STATIC RENDERING IN 04-07; INTERACTIVE ADD/REMOVE IN 04-11. The 04-07 form ships the inert markup; plan 04-11 mounts the `flavorNoteChips` Alpine component on it. CONTRACT for 04-11 to mount on (locked here):
  - Wrapper `<div>` carries `x-data="flavorNoteChips"` (added by 04-11 — 04-07 just leaves the wrapper with class `flavor-note-chips-field` and `id="flavor-note-chips"`).
  - Chip-list container: `<div id="flavor-note-chip-list" class="flex flex-wrap gap-2 mb-2">` — 04-11 replaces children with an Alpine `<template x-for>` over `selectedChips`.
  - Hidden-input container: `<div id="flavor-note-hidden-inputs">` — 04-11 fills with a parallel `<template x-for>` emitting `<input type="hidden" name="advertised_flavor_note_ids" :value="chip.id">` so every chip has a submitted-id sibling.
  - Autocomplete input: `<input type="text" name="flavor_note_query" autocomplete="off" class="... text-base">` carries the HTMX autocomplete attrs (D-13 + D-14) when 04-11 wires it.
  - Server-rendered seed for re-render after validation failure: 04-07 renders one chip + one hidden input per id in `values.advertised_flavor_note_ids` directly so the form survives a 200 re-render even before Alpine boots; Alpine takes over on client-side hydration.
  - The eventual `04-07-SUMMARY.md` captures this contract verbatim so plan 04-11's executor reads it before authoring the component.

  Chip add/remove behavior LIVES IN PLAN 04-11 via the `flavorNoteChips` Alpine component. Plan 04-07 ships ONLY the inert markup per the integration contract above (`#flavor-note-chip-list`, `#flavor-note-hidden-inputs`, the autocomplete input shell with `name="flavor_note_query"`, and server-rendered seed chips for the validation-re-render case). 04-07-SUMMARY.md MUST record the exact element IDs and the `name="advertised_flavor_note_ids"` hidden-input pattern so 04-11's Alpine factory mounts on the right DOM.

- `notes` (`<textarea rows="4">`).
- Submit row: Cancel ghost + Save accent.

Inline error rendering identical to roaster_form. `errors.<field>` renders red helper text under each field.

**F. `app/templates/fragments/coffee_filters_panel.html`** — server-rendered filter dropdown panel for HTMX-fetched refresh (Open Question 2 — the filter values evolve as roasters are added). Same shape as the inline filter form on the page; this fragment is the swap target when filter values need updating.

For Phase 4 simplicity, the filter dropdowns are static in the page template (rendered with `roasters`/`countries`/`processes` from the GET / context). The `filters-panel` endpoint is optional refresh — useful when a new roaster is created without a full page reload. If the executor punts on this, simplify by removing the `GET /filters-panel` endpoint + this fragment from the plan, and document the deviation. Recommendation: ship the simpler static-filters version; add `GET /filters-panel` as a Phase 5 follow-up.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery pytest -q tests/phase_04/test_routers_coffees.py tests/phase_04/test_coffee_filters.py -x</automated>
  </verify>
  <done>
    All templates exist. `grep -c 'hx-push-url' app/templates/pages/coffees.html` returns 1. `grep -c 'hx-trigger=.input changed delay:350ms' app/templates/fragments/coffee_form.html` returns ≥ 1. `grep -c 'hx-sync=.this:replace' app/templates/fragments/coffee_form.html` returns ≥ 1. `grep -c 'md:hidden\\|hidden md:' app/templates/fragments/coffee_list.html` returns ≥ 2 (the responsive table↔card pivot). No `|safe` and no `hx-on:` in any coffee template.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author real router tests + filter integration tests</name>
  <files>
    tests/phase_04/test_routers_coffees.py,
    tests/phase_04/test_coffee_filters.py
  </files>
  <read_first>
    tests/phase_04/test_routers_roasters.py,
    tests/phase_04/conftest.py,
    .planning/phases/04-shared-catalog/04-VALIDATION.md
  </read_first>
  <action>
**A. `tests/phase_04/test_routers_coffees.py`** — replace Wave-0 stub. Cases per 04-VALIDATION.md row 04-06-NN:

1. `test_list_coffees_renders_page` — authed GET `/coffees`; 200; body has `<h1` "Coffees" + the filter bar form `id="coffee-filters"`.
2. `test_list_coffees_hx_request_returns_fragment_only` — GET with `HX-Request: true`; body lacks `<html>` / `<head>` markers.
3. `test_list_coffees_includes_responsive_layout_markers` — body contains both `hidden md:table` (or analogous desktop class) AND `md:hidden` (card layout) — confirms CAT-07 dual layout shipped.
4. `test_create_coffee_minimal_valid` — POST `name="Geometry"` + `process="washed"` + `roast_level="light"` (all other fields optional); 200 + row fragment.
5. `test_create_coffee_with_array_round_trip` — seed two flavor notes; POST coffee with `advertised_flavor_note_ids=[<id1>, <id2>]`; service `get_coffee` returns the coffee with the array preserved as `[id1, id2]`.
6. `test_create_coffee_rejects_unknown_process` — POST `process="cold_brewed"`; 200 + form re-render with error on `process`.
7. `test_create_coffee_rejects_blank_name` — POST `name=""`; 200 + error on `name`.
8. `test_create_coffee_extra_field_rejected` — T-04-MASS.
9. `test_coffee_detail_page_renders` — seed a coffee + a bag; GET /{id}; body contains coffee name + the bag section heading "Bags" + the "Open new bag" button.
10. `test_coffee_detail_page_404_for_unknown_id` — GET /999999; 404.
11. `test_edit_pre_populates_advertised_array` — seed coffee with `[id1, id2]`; GET /{id}/edit; body contains hidden inputs for both ids.
12. `test_update_persists_array_change` — POST /{id} with `advertised_flavor_note_ids=[id1]` (drop id2); re-fetch; array is `[id1]`.
13. `test_archive_marks_archived` — POST /archive; archived=True.
14. `test_csrf_missing_returns_403` — negative CSRF.
15. `test_form_renders_roaster_autocomplete_attributes` — GET /new; body contains `hx-trigger="input changed delay:350ms[target.value.length >= 2]"` AND `hx-sync="this:replace"` on the roaster input (HX-4 mitigation locked in template).

**B. `tests/phase_04/test_coffee_filters.py`** — CAT-07 filter integration (per 04-VALIDATION.md row 04-06-NN + Manual-Only Verifications "Filter URL state survives reload" → automated portion):

1. `test_filter_by_roaster_returns_only_matching` — seed coffee A (roaster_id=1) + coffee B (roaster_id=2); GET `/coffees?roaster_id=1`; body contains "A" but not "B".
2. `test_filter_by_country_returns_only_matching` — seed coffees with `country="Ethiopia"` and `country="Kenya"`; GET `?country=Ethiopia`; only Ethiopia in response.
3. `test_filter_by_process_returns_only_matching` — seed washed + natural; GET `?process=washed`; only washed.
4. `test_filter_archived_false_excludes_archived` — seed one archived + one active; GET `?archived=false` (default); only active.
5. `test_filter_archived_true_returns_only_archived` — same setup; GET `?archived=true`; only archived (verify the UI-SPEC lock: archived=true filters to archived-only, not a union).
6. `test_filter_combinations_logical_and` — seed coffees varying multiple dims; GET `?roaster_id=1&country=Ethiopia&process=washed`; result is the intersection.
7. `test_hx_request_with_filters_returns_fragment` — GET `?roaster_id=1` with `HX-Request: true`; body is the list fragment, not a full page.
8. `test_filter_form_hx_push_url_present` — GET `/coffees`; assert the filter form has `hx-push-url="true"` attribute (string match in body) — locks the D-03 contract.

CAT-07's mobile card-list collapse at 375px is in the "Manual-Only Verifications" table (visual test); Phase 12 Playwright will assert no-horizontal-scroll. Phase 4 automated tests cover the HTML markers (test 3 above).

Use the `authed_client` and `fresh_db` fixtures. For test 1+, seed via direct DB inserts (factory helpers in `tests/phase_04/conftest.py` if not already there — add `make_coffee(db, name, roaster_id, country, process, archived)` if useful, or use raw SQLAlchemy inserts).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery pytest -q tests/phase_04/test_routers_coffees.py tests/phase_04/test_coffee_filters.py -x</automated>
  </verify>
  <done>
    Both test files exit 0 with ≥ 15 (router) + ≥ 8 (filters) = ≥ 23 passing tests. Filter tests cover all 4 dimensions individually + a combination. `grep -c 'hx-push-url' tests/phase_04/test_coffee_filters.py` returns ≥ 1 (the contract test).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /coffees | Untrusted form values; Pydantic schema is the gate, including the `advertised_flavor_note_ids: list[int]` array. |
| browser → GET /coffees?... | Filter params bound via SQLAlchemy `select()` — parameterized, never f-string SQL. |
| Jinja template → response body | Autoescape ON; pills + names + filter values all autoescape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-CSRF | Tampering | All state-changing coffee routes | mitigate | Hidden CSRF input verbatim; CSRFFormFieldShim hoists; CSRFMiddleware enforces. |
| T-04-XSS | Tampering / Info Disclosure | Templates rendering coffee name + roaster name + country + origin + notes + flavor pills | mitigate | Jinja autoescape; no `|safe`; pill names from DB (autoescaped); query echo in filter selectbox `selected` attribute is from server-side compared values, not raw client echo. |
| T-04-MASS | Tampering | POST /coffees | mitigate | `CoffeeCreate` declares `extra='forbid'`. |
| (SQLi via filter params) | Tampering | `list_coffees` filter builder | mitigate | All filter params bound via SQLAlchemy `select().where(Column == :param)`; no string concatenation. |
| (array injection) | Tampering | `advertised_flavor_note_ids` form parsing | mitigate | FastAPI `Form(default_factory=list)` types each entry as `int`; non-int entries raise FastAPI's own 422 BEFORE Pydantic. The `field_validator` further asserts `i >= 1`. |
</threat_model>

<verification>
- All 9 endpoints respond as documented.
- Filter bar fires HX-GET with `hx-push-url="true"` (asserted in test_filter_form_hx_push_url_present).
- Responsive layout markers present (asserted in test_list_coffees_includes_responsive_layout_markers).
- Array round-trip (ARRAY(BigInteger)) works for create + edit + update.
- Coffee detail page renders bags section + "Open new bag" affordance.
</verification>

<success_criteria>
- ROADMAP success criterion #2 partially satisfied: coffees list renders + filters work; the mobile-card collapse markers ship (visual verification deferred to Phase 12 Playwright per VALIDATION Manual-Only table).
- ROADMAP success criterion #1 partially satisfied: a logged-in user can create a coffee with autocomplete roaster + flavor notes (autocomplete attributes locked in the template; full interaction wired in plan 04-11).
- The `/coffees/{id}` detail page is the launching pad for plan 04-09 (bag CRUD); the "Open new bag" affordance + `#bag-form-mount` mount div ship now.
</success_criteria>

<output>
Create `.planning/phases/04-shared-catalog/04-07-SUMMARY.md`: list_coffees filter strategy (countries via distinct query vs static dropdown), `selectinload` decision on Coffee.roaster, advertised_flavor_note_ids form-parsing approach, decisions on `/filters-panel` shipping or deferring, the bag-form-mount contract for plan 04-09, AND the chip-widget integration contract for plan 04-11 (exact element IDs, `name="advertised_flavor_note_ids"` hidden-input pattern, `name="flavor_note_query"` autocomplete input shell).
</output>
