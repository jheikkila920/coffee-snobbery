---
phase: 07-ai-services
plan: 06
type: execute
wave: 5
depends_on: ["07-05"]
files_modified:
  - app/routers/home.py
  - app/templates/pages/home.html
  - app/templates/fragments/home/sweet_spots.html
  - app/templates/fragments/home/ai_rec_hero.html
  - app/templates/fragments/home/ai_rec_in_flight.html
  - app/templates/fragments/home/ai_rec_cold_start.html
  - app/templates/fragments/home/ai_rec_not_configured.html
  - app/templates/fragments/home/ai_rec_try_again.html
  - tests/routers/test_home.py
autonomous: false
requirements: [AI-04, AI-05, AI-10, AI-11, AI-14, AI-15, AI-16, HOME-06]
must_haves:
  truths:
    - "The home page shows a top-hero 'what to buy next' card above the analytics aggregate cards (D-01)"
    - "GET /home/cards/ai-recommendation returns one of: cold_start, not_configured, in_flight (polling), try_again, or the hero card"
    - "The in-flight fragment carries hx-trigger='every 2s' so HTMX polls; the completed hero card omits hx-trigger so polling stops"
    - "A manual-refresh button sits on the hero card; a stale badge shows when the current signature differs from the stored one (AI-15)"
    - "The buy link renders verified / 'verifying…' / 'couldn't verify' per url_verified (AI-05); the add-to-wishlist action posts to /ai/wishlist/add"
    - "Sweet-spots AI prose renders below the SQL sweet-spots list when a sweet_spots row exists, as escaped text + <br> (HOME-06, never |safe)"
  artifacts:
    - path: "app/routers/home.py"
      provides: "GET /home/cards/ai-recommendation hero-card fragment endpoint + sweet_spots prose context"
      contains: "card_ai_recommendation"
    - path: "app/templates/fragments/home/ai_rec_hero.html"
      provides: "Composite hero card: name/roaster/why-prose/buy-state/wishlist/recipe/alt-brewer/stale badge/refresh button"
      contains: "ai-rec-hero"
    - path: "app/templates/fragments/home/sweet_spots.html"
      provides: "HOME-06 AI prose append below the SQL list"
      contains: "sweet_spots_prose"
  key_links:
    - from: "app/templates/pages/home.html"
      to: "/home/cards/ai-recommendation"
      via: "hx-get on the AI hero slot at the documented insert point"
      pattern: "home/cards/ai-recommendation"
    - from: "app/routers/home.py::card_ai_recommendation"
      to: "ai_service.get_latest_recommendation / is_stale / in_flight"
      via: "fragment branch selection"
      pattern: "get_latest_recommendation"
---

<objective>
Wire the AI surface into the home page (D-01 top-hero): add the `GET /home/cards/ai-recommendation`
fragment endpoint that returns the correct state (cold-start / not-configured / in-flight polling /
try-again / hero card), replace the home.html AI-slot comment with the lazy-loaded hero section,
create the five home AI fragments, and append the HOME-06 sweet-spots AI prose to sweet_spots.html.
This is the user-visible payoff of the phase.

Purpose: The hero card is the core-value surface ("what to buy next, grounded in your log"). It must
render at 375px, poll while a run is in flight, show the stale badge + manual refresh, render the buy
link verification state, and offer add-to-wishlist — all CSP-clean (no |safe, no hx-on:).

Output: home.py extension + home.html edit + 5 new fragments + sweet_spots.html append + tests +
a human-verify checkpoint at 375px.

This plan has a human-verify checkpoint, so `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-services/07-RESEARCH.md
@.planning/phases/07-ai-services/07-PATTERNS.md
@.planning/phases/07-ai-services/07-CONTEXT.md
@.planning/phases/07-ai-services/07-05-SUMMARY.md

<interfaces>
<!-- ai_service read surface (07-03): -->
get_latest_recommendation(db, *, user_id, rec_type) -> AIRecommendation|None
is_stale(db, *, user_id) -> bool                 # AI-15 stale badge
in_flight(user_id, rec_type="coffee") -> bool    # drives the in_flight polling fragment
credentials.get_provider_credential(db, "anthropic"|"openai") -> ProviderCredential|None  # both None → not_configured (AI-16)
analytics.get_cold_start_counts(db, user_id) -> {..., gate_open}  # AI-11

<!-- AIRecommendation.response_json holds CoffeeRecSchema fields: coffee_name, roaster_name, origin, process,
     roast_level, buy_url, search_tier, summary_prose, recipe_suggestion{recipe_id,recipe_name,summary,no_match},
     alt_brewer{brewer_name,rating_delta,summary}. url_verified is on the ROW (bool|None), not response_json. -->

<!-- Existing home conventions (verified): -->
home.py: Depends(require_user), get_session (SYNC), templates from app.templates_setup, user.id only.
home.html AI slot: the comment at the documented insert point (after the Sweet Spots <section>) — REPLACE it.
sweet_spots.html: append AFTER the closing of the {% if rows %}/{% else %} block (after line 27); the file
   documents "Phase 7 owns the recommendation prose (HOME-06)".
card_sweet_spots handler must additionally pass sweet_spots_prose to the template context (read from the
   latest sweet_spots AIRecommendation row's response_json.summary_prose, or None).
Templates: CSP-clean — NO |safe (AI prose is escaped + <br>), NO hx-on:, Alpine via Alpine.data only.
CSRF: forms carry <input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken','') }}">.
recipe builder link target: /recipes (recipe suggestion no-match links here, AI-06).
Add-to-wishlist posts to /ai/wishlist/add with hidden coffee_name/roaster_name/source_url fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five home AI fragments + sweet_spots.html HOME-06 append</name>
  <files>app/templates/fragments/home/ai_rec_hero.html, app/templates/fragments/home/ai_rec_in_flight.html, app/templates/fragments/home/ai_rec_cold_start.html, app/templates/fragments/home/ai_rec_not_configured.html, app/templates/fragments/home/ai_rec_try_again.html, app/templates/fragments/home/sweet_spots.html</files>
  <read_first>
    - app/templates/fragments/home/top_coffees.html (list card body pattern, autoescape)
    - app/templates/fragments/home/_cold_start.html (progress meter — reuse for ai_rec_cold_start)
    - app/templates/fragments/home/_card_sparse.html (sparse/empty state pattern)
    - app/templates/fragments/home/sweet_spots.html (full file — append point after line 27)
    - .planning/phases/07-ai-services/07-PATTERNS.md (ai_rec_hero lines 533-587; in_flight lines 591-606; cold_start lines 610-617; not_configured lines 622-631; try_again lines 635-647; sweet_spots append lines 651-665; no-|safe rule lines 824-833; Alpine CSP lines 880-894)
    - .planning/phases/07-ai-services/07-CONTEXT.md (D-02 single hero pick; D-06 composite card; AI-05 buy-link states; AI-15 stale badge)
  </read_first>
  <action>
    Create the five fragments under `app/templates/fragments/home/` (all CSP-clean — no |safe, no hx-on:):
    - `ai_rec_hero.html` (D-02/D-06 composite, single hero card): render coffee_name, roaster_name, origin·process·roast_level,
      summary_prose (escaped, <br> for newlines via the |replace('\n','<br>') ONLY if a safe filter exists — otherwise plain
      escaped text; NEVER |safe). Buy-link tri-state per AI-05: rec.url_verified True → real <a href=buy_url>Buy</a>;
      None → "verifying…" span; False → plain-text url + "(couldn't verify)" span. Stale badge when `stale` (AI-15). A manual
      refresh button posting to /ai/refresh (hx-post, CSRF hidden field). recipe_suggestion: if no_match link to /recipes
      (AI-06), else show recipe_name + summary. alt_brewer: render inline only when present (>=0.5 delta already enforced
      server-side, AI-07). add-to-wishlist form posting to /ai/wishlist/add with hidden coffee_name/roaster_name/source_url.
      The COMPLETE state root div has id="ai-rec-hero" and NO hx-trigger (polling stops, Pattern 8).
    - `ai_rec_in_flight.html`: root div id="ai-rec-hero" WITH hx-get="/home/cards/ai-recommendation" hx-trigger="every 2s"
      hx-swap="outerHTML" + an animate-pulse spinner + "Searching the web for fresh coffees…" copy (AI-14).
    - `ai_rec_cold_start.html`: `{% include "fragments/home/_cold_start.html" %}` (reuse Phase 6 meter, AI-11).
    - `ai_rec_not_configured.html`: "AI recommendations are not configured yet." + admin link when user.is_admin (AI-16).
    - `ai_rec_try_again.html`: "Couldn't generate a recommendation right now." + a Try again form posting to /ai/refresh (AI-04).
    Then append the HOME-06 block to `sweet_spots.html` after the existing if/else block: `{% if sweet_spots_prose %}` →
    a bordered <div> rendering sweet_spots_prose as escaped text (NEVER |safe), per 07-PATTERNS lines 651-665.
    If an interactive element needs Alpine, register a component file under /static/js/alpine-components/ and add its <script>
    to base.html before the alpine-csp core (per the established pattern) — prefer plain HTMX + hidden fields to avoid new Alpine.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/ci -q -k "safe or jinja" </automated>
  </verify>
  <acceptance_criteria>
    - Five files exist: ai_rec_hero.html, ai_rec_in_flight.html, ai_rec_cold_start.html, ai_rec_not_configured.html, ai_rec_try_again.html.
    - `ai_rec_in_flight.html` contains `hx-trigger="every 2s"`; `ai_rec_hero.html` (complete state) does NOT contain `hx-trigger` on its root (polling stops).
    - `ai_rec_hero.html` renders all three buy-link states (verified / verifying / couldn't verify) keyed on `rec.url_verified`.
    - `sweet_spots.html` contains a `{% if sweet_spots_prose %}` block rendering the prose (HOME-06).
    - No `|safe` appears in any of the six touched templates (the existing Jinja-safety CI grep test passes).
    - `grep -L "hx-on:" app/templates/fragments/home/ai_rec_*.html` shows no hx-on: usage (CSP-clean).
  </acceptance_criteria>
  <done>Five home AI fragments + the HOME-06 sweet-spots prose append created, CSP-clean, no |safe.</done>
</task>

<task type="auto">
  <name>Task 2: home.py AI hero-card endpoint + home.html slot + sweet_spots prose context</name>
  <files>app/routers/home.py, app/templates/pages/home.html, tests/routers/test_home.py</files>
  <read_first>
    - app/routers/home.py (full file — card_sweet_spots at lines 213-230 is the analog; add the new endpoint + extend card_sweet_spots context)
    - app/templates/pages/home.html (the AI-slot comment after the Sweet Spots section ~line 115)
    - app/services/ai_service.py (get_latest_recommendation, is_stale, in_flight)
    - .planning/phases/07-ai-services/07-PATTERNS.md (home.py new endpoint lines 486-508; home.html insert lines 510-529)
    - .planning/phases/07-ai-services/07-RESEARCH.md (architecture diagram lines 143-205 — branch order)
  </read_first>
  <behavior>
    - GET /home/cards/ai-recommendation returns the cold-start fragment when gate is closed (AI-11).
    - It returns the not-configured fragment when no provider credential is enabled (AI-16).
    - It returns the in-flight fragment (polling) when a run is active (in_flight True).
    - Otherwise it loads the latest coffee row: none → in-flight/empty prompt to generate; present → hero card with the stale badge computed from is_stale.
    - card_sweet_spots additionally passes sweet_spots_prose (from the latest sweet_spots row, or None) so HOME-06 renders.
  </behavior>
  <action>
    Edit `app/routers/home.py`:
    - Add `from app.services import ai_service` (and credentials if not present) and a new handler
      `GET /home/cards/ai-recommendation` -> `card_ai_recommendation(request, user=Depends(require_user), db=Depends(get_session))`
      mirroring 07-PATTERNS lines 486-508. Branch order (RESEARCH diagram): cold-start gate → not-configured (both creds None)
      → in_flight → latest coffee row. For the hero branch, build context: rec (the row), prose (rec.response_json),
      stale=ai_service.is_stale(db, user_id=user.id). Render the matching fragment. For "no row yet but gate open + configured",
      return the in-flight fragment so the poll keeps trying (or a one-time "generate" prompt — keep consistent with the poll).
    - Extend `card_sweet_spots`: load the latest sweet_spots row via ai_service.get_latest_recommendation(db, user_id=user.id,
      rec_type="sweet_spots"); pass `sweet_spots_prose = row.response_json.get("summary_prose") if row else None` into the
      template context (HOME-06, AI-10).
    Edit `app/templates/pages/home.html`: replace the AI-slot comment (after the Sweet Spots <section>) with a top-positioned
    hero <section> per D-01. IMPORTANT placement: the hero card is the home HEADLINE ABOVE the analytics aggregate cards (D-01) —
    move/insert the hero <section> ahead of the aggregate-cards block (top of the gate-open content), not merely where the
    comment sits. The hero slot uses hx-get="/home/cards/ai-recommendation" hx-trigger="load delay:600ms" hx-swap="outerHTML"
    with a skeleton placeholder (07-PATTERNS lines 516-528). Keep the original comment's intent but use load-delay (the
    "revealed" wording in the stale comment is superseded — note this in the SUMMARY).
    Add router tests to `tests/routers/test_home.py`: test_ai_card_cold_start (gate closed → cold-start fragment),
    test_ai_card_not_configured (no creds → not-configured fragment), test_ai_card_in_flight (in_flight monkeypatched True →
    in-flight fragment with hx-trigger), test_ai_card_hero (latest row present → hero card), test_sweet_spots_prose_in_context
    (sweet_spots row present → prose rendered). Mock credentials/ai_service as needed so no SDK is hit.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/routers/test_home.py -x -q -k "ai_card or sweet_spots_prose"</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/home.py` defines `card_ai_recommendation` at GET /home/cards/ai-recommendation gated by require_user.
    - Branch order is cold-start → not-configured → in-flight → hero (matches RESEARCH diagram); each returns the matching fragment.
    - `card_sweet_spots` passes `sweet_spots_prose` into the sweet_spots.html context (HOME-06).
    - `app/templates/pages/home.html` contains `hx-get="/home/cards/ai-recommendation"` positioned ABOVE the analytics aggregate cards (D-01 top-hero).
    - `test_ai_card_not_configured` and `test_ai_card_in_flight` pass.
    - Named pytest selection exits 0.
  </acceptance_criteria>
  <done>Home hero-card endpoint + top-positioned home.html slot + sweet-spots prose context wired; state-branch + HOME-06 tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — home AI hero card at 375px</name>
  <what-built>
    The home page top-hero "what to buy next" card with lazy load + polling, the five AI states
    (cold-start, not-configured, in-flight spinner, try-again, hero card), the buy-link verification
    states, the stale badge + manual-refresh button, add-to-wishlist, and the HOME-06 sweet-spots prose.
  </what-built>
  <how-to-verify>
    1. `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery` (templates are baked; rebuild required).
    2. Open the app at 375px viewport (DevTools device toolbar, iPhone SE width).
    3. As a cold-start user (<3 sessions): confirm the AI hero slot shows the progress meter, NOT a broken card.
    4. With a provider enabled in admin + a gate-open user: confirm the hero card lazy-loads, shows a single
       confident pick (name, roaster, why-prose), a buy link (verified / "verifying…" / "couldn't verify"),
       an add-to-wishlist control, and the manual-refresh button. Confirm no horizontal scroll at 375px.
    5. With NO provider enabled: confirm the "AI not configured" state renders gracefully (no crash).
    6. Click manual refresh: confirm the spinner shows and the card swaps in on completion (polling).
    7. Confirm the hero card sits ABOVE the analytics aggregate cards (D-01).
    8. Scroll to Sweet Spots: confirm the AI prose paragraph renders below the SQL list when present, as plain text.
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/state issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI prose + buy_url → rendered HTML | LLM-generated text/URL rendered in the user's browser |
| browser → home AI-card GET + refresh/wishlist POSTs | Untrusted client requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering / XSS | hero card + sweet-spots prose rendering | mitigate | All AI prose rendered via Jinja autoescape; NEVER |safe; line breaks via escaped <br> only (SEC-05). Acceptance: Jinja-safety CI grep passes; no |safe in touched templates. |
| T-07-14 | Tampering (malicious buy_url) | hero buy link | mitigate | buy_url shown as a live <a> ONLY when rec.url_verified is True (server-verified, AI-05); unverified → plain text, not a clickable link. |
| T-07-12 | Spoofing / Access Control | /home/cards/ai-recommendation | mitigate | `Depends(require_user)`; user_id from request.state.user.id; FragmentCacheHeadersMiddleware no-store. |
| T-07-15 | Information Disclosure (secret) | response_json render | mitigate | response_json holds only CoffeeRecSchema fields; the decrypted key never enters response_json (enforced in 07-03 write helper). |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery python -m pytest tests/routers/test_home.py -q` green.
- Jinja-safety CI grep test passes (no |safe in pages/ or the new fragments).
- Human-verify checkpoint approved at 375px.
</verification>

<success_criteria>
- The home top-hero AI card renders all five states, polls while in-flight, shows the stale badge + manual refresh + buy-link verification + add-to-wishlist, sits above the analytics cards, and the HOME-06 sweet-spots prose renders — all CSP-clean and verified at 375px.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-services/07-06-SUMMARY.md`
</output>
