---
phase: 07-ai-services
plan: 05
type: execute
wave: 4
depends_on: ["07-03", "07-04", "07-02"]
files_modified:
  - app/routers/ai.py
  - app/main.py
  - tests/routers/test_ai_router.py
autonomous: true
requirements: [AI-05, AI-09, AI-13, AI-14, AI-16]
must_haves:
  truths:
    - "POST /ai/refresh triggers regenerate(force=True); a second refresh within 5 minutes returns 429 with HX-Retarget to the please-wait fragment"
    - "A refresh while a run is in flight returns 429 + HX-Retarget (the in-memory lock is the in-flight signal)"
    - "After a coffee row is generated, the buy_url is verified via the SSRF-hardened ranged GET as a background task and url_verified is updated"
    - "POST /ai/equipment generates the on-demand equipment rec; POST /ai/paste-rank ranks pasted text/URLs"
    - "Wishlist add/purchase/remove POSTs are CSRF-enforced and user-scoped; cross-user entry id returns 404"
    - "Every AI route is gated by require_user and reads user_id from request.state.user.id only"
  artifacts:
    - path: "app/routers/ai.py"
      provides: "/ai/refresh, /ai/equipment, /ai/paste-rank, /ai/wishlist/* routes + background URL verify"
      contains: "router = APIRouter(prefix=\"/ai\")"
    - path: "app/main.py"
      provides: "ai_router registration"
      contains: "ai_router.router"
    - path: "tests/routers/test_ai_router.py"
      provides: "Throttle 429, in-flight 429, CSRF, IDOR route tests"
      contains: "def test_throttle_429"
  key_links:
    - from: "app/routers/ai.py"
      to: "app/services/ai_service.py::regenerate"
      via: "manual refresh calls regenerate(user.id, 'manual_refresh', db=db, force=True)"
      pattern: "regenerate\\(.*manual_refresh"
    - from: "app/main.py"
      to: "app/routers/ai.py"
      via: "app.include_router(ai_router.router)"
      pattern: "include_router\\(ai_router"
---

<objective>
Build `app/routers/ai.py`: the state-changing AI routes (manual refresh, equipment-rec generate,
paste-rank submit) and the wishlist routes (add / mark-purchased / remove), plus the background
URL-verification task (AI-05) that updates `url_verified` after a coffee row is written. Wire the
5-minute manual-refresh throttle (AI-14, COST-2) and the in-flight 429 + HX-Retarget pattern
(AI-13). Register the router in `app/main.py`.

Purpose: This is the request surface for every user-triggered AI action. The home-card GET (polling)
lives in 07-06 on the home router; this plan owns the POST actions and the verification background task.

Output: `app/routers/ai.py`, the main.py registration, and `tests/routers/test_ai_router.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-services/07-RESEARCH.md
@.planning/phases/07-ai-services/07-PATTERNS.md
@.planning/phases/07-ai-services/07-CONTEXT.md
@.planning/phases/07-ai-services/07-03-SUMMARY.md
@.planning/phases/07-ai-services/07-04-SUMMARY.md
@.planning/phases/07-ai-services/07-02-SUMMARY.md

<interfaces>
<!-- ai_service public surface (07-03/07-04): -->
async regenerate(user_id, generated_by, *, db, force=False) -> str  # "generated"|"skipped"|"locked"|"try_again"|"not_configured"|"error"
async generate_equipment_rec(user_id, generated_by, *, db) -> tuple[str, AIRecommendation|None]
async rank_pasted_coffees(user_id, generated_by, *, db, raw_input) -> tuple[str, PasteRankSchema|None]
async _verify_buy_url(url, roaster_name, coffee_name) -> bool   # SSRF-hardened
get_latest_recommendation(db, *, user_id, rec_type) -> AIRecommendation|None
in_flight(user_id, rec_type="coffee") -> bool                  # _get_lock(...).locked()
_THROTTLE: dict[int,float] ; _evict_stale_throttle(*, now, window_secs)  # 07-01 throttle state
AI_THROTTLE_BLOCK event constant

<!-- wishlist service (07-02): -->
add_to_wishlist(db, *, by_user_id, coffee_name, roaster_name, source_url, source="ai_recommendation", notes="")
get_wishlist_entry(db, *, entry_id, by_user_id) -> WishlistEntry|None
mark_purchased(db, *, entry_id, by_user_id) -> WishlistEntry|None
remove_entry(db, *, entry_id, by_user_id) -> bool

<!-- Conventions (verified): -->
require_user from app.dependencies.auth ; get_session (SYNC) from app.dependencies.db
templates from app.templates_setup ; user_id ALWAYS request.state.user.id
CSRF: starlette-csrf double-submit; forms carry hidden <input name="X-CSRF-Token"> hoisted by CSRFFormFieldShim — POSTs are NEVER exempt.
FragmentCacheHeadersMiddleware auto-applies no-store + Vary: HX-Request on HX-Request responses (already wired main.py).
main.py router includes block at lines ~220-232 (append ai_router before/after home_router).
FastAPI BackgroundTasks for the post-write URL verification (do not block the response).
The 5 home AI fragments (ai_rec_in_flight.html etc.) are created in 07-06; this plan references
fragments/home/ai_rec_in_flight.html for the 429 body — it MUST exist before merge, so 07-06 is a sibling;
to avoid a cross-wave dependency, return a minimal inline please-wait HTML string from the 429 path if the
fragment is not yet present (07-06 replaces it with the shared fragment).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AI router — manual refresh (throttle + in-flight 429) + background URL verify</name>
  <files>app/routers/ai.py, tests/routers/test_ai_router.py</files>
  <read_first>
    - app/routers/ai.py (new — does not exist yet)
    - app/routers/home.py (fragment endpoint + Depends(require_user) + get_session conventions)
    - app/routers/brew.py (lines 45-66 imports/router setup; CSRF-enforced POST handlers; 824-826 None→404)
    - .planning/phases/07-ai-services/07-PATTERNS.md (router setup lines 366-385; auth gate lines 388-399; HX 429+HX-Retarget lines 439-454; router registration lines 472-478)
    - .planning/phases/07-ai-services/07-RESEARCH.md (Pattern 8 polling lines 491-509; AI-14 throttle line 73; URL verify AI-05 lines 401-449)
    - app/services/ai_service.py (regenerate, in_flight, get_latest_recommendation, _verify_buy_url, _THROTTLE)
  </read_first>
  <behavior>
    - POST /ai/refresh calls regenerate(user.id, "manual_refresh", db=db, force=True) and, on success, triggers an HTMX swap of the hero card region.
    - A second POST /ai/refresh within 5 minutes for the same user returns 429 with HX-Retarget="#ai-rec-hero" + HX-Reswap="outerHTML" and a please-wait body; emits AI_THROTTLE_BLOCK.
    - A POST /ai/refresh while in_flight(user.id) is True returns 429 + HX-Retarget (run already active).
    - After a coffee row is generated, a background task verifies the buy_url via _verify_buy_url and updates ai_recommendations.url_verified (AI-05).
    - The route is gated by require_user; user_id is read only from request.state.user.id.
  </behavior>
  <action>
    Create `app/routers/ai.py` with `router = APIRouter(prefix="/ai")`, imports per 07-PATTERNS lines 371-385,
    structlog logger. Implement:
    - `POST /ai/refresh` (CSRF-enforced, NOT exempt): check throttle — read _THROTTLE.get(user.id); if now - last < 300s,
      emit AI_THROTTLE_BLOCK(seconds_remaining), return a 429 Response with headers HX-Retarget="#ai-rec-hero",
      HX-Reswap="outerHTML" and a please-wait body (render fragments/home/ai_rec_in_flight.html if present, else a minimal
      inline string). Also return the same 429 if `ai_service.in_flight(user.id)`. Otherwise set _THROTTLE[user.id]=now
      (and call _evict_stale_throttle), await regenerate(user.id,"manual_refresh",db=db,force=True). On "generated": schedule
      a BackgroundTasks job `_verify_and_persist_url(user.id)` that loads the latest coffee row, runs _verify_buy_url on its
      buy_url, updates url_verified, commits; then return a 200 that triggers the hero-card poll/swap (HX-Trigger or a direct
      hero fragment render — keep consistent with 07-06's poll endpoint by returning a 204 + HX-Trigger="aiRecUpdated" or a
      fresh hero fragment). On "locked": 429 + HX-Retarget. On "not_configured"/"try_again": render the matching state (07-06
      fragments) or a minimal inline equivalent.
    - `_verify_and_persist_url(user_id)` helper (module function, sync DB + await verify): open a SessionLocal, load the latest
      coffee row via get_latest_recommendation, extract buy_url + roaster/coffee names from response_json, await _verify_buy_url,
      set url_verified True/False, commit. Emit AI_URL_VERIFY. Guard: no buy_url → set url_verified stays None / skip.
    Register nothing in main.py yet (Task 3). Gate every handler with Depends(require_user); never read user_id from form/query.
    Add tests to `tests/routers/test_ai_router.py` (mock ai_service.regenerate so no SDK is hit): test_refresh_triggers_regenerate,
    test_throttle_429 (second refresh within window → 429 + HX-Retarget header present), test_in_flight_429
    (in_flight monkeypatched True → 429), test_refresh_requires_auth (no session → 401).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py -x -q -k "refresh or throttle or in_flight"</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/ai.py` defines `router = APIRouter(prefix="/ai")` and a `POST /ai/refresh` handler gated by `Depends(require_user)`.
    - `test_throttle_429` passes: a second refresh within 5 min returns status 429 with an `HX-Retarget` header (AI-14, COST-2).
    - `test_in_flight_429` passes: refresh while in_flight returns 429 (AI-13).
    - `/ai/refresh` calls `regenerate(user.id, "manual_refresh", db=db, force=True)` (grep matches the pattern).
    - The background task `_verify_and_persist_url` calls `_verify_buy_url` and updates `url_verified` (AI-05).
    - user_id is never read from form/query in any handler (grep: no `form.get("user_id")` / query user_id).
    - Named pytest selection exits 0.
  </acceptance_criteria>
  <done>Manual-refresh route with 5-min throttle + in-flight 429 + HX-Retarget + background URL verification implemented and tested with a mocked service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Equipment-rec, paste-rank, and wishlist routes (CSRF + IDOR)</name>
  <files>app/routers/ai.py, tests/routers/test_ai_router.py</files>
  <read_first>
    - app/routers/ai.py (from Task 1)
    - app/routers/brew.py (CSRF-enforced POST + None→404 sentinel; form parsing minus X-CSRF-Token line 962)
    - app/services/wishlist.py (add/get/mark_purchased/remove signatures)
    - app/services/ai_service.py (generate_equipment_rec, rank_pasted_coffees)
    - .planning/phases/07-ai-services/07-PATTERNS.md (wishlist POST lines 456-470; IDOR scoping lines 795-802; CSRF lines 804-813)
    - .planning/phases/07-ai-services/07-CONTEXT.md (D-05 equipment on-demand; D-07/D-08 paste-rank; D-09 wishlist)
  </read_first>
  <behavior>
    - POST /ai/equipment generates the on-demand equipment rec and returns its rendered fragment/result.
    - POST /ai/paste-rank reads the textarea input, calls rank_pasted_coffees, and returns the top-3 results (or a try_again/not_configured state).
    - POST /ai/wishlist/add adds an entry scoped to the user with source="ai_recommendation".
    - POST /ai/wishlist/{id}/purchase marks purchased; POST /ai/wishlist/{id}/remove deletes — both return 404 for a cross-user id.
    - Every POST is CSRF-enforced (not exempt) and gated by require_user.
  </behavior>
  <action>
    Extend `app/routers/ai.py`:
    - `POST /ai/equipment` (CSRF, require_user): await generate_equipment_rec(user.id,"manual_refresh",db=db); render the
      equipment result region (a fragment created in 07-06, or minimal inline if absent). D-05 on-demand.
    - `POST /ai/paste-rank` (CSRF, require_user): parse `input_text` from the form (strip the X-CSRF-Token field per brew.py
      line 962 convention); await rank_pasted_coffees(user.id,"manual_refresh",db=db,raw_input=input_text); on success render
      pages/paste_rank.html with results (07-07 owns the page; here return the results region or full page). Handle
      "try_again"/"not_configured" with the matching message.
    - `POST /ai/wishlist/add` (CSRF, require_user): parse coffee_name/roaster_name/source_url from form; call
      add_to_wishlist(db, by_user_id=user.id, ...); return 204 + HX-Trigger="wishlistUpdated" (or a small confirmation fragment).
    - `POST /ai/wishlist/{entry_id}/purchase` (CSRF, require_user): mark_purchased(db, entry_id=entry_id, by_user_id=user.id);
      if None raise HTTPException(404); return updated row fragment / 204 + HX-Trigger.
    - `POST /ai/wishlist/{entry_id}/remove` (CSRF, require_user): remove_entry(...); if False raise HTTPException(404); return 204 + HX-Trigger.
    Add tests: test_wishlist_add (row created, user-scoped), test_wishlist_purchase_cross_user_404,
    test_wishlist_remove_cross_user_404, test_paste_rank_route (mock rank_pasted_coffees), test_equipment_route (mock generate_equipment_rec),
    test_wishlist_add_requires_csrf (POST without token → 403). Use the test client's CSRF-cookie helper if one exists in conftest;
    otherwise assert the route is registered without @csrf-exempt and rely on the middleware test for 403.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py -x -q -k "wishlist or paste_rank_route or equipment_route"</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/ai.py` defines POST routes /ai/equipment, /ai/paste-rank, /ai/wishlist/add, /ai/wishlist/{entry_id}/purchase, /ai/wishlist/{entry_id}/remove.
    - `test_wishlist_purchase_cross_user_404` and `test_wishlist_remove_cross_user_404` pass (cross-user id → 404, IDOR).
    - All wishlist/paste-rank/equipment POSTs read user_id from request.state.user.id (grep: no form/query user_id).
    - No AI POST route is marked CSRF-exempt (grep: no csrf-exempt decorator/registration in ai.py).
    - Named pytest selection exits 0.
  </acceptance_criteria>
  <done>Equipment-rec, paste-rank, and wishlist add/purchase/remove routes implemented, CSRF-enforced, user-scoped with 404 IDOR sentinels, and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Register ai_router in main.py + router smoke test</name>
  <files>app/main.py, tests/routers/test_ai_router.py</files>
  <read_first>
    - app/main.py (lines 84-96 router imports; lines 219-232 include_router block)
    - app/routers/ai.py (from Tasks 1-2)
    - tests/routers/test_home.py (router-registration smoke convention)
  </read_first>
  <behavior>
    - The app includes the ai_router so /ai/* routes resolve.
    - The full app boots with the new router registered (no import/registration error).
  </behavior>
  <action>
    Edit `app/main.py`: add `from app.routers import ai as ai_router` in the router-import block (alphabetical, before
    auth_router) and `app.include_router(ai_router.router)` in the include block (place near home_router so the AI surface
    sits with the home surface). Add a smoke test to `tests/routers/test_ai_router.py`: test_ai_routes_registered — assert
    the app's route table contains "/ai/refresh" and "/ai/wishlist/add" (iterate app.routes / app.router.routes).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py -x -q -k "registered"</automated>
  </verify>
  <acceptance_criteria>
    - `app/main.py` contains `from app.routers import ai as ai_router` and `app.include_router(ai_router.router)`.
    - `test_ai_routes_registered` passes: /ai/refresh and /ai/wishlist/add are present in the route table.
    - `ruff check app/routers/ai.py app/main.py` passes.
    - Full file `python -m pytest tests/routers/test_ai_router.py -q` exits 0.
  </acceptance_criteria>
  <done>ai_router registered in main.py; routes resolve; smoke test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /ai/* POST routes | Untrusted client requests, including entry ids and pasted input |
| AI-suggested buy_url → background fetch | AI-generated URL fetched server-side after generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure / Tampering (SSRF) | `_verify_and_persist_url` background task | mitigate | Uses 07-01 `_verify_buy_url` (https-only, no cross-host redirect, 5s, 64KB) on the stored buy_url (HIGH; covered by 07-01 tests). |
| T-07-05 | Information Disclosure (IDOR) | wishlist purchase/remove by id | mitigate | Service filters by user_id; None/False → 404. Tests: test_wishlist_purchase_cross_user_404, test_wishlist_remove_cross_user_404. |
| T-07-07 | Denial of Service / cost abuse | /ai/refresh | mitigate | 5-min per-user throttle (429) + in-flight lock (429) before any LLM call. Tests: test_throttle_429, test_in_flight_429. |
| T-07-11 | Cross-Site Request Forgery | every /ai/* POST | mitigate | starlette-csrf double-submit; no AI POST is csrf-exempt; forms carry the X-CSRF-Token field. Test: test_wishlist_add_requires_csrf (403 without token). |
| T-07-12 | Spoofing / Access Control | all /ai/* routes | mitigate | `Depends(require_user)`; user_id only from request.state.user.id; no form/query user_id. |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py -q` green.
- `ruff check app/routers/ai.py app/main.py` passes.
- grep: every handler in ai.py has `Depends(require_user)`; no `csrf` exemption; no form/query `user_id`.
</verification>

<success_criteria>
- All AI POST routes (refresh, equipment, paste-rank, wishlist add/purchase/remove) live, CSRF-enforced, user-scoped; throttle + in-flight 429 + HX-Retarget work; background URL verification updates url_verified; router registered.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-services/07-05-SUMMARY.md`
</output>
