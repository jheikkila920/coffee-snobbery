---
phase: 07-ai-services
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/wishlist.py
  - tests/services/test_wishlist.py
autonomous: true
requirements: [AI-13]
must_haves:
  truths:
    - "A coffee can be added to the wishlist with source='ai_recommendation' scoped to the calling user"
    - "Listing the wishlist returns only the calling user's entries, newest first"
    - "Marking an entry purchased sets purchased_at; removing deletes it — both reject cross-user ids via a None sentinel"
  artifacts:
    - path: "app/services/wishlist.py"
      provides: "add / list / get / mark-purchased / remove, all user-scoped"
      contains: "def add_to_wishlist"
    - path: "tests/services/test_wishlist.py"
      provides: "Unit tests incl. IDOR cross-user None sentinel"
      contains: "def test_get_wishlist_entry_cross_user_returns_none"
  key_links:
    - from: "app/services/wishlist.py"
      to: "app/models/wishlist_entry.py"
      via: "WishlistEntry select/insert filtered by user_id"
      pattern: "WishlistEntry.user_id =="
---

<objective>
Build the wishlist service (D-09 minimal view): add (with `source="ai_recommendation"`),
list, get-by-id, mark-purchased, remove — every operation scoped to the calling user with the
None→404 IDOR sentinel pattern. This service is independent of the AI flow logic, so it runs
in Wave 1 in parallel with 07-01.

Purpose: Close the "suggest-and-forget" loop — the reason `wishlist_entries` exists. The router
(07-05) and pages (07-07) consume this service.

Output: `app/services/wishlist.py` + `tests/services/test_wishlist.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-services/07-PATTERNS.md
@.planning/phases/07-ai-services/07-CONTEXT.md

<interfaces>
WishlistEntry model columns (app/models/wishlist_entry.py):
  id, user_id, coffee_name (str), roaster_name (str|None), source_url (str|None),
  source (str|None — "ai_recommendation"|"manual"), notes (str, default ""),
  added_at (tz datetime), purchased_at (tz datetime|None).

Per-user CRUD analog: app/services/brew_sessions.py (kwarg-after-star by_user_id; select() filtered by user_id;
get-by-id returns None for cross-user → router maps to 404).
get_session yields a SYNC sqlalchemy.orm.Session.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wishlist service (add / list / get / mark-purchased / remove)</name>
  <files>app/services/wishlist.py</files>
  <read_first>
    - app/services/wishlist.py (new — does not exist yet)
    - app/models/wishlist_entry.py (full column set)
    - app/services/brew_sessions.py (lines 1-49 imports + structlog; per-user scoping; None→sentinel pattern)
    - app/routers/brew.py (lines 824-826 None→404 sentinel usage)
    - .planning/phases/07-ai-services/07-PATTERNS.md (wishlist.py section lines 299-362)
  </read_first>
  <behavior>
    - add_to_wishlist creates a row with user_id=by_user_id and source default "ai_recommendation"; returns the row.
    - list_wishlist returns only by_user_id's rows ordered by added_at desc.
    - get_wishlist_entry returns None for an entry_id that belongs to a different user (IDOR sentinel).
    - mark_purchased sets purchased_at to a non-null timestamp and returns the updated row; returns None for cross-user id.
    - remove_entry deletes the row and returns True; returns False for cross-user id (no delete performed).
  </behavior>
  <action>
    Create `app/services/wishlist.py` with `from __future__ import annotations`, structlog logger, and SQLAlchemy 2.0
    `select()` reads. Implement per 07-PATTERNS lines 299-362:
    - `add_to_wishlist(db, *, by_user_id, coffee_name, roaster_name, source_url, source="ai_recommendation", notes="")` — server-sets user_id, commits, returns WishlistEntry.
    - `list_wishlist(db, *, by_user_id) -> list[WishlistEntry]` — filtered by user_id, order_by added_at desc.
    - `get_wishlist_entry(db, *, entry_id, by_user_id) -> WishlistEntry | None` — select where id==entry_id AND user_id==by_user_id; scalar_one_or_none (cross-user → None).
    - `mark_purchased(db, *, entry_id, by_user_id) -> WishlistEntry | None` — fetch via get_wishlist_entry; if None return None; set purchased_at = func.now() (or datetime.now(tz=UTC)); commit; return row.
    - `remove_entry(db, *, entry_id, by_user_id) -> bool` — fetch via get_wishlist_entry; if None return False; db.delete; commit; return True.
    Emit a structlog line on each write (add/purchase/remove) with user_id + entry_id; never log full source_url at error level beyond what's needed.
    `by_user_id` is ALWAYS a kwarg-after-star and server-set — never sourced from form/query (IDOR defense).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_wishlist.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/wishlist.py` defines add_to_wishlist, list_wishlist, get_wishlist_entry, mark_purchased, remove_entry.
    - All reads/writes filter by `user_id == by_user_id` (grep `WishlistEntry.user_id ==` appears in get/list).
    - `add_to_wishlist` defaults `source="ai_recommendation"` (D-09).
    - `mark_purchased` and `remove_entry` return the None/False sentinel for cross-user ids (no row mutated).
    - `ruff check app/services/wishlist.py` passes.
  </acceptance_criteria>
  <done>Wishlist service complete with user-scoped add/list/get/purchase/remove and IDOR sentinels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wishlist service tests (incl. IDOR cross-user)</name>
  <files>tests/services/test_wishlist.py</files>
  <read_first>
    - tests/services/test_brew_sessions_service.py (DB fixture usage, two-user IDOR test convention)
    - tests/conftest.py (lines 1-60 — db fixtures, seeded user fixtures)
    - app/services/wishlist.py (from Task 1)
  </read_first>
  <behavior>
    - Adding two entries for user A and one for user B → list_wishlist(A) returns exactly A's two, newest first.
    - get_wishlist_entry(A's entry, by_user_id=B) returns None.
    - mark_purchased(A's entry, by_user_id=A) sets purchased_at; mark_purchased(A's entry, by_user_id=B) returns None and leaves purchased_at unchanged.
    - remove_entry(A's entry, by_user_id=B) returns False and the row still exists; remove_entry(A's entry, by_user_id=A) returns True.
  </behavior>
  <action>
    Create `tests/services/test_wishlist.py` using the existing DB + seeded-user fixtures (mirror
    tests/services/test_brew_sessions_service.py). Cover: test_add_and_list_scoped_to_user,
    test_list_order_newest_first, test_get_wishlist_entry_cross_user_returns_none,
    test_mark_purchased_sets_timestamp, test_mark_purchased_cross_user_none,
    test_remove_cross_user_false_keeps_row, test_remove_owner_true.
    If a two-user fixture is not available, create entries against two distinct user_ids seeded inline.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_wishlist.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/services/test_wishlist.py::test_get_wishlist_entry_cross_user_returns_none` passes.
    - `tests/services/test_wishlist.py::test_remove_cross_user_false_keeps_row` passes (row survives a cross-user delete attempt).
    - Full file `python -m pytest tests/services/test_wishlist.py -q` exits 0.
  </acceptance_criteria>
  <done>Wishlist tests green, including cross-user IDOR coverage for get/purchase/remove.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → wishlist write | entry_id and coffee fields arrive from the client; user_id must be server-set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure / Tampering (IDOR) | get/mark_purchased/remove by entry_id | mitigate | Every query filters `user_id == by_user_id`; cross-user id returns None/False; router (07-05) maps None→404 (existence non-leak). Tests: test_get_wishlist_entry_cross_user_returns_none, test_remove_cross_user_false_keeps_row. |
| T-07-06 | Tampering | user_id assignment on add | mitigate | `by_user_id` is a kwarg-after-star, server-set from request.state.user.id; never read from form/query. |
</threat_model>

<verification>
- `python -m pytest tests/services/test_wishlist.py -q` green.
- `ruff check app/services/wishlist.py tests/services/test_wishlist.py` passes.
</verification>

<success_criteria>
- Wishlist service supports the full D-09 minimal view (add/list/purchase/remove), user-scoped, IDOR-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-services/07-02-SUMMARY.md`
</output>
