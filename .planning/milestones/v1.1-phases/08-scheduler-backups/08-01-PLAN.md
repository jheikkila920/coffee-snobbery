---
phase: 08-scheduler-backups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/events.py
  - tests/conftest.py
  - tests/test_scheduler.py
  - tests/test_backup.py
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04]

must_haves:
  truths:
    - "scheduler.* and backup.* event constants exist in app/events.py and are importable"
    - "tests/test_scheduler.py and tests/test_backup.py are collectable (every test imports cleanly or skips, never errors at collection)"
    - "A sync test DB fixture and a mock-regenerate helper exist so Plans 02/03 have ready harnesses"
  artifacts:
    - path: "app/events.py"
      provides: "scheduler.* and backup.* structured-event constants following the ai.* pattern"
      contains: "SCHEDULER_AI_RUN_COMPLETE"
    - path: "tests/test_scheduler.py"
      provides: "Wave 0 test stubs for SCHED-01/02/03 named exactly per 08-VALIDATION.md"
      contains: "test_idempotent_job_registration"
    - path: "tests/test_backup.py"
      provides: "Wave 0 test stubs for SCHED-04 named exactly per 08-VALIDATION.md"
      contains: "test_retention_prune"
  key_links:
    - from: "tests/test_scheduler.py"
      to: "app.events"
      via: "import of SCHEDULER_AI_RUN_COMPLETE"
      pattern: "from app.events import.*SCHEDULER"
---

<objective>
Land the shared foundation both Phase 8 jobs depend on: the `scheduler.*` / `backup.*`
structured-event taxonomy in `app/events.py`, and the Wave 0 pytest scaffolding
(`tests/test_scheduler.py`, `tests/test_backup.py`, plus a sync DB fixture + a
mock-`regenerate` helper in `tests/conftest.py`) named exactly to match the
08-VALIDATION.md requirement→test map.

Purpose: Every `<verify>` in Plans 02/03 references a test that must already exist
(Nyquist Wave 0). Both the AI-refresh job and the backup service emit structured log
lines using the constants added here. Landing both leaves the two implementation
plans (02, 03) free to run in parallel against ready harnesses.

Output: `app/events.py` extended with `scheduler.*` + `backup.*` constants; two new
test modules with named (xfail/skip-until-implemented) stubs; conftest additions for
a sync DB session fixture and a `regenerate` mock helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scheduler-backups/08-CONTEXT.md
@.planning/phases/08-scheduler-backups/08-RESEARCH.md
@.planning/phases/08-scheduler-backups/08-VALIDATION.md

<interfaces>
<!-- Existing event-taxonomy pattern the new constants must follow. Extracted from app/events.py. -->
<!-- ai.* block (Phase 7) is the closest analog — copy its docstring-field-shape style. -->

From app/events.py (existing ai.* pattern — Phase 8 mirrors this structure):
  AI_GENERATION_START = "ai.generation.start"
  AI_GENERATION_SUCCESS = "ai.generation.success"
  AI_REGEN_SKIPPED = "ai.regen.skipped"
  # ...each block has a field-shape comment above it; all names are exported in __all__ (alpha-sorted)

From app/services/settings.py (status rows are written via this; value_type="string" → pass a JSON STRING):
  def set_setting(db: Session, key: str, value: Any, *, by_user_id: int | None) -> None
  # For value_type="string" rows, set_setting coerces with str(value) — so callers MUST json.dumps() first
  # and pass the resulting str. Passing a dict would store its Python repr, NOT valid JSON.

From app/services/ai_service.py (the frozen SCHED-02 contract — DO NOT redesign):
  async def regenerate(user_id: int, generated_by: str, *, db: Session, force: bool = False) -> str
  # returns one of: "generated" | "skipped" | "locked" | "try_again" | "not_configured" | "error"

From tests/conftest.py (existing harness this plan extends):
  - Module-top env bootstrap forces a "<db>_test" DATABASE_URL before any app import.
  - `_postgres_reachable()` — 0.5s TCP probe; tests skip cleanly when Postgres is down.
  - `_seed_user(*, is_admin)` bridges sync fixtures to async inserts via `asyncio.run(_do())`
    — this is the SAME sync→async bridge Plan 03's job body uses to call async regenerate().
  - `fresh_db` autouse fixture DELETEs users/sessions and resets setup_completed before each test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scheduler.* and backup.* event taxonomy to app/events.py</name>
  <files>app/events.py</files>
  <read_first>
    - app/events.py (the full file — copy the ai.* block's field-shape-comment style and the alpha-sorted __all__ convention)
    - .planning/phases/08-scheduler-backups/08-RESEARCH.md §"app/events.py Additions (scheduler.* and backup.*)" (the exact constant names + field shapes)
  </read_first>
  <action>
    Append two new comment-headed blocks to app/events.py after the ai.* block, following
    the existing `<category>.<action>` taxonomy and the per-block field-shape comment style.

    scheduler.* block — add these constants with these exact string values:
    SCHEDULER_STARTED = "scheduler.started", SCHEDULER_SHUTDOWN = "scheduler.shutdown",
    SCHEDULER_JOB_START = "scheduler.job.start", SCHEDULER_JOB_SUCCESS = "scheduler.job.success",
    SCHEDULER_JOB_ERROR = "scheduler.job.error", SCHEDULER_AI_RUN_COMPLETE = "scheduler.ai_run.complete".
    Document the SCHEDULER_AI_RUN_COMPLETE field shape in the block comment: users_processed,
    regenerations, skips, errors, tokens_input_total, tokens_output_total,
    tokens_input_search_total, timestamp (these are the SCHED-03 summary fields).

    backup.* block — add: BACKUP_STARTED = "backup.started", BACKUP_COMPLETE = "backup.complete",
    BACKUP_ARTIFACT_OK = "backup.artifact.ok", BACKUP_ARTIFACT_ERROR = "backup.artifact.error",
    BACKUP_PRUNED = "backup.pruned". Document the BACKUP_COMPLETE field shape: status (ok/error),
    db_filename, db_bytes, photos_filename, photos_bytes, duration_ms, error_msg (optional),
    pruned_count.

    Add all 11 new constant names to the module __all__ list, preserving alphabetical order.
    Note in the block comment that lifespan-emitted events (SCHEDULER_STARTED / SCHEDULER_SHUTDOWN)
    have no request_id (no request context) — same convention as the encryption.* block.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -c "from app.events import SCHEDULER_AI_RUN_COMPLETE, BACKUP_COMPLETE, SCHEDULER_STARTED, BACKUP_PRUNED; assert SCHEDULER_AI_RUN_COMPLETE=='scheduler.ai_run.complete' and BACKUP_COMPLETE=='backup.complete'"</automated>
  </verify>
  <acceptance_criteria>
    - app/events.py contains the literal string `"scheduler.ai_run.complete"` and `"backup.complete"`.
    - All 11 new constants importable from app.events and present in __all__ (grep: __all__ block lists SCHEDULER_AI_RUN_COMPLETE and BACKUP_COMPLETE).
    - `ruff check app/events.py` exits 0.
    - No existing ai.*/catalog.*/brew.* constant removed or renamed.
  </acceptance_criteria>
  <done>The scheduler.* and backup.* taxonomies exist, are exported, follow the ai.* field-shape-comment convention, and import cleanly inside the container.</done>
</task>

<task type="auto">
  <name>Task 2: Add Wave 0 conftest fixtures (sync DB session + mock-regenerate helper)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the whole file — reuse `_postgres_reachable()`, the `<db>_test` env forcing, the `fresh_db` autouse pattern, and the `_seed_user` asyncio.run bridge)
    - app/db.py (the sync `SessionLocal` + `engine` the new fixture binds to)
    - app/services/ai_service.py lines 1126-1152 (the regenerate signature + return-status set to mock against)
  </read_first>
  <action>
    Extend tests/conftest.py with two reusable fixtures Plans 02/03 consume. Do NOT modify
    the existing Phase 0-7 fixtures.

    (a) `sync_db` fixture: yields a sync Session from `app.db.SessionLocal` inside a
    context manager; wraps in the `_postgres_reachable()` probe so it skips cleanly when
    Postgres is down (mirror the existing `fresh_db` skip pattern). This is the session
    Plans 02/03 unit tests use to seed users/recommendations and read back app_settings rows.

    (b) `mock_regenerate` fixture: returns a helper (e.g. a factory that monkeypatches
    `app.services.ai_service.regenerate` with an AsyncMock returning a caller-supplied
    status string or a per-user status map). The job body under test (Plan 03) calls the
    async `regenerate`; the mock must be awaitable. Document that the mock asserts
    `force=False` is passed (highest-risk behavior #4 in 08-VALIDATION.md — the AI job must
    never force-regenerate, which would bypass the signature cost control).

    Keep both fixtures defensive (try/except ImportError → pytest.skip) so the conftest stays
    collectable before Plans 02/03 land their symbols.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` exits 0 (collection succeeds for the whole suite including the new test modules from Task 3).
    - tests/conftest.py defines a `sync_db` fixture and a `mock_regenerate` fixture.
    - The mock_regenerate helper produces an awaitable (AsyncMock or async def) — verifiable by a stub test in Task 3 that awaits it.
    - No existing fixture signature changed.
  </acceptance_criteria>
  <done>Two new collectable conftest fixtures (`sync_db`, `mock_regenerate`) exist; the full suite still collects without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create named Wave 0 test stubs for tests/test_scheduler.py and tests/test_backup.py</name>
  <files>tests/test_scheduler.py, tests/test_backup.py</files>
  <read_first>
    - .planning/phases/08-scheduler-backups/08-VALIDATION.md §"Per-Task Verification Map" (the EXACT test function names and requirement mapping — these names are the contract Plans 02/03 verify against)
    - .planning/phases/08-scheduler-backups/08-RESEARCH.md §"Validation Architecture" + §"Highest-Risk Behaviors to Validate"
    - tests/services/test_ai_service.py lines 707-840 (the @pytest.mark.asyncio + db-session test pattern to mirror)
  </read_first>
  <behavior>
    - Each named test function from 08-VALIDATION.md exists and is collectable.
    - Tests for not-yet-implemented behavior are marked xfail (reason="Plan 02/03 implements") or skip — they MUST NOT error at collection and MUST NOT pass falsely.
    - Once Plans 02/03 land, removing the xfail marker turns each into a real assertion (the stub body already encodes the intended assertion shape as a comment or a minimal assert behind the import guard).
  </behavior>
  <action>
    Create tests/test_scheduler.py with these exactly-named test functions (per 08-VALIDATION.md):
    test_idempotent_job_registration (SCHED-01: exactly 2 jobs after N register_jobs() calls),
    test_lifespan_scheduler_lifecycle (SCHED-01: scheduler starts/stops cleanly),
    test_eligibility_filter (SCHED-02: is_active=True AND >=3 sessions),
    test_ai_run_summary_tally (SCHED-02: regenerate statuses tallied; force=False asserted),
    test_token_aggregation (SCHED-03: sums this-run rows only; web-search split),
    test_status_row_write (SCHED-03: last_ai_run_status written as a JSON string).

    Create tests/test_backup.py with: test_retention_prune (SCHED-04: filename-date prune
    deletes the right files), test_partial_failure_keeps_good (SCHED-04: one artifact fails,
    the other is kept, overall status=error), test_backup_status_row_write (SCHED-04:
    last_backup_status written as a JSON string).

    Mark every test xfail(strict=False) or skip with a reason pointing at the implementing
    plan, importing the not-yet-existing symbols inside the test body behind try/except so
    collection never errors. Encode the intended assertion in the body (so Plan 02/03 just
    removes the marker). Use @pytest.mark.asyncio only where the test awaits regenerate or
    an async path; the backup tests are pure sync.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/test_scheduler.py tests/test_backup.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_scheduler.py tests/test_backup.py` collects exactly the 9 named functions and exits 0 (xfails/skips, zero errors, zero false passes).
    - grep confirms all 9 function names from 08-VALIDATION.md exist in the two files.
    - At least one stub awaits the `mock_regenerate` fixture from Task 2 to prove it is awaitable.
  </acceptance_criteria>
  <done>Both test modules exist with the 9 contractually-named stubs, collectable and xfail/skip-marked; Plans 02/03 verify their work by un-marking these.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only event-name constants and test scaffolding; no untrusted input crosses any boundary, no subprocess, no DB writes outside the test DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | tests/conftest.py `sync_db` fixture | mitigate | Reuse the existing `_postgres_reachable()` + `"test" in db_name` interlock so the new fixture can never mutate the live DB (inherits the conftest safety interlock; do not bypass it). |
| T-08-02 | Information Disclosure | event field shapes | accept | New event constants document field shapes only; no constant carries a secret. The existing structlog redactor (app/logging.py) is unchanged; SCHED-03 summary fields are token counts + statuses, not credentials. |
</threat_model>

<verification>
- `ruff check app/events.py tests/conftest.py tests/test_scheduler.py tests/test_backup.py` exits 0.
- `docker compose exec coffee-snobbery python -m pytest tests/ --collect-only -q` exits 0 (whole suite still collects).
- `docker compose exec coffee-snobbery python -c "import app.events"` exits 0.
</verification>

<success_criteria>
- 11 new event constants exist, are exported, and import cleanly.
- tests/test_scheduler.py + tests/test_backup.py contain all 9 contractually-named stubs from 08-VALIDATION.md, collectable and non-erroring.
- `sync_db` + `mock_regenerate` conftest fixtures exist and are collectable.
- No existing event constant or fixture changed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-scheduler-backups/08-01-SUMMARY.md`
</output>
