---
phase: 10-global-search
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - app/templates/base.html
  - app/static/js/alpine-components/search-bar.js
  - app/static/css/tailwind.src.css
autonomous: true
requirements: [SEARCH-01, SEARCH-03, SEARCH-04]
nyquist_compliant: true

must_haves:
  truths:
    - "The persistent search header renders on authenticated pages and is ABSENT on /login and /setup (auth-gated on request.state.user)"
    - "The search input is wired with hx-get=/search, hx-trigger='keyup changed delay:250ms', hx-sync='this:replace', hx-target, hx-indicator (HX-4 / SEARCH-03)"
    - "At >=768px the input is inline in the header; at <768px the header shows a search icon that opens a full-screen sheet (SEARCH-04)"
    - "The searchBar Alpine component opens/closes the mobile sheet via openSheet()/closeSheet() with Esc + backdrop dismiss, CSP-eval-free"
  artifacts:
    - path: "app/templates/base.html"
      provides: "Auth-gated persistent search header + search-bar.js script registration + #search-results live region"
      contains: "request.state.user"
    - path: "app/static/js/alpine-components/search-bar.js"
      provides: "Alpine.data('searchBar') CSP factory: sheetOpen state, openSheet/closeSheet, Esc handler"
      contains: "Alpine.data('searchBar'"
  key_links:
    - from: "app/templates/base.html"
      to: "/search"
      via: "hx-get on the search input element"
      pattern: "hx-get=\"/search\""
    - from: "app/templates/base.html"
      to: "app/static/js/alpine-components/search-bar.js"
      via: "defer script tag with csp_nonce before the @alpinejs/csp core"
      pattern: "search-bar.js"
    - from: "app/templates/base.html"
      to: "request.state.user"
      via: "{% if request.state.user %} wrapping the entire <header>"
      pattern: "if request.state.user"
---

<objective>
Mount global search in the UI: a thin auth-gated persistent header in base.html holding the search input, the responsive shell (inline desktop input >=768px; icon → full-screen sheet <768px), and the Alpine `searchBar` CSP component that drives the mobile sheet. This calls the `GET /search` endpoint Plan 02 built and turns Plan 01's `test_header_auth_gate` GREEN.

Purpose: This is the SEARCH-01 responsive shell + SEARCH-04 placement. The header is deliberately minimal (search only) — Phase 11 absorbs it into the full top/bottom nav + sign-out (D-01, memory `phase-11-owes-nav-and-signout`). The component is self-contained so Phase 11 can relocate it.

Output: modified `app/templates/base.html` (auth-gated header + script tag + live-results region), new `app/static/js/alpine-components/search-bar.js`, and a confirmation pass on `app/static/css/tailwind.src.css` for the `.htmx-indicator` spinner under strict CSP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-global-search/10-CONTEXT.md
@.planning/phases/10-global-search/10-RESEARCH.md
@.planning/phases/10-global-search/10-PATTERNS.md
@.planning/phases/10-global-search/10-UI-SPEC.md
@.planning/phases/10-global-search/10-01-PLAN.md

<interfaces>
<!-- VERIFIED from codebase this session. -->

base.html structure (VERIFIED):
- head lines 17-25: Alpine.data() component scripts, each `<script defer src="/static/js/alpine-components/X.js" nonce="{{ csp_nonce(request) }}"></script>`, ALL load BEFORE the @alpinejs/csp core (line 31). New search-bar.js tag goes here (after brew-draft.js line 25, before line 26 comment / line 31 core).
- body line 39: `<body class="bg-cream-50 text-espresso-900 dark:bg-espresso-950 dark:text-cream-100">`
- line 40: `{% block content %}{% endblock %}` — the header inserts BETWEEN <body> (39) and this (40); current base.html has no chrome there, the insert is purely additive.
- line 47: `<div id="modal-mount"></div>` already exists — do NOT touch.
- csrf-token meta + theme-color metas already present — do NOT touch.

Auth gate (VERIFIED home.html pattern): `{% if request.state.user %}` (no is_admin needed for search). login.html + setup.html both `{% extends "base.html" %}` (VERIFIED 10-RESEARCH §RQ6) so the gate is REQUIRED to keep the header off /login and /setup.

Alpine component analog (VERIFIED mini-modal.js): register inside `document.addEventListener('alpine:init', () => { Alpine.data('searchBar', () => ({...})); })`; ESC via `window.addEventListener('keydown', this._onKeydown)` in init(), removed in destroy(); no eval / no new Function / no inline object literals in x-data (CSP build rejects them — use string ref `x-data="searchBar"`).

HTMX wiring (VERIFIED UI-SPEC + base.html HTMX 2.0.10): input carries
  hx-get="/search" hx-trigger="keyup changed delay:250ms" hx-sync="this:replace"
  hx-target="#search-results" hx-indicator="#search-spinner" hx-swap="innerHTML"
  name="q" autocomplete="off". Enter is a no-op (@keydown.enter.prevent="" — D-04).

.htmx-indicator (VERIFIED tailwind.src.css lines 22-25): rule ALREADY EXISTS (opacity 0 → 1 on .htmx-request). Confirm it is present; add ONLY if missing (memory `strict-csp-blocks-htmx-indicator` — strict nonce-CSP blocks htmx's auto-injected indicator style, so the rule must live in compiled CSS not be auto-injected). Do not duplicate.

Test target (VERIFIED tests/test_search.py): test_header_auth_gate asserts the search header markup is present on an authed page and ABSENT on GET /login and GET /setup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: searchBar Alpine CSP component — app/static/js/alpine-components/search-bar.js</name>
  <read_first>
    - app/static/js/alpine-components/mini-modal.js (the EXACT analog — alpine:init registration, _onKeydown ESC handler added in init() + removed in destroy(), CSP-eval-free factory, backdrop dismiss method)
    - app/static/js/alpine-components/rating-stars.js lines 31-38 (the this.$root.dataset.* read pattern — only if the component needs DOM-supplied config; searchBar likely does not)
    - .planning/phases/10-global-search/10-UI-SPEC.md §"Component Inventory" 3 (searchBar state: { sheetOpen: false }; methods openSheet/closeSheet; closeSheet clears input + #search-results; Esc + backdrop dismiss) + §"Security Constraints" (no eval / Alpine CSP build)
    - .planning/phases/10-global-search/10-PATTERNS.md §"app/static/js/alpine-components/search-bar.js" (state + method skeleton)
  </read_first>
  <action>
    Create app/static/js/alpine-components/search-bar.js mirroring mini-modal.js exactly. Register inside `document.addEventListener('alpine:init', () => { Alpine.data('searchBar', () => ({ ... })); });` — registration MUST be inside alpine:init (not module top-level) so it exists before the CSP core boots.

    State: `sheetOpen: false`.

    Methods:
    - `init()` — define `this._onKeydown = (e) => { if (e.key === 'Escape' && this.sheetOpen) { this.closeSheet(); } }` and `window.addEventListener('keydown', this._onKeydown)`.
    - `destroy()` — `window.removeEventListener('keydown', this._onKeydown)` (leak-safe, matches mini-modal).
    - `openSheet()` — set `this.sheetOpen = true`; auto-focus the sheet input after render using Alpine's `this.$nextTick(() => { ... .focus(); })` (query the sheet input within `this.$root`). Per UI-SPEC the sheet input auto-focuses on open.
    - `closeSheet()` — set `this.sheetOpen = false`; clear the input value(s) and empty the `#search-results` / sheet results container innerHTML (so a reopened sheet starts clean — UI-SPEC closeSheet contract). Reference elements via `this.$root.querySelector(...)`, not by global id where avoidable, to stay component-scoped.

    CSP rules (hard): NO `eval`, NO `new Function`, NO inline object-literal x-data. All template handlers are declarative Alpine attributes (`x-on:click="openSheet()"`, `@keydown.escape.window="closeSheet()"`, `@click.self="closeSheet()"`) or method references — never string-literal expressions that the CSP evaluator rejects.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/static/js/alpine-components/search-bar.js','utf8'); if(!/Alpine\.data\(['\"]searchBar['\"]/.test(s)) throw new Error('no searchBar factory'); if(/\beval\(|new Function\(/.test(s)) throw new Error('CSP-unsafe construct present'); if(!/addEventListener\(['\"]keydown/.test(s)||!/removeEventListener\(['\"]keydown/.test(s)) throw new Error('Esc handler not leak-safe'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - search-bar.js contains "Alpine.data('searchBar'" registered inside an alpine:init listener
    - Contains both `addEventListener('keydown'` (in init) and `removeEventListener('keydown'` (in destroy)
    - Contains openSheet() and closeSheet() methods; closeSheet clears the results container
    - Contains NO `eval(` and NO `new Function(` (CSP build safety)
    - File is valid JS (node -e parse check above exits 0)
  </acceptance_criteria>
  <done>search-bar.js registers the searchBar CSP factory with sheetOpen state, openSheet/closeSheet, leak-safe Esc handling, and no eval — ready to load before the Alpine CSP core.</done>
</task>

<task type="auto">
  <name>Task 2: Auth-gated persistent search header in base.html + .htmx-indicator confirm</name>
  <read_first>
    - app/templates/base.html (the file being modified — head script block lines 17-25, @alpinejs/csp core line 31, body line 39, {% block content %} line 40, #modal-mount line 47)
    - app/templates/pages/home.html (the request.state.user auth-gate pattern + existing per-page header chrome that STAYS as-is)
    - app/templates/fragments/autocomplete_list.html (sibling fragment styling reference — min-h-[44px], hover classes, color tokens for the results region)
    - .planning/phases/10-global-search/10-UI-SPEC.md §"Component Inventory" 1 (header desktop/mobile spec, exact Tailwind classes, h-14, px-6, input classes, sheet classes, scrim) + §"Interaction Contract" (HTMX wiring table, Enter no-op, dropdown dismiss, aria-live) + §"Copywriting Contract" (placeholder, sheet heading "Search", icon aria-labels, spinner aria-label)
    - .planning/phases/10-global-search/10-PATTERNS.md §"app/templates/base.html (MODIFIED)" (auth gate, script registration, injection point, x-data string-ref rule) + §"CSP nonce on script tags"
    - app/static/css/tailwind.src.css lines 20-30 (confirm the .htmx-indicator rule is present)
    - tests/test_search.py::test_header_auth_gate (the RED test this task turns GREEN)
  </read_first>
  <action>
    Modify app/templates/base.html (additive — do not remove existing chrome, csrf-token meta, theme-color metas, or #modal-mount):

    1. Script registration: add `<script defer src="/static/js/alpine-components/search-bar.js" nonce="{{ csp_nonce(request) }}"></script>` in the head, immediately after the `brew-draft.js` tag (line 25) and BEFORE the `@alpinejs/csp` core (line 31). Every script tag MUST carry `nonce="{{ csp_nonce(request) }}"` or strict CSP blocks it.

    2. Persistent header: insert BETWEEN `<body ...>` (line 39) and `{% block content %}` (line 40), wrapped ENTIRELY in `{% if request.state.user %} ... {% endif %}` (D-01 auth gate — keeps it off /login and /setup, which extend base.html). The outer element is `<header x-data="searchBar" ...>` (string-ref x-data — CSP build rejects object literals). Build the two responsive surfaces per UI-SPEC §Component Inventory 1:

       Desktop (>=768px, `hidden md:flex`): a thin bar `h-14 px-6 bg-cream-100 dark:bg-espresso-900 border-b border-espresso-200 dark:border-espresso-700`, containing an inline magnifying-glass SVG + the search `<input type="search" name="q" autocomplete="off" placeholder="Search coffees, roasters, recipes…">` with the HX-4 attributes: `hx-get="/search" hx-trigger="keyup changed delay:250ms" hx-sync="this:replace" hx-target="#search-results" hx-indicator="#search-spinner" hx-swap="innerHTML"` and `@keydown.enter.prevent=""` (D-04 Enter no-op). Input classes per UI-SPEC: `rounded border border-espresso-200 dark:border-espresso-700 bg-cream-50 dark:bg-espresso-900 px-3 py-2 text-base w-64 lg:w-80`. Add the spinner `<span id="search-spinner" class="htmx-indicator">` (16×16 SVG, aria-label "Searching…"). Add the live-results region `<div id="search-results" role="listbox" aria-label="Search results" aria-live="polite" aria-atomic="false" class="absolute top-full left-0 right-0 z-50 ...">` positioned under the input (relative wrapper).

       Mobile (<768px, `md:hidden`): a magnifying-glass icon `<button x-on:click="openSheet()" aria-label="Open search" class="min-h-[44px] min-w-[44px] ...">`. The full-screen sheet `<div x-show="sheetOpen" @keydown.escape.window="closeSheet()" class="fixed inset-0 z-50 flex flex-col bg-cream-50 dark:bg-espresso-950 md:hidden">` with: a scrim `@click.self="closeSheet()"`; a header row with an h2 "Search" and a close `<button x-on:click="closeSheet()" aria-label="Close search" class="min-h-[44px] min-w-[44px]">` (X icon); a full-width search input carrying the SAME HX-4 attributes but `hx-target` pointed at the sheet's own results container; and a `flex-1 overflow-y-auto` results container. No `history.pushState` (D-03 — back button does not close the sheet). Use only declarative Alpine attributes (CSP-safe).

       Copy (exact, from UI-SPEC §Copywriting): placeholder `Search coffees, roasters, recipes…`; sheet heading `Search`; icon aria-label `Open search`; close aria-label `Close search`; spinner aria-label `Searching…`.

    3. `.htmx-indicator` confirm: verify app/static/css/tailwind.src.css already contains the `.htmx-indicator` opacity rule (VERIFIED present this session). If — and only if — it is missing, add it to the `@layer`/source so the search spinner shows under strict nonce-CSP (memory `strict-csp-blocks-htmx-indicator`). Do NOT duplicate an existing rule.

    Rebuild required after editing base.html / static (CLAUDE.md: image is baked, Jinja caches templates — even an HTML-only edit needs a rebuild or file-level docker cp + restart).
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose cp tests/test_search.py coffee-snobbery:/app/tests/test_search.py && docker compose exec coffee-snobbery python -m pytest tests/test_search.py::test_header_auth_gate -rs -x -q</automated>
  </verify>
  <acceptance_criteria>
    - base.html contains `{% if request.state.user %}` wrapping the new `<header x-data="searchBar"`
    - base.html contains `hx-get="/search"`, `hx-trigger="keyup changed delay:250ms"`, `hx-sync="this:replace"`, and `id="search-results"`
    - base.html contains the `search-bar.js` script tag WITH `nonce="{{ csp_nonce(request) }}"`, placed before the `@alpinejs/csp` core line
    - base.html still contains the unchanged `<div id="modal-mount"></div>` and the csrf-token meta (no accidental removal)
    - base.html contains NO `|safe`, NO `hx-on:`, NO inline object-literal x-data (CSP rules)
    - tailwind.src.css contains a `.htmx-indicator` rule (present, not duplicated)
    - `pytest tests/test_search.py::test_header_auth_gate -x` exits 0 (header present on authed page, absent on /login + /setup)
    - No pytest.skip masking the test (run -rs)
  </acceptance_criteria>
  <done>base.html renders the auth-gated persistent search header (inline desktop input + mobile icon→sheet) wired to GET /search; search-bar.js loads under CSP nonce; test_header_auth_gate is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor → base.html | The persistent header must NOT render for unauthenticated requests (no search affordance on /login or /setup) |
| browser DOM → Alpine searchBar | Component runs under the strict nonce-CSP / Alpine CSP build — no eval-able expressions cross into the evaluator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-HDR-LEAK | Information Disclosure | persistent header in base.html | mitigate | The entire `<header>` is wrapped in `{% if request.state.user %}` so the search affordance never renders for unauthenticated visitors on /login or /setup (D-01). Acceptance: `test_header_auth_gate` GREEN. |
| T-10-CSP | Tampering (script injection) | search-bar.js + base.html script tag | mitigate | Component is the Alpine CSP build (no eval/new Function); the script tag carries `nonce="{{ csp_nonce(request) }}"`; no `hx-on:` or inline object-literal x-data in the template. Acceptance: grep confirms nonce present, no `eval(`/`new Function(`/`hx-on:`/`|safe`. |
| T-10-XSS | Tampering | rendered search results (inherited) | transfer | Result-row escaping is OWNED and tested by Plan 02 (`highlight()` + `test_highlight_xss_safe`). This plan only swaps Plan 02's already-escaped fragment into `#search-results` via HTMX `innerHTML`; it introduces no new user-text rendering. |
</threat_model>

<verification>
- `pytest tests/test_search.py::test_header_auth_gate -rs -q` is GREEN (header present on an authed page, absent on /login + /setup).
- After this plan, the FULL Plan 01 test set is GREEN: `pytest tests/test_search.py -rs -q` (all 15 named tests).
- Per-wave full-suite gate: `pytest tests/ -q` (drop `snobbery_test` first per memory `full-suite-test-isolation-gaps`; treat skips as gaps with -rs).
- Rebuild + restart the container after base.html / static edits (CLAUDE.md baked-image rule) — HTML-only edits do not take effect without a rebuild or file-level docker cp + restart.
- Manual-only (per 10-VALIDATION §Manual-Only): 375px + 768px responsive smoke (icon→sheet, auto-focus, X/Esc/backdrop close, inline desktop input); devtools confirm ≤2 requests on rapid typing (debounce + hx-sync).
</verification>

<success_criteria>
- base.html renders an auth-gated persistent search header: inline input at >=768px, icon→full-screen-sheet at <768px (SEARCH-01/SEARCH-04), wired to GET /search with the HX-4 attributes (SEARCH-03), absent on /login + /setup (D-01).
- search-bar.js is a CSP-safe Alpine factory (sheetOpen, openSheet/closeSheet, leak-safe Esc, no eval).
- `.htmx-indicator` rule confirmed present so the spinner shows under strict CSP.
- `test_header_auth_gate` GREEN; the entire Phase 10 test set GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/10-global-search/10-03-SUMMARY.md`.
Note: commit_docs is false — list the planning/source files for the developer to commit manually (memory `commit-docs-false-untracked-review-docs`).
</output>
