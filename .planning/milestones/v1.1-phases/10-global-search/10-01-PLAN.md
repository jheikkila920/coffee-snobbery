---
phase: 10-global-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_search.py
  - app/migrations/versions/p10_search_indexes.py
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04]
nyquist_compliant: true

must_haves:
  truths:
    - "The full Nyquist test map from 10-VALIDATION.md exists as named tests in tests/test_search.py"
    - "tests/test_search.py defines two distinct users each with their own brew-session notes (IDOR fixtures)"
    - "Running the search tests RED before the service exists fails on a missing search_service.run_search import, not on a collection error"
    - "A migration p10_search_indexes.py creates six GIN trigram indexes and is reachable from the current alembic head (p5_brew_sessions)"
  artifacts:
    - path: "tests/test_search.py"
      provides: "All SEARCH-01..04 + D-06/D-09/D-12 tests including the two-user IDOR fixtures"
      contains: "def test_brew_note_user_scoping"
      min_lines: 200
    - path: "app/migrations/versions/p10_search_indexes.py"
      provides: "Six GIN trigram indexes via op.execute()"
      contains: "USING GIN"
  key_links:
    - from: "tests/test_search.py"
      to: "app.services.search.run_search"
      via: "import inside each test (RED until Plan 02 builds it)"
      pattern: "from app.services import search"
    - from: "app/migrations/versions/p10_search_indexes.py"
      to: "p5_brew_sessions"
      via: "down_revision"
      pattern: "down_revision.*p5_brew_sessions"
---

<objective>
Build the Wave 0 Nyquist test scaffold for global search and the index migration. This plan lands the per-requirement test contract from 10-VALIDATION.md (Dimension 8) — including the CRITICAL two-user IDOR test — and the six GIN trigram indexes that make `ILIKE '%term%'` index-accelerated.

Purpose: Tests come first so Plan 02's service/router has a RED→GREEN target, and the IDOR fixture (User A must never see User B's brew notes) is owned by a dedicated test before any query is written. The migration is independent of app code and can build in parallel with nothing blocking it.

Output: `tests/test_search.py` (all VALIDATION tests, RED initially) + `app/migrations/versions/p10_search_indexes.py` (six indexes, applied on container start).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-global-search/10-VALIDATION.md
@.planning/phases/10-global-search/10-RESEARCH.md
@.planning/phases/10-global-search/10-PATTERNS.md

<interfaces>
<!-- Confirmed from codebase. Executor uses these directly — no exploration needed. -->

Alembic chain (VERIFIED 2026-05-22): current head is `p5_brew_sessions`.
New migration: revision = "p10_search_indexes", down_revision = "p5_brew_sessions".

Conftest fixtures available (tests/conftest.py):
- `client` — sync TestClient wrapping the FastAPI app.
- `seeded_admin_user` / `seeded_regular_user` — each returns
  {"user": User, "session_id": uuid, "signed_cookie": str}. These are TWO
  DISTINCT users — use both to get User A / User B for the IDOR test.
- Authenticated request idiom: client.get(url, cookies={"session_id": fixture["signed_cookie"]})
- `db_session` / `sync_db` — sync Session for direct row inserts.
- `fresh_db` (autouse) wipes users+sessions between tests.

Searchable columns (VERIFIED from models):
- coffees: name (CITEXT), origin, country, roaster_id, archived
- roasters: name (CITEXT), archived
- flavor_notes: name (CITEXT), archived
- recipes: name (Text), grind_setting, archived  — NO description column (D-13)
- equipment: type, brand (Text), model (Text), archived — NO name column (D-14); identity = "{brand} {model}"
- brew_sessions: user_id, coffee_id (NOT NULL RESTRICT), notes (Text, server_default=""), brewed_at

Link targets (VERIFIED router prefixes): /coffees/{id} (detail, D-11) ; /roasters/{id}/edit ;
/equipment/{id}/edit ; /recipes/{id}/edit ; /flavor-notes/{id}/edit ; /brew/{id}/edit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test scaffold — tests/test_search.py with the full VALIDATION map + two-user IDOR fixtures</name>
  <read_first>
    - tests/conftest.py (fixtures: client, seeded_admin_user, seeded_regular_user, db_session, sync_db, fresh_db — lines 81-160, 389-474)
    - .planning/phases/10-global-search/10-VALIDATION.md (the Per-Task Verification Map table — every named test in it must exist here)
    - tests/phase_09/ (closest analog for authenticated TestClient integration tests using the signed_cookie idiom)
    - .planning/phases/10-global-search/10-RESEARCH.md §"Critical Test: SEARCH-04 User Scoping (IDOR)" (the exact assert shape)
  </read_first>
  <action>
    Create tests/test_search.py covering the complete 10-VALIDATION.md Per-Task Verification Map. Each test imports `from app.services import search as search_service` (or hits `GET /search` via the `client`) — these imports/requests stay RED until Plan 02 builds the service and router; that is the intended Wave-0 state. Do NOT stub the service.

    Build a two-user fixture pair using `seeded_admin_user` and `seeded_regular_user` (two distinct users = User A and User B). Insert brew_sessions directly via `sync_db`: User B gets notes="secret Ethiopia mango" tied to a seeded coffee (coffee_id is NOT NULL — seed one coffee first), User A gets a different note. Also seed a shared coffee, roaster, recipe, equipment, flavor_note for the catalog-visibility tests.

    Implement exactly these named test functions (names are load-bearing — the VALIDATION map references them by name):
    - test_header_auth_gate — GET / (or any authed page) as a logged-in user renders the search header; GET /login and GET /setup do NOT contain the search header markup. (SEARCH-01)
    - test_search_coffees — GET /search?q=<substr of seeded coffee name> returns 200 and the coffee name appears. (SEARCH-02)
    - test_search_roasters — roaster name match appears. (SEARCH-02)
    - test_search_recipes — recipe name match appears; assert search is name-only (a query matching only grind_setting must NOT return the recipe). (SEARCH-02, D-13)
    - test_search_equipment — query matching "{brand} {model}" concatenation returns the equipment row; query matching brand alone and model alone each succeed. (SEARCH-02, D-14)
    - test_search_flavor_notes — flavor note name match appears. (SEARCH-02)
    - test_result_group_order — when multiple entity types match, group headers appear in the fixed D-07 order: Coffees, Roasters, Recipes, Equipment, Flavor Notes, Your Brew Notes. (SEARCH-03)
    - test_result_links — each rendered result row links to the correct D-11 URL (/coffees/{id}, /{entity}/{id}/edit, /brew/{id}/edit). (SEARCH-03)
    - test_short_query_empty — GET /search?q=e (1 char) returns 200 with empty body. (SEARCH-04)
    - test_brew_note_user_scoping — CRITICAL IDOR (T-10-IDOR): User A's GET /search?q=mango response does NOT contain "secret Ethiopia mango"; User B's GET /search?q=mango DOES contain it. (SEARCH-04)
    - test_shared_catalog_visible — User A sees the shared coffee/roaster in results. (SEARCH-03)
    - test_highlight_xss_safe — call search_service.highlight(text="<script>alert(1)</script> beans", query="beans") and assert the result string contains the escaped form "&lt;script&gt;" and NOT a raw "<script>"; also assert the matched substring is wrapped in <strong. (D-06, T-10-XSS)
    - test_highlight_markup — search_service.highlight("Ethiopia", "thio") wraps "thio" in <strong class='font-semibold'> with surrounding text intact. (D-06)
    - test_group_cap — seed 6 matching coffees; assert at most 5 result rows render and a "+1 more" hint appears. (D-09)
    - test_archived_scope — seed an archived coffee and archived equipment that match: both surface WITH an "Archived" badge; seed an archived roaster, archived recipe, archived flavor note that match: none of those three appear. (D-12, D-14)

    Use a module-level marker comment at top: tests are RED until Plan 02 (service) and Plan 03 (header). Where a test depends on the header (test_header_auth_gate), it depends on Plan 03 — keep it in this file but it stays RED until Plan 03; do not skip it.

    Do NOT use pytest.skip to make tests pass-by-skip (memory: tests-pass-by-skip-mask-green) — a missing import should xfail or hard-fail at the assertion, never silently skip. Prefer letting the import error surface as a real failure in Wave 0.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -c "import ast; ast.parse(open('/app/tests/test_search.py').read())" && grep -c "def test_" tests/test_search.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_search.py contains def test_brew_note_user_scoping
    - tests/test_search.py contains all 15 named test functions listed above (grep each name returns 1)
    - tests/test_search.py references both seeded_admin_user and seeded_regular_user (two-user fixtures)
    - The IDOR test asserts "secret Ethiopia mango" NOT in User A response AND in User B response
    - No pytest.skip used to mask a missing app.services.search import
    - test_highlight_xss_safe asserts "&lt;script&gt;" present and raw "<script>" absent
  </acceptance_criteria>
  <done>tests/test_search.py exists with all VALIDATION-map tests; running it RED (before Plan 02) fails on missing search_service / 404 /search, never on a collection/syntax error.</done>
</task>

<task type="auto">
  <name>Task 2: Index migration — p10_search_indexes.py (six GIN trigram indexes)</name>
  <read_first>
    - app/migrations/versions/p4_shared_catalog.py lines 32-63 (header + Alembic-safe convention) and 174-178 + 279 (op.execute GIN index DDL + DROP INDEX downgrade)
    - app/migrations/versions/p5_brew_sessions.py lines 53-54 (current head revision + down_revision form to mirror)
    - app/migrations/versions/0001_initial.py lines 60-62 (proof pg_trgm + unaccent already installed — do NOT re-create them)
    - .planning/phases/10-global-search/10-PATTERNS.md §"app/migrations/versions/p10_search_indexes.py" (the exact six DDL statements)
  </read_first>
  <action>
    Create app/migrations/versions/p10_search_indexes.py. Set revision = "p10_search_indexes", down_revision = "p5_brew_sessions". Follow the p4_shared_catalog Alembic-safe convention: NO imports from app.models — describe everything as op.execute() SQL strings.

    Do NOT emit CREATE EXTENSION — pg_trgm and unaccent are already installed in 0001_initial.py (RESEARCH §Standard Stack). Do NOT use CONCURRENTLY — Alembic wraps migrations in a transaction and CREATE INDEX CONCURRENTLY cannot run in one; these tables carry no traffic at first deploy (RESEARCH Pitfall 7).

    upgrade() emits six statements, each `CREATE INDEX IF NOT EXISTS ... USING GIN (... gin_trgm_ops)`:
    - ix_search_coffees_name        ON coffees        (name gin_trgm_ops)
    - ix_search_roasters_name       ON roasters       (name gin_trgm_ops)
    - ix_search_flavor_notes_name   ON flavor_notes   (name gin_trgm_ops)
    - ix_search_recipes_name        ON recipes        (name gin_trgm_ops)
    - ix_search_equipment_brand_model ON equipment    ((brand || ' ' || model) gin_trgm_ops)   ← expression index per D-14
    - ix_search_brew_sessions_notes ON brew_sessions  (notes gin_trgm_ops)

    downgrade() drops all six via `DROP INDEX IF EXISTS ...` in reverse order.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery alembic upgrade head && docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d $POSTGRES_DB -c "SELECT indexname FROM pg_indexes WHERE indexname LIKE 'ix_search_%' ORDER BY 1"</automated>
  </verify>
  <acceptance_criteria>
    - p10_search_indexes.py contains revision = "p10_search_indexes" and down_revision referencing "p5_brew_sessions"
    - File contains "USING GIN" and "gin_trgm_ops" and "(brand || ' ' || model)"
    - File contains NO "CREATE EXTENSION" and NO "CONCURRENTLY" and NO "from app.models"
    - `alembic upgrade head` succeeds and the psql query lists all six ix_search_* indexes
    - `alembic downgrade -1` then `alembic upgrade head` round-trips cleanly
  </acceptance_criteria>
  <done>Migration applies on container start; six ix_search_* GIN trigram indexes exist in Postgres; downgrade removes them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /search | Untrusted `q` query parameter crosses into the search service (tested in Plan 02) |
| User A session → User B brew notes | Per-user data isolation boundary — the IDOR test in this plan is the proof harness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-IDOR | Information Disclosure | brew-note search query | mitigate | This plan OWNS the proving test `test_brew_note_user_scoping`; Plan 02 implements `WHERE brew_sessions.user_id == user_id` as the first clause. Acceptance: User A response excludes User B's note. |
| T-10-XSS | Tampering | highlight() helper | mitigate | This plan OWNS `test_highlight_xss_safe` with the `<script>alert(1)</script>` payload; Plan 02 implements markupsafe.escape composition. Acceptance: escaped output, no raw `<script>`. |
| T-10-MIG | Denial of Service (index build) | p10_search_indexes migration | accept | No CONCURRENTLY: tables are empty/tiny at first deploy; a brief lock during index build is acceptable at household scale (RESEARCH Pitfall 7). |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(...)"` confirms test file parses.
- `grep -c "def test_"` returns 15 (all VALIDATION-map tests present).
- `alembic upgrade head` applies the migration; psql lists six ix_search_* indexes.
- Running `pytest tests/test_search.py -rs -q` at this wave is expected RED (service/router/header not built yet) — failures must be import/404 assertions, never collection errors or pass-by-skip.
</verification>

<success_criteria>
- tests/test_search.py covers the full 10-VALIDATION.md map including the two-user IDOR fixtures and the XSS highlight test.
- p10_search_indexes.py applies cleanly from head p5_brew_sessions and creates six GIN trigram indexes (one an expression index on `brand || ' ' || model`).
- No CREATE EXTENSION, no CONCURRENTLY, no app.models import in the migration.
</success_criteria>

<output>
After completion, create `.planning/phases/10-global-search/10-01-SUMMARY.md`.
Note: commit_docs is false — list the planning/test/migration files for the developer to commit manually.
</output>
