---
phase: 0
plan_id: 05
wave: 4
depends_on: [04]
requirements: FOUND-01, FOUND-02, FOUND-03
files_modified:
  - docker-compose.yml
  - Makefile
  - README.md
autonomous: true
must_haves:
  truths:
    - "`docker compose up -d` from a clean checkout brings up two services on `coffee-snobbery-net` with three named volumes and the web app reachable on `127.0.0.1:8080` (FOUND-01, FOUND-02, FOUND-03)"
    - "The `coffee-snobbery-db` service has a `pg_isready` healthcheck; the `coffee-snobbery` service uses `depends_on: condition: service_healthy` so `entrypoint.sh` never races startup (CONTEXT D-07)"
    - "Web service binds to `127.0.0.1:8080:8000` exclusively — never `0.0.0.0` (CONTEXT D-09)"
    - "Three named volumes exist: `coffee_snobbery_postgres_data`, `coffee_snobbery_photos`, `coffee_snobbery_backups` (FOUND-03)"
    - "`make smoke` performs the full end-to-end cold-start check: `docker compose down -v && docker compose up -d --build && curl http://127.0.0.1:8080/healthz` returns 200"
    - "README.md is publishable: stack overview, prerequisites, env setup, deployment runbook with the loud single-worker note (location #3 of three), NGINX server-block snippet, backup restore stub, troubleshooting"
    - "Single-worker rule reaches location #3 of three (README.md) — `grep -RIn -E '\\-\\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py` returns ≥3 hits across the three required files"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Two-service stack: coffee-snobbery + coffee-snobbery-db on coffee-snobbery-net with three named volumes"
      contains: "coffee_snobbery_postgres_data"
    - path: "Makefile"
      provides: "Developer ergonomics: up, down, logs, psql, migrate, revision, test, smoke, shell, build, fmt, lint"
      contains: "smoke:"
    - path: "README.md"
      provides: "Publishable developer + operator runbook"
      contains: "Snobbery"
  key_links:
    - from: "docker-compose.yml coffee-snobbery service"
      to: "Dockerfile (Plan 04)"
      via: "build: { context: ., dockerfile: Dockerfile }"
      pattern: "dockerfile:\\s*Dockerfile"
    - from: "docker-compose.yml coffee-snobbery service"
      to: "entrypoint.sh (Plan 04)"
      via: "ENTRYPOINT [\"./entrypoint.sh\"] in the Dockerfile"
      pattern: "entrypoint"
    - from: "Makefile smoke target"
      to: "/healthz route (Plan 04)"
      via: "curl http://127.0.0.1:8080/healthz"
      pattern: "/healthz"
    - from: "README.md NGINX section"
      to: "uvicorn --proxy-headers (Plan 04 entrypoint.sh)"
      via: "proxy_pass http://127.0.0.1:8080 + proxy_set_header X-Forwarded-Proto $scheme"
      pattern: "X-Forwarded-Proto"
---

<objective>
Wire the compose stack, the `Makefile` developer ergonomics, and the publishable `README.md`. After this plan, a fresh checkout can be brought up with `make up` (or `docker compose up -d --build`), the smoke runbook (`make smoke`) is green end-to-end, and a new operator has everything they need to deploy to the VPS, restore from backup, and trip over the single-worker rule three separate times (entrypoint.sh, scheduler.py, README.md).

Purpose: This is the closing plan of Phase 0. It does not produce new application code — it produces the deployment surface and the developer documentation that the rest of the project relies on. Three remaining requirements land here: FOUND-01 (compose brings the stack up on port 8080), FOUND-02 (two named services on the named bridge), FOUND-03 (three named volumes). The single-worker rule reaches its third documented location (README.md) per CONTEXT <specifics>.

Output: `docker-compose.yml`, `Makefile`, `README.md`. After this plan, `make smoke` is the phase-gate command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/phases/00-foundation/00-02-SUMMARY.md
@.planning/phases/00-foundation/00-03-SUMMARY.md
@.planning/phases/00-foundation/00-04-SUMMARY.md
@CLAUDE.md
@Dockerfile
@entrypoint.sh
@.env.example

<interfaces>
<!--
Plan 05 publishes:
  - docker-compose.yml — `docker compose` commands are the project's primary developer interface (per CLAUDE.md "Working with the code" section)
  - Makefile — wraps the common flows (CONTEXT D-18 targets: up, down, logs, psql, migrate, revision, test, shell, build; plus the planner-added `smoke`, `fmt`, `lint`)
  - README.md — publishable per ROADMAP.md Phase 0 success criterion #4

Plan 05 consumes:
  - Plan 04 image: built via `docker compose build coffee-snobbery`
  - Plan 04 entrypoint.sh: invoked by Docker on container start
  - Plan 03 migrations: run by entrypoint.sh via `alembic upgrade head`
  - .env.example (Plan 01): documents every env var that docker-compose.yml expects

Verified upstream context (RESEARCH §docker-compose.yml Shape):
  Service names: coffee-snobbery + coffee-snobbery-db
  Network: coffee-snobbery-net (driver: bridge, name: coffee-snobbery-net)
  Volumes (named): coffee_snobbery_postgres_data, coffee_snobbery_photos, coffee_snobbery_backups
  DB healthcheck: pg_isready -U $POSTGRES_USER -d $POSTGRES_DB; interval 5s; timeout 3s; retries 10; start_period 10s
  Web depends_on: coffee-snobbery-db: condition: service_healthy
  Web ports: 127.0.0.1:8080:8000 (CONTEXT D-09 — never 0.0.0.0)
  Web volumes: coffee_snobbery_photos:/app/data/photos + coffee_snobbery_backups:/app/data/backups
  Web env: env_file: .env + DATABASE_URL computed inline
  No DB port published to host (FOUND-02 — internal only)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.yml — two services, three volumes, healthcheck, dependency, port binding</name>
  <files>docker-compose.yml</files>
  <read_first>
    .planning/phases/00-foundation/00-RESEARCH.md §docker-compose.yml Shape (the verbatim YAML — read in full),
    .planning/phases/00-foundation/00-CONTEXT.md §Decisions (D-07 healthcheck dependency, D-09 127.0.0.1:8080:8000 bind),
    .planning/REQUIREMENTS.md FOUND-01, FOUND-02, FOUND-03 verbatim,
    .planning/research/PITFALLS.md SH-3 (photos volume permission rationale — named volumes, not bind mounts) and SH-4 (UID alignment via named volumes),
    .env.example (Plan 01 — every env_file reference must match what's documented),
    CLAUDE.md §"Project" — Container naming + Volume names (verbatim)
  </read_first>
  <action>
    Author `docker-compose.yml` at the repo root per RESEARCH §docker-compose.yml Shape, verbatim. Required shape:

    1. **Top-level `name: coffee-snobbery`** — sets the compose project name so volumes/networks get the expected prefix.

    2. **`services.coffee-snobbery-db`** (database):
       - `image: postgres:16-alpine` (FOUND-02 — Postgres 16; CLAUDE.md stack invariant)
       - `container_name: coffee-snobbery-db` (FOUND-02 verbatim)
       - `restart: unless-stopped`
       - `environment:` `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` all forwarded from the `.env` via `${POSTGRES_USER}` syntax.
       - `volumes:` `coffee_snobbery_postgres_data:/var/lib/postgresql/data` (FOUND-03).
       - `networks:` `- coffee-snobbery-net` (FOUND-02).
       - `healthcheck:` `test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]`, `interval: 5s`, `timeout: 3s`, `retries: 10`, `start_period: 10s` (CONTEXT D-07).
       - NO `ports:` block — DB is NOT published to the host (FOUND-02 "internal only").

    3. **`services.coffee-snobbery`** (web):
       - `image: coffee-snobbery:latest` and a `build:` block (`context: .`, `dockerfile: Dockerfile`) so `docker compose build` rebuilds the image from the Plan 04 Dockerfile.
       - `container_name: coffee-snobbery` (FOUND-02 verbatim).
       - `restart: unless-stopped`.
       - `depends_on: coffee-snobbery-db: { condition: service_healthy }` (CONTEXT D-07; the web container only starts after the DB has answered `pg_isready` successfully).
       - `env_file: [.env]` — pulls every env var documented in `.env.example` from the dev's `.env` (FOUND-09).
       - `environment:` block with `DATABASE_URL: postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@coffee-snobbery-db:5432/${POSTGRES_DB}` — computed in compose so the URL always references the correct service hostname (RESEARCH §docker-compose.yml Shape).
       - `volumes:` two entries:
         - `coffee_snobbery_photos:/app/data/photos` (Phase 4 writes here; volume created now)
         - `coffee_snobbery_backups:/app/data/backups` (Phase 8 writes here; volume created now)
       - `networks: [coffee-snobbery-net]`.
       - `ports: ["127.0.0.1:8080:8000"]` (CONTEXT D-09 — host bind is localhost-only; NGINX on the host proxies to this).

    4. **`networks.coffee-snobbery-net`**: `driver: bridge`, `name: coffee-snobbery-net` (the explicit `name:` prevents compose from prefixing the project name; FOUND-02 requires the bare network name).

    5. **`volumes`**: three top-level entries, each with an explicit `name:` to prevent project-name prefixing (FOUND-03 requires the bare volume names per CLAUDE.md "Container naming"):
       - `coffee_snobbery_postgres_data: { name: coffee_snobbery_postgres_data }`
       - `coffee_snobbery_photos: { name: coffee_snobbery_photos }`
       - `coffee_snobbery_backups: { name: coffee_snobbery_backups }`

    No `version:` key at the top — Compose v2 ignores it and emits a warning. Verified per CLAUDE.md "Working with the code" section.
  </action>
  <verify>
    <automated>python -c "import yaml, pathlib; d = yaml.safe_load(pathlib.Path('docker-compose.yml').read_text()); assert d['name'] == 'coffee-snobbery'; svcs = d['services']; assert set(svcs.keys()) == {'coffee-snobbery','coffee-snobbery-db'}, f'services: {set(svcs.keys())}'; assert svcs['coffee-snobbery-db']['container_name'] == 'coffee-snobbery-db'; assert svcs['coffee-snobbery']['container_name'] == 'coffee-snobbery'; assert 'ports' not in svcs['coffee-snobbery-db'], 'DB must NOT publish ports'; ports = svcs['coffee-snobbery']['ports']; assert ports == ['127.0.0.1:8080:8000'], f'wrong port bind: {ports}'; hc = svcs['coffee-snobbery-db']['healthcheck']; assert 'pg_isready' in ' '.join(hc['test']); dep = svcs['coffee-snobbery']['depends_on']; assert dep['coffee-snobbery-db']['condition'] == 'service_healthy'; vols = d['volumes']; assert set(vols.keys()) == {'coffee_snobbery_postgres_data','coffee_snobbery_photos','coffee_snobbery_backups'}; for k,v in vols.items(): assert v.get('name') == k, f'volume {k} missing explicit name'; net = d['networks']['coffee-snobbery-net']; assert net['name'] == 'coffee-snobbery-net'; assert net['driver'] == 'bridge'; web_env = svcs['coffee-snobbery']['environment']; assert 'postgresql+psycopg://' in web_env.get('DATABASE_URL',''); print('docker-compose.yml OK: 2 services, 3 named volumes, healthcheck, port 127.0.0.1:8080:8000')"</automated>
  </verify>
  <done>
    `docker-compose.yml` parses as valid YAML. Has exactly two services with the verbatim names `coffee-snobbery` + `coffee-snobbery-db`. The DB service has a `pg_isready` healthcheck; the web service uses `condition: service_healthy`. Web service binds `127.0.0.1:8080:8000` (NOT `0.0.0.0`). The DB service has no `ports` block. All three named volumes are declared with explicit `name:` fields. Network `coffee-snobbery-net` is declared with an explicit `name:` field. The web service's `DATABASE_URL` env var uses the `postgresql+psycopg://` scheme.
  </done>
</task>

<task type="auto">
  <name>Task 2: Makefile — developer ergonomics including `smoke` end-to-end gate</name>
  <files>Makefile</files>
  <read_first>
    docker-compose.yml (Task 1 — read so executor knows the service names),
    .planning/phases/00-foundation/00-CONTEXT.md §Decisions (D-18 — required targets: up, down, logs, psql, migrate, revision, test, shell, build),
    .planning/phases/00-foundation/00-VALIDATION.md §Test Infrastructure (`make smoke` is the smoke command),
    .planning/phases/00-foundation/00-RESEARCH.md §Validation Architecture §Sampling Rate (per-task `make test`, per-wave `make smoke`),
    CLAUDE.md §"Working with the code" (the docker compose flows the Makefile wraps verbatim)
  </read_first>
  <action>
    Author `Makefile` at the repo root. Use TAB indentation (Makefiles are TAB-sensitive). Required targets per CONTEXT D-18 plus the planner-added smoke / fmt / lint:

    - `.PHONY: up down logs psql migrate revision test smoke shell build fmt lint`

    - `up:` → `docker compose up -d`
    - `down:` → `docker compose down`
    - `logs:` → `docker compose logs -f coffee-snobbery` (per CLAUDE.md "Working with the code")
    - `logs-db:` → `docker compose logs -f coffee-snobbery-db`
    - `psql:` → `docker compose exec coffee-snobbery-db psql -U $$POSTGRES_USER -d $$POSTGRES_DB` (note the doubled `$$` so make passes a literal `$` through to the shell — the env var is read from the dev's interactive shell or `.env`)
    - `migrate:` → `docker compose exec coffee-snobbery alembic upgrade head`
    - `revision:` → `docker compose exec coffee-snobbery alembic revision --autogenerate -m "$$MSG"` (the dev runs `make revision MSG="add foo column"`)
    - `test:` → `docker compose exec coffee-snobbery pytest -x`
    - `shell:` → `docker compose exec coffee-snobbery bash`
    - `build:` → `docker compose build coffee-snobbery`
    - `fmt:` → `docker compose exec coffee-snobbery ruff format .` (the lint+format target per CLAUDE.md code conventions)
    - `lint:` → `docker compose exec coffee-snobbery ruff check .`
    - `smoke:` → composite recipe: `docker compose down -v` (drop volumes so we test from clean state), then `docker compose up -d --build`, then a `sleep 30` (give the DB healthcheck + migrations time to settle — RESEARCH §Validation §Sampling Rate notes ~30s smoke), then `curl -fsS http://127.0.0.1:8080/healthz` (asserts 200 + JSON), then a series of grep checks against `docker compose logs coffee-snobbery` confirming `Running upgrade .* -> 0001` appears (FOUND-05). The recipe MUST exit non-zero if any sub-command fails — use `&&` chaining or `set -e` semantics.

    Optionally add a `help:` target at the top printing each target with a one-line description, and set `.DEFAULT_GOAL := help`.

    Document at the top of the file (as `#`-prefixed comments) that the Makefile is a thin wrapper around `docker compose` — raw `docker compose` commands still work, per CLAUDE.md.
  </action>
  <verify>
    <automated>python -c "import pathlib, re; m = pathlib.Path('Makefile').read_text(); required = ['up:', 'down:', 'logs:', 'psql:', 'migrate:', 'revision:', 'test:', 'smoke:', 'shell:', 'build:', 'fmt:', 'lint:']; missing = [t for t in required if not re.search(r'^' + re.escape(t), m, re.MULTILINE)]; assert not missing, f'missing make targets: {missing}'; assert '.PHONY' in m, 'PHONY missing'; assert 'docker compose' in m, 'must use docker compose'; assert '/healthz' in m, 'smoke must curl /healthz'; assert 'docker compose down -v' in m, 'smoke must drop volumes for clean state'; assert re.search(r'\.PHONY:.*\\bsmoke\\b|^smoke:', m, re.MULTILINE), 'smoke target malformed'; print('Makefile OK: all 12 required targets present, smoke targets healthz')"</automated>
  </verify>
  <done>
    `Makefile` exists with every required target: `up`, `down`, `logs`, `psql`, `migrate`, `revision`, `test`, `shell`, `build`, `fmt`, `lint`, `smoke`. The `smoke` target performs the end-to-end cold-start check (`down -v && up -d --build && wait && curl /healthz`). All targets are TAB-indented (not space-indented). `.PHONY` is declared for all targets. The file does not invoke `make` recursively (no `$(MAKE)` self-calls).
  </done>
</task>

<task type="auto">
  <name>Task 3: README.md — publishable developer + operator runbook (single-worker rule location #3)</name>
  <files>README.md</files>
  <read_first>
    docker-compose.yml + Makefile (Tasks 1+2 above — README sections must match these),
    Dockerfile + entrypoint.sh (Plan 04 — README references their behavior),
    .env.example (Plan 01 — every documented env var must appear in the README setup section),
    .planning/phases/00-foundation/00-RESEARCH.md §Implementation Strategy Step 7 (README content outline: stack overview, prereqs, setup, NGINX server-block snippet, backup restore stub, troubleshooting with the single-worker note),
    .planning/phases/00-foundation/00-CONTEXT.md §Specifics (the verbatim NGINX server-block snippet pattern; the TRUSTED_PROXY_IPS=127.0.0.1 alignment with reality),
    .planning/phases/00-foundation/00-CONTEXT.md §Decisions (D-04 multi-stage; D-05 non-root UID; D-09 127.0.0.1:8080:8000),
    .planning/ROADMAP.md Phase 0 §Success Criteria #4 (single-worker rule documented in README + entrypoint.sh + scheduler.py),
    .planning/ROADMAP.md Phase 0 §Success Criteria #5 (Tailwind CLI baked; structlog JSON with request_id),
    CLAUDE.md §"Working with the code" (the exact docker compose commands the README must teach),
    CLAUDE.md §"Restore from backup" (verbatim restore commands — README cross-links to CLAUDE.md or duplicates the procedure)
  </read_first>
  <action>
    Author `README.md` at the repo root replacing whatever stub is currently in place. Required sections (markdown headers; the content is the load-bearing part):

    1. **`# Snobbery`** — one-line tagline: "Self-hosted household coffee log — FastAPI + PostgreSQL 16 + HTMX, deployed via Docker Compose."

    2. **`## What this is`** — 2-3 sentences sourced from CLAUDE.md / PROJECT.md: household coffee log for pour-over enthusiasts, shared catalog + per-user sessions + AI rec, deployed behind NGINX on a VPS.

    3. **`## Stack`** — bullet list of pinned tech:
       - Python 3.12 + FastAPI ≥0.136
       - PostgreSQL 16
       - SQLAlchemy 2.0 + Alembic + psycopg 3
       - Jinja2 + HTMX 2.x + Tailwind CSS (standalone CLI v4) + Alpine.js (CDN, added in Phase 1)
       - argon2-cffi + Fernet (`cryptography`) for credentials + API keys
       - structlog 25.x for JSON logging
       - APScheduler in-process (Phase 8)
       - Docker Compose, two containers

    4. **`## Prerequisites`** — Docker + Docker Compose v2 on host. No Python or Node required on the host (the image bakes both Python 3.12 and the Tailwind CLI binary).

    5. **`## Quick start`**:
       ```
       cp .env.example .env
       # Fill in the four secrets (each .env.example line has a generation hint):
       #   POSTGRES_PASSWORD, APP_SECRET_KEY, APP_ENCRYPTION_KEY, (POSTGRES_USER + POSTGRES_DB default to "snobbery")
       make up        # docker compose up -d
       make logs      # tail web container logs
       curl http://127.0.0.1:8080/healthz   # should return {"status":"ok"}
       ```

    6. **`## Working with the code`** — copy/adapt CLAUDE.md's "Working with the code" block: `make up`, `make logs`, `make psql`, `make migrate`, `make revision MSG="..."`, `make test`, `make smoke`, `make shell`, `make build`, `make fmt`, `make lint`. Each target gets one line of explanation.

    7. **`## Deployment (single VPS behind NGINX)`** — the operational section. Required content:
       - Section subheader: **Single uvicorn worker — DO NOT change**. The literal text MUST contain the strings `single worker` and `--workers 1` and explain WHY (APScheduler in-process; module-level AI locks; a future `--workers 4` would fire every nightly job 4× and bill 4× the AI cost). THIS IS LOCATION #3 of THREE for the single-worker rule per CONTEXT <specifics> — the other two are `entrypoint.sh` and `app/services/scheduler.py`. Reference both files explicitly in this section.
       - Subheader: **NGINX server-block snippet** — a verbatim NGINX config block (CONTEXT <specifics>):
         ```
         server {
           listen 443 ssl http2;
           server_name snobbery.example.com;
           # ssl_certificate / ssl_certificate_key handled by your existing setup
           location / {
             proxy_pass http://127.0.0.1:8080;
             proxy_set_header Host $host;
             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
             proxy_set_header X-Forwarded-Proto $scheme;
           }
         }
         ```
         Note that Phase 1 will extend this snippet with HSTS + `proxy_buffering off` + the SSL cipher list — call this out as a forward-looking comment.
       - Subheader: **Deploying a change** — verbatim from CLAUDE.md "Deploy a change to the VPS":
         ```
         git pull
         docker compose build coffee-snobbery
         docker compose up -d coffee-snobbery
         docker compose logs -f coffee-snobbery
         ```
       - Subheader: **TRUSTED_PROXY_IPS** — explain that the default `127.0.0.1` aligns with NGINX-on-the-same-host. If NGINX runs on a different host or a Docker network address, the trust list must be updated; setting it wrong breaks `Secure` session cookies (PITFALL SH-6). Cross-reference Phase 1's future `/debug/proxy` smoke endpoint.

    8. **`## Environment variables`** — one row per env var from `.env.example`, repeating the generation hint. This duplicates `.env.example` but provides a human-readable reference. Keep the list in the same order as `.env.example`.

    9. **`## Restore from backup`** — paste/adapt the verbatim restore procedure from CLAUDE.md §"Restore from backup". Include a warning (sourced from PITFALL SH-3): NEVER restore by copying data files into the volume — always use `psql < dump.sql`. Phase 8 will own the full procedure; Phase 0 documents only what's necessary to recover from a catastrophic loss now.

    10. **`## Troubleshooting`** — a short FAQ:
        - "Healthcheck failing" → run `make logs` + `make logs-db`; check pool exhaustion (PITFALL SH-2 references `pool_timeout=5`).
        - "Cookies dropping after login (future Phase 2)" → check `TRUSTED_PROXY_IPS` (PITFALL SH-6).
        - "Migrations didn't run on startup" → check `docker compose logs coffee-snobbery` for `Running upgrade ... -> 0001_initial` (FOUND-05).
        - "`pg_dump` version mismatch" → the image installs `postgresql-client-16` from PGDG; the slim default is 15 (PITFALL SH-5).

    11. **`## Project history`** — one paragraph: this project was bootstrapped by GSD (an agentic planner+builder) from `docs/snobbery-gsd-prompt.md`. Per CLAUDE.md, that file is HISTORICAL — the code is now the source of truth.

    12. **`## License`** — left blank or "Private (household use)" per spec; planner picks.

    Verify the document is publishable: no TODO markers, no internal `.planning/` references that an external reader couldn't decode, all code blocks complete.
  </action>
  <verify>
    <automated>python -c "import pathlib, re; r = pathlib.Path('README.md').read_text(); required_sections = ['Snobbery', '## Stack', '## Prerequisites', '## Quick start', '## Deployment', '## Environment variables', '## Restore from backup', '## Troubleshooting']; missing = [s for s in required_sections if s not in r]; assert not missing, f'missing README sections: {missing}'; assert 'single worker' in r.lower() or '--workers 1' in r, 'single-worker rule not loud enough in README'; assert 'X-Forwarded-Proto' in r, 'NGINX snippet missing'; assert 'X-Forwarded-For' in r, 'NGINX snippet missing X-Forwarded-For'; assert 'proxy_pass' in r and '127.0.0.1:8080' in r, 'NGINX proxy_pass missing'; assert 'POSTGRES_PASSWORD' in r and 'APP_SECRET_KEY' in r and 'APP_ENCRYPTION_KEY' in r and 'TRUSTED_PROXY_IPS' in r, 'env vars missing from README'; assert 'pg_dump' in r and 'postgresql-client-16' in r, 'PITFALL SH-5 troubleshooting missing'; assert 'snobbery-gsd-prompt.md' in r, 'project history reference missing'; assert 'make up' in r, 'make wrappers undocumented'; assert 'curl' in r and '/healthz' in r, 'quick-start smoke missing'; print('README OK: 8 required sections, single-worker rule, NGINX snippet, all env vars, PITFALL refs')"</automated>
  </verify>
  <done>
    `README.md` is publishable: every required section is present (What this is, Stack, Prerequisites, Quick start, Working with the code, Deployment, Environment variables, Restore from backup, Troubleshooting, Project history). The single-worker rule (with the literal strings `single worker` and `--workers 1` plus the rationale) is in the Deployment section and cross-references `entrypoint.sh` + `app/services/scheduler.py`. The NGINX server-block snippet uses `proxy_pass http://127.0.0.1:8080` + `X-Forwarded-Proto $scheme` + `X-Forwarded-For $proxy_add_x_forwarded_for`. Every env var from `.env.example` appears in the README env-var section. PITFALL SH-5 (`pg_dump` version match) and PITFALL SH-6 (Secure cookies + trust list) are documented in Troubleshooting and Deployment respectively. No `.planning/`-internal references leak into the published doc.

    **Single-worker rule three-place audit:** `grep -RIn -E '\-\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py | wc -l` returns ≥3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → NGINX (host) | TLS termination; host firewall must permit 443; outside Phase 0's scope but documented |
| NGINX (host) → uvicorn (127.0.0.1:8080) | The only ingress to the web container; PROXY headers must be honored (FOUND-08 — wired in Plan 04 entrypoint.sh) |
| host → docker daemon | `docker compose` commands implicitly trust the docker socket; the README does not document any host-side `chown` operations (PITFALL SH-3) |
| README readers (operators) → operational decisions | The single-worker rule is documented THREE times because operator decisions are the highest-risk human factor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-05-01 | Tampering | A future operator scales `--workers` to 4 → APScheduler fires nightly jobs 4× and bills 4× the AI cost (CONTEXT D-04 invariant) | mitigate | Single-worker rule is documented loudly in THREE places: `entrypoint.sh` (location #1), `app/services/scheduler.py` (location #2), README.md (location #3). Audit grep is in the verify command. |
| T-00-05-02 | Information Disclosure | Web service exposed on the public interface | mitigate | `docker-compose.yml` binds `127.0.0.1:8080:8000` only (CONTEXT D-09). README documents this. NGINX runs on host and proxies from `127.0.0.1:8080`. |
| T-00-05-03 | Information Disclosure | DB service exposed to host | mitigate | `docker-compose.yml` has NO `ports:` block on `coffee-snobbery-db`. DB is reachable ONLY from `coffee-snobbery` on the `coffee-snobbery-net` bridge. FOUND-02 verbatim. |
| T-00-05-04 | Denial of Service | Container startup races: web container's `alembic upgrade head` hits a not-yet-ready DB → migration fails → container restarts in a loop | mitigate | `depends_on: { coffee-snobbery-db: { condition: service_healthy } }` plus the DB `pg_isready` healthcheck blocks web startup until DB is fully ready (CONTEXT D-07). |
| T-00-05-05 | Tampering | Operator restores from backup via file copy → Postgres refuses to start due to ownership mismatch (PITFALL SH-3) | mitigate | README "Restore from backup" section documents the `psql < dump.sql` procedure ONLY, with an explicit DO-NOT warning against file-level restore. |
| T-00-05-06 | Spoofing | `TRUSTED_PROXY_IPS` set wrong → uvicorn ignores `X-Forwarded-Proto`, Secure cookies dropped, login bounces (PITFALL SH-6) | mitigate | README Deployment section explicitly documents the trust-list alignment with the NGINX server-block. Phase 1's `/debug/proxy` will end-to-end-verify. |
| T-00-05-07 | Information Disclosure | `.env` committed to a forked repo because the README didn't warn loudly | mitigate | README Quick Start documents `cp .env.example .env`; `.gitignore` (Plan 01) excludes `.env`; `.dockerignore` (Plan 01) excludes `.env` from the build context. Defense in depth. |
</threat_model>

<verification>
1. **Static checks (no Docker required):**
   - Task 1 verify command exits 0 (compose YAML shape).
   - Task 2 verify command exits 0 (Makefile targets present).
   - Task 3 verify command exits 0 (README sections + single-worker rule + NGINX snippet).

2. **Single-worker rule three-place audit:**
   - `grep -RIn -E '\-\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py | wc -l` returns ≥3 (one per file; more is fine).

3. **FOUND-10 not regressed:**
   - `pytest tests/test_no_direct_env.py -x` returns exit 0.

4. **Wave-0 unit tests still green:**
   - `pytest tests/test_logging.py tests/test_env_example.py tests/test_no_direct_env.py -x` returns exit 0.

5. **End-to-end smoke (the gating verification — requires docker):**
   - `make smoke` exits 0 and:
     - `docker compose ps` shows both services up with `coffee-snobbery` healthy.
     - `docker volume ls | grep -E 'coffee_snobbery_(postgres_data|photos|backups)' | wc -l` returns 3 (FOUND-03).
     - `docker network ls | grep coffee-snobbery-net` returns 1 match (FOUND-02).
     - `docker compose logs coffee-snobbery | grep -E 'Running upgrade .* -> 0001'` returns ≥1 match (FOUND-05).
     - `docker compose exec coffee-snobbery ps -ef | grep uvicorn | grep -- '--workers 1'` exits 0 (FOUND-04).
     - `docker compose exec coffee-snobbery ps -ef | grep -- '--proxy-headers'` exits 0 (FOUND-08).
     - `docker compose exec coffee-snobbery pg_dump --version | grep -E '16\.'` exits 0 (FOUND-07).
     - `docker compose exec coffee-snobbery ls app/static/css/ | grep -E 'tailwind\.[0-9a-f]{8}\.css'` exits 0 (FOUND-12).
     - `docker compose exec -T coffee-snobbery-db psql -U $POSTGRES_USER -d $POSTGRES_DB -tAc "SELECT count(*) FROM pg_extension WHERE extname IN ('citext','pg_trgm','unaccent')"` returns `3` (FOUND-06).
     - `docker compose exec -T coffee-snobbery-db psql -U $POSTGRES_USER -d $POSTGRES_DB -tAc "SELECT count(*) FROM app_settings"` returns ≥18 (CONTEXT D-17).
     - `curl -fsS http://127.0.0.1:8080/healthz` returns `{"status":"ok"}` with HTTP 200 (FOUND-01).
     - `curl -fsS http://127.0.0.1:8080/` returns HTML containing "Snobbery" and a `<link>` tag referencing `tailwind.<8 hex>.css` (FOUND-12).
     - `docker compose exec coffee-snobbery pytest -x` runs all tests (including `tests/test_migrations.py` and `tests/test_healthz.py`) green inside the container.

6. **Host-side bind check (FOUND-01 / CONTEXT D-09):**
   - `ss -tlnp | grep 8080` on the host shows a listener on `127.0.0.1:8080`, NOT `0.0.0.0:8080`.
</verification>

<success_criteria>
- `docker-compose.yml` declares two services with the verbatim names from FOUND-02, three named volumes from FOUND-03, the `coffee-snobbery-net` bridge, the `pg_isready` healthcheck, the `service_healthy` dependency, and the `127.0.0.1:8080:8000` bind.
- `Makefile` has all twelve required targets (CONTEXT D-18 + `smoke`/`fmt`/`lint`).
- `README.md` is publishable: all required sections present, single-worker rule called out loudly (location #3 of three), NGINX server-block snippet present, env-var reference complete, restore-from-backup stub present.
- Three-place single-worker rule audit passes (`grep` returns ≥3 hits across README.md + entrypoint.sh + app/services/scheduler.py).
- `make smoke` exits 0 end-to-end:
  - Stack comes up cleanly from `down -v` to healthy in ≤60s.
  - Migrations run on container start.
  - `/healthz` returns 200 with `{"status":"ok"}`.
  - `pg_dump` inside the web container reports v16.x.
  - Tailwind CSS is content-hashed and served from `/static/css/`.
  - Three Postgres extensions present; five tables created; ≥18 `app_settings` rows seeded.
  - `pytest -x` inside the container is green.
- FOUND-10 and Wave-0 unit tests still green.
- DB service has NO `ports` block (internal only).
- Web service binds host-side to `127.0.0.1`, never `0.0.0.0`.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-05-SUMMARY.md` documenting:
- Output of `docker compose ps` after `make smoke` (the two services + their health states).
- Output of `docker volume ls | grep coffee_snobbery_` (proves three named volumes).
- Output of `docker compose exec coffee-snobbery pg_dump --version` (proves FOUND-07).
- Output of `docker compose exec coffee-snobbery ls app/static/css/` (proves FOUND-12 content-hashed filename).
- The grep audit confirming three-place single-worker rule: `grep -RIn -E '\-\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py` output.
- Confirmation that ROADMAP Phase 0 Success Criteria #1–#5 are all green.
- Note for `/gsd-transition`: Phase 0 ships; Phase 1 begins from a baseline where every cross-cutting middleware seat is empty but the structlog contextvars chain (Plan 02), pydantic-settings (Plan 01), Jinja autoescape ON (Plan 04), and `/healthz` (Plan 04) are all in place.
- Note for Phase 1: `/debug/proxy` end-to-end-verifies the trust-list configuration this plan documented in README.
</output>
