---
phase: 0
plan_id: 04
wave: 3
depends_on: [01, 02, 03]
requirements: FOUND-04, FOUND-07, FOUND-08, FOUND-12
files_modified:
  - Dockerfile
  - entrypoint.sh
  - tailwind.config.js
  - app/static/css/tailwind.src.css
  - app/templates/base.html
  - app/templates/pages/index.html
  - app/main.py
  - tests/test_healthz.py
autonomous: true
must_haves:
  truths:
    - "Multi-stage Dockerfile: stage 1 (tailwind-builder) downloads the Tailwind v4 standalone CLI binary and emits a content-hashed CSS file; stage 2 (python:3.12-slim) runtime carries zero Tailwind tooling (FOUND-12, CONTEXT D-04, D-15)"
    - "Runtime image installs postgresql-client-16 from PostgreSQL's PGDG APT repo so `pg_dump` version matches the Postgres 16 server (FOUND-07, PITFALL SH-5)"
    - "Web container runs as non-root user `app` UID 1000 (CONTEXT D-05)"
    - "entrypoint.sh runs `alembic upgrade head` then exec uvicorn with `--workers 1 --proxy-headers --forwarded-allow-ips=$TRUSTED_PROXY_IPS` (FOUND-04, FOUND-08, PITFALL SH-6)"
    - "Single-uvicorn-worker rule is documented in entrypoint.sh as location #1 of three (CONTEXT <specifics>, FOUND-04)"
    - "FastAPI app uses `@asynccontextmanager async def lifespan(app)` — never the deprecated `@app.on_event` (RESEARCH §Pattern 1, RESEARCH §Anti-Patterns to Avoid)"
    - "Lifespan startup calls `configure_logging(...)` from Plan 02 and verifies DB reachability with a `SELECT 1` before yielding"
    - "Exactly two routes: GET /healthz (DB-touching with a 2-second per-request statement_timeout enforced via `SET LOCAL statement_timeout = '2000ms'` inside the request transaction — NOT a second engine; 200 on success / 503 on failure per CONTEXT D-08) and GET / (placeholder Jinja render)"
    - "Jinja2 environment has autoescape ON globally; base.html ships dual theme-color meta tags and references the content-hashed Tailwind CSS via the `tailwind_css_path` global (CONTEXT D-13, PITFALL PWA-5)"
    - "Docker HEALTHCHECK calls /healthz with curl --fail; image marked unhealthy when DB is down (CONTEXT D-08)"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build (tailwind-builder + python:3.12-slim runtime)"
      contains: "FROM python:3.12-slim"
    - path: "entrypoint.sh"
      provides: "alembic upgrade head + uvicorn launcher with proxy-headers + single-worker"
      contains: "--workers 1"
    - path: "tailwind.config.js"
      provides: "Palette baseline (cream + espresso ramps) and content scan paths"
      contains: "darkMode"
    - path: "app/static/css/tailwind.src.css"
      provides: "Tailwind source: @tailwind directives + 16px form-input rule"
      contains: "@tailwind"
    - path: "app/templates/base.html"
      provides: "Jinja shell: doctype, viewport, dual theme-color, Tailwind link"
      contains: "theme-color"
    - path: "app/templates/pages/index.html"
      provides: "Placeholder home page"
      contains: "Snobbery"
    - path: "app/main.py"
      provides: "FastAPI factory + lifespan + StaticFiles + Jinja2 templates + /healthz + /"
      contains: "lifespan"
    - path: "tests/test_healthz.py"
      provides: "TestClient smoke test asserting GET /healthz returns 200 with JSON body"
      contains: "healthz"
  key_links:
    - from: "Dockerfile stage 2"
      to: "Dockerfile stage 1 (tailwind-builder)"
      via: "COPY --from=tailwind-builder /build/app/static/css/tailwind.*.css ./app/static/css/"
      pattern: "COPY --from=tailwind-builder"
    - from: "entrypoint.sh"
      to: "app/migrations/versions/0001_initial.py (Plan 03)"
      via: "alembic upgrade head"
      pattern: "alembic upgrade head"
    - from: "entrypoint.sh"
      to: "app/main.py:app"
      via: "exec uvicorn app.main:app --workers 1 --proxy-headers"
      pattern: "uvicorn app\\.main:app"
    - from: "app/main.py lifespan"
      to: "app/logging.py:configure_logging (Plan 02)"
      via: "lifespan startup calls configure_logging(format=settings.LOG_FORMAT, level=settings.LOG_LEVEL)"
      pattern: "configure_logging"
    - from: "app/main.py /healthz"
      to: "app/db.py:SessionLocal (Plan 03)"
      via: "session.execute(text('SELECT 1')) with the engine's 2-second pool_timeout"
      pattern: "SELECT 1"
    - from: "app/templates/base.html"
      to: "Jinja2 global `tailwind_css_path`"
      via: "<link rel='stylesheet' href='{{ tailwind_css_path }}'>"
      pattern: "tailwind_css_path"
---

<objective>
Land the container build (multi-stage Dockerfile + Tailwind builder stage + postgresql-client-16 install), the runtime entrypoint that runs migrations and launches uvicorn behind the proxy-headers trust list, and the FastAPI app itself — `app/main.py` with lifespan, Jinja2 templates with autoescape ON, StaticFiles mount, and the two Phase 0 routes (`/healthz` DB-touching, `/` placeholder).

Purpose: This plan wires the four bottom-half foundation requirements — FOUND-04 (single uvicorn worker), FOUND-07 (postgresql-client-16 version match), FOUND-08 (proxy-headers trust list), FOUND-12 (Tailwind CLI bake into image). It is the seam between the substrate (Plans 01/02/03) and the deployable container (Plan 05). After this plan, the image builds; after Plan 05, the compose stack runs end-to-end.

Output: `Dockerfile`, `entrypoint.sh`, `tailwind.config.js`, `app/static/css/tailwind.src.css`, `app/templates/base.html`, `app/templates/pages/index.html`, `app/main.py`, `tests/test_healthz.py`. The compose stack is wired in Plan 05 — but `docker build -t coffee-snobbery .` should already succeed after this plan completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/phases/00-foundation/00-02-SUMMARY.md
@.planning/phases/00-foundation/00-03-SUMMARY.md
@.planning/research/PITFALLS.md
@app/config.py
@app/logging.py
@app/db.py
@CLAUDE.md

<interfaces>
<!--
Plan 04 publishes:
  - The built image (no Python API surface for downstream plans — just compose can run it).
  - app/main.py:app — the ASGI callable that entrypoint.sh boots.

Plan 04 consumes:
  - Plan 01: app/config.py:settings, app/services/scheduler.py (already exists — this plan does NOT touch it; it's only referenced for the "three places say single worker" audit)
  - Plan 02: app/logging.py:configure_logging
  - Plan 03: app/db.py:engine, app/db.py:SessionLocal, app/db.py:dispose_engine; alembic.ini; app/migrations/versions/0001_initial.py

Verified upstream context (RESEARCH):
  - Tailwind CLI v4 binary URL: https://github.com/tailwindlabs/tailwindcss/releases/download/${TAILWIND_VERSION}/tailwindcss-linux-x64 (and -linux-arm64 for arm)
  - TAILWIND_VERSION recommended pin: v4.3.0 (RESEARCH §Key Technical Decisions; planner may pin a more recent v4.x)
  - PGDG APT repo install: curl PostgreSQL signing key + add `deb [signed-by=...] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main`
  - Researcher Open Question #5: keep curl in runtime stage (do NOT purge after PGDG install); HEALTHCHECK uses it
  - uvicorn flags: --host 0.0.0.0 --port 8000 --workers 1 --proxy-headers --forwarded-allow-ips="$TRUSTED_PROXY_IPS"
  - lifespan pattern: @asynccontextmanager async def lifespan(app: FastAPI); startup → configure_logging → verify engine.connect() can SELECT 1; yield; shutdown → dispose_engine()
  - Jinja2: autoescape=True globally; expose tailwind_css_path as a global computed at startup via glob('app/static/css/tailwind.*.css')
  - base.html dual theme-color: cream #FAF7F2 (light) + espresso #1A1110 (dark); viewport=width=device-width,initial-scale=1,viewport-fit=cover; NO HTMX/Alpine yet (Phase 1)
  - /healthz returns {"status":"ok"} 200 / 503 + structured-log entry on failure
  - HEALTHCHECK: curl -fsS http://127.0.0.1:8000/healthz
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind source + config + base.html + placeholder index.html</name>
  <files>
    tailwind.config.js,
    app/static/css/tailwind.src.css,
    app/templates/base.html,
    app/templates/pages/index.html
  </files>
  <read_first>
    .planning/phases/00-foundation/00-CONTEXT.md §Decisions (D-13 base.html shell shape, D-14 palette baseline + darkMode media, D-15 content-hashed static asset path),
    .planning/phases/00-foundation/00-RESEARCH.md §Implementation Strategy Step 2 (Tailwind v4 + base.html walkthrough),
    .planning/phases/00-foundation/00-RESEARCH.md §Pitfall PWA-5 (dual theme-color meta — verbatim hex values cream #FAF7F2 + espresso #1A1110),
    .planning/phases/00-foundation/00-RESEARCH.md §Open Questions #6 (content scan paths include app/static/js/ even though Phase 0 has no JS files there yet — saves a Tailwind config edit in Phase 1),
    CLAUDE.md §"Code conventions" (templates: 2-space indent, snake_case variables, NO `|safe`, autoescape ON)
  </read_first>
  <action>
    Author `tailwind.config.js` (Tailwind v4 standalone CLI config — JavaScript module form). Required content:
      - `content: ["./app/templates/**/*.html", "./app/static/js/**/*.js"]` (scan templates + future JS; RESEARCH Open Question #6).
      - `darkMode: 'media'` (CONTEXT D-14 — system preference; no manual toggle in v1).
      - `theme.extend.colors.cream`: a 50–950 ramp anchored on `#FAF7F2`. Defensible defaults (planner picks any monotonic warm-off-white scale; tunable in `/gsd-ui-phase`). Required hex anchors: `50: '#FAF7F2'` (the light-mode theme-color), and `950` darker than the rest.
      - `theme.extend.colors.espresso`: a 50–950 ramp anchored on `#3D2817` (CONTEXT D-14 mid-tone) with `950: '#1A1110'` (PITFALL PWA-5 dark-mode theme-color value). Required hex anchors: `950: '#1A1110'`.
      - No plugins (Phase 0 doesn't need forms/typography plugins).

    Author `app/static/css/tailwind.src.css` with three Tailwind v4 directives plus the 16px form-input rule:
      - `@tailwind base;`
      - `@tailwind components;`
      - `@tailwind utilities;`
      - Custom layer block: `@layer base { input, select, textarea { font-size: 16px; } }` — PITFALL MX-1 (iOS Safari auto-zoom prevention). Phase 5 depends on this rule existing globally; landing it now is cheap (RESEARCH §Implementation Strategy Step 2).

    Author `app/templates/base.html` per CONTEXT D-13 — a REAL shell, not a stub. Required content (2-space indent, snake_case Jinja variables):
      - `<!doctype html>`
      - `<html lang="en">`
      - `<head>` with:
        - `<meta charset="utf-8">`
        - `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">` (the `viewport-fit=cover` is REQUIRED for iOS safe-area handling later in Phase 11)
        - `<meta name="theme-color" content="#FAF7F2" media="(prefers-color-scheme: light)">` (PITFALL PWA-5)
        - `<meta name="theme-color" content="#1A1110" media="(prefers-color-scheme: dark)">` (PITFALL PWA-5)
        - `<title>Snobbery — {% block page_title %}{% endblock %}</title>` (per CLAUDE.md / UX-03 future requirement — landing the format now)
        - `<link rel="stylesheet" href="{{ tailwind_css_path }}">` (the global is set by `app/main.py` at startup)
      - `<body class="bg-cream-50 text-espresso-900 dark:bg-espresso-950 dark:text-cream-100">`
      - `{% block content %}{% endblock %}`
      - `</body></html>`
      - NO `<script>` tags (Phase 1 adds HTMX + Alpine).
      - NO `{% autoescape false %}` and NO `{{ ... |safe }}` anywhere — this is the file that establishes the template convention CLAUDE.md mandates.

    Author `app/templates/pages/index.html`:
      - `{% extends "base.html" %}`
      - `{% block page_title %}Bootstrap{% endblock %}`
      - `{% block content %}` containing a single `<main class="mx-auto max-w-prose px-6 py-12">` with `<h1 class="text-2xl font-semibold">Snobbery</h1>` and a `<p>` whose literal text is `Snobbery — setup pending. POST /setup once auth lands.` (CONTEXT D-12 — exact placeholder copy). Mobile-readable at 375px (the `max-w-prose px-6` handles this trivially).
      - `{% endblock %}`
  </action>
  <verify>
    <automated>python -c "import pathlib, re; tw = pathlib.Path('tailwind.config.js').read_text(); assert \"darkMode: 'media'\" in tw or 'darkMode: \"media\"' in tw, 'darkMode missing'; assert 'cream' in tw and 'espresso' in tw, 'palette colors missing'; src = pathlib.Path('app/static/css/tailwind.src.css').read_text(); assert '@tailwind base' in src and '@tailwind utilities' in src; assert 'font-size: 16px' in src or 'font-size:16px' in src, '16px rule missing'; base = pathlib.Path('app/templates/base.html').read_text(); assert 'tailwind_css_path' in base; assert '#FAF7F2' in base and '#1A1110' in base, 'theme-color values missing'; assert 'viewport-fit=cover' in base; assert '|safe' not in base, '|safe forbidden in base.html'; idx = pathlib.Path('app/templates/pages/index.html').read_text(); assert 'extends \"base.html\"' in idx; assert 'Snobbery' in idx; print('Templates + Tailwind config OK')"</automated>
  </verify>
  <done>
    `tailwind.config.js` has `darkMode: 'media'` and the cream + espresso color ramps with `#FAF7F2` and `#1A1110` hex anchors. `app/static/css/tailwind.src.css` has all three `@tailwind` directives + the 16px input rule. `app/templates/base.html` has the dual theme-color meta tags with the exact hex values, the viewport-fit=cover meta, the `tailwind_css_path` Jinja global reference, and no `|safe` anywhere. `app/templates/pages/index.html` extends base.html and renders the placeholder copy.
  </done>
</task>

<task type="auto">
  <name>Task 2: app/main.py — FastAPI factory + lifespan + StaticFiles + /healthz + / + Jinja config</name>
  <files>
    app/main.py,
    tests/test_healthz.py
  </files>
  <read_first>
    app/main.py (does not exist yet — confirm),
    app/config.py (Plan 01),
    app/logging.py (Plan 02),
    app/db.py (Plan 03 — note `engine`, `SessionLocal`, `dispose_engine`, `Base`),
    app/templates/base.html (Task 1 above),
    .planning/phases/00-foundation/00-RESEARCH.md §Pattern 1 (FastAPI lifespan + StaticFiles mount + Jinja env — verbatim),
    .planning/phases/00-foundation/00-RESEARCH.md §Implementation Strategy Step 5 (healthz route detail — 2s pool_timeout via session.execute),
    .planning/phases/00-foundation/00-CONTEXT.md §Decisions (D-08 DB-touching /healthz, D-12 exactly two routes, D-15 tailwind_css_path computed at startup),
    .planning/phases/00-foundation/00-RESEARCH.md §Anti-Patterns to Avoid (no @app.on_event; no os.environ outside config; no async handler calling sync Session.execute),
    .planning/REQUIREMENTS.md FOUND-04, FOUND-08, FOUND-12 verbatim
  </read_first>
  <action>
    Author `app/main.py` per RESEARCH §Pattern 1 with these specifics:

    1. **Imports:** `from __future__ import annotations` first; then `from contextlib import asynccontextmanager`; `from pathlib import Path`; `from typing import AsyncIterator`; `from fastapi import FastAPI, Request, Response`; `from fastapi.staticfiles import StaticFiles`; `from fastapi.templating import Jinja2Templates`; `from sqlalchemy import text`; `from app.config import settings`; `from app.db import SessionLocal, dispose_engine, engine`; `from app.logging import configure_logging`.

    2. **`compute_tailwind_css_path()` helper** — a module-level function that globs `app/static/css/tailwind.*.css` (excluding `tailwind.src.css`) and returns the URL path `/static/css/<filename>` of the single hashed result. If exactly zero results: raise `RuntimeError("Tailwind CSS missing — Dockerfile stage 1 did not produce app/static/css/tailwind.<hash>.css")`. If multiple results: raise `RuntimeError` listing the duplicates (something has gone wrong in the build). This function is called ONCE at app-factory time, NOT per request (CONTEXT D-15).

    3. **`@asynccontextmanager async def lifespan(app: FastAPI) -> AsyncIterator[None]`:**
       - Startup:
         - `configure_logging(format=settings.LOG_FORMAT, level=settings.LOG_LEVEL)` (Plan 02)
         - Smoke the engine: `with engine.connect() as conn: conn.execute(text("SELECT 1"))` — this proves the DB is reachable. If this raises, FastAPI startup fails loudly (intended).
         - Log a structured `app.startup` event via structlog.
       - `yield`
       - Shutdown:
         - Log `app.shutdown` event.
         - `dispose_engine()` (Plan 03 helper).

    4. **`def create_app() -> FastAPI`:**
       - `app = FastAPI(lifespan=lifespan, title="Snobbery", version="0.1.0")`
       - `app.mount("/static", StaticFiles(directory="app/static"), name="static")` — mounts the static directory. RESEARCH §Anti-Patterns: do NOT mount `app/static/photos` here (Phase 4 owns photos via a custom router).
       - `templates = Jinja2Templates(directory="app/templates")` — autoescape is ON by default in `Jinja2Templates`. Verify by NOT calling `templates.env.autoescape = False` anywhere.
       - Compute `tailwind_css_path = compute_tailwind_css_path()` at factory time, then `templates.env.globals["tailwind_css_path"] = tailwind_css_path` so `base.html`'s `{{ tailwind_css_path }}` resolves on every render.
       - Attach `templates` to `app.state.templates` so route handlers can pull it via `request.app.state.templates`.

    5. **Two routes registered on `app`:**

       - `@app.get("/healthz")` — synchronous handler (RESEARCH Anti-Patterns: do NOT make it `async def`; sync handler avoids the async/sync DB problem). Honor CONTEXT D-08's 2-second timeout via a per-request statement_timeout — NOT a second engine (a second engine duplicates pool bookkeeping, complicates Phase 8 scheduler wiring, and would still inherit the same connection-acquisition pool_timeout from the main engine). Use the simpler psycopg-native pattern: open a transaction with `with engine.begin() as conn:`, run `conn.execute(text("SET LOCAL statement_timeout = '2000ms'"))` FIRST (the `LOCAL` keyword scopes the setting to the current transaction only, so the rest of the application is unaffected), then `conn.execute(text("SELECT 1"))`. The literal strings `SET LOCAL statement_timeout` and `2000ms` MUST appear in the handler source so a future audit / grep can confirm the timeout is honored. Return `{"status": "ok"}` on success. On any exception, log an `app.healthz_failed` structured-log event with the exception class name (NOT the full traceback to avoid leaking SQL parameter values) and return `JSONResponse({"status":"error"}, status_code=503)`. The connection-pool acquisition still uses the main engine's `pool_timeout=5` (CONTEXT D-10 / PITFALL SH-2); the 2s ceiling applies once the connection is in hand and the SELECT is running, which is the slow-path that CONTEXT D-08 actually cares about (a hanging DB query).

       - `@app.get("/")` — synchronous handler. Renders `pages/index.html` via `request.app.state.templates.TemplateResponse(request=request, name="pages/index.html", context={})`. CONTEXT D-12 — this is the placeholder home page.

    6. **Module bottom:** `app = create_app()` so `entrypoint.sh`'s `uvicorn app.main:app` finds the ASGI callable.

    Also author `tests/test_healthz.py` with a single test:
      - Use FastAPI's `TestClient` (which wraps httpx) imported from `fastapi.testclient`.
      - Construct the client inside the test (so the lifespan startup runs — `TestClient` supports lifespan by default in modern FastAPI).
      - SKIP the test if the DB is unreachable (catch `OperationalError` during `TestClient(app)` construction — same pattern as `tests/test_migrations.py`).
      - `response = client.get("/healthz")`; assert `response.status_code == 200`; assert `response.json() == {"status": "ok"}`.
      - When DB is unreachable, `pytest.skip(...)`.

    Imports at top of `tests/test_healthz.py`: `from __future__ import annotations`, `import pytest`, `from fastapi.testclient import TestClient`.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'.'); import pathlib; p = pathlib.Path('app/static/css'); p.mkdir(parents=True, exist_ok=True); (p / 'tailwind.deadbeef.css').write_text('/* stub for import test */'); from app.main import app; routes = {r.path for r in app.routes if hasattr(r,'path')}; assert '/healthz' in routes, f'/healthz missing: {routes}'; assert '/' in routes, f'/ missing: {routes}'; assert '/static' in {getattr(r,'path',None) for r in app.routes}, f'static mount missing'; tpl_globals = app.state.templates.env.globals; assert 'tailwind_css_path' in tpl_globals; assert '/static/css/tailwind.deadbeef.css' == tpl_globals['tailwind_css_path']; assert app.state.templates.env.autoescape is True or callable(app.state.templates.env.autoescape), 'autoescape must be on'; print('app/main.py routes + jinja globals OK')"</automated>
  </verify>
  <done>
    `app/main.py` exists; `from app.main import app` succeeds when a stub `tailwind.<hash>.css` is present in `app/static/css/`. The app exposes exactly two HTTP routes (`/healthz`, `/`) plus the `/static` mount. Lifespan startup calls `configure_logging(...)` and verifies DB reachability via `SELECT 1`. The `/healthz` handler source contains the literal strings `SET LOCAL statement_timeout` AND `2000ms` (verifiable via `grep -F 'SET LOCAL statement_timeout' app/main.py` and `grep -F '2000ms' app/main.py` — both must return ≥1 match). Jinja2 autoescape is ON and `tailwind_css_path` is in `templates.env.globals`. `tests/test_healthz.py` exists and skips when DB is unreachable. No `os.environ` references anywhere in this file (`pytest tests/test_no_direct_env.py` still green).
  </done>
</task>

<task type="auto">
  <name>Task 3: Dockerfile (multi-stage) + entrypoint.sh</name>
  <files>
    Dockerfile,
    entrypoint.sh
  </files>
  <read_first>
    .planning/phases/00-foundation/00-RESEARCH.md §Dockerfile Strategy (the verbatim multi-stage Dockerfile — read top to bottom),
    .planning/phases/00-foundation/00-RESEARCH.md §Open Questions #5 (keep curl in runtime; do NOT purge after PGDG install),
    .planning/phases/00-foundation/00-RESEARCH.md §Open Questions #1 (TAILWIND_VERSION ARG — recommended pin v4.3.0),
    .planning/phases/00-foundation/00-RESEARCH.md §docker-compose.yml Shape — §entrypoint.sh (the verbatim entrypoint script),
    .planning/phases/00-foundation/00-CONTEXT.md §Decisions (D-04 multi-stage, D-05 non-root UID 1000, D-06 postgresql-client-16 from PGDG, D-09 informs port; the EXPOSE 8000 inside the container is unaffected by the host-side bind),
    .planning/phases/00-foundation/00-CONTEXT.md §Specifics (single-worker rule location #1 — entrypoint.sh comment block),
    .planning/research/PITFALLS.md SH-5 (postgresql-client-16 from PGDG) and SH-6 (proxy-headers + trust list),
    .planning/REQUIREMENTS.md FOUND-04, FOUND-07, FOUND-08, FOUND-12 verbatim
  </read_first>
  <action>
    Author `Dockerfile` at the repo root per RESEARCH §Dockerfile Strategy verbatim, with these planner decisions:

    - **`# syntax=docker/dockerfile:1.7`** at the top.

    - **Stage 1 (`FROM debian:bookworm-slim AS tailwind-builder`):**
      - `ARG TAILWIND_VERSION=v4.3.0` (RESEARCH §Open Questions #1; planner picks this stable pin)
      - `ARG TARGETARCH` (Docker buildx provides this).
      - Install `ca-certificates` + `curl` only.
      - Download the Tailwind binary from `https://github.com/tailwindlabs/tailwindcss/releases/download/${TAILWIND_VERSION}/tailwindcss-linux-x64` (or `-linux-arm64` when `TARGETARCH=arm64`) into `/usr/local/bin/tailwindcss` and `chmod +x`.
      - `WORKDIR /build`
      - `COPY tailwind.config.js ./`
      - `COPY app/static/css/tailwind.src.css ./app/static/css/tailwind.src.css`
      - `COPY app/templates ./app/templates` (Tailwind needs templates for content-based tree-shaking).
      - Compute SHA-8 of the source: `HASH=$(sha256sum app/static/css/tailwind.src.css | cut -c1-8)` and emit `app/static/css/tailwind.${HASH}.css` via `tailwindcss -i app/static/css/tailwind.src.css -o app/static/css/tailwind.${HASH}.css --minify`.

    - **Stage 2 (`FROM python:3.12-slim AS runtime`):**
      - `ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1 PIP_DISABLE_PIP_VERSION_CHECK=1 PIP_NO_CACHE_DIR=1`
      - Install `postgresql-client-16` from the PGDG APT repo (PITFALL SH-5, RESEARCH §Dockerfile Strategy). Verbatim from RESEARCH:
        - `apt-get update`
        - install `ca-certificates curl gnupg lsb-release` first.
        - `curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc | gpg --dearmor -o /etc/apt/keyrings/postgresql.gpg`
        - `echo "deb [signed-by=/etc/apt/keyrings/postgresql.gpg] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list`
        - `apt-get update`
        - `apt-get install -y --no-install-recommends postgresql-client-16`
        - **Do NOT purge `curl`** (RESEARCH Open Question #5 — curl stays for HEALTHCHECK and ad-hoc debugging). Purge `gnupg lsb-release` only.
        - `rm -rf /var/lib/apt/lists/*`
      - `RUN useradd -u 1000 -m -s /bin/bash app` (CONTEXT D-05).
      - `WORKDIR /app`
      - `COPY requirements.txt ./`
      - `RUN pip install -r requirements.txt` (cached layer separate from code copy).
      - `COPY --chown=app:app . .` (copies the whole repo; `.dockerignore` from Plan 01 trims it).
      - `COPY --from=tailwind-builder --chown=app:app /build/app/static/css/tailwind.*.css ./app/static/css/`
      - `RUN chmod +x entrypoint.sh`
      - `USER app`
      - `EXPOSE 8000`
      - `HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=3 CMD curl -fsS http://127.0.0.1:8000/healthz || exit 1` (CONTEXT D-08; RESEARCH §Dockerfile Strategy).
      - `ENTRYPOINT ["./entrypoint.sh"]`

    Author `entrypoint.sh` at the repo root per RESEARCH §docker-compose.yml Shape §entrypoint.sh verbatim. Required content:
      - Shebang `#!/usr/bin/env bash`.
      - A multi-line comment block at the top stating the single-worker rule (LOCATION #1 of THREE per CONTEXT <specifics>). The comment block MUST include the literal strings `single worker`, `--workers 1`, and reference the other two locations (README.md and `app/services/scheduler.py`). The exact wording from RESEARCH:
        - `# IMPORTANT: This service MUST run with exactly one uvicorn worker. APScheduler`
        - `# (Phase 8) is in-process and module-level AI locks (Phase 7) require single-process.`
        - `# A future `--workers 4` would fire every nightly job 4x and bill 4x the AI cost.`
        - `# This is reinforced in README.md and app/services/scheduler.py.`
      - `set -euo pipefail` (fail loudly on any error).
      - Line: `alembic upgrade head` (runs the migration; depends on compose `service_healthy` from Plan 05).
      - Line: `exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1 --proxy-headers --forwarded-allow-ips "${TRUSTED_PROXY_IPS:-127.0.0.1}"` (FOUND-04, FOUND-08; PITFALL SH-6). The `${TRUSTED_PROXY_IPS:-127.0.0.1}` fallback handles a missing env var gracefully — the var is mandatory in `.env.example` but defensive default in the script.

    The script must be executable; the Dockerfile's `RUN chmod +x entrypoint.sh` handles this for the image, and the file should have a `#!/usr/bin/env bash` shebang so a developer running it directly on a Unix host also works.
  </action>
  <verify>
    <automated>python -c "import pathlib, re; df = pathlib.Path('Dockerfile').read_text(); assert 'FROM debian:bookworm-slim AS tailwind-builder' in df, 'stage 1 missing'; assert 'FROM python:3.12-slim AS runtime' in df, 'stage 2 missing'; assert 'TAILWIND_VERSION=v4' in df, 'tailwind version pin missing'; assert 'postgresql-client-16' in df, 'pg client 16 missing (FOUND-07)'; assert 'apt.postgresql.org/pub/repos/apt' in df, 'PGDG repo missing'; assert 'useradd -u 1000' in df, 'non-root user missing (D-05)'; assert 'USER app' in df, 'USER app missing'; assert 'HEALTHCHECK' in df and '/healthz' in df, 'healthcheck missing'; assert 'COPY --from=tailwind-builder' in df, 'tailwind COPY missing'; ep = pathlib.Path('entrypoint.sh').read_text(); assert 'alembic upgrade head' in ep, 'migration not in entrypoint'; assert '--workers 1' in ep, 'single worker flag missing'; assert '--proxy-headers' in ep, 'proxy-headers flag missing'; assert '--forwarded-allow-ips' in ep, 'forwarded-allow-ips missing'; assert 'TRUSTED_PROXY_IPS' in ep, 'trust list env var not referenced'; assert re.search(r'(single worker|workers 1)', ep, re.IGNORECASE), 'single-worker doc missing'; assert 'README' in ep and 'scheduler.py' in ep, 'three-place rule cross-refs missing'; print('Dockerfile + entrypoint.sh OK')"</automated>
  </verify>
  <done>
    `Dockerfile` exists with both stages; stage 1 downloads Tailwind v4.x and emits a content-hashed CSS file; stage 2 installs `postgresql-client-16` from PGDG, runs as user `app` UID 1000, exposes 8000, and has the curl-based HEALTHCHECK. `entrypoint.sh` runs `alembic upgrade head` then execs uvicorn with `--workers 1 --proxy-headers --forwarded-allow-ips`, and its top-of-file comment block is the single-worker rule LOCATION #1 (cross-referencing README and `app/services/scheduler.py`). Grepping `--workers 1` across `entrypoint.sh` + `app/services/scheduler.py` returns ≥2 hits (the third hit lands in Plan 05's README.md).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NGINX (host) → uvicorn (container) | uvicorn must trust `X-Forwarded-Proto` and `X-Forwarded-For` from the trust list; failing to do so causes `Secure` session cookies (Phase 2) to be silently dropped |
| Docker build context → image | `.dockerignore` (Plan 01) trims `.env` and secrets out of the build context; the COPY step picks up only sanitized files |
| host filesystem → container | The web container runs as non-root UID 1000 (CONTEXT D-05); future bind-mounts (deferred — Phase 0 uses named volumes only) align with UID 1000 |
| stdout/stderr → Docker log driver | Anything `app/main.py` or `entrypoint.sh` emits is captured by Docker; treat as observable to the operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04-01 | Spoofing | Without `--proxy-headers`, uvicorn sees `scheme=http` and Phase 2's `Secure` session cookies are dropped (PITFALL SH-6) | mitigate | `entrypoint.sh` passes `--proxy-headers --forwarded-allow-ips="${TRUSTED_PROXY_IPS:-127.0.0.1}"`. Phase 1 will ship `/debug/proxy` to end-to-end-verify. |
| T-00-04-02 | Tampering | `pg_dump` version mismatch silently truncates backups (PITFALL SH-5) | mitigate | Dockerfile installs `postgresql-client-16` from PGDG, not the slim default (15). Verified by Plan 05's `make smoke` running `docker compose exec coffee-snobbery pg_dump --version`. |
| T-00-04-03 | Elevation of Privilege | Container runs as root → larger blast radius if any RCE found in FastAPI/Jinja later | mitigate | `RUN useradd -u 1000 -m app` + `USER app` before `CMD`. All COPYs use `--chown=app:app`. |
| T-00-04-04 | Information Disclosure | `tailwind_css_path` glob returns a wrong-format filename (e.g. `tailwind.src.css` because the build stage missed) → 404 on the stylesheet, no leak but visible failure | mitigate | `compute_tailwind_css_path()` raises `RuntimeError` listing what it found if zero or multiple hashed files match. Catastrophic and loud, not silent. |
| T-00-04-05 | Information Disclosure | `/healthz` reveals DB version on failure | mitigate | The 503 response body is `{"status":"error"}` only; no exception detail. Structured log entry on the server side has the exception class name only, not the full traceback (per Task 2). Per-request `SET LOCAL statement_timeout = '2000ms'` enforces the CONTEXT D-08 2-second ceiling on the SELECT itself so a hung query cannot extend an attacker's observation window. |
| T-00-04-06 | Denial of Service | `alembic upgrade head` blocks at startup if DB is slow → container fails liveness | accept | Compose `depends_on: condition: service_healthy` (Plan 05) ensures DB is ready before web container's entrypoint runs. `pg_isready` healthcheck (Plan 05) gates the dependency. |
| T-00-04-07 | Tampering | Tailwind binary URL serves a malicious replacement | accept (Phase 12 to harden) | The binary is downloaded over HTTPS from `github.com/tailwindlabs/...` — GitHub's HTTPS + Cloudflare are the trust anchor for v1. Phase 12 hardening can add a sha256 pin in the Dockerfile ARG. |
| T-00-04-08 | Information Disclosure | `app/main.py` `app.startup` log line emits configuration that includes secrets | mitigate | The lifespan startup logger emits a structured `event="app.startup"` with NO config field dumps. Settings values (DATABASE_URL etc.) are NOT logged. |
| T-00-04-09 | Information Disclosure | Jinja2 autoescape accidentally disabled | mitigate | `Jinja2Templates(directory=...)` defaults to `autoescape=True`. Task 2 explicitly does NOT call `templates.env.autoescape = False`. Phase 12 grep test forbids `|safe` in `templates/pages/`. |
</threat_model>

<verification>
1. **Static source checks (no Docker required):**
   - Task 1 verify command exits 0 (Tailwind config + base.html shape).
   - Task 2 verify command exits 0 (app/main.py routes + Jinja globals).
   - Task 3 verify command exits 0 (Dockerfile + entrypoint.sh shape).

2. **FOUND-10 still green:**
   - `pytest tests/test_no_direct_env.py -x` returns exit 0.

3. **Wave-0 tests still green:**
   - `pytest tests/test_logging.py tests/test_env_example.py tests/test_no_direct_env.py -x` returns exit 0.

4. **Image builds (gated behind docker availability — Plan 05 ships the compose smoke):**
   - `docker build -t coffee-snobbery:test .` exits 0.
   - `docker run --rm coffee-snobbery:test pg_dump --version` returns a line containing `16.` (FOUND-07).
   - `docker run --rm --entrypoint sh coffee-snobbery:test -c 'ls app/static/css/'` shows `tailwind.<8 hex>.css` (FOUND-12).
   - `docker run --rm --entrypoint sh coffee-snobbery:test -c 'whoami'` returns `app` (CONTEXT D-05).

5. **Live container smoke (gated behind Plan 05):**
   - `docker compose exec coffee-snobbery ps -ef | grep uvicorn | grep -- '--workers 1'` exits 0 (FOUND-04).
   - `docker compose exec coffee-snobbery ps -ef | grep -- '--proxy-headers'` exits 0 (FOUND-08).
   - `docker compose exec coffee-snobbery ps -ef | grep -- '--forwarded-allow-ips'` exits 0 (FOUND-08).
   - `curl -fsS http://127.0.0.1:8080/healthz` returns `{"status":"ok"}` with HTTP 200.
   - `curl -fsS http://127.0.0.1:8080/static/css/tailwind.*.css | wc -c` returns >5000 (compiled CSS, not the source) — Plan 05 smoke runbook.
</verification>

<success_criteria>
- `Dockerfile` is multi-stage (tailwind-builder + python:3.12-slim runtime); stage 2 installs `postgresql-client-16` from PGDG and runs as user `app` UID 1000.
- `entrypoint.sh` runs `alembic upgrade head` then execs uvicorn with `--workers 1 --proxy-headers --forwarded-allow-ips=$TRUSTED_PROXY_IPS`; the top-of-file comment is single-worker rule location #1 of three.
- `tailwind.config.js` ships the cream + espresso palette + `darkMode: 'media'` + content scan paths including `app/static/js/**/*.js`.
- `app/static/css/tailwind.src.css` carries the `@tailwind` directives + the 16px form-input rule.
- `app/templates/base.html` ships the dual theme-color meta (cream #FAF7F2 light, espresso #1A1110 dark), the viewport-fit=cover meta, the `tailwind_css_path` reference, and no `|safe`.
- `app/templates/pages/index.html` extends base and renders the CONTEXT D-12 placeholder copy.
- `app/main.py` registers exactly two routes (`/healthz`, `/`), mounts `/static`, runs lifespan with `configure_logging` + DB `SELECT 1`, and exposes the Jinja `tailwind_css_path` global.
- `tests/test_healthz.py` passes against a running compose stack and skips cleanly when DB is unreachable.
- `pytest tests/test_no_direct_env.py` still green.
- Image builds and (when run via Plan 05) responds 200 on `/healthz` with `{"status":"ok"}`.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-04-SUMMARY.md` documenting:
- The exact TAILWIND_VERSION pin chosen (v4.3.0 per RESEARCH; planner may bump to a more recent v4.x — record the value).
- The `/healthz` 2-second timeout pattern: per-request `SET LOCAL statement_timeout = '2000ms'` inside `with engine.begin() as conn:`. NO second engine is constructed; main engine's `pool_timeout=5` handles connection acquisition (CONTEXT D-10). Confirm the literal strings are greppable in `app/main.py`.
- The exact uvicorn flags in `entrypoint.sh`.
- Confirmation that the dual theme-color meta tags are in `base.html`.
- Image build size (best-effort — record `docker image ls coffee-snobbery:test` output line).
- Note for Plan 05: image already builds; Plan 05 wires the compose stack, READMEs the deployment runbook, and lands the smoke target. Three-place single-worker rule reaches location #3 (README.md) in Plan 05.
- Note for Phase 1: the structlog `request_id` middleware seat is present (Plan 02); add the middleware in Phase 1's first plan.
- Note for Phase 2: `/setup` route handler attaches to `app` via a new router under `app/routers/setup.py`; Phase 0 leaves `app/routers/__init__.py` empty so the registration is additive.
</output>
