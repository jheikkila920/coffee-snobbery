---
phase: 11-pwa-mobile-polish
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - app/templates/base.html
  - app/templates/pages/login.html
  - app/templates/pages/config_hub.html
  - app/routers/config_hub.py
  - app/static/js/alpine-components/nav-bar.js
  - app/static/js/alpine-components/account-dropdown.js
  - app/static/js/alpine-components/ios-banner.js
  - app/static/css/tailwind.src.css
  - app/main.py
  - tests/test_phase02_smoke.py
  - tests/test_nav.py
autonomous: false
requirements: [MOB-01, MOB-02, MOB-11, UX-01, UX-03, UX-04]
user_setup: []

must_haves:
  truths:
    - "Every authenticated page shows a persistent nav: bottom tab nav (Home/Log/Config/Admin) <768px, top nav >=768px"
    - "Admin tab/link is hidden for non-admin users"
    - "An authenticated user can sign out: desktop via the account dropdown, mobile via the Config hub bottom section — both CSRF-protected POSTs to /logout"
    - "Login page is always dark (espresso-950) with a centered mascot hero and form card below"
    - "Nav and search are hidden on /login and /setup"
    - "The service worker registers via a nonce-tagged script and base.html exposes an empty {% block head_extra %}"
    - "GET /config returns 200 and links the five catalog entities"
  artifacts:
    - path: "app/templates/base.html"
      provides: "Persistent nav frame (top + bottom), logo, absorbed search, SW registration, head_extra block, iOS banner"
      contains: "head_extra"
    - path: "app/routers/config_hub.py"
      provides: "GET /config catalog hub route (require_user)"
      contains: "/config"
    - path: "app/templates/pages/config_hub.html"
      provides: "Catalog hub + mobile account/sign-out surface"
      contains: "/logout"
    - path: "app/static/js/alpine-components/nav-bar.js"
      provides: "navBar Alpine.data active-tab component"
      contains: "Alpine.data('navBar'"
    - path: "app/static/js/alpine-components/account-dropdown.js"
      provides: "accountDropdown Alpine.data component"
      contains: "Alpine.data('accountDropdown'"
    - path: "app/static/js/alpine-components/ios-banner.js"
      provides: "iosBanner Alpine.data component"
      contains: "Alpine.data('iosBanner'"
  key_links:
    - from: "app/templates/base.html"
      to: "/logout"
      via: "account dropdown CSRF POST form"
      pattern: "action=\"/logout\""
    - from: "app/templates/base.html"
      to: "/sw.js"
      via: "navigator.serviceWorker.register"
      pattern: "serviceWorker.register\\('/sw.js'\\)"
    - from: "app/main.py"
      to: "app.routers.config_hub"
      via: "include_router"
      pattern: "include_router\\(config_hub"
    - from: "app/templates/base.html"
      to: "request.state.user.is_admin"
      via: "Admin tab gating"
      pattern: "is_admin"
---

<objective>
Build the persistent navigation frame and brand chrome that the app has owed since Phase 6 retired the placeholder index (memory `phase-11-owes-nav-and-signout`). This plan owns ALL `base.html` edits so later plans never collide on it: it replaces the Phase 10 search-only header with a full nav frame (bottom tab nav Home/Log/Config/Admin <768px, top horizontal nav with logo + absorbed search + account dropdown >=768px), adds the service-worker registration script, adds the empty `{% block head_extra %}` that the GBM plan needs, and adds the iOS install banner. It also redesigns the login page as an always-dark mascot hero (D-13/D-14), creates the `/config` catalog hub (the mobile account + sign-out surface, D-03), and registers the three new Alpine CSP components.

Purpose: Deliver the first sign-out affordance the app has had since Phase 6 (D-05 desktop dropdown / D-03 mobile config-hub), the four-tab nav (MOB-01), Admin-tab hiding for non-admins (MOB-02), the iOS install banner (MOB-11), the wordmark/logo branding (UX-03), and snobbery-tone empty-state copy on the new hub (UX-04). It is the foundation Plan 04 (GBM, needs head_extra + nav-hide) and Plan 05 (polish, offsets sticky actions against the bottom nav) build on, so it runs in Wave 2 after Plan 01 produced the logo-badge.png and SW route.
Output: Rebuilt base.html nav frame, dark login, config hub page+route, three Alpine components, nav CSS tokens, main.py registration, an updated phase-02 smoke assertion, and a nav smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pwa-mobile-polish/11-CONTEXT.md
@.planning/phases/11-pwa-mobile-polish/11-RESEARCH.md
@.planning/phases/11-pwa-mobile-polish/11-PATTERNS.md
@.planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md
@app/templates/base.html
@app/templates/pages/login.html

<interfaces>
<!-- Contracts the executor must replicate. Extracted from codebase. -->

base.html (current):
- `<head>` loads Alpine component scripts (lines 17-28) each with `nonce="{{ csp_nonce(request) }}"`, BEFORE `@alpinejs/csp@3.15.12` core (line 34), then htmx 2.0.10 (line 36), then htmx-listeners.js (line 38), then photo-upload.js (line 40).
- Dual theme-color metas already present (lines 7-8) — DO NOT change.
- `<meta name="csrf-token" ...>` (line 10) — the double-submit token surface.
- The auth-gated search header is `{% if request.state.user %}<header x-data="searchBar" ...>` (lines 46-151). This whole block is replaced by the nav frame; the search input/sheet markup (lines 48-149) is ABSORBED into the new top nav (desktop inline) and remains reachable on mobile.
- `<body class="bg-cream-50 text-espresso-900 dark:bg-espresso-950 dark:text-cream-100">` (line 42) — DO NOT change the body class (login overrides via a wrapping div, not the body).
- `#modal-mount` div (line 159) before `</body>` — keep it.

CSRF form field idiom (login.html line 9, setup.html line 9):
`<input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken', '') }}">` — the CSRFFormFieldShim hoists it into the X-CSRF-Token header.

Auth gate (app/dependencies/auth.py): `Depends(require_user)` returns 401 for anonymous. `request.state.user` is the User row (has `.username`, `.is_admin`). `/logout` is POST-only with CSRF (app/routers/auth.py line 362).

Routes the nav links target (all exist): `/` (home), `/brew` (sessions list), `/config` (NEW, this plan), `/admin` (admin hub), `/coffees` `/equipment` `/recipes` `/roasters` `/flavor-notes` (catalog).

Alpine CSP constraint (docs/decisions/0001 + UI-SPEC Stack constraints): components are `Alpine.data('name', () => ({...}))` factories registered inside `alpine:init`, loaded BEFORE the csp core; x-data is a STRING reference; no eval, no inline `hx-on:`, no inline event handlers other than Alpine `x-on:`/`@` declarative bindings; string comparisons like `window.location.pathname.startsWith(...)` are allowed.

SW registration (RESEARCH Code Examples + Pitfall 5): the registration call must live in a nonce-tagged inline `<script>` in base.html — `navigator.serviceWorker.register('/sw.js')` — because StaticFiles responses bypass the nonce.

Logo badge asset: `/static/img/logo-badge.png` (produced by Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three Alpine CSP components — navBar, accountDropdown, iosBanner</name>
  <read_first>
    - app/static/js/alpine-components/search-bar.js (open/close + ESC + structure analog for navBar/accountDropdown — PATTERNS §nav-bar.js and §account-dropdown.js)
    - app/static/js/alpine-components/brew-draft.js (localStorage read/write in init() with try/catch — analog for iosBanner — PATTERNS §ios-banner.js)
    - app/static/js/alpine-components/mini-modal.js (click-outside/backdrop pattern for accountDropdown close)
    - docs/decisions/0001 (the Alpine CSP-build / eval-free contract; or the relevant ADR file under docs/decisions/)
    - .planning/phases/11-pwa-mobile-polish/11-PATTERNS.md (§nav-bar.js, §account-dropdown.js, §ios-banner.js — exact component bodies)
  </read_first>
  <action>
    Create three files under app/static/js/alpine-components/. nav-bar.js: register `Alpine.data('navBar', () => ({ get activeTab() {...} }))` inside an `alpine:init` listener; activeTab derives from window.location.pathname via startsWith comparisons — '/' or '/home' → 'home'; '/brew' → 'brew'; '/config'|'/coffees'|'/equipment'|'/recipes'|'/roasters'|'/flavor-notes' → 'config'; '/admin' → 'admin'; else ''. account-dropdown.js: `Alpine.data('accountDropdown', () => ({ open: false, init(){...ESC keydown listener...}, destroy(){...remove listener...}, toggle(){this.open=!this.open}, close(){this.open=false}, onBackdropClick(e){if(e.target===e.currentTarget)this.close()} }))`. ios-banner.js: `Alpine.data('iosBanner', () => ({ show:false, init(){ if(localStorage.getItem('snobbery:ios-banner-dismissed')) return; const isIOS=/iPhone|iPad|iPod/.test(navigator.userAgent); const isStandalone = window.navigator.standalone === true || window.matchMedia('(display-mode: standalone)').matches; if(isIOS && !isStandalone) this.show=true; }, dismiss(){ this.show=false; try{ localStorage.setItem('snobbery:ios-banner-dismissed','1'); }catch(_e){} } }))`. All three use NO eval, NO new Function — mirror search-bar.js exactly. Match the file-header comment style of search-bar.js.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery sh -c "grep -q \"Alpine.data('navBar'\" app/static/js/alpine-components/nav-bar.js && grep -q \"Alpine.data('accountDropdown'\" app/static/js/alpine-components/account-dropdown.js && grep -q \"Alpine.data('iosBanner'\" app/static/js/alpine-components/ios-banner.js && ! grep -RnE 'eval\\(|new Function' app/static/js/alpine-components/nav-bar.js app/static/js/alpine-components/account-dropdown.js app/static/js/alpine-components/ios-banner.js && echo COMPONENTS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - nav-bar.js registers Alpine.data('navBar') with an activeTab getter using pathname.startsWith comparisons
    - account-dropdown.js registers Alpine.data('accountDropdown') with open state, ESC-close, click-outside close
    - ios-banner.js registers Alpine.data('iosBanner') reading/writing localStorage key 'snobbery:ios-banner-dismissed', UA + standalone detection
    - None of the three files contain `eval(` or `new Function`
  </acceptance_criteria>
  <done>Three CSP-safe Alpine components exist following the search-bar.js / brew-draft.js idioms.</done>
</task>

<task type="auto">
  <name>Task 2: base.html nav frame + head_extra + SW registration + iOS banner</name>
  <read_first>
    - app/templates/base.html (full file — the search header to absorb/replace, the script-load order, the body class, #modal-mount)
    - app/static/css/tailwind.src.css (the .htmx-indicator + @layer base patterns; add nav-safe-area helper here if needed — PATTERNS §tailwind.src.css)
    - .planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md (§1 Persistent Navigation Frame — bottom nav 1a, top nav 1b, logo 1c; §3c iOS banner copy + visual; §Color accent reserved list)
    - .planning/phases/11-pwa-mobile-polish/11-PATTERNS.md (§base.html — exact insertion points for head_extra, component scripts, SW registration, nav frame, iOS banner)
  </read_first>
  <action>
    Edit app/templates/base.html. (a) In `<head>`, after search-bar.js (line 28) and BEFORE the @alpinejs/csp core (line 34), add three nonce-tagged `<script defer src="/static/js/alpine-components/{nav-bar,account-dropdown,ios-banner}.js" nonce="{{ csp_nonce(request) }}">` tags. (b) Add an empty `{% block head_extra %}{% endblock %}` in `<head>` between the new component scripts and the @alpinejs/csp core (the GBM plan populates it). (c) After htmx-listeners.js (line 38) add a nonce-tagged inline `<script nonce="{{ csp_nonce(request) }}">if ('serviceWorker' in navigator) { navigator.serviceWorker.register('/sw.js').catch(function(){}); }</script>`. (d) Replace the entire `{% if request.state.user %}<header x-data="searchBar" ...>...</header>{% endif %}` block (lines 46-151) with the new nav frame, KEEPING the `{% if request.state.user %}` auth gate so nav+search stay hidden on /login and /setup. Top nav (`hidden md:flex h-14 px-6 items-center justify-between bg-cream-100 dark:bg-espresso-900 border-b ...`): left = logo badge `<img src="/static/img/logo-badge.png" class="h-8 w-8 rounded-full">` + wordmark "Snobbery" linking to `/`; left-of-center = nav links Home/Log/Config/Admin (Admin wrapped in `{% if request.state.user.is_admin %}`), `text-sm font-semibold`, active link espresso-700; center = the ABSORBED desktop search input markup from the old header (the input + #search-results dropdown + #search-spinner, wrapped in `x-data="searchBar"`); right = accountDropdown trigger (username + chevron, `min-h-[44px]`) opening a panel with a username display and a CSRF POST `<form method="post" action="/logout">` containing the hidden X-CSRF-Token field and a `<button type="submit">Sign out</button>`. Bottom tab nav (`fixed bottom-0 left-0 right-0 z-40 h-16 pb-[env(safe-area-inset-bottom)] bg-cream-100 dark:bg-espresso-900 border-t ... md:hidden`, `x-data="navBar"`): four tab links Home(`/`)/Log(`/brew`)/Config(`/config`)/Admin(`/admin`), each `flex flex-col items-center justify-center gap-1 min-h-[44px] min-w-[44px]`, inline Heroicon SVG `h-6 w-6` + `text-xs` label, Admin tab wrapped in `{% if request.state.user.is_admin %}`, active tab espresso-700 via `:class` bound to `activeTab === 'home'` etc. Keep the mobile search icon→full-screen-sheet markup from the old header (absorb it into a small mobile top strip OR a search button in the chrome — preserve the searchBar component and its sheet). Add `pb-16` (or safe-area-aware bottom padding) to the page content wrapper so content scrolls clear of the fixed bottom nav (Pitfall 7). (e) Before `</body>`, after `#modal-mount`, add the iOS banner: `<div x-data="iosBanner" x-show="show" class="fixed bottom-16 left-0 right-0 z-30 bg-espresso-900 text-cream-100 px-4 py-3 text-sm flex items-center justify-between md:hidden">` with the locked copy "Add to Home Screen: tap [share icon] then 'Add to Home Screen' for the best experience." and a dismiss button `@click="dismiss()"` `aria-label="Dismiss"` `min-h-[44px] min-w-[44px]`. Every new `<script>` carries the nonce; no inline `hx-on:`; no `|safe`. If a `nav-safe-area` helper is needed, add it OUTSIDE `@layer` in tailwind.src.css per the .htmx-indicator pattern.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery sh -c "grep -q 'head_extra' app/templates/base.html && grep -q \"serviceWorker.register('/sw.js')\" app/templates/base.html && grep -q 'x-data=\"navBar\"' app/templates/base.html && grep -q 'x-data=\"iosBanner\"' app/templates/base.html && grep -q 'action=\"/logout\"' app/templates/base.html && ! grep -n '|safe' app/templates/base.html && echo BASE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - base.html contains `{% block head_extra %}{% endblock %}` in head, between the component scripts and the @alpinejs/csp core
    - Three new nonce-tagged component script tags (nav-bar, account-dropdown, ios-banner) load before the csp core
    - A nonce-tagged inline script calls `navigator.serviceWorker.register('/sw.js')`
    - The nav frame is inside `{% if request.state.user %}` (hidden on /login, /setup); bottom nav `md:hidden` + `fixed bottom-0` + `pb-[env(safe-area-inset-bottom)]`; top nav `hidden md:flex`
    - Admin tab and Admin nav link are each wrapped in `{% if request.state.user.is_admin %}`
    - Account dropdown sign-out is a `<form method="post" action="/logout">` with the hidden X-CSRF-Token field (not a plain link)
    - iOS banner div with x-data="iosBanner" + the locked copy + dismiss button present before </body>
    - No `|safe` and no inline `hx-on:` in base.html
  </acceptance_criteria>
  <done>base.html serves a persistent auth-gated nav (top+bottom), absorbed search, logo, SW registration, head_extra block, and iOS banner — all CSP-strict.</done>
</task>

<task type="auto">
  <name>Task 3: Config hub route + page + dark login redesign + main.py wiring + test fixes</name>
  <read_first>
    - app/routers/home.py (require_user GET-page render analog — PATTERNS §config_hub.py)
    - app/templates/pages/home.html (extends base.html, page_title block, is_admin gating idiom — PATTERNS §config_hub.html)
    - app/templates/pages/login.html (current 21-line file to redesign — PATTERNS §login.html: always-dark wrapping div, CSRF field, do NOT change base body class)
    - app/main.py (router-include block lines 228-243)
    - tests/test_phase02_smoke.py (test_cold_container_through_login + the GET / after logout assertion — update if anonymous-redirect behavior is touched; here it is NOT, but verify the assertion still matches)
    - .planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md (§1d Config Hub, §2 Login Page Redesign with exact classes, §Copywriting Contract empty states + page titles)
  </read_first>
  <action>
    Create app/routers/config_hub.py: `router = APIRouter()`, `GET /config` gated by `Depends(require_user)` rendering `pages/config_hub.html` with the user in context. Register it in app/main.py (`from app.routers import config_hub as config_hub_router`; `app.include_router(config_hub_router.router)`). Create app/templates/pages/config_hub.html extending base.html with `{% block page_title %}Catalog{% endblock %}` and a hub linking Coffees(/coffees), Equipment(/equipment), Recipes(/recipes), Roasters(/roasters), Flavor Notes(/flavor-notes) as `min-h-[44px]` tap-target cards/links; at the bottom add a `md:hidden` section with "Signed in as {{ user.username }}" and a CSRF POST `<form method="post" action="/logout">` (hidden X-CSRF-Token field + Sign out button) — this is the ONLY mobile sign-out affordance (D-03). Use the snobbery-tone empty-state copy from UI-SPEC if the catalog hub needs an empty message ("The catalog is empty." / "Add a coffee, roaster, or recipe to get started.") — UX-04. Redesign app/templates/pages/login.html per UI-SPEC §2: wrap content in `<div class="min-h-screen bg-espresso-950 flex flex-col items-center justify-center px-6 py-12">` (always dark, D-14 — do NOT modify the base.html body class), centered mascot hero `<img src="/static/img/snobbery-login-hero.jpg" alt="Snobbery" class="max-w-[280px] md:max-w-[360px] mx-auto rounded-xl mb-6">`, login card `bg-espresso-900 rounded-xl p-6 shadow-xl max-w-sm w-full` with text-cream-100, inputs overridden to `bg-espresso-800 text-cream-100 border-espresso-700`, CTA `bg-cream-50 text-espresso-900 font-semibold` (inverted), keep the existing CSRF hidden field and the `{% if error %}` block. Keep `{% block page_title %}Sign in{% endblock %}`. Confirm tests/test_phase02_smoke.py still passes (the GET / after-logout 401 assertion is unaffected — this plan does not add an anonymous-friendly /login redirect; if it did, the CONTEXT note says update that assertion). Add tests/test_nav.py asserting: GET /config returns 200 for an authenticated user; an authenticated NON-admin's home/config response HTML does not contain an /admin nav link while an admin's does; GET / for an authenticated user contains the bottom-nav marker (x-data="navBar").
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery python -m pytest tests/test_nav.py tests/test_phase02_smoke.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - GET /config returns 200 for an authenticated user and 401 for anonymous (require_user)
    - config_hub.html links all five catalog entities and has a md:hidden sign-out CSRF POST form to /logout
    - login.html wraps content in an always-dark espresso-950 container with the mascot hero + form card; CSRF field retained; base body class unchanged
    - app/main.py registers config_hub_router
    - tests/test_nav.py asserts: /config 200 authenticated; admin link present for admin / absent for non-admin; navBar present on authenticated home
    - tests/test_phase02_smoke.py still passes (no skips)
  </acceptance_criteria>
  <done>/config hub renders with mobile sign-out, login is always-dark with the mascot hero, routes registered, nav + smoke tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Persistent nav frame (bottom tabs <768px / top nav >=768px), account dropdown + mobile config-hub sign-out, dark login hero, iOS install banner, and the absorbed search — all CSP-strict.</what-built>
  <how-to-verify>
    1. Rebuild + restart: `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`.
    2. In Chrome DevTools responsive mode at 375x667, log in. Confirm: login page is dark with the centered mascot hero and a readable form card (no horizontal scroll, hero not stretched). After login, a bottom tab nav shows Home/Log/Config (and Admin only if you are an admin), each tab tappable and >=44px, content scrolls clear of the nav. The iOS install banner appears only when simulating iOS UA + non-standalone (it may not show in desktop Chrome — that is expected; verify it does NOT show once dismissed).
    3. Tap Config → the hub lists the five catalog entities; scroll to the bottom and confirm "Signed in as <you>" + a Sign out button; tap Sign out and confirm you land back on the dark login page.
    4. Switch to >=768px width: confirm a top nav with logo + "Snobbery" wordmark, inline search, nav links, and a top-right account dropdown whose Sign out works (CSRF POST).
    5. Open DevTools Console: confirm NO CSP violation errors (no "Refused to execute inline script") and that `/sw.js` registered (Application → Service Workers shows scope "/").
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/CSP/sign-out issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /logout | State-changing POST must be CSRF-protected |
| client JS → CSP | Every new script must be nonce-tagged; no inline handlers |
| non-admin user → admin nav targets | UI must not advertise admin routes to non-admins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Spoofing/CSRF | Sign-out forms (base.html dropdown + config_hub mobile) | mitigate | Both are `<form method="post" action="/logout">` with the hidden X-CSRF-Token field hoisted by CSRFFormFieldShim; /logout is POST-only with CSRF (no GET drive-by). Verified by grep `action="/logout"` and the existing /logout CSRF enforcement. |
| T-11-09 | Information disclosure | Admin nav tab/link visible to non-admins | mitigate | Admin tab + Admin nav link wrapped in `{% if request.state.user.is_admin %}`; /admin itself is independently 403-gated by require_admin. tests/test_nav.py asserts the link is absent for non-admins (MOB-02). |
| T-11-10 | Tampering (XSS via CSP bypass) | New nav/SW/banner scripts | mitigate | All scripts nonce-tagged (`nonce="{{ csp_nonce(request) }}"`); Alpine CSP build (no eval); no inline `hx-on:`; no `|safe`. Verified by grep gate in Task 2. |
| T-11-11 | Information disclosure | iOS banner localStorage | accept | Stores only the boolean dismiss flag `snobbery:ios-banner-dismissed`; no user-identifying data (RESEARCH Security Domain). |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery python -m pytest tests/test_nav.py tests/test_phase02_smoke.py -q -rs` → green, no skips.
- Human-verify checkpoint passed (375px + desktop nav, sign-out, dark login, no CSP errors, SW scope "/").
</verification>

<success_criteria>
- Persistent auth-gated nav on every authenticated page; Admin hidden for non-admins.
- Sign-out works on both mobile (config hub) and desktop (dropdown), CSRF-protected.
- Login is always-dark with the mascot hero; head_extra + SW registration + iOS banner in place.
- /config renders; nav + phase-02 tests green; no CSP console violations.
</success_criteria>

<output>
After completion, create `.planning/phases/11-pwa-mobile-polish/11-03-SUMMARY.md`
</output>
