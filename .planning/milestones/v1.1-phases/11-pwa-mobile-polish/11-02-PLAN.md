---
phase: 11-pwa-mobile-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/brew_session.py
  - app/migrations/versions/p11_brew_time_seconds.py
  - app/schemas/brew_session.py
  - tests/test_migrations.py
autonomous: true
requirements: [BREW-12]
user_setup: []

must_haves:
  truths:
    - "brew_sessions has a nullable brew_time_seconds integer column after migration"
    - "BrewSessionCreate accepts brew_time_seconds in range 0..86400 and rejects out-of-range/negative values"
    - "alembic upgrade head applies cleanly and downgrade drops the column"
  artifacts:
    - path: "app/migrations/versions/p11_brew_time_seconds.py"
      provides: "Additive nullable brew_time_seconds column migration"
      contains: "brew_time_seconds"
    - path: "app/models/brew_session.py"
      provides: "Mapped[int | None] brew_time_seconds column"
      contains: "brew_time_seconds"
    - path: "app/schemas/brew_session.py"
      provides: "brew_time_seconds field with ge=0, le=86400"
      contains: "brew_time_seconds"
  key_links:
    - from: "app/migrations/versions/p11_brew_time_seconds.py"
      to: "p10_search_indexes"
      via: "down_revision chain"
      pattern: "down_revision.*p10_search_indexes"
    - from: "app/models/brew_session.py"
      to: "brew_sessions table"
      via: "Mapped column declaration"
      pattern: "brew_time_seconds.*Mapped\\[int"
---

<objective>
Land the additive, nullable `brew_time_seconds` integer column on `brew_sessions` (D-10): the SQLAlchemy model column, the Alembic migration chained off `p10_search_indexes`, and the Pydantic field on `BrewSessionCreate` (range 0..86400, max 24h) so the brew form and Guided Brew Mode completion path can persist and validate elapsed brew time. This is a deliberately small, self-contained schema plan that unblocks the GBM plan (Plan 04) without touching any template or router.

Purpose: GBM populates total elapsed brew time on "Done brewing", and the field is also user-editable on the session form (manual entry). The column must exist and validate before GBM consumes it. Per CLAUDE.md "ask first on schema", John signed off on this additive Phase-5-table deviation (D-10) — it is additive + nullable, so it is low-risk and requires no data migration. Phase 6 analytics is intentionally NOT touched (analytics use of brew_time_seconds is deferred per D-10 / Deferred Ideas).
Output: Migration `p11_brew_time_seconds.py`, model column, schema field, and a migration smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pwa-mobile-polish/11-RESEARCH.md
@.planning/phases/11-pwa-mobile-polish/11-PATTERNS.md

<interfaces>
<!-- Migration chain + schema contracts. Extracted from codebase. -->

Migration head (verified): the latest revision is `p10_search_indexes` (down_revision chain: 0001_initial → p1_sessions → p3_api_credentials → p4_shared_catalog → p5_brew_sessions → p10_search_indexes). The new migration's `down_revision` MUST be "p10_search_indexes".

From app/schemas/brew_session.py:
- `class BrewSessionCreate(BaseModel)` (line 74) holds the form fields with explicit Field ranges, e.g. `rating: Decimal | None = Field(None, ge=0, le=5, multiple_of=Decimal("0.25"))` (line 96), `notes: str = Field("", max_length=5000)` (line 98).
- `class BrewSessionUpdate(BrewSessionCreate)` (line 127) inherits the shape — the new field added to Create automatically applies to Update.

From app/migrations/versions/p5_brew_sessions.py (the analog migration):
- Alembic-safe convention: migration bodies do NOT import from app.models.
- Header declares revision / down_revision / branch_labels / depends_on module-level strings.

Security (RESEARCH Security Domain V5): `brew_time_seconds: int | None = Field(None, ge=0, le=86400)` — ge=0 rejects negatives, le=86400 caps at 24h (Tampering mitigation T-11-06).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model column + Alembic migration</name>
  <read_first>
    - app/models/brew_session.py (the existing Mapped[...] columns; place the new one after `notes`, before the timestamps block — PATTERNS §app/models/brew_session.py)
    - app/migrations/versions/p5_brew_sessions.py (migration header + body conventions; the no-app.models-import rule)
    - app/migrations/versions/p10_search_indexes.py (confirm it is the current head / down_revision target)
    - .planning/phases/11-pwa-mobile-polish/11-PATTERNS.md (§app/migrations/versions/p11_brew_time_seconds.py — exact header + upgrade/downgrade)
  </read_first>
  <behavior>
    - After `alembic upgrade head`, `brew_sessions` has a nullable integer column `brew_time_seconds`
    - `alembic downgrade -1` drops the column
    - The SQLAlchemy model exposes `brew_time_seconds: Mapped[int | None]`
  </behavior>
  <action>
    In app/models/brew_session.py add `brew_time_seconds: Mapped[int | None] = mapped_column(sa.Integer, nullable=True)` after the `notes` column and before the timestamps comment block (use the project's existing import alias for sqlalchemy / mapped_column as used elsewhere in the file). Create app/migrations/versions/p11_brew_time_seconds.py with revision "p11_brew_time_seconds", down_revision "p10_search_indexes", branch_labels None, depends_on None. upgrade() calls `op.add_column("brew_sessions", sa.Column("brew_time_seconds", sa.Integer(), nullable=True))`; downgrade() calls `op.drop_column("brew_sessions", "brew_time_seconds")`. Do NOT import from app.models in the migration body. Run the migration (`docker compose exec coffee-snobbery alembic upgrade head`).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery sh -c "alembic upgrade head && python -c \"import sqlalchemy as sa; from app.db import engine; insp=sa.inspect(engine); cols={c['name']: c for c in insp.get_columns('brew_sessions')}; assert 'brew_time_seconds' in cols, 'column missing'; assert cols['brew_time_seconds']['nullable'] is True, 'must be nullable'; print('COLUMN_OK')\""</automated>
  </verify>
  <acceptance_criteria>
    - app/models/brew_session.py declares `brew_time_seconds: Mapped[int | None]`
    - p11_brew_time_seconds.py has down_revision == "p10_search_indexes" and adds a nullable integer column
    - `alembic upgrade head` succeeds; brew_sessions.brew_time_seconds exists and is nullable (verified via inspector)
    - The migration body does not import from app.models
  </acceptance_criteria>
  <done>Nullable brew_time_seconds column exists on brew_sessions via a clean migration; model reflects it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic field + migration smoke test</name>
  <read_first>
    - app/schemas/brew_session.py (BrewSessionCreate field block lines 74-98; the Field(..., ge=, le=) range idiom; BrewSessionUpdate inherits)
    - tests/test_migrations.py (the existing migration-smoke test to extend with a brew_time_seconds column assertion)
    - .planning/phases/11-pwa-mobile-polish/11-RESEARCH.md (Security Domain V5: ge=0, le=86400)
  </read_first>
  <behavior>
    - BrewSessionCreate(...valid..., brew_time_seconds=300) validates
    - brew_time_seconds=-1 raises ValidationError
    - brew_time_seconds=86401 raises ValidationError
    - brew_time_seconds omitted defaults to None
  </behavior>
  <action>
    Add `brew_time_seconds: int | None = Field(None, ge=0, le=86400)` to BrewSessionCreate in app/schemas/brew_session.py (placed near `notes`). It inherits into BrewSessionUpdate automatically. Extend tests/test_migrations.py to assert the brew_sessions table has a brew_time_seconds column after `upgrade head` (inspector-based, mirroring the existing migration assertions). Add a small schema validation test (in tests/test_migrations.py or a brew schema test file if one exists) covering: a valid value passes, -1 and 86401 raise ValidationError, and the default is None. Use a minimal valid BrewSessionCreate payload (coffee_id, dose_grams_actual, water_grams_actual at minimum per the existing required fields).
  </action>
  <verify>
    <automated>docker compose cp app/schemas/brew_session.py coffee-snobbery:/app/app/schemas/brew_session.py && docker compose cp tests/test_migrations.py coffee-snobbery:/app/tests/test_migrations.py && docker compose exec coffee-snobbery python -m pytest tests/test_migrations.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - app/schemas/brew_session.py BrewSessionCreate has `brew_time_seconds: int | None = Field(None, ge=0, le=86400)`
    - A BrewSessionCreate with brew_time_seconds=300 validates; -1 and 86401 raise ValidationError; omission yields None
    - tests/test_migrations.py asserts the brew_time_seconds column exists and is nullable
    - pytest tests/test_migrations.py exits 0 with no skips
  </acceptance_criteria>
  <done>Schema validates brew_time_seconds within 0..86400; migration + schema tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brew form / GBM client → BrewSessionCreate | User-supplied brew_time_seconds crosses into persistence here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | brew_time_seconds submitted negative or absurdly large | mitigate | Pydantic Field(ge=0, le=86400) on BrewSessionCreate rejects negatives and values over 24h (verified by the schema validation test). |
| T-11-07 | Denial of service | Migration locking brew_sessions during deploy | accept | Additive nullable column = a metadata-only `ADD COLUMN` with no default backfill; Postgres takes a brief ACCESS EXCLUSIVE lock with no table rewrite. Household-scale table; negligible. |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery python -m pytest tests/test_migrations.py -q -rs` → green, no skips.
- Inspector confirms brew_sessions.brew_time_seconds is nullable integer.
</verification>

<success_criteria>
- brew_time_seconds exists as a nullable integer column with a clean up/down migration chained off p10_search_indexes.
- BrewSessionCreate/Update validate brew_time_seconds in 0..86400.
- Migration + schema tests pass with no skips.
</success_criteria>

<output>
After completion, create `.planning/phases/11-pwa-mobile-polish/11-02-SUMMARY.md`
</output>
