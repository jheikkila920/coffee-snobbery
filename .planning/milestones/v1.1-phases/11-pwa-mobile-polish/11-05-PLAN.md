---
phase: 11-pwa-mobile-polish
plan: 05
type: execute
wave: 4
depends_on: [11-03, 11-04]
files_modified:
  - app/templates/fragments/session_list.html
  - app/templates/fragments/session_row.html
  - app/templates/fragments/coffee_list.html
  - app/templates/fragments/coffee_row.html
  - app/templates/fragments/recipe_list.html
  - app/templates/fragments/equipment_list.html
  - app/templates/fragments/equipment_row.html
  - app/templates/fragments/flavor_note_list.html
  - app/templates/fragments/flavor_note_row.html
  - app/templates/fragments/roaster_list.html
  - app/templates/fragments/roaster_row.html
  - app/static/css/tailwind.src.css
  - .planning/phases/11-pwa-mobile-polish/11-POLISH-AUDIT.md
autonomous: false
requirements: [MOB-03, MOB-04, MOB-07, MOB-08, MOB-13]
user_setup: []

must_haves:
  truths:
    - "Every list fragment renders a card layout at <768px and a table at >=768px with no horizontal scroll at 375px"
    - "Every interactive control in card mode (edit/delete/duplicate/brew-again/archive) is at least 44x44px"
    - "The mini-modal renders as a full-screen sheet at <768px and a centered dialog at >=768px"
    - "Short dropdowns use native <select>; the long coffees list uses the searchable HTMX dropdown; no broken control at 375px"
    - "A documented 375px audit records each surface checked and the fixes applied"
  artifacts:
    - path: ".planning/phases/11-pwa-mobile-polish/11-POLISH-AUDIT.md"
      provides: "Per-surface 375px audit log (table-collapse, 44px, sheet, select) with PASS/fix notes"
      contains: "375"
    - path: "app/templates/fragments/session_row.html"
      provides: "Card-mode rating stars + 44px Brew-again tap target"
      contains: "min-h-[44px]"
  key_links:
    - from: "app/templates/fragments/*_list.html"
      to: "responsive card/table split"
      via: "hidden md:block / md:hidden dual layout"
      pattern: "md:hidden"
    - from: "mini-modal full-screen sheet"
      to: "modal-body fragments"
      via: "structural Tailwind classes (fixed inset-0 sheet <768px, centered dialog >=768px)"
      pattern: "inset-0"
---

<objective>
Run the mobile-layout polish audit-and-fix sweep (D-19/D-20/D-21): confirm every list fragment collapses table→card at <768px with no horizontal scroll, sweep all interactive controls to >=44x44px tap targets, convert the mini-modal to a full-screen sheet at <768px (centered dialog at >=768px), audit native-select vs searchable-HTMX-dropdown usage, and produce a per-surface 375px audit log. This is verify-and-fix, NOT redesign — the dual responsive patterns already exist (D-21); the work is closing gaps.

Purpose: Deliver MOB-03 (tables → card lists, no horizontal scroll), MOB-04 (44px tap targets), MOB-07 (native select for short lists, searchable dropdown only for coffees), MOB-08 (modals → full-screen sheets <768px / dialogs >=768px), and the MOB-13 manual responsive verification at 375x667 and 390x844 (the Playwright automation itself is Phase 12 / TEST-06). It runs in Wave 4 after Plan 03 (the nav frame exists, so content-clearance and tap-target sweeps account for it) and after Plan 04 (which owns the recipe_row + brew_form edits — this plan does NOT touch those two files, avoiding collisions, and the auditor verifies them as part of the 375px sweep).
Output: Fixed list fragments, the modal→sheet treatment, any needed tailwind.src.css helper, and 11-POLISH-AUDIT.md documenting each surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pwa-mobile-polish/11-CONTEXT.md
@.planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md
@.planning/phases/11-pwa-mobile-polish/11-PATTERNS.md

<interfaces>
<!-- Contracts the executor must replicate. Extracted from codebase + UI-SPEC. -->

Existing dual responsive pattern (UI-SPEC §5b, PATTERNS §*_list.html): each list fragment has `<div class="hidden md:block"><table>...</table></div>` (desktop) + `<div class="md:hidden space-y-3">...cards...</div>` (mobile). The audit confirms BOTH render and the card mode has no horizontal scroll and 44px controls. Admin pages use no `<table>` — verify their card/grid layouts at 375px but they are not in files_modified (no change expected; record in the audit).

Tap-target standard (UI-SPEC §Spacing, PATTERNS §*_list.html): `min-h-[44px] min-w-[44px]` — already the project pattern (base.html search button). Apply to any card-mode interactive element rendering smaller (edit/delete/duplicate/archive/brew-again links + buttons).

Modal → sheet (UI-SPEC §5a): the mini-modal substrate is `#modal-mount` (base.html) with modal-body fragments swapped in; the miniModal Alpine component already handles ESC + backdrop close. The CHANGE is the structural Tailwind classes on the modal panel: at <768px `fixed inset-0 z-50 flex flex-col` full-height sheet with a header (close X) + sticky actions above the safe-area; at >=768px `fixed inset-0 z-50 flex items-center justify-center` with the panel `rounded-xl w-full max-w-lg mx-4 max-h-[90vh] overflow-y-auto`. The panel classes live in the modal-body fragment template(s) that get swapped into #modal-mount — locate them and apply the responsive sheet/dialog classes. Backdrop `bg-espresso-950/50`.

Native select vs HTMX dropdown (UI-SPEC §5c): short lists (equipment type, roast level, process, water type via datalist) use native `<select>`/`<datalist>` — already implemented; audit only. The long coffees list uses the searchable HTMX dropdown — audit it works at 375px. No redesign unless broken.

Bottom-nav clearance (UI-SPEC §5d, Pitfall 7): normal pages keep content scrolling clear of the fixed bottom nav (Plan 03 added page bottom padding). The search sheet and GBM already cover the nav via `fixed inset-0 z-50`. This plan verifies list pages do not hide their last row/control behind the nav at 375px.

Files NOT in this plan (owned by Plan 04, do NOT edit here): app/templates/fragments/recipe_row.html and app/templates/pages/brew_form.html. The auditor STILL verifies them at 375px and records findings; any required fix to those two files is logged as a follow-up for Plan 04 (or a quick task), not edited here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Table→card + 44px tap-target audit-and-fix across the six list fragments</name>
  <read_first>
    - app/templates/fragments/session_list.html + session_row.html (rating stars in card mode, Brew-again link tap size — UI-SPEC §5b first row)
    - app/templates/fragments/coffee_list.html + coffee_row.html (filter bar collapse, no horizontal scroll)
    - app/templates/fragments/recipe_list.html (step count visible in card mode)
    - app/templates/fragments/equipment_list.html + equipment_row.html (type label visible)
    - app/templates/fragments/flavor_note_list.html + flavor_note_row.html (category badge visible)
    - app/templates/fragments/roaster_list.html + roaster_row.html (location visible)
    - .planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md (§5b audit table — per-fragment focus; §Spacing tap-target rule)
  </read_first>
  <action>
    For each of the six list fragments (and their *_row.html partials where present), open at a 375px mental model and confirm: (a) the desktop `hidden md:block` table and the mobile `md:hidden` card layouts both exist and render the fields called out in UI-SPEC §5b (session: rating stars + Brew-again; coffee: filter bar collapses, no horizontal scroll; recipe: step count; equipment: type label; flavor note: category badge; roaster: location); (b) no element forces horizontal overflow at 375px (no fixed min-widths wider than the viewport, no non-wrapping long strings without truncation); (c) every interactive control in card mode (edit/delete/duplicate/archive/brew-again links + buttons) carries `min-h-[44px] min-w-[44px]` (or equivalent padding reaching 44px). Apply the MINIMUM fixes needed — add `min-h-[44px] min-w-[44px]`, wrap/truncate overflowing text, ensure the card stack uses `space-y-*` not a horizontal scroller. Do NOT redesign layouts that already pass. Do NOT touch recipe_row.html or brew_form.html (Plan 04 owns them) — record their 375px status in the audit (Task 3).
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery sh -c "for f in session_list session_row coffee_list coffee_row recipe_list equipment_list equipment_row flavor_note_list flavor_note_row roaster_list roaster_row; do test -f app/templates/fragments/$f.html || { echo MISSING $f; exit 1; }; done; grep -lq 'md:hidden' app/templates/fragments/session_list.html app/templates/fragments/coffee_list.html app/templates/fragments/recipe_list.html app/templates/fragments/equipment_list.html app/templates/fragments/flavor_note_list.html app/templates/fragments/roaster_list.html && grep -q 'min-h-\\[44px\\]' app/templates/fragments/session_row.html && echo LISTS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - All six *_list.html retain the dual `hidden md:block` table / `md:hidden` card split
    - session_row.html card mode has rating stars and a >=44px Brew-again control (min-h-[44px] present)
    - Each card-mode interactive control across the fragments reaches 44px; no fragment introduces horizontal overflow at 375px (no >viewport min-widths, long strings wrap/truncate)
    - recipe_row.html and brew_form.html are NOT modified by this plan
  </acceptance_criteria>
  <done>The six list fragments collapse cleanly to cards with 44px controls and no horizontal scroll at 375px.</done>
</task>

<task type="auto">
  <name>Task 2: Modal → full-screen sheet (<768px) / dialog (>=768px) + native-select audit</name>
  <read_first>
    - app/templates/base.html (#modal-mount; the search full-screen sheet `fixed inset-0 z-50 flex flex-col` to mirror — UI-SPEC §5a)
    - app/static/js/alpine-components/mini-modal.js (the miniModal ESC/backdrop/dirty-check component — structure unchanged; only classes change)
    - the modal-body fragment template(s) swapped into #modal-mount (find them: grep for hx-target="#modal-mount" and the create-new modal fragments from Phase 4 plan 04-11 / autocomplete create-new)
    - app/static/css/tailwind.src.css (.htmx-indicator + @layer base patterns; add a helper OUTSIDE @layer only if a utility cannot express the sheet)
    - .planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md (§5a modal/sheet table; §5c native-select vs HTMX dropdown)
  </read_first>
  <action>
    Locate the modal-body fragment(s) rendered into #modal-mount (the Phase 4 create-new mini-modal substrate). Apply the responsive sheet/dialog structure from UI-SPEC §5a: outer container `fixed inset-0 z-50 flex flex-col md:flex-row md:items-center md:justify-center` with a `bg-espresso-950/50` backdrop (tap-to-close, already wired by miniModal); panel `bg-cream-50 dark:bg-espresso-950 flex flex-col flex-1 md:flex-none md:rounded-xl md:w-full md:max-w-lg md:mx-4 md:max-h-[90vh] md:overflow-y-auto` so it is a FULL-HEIGHT sheet <768px and a CENTERED dialog >=768px; header row `flex items-center justify-between px-4 pt-4 pb-2` with a close X (`min-h-[44px] min-w-[44px]`); actions `sticky bottom-0 ... px-4 py-3 border-t`. Keep the miniModal component (ESC/backdrop/dirty-check) unchanged — only the Tailwind classes change. Then AUDIT native-select usage (UI-SPEC §5c): confirm equipment-type / roast-level / process / water-type controls are native `<select>`/`<datalist>` (record where) and that the coffees searchable HTMX dropdown renders/works at 375px; fix only if broken. If a sheet behavior needs a non-utility CSS rule, add it OUTSIDE `@layer` in tailwind.src.css per the .htmx-indicator pattern (no app/static/css/custom.css — MX-1 lock).
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery sh -c "grep -RIl 'inset-0' app/templates/fragments/ app/templates/pages/ | head -1 && grep -RIq 'md:max-w-lg' app/templates/ && ! test -f app/static/css/custom.css && echo SHEET_OK"</automated>
  </verify>
  <acceptance_criteria>
    - The mini-modal panel renders full-height (`fixed inset-0` sheet) at <768px and a centered `md:max-w-lg` dialog at >=768px with a `bg-espresso-950/50` backdrop and a >=44px close X
    - The miniModal Alpine component is unchanged (only template classes changed)
    - Native-select audit recorded; coffees searchable dropdown verified at 375px
    - app/static/css/custom.css does NOT exist (MX-1)
  </acceptance_criteria>
  <done>Mini-modal is a full-screen sheet on mobile / dialog on desktop; native-select usage audited.</done>
</task>

<task type="auto">
  <name>Task 3: 375px / 390px audit log — 11-POLISH-AUDIT.md</name>
  <read_first>
    - .planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md (§5b audit-focus table, §5d bottom-nav hide rules — the surfaces to enumerate)
    - app/templates/pages/admin_*.html (admin pages — verify card/grid at 375px, record; no <table> expected)
    - app/templates/pages/brew_form.html + app/templates/fragments/recipe_row.html (Plan-04-owned; record their 375px status as verification, log a follow-up if broken)
    - .planning/phases/11-pwa-mobile-polish/11-RESEARCH.md (Validation Architecture — MOB-03/MOB-13 are manual during implementation; Playwright is Phase 12)
  </read_first>
  <action>
    Create .planning/phases/11-pwa-mobile-polish/11-POLISH-AUDIT.md as a structured per-surface log. For each surface — the six list pages (sessions, coffees, recipes, equipment, flavor notes, roasters), the config hub, the home page, the admin pages, the brew form, the GBM page, the login page — record at 375x667 (and spot-check 390x844): (1) table→card collapse OK? (2) any horizontal scroll? (3) all tap targets >=44px? (4) content clears the bottom nav? (5) modal/sheet correct? Mark each cell PASS or the specific fix applied (with the file + class change). For the two Plan-04-owned files (brew_form.html, recipe_row.html) record PASS or a follow-up note (do not edit them here). End with a summary: total surfaces audited, fixes applied in this plan, and any deferred follow-ups. This log is the manual MOB-13 evidence for the phase gate (the automated Playwright assertion is Phase 12 / TEST-06).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery sh -c "test -f .planning/phases/11-pwa-mobile-polish/11-POLISH-AUDIT.md && grep -q '375' .planning/phases/11-pwa-mobile-polish/11-POLISH-AUDIT.md && echo AUDIT_OK" 2>/dev/null || (test -f .planning/phases/11-pwa-mobile-polish/11-POLISH-AUDIT.md && echo AUDIT_OK)</automated>
  </verify>
  <acceptance_criteria>
    - 11-POLISH-AUDIT.md exists and enumerates each surface with the five audit checks (collapse / no-scroll / 44px / nav-clearance / sheet) marked PASS or fix-applied
    - The two Plan-04-owned files are recorded (PASS or follow-up), not edited by this plan
    - A summary lists fixes applied and any deferred follow-ups
  </acceptance_criteria>
  <done>A complete 375/390px audit log documents the responsive sweep and serves as the manual MOB-13 evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile polish sweep: table→card collapse, 44px tap targets, full-screen modal sheets, native-select audit — across every list and modal surface.</what-built>
  <how-to-verify>
    1. Rebuild + restart: `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`.
    2. In Chrome DevTools responsive mode at 375x667 (then spot-check 390x844), log in and visit each list page: Sessions (/brew), Coffees (/coffees), Recipes (/recipes), Equipment (/equipment), Roasters (/roasters), Flavor Notes (/flavor-notes), plus the Config hub and Home. For each confirm: the layout is CARDS (not a horizontally-scrolling table), there is NO horizontal scroll, every edit/delete/duplicate/brew-again control is comfortably tappable (>=44px), and the last row/control is not hidden behind the bottom nav.
    3. Trigger a create-new mini-modal (e.g. the "+ Create new roaster" affordance inside the coffee form's roaster autocomplete): confirm it opens as a FULL-SCREEN sheet at 375px with a close X and sticky actions; resize to >=768px and confirm it becomes a centered dialog.
    4. Open a native dropdown (equipment type or roast level) and confirm it uses the OS-native picker; open the coffees searchable dropdown and confirm it works at 375px.
    5. Cross-check against 11-POLISH-AUDIT.md — every surface marked PASS should visually pass.
  </how-to-verify>
  <resume-signal>Type "approved" or list the surfaces with horizontal scroll / sub-44px controls / wrong modal mode to fix.</resume-signal>
</task>

</tasks>

<threat_model>
No new threats — UI/CSS-only audit-and-fix sweep on existing templates plus a documentation file. No new routes, inputs, scripts, or data flows are introduced (the mini-modal class change preserves the existing CSRF-protected create-new forms and the eval-free miniModal component).
</threat_model>

<verification>
- The grep gates in Tasks 1-3 pass (dual layouts present, 44px controls, sheet classes, no custom.css, audit file exists).
- Human-verify checkpoint passed: every list is cards-not-scrolling-table at 375px, 44px controls, sheet/dialog modal modes, native pickers.
</verification>

<success_criteria>
- Every list collapses to cards at <768px with no horizontal scroll and 44px controls.
- The mini-modal is a full-screen sheet <768px / dialog >=768px.
- Native-select usage audited; coffees searchable dropdown works at 375px.
- 11-POLISH-AUDIT.md documents the full sweep (manual MOB-13 evidence).
</success_criteria>

<output>
After completion, create `.planning/phases/11-pwa-mobile-polish/11-05-SUMMARY.md`
</output>
