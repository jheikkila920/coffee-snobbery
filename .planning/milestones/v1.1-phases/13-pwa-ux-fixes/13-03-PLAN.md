---
phase: 13-pwa-ux-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/equipment.py
  - app/routers/coffees.py
  - app/templates/fragments/equipment_row.html
  - app/templates/pages/equipment.html
  - app/templates/pages/coffees.html
  - tests/routers/test_equipment_create_fragment.py
autonomous: false
requirements: [C2, C3, D-03, D-04]
must_haves:
  truths:
    - "Creating equipment renders the full, correctly-laid-out list with the new item — no malformed card, no page refresh (C2, D-03)"
    - "Creating a coffee renders the full coffee list with the new item, not a bare <tr> dropped into a non-table div (C2, D-03)"
    - "On successful create, the create form collapses/closes (D-04)"
    - "Equipment cards at 375px group type + usage as flex-wrap pills, like coffee cards, so they are shorter (C3)"
  artifacts:
    - path: "app/routers/equipment.py"
      provides: "create_equipment returns equipment_list.html fragment, not the row variant (C2, D-03)"
      contains: "equipment_list.html"
    - path: "app/routers/coffees.py"
      provides: "create_coffee returns coffee_list.html fragment with full list context (C2, D-03)"
      contains: "coffee_list.html"
    - path: "app/templates/fragments/equipment_row.html"
      provides: "Flex-wrap pill card layout (C3) + OOB form-clear removed (C2)"
      contains: "flex-wrap"
    - path: "tests/routers/test_equipment_create_fragment.py"
      provides: "C2 regression test for both create routes"
      exports: ["test functions"]
  key_links:
    - from: "app/routers/equipment.py create_equipment"
      to: "fragments/equipment_list.html"
      via: "TemplateResponse with grouped list context"
      pattern: "equipment_list\\.html"
    - from: "app/templates/pages/equipment.html create form"
      to: "#equipment-list"
      via: "hx-target=#equipment-list hx-swap=innerHTML"
      pattern: "equipment-list"
    - from: "app/routers/coffees.py create_coffee"
      to: "fragments/coffee_list.html"
      via: "TemplateResponse with coffees + flavor_note_names + roaster_name_map"
      pattern: "coffee_list\\.html"
---

<objective>
Fix C2 + C3.

C2 (D-03/D-04): The equipment and coffee create routes return the `mode="row"` `<tr>` fragment dropped into the non-table `#*-form-mount` div, producing a malformed card. Fix: return the full LIST fragment (`equipment_list.html` / `coffee_list.html`) swapped into the list container, so sort/filter order and the empty-state→first-item transition are correct for free, and the create form collapses on success (the form-mount div is replaced by the list swap).

C3: Equipment cards are too tall at 375px (one field per line). Group type + usage_count as flex-wrap pills, mirroring the coffee card pattern (`coffee_row.html`), to shorten the card.

Purpose: Correct, immediate post-create rendering and compact mobile equipment cards.
Output: Two fixed create handlers, updated HTMX targets on both create forms, a pill-based equipment card, and a regression test covering both create routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pwa-ux-fixes/13-RESEARCH.md
@.planning/phases/13-pwa-ux-fixes/13-PATTERNS.md

<interfaces>
<!-- Verified from source -->
app/routers/equipment.py:
  - list_equipment (lines 107-133): builds `groups = equipment_service.list_equipment_grouped_by_type(db, include_archived=...)`,
    returns fragments/equipment_list.html with context {"groups": groups, "include_archived": ...} on HX-Request
  - create_equipment (POST "", lines 174-225): on success returns fragments/equipment_row.html with
    {"equipment": equipment, "mode": "row", "include_oob_form_clear": True}  <-- the bug
  - get_session, require_user deps already imported

app/routers/coffees.py:
  - list-context builder (lines ~260-309): builds list_context = {coffees: rows, filters, flavor_note_names, roaster_name_map};
    returns fragments/coffee_list.html with list_context on HX-Request
  - create_coffee (POST "", lines 351-414): on success builds flavor_note_names + roaster_name_map for the NEW coffee only,
    returns fragments/coffee_row.html with {coffee, mode:"row", flavor_note_names, roaster_name_map, include_oob_form_clear:True}  <-- the bug
  - coffees_service.list_coffees(db, ...) is the query the GET list uses

app/templates/pages/equipment.html:
  - line 9: hx-target="#equipment-form-mount"  (create form)
  - line 10: hx-swap="innerHTML"
  - line 15: <div id="equipment-form-mount" class="mb-4"></div>
  - line 16: <div id="equipment-list"> ... </div>  (the list container the GET fragment fills)

app/templates/pages/coffees.html:
  - line 12: hx-target="#coffee-form-mount"  (create form)
  - line 13: hx-swap="innerHTML"
  - line 66: <div id="coffee-form-mount" class="mb-4"></div>
  - line 67: <div id="coffee-list"> ... </div>

app/templates/fragments/equipment_row.html:
  - card mode lines ~27-33: current one-field-per-line layout (C3 target)
  - lines 88-91: {% if include_oob_form_clear %}<div id="equipment-form-mount" hx-swap-oob="innerHTML"></div>{% endif %}  (C2 — remove)

app/templates/fragments/coffee_row.html lines 43-50: the flex-wrap pill pattern C3 copies
  (`<div class="mt-1 flex flex-wrap gap-2"> <span class="inline-flex px-2 py-1 rounded text-sm ...">...</span> ...`)

app/templates/fragments/equipment_list.html:
  - line 35: <div class="md:hidden space-y-3"> ... {% include "fragments/equipment_row.html" with mode="card" %}
  - line 15: <div class="hidden md:block"><table> (desktop table mode)

Test fixtures (tests/routers/test_brew_router.py is the analog):
  - parent conftest seeded_regular_user -> {"user", "session_id", "signed_cookie"}
  - local _authed_client(app, signed_cookie) builds a TestClient, sets session_id cookie, primes csrftoken via GET / + sets X-CSRF-Token header
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: C2 regression test for both create routes (RED)</name>
  <files>tests/routers/test_equipment_create_fragment.py</files>
  <read_first>
    - tests/routers/test_brew_router.py (the analog — _authed_client + _prime_csrf pattern, lines 148-172; seeded_regular_user usage)
    - tests/conftest.py lines 127-186 + 555-572 (client fixture, seeded_regular_user)
    - app/templates/fragments/equipment_list.html (the list-container markup the success response must now contain: the `hidden md:block` table + `md:hidden space-y-3` card wrapper)
    - app/templates/fragments/coffee_list.html (same — the list container markup)
    - app/routers/equipment.py create_equipment + the EquipmentCreate schema fields (type/brand/model/notes)
    - app/routers/coffees.py create_coffee + the CoffeeCreate schema fields (name required, roaster_id etc.)
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "Tests (NEW files)" test_equipment_create_fragment.py excerpt
    - C:\Users\John\.claude\projects\C--Claude-Coffee-Snobbery\memory\full-suite-test-isolation-gaps.md (catalog tables must be cleaned; LIKE-scope this test's rows or use a clean fixture so it does not pollute phase_04 DELETE FROM coffees)
  </read_first>
  <behavior>
    - Test 1 (test_equipment_create_returns_list_fragment): authed POST /equipment with valid form data returns 200; body contains the list-container markup (e.g. `space-y-3` AND/OR `hidden md:block`); body does NOT contain `equipment-form-mount` (the OOB clear is gone); the newly-created equipment's identifying field (brand/model) appears in the body.
    - Test 2 (test_coffee_create_returns_list_fragment): authed POST /coffees with valid form data (at minimum a `name`) returns 200; body contains the coffee list-container markup; body does NOT contain `coffee-form-mount`; the new coffee name appears.
    - Both tests must clean up their created rows (before+after) so they don't trip the known full-suite catalog-isolation gap.
  </behavior>
  <action>Create tests/routers/test_equipment_create_fragment.py modeled on test_brew_router.py. Reuse the `_authed_client` + `_prime_csrf` pattern (copy it locally or import the analog's helpers). Add a clean fixture that LIKE-scoped deletes this test's equipment + coffee rows before and after (per the isolation memory). Test 1 POSTs to /equipment, Test 2 POSTs to /coffees. Assert on the list-container markers and the absence of the form-mount id, and presence of the created entity's text. These are RED until Tasks 2-3 change the handlers.</action>
  <verify>
    <automated>docker compose run --rm coffee-snobbery-test tests/routers/test_equipment_create_fragment.py -rs --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File tests/routers/test_equipment_create_fragment.py exists with test_equipment_create_returns_list_fragment and test_coffee_create_returns_list_fragment.
    - Both tests are RED before Tasks 2-3, GREEN after.
    - Tests clean their own catalog rows (no full-suite pollution).
  </acceptance_criteria>
  <done>Two RED C2 tests exist covering equipment + coffee create routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix equipment create route + form target + C3 pill card (GREEN)</name>
  <files>app/routers/equipment.py, app/templates/pages/equipment.html, app/templates/fragments/equipment_row.html</files>
  <read_first>
    - app/routers/equipment.py (full — list_equipment lines 107-133 for the list-context recipe, create_equipment lines 174-225)
    - app/templates/pages/equipment.html (create form hx-target line 9, form-mount line 15, equipment-list line 16)
    - app/templates/fragments/equipment_row.html (card mode lines ~27-33; include_oob_form_clear block lines 88-91)
    - app/templates/fragments/coffee_row.html lines 43-50 (the flex-wrap pill pattern to copy)
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "equipment_row.html — C2, C3" + "equipment.py (router) — C2"
    - .planning/phases/13-pwa-ux-fixes/13-CONTEXT.md D-03, D-04
  </read_first>
  <action>
equipment.py create_equipment success branch (lines 217-225): replace the equipment_row.html `mode="row"` return with the SAME list-fragment return the GET list handler uses — `groups = equipment_service.list_equipment_grouped_by_type(db, include_archived=False)` then `TemplateResponse(request, "fragments/equipment_list.html", {"groups": groups, "include_archived": False})`. The validation-error branch (lines 197-207) is unchanged (still re-renders the form). Per D-03/D-04.

equipment.html: change the create form's `hx-target="#equipment-form-mount"` to `hx-target="#equipment-list"` and keep `hx-swap="innerHTML"`. This makes the success response replace the list region; the form collapses because the swap target is the list, leaving the form-mount empty (D-04 collapse-on-success). Confirm the form itself lives in/near the form-mount so it visually closes — if the form is rendered inside #equipment-form-mount via a separate "New" toggle, ensure the toggle resets; otherwise the list-swap alone collapses the inline form. Keep behavior CSP-safe (no inline hx-on:).

equipment_row.html: (C3) replace the card-mode one-field-per-line block (~lines 27-33) with a `<div class="mt-1 flex flex-wrap gap-2">` containing two pills — `equipment.type` and `equipment.usage_count` + " session(s)" — using the exact pill classes from coffee_row.html (`inline-flex px-2 py-1 rounded text-sm bg-cream-200 text-espresso-900 dark:bg-espresso-800 dark:text-cream-200`); keep notes on its own truncated line below. (C2) DELETE the `{% if include_oob_form_clear %} ... #equipment-form-mount ... {% endif %}` block (lines 88-91) — no longer needed once the route returns the list. Leave the desktop `mode="row"` table-cell branch intact (the list fragment's table still uses it).
  </action>
  <verify>
    <automated>docker compose run --rm coffee-snobbery-test tests/routers/test_equipment_create_fragment.py -rs --tb=short -k equipment</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'equipment_list.html' app/routers/equipment.py` >= 2 (list handler + create handler).
    - app/routers/equipment.py create handler no longer references `"include_oob_form_clear"` for the success branch.
    - app/templates/pages/equipment.html create form has `hx-target="#equipment-list"`.
    - equipment_row.html card mode contains `flex flex-wrap gap-2` with two pill spans; the include_oob_form_clear block is gone.
    - test_equipment_create_returns_list_fragment passes.
  </acceptance_criteria>
  <done>Equipment create returns the list fragment; form targets #equipment-list; card uses flex-wrap pills; OOB clear removed; equipment C2 test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix coffee create route + form target (GREEN)</name>
  <files>app/routers/coffees.py, app/templates/pages/coffees.html</files>
  <read_first>
    - app/routers/coffees.py (full — the list-context builder lines ~260-309 and create_coffee lines 351-414; note list_coffees + flavor_note_names + roaster_name_map are built for the full list there)
    - app/templates/pages/coffees.html (create form hx-target line 12, form-mount line 66, coffee-list line 67; the filter form at lines 25-31 also targets #coffee-list — confirm no conflict)
    - app/templates/fragments/coffee_list.html (the list container markup)
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "coffees.py (router) — C2"
  </read_first>
  <action>coffees.py create_coffee success branch (lines 404-414): replace the coffee_row.html `mode="row"` return with the FULL coffee_list.html fragment. Rebuild the list context exactly as the GET list handler does — call `coffees_service.list_coffees(db, ...)` with default (no) filters, then resolve `flavor_note_names` and `roaster_name_map` for the FULL result set (reuse the same helper calls the list-context builder uses, lines ~260-288), and return `TemplateResponse(request, "fragments/coffee_list.html", {"coffees": rows, "filters": {...defaults...}, "flavor_note_names": ..., "roaster_name_map": ...})`. Match the exact context keys coffee_list.html expects (coffees, filters, flavor_note_names, roaster_name_map). The validation-error branch is unchanged. coffees.html: change the create form's `hx-target="#coffee-form-mount"` to `hx-target="#coffee-list"`, keep `hx-swap="innerHTML"`; the form collapses on success via the list swap. Leave the filter form (lines 25-31) untouched — it already targets #coffee-list, which is the correct shared target now.</action>
  <verify>
    <automated>docker compose run --rm coffee-snobbery-test tests/routers/test_equipment_create_fragment.py -rs --tb=short -k coffee</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'coffee_list.html' app/routers/coffees.py` >= 2 (list builder + create handler).
    - coffees.py create handler success branch no longer returns coffee_row.html / `mode="row"`.
    - app/templates/pages/coffees.html create form has `hx-target="#coffee-list"`.
    - The coffee_list.html context keys returned by create_coffee match those the GET handler returns (coffees, filters, flavor_note_names, roaster_name_map).
    - test_coffee_create_returns_list_fragment passes.
  </acceptance_criteria>
  <done>Coffee create returns the full coffee_list fragment with correct context; form targets #coffee-list; coffee C2 test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /equipment, POST /coffees | Authenticated, CSRF-protected, Pydantic-validated catalog create (existing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering / Mass-assignment | create_equipment / create_coffee form parse | accept | Unchanged — the existing `await request.form()` + Pydantic `extra="forbid"` mass-assignment guard (T-04-MASS) is preserved; this plan only changes the SUCCESS-branch fragment returned. |
| T-13-07 | Spoofing (CSRF) | create form POST | mitigate | The create forms already carry the double-submit CSRF token; hx-target change does not alter CSRF. Test primes csrftoken to exercise the protected path. |
| T-13-08 | Information Disclosure | list fragment returned on create | accept | Equipment + coffees are shared household catalog (CLAUDE.md invariant) — returning the full list to any authed user leaks nothing cross-user. INFO residual. |

No HIGH-severity threats. No auth/CSRF/header surface changed.
</threat_model>

<verification>
- `docker compose run --rm coffee-snobbery-test tests/routers/test_equipment_create_fragment.py -rs --tb=short` green (both routes).
- Per-wave: full suite `docker compose run --rm coffee-snobbery-test tests/ -rs --tb=short` shows no new failures.
- Manual 375px: create an equipment item → compact pill card appears in the list, form collapses (covered by Plan-level human checks where applicable).
</verification>

<success_criteria>
- C2 satisfied: both create routes return the correct full list fragment; no malformed card; form collapses (D-03/D-04).
- C3 satisfied: equipment cards use flex-wrap pills and are shorter at 375px.
- Regression test covers both create routes and is green.
</success_criteria>

<output>
After completion, create `.planning/phases/13-pwa-ux-fixes/13-03-SUMMARY.md`
</output>
