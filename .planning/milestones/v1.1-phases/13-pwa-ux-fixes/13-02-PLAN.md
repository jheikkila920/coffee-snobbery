---
phase: 13-pwa-ux-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_pwa_icons.py
  - app/static/img/logo-badge.png
  - app/static/img/icon-192.png
  - app/static/img/icon-512.png
  - app/static/img/icon-512-maskable.png
  - app/static/img/apple-touch-icon.png
  - app/static/img/hero-alt.jpg
autonomous: false
requirements: [C10, D-07]
must_haves:
  truths:
    - "The circular nav badge and PWA icons show the full mascot (bean + top-hat + monocle + cup + steam) undistorted, inscribed in the circle (C10, D-07)"
    - "circular_crop() center-crops to a square BEFORE resizing, so a non-square source is never squished (C10, D-07)"
    - "scripts/generate_pwa_icons.py reads app/static/img/hero.jpg as SRC, not snobbery-login.jpg (C10, D-07)"
    - "The maskable icon keeps its 10% safe-zone padding + cream-50 background (Pitfall 8)"
    - "The login hero (snobbery-login-hero.jpg) is NOT regenerated (D-07 brand lock)"
  artifacts:
    - path: "scripts/generate_pwa_icons.py"
      provides: "Hardened circular_crop (center-crop-square -> resize -> mask) + hero.jpg SRC (C10)"
      contains: "hero.jpg"
    - path: "app/static/img/logo-badge.png"
      provides: "Regenerated circular nav badge, full mascot, undistorted"
    - path: "app/static/img/icon-512-maskable.png"
      provides: "Regenerated maskable icon with safe-zone padding"
  key_links:
    - from: "scripts/generate_pwa_icons.py SRC"
      to: "app/static/img/hero.jpg"
      via: "Path('app/static/img/hero.jpg')"
      pattern: "hero\\.jpg"
    - from: "circular_crop()"
      to: "square crop then resize"
      via: "img.crop((left, top, ...)) before resize"
      pattern: "\\.crop\\("
---

<objective>
Fix C10: the circular nav badge and PWA app icons are skewed because `scripts/generate_pwa_icons.py` points at `snobbery-login.jpg` (2816x1536 landscape) and `circular_crop()` does `resize((size,size))` with no aspect preservation — squishing the landscape source into a square. Regenerate all icons from the new square mascot `app/static/img/hero.jpg` (1021x1021) and harden the crop so this class of bug cannot recur.

Per D-07: full mascot inscribed in the circle (keep cup/steam/monocle chain — do NOT zoom to just the bean face); regenerate logo-badge.png, icon-192.png, icon-512.png, icon-512-maskable.png, apple-touch-icon.png; login hero is NOT regenerated; the maskable variant keeps its 10% safe-zone padding + cream-50 background.

Purpose: Restore correct, undistorted branding on the installed PWA icon and the in-app nav badge.
Output: Hardened generator script + 5 regenerated, committed PNGs. (The nav `<img>` markup is already correct — `h-N w-N rounded-full` — so no template change.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pwa-ux-fixes/13-RESEARCH.md
@.planning/phases/13-pwa-ux-fixes/13-PATTERNS.md

<interfaces>
<!-- Verified from PATTERNS.md "scripts/generate_pwa_icons.py (utility, file-I/O) — C10" -->
scripts/generate_pwa_icons.py:
  - SRC = Path("app/static/img/snobbery-login.jpg")  (line ~29 — the BUG: landscape source)
  - circular_crop(img, size): convert RGBA + resize((size,size)) [no aspect preservation] + ellipse mask
  - maskable_icon(...): already handles 10% safe-zone padding + cream-50 background — calls circular_crop, so the crop fix propagates
  - Output writers (lines ~72-97): icon-192.png, icon-512.png, icon-512-maskable.png, apple-touch-icon.png, logo-badge.png, and a snobbery-login-hero.jpg block (lines ~93-97) that MUST be removed/skipped (D-07 brand lock)
  - Pillow >=12.2 (per requirements; dev-time only — generated PNGs are committed, D-15)

Source assets:
  - app/static/img/hero.jpg — NEW, 1021x1021 square (the correct source, John's choice)
  - app/static/img/hero-alt.jpg — 1149x1149 unused alternate; safe to DELETE during cleanup (CONTEXT Deferred / D-07)

base.html (NO CHANGE — markup already correct):
  - line 74: <img src="/static/img/logo-badge.png" ... class="h-12 w-12 rounded-full">
  - line 167: <img src="/static/img/logo-badge.png" ... class="h-10 w-10 rounded-full">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden circular_crop + repoint SRC to hero.jpg</name>
  <files>scripts/generate_pwa_icons.py</files>
  <read_first>
    - scripts/generate_pwa_icons.py (full file — SRC constant, circular_crop, maskable_icon, output writers, the snobbery-login-hero.jpg block)
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "scripts/generate_pwa_icons.py (utility, file-I/O) — C10" (the exact before/after circular_crop excerpt)
    - .planning/phases/13-pwa-ux-fixes/13-CONTEXT.md <decisions> D-07 (full mascot in circle, center-crop-then-resize-then-mask, which outputs, login hero untouched)
    - .planning/phases/13-pwa-ux-fixes/13-RESEARCH.md Pitfall 8 (maskable safe-zone)
  </read_first>
  <action>Change `SRC` to `Path("app/static/img/hero.jpg")`. Harden `circular_crop(img, size)` to: (1) center-crop to a square first — `w, h = img.size; min_dim = min(w, h); left = (w - min_dim)//2; top = (h - min_dim)//2; img = img.crop((left, top, left+min_dim, top+min_dim))`; (2) THEN `img.convert("RGBA").resize((size, size), Image.LANCZOS)`; (3) THEN apply the existing ellipse mask. This is defensive per D-07 (correct even though hero.jpg is already square). Leave `maskable_icon()` unchanged structurally — it already does 10% safe-zone padding + cream-50 background and calls circular_crop, so the crop fix propagates. Remove (or comment out) the `snobbery-login-hero.jpg` generation block — D-07 locks the login hero as out of scope. Add a docstring on circular_crop documenting the 3 steps. Do NOT change the output filenames or sizes.</action>
  <verify>
    <automated>python -c "import ast,sys; src=open('scripts/generate_pwa_icons.py').read(); assert 'hero.jpg' in src and '.crop(' in src and 'snobbery-login.jpg' not in src.replace('snobbery-login-hero',''), 'SRC/crop check failed'; ast.parse(src); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/generate_pwa_icons.py contains `Path("app/static/img/hero.jpg")` and NOT a bare `snobbery-login.jpg` SRC.
    - circular_crop contains an `img.crop(` call that executes BEFORE the `.resize(` call (center-crop-then-resize).
    - The snobbery-login-hero.jpg generation block is removed or commented out.
    - The file parses (ast.parse succeeds).
  </acceptance_criteria>
  <done>SRC points at hero.jpg; circular_crop center-crops to square before resize; login-hero block gone; script parses.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate + commit the 5 icon PNGs; delete hero-alt.jpg</name>
  <files>app/static/img/logo-badge.png, app/static/img/icon-192.png, app/static/img/icon-512.png, app/static/img/icon-512-maskable.png, app/static/img/apple-touch-icon.png, app/static/img/hero-alt.jpg</files>
  <read_first>
    - scripts/generate_pwa_icons.py (the now-hardened script from Task 1)
    - app/routers/pwa.py manifest() icons block (the icon filenames + sizes the manifest references — confirm the regenerated names match)
    - .planning/phases/13-pwa-ux-fixes/13-CONTEXT.md <decisions> D-07 (which 5 outputs; hero-alt.jpg safe to delete)
    - C:\Users\John\.claude\projects\C--Claude-Coffee-Snobbery\memory\commit-docs-false-untracked-review-docs.md (commit_docs:false context — binary assets are NOT docs and ARE committed normally)
  </read_first>
  <action>Run the hardened generator: `python scripts/generate_pwa_icons.py` (install Pillow first if absent: `pip install "Pillow>=12.2"`). Confirm it writes the 5 PNGs: logo-badge.png, icon-192.png, icon-512.png, icon-512-maskable.png, apple-touch-icon.png. Verify each output exists and is a valid PNG of the expected square dimensions (192x192, 512x512, 180x180 for apple-touch, and the badge size the script targets) via Pillow `Image.open(...).size`. Delete the unused `app/static/img/hero-alt.jpg` (D-07 — John named hero.jpg as the source; alt is reference clutter). These are pre-generated-and-committed assets per D-15 — they are committed binaries, not runtime-built. Do NOT regenerate snobbery-login-hero.jpg.</action>
  <verify>
    <automated>python -c "from PIL import Image; from pathlib import Path; req={'app/static/img/icon-192.png':(192,192),'app/static/img/icon-512.png':(512,512),'app/static/img/icon-512-maskable.png':(512,512),'app/static/img/apple-touch-icon.png':(180,180)}; [print(p, Image.open(p).size) or (Image.open(p).size==s or (_ for _ in ()).throw(AssertionError(p))) for p,s in req.items()]; assert Path('app/static/img/logo-badge.png').exists(); assert not Path('app/static/img/hero-alt.jpg').exists(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 PNGs exist and are valid PNGs at the expected dimensions (192, 512, 512-maskable, 180 apple-touch, badge present).
    - app/static/img/hero-alt.jpg no longer exists.
    - app/static/img/snobbery-login-hero.jpg is UNCHANGED (git diff shows no modification).
    - Manifest icon filenames in pwa.py still match the regenerated filenames.
  </acceptance_criteria>
  <done>5 PNGs regenerated at correct dimensions; hero-alt.jpg deleted; login hero untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual check — full mascot, undistorted, in the circle (C10)</name>
  <files>(verification only — no files modified)</files>
  <action>Manual verification checkpoint — perform the steps in <how-to-verify>. No source files are modified by this task.</action>
  <verify>Manual: follow <how-to-verify>; the gate is met when <acceptance_criteria> hold.</verify>
  <done>See <acceptance_criteria> / <resume-signal>.</done>
  <what-built>Regenerated circular badge + PWA icons from hero.jpg with an aspect-safe center-crop.</what-built>
  <how-to-verify>
    Icon content is a visual judgment that pytest cannot make.
    1. Open app/static/img/logo-badge.png and app/static/img/icon-512.png in an image viewer.
    2. Confirm the FULL mascot is visible — bean + top-hat + monocle (with chain) + espresso cup + steam — inscribed in the circle, NOT zoomed to just the bean's face, and NOT horizontally/vertically squished.
    3. Open icon-512-maskable.png — confirm the mascot sits inside a ~10% safe-zone padding on a solid cream background (so an Android circle/squircle mask won't clip it).
    4. Rebuild + view in-app: `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`, then load the app at 375px — the nav badge (top-left) is a clean circle with the full undistorted mascot.
    NOTE (memory sw-stale-cache-confounds-ui-verify): the service worker SWRs /static/, so do a "Clear site data" (not just hard reload) before trusting the in-browser badge.
    Expected: full mascot, round, no distortion, maskable padding intact.
  </how-to-verify>
  <acceptance_criteria>
    - Badge + icons show the complete mascot, undistorted, inscribed in the circle.
    - Maskable icon has visible safe-zone padding on cream background.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the distortion/framing issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time script → committed static assets | generate_pwa_icons.py writes PNGs served by StaticFiles + referenced by /manifest.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | regenerated icon PNGs | accept | Icons are non-sensitive brand assets; no auth/integrity surface; manifest icon refs unchanged. INFO residual. |
| T-13-05 | Spoofing | manifest icon filename mismatch | mitigate | Task 2 verifies regenerated filenames match the icon list in pwa.py manifest() so installability is preserved (don't break manifest — CLAUDE.md invariant). |

No HIGH-severity threats. No header, CSP, or auth surface touched.
</threat_model>

<verification>
- Generator parses + writes 5 PNGs at correct dimensions (automated).
- hero-alt.jpg deleted; login hero unchanged (automated).
- Manual visual check: full undistorted mascot in circle + maskable padding (Task 3).
</verification>

<success_criteria>
- C10 satisfied: badge + PWA icons regenerated from hero.jpg, full mascot, undistorted.
- circular_crop hardened (center-crop-square then resize then mask) — D-07.
- Maskable safe-zone preserved; login hero untouched; hero-alt.jpg removed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-pwa-ux-fixes/13-02-SUMMARY.md`
</output>
