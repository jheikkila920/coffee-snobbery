---
phase: 05-brew-sessions
plan: 04
type: execute
wave: 3
depends_on: [2]
files_modified:
  - app/routers/brew.py
  - app/main.py
  - app/templates/fragments/brew_prefill_fields.html
  - tests/routers/test_brew_router.py
autonomous: true
requirements: [BREW-02, BREW-03, BREW-05, BREW-07, BREW-09]
user_setup: []

must_haves:
  truths:
    - "GET /brew/new renders the form with D-04/D-05/D-06/D-08 prefill values + per-field touched-state for pills"
    - "GET /brew/prefill?coffee_id=X|recipe_id=Y returns a 200 form-fields fragment with re-resolved prefill (D-04 coffee re-prefill, D-05 recipe-wins) + refreshed advertised quick-add chips (D-11) for the dynamic post-load update path"
    - "POST /brew (and /brew/{id}) validate via Pydantic -> 200 + re-rendered form fragment on error (SEC-06), HX-Redirect on success"
    - "GET /brew/{id}/edit shows actual stored values (no ghost prefill) and 404s on a cross-user id"
    - "POST /brew/draft upserts the per-user draft on blur; the draft is cleared on successful submit"
    - "extraction_yield_pct is never accepted from the form; user_id comes from request.state.user.id"
  artifacts:
    - path: "app/routers/brew.py"
      provides: "form routes /brew/new, GET /brew/prefill (dynamic re-prefill fragment), /brew/{id}/edit, POST /brew, POST /brew/{id}, POST /brew/draft + payload parsing + hydrate context"
      contains: "require_user"
    - path: "app/main.py"
      provides: "brew router registration"
      contains: "brew"
  key_links:
    - from: "app/routers/brew.py"
      to: "app/services/brew_sessions.py + brew_drafts.py"
      via: "resolve_prefill / create / update / upsert_draft / clear_draft"
      pattern: "resolve_prefill|create_brew_session"
    - from: "app/routers/brew.py (GET /brew/prefill)"
      to: "app/services/brew_sessions.py::resolve_prefill"
      via: "coffee_id/recipe_id query params -> re-resolved prefill + advertised chips fragment (dynamic D-04/D-05/D-11 update path)"
      pattern: "/brew/prefill"
    - from: "app/routers/brew.py"
      to: "request.state.user.id"
      via: "require_user gate + per-user scoping; server sets user_id"
      pattern: "require_user"
---

<objective>
Build the brew form router: the dedicated-route add/edit form (D-01), the SEC-06 validation-to-200-fragment path, the prefill-driven GET that powers the <30s flow (BREW-02), the brew-again deep link (BREW-09/D-08), and the draft autosave endpoint (BREW-07). Register the router in the app.

Purpose: This is the server contract the form template + Alpine components (Plan 05) render against, and the route surface the sessions list (Plan 06) links into. Defining the routes + hydrate-context shape first lets the template plan render without guessing the context keys.
Output: `app/routers/brew.py` (form routes), `app/main.py` registration, `tests/routers/test_brew_router.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-brew-sessions/05-CONTEXT.md
@.planning/phases/05-brew-sessions/05-RESEARCH.md
@.planning/phases/05-brew-sessions/05-PATTERNS.md
@.planning/phases/05-brew-sessions/05-UI-SPEC.md
@.planning/phases/05-brew-sessions/05-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 02 services + the canonical coffees router. Use directly. -->

From app/services/brew_sessions.py (Plan 02):
  resolve_prefill(db, *, by_user_id, from_session_id=None, coffee_id=None, recipe_id=None) -> dict
  create_brew_session(db, *, by_user_id, **fields) -> BrewSession
  update_brew_session(db, *, session_id, by_user_id, **fields) -> BrewSession | None  (None=not owned)
  get_brew_session(db, *, session_id, by_user_id) -> BrewSession | None
From app/services/brew_drafts.py (Plan 02):
  upsert_draft(db, *, by_user_id, payload) ; get_draft(db, *, by_user_id) -> dict|None ; clear_draft(...)

Router analog (app/routers/coffees.py):
  router = APIRouter(prefix="/brew") ; every handler `user: User = Depends(require_user)` and scopes by user.id.
  SEC-06 catch path (lines 353-382): await request.form() -> _parse_form_payload -> Schema(**input)
    -> on ValidationError re-render the form at status_code=200 with values+errors via errors_by_field.
  ARRAY getlist parsing (lines 155-168): form_data.getlist("flavor_note_ids_observed") -> [int].
  _normalize_errors + _FORM_FIELDS sentinel fold (lines 70-125): extra=forbid rejection folds to _form.
  _hydrate_form_context (lines 186-238): resolves selected names + chip seeds + (here) prefill values.
  List/fragment branch + ROUTE-ORDER gotcha (lines 246-312, 420-456): declare LITERAL paths
    (/brew/new, /brew/import, /brew/export, /brew/draft) BEFORE /brew/{session_id} so the int
    matcher doesn't capture them.

require_user: app/dependencies/auth.py lines 33-45 (returns full User from request.state.user).
get_session: app/dependencies/db.py::get_session. templates: app/templates_setup.py::templates.
errors_by_field: app/services/form_validation.py lines 45-69 (returns HTTP 200, not 422, for clean HTMX swap).
main.py include_router block: imports lines 84-94, registration lines 218-228 (no middleware changes).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: brew router — GET new/edit (prefill) + POST create/update (SEC-06) + register</name>
  <files>app/routers/brew.py, app/main.py, tests/routers/test_brew_router.py</files>
  <read_first>
    - app/routers/coffees.py (router setup + require_user lines 46-64; SEC-06 catch lines 353-382; ARRAY getlist lines 155-168; _normalize_errors + sentinel lines 70-125; _hydrate_form_context lines 186-238; route-order gotcha lines 420-456)
    - app/dependencies/auth.py (require_user lines 33-45)
    - app/services/form_validation.py (errors_by_field lines 45-69; DuplicateNameError lines 31-42)
    - app/services/brew_sessions.py (Plan 02 — resolve_prefill / create / update / get)
    - app/schemas/brew_session.py (Plan 01 — BrewSessionCreate/Update)
    - app/main.py (include_router block: imports lines 84-94, registration lines 218-228)
    - .planning/phases/05-brew-sessions/05-PATTERNS.md (section: brew.py router; Shared Patterns)
    - .planning/phases/05-brew-sessions/05-UI-SPEC.md (Brew Session Form layout + form mechanics; Prefill Indicators states)
  </read_first>
  <behavior>
    - Test: GET /brew/new (authed, with one prior session) -> 200; context carries prefill values for coffee/recipe/equipment/dose/water/temp/grind and blank rating/observed/notes, plus a per-field touched-state map seeded false (prefilled-untouched).
    - Test: GET /brew/new?from={id} -> prefill from that session, rating/observed/notes blank; from a cross-user session id -> prefill source ignored (no leak) / treated as no source.
    - Test: GET /brew/{id}/edit for an owned session -> 200 with actual stored values, no ghost-prefill flags; for a non-owned id -> 404 (not 403 — IDOR existence non-leak).
    - Test: POST /brew with valid form -> session created (user_id == authed user), response HX-Redirect to the sessions list; draft cleared.
    - Test: POST /brew with invalid rating (3.3) -> HTTP 200 + form re-rendered with the rating error (SEC-06), no row inserted.
    - Test: POST /brew including an `extraction_yield_pct` form field -> extra=forbid folds to _form error at 200; EY never written. POST cannot set user_id from the form.
    - Test: POST /brew/{id} (edit) updates an owned session; a non-owned id -> 404.
  </behavior>
  <action>
    Create app/routers/brew.py mirroring coffees.py. `router = APIRouter(prefix="/brew")`; every handler `user: User = Depends(require_user)`, `db: Session = Depends(get_session)`, scoping all queries by `user.id` (architectural invariant). Implement a `_parse_form_payload(form_data)` like coffees.py: build `raw_view` (echo) + `schema_input`; collect `flavor_note_ids_observed` via `form_data.getlist(...)` -> `[int]` (lines 155-168 pattern); NEVER read `extraction_yield_pct` or `user_id` from the form. `_normalize_errors` + a `_FORM_FIELDS` set so an extra=forbid rejection folds into `_form` (lines 70-125). `_hydrate_form_context(db, *, user, values, errors, mode, prefill=None)` resolves: selectable coffees/bags/recipes/equipment + the selected names, `selected_flavor_notes` (seed observed chips), the D-11 advertised-note chips for the selected coffee (read `coffees.advertised_flavor_note_ids` — do NOT conflate with observed), and (for /brew/new) the prefill values + a per-field touched-state map (all false = prefilled-untouched, drives the pills) + the pill caption source ("from last brew" / "from recipe" / "from this brew") per UI-SPEC.

    Routes (declare LITERAL paths before `/{session_id}` — route-order gotcha):
    - `GET /brew/new` -> call resolve_prefill(db, by_user_id=user.id, from_session_id=request.query_params.get("from"), coffee_id=..., recipe_id=...) and render `pages/brew_form.html` (mode="create"). If the user has no prior sessions, prefill is empty (the first-brew hint, UI-SPEC).
    - `POST /brew` -> SEC-06 path: parse -> `BrewSessionCreate(**schema_input)` -> on ValidationError re-render `pages/brew_form.html` at status_code=200 with values+errors; on success `create_brew_session(...)`, `clear_draft(db, by_user_id=user.id)`, and respond `HX-Redirect` to the sessions list (planner-chosen; UI-SPEC allows full-nav redirect too).
    - `GET /brew/{session_id}/edit` -> `get_brew_session(...)`; 404 if None (IDOR); render `pages/brew_form.html` mode="edit" with the session's actual stored values, NO ghost-prefill flags; auto-open the disclosure if yield_grams_actual or tds_pct is non-null (UI-SPEC).
    - `POST /brew/{session_id}` -> SEC-06 path with `BrewSessionUpdate`; `update_brew_session(...)`; 404 if None; HX-Redirect on success (edit is NOT draft-backed).

    Register in app/main.py: `from app.routers import brew as brew_router` (imports block) and `app.include_router(brew_router.router)` (registration block). No middleware changes.

    Write tests/routers/test_brew_router.py using the conftest authed `client` + `seeded_regular_user`: `test_form_validation_200`, `test_ey_not_writable`, `test_list_user_scoped` is in Plan 06 — here cover `test_create_sets_user_id`, `test_edit_404_cross_user`, `test_brew_new_prefill_context`, `test_brew_again_blanks_per_attempt` (HTTP-level).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/routers/test_brew_router.py::test_form_validation_200 tests/routers/test_brew_router.py::test_ey_not_writable -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/brew.py` declares the literal routes (/brew/new, POST /brew, /brew/{id}/edit, POST /brew/{id}) with /brew/new before /{session_id}.
    - Every handler is gated by `require_user` and scopes by `user.id`.
    - `_parse_form_payload` never reads extraction_yield_pct or user_id from the form; getlist collects flavor_note_ids_observed.
    - Invalid POST returns HTTP 200 with the re-rendered form + errors (not 422).
    - Cross-user `/brew/{id}/edit` and POST `/brew/{id}` return 404.
    - On success the draft is cleared and the response HX-Redirects to the list.
    - `app/main.py` registers the brew router; the app boots.
    - The two named tests exit 0.
  </acceptance_criteria>
  <done>Form router renders prefill, enforces SEC-06 + extra=forbid, scopes per-user with 404 on IDOR, clears the draft on submit, and is registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Draft autosave endpoint (BREW-07) + draft restore wiring</name>
  <files>app/routers/brew.py, tests/routers/test_brew_router.py</files>
  <read_first>
    - app/routers/coffees.py (a POST handler reading request.form() + CSRF expectations)
    - app/services/brew_drafts.py (Plan 02 — upsert_draft / get_draft / clear_draft)
    - app/csrf.py (CSRFFormFieldShim — hidden field hoist; the autosave must carry the token)
    - app/static/js/htmx-listeners.js (global X-CSRF-Token injection lines 34-39 — HTMX requests get it for free)
    - .planning/phases/05-brew-sessions/05-RESEARCH.md (Draft Persistence: reconciliation order, CSRF for autosave)
    - .planning/phases/05-brew-sessions/05-UI-SPEC.md (Draft Persistence UX: /brew/new only, reconciliation, restore notice)
  </read_first>
  <behavior>
    - Test: POST /brew/draft (authed) with a JSON/form payload -> 200; upserts exactly one draft row for the user; second POST replaces it.
    - Test: POST /brew/draft without a CSRF token -> 403 (CSRF enforced like every state-changing route).
    - Test: GET /brew/new when the user has a server draft and the request indicates localStorage was empty -> the restore path returns the server draft (reconciliation: localStorage primary, server fallback — server side exposes get_draft; the Alpine layer in Plan 05 decides order).
    - Test: a draft for user A is never returned to user B.
  </behavior>
  <action>
    Add `POST /brew/draft` to app/routers/brew.py (gated by require_user). Read the autosave payload (form-encoded or JSON — match what Plan 05's brew-draft.js sends; recommend an HTMX request so the global htmx-listeners.js injects X-CSRF-Token automatically, per RESEARCH). Call `upsert_draft(db, by_user_id=user.id, payload=...)`; return a minimal 200 (no body needed; silent autosave per UI-SPEC). The CSRF middleware already enforces the token on state-changing routes — do NOT exempt this route. Drafts apply to /brew/new ONLY; do not draft-back the edit form.

    Expose the server draft to the GET /brew/new render so the Alpine layer can reconcile: include `server_draft` (from get_draft) in the brew_form context for the create mode only. The reconciliation ORDER (localStorage primary, server only when localStorage empty — BREW-07) is enforced client-side in brew-draft.js (Plan 05); the router just provides the server draft. Ensure `clear_draft` is already wired into the successful POST /brew path (Task 1).

    Extend tests/routers/test_brew_router.py with `test_draft_upsert_one_row`, `test_draft_requires_csrf`, `test_draft_per_user`, and `test_brew_new_includes_server_draft`.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/routers/test_brew_router.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `POST /brew/draft` exists, is require_user-gated, and upserts one draft per user.
    - The route is NOT CSRF-exempt; a tokenless POST returns 403.
    - GET /brew/new (create mode) includes the server draft in context so the client can reconcile.
    - clear_draft fires on successful POST /brew (verified by Task 1's create test or a dedicated test).
    - A draft never crosses users.
    - Full `tests/routers/test_brew_router.py` exits 0.
  </acceptance_criteria>
  <done>Draft autosave endpoint persists one per-user draft under CSRF, exposes the server draft for client reconciliation, and the draft is cleared on submit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /brew/prefill — dynamic re-prefill fragment (D-04 / D-05 / D-11 post-load update path)</name>
  <files>app/routers/brew.py, app/templates/fragments/brew_prefill_fields.html, tests/routers/test_brew_router.py</files>
  <read_first>
    - app/routers/coffees.py (HX-Request fragment branch lines 246-312; route-order gotcha lines 420-456 — declare /brew/prefill among the LITERAL paths before /brew/{session_id})
    - app/services/brew_sessions.py (Plan 02 — resolve_prefill already accepts coffee_id / recipe_id)
    - app/templates/pages/brew_form.html (the fields this fragment must mirror — Plan 05 builds it; this fragment renders the prefill-dependent subset)
    - .planning/phases/04-shared-catalog/04-CONTEXT.md (D-01 HTMX-fragment + hx-get-on-change pattern; HX-Trigger pre-select handoff — mirror this for re-prefill)
    - .planning/phases/05-brew-sessions/05-CONTEXT.md (D-04 coffee re-prefill, D-05 recipe-wins-on-change, D-11 advertised-chips-refresh)
    - .planning/phases/05-brew-sessions/05-UI-SPEC.md (Brew Session Form: which fields are prefill-dependent; Advertised quick-add chips "refresh when the selected coffee changes")
  </read_first>
  <behavior>
    - Test: GET /brew/prefill?coffee_id=X (authed, HX-Request) -> 200 fragment containing the prefilled dose/water/temp/grind + bag/equipment/water_type values re-resolved from the user's last session with coffee X (D-04), the advertised quick-add chips for coffee X (D-11), and "from last brew" pill captions; rating/observed/notes are NOT in the fragment (per-attempt fields untouched).
    - Test: GET /brew/prefill?recipe_id=Y -> 200 fragment where dose/water/temp/grind carry the recipe's targets with "from recipe" captions (D-05 recipe-wins overwrites exactly those four template fields); other fields unchanged.
    - Test: GET /brew/prefill?coffee_id=X with a coffee the user has never brewed -> 200 fragment with blank prefill-dependent fields + that coffee's advertised chips (still refreshed) — no last-session values, no crash.
    - Test: GET /brew/prefill scoped to user.id — a coffee/recipe id resolves only against the authed user's own sessions; no cross-user prefill leak.
    - Test: GET /brew/prefill is require_user-gated (anonymous -> redirect/401 per the auth dependency).
  </behavior>
  <action>
    Add `GET /brew/prefill` to app/routers/brew.py (declare it among the LITERAL paths BEFORE `/brew/{session_id}` — route-order gotcha). Gate with `require_user`; scope to `user.id`. Read `coffee_id` and `recipe_id` from `request.query_params` (both optional). Call `resolve_prefill(db, by_user_id=user.id, coffee_id=coffee_id, recipe_id=recipe_id)` — the SAME service the GET /brew/new path uses (no new service code). Build the prefill context the way `_hydrate_form_context` does for create mode: the re-resolved prefill values, the per-field touched-state map (false = prefilled-untouched, drives the pills), the pill caption source ("from last brew" when coffee-driven, "from recipe" for the four D-05 template fields when recipe-driven), and the D-11 advertised-note chips for the selected coffee (read `coffees.advertised_flavor_note_ids`; NEVER conflate with observed). Render the new partial `app/templates/fragments/brew_prefill_fields.html` — the prefill-DEPENDENT subset of the form ONLY (Coffee-driven: bag, recipe, brewer, grinder, kettle, water_type, dose, water, temp, grind + their pills; plus the advertised quick-add chip region). It must NOT include rating, observed flavor notes, or notes (per-attempt fields stay as the user left them). The fragment is what Plan 05's coffee/recipe `<select>` `hx-get` swaps into the prefill region + advertised-chips region. The FragmentCacheHeadersMiddleware applies no-store + Vary: HX-Request for free. Re-prefill semantics: per D-05 a recipe change OVERWRITES exactly the four template fields (dose/water/temp/grind); per D-04 a coffee change re-prefills from the user's last session with that coffee; per D-11 the advertised chips always refresh to the newly selected coffee's notes.

    Plan 05 builds the page (`pages/brew_form.html`) that includes this fragment for the initial render and targets it for re-prefill swaps — keep the fragment's field markup consistent so the initial server render and the swap produce identical DOM. (If brew_form.html does not yet exist when this task runs, scaffold `brew_prefill_fields.html` with the field block here and have Plan 05 `{% include %}` it; coordinate the include in 05-04-SUMMARY so Plan 05 renders against it.)

    Extend tests/routers/test_brew_router.py with `test_prefill_fragment_coffee_change` (D-04), `test_prefill_fragment_recipe_wins` (D-05 overwrites the four fields), `test_prefill_fragment_advertised_chips` (D-11 chips for the selected coffee), `test_prefill_user_scoped` (no cross-user leak), and `test_prefill_unknown_coffee_blank` (never-brewed coffee -> blank fields, chips still present).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/routers/test_brew_router.py -k prefill -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /brew/prefill?coffee_id=X` returns 200 with a fragment containing the prefilled dose/water/temp/grind + advertised chips (re-resolved for coffee X); rating/observed/notes absent.
    - `GET /brew/prefill?recipe_id=Y` overwrites exactly dose/water/temp/grind with the recipe targets and tags them "from recipe" (D-05 recipe-wins); other fields unchanged.
    - The route is declared BEFORE `/brew/{session_id}` (route-order), is `require_user`-gated, and scopes prefill to `user.id` (no cross-user leak).
    - The route reuses `resolve_prefill` (grep confirms `resolve_prefill` in the prefill handler) — no duplicate prefill logic.
    - `app/templates/fragments/brew_prefill_fields.html` exists and renders ONLY the prefill-dependent fields + advertised-chip region (no rating/observed/notes).
    - The named `-k prefill` tests exit 0.
  </acceptance_criteria>
  <done>The dynamic re-prefill server endpoint exists: a coffee/recipe change fetches a re-resolved prefill-fields fragment (D-04/D-05) with refreshed advertised chips (D-11), scoped to the user, reusing resolve_prefill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form/POST -> brew router | Untrusted form; Pydantic validates; user_id + EY never taken from input; CSRF on every POST |
| URL path /brew/{id} -> service | session_id from the URL must be checked for ownership before any read/write |
| draft autosave POST -> brew_drafts | Must carry CSRF; keyed by server user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Elevation / Info disclosure (IDOR) | GET/POST /brew/{id}, /brew/new?from= | mitigate | get/update scoped by user.id; 404 (not 403) on non-owned id; from= source is user-scoped (Task 1) |
| T-05-16 | Tampering / Elevation (mass assignment) | POST /brew payload | mitigate | _parse_form_payload never reads user_id/extraction_yield_pct; BrewSessionCreate extra=forbid folds extras to _form; server sets user_id from request.state.user.id (Task 1) |
| T-05-17 | Spoofing (CSRF) | POST /brew, POST /brew/{id}, POST /brew/draft | mitigate | Hidden X-CSRF-Token field + global htmx-listeners.js header; no route exempted (Tasks 1,2) |
| T-05-18 | Info disclosure | server draft cross-user read | mitigate | get_draft keyed by user.id; never returns another user's draft (Task 2) |
| T-05-18b | Info disclosure (IDOR) | GET /brew/prefill?coffee_id/recipe_id | mitigate | resolve_prefill scoped by user.id; a coffee/recipe id resolves only against the authed user's own sessions; no cross-user prefill leak (Task 3) |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery python -m pytest -q tests/routers/test_brew_router.py` is green.
- App boots with the brew router registered (`docker compose up -d coffee-snobbery` healthy; GET /brew/new returns 200 for an authed user).
</verification>

<success_criteria>
- Dedicated form routes with SEC-06 validation, prefill-driven GET, brew-again deep link, 404-on-IDOR.
- Draft autosave under CSRF; draft cleared on submit; server draft exposed for client reconciliation.
- Router registered; user_id + EY never accepted from input.
- Router test file passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-brew-sessions/05-04-SUMMARY.md`. Document the exact context keys `pages/brew_form.html` will receive (prefill dict shape, touched-state map, advertised-chip list, server_draft) so Plan 05 renders against them.
</output>
