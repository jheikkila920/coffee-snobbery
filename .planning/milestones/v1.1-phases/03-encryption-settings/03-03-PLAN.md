---
phase: 03-encryption-settings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/settings.py
autonomous: true
requirements: [SEC-08]
requirements_addressed: [SEC-08]
must_haves:
  truths:
    - "`settings.prewarm_cache(db)` populates a module-level `_cache: dict[str, _CachedSetting]` with every `app_settings` row coerced to its native Python type"
    - "`settings.get_str / get_int / get_bool / get_json` return the coerced value from the cache without hitting the DB on the read path"
    - "Rows where `value_type='null'` return `None` from every typed accessor (does NOT raise `SettingTypeError`)"
    - "Accessor type mismatch (e.g., `get_int` on a `value_type='string'` row) raises `SettingTypeError`"
    - "`settings.get_raw(key)` returns `(value: str | None, value_type: str)` for the Phase 9 admin editor"
    - "Unknown key from any accessor raises `SettingNotFoundError`"
    - "`settings.set_setting(db, key, value, *, by_user_id)` UPDATEs the row in one transaction, pops the key from `_cache`, and emits a structured `admin.app_setting_changed` event with `setting_key`, `old_value`, `new_value`, `value_type`, `user_id` fields"
  artifacts:
    - path: "app/services/settings.py"
      provides: "Typed app_settings reader + module-level cache + write-through invalidation + audit emit"
      contains: "_cache"
      exports: ["SettingNotFoundError", "SettingTypeError", "get_str", "get_int", "get_bool", "get_json", "get_raw", "set_setting", "prewarm_cache", "invalidate"]
      min_lines: 130
  key_links:
    - from: "app/services/settings.py"
      to: "app/models/app_setting.py"
      via: "SQLAlchemy 2.0 select / update on AppSetting"
      pattern: "select\\(AppSetting\\)"
    - from: "app/services/settings.py"
      to: "app/events.py"
      via: "import ADMIN_APP_SETTING_CHANGED"
      pattern: "from app.events import ADMIN_APP_SETTING_CHANGED"
---

<objective>
Land the typed `app_settings` reader (`app/services/settings.py`) — module-level cache + write-through invalidation + structured audit event on writes. Public API per D-05: `get_str`, `get_int`, `get_bool`, `get_json`, `get_raw`, `set_setting`, `prewarm_cache`, `invalidate` (test hook) + two exceptions (`SettingNotFoundError`, `SettingTypeError`).

Purpose: Provides the typed read surface that Phase 7 (AI service: `get_str("ai_provider_default")`, `get_int("ai_primary_max_searches")`, etc.), Phase 8 (scheduler reads timezone + retention settings), and Phase 9 (admin editor: `get_raw` + `set_setting`) all consume. Plan 03-04 also depends on this for the `encryption_key_primary_fingerprint` read/write during rewrap.

Output: One new service module (~130 lines). No DB schema change, no routes, no lifespan wiring (that's Plan 03-05). Reads from the 20 existing `app_settings` rows (19 from Phase 0 + the fingerprint row from Plan 03-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-encryption-settings/03-CONTEXT.md
@.planning/phases/03-encryption-settings/03-RESEARCH.md
@.planning/phases/03-encryption-settings/03-PATTERNS.md
@.planning/phases/03-encryption-settings/03-VALIDATION.md
@app/services/auth.py
@app/services/sessions.py
@app/services/setup.py
@app/models/app_setting.py
@app/db.py
@app/events.py
</context>

<interfaces>
<!-- Plan 03-04 (credentials.py) imports get_str + set_setting + SettingNotFoundError for the encryption_key_primary_fingerprint read/write inside rewrap_if_needed and set_provider_credential. Plan 03-05 (lifespan) calls prewarm_cache. Phase 7+ consumers read via get_str/get_int/get_bool/get_json. -->

From app/services/settings.py (this plan creates):
```
class SettingNotFoundError(KeyError): ...
class SettingTypeError(TypeError): ...

@dataclass(frozen=True, slots=True)
class _CachedSetting:
    value: str | None
    value_type: str  # 'string' | 'int' | 'float' | 'bool' | 'json' | 'null'
    coerced: Any

_cache: dict[str, _CachedSetting]   # module-level, empty until prewarm_cache runs

def prewarm_cache(db: Session) -> None: ...
def get_str(key: str) -> str: ...           # raises SettingTypeError if value_type != 'string'/'null'
def get_int(key: str) -> int: ...
def get_bool(key: str) -> bool: ...
def get_json(key: str) -> Any: ...
def get_raw(key: str) -> tuple[str | None, str]: ...
def set_setting(db: Session, key: str, value: Any, *, by_user_id: int | None) -> None: ...
def invalidate(key: str) -> None: ...        # test-only hook
```

From app/models/app_setting.py (existing, this plan READS via SQLAlchemy 2.0 select/update):
```
class AppSetting(Base):
    key: Mapped[str]           # primary_key
    value: Mapped[str | None]
    value_type: Mapped[str]    # 'string' | 'int' | 'float' | 'bool' | 'json' | 'null'
    description: Mapped[str | None]
    updated_at: Mapped[datetime]
    updated_by_user_id: Mapped[int | None]
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → cache | Sync callers (any phase) and async callers (Phase 7 AI service) read the same `_cache` dict; single-worker invariant (FOUND-04) + GIL atomic-dict-read guarantees consistency. |
| Caller → `set_setting` | Currently no Phase 3 caller mutates `app_settings`; the function is called by Plan 03-04 (rewrap writes fingerprint) and Phase 9 (admin editor). |
| `set_setting` → audit log | Logs `old_value` and `new_value` verbatim. Today no `app_settings` row holds sensitive data; D-08 documents this as a deferred concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-T6 | Tampering / Data integrity | Cache poisoning — a writer's `set_setting` runs while a concurrent reader holds a stale `_cache` entry, returning the pre-write value | mitigate | Single-worker invariant (FOUND-04 / Phase 0 D-13 / single uvicorn worker — reaffirmed in CLAUDE.md) means there is no inter-process race. Write-through invalidation: `_cache.pop(key, None)` runs AFTER `db.commit()` so the next read repopulates from the committed row. Phase 2's `SELECT ... FOR UPDATE` path on `setup_completed` is documented as the explicit exception (CONTEXT `<specifics>`: "Phase 2 raw-SQL call stays raw SQL"). |
| T-03-T8 | Audit log redaction | A future `app_settings` row holds sensitive data and `set_setting` logs the full new_value | mitigate (today) | D-08 logs `old_value` and `new_value` verbatim, valid for the 20 current rows (recommendation_region, max_searches counts, timezone, fingerprint — none secret). The structlog redactor in `app/logging.py` already covers `api_key`/`secret`/`encryption_key`-shaped keys. If a future row holds sensitive data (e.g., webhook secret), a per-key redaction list is the documented next step (CONTEXT.md Deferred). Plan 03-03 emits NO new sensitive field name. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create app/services/settings.py — typed cache + accessors + write-through invalidation + audit emit</name>
  <files>app/services/settings.py</files>
  <read_first>
    - app/services/auth.py — primary analog for service-module shape + custom exception class + module-level singleton + `__all__` discipline (PATTERNS.md classifies as role-match)
    - app/services/sessions.py:118-126 — `select()` shape for SQLAlchemy 2.0 sync reads (PATTERNS.md cites these lines for the read pattern)
    - app/services/setup.py:159-161 — `update()` pattern + transactional commit (PATTERNS.md cites this for set_setting)
    - app/models/app_setting.py — the AppSetting model; READ to confirm column names, types, and the `value_type` value set ('string', 'int', 'float', 'bool', 'json', 'null')
    - app/db.py — `SessionLocal` (sync) is the session factory; this module's callers pass a sync `Session`
    - app/events.py — confirm `ADMIN_APP_SETTING_CHANGED` is importable (added by Plan 03-02 Task 1)
    - .planning/phases/03-encryption-settings/03-PATTERNS.md §"`app/services/settings.py`" — full code sketch including imports, cache dataclass, prewarm, accessors, set_setting, `__all__`
    - .planning/phases/03-encryption-settings/03-RESEARCH.md §"Code Examples → `app/services/settings.py`" lines 706-884 — verified verbatim
    - .planning/phases/03-encryption-settings/03-CONTEXT.md D-05, D-06, D-07, D-08 — public surface, prewarm semantics, sync, write-through+audit
    - .planning/phases/03-encryption-settings/03-VALIDATION.md rows 7-12 — six tests this module must pass
  </read_first>
  <action>
    Create `app/services/settings.py` per RESEARCH.md's verbatim verified code example (lines 706-884) and PATTERNS.md's pattern map. The module structure (in this exact order):

    **1. Module docstring** — single short paragraph: "Typed app_settings reader + write-through cache (D-05 through D-08). Module-level `_cache` populated at lifespan startup via `prewarm_cache()`. Reads are pure cache lookups; writes go through DB then invalidate. Single-worker invariant (FOUND-04) makes the cache consistent across requests. Do NOT call from inside a `SELECT ... FOR UPDATE` transaction — the cache does not compose with row locks; use raw SQL there (CONTEXT.md `<specifics>` — Phase 2 setup_completed read site)."

    **2. Imports** (mirror `app/services/sessions.py:32-40` but sync; PATTERNS.md analog):
    - `from __future__ import annotations`
    - `import json`
    - `from dataclasses import dataclass`
    - `from typing import Any`
    - `import structlog`
    - `from sqlalchemy import select, update`
    - `from sqlalchemy.orm import Session`
    - `from app.events import ADMIN_APP_SETTING_CHANGED`
    - `from app.models.app_setting import AppSetting`

    **3. Module-level `log = structlog.get_logger(__name__)`.**

    **4. Custom exceptions:**
    - `class SettingNotFoundError(KeyError):` — docstring "Raised when a typed accessor receives an unknown key."
    - `class SettingTypeError(TypeError):` — docstring "Raised when an accessor's expected type doesn't match the row's `value_type`."

    **5. Frozen+slots cached-row dataclass** (planner discretion picks frozen dataclass per CONTEXT):
    ```
    @dataclass(frozen=True, slots=True)
    class _CachedSetting:
        value: str | None
        value_type: str  # 'string' | 'int' | 'float' | 'bool' | 'json' | 'null'
        coerced: Any  # pre-coerced Python value (None for value_type='null')
    ```
    Comment: "Per CONTEXT.md `<specifics>`, value_type='null' is the typed-null sentinel; every accessor returns None for that row."

    **6. Module-level `_cache: dict[str, _CachedSetting] = {}`** with a comment: "Single-worker invariant (FOUND-04) means this is consistent across every request. Empty until prewarm_cache() runs in lifespan."

    **7. Private `_coerce(value: str | None, value_type: str) -> Any`** helper. Mapping:
    - `'null'` → None (regardless of value)
    - `value is None` → None (defensive; invalid combination)
    - `'string'` → `value`
    - `'int'` → `int(value)`
    - `'float'` → `float(value)`
    - `'bool'` → `value.lower() == "true"` (Phase 0 stores booleans as the strings `"true"` / `"false"`)
    - `'json'` → `json.loads(value)`
    - default → raise `SettingTypeError(f"Unknown value_type: {value_type!r}")`

    **8. Public `prewarm_cache(db: Session) -> None`** (D-06). Single `select(AppSetting)` then for each row populate `_cache[row.key] = _CachedSetting(...)` with the coerced value. Clear the cache FIRST (`_cache.clear()`) so re-running prewarm during tests resets cleanly. Docstring: "D-06: single `SELECT * FROM app_settings` populates the module-level cache. Idempotent — safe to call multiple times."

    **9. Private `_get(key: str, expected_type: str) -> Any`** helper. Implements the typed-accessor logic:
    - `cached = _cache.get(key)`; if `None`, raise `SettingNotFoundError(key)`.
    - If `cached.value_type == "null"`, return `None`.
    - If `cached.value_type != expected_type`, raise `SettingTypeError(f"Setting {key!r} has value_type={cached.value_type!r}; caller expected {expected_type!r}")`.
    - Else return `cached.coerced`.

    **10. Public `get_str(key: str) -> str | None`** — `return _get(key, "string")`. Same shape for `get_int`, `get_bool`, `get_json` (each calls `_get` with the matching `expected_type`). Docstring per accessor: 1-line + "Raises SettingTypeError if value_type doesn't match; returns None for value_type='null'."

    **11. Public `get_raw(key: str) -> tuple[str | None, str]`** — returns `(cached.value, cached.value_type)` for the Phase 9 admin editor. Raises `SettingNotFoundError` for unknown keys. (Does NOT return `None`-on-null — the admin editor needs the typed-null marker to render the right UI control.)

    **12. Public `set_setting(db: Session, key: str, value: Any, *, by_user_id: int | None) -> None`** (D-08). Implementation:

    Docstring MUST include the sentence: "`set_setting` does NOT change `value_type`; callers needing a `value_type` transition (e.g., the `value_type='null'` → `value_type='string'` first-write of `encryption_key_primary_fingerprint`) must perform a direct UPDATE — see `credentials.rewrap_if_needed` and `credentials.set_provider_credential` for the documented exception."

    - `existing = db.execute(select(AppSetting).where(AppSetting.key == key)).scalar_one()` — raises if key doesn't exist (the caller should check first via `get_raw` if it's a possibility).
    - Capture `old_value = existing.value` and `value_type = existing.value_type`.
    - Coerce input to text for storage:
      - `value is None` → `text_value = None`
      - `value_type == "json"` → `text_value = json.dumps(value)`
      - `value_type == "bool"` → `text_value = "true" if value else "false"`
      - else → `text_value = str(value)`
    - `db.execute(update(AppSetting).where(AppSetting.key == key).values(value=text_value, updated_by_user_id=by_user_id))`
    - `db.commit()`
    - `_cache.pop(key, None)` — re-loaded on next access (write-through invalidate).
    - `log.info(ADMIN_APP_SETTING_CHANGED, setting_key=key, old_value=old_value, new_value=text_value, value_type=value_type, user_id=by_user_id)` — kwargs `setting_key`, `old_value`, `new_value`, `value_type`, `user_id` (NOT `by_user_id`) per Phase 1 D-14 taxonomy alignment. The structlog redactor in `app/logging.py` does NOT redact `setting_key`/`old_value`/`new_value` (no current row holds sensitive data per CONTEXT.md `<deferred>`).

    **13. Public `invalidate(key: str) -> None`** — `_cache.pop(key, None)`. Docstring: "Drop *key* from the cache. Test-only hook for out-of-band SQL edits; production callers use `set_setting` which already invalidates."

    **14. `__all__`** alphabetized: `["SettingNotFoundError", "SettingTypeError", "get_bool", "get_int", "get_json", "get_raw", "get_str", "invalidate", "prewarm_cache", "set_setting"]`.

    **Locked constraints:**
    - DO NOT call `db.commit()` inside `prewarm_cache` — the function is read-only.
    - DO NOT add an async variant. D-07 locks sync for this phase; Phase 7's async AI service calls the read-side accessors directly (cache hit, no I/O).
    - DO NOT include the field `by_user_id` in the structlog emit — the field name in logs MUST be `user_id` per Phase 1 D-14 taxonomy (the function kwarg is `by_user_id`, the emitted field is `user_id`).
    - DO NOT call `_load_one(db, key)` on cache miss in the accessors (sketch in some PATTERNS code is illustrative only). The contract per CONTEXT is: keys are populated at prewarm; cache miss == unknown key == `SettingNotFoundError`. (Plan 03-04's `rewrap_if_needed` handles the pre-prewarm `set_provider_credential` case by catching `SettingNotFoundError` from `get_str`.)
  </action>
  <verify>
    <automated>cd /app &amp;&amp; pytest tests/services/test_settings.py -x 2>/dev/null || python -c "
from app.services import settings as s
import inspect
# Public surface check
required = {'SettingNotFoundError', 'SettingTypeError', 'prewarm_cache', 'get_str', 'get_int', 'get_bool', 'get_json', 'get_raw', 'set_setting', 'invalidate'}
missing = required - set(dir(s))
assert not missing, f'Missing: {missing}'
# Signature contracts
assert 'by_user_id' in inspect.signature(s.set_setting).parameters
assert 'db' in inspect.signature(s.prewarm_cache).parameters
# Cache is a dict
assert isinstance(s._cache, dict)
print('OK public surface + signatures')
"</automated>
  </verify>
  <done>
    `app/services/settings.py` exports the 10 names in `__all__`, exposes the module-level `_cache: dict`, and the public functions have the locked signatures (`prewarm_cache(db)`, `get_X(key)`, `get_raw(key) -> tuple`, `set_setting(db, key, value, *, by_user_id)`, `invalidate(key)`). `ruff check app/services/settings.py` clean. No async path. Phase 7+ callers can `from app.services.settings import get_int` and call cache reads inline.
  </done>
  <threat_refs>T-03-T6, T-03-T8</threat_refs>
  <acceptance_criteria>
    - Validation Map row 7: `prewarm_cache` populates the cache with every `app_settings` row (proved in Plan 03-06).
    - Validation Map row 8: typed accessors coerce values; `value_type='null'` rows return `None` (proved in Plan 03-06).
    - Validation Map row 9: type mismatch raises `SettingTypeError` (proved in Plan 03-06).
    - Validation Map row 10: `set_setting` UPDATEs the row, invalidates cache, next read returns new value (proved in Plan 03-06).
    - Validation Map row 11: `set_setting` emits `ADMIN_APP_SETTING_CHANGED` with `setting_key, old_value, new_value, value_type, user_id` (proved in Plan 03-06).
    - Validation Map row 12: unknown key raises `SettingNotFoundError` (proved in Plan 03-06).
    - The structlog `set_setting` emit uses the kwarg name `user_id` (NOT `by_user_id`) to align with Phase 1 D-14 taxonomy.
    - `set_setting` invalidates the cache AFTER `db.commit()` (verify ordering in source).
    - `_coerce("true", "bool")` → `True`; `_coerce("false", "bool")` → `False`; `_coerce("US", "string")` → `"US"`; `_coerce("5", "int")` → `5`; `_coerce(None, "null")` → `None`.
    - `set_setting` docstring contains the sentence: "`set_setting` does NOT change `value_type`; callers needing a value_type transition (e.g., the `value_type='null'` → `value_type='string'` first-write of `encryption_key_primary_fingerprint`) must perform a direct UPDATE — see `credentials.rewrap_if_needed` for the documented exception." This makes the value_type immutability invariant explicit at the API surface.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `python -c "from app.services import settings; assert callable(settings.prewarm_cache) and callable(settings.set_setting); print('OK')"` exits 0.
- `ruff check app/services/settings.py` is clean.
- A manual `prewarm_cache` against the dev DB followed by `get_str("recommendation_region")` returns `"US"` (the seeded value from Phase 0 D-17).
- `get_int("ai_primary_max_searches")` returns `5` (the seeded value from Phase 0 D-17).
- `set_setting(db, "ai_primary_max_searches", 7, by_user_id=None)` UPDATEs the row, pops cache, next `get_int` returns 7.
</verification>

<success_criteria>
- Module exports the 10 names in `__all__`.
- Sync `Session` parameter on `prewarm_cache` + `set_setting`.
- Single `_coerce` helper covers all 6 value_types.
- Audit emit uses `user_id` (Phase 1 D-14 alignment), not `by_user_id`.
- Cache invalidation runs after `db.commit()` in `set_setting`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-encryption-settings/03-03-SUMMARY.md` documenting:
- Files created: `app/services/settings.py`
- Public surface (10 names) and the exact kwarg of `set_setting`.
- The coercion table (value_type → Python type) for downstream readers.
- Confirmation that the audit emit field is `user_id` (Phase 1 D-14 alignment).
- Decisions implemented: D-05, D-06, D-07, D-08.
</output>
