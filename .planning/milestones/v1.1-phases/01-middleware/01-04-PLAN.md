---
phase: 01-middleware
plan: 04
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - app/migrations/versions/p1_sessions_table.py
  - app/models/session.py
  - app/middleware/session.py
  - app/services/__init__.py
  - app/services/sessions.py
  - app/signing.py
autonomous: true
requirements:
  - AUTH-05
tags:
  - sessions
  - middleware
  - database
  - migration
must_haves:
  truths:
    - "Alembic migration `p1_sessions` creates the `sessions` table with EXACTLY these columns (and no others): `session_id` UUID PRIMARY KEY, `user_id` INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE, `last_seen` TIMESTAMP WITH TIME ZONE NOT NULL, `expires_at` TIMESTAMP WITH TIME ZONE NOT NULL, `created_at` TIMESTAMP WITH TIME ZONE NOT NULL — per D-07. Indexes: PK on session_id, btree on user_id, btree on expires_at. NO columns named ip, user_agent, or device_label. (D-07 schema; D-08 minimal/auth-only footprint)"
    - "`SessionMiddleware` is pure ASGI (defines `async def __call__(self, scope, receive, send)`); NEVER `BaseHTTPMiddleware`"
    - "On a request carrying a valid signed `session_id` cookie, `request.state.user` is populated with the User row and `request.state.session` is populated with the Session row"
    - "On a request with no cookie / invalid signature / unknown session_id / expired session, `request.state.user is None`"
    - "An invalid signed cookie value triggers a `Set-Cookie: session_id=; Max-Age=0; Path=/; HttpOnly; Secure; SameSite=Lax` response header (cookie cleared)"
    - "`last_seen` is refreshed ONLY when `now() - last_seen > 300` seconds (5 minutes), avoiding one UPDATE per request"
    - "The signed cookie uses `itsdangerous.URLSafeSerializer` (NOT `URLSafeTimedSerializer`) with `salt=\"session\"` and `secret_key=settings.APP_SECRET_KEY` per RESEARCH §5 (DB row is the authoritative expiry source)"
    - "Set-Cookie attributes on a newly-minted session: `HttpOnly; Secure; SameSite=Lax; Max-Age=2592000; Path=/` per D-10"
    - "`regenerate_session(db, current_session_id, user_id)` async helper in `app/services/sessions.py` deletes the old row, mints a new UUID4, inserts a new row, and returns the new UUID — Phase 2 wires this into /login, /logout, and admin-toggle (D-09 single-session delete model; Phase 2 wires logout)"
  artifacts:
    - path: "app/migrations/versions/p1_sessions_table.py"
      provides: "Alembic migration creating the sessions table per D-07"
      contains: "sessions"
    - path: "app/models/session.py"
      provides: "SQLAlchemy 2.0 typed Mapped[...] Session model"
      contains: "class Session"
    - path: "app/middleware/session.py"
      provides: "Pure ASGI SessionMiddleware resolving request.state.user from signed cookie"
      contains: "class SessionMiddleware"
      min_lines: 60
    - path: "app/services/sessions.py"
      provides: "regenerate_session helper + session-row helpers reusable across Plan 04 + Phase 2"
      contains: "regenerate_session"
      min_lines: 30
    - path: "app/signing.py"
      provides: "Module-level URLSafeSerializer instance bound to APP_SECRET_KEY with salt='session'"
      contains: "URLSafeSerializer"
  key_links:
    - from: "app/middleware/session.py"
      to: "app/models/session.py:Session"
      via: "SELECT FROM sessions WHERE session_id = ?"
      pattern: "select.*Session"
    - from: "app/middleware/session.py"
      to: "app/signing.py:session_signer"
      via: "signer.loads(cookie_value)"
      pattern: "session_signer"
    - from: "app/services/sessions.py:regenerate_session"
      to: "app/models/session.py:Session"
      via: "delete + insert under one transaction"
      pattern: "delete\\(Session\\)"
---

<objective>
Implement the table-backed session layer that every later phase relies on for `request.state.user`. Builds the `sessions` table (D-07 schema, no additions), the SQLAlchemy model, the signed-cookie serializer, the pure ASGI `SessionMiddleware`, and the `regenerate_session` helper that Phase 2 will wire into `/login` and `/logout`.

Purpose: Satisfies AUTH-05 (custom session middleware backed by `sessions` table; 30-day expiry; refresh on activity) and locks in D-07..D-10. Phase 1 does NOT exercise the helper from any route (Phase 2 owns the auth flow); Phase 1 ships the helper with a unit test so Phase 2 inherits a known-good primitive.

Output: One Alembic migration, one SQLAlchemy model, one middleware, one service module, one signer module. Wave 0 tests `tests/middleware/test_session.py` and `tests/services/test_sessions.py::test_regenerate` flip green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-middleware/01-CONTEXT.md
@.planning/phases/01-middleware/01-RESEARCH.md
@CLAUDE.md
@tests/middleware/test_session.py
@tests/services/test_sessions.py
@tests/conftest.py

<interfaces>
<!-- Plan 04 creates these for Plans 08 (debug/whoami probe), 09 (main.py assembly), and Phase 2 -->

From app/models/session.py:
- class Session(Base):
    __tablename__ = "sessions"
    session_id: Mapped[uuid.UUID]  (primary key)
    user_id: Mapped[int]  (FK users.id, ON DELETE CASCADE)
    last_seen: Mapped[datetime]  (tz-aware)
    expires_at: Mapped[datetime]  (tz-aware, indexed)
    created_at: Mapped[datetime]  (tz-aware)

From app/signing.py:
- session_signer: URLSafeSerializer instance (already constructed)
- def sign_session_id(session_id: uuid.UUID) -> str
- def load_session_id(signed_cookie_value: str) -> uuid.UUID | None  (returns None on BadSignature)

From app/middleware/session.py:
- class SessionMiddleware:
    def __init__(self, app: ASGIApp, *, session_factory: Callable[[], AsyncSession], cookie_name: str = "session_id", refresh_threshold_seconds: int = 300, max_age_seconds: int = 2592000) -> None
    async def __call__(self, scope, receive, send) -> None
- Module constant: REFRESH_THRESHOLD_SECONDS = 300
- Module constant: MAX_AGE_SECONDS = 2592000  # 30 days

From app/services/sessions.py:
- async def regenerate_session(db: AsyncSession, current_session_id: uuid.UUID | None, user_id: int) -> uuid.UUID
- async def create_session(db: AsyncSession, user_id: int) -> uuid.UUID  (used by Phase 2 /login)
- async def delete_session(db: AsyncSession, session_id: uuid.UUID) -> None  (used by Phase 2 /logout)
- def build_session_cookie(signed_value: str, *, max_age: int = 2592000) -> str  (Set-Cookie value)
- def build_session_clear_cookie() -> str  (Set-Cookie value for cookie deletion)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sessions table — Alembic migration + SQLAlchemy model + signing serializer</name>
  <files>app/migrations/versions/p1_sessions_table.py, app/models/session.py, app/signing.py</files>
  <read_first>
    - .planning/phases/01-middleware/01-CONTEXT.md D-07 (sessions schema — exactly these columns, no others)
    - .planning/phases/01-middleware/01-RESEARCH.md §5 "Schema (per D-07, no additions)" — the verified SQLAlchemy 2.0 Mapped[...] shape
    - .planning/phases/01-middleware/01-RESEARCH.md §5 "Cookie signing — URLSafeSerializer vs URLSafeTimedSerializer" — use URLSafeSerializer
    - REQUIREMENTS.md AUTH-05 + AUTH-06 (cookie attributes, signed with APP_SECRET_KEY, 30-day max-age)
    - CLAUDE.md "Stack invariants" (SQLAlchemy 2.0 typed `Mapped[...]`, psycopg 3, Pydantic v2)
    - Existing Phase 0 Alembic env (look for `app/migrations/env.py` and a `Base` declarative class — typically at `app/models/base.py`. If Phase 0 hasn't landed `app/models/base.py` yet, ship a minimal `Base = declarative_base()` import path that Phase 0 can supersede.)
  </read_first>
  <action>
    Locate the existing Alembic scaffold from Phase 0:
    - `app/migrations/env.py` (Phase 0 should already provide this)
    - `app/migrations/versions/` (where versioned migration files live)
    - `app/models/base.py` (where `Base = DeclarativeBase` or similar lives)

    If `app/models/base.py` does NOT yet exist (Phase 0 didn't ship one), create a minimal:
    - `from __future__ import annotations`
    - `from sqlalchemy.orm import DeclarativeBase`
    - `class Base(DeclarativeBase): pass`
    Note in module docstring: "If Phase 0 ships a Base class later, prefer that one and remove this module."

    Create `app/models/session.py`:
    - `from __future__ import annotations`
    - Imports: `import uuid`, `from datetime import datetime`, `from sqlalchemy import ForeignKey, DateTime`, `from sqlalchemy.dialects.postgresql import UUID`, `from sqlalchemy.orm import Mapped, mapped_column`, `from app.models.base import Base`
    - `class Session(Base):` with `__tablename__ = "sessions"` and these Mapped columns EXACTLY (no others):
      - `session_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)`
      - `user_id: Mapped[int] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), nullable=False, index=True)`
      - `last_seen: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)`
      - `expires_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, index=True)`
      - `created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)`
    - NO relationship loaders to User at this phase (avoids circular imports until Phase 2 builds the User model; cross-table joins use explicit `select`s)

    Create `app/migrations/versions/p1_sessions_table.py` (use whatever Alembic revision-ID convention Phase 0 established — typically a short hex string + a slug. Use revision id `p1_sessions` or follow Phase 0's pattern):
    - Standard Alembic migration shape with `revision`, `down_revision`, `branch_labels`, `depends_on` module variables
    - `down_revision` MUST reference the LAST Phase 0 migration's revision id (Phase 0 establishes the chain; this plan's executor reads `app/migrations/versions/` to find it; if Phase 0's chain is not yet on disk, use `down_revision = None` and document in SUMMARY that Plan 09 or a follow-up will fix-up `down_revision` once Phase 0 lands)
    - `def upgrade() -> None`: `op.create_table("sessions", ...)` with the five columns above plus `sa.Index("ix_sessions_user_id", "user_id")` and `sa.Index("ix_sessions_expires_at", "expires_at")`. Use `sa.dialects.postgresql.UUID(as_uuid=True)` for the PK, `sa.DateTime(timezone=True)` for the three timestamps, `sa.ForeignKey("users.id", ondelete="CASCADE")` on `user_id`.
    - `def downgrade() -> None`: `op.drop_table("sessions")`
    - DO NOT add `ip`, `user_agent`, or `device_label` columns (D-07 explicitly excludes them)

    Create `app/signing.py`:
    - `from __future__ import annotations`
    - Imports: `import uuid`, `from itsdangerous import URLSafeSerializer, BadSignature`, `from app.config import settings`
    - Module constant: `session_signer = URLSafeSerializer(secret_key=settings.APP_SECRET_KEY, salt="session")`
    - `def sign_session_id(session_id: uuid.UUID) -> str`: returns `session_signer.dumps(str(session_id))`
    - `def load_session_id(signed_cookie_value: str) -> uuid.UUID | None`:
      - `try: raw = session_signer.loads(signed_cookie_value); return uuid.UUID(raw)`
      - `except (BadSignature, ValueError): return None`

    If `app.config.settings` does not exist yet (Phase 0 didn't ship `app/config.py`), import lazily inside the function bodies via `def _get_signer(): from app.config import settings; ...` — but module-level construction is preferred. Worst case: ship a TODO comment and `pytest.skip` the dependent test until Phase 0 catches up.
  </action>
  <verify>
    <automated>cd C:\Claude\Coffee-Snobbery && python -c "from app.models.session import Session; from app.signing import session_signer, sign_session_id, load_session_id; import uuid; u = uuid.uuid4(); signed = sign_session_id(u); assert load_session_id(signed) == u; assert load_session_id('tampered') is None; assert Session.__tablename__ == 'sessions'; cols = {c.name for c in Session.__table__.columns}; assert cols == {'session_id', 'user_id', 'last_seen', 'expires_at', 'created_at'}, cols"</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/session.py` defines class `Session` with EXACTLY these column names: `session_id`, `user_id`, `last_seen`, `expires_at`, `created_at` (no others — verified via `{c.name for c in Session.__table__.columns}` equality assertion)
    - `session_id` is the primary key with `UUID(as_uuid=True)` type and `default=uuid.uuid4`
    - `user_id` is a `ForeignKey("users.id", ondelete="CASCADE")`, NOT NULL, indexed
    - `expires_at` is indexed (so cleanup-by-expiry queries scale)
    - `app/migrations/versions/p1_sessions_table.py` defines `upgrade()` creating the `sessions` table and `downgrade()` dropping it; no `ip`/`user_agent`/`device_label` columns
    - `app/signing.py` exports `session_signer`, `sign_session_id`, `load_session_id`
    - Roundtrip test passes: `load_session_id(sign_session_id(uuid))` returns the original UUID; tampered cookie returns `None`
    - `ruff check app/models/session.py app/signing.py app/migrations/versions/p1_sessions_table.py` exits 0
  </acceptance_criteria>
  <done>Migration + model + signer ship. The schema is locked at D-07's five columns; the signer wraps `URLSafeSerializer` with `salt="session"`; the migration is sequenced behind Phase 0's last migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app/services/sessions.py — create / regenerate / delete + cookie builders</name>
  <files>app/services/__init__.py, app/services/sessions.py</files>
  <read_first>
    - .planning/phases/01-middleware/01-CONTEXT.md D-08 (session row exists only for authenticated requests), D-09 (logout deletes single row), D-10 (regeneration on login/logout/admin-toggle + cookie attributes)
    - .planning/phases/01-middleware/01-RESEARCH.md §5 "Session ID regeneration" (regenerate_session async function signature + DELETE-then-INSERT semantics)
    - tests/services/test_sessions.py (executable spec for `test_regenerate`)
    - app/models/session.py (Task 1 just landed)
    - app/signing.py (Task 1 just landed)
  </read_first>
  <behavior>
    - `create_session(db, user_id)` returns a fresh UUID; inserts one row with `last_seen = now()`, `expires_at = now() + 30 days`, `created_at = now()`; commits
    - `regenerate_session(db, current_session_id, user_id)` deletes the row whose pk = `current_session_id` if it exists, then calls `create_session` (or inlines the equivalent); commits ONCE so the swap is atomic; returns the new UUID
    - `delete_session(db, session_id)` deletes the matching row; commits
    - `build_session_cookie(signed_value, *, max_age=2592000)` returns the EXACT `Set-Cookie` header value `f"session_id={signed_value}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age={max_age}"` (the literal Set-Cookie value WITHOUT the `Set-Cookie:` prefix — ASGI middleware adds the prefix when injecting the header tuple)
    - `build_session_clear_cookie()` returns `"session_id=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0"` (cookie clearing)
  </behavior>
  <action>
    Create empty `app/services/__init__.py` (package marker).

    Create `app/services/sessions.py`:
    - `from __future__ import annotations`
    - Imports: `import uuid`, `from datetime import datetime, timedelta, timezone`, `from sqlalchemy import delete, select`, `from sqlalchemy.ext.asyncio import AsyncSession`, `from app.models.session import Session`
    - Module constant: `SESSION_LIFETIME_DAYS = 30`
    - `async def create_session(db: AsyncSession, user_id: int) -> uuid.UUID`:
      - `now = datetime.now(timezone.utc)`
      - `expires = now + timedelta(days=SESSION_LIFETIME_DAYS)`
      - `new_id = uuid.uuid4()`
      - `db.add(Session(session_id=new_id, user_id=user_id, last_seen=now, expires_at=expires, created_at=now))`
      - `await db.commit()`
      - `return new_id`
    - `async def regenerate_session(db: AsyncSession, current_session_id: uuid.UUID | None, user_id: int) -> uuid.UUID`:
      - If `current_session_id is not None`: `await db.execute(delete(Session).where(Session.session_id == current_session_id))`
      - Then inline the same logic as `create_session` (do NOT call `create_session` to avoid two commits; commit once at the end so old-delete + new-insert are atomic)
      - Return the new UUID
    - `async def delete_session(db: AsyncSession, session_id: uuid.UUID) -> None`:
      - `await db.execute(delete(Session).where(Session.session_id == session_id))`
      - `await db.commit()`
    - `async def get_session_by_id(db: AsyncSession, session_id: uuid.UUID) -> Session | None`:
      - `result = await db.execute(select(Session).where(Session.session_id == session_id))`
      - `return result.scalar_one_or_none()`
    - `async def refresh_last_seen(db: AsyncSession, session_id: uuid.UUID) -> None`:
      - Issues a single UPDATE setting `last_seen = now()` AND `expires_at = now() + 30d` for the given session_id
      - Commits

    Cookie builders (pure functions, no DB):
    - `def build_session_cookie(signed_value: str, *, max_age: int = 2592000) -> str`: returns literal `f"session_id={signed_value}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age={max_age}"`
    - `def build_session_clear_cookie() -> str`: returns literal `"session_id=; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=0"`

    Use `Mapped[...]` semantics correctly — no legacy `Query` API; only `select()`, `delete()`, `update()` constructs (CLAUDE.md invariant).

    Type-hint every public function signature; `from __future__ import annotations` enables forward refs.
  </action>
  <verify>
    <automated>cd C:\Claude\Coffee-Snobbery && python -c "from app.services.sessions import create_session, regenerate_session, delete_session, get_session_by_id, refresh_last_seen, build_session_cookie, build_session_clear_cookie; v = build_session_cookie('signed_val'); assert 'session_id=signed_val' in v; assert 'HttpOnly' in v; assert 'Secure' in v; assert 'SameSite=Lax' in v; assert 'Max-Age=2592000' in v; assert 'Path=/' in v; c = build_session_clear_cookie(); assert 'Max-Age=0' in c"</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/sessions.py` exports `create_session`, `regenerate_session`, `delete_session`, `get_session_by_id`, `refresh_last_seen`, `build_session_cookie`, `build_session_clear_cookie`
    - `build_session_cookie("X")` returns a string containing all of: `session_id=X`, `Path=/`, `HttpOnly`, `Secure`, `SameSite=Lax`, `Max-Age=2592000`
    - `build_session_clear_cookie()` returns a string containing `session_id=` and `Max-Age=0` (cookie deletion)
    - `regenerate_session` issues exactly one COMMIT (atomic swap — verified by reading source: `grep -c "await db.commit" app/services/sessions.py` shows regenerate_session has only ONE commit call inside it)
    - All four async helpers use SQLAlchemy 2.0 `select()` / `delete()` constructs (no `.query()` legacy API): `grep -c "\.query\(" app/services/sessions.py` == 0
    - `pytest tests/services/test_sessions.py::test_regenerate -x` exits 0 (assuming the test fixture provides an `AsyncSession` and a User row to FK-reference; if Phase 0 doesn't yet ship the User model, the test will skip — that's acceptable; the helper code is correct independent of fixture status)
    - `ruff check app/services/__init__.py app/services/sessions.py` exits 0
  </acceptance_criteria>
  <done>Session helpers ship with the regenerate-as-atomic-swap semantics, cookie builders match D-10 attributes exactly, and Phase 2 inherits a known-good primitive for /login/logout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: app/middleware/session.py — pure ASGI SessionMiddleware</name>
  <files>app/middleware/session.py, app/middleware/__init__.py</files>
  <read_first>
    - .planning/phases/01-middleware/01-RESEARCH.md §5 (Middleware request flow + Write throttling + Pure ASGI middleware shape — the canonical implementation)
    - .planning/phases/01-middleware/01-CONTEXT.md D-07, D-08, D-09, D-10 (session row only on authenticated requests; logout deletes; cookie attributes)
    - tests/middleware/test_session.py (executable spec)
    - app/models/session.py (Task 1)
    - app/signing.py (Task 1)
    - app/services/sessions.py (Task 2)
  </read_first>
  <behavior>
    - Pure ASGI, `__init__(self, app, *, session_factory, cookie_name="session_id", refresh_threshold_seconds=300, max_age_seconds=2592000)`
    - On HTTP scope:
      1. Parse cookies from request headers (do NOT use Request.cookies — work at the ASGI scope level via the `cookie` header value)
      2. Extract `session_id` cookie value if present
      3. If no cookie: set `scope["state"]["user"] = None` and `scope["state"]["session"] = None`; pass through
      4. If cookie present: call `load_session_id(value)` → on `None`, set state to None AND queue a clear-cookie response header; pass through
      5. Otherwise lookup via `get_session_by_id(db, session_id)`; on no row OR expired (`expires_at < now()`), set state None AND delete the row (if expired) AND queue clear-cookie
      6. Valid row → set `scope["state"]["session"] = session_row`; do a SECOND query to fetch the User row by `session_row.user_id` (Phase 2 owns the User model; for Phase 1, fall back to a minimal `{"user_id": session_row.user_id}` dict or skip the lookup with a TODO marker — the only consumer in Phase 1 is `/debug/proxy` which doesn't read `request.state.user`, so a stub here is acceptable)
      7. If valid row AND `now() - session_row.last_seen > 300 seconds`: call `refresh_last_seen(db, session_id)` (write-throttled per RESEARCH §5)
    - On the response path, if a clear-cookie was queued, inject a `Set-Cookie: session_id=; Max-Age=0; ...` header via the send_wrapper pattern from Plan 02
    - Non-HTTP scopes pass through unchanged
  </behavior>
  <action>
    Create `app/middleware/session.py`:
    - `from __future__ import annotations`
    - Imports: `from datetime import datetime, timezone`, `from typing import Callable`, `from starlette.types import ASGIApp, Receive, Scope, Send`, `from sqlalchemy.ext.asyncio import AsyncSession`, `from app.models.session import Session`, `from app.services.sessions import get_session_by_id, refresh_last_seen, delete_session, build_session_clear_cookie`, `from app.signing import load_session_id`
    - Module constants: `REFRESH_THRESHOLD_SECONDS = 300`, `MAX_AGE_SECONDS = 2592000`, `COOKIE_NAME = "session_id"`
    - Class `SessionMiddleware`:
      - `__init__(self, app: ASGIApp, *, session_factory: Callable[[], AsyncSession], cookie_name: str = COOKIE_NAME, refresh_threshold_seconds: int = REFRESH_THRESHOLD_SECONDS, max_age_seconds: int = MAX_AGE_SECONDS) -> None`
      - `async def __call__(self, scope, receive, send) -> None`:
        - Non-HTTP → pass through
        - `scope.setdefault("state", {})`
        - Parse the `cookie` header to a dict via a small helper `_parse_cookies(headers)`:
          - Walk `scope.get("headers", [])`; collect the value bytes of the first `b"cookie"` entry; decode as latin-1; split on `; ` and `=` to build a dict
        - `signed_value = cookies.get(self.cookie_name)`
        - Initialize `clear_cookie = False`
        - If `signed_value is None`: `scope["state"]["user"] = None; scope["state"]["session"] = None`
        - Else: `session_id = load_session_id(signed_value)`; if `None`: `clear_cookie = True; scope["state"]["user"] = scope["state"]["session"] = None`
        - Else: open `async with self.session_factory() as db:` (or use the factory's contract)
          - `session_row = await get_session_by_id(db, session_id)`
          - If `session_row is None`: `clear_cookie = True; scope["state"]["user"] = scope["state"]["session"] = None`
          - Elif `session_row.expires_at < datetime.now(timezone.utc)`: `await delete_session(db, session_id); clear_cookie = True; scope["state"]["user"] = scope["state"]["session"] = None`
          - Else:
            - `scope["state"]["session"] = session_row`
            - `scope["state"]["user"] = {"user_id": session_row.user_id}`  # TODO Phase 2: replace with full User model lookup once User model lands
            - If `(datetime.now(timezone.utc) - session_row.last_seen).total_seconds() > self.refresh_threshold_seconds`:
              - `await refresh_last_seen(db, session_id)`
        - Define `send_wrapper(message)`:
          - If `message["type"] == "http.response.start"` AND `clear_cookie`:
            - Append `(b"set-cookie", build_session_clear_cookie().encode("ascii"))` to headers
          - `await send(message)`
        - `await self.app(scope, receive, send_wrapper)`

    Add to `app/middleware/__init__.py`:
    - `from app.middleware.session import SessionMiddleware`
    - Append `"SessionMiddleware"` to `__all__`

    Document via module docstring: "Pure ASGI session middleware. Reads signed `session_id` cookie; resolves `request.state.user` (stub dict for Phase 1; Phase 2 replaces with User row lookup). Write-throttles `last_seen` updates to once per 5 minutes per session. Phase 1 ships the helper + middleware shape; Phase 2 wires it into /login + /logout via the regenerate_session helper."

    Do NOT use `BaseHTTPMiddleware` (pitfall 13.1). Do NOT use Starlette's `Request` wrapper inside the middleware — work with raw scope. The cookie parser is ~10 LOC and avoids the import of `http.cookies.SimpleCookie` which can be brittle on malformed input.

    If `app.session_factory` (or equivalent — Phase 0's async DB factory) doesn't yet exist, document in the `__init__` docstring that the middleware accepts ANY callable returning an async-context-manager-compatible AsyncSession. Plan 09's main.py wiring step will pass the actual factory.
  </action>
  <verify>
    <automated>cd C:\Claude\Coffee-Snobbery && python -c "from app.middleware.session import SessionMiddleware, REFRESH_THRESHOLD_SECONDS, MAX_AGE_SECONDS, COOKIE_NAME; import inspect; src = inspect.getsource(SessionMiddleware); assert 'BaseHTTPMiddleware' not in src; assert 'load_session_id' in src; assert 'refresh_last_seen' in src; assert REFRESH_THRESHOLD_SECONDS == 300; assert MAX_AGE_SECONDS == 2592000; assert COOKIE_NAME == 'session_id'" && python -m pytest tests/middleware/test_session.py --co -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/middleware/session.py` defines class `SessionMiddleware` and module constants `REFRESH_THRESHOLD_SECONDS = 300`, `MAX_AGE_SECONDS = 2592000`, `COOKIE_NAME = "session_id"`
    - `SessionMiddleware` does NOT inherit from `BaseHTTPMiddleware` (`grep -c BaseHTTPMiddleware app/middleware/session.py` == 0)
    - Constructor signature accepts `session_factory` as a keyword-only argument (so Plan 09 can pass the Phase 0 async DB session factory)
    - The middleware reads the `cookie` header from `scope["headers"]` directly (not via `Request.cookies`): `grep -c "Request.cookies\|request.cookies" app/middleware/session.py` == 0
    - The middleware refreshes `last_seen` ONLY when `(now - last_seen).total_seconds() > refresh_threshold_seconds`: source contains the threshold comparison
    - On invalid signature or unknown session_id, the response carries a `Set-Cookie: session_id=; Max-Age=0; ...` header (via the `clear_cookie` flag → `send_wrapper`)
    - `app/middleware/__init__.py` re-exports `SessionMiddleware` and includes it in `__all__`
    - `pytest tests/middleware/test_session.py --co -q` collects without error (tests may skip until Plan 09 mounts the middleware on an app instance — acceptable)
    - `ruff check app/middleware/session.py` exits 0
  </acceptance_criteria>
  <done>SessionMiddleware ships as pure ASGI with cookie parsing, signature verification, expiry check, and write-throttled `last_seen` refresh. Wave 0 session tests are ready to flip green once Plan 09 wires the middleware into `app/main.py` with the real session_factory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → app | A client may present any cookie value, including ones never minted by this server (replay, fuzzing, fixation attempts) |
| client → DB | A successful session lookup grants `request.state.user` access to every downstream route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Forged `session_id` cookie | mitigate | `URLSafeSerializer` signs with `APP_SECRET_KEY` + `salt="session"`; `load_session_id` returns `None` on `BadSignature` (ASVS V3.2.1 — session token entropy / integrity) |
| T-04-02 | Spoofing | Session fixation (attacker pre-sets a `session_id` cookie before victim authenticates) | mitigate | `regenerate_session` helper deletes the old row and mints a fresh UUID — Phase 2 wires this into `/login` per D-10 (ASVS V3.2.3) |
| T-04-03 | Information Disclosure | `session_id` cookie readable by JavaScript (XSS leak) | mitigate | Cookie attributes `HttpOnly; Secure; SameSite=Lax` on every Set-Cookie (D-10) — `HttpOnly` blocks `document.cookie` reads; `Secure` blocks HTTP transmission; `SameSite=Lax` blocks cross-site CSRF in most browsers (ASVS V3.4.1, V3.4.2, V3.4.3) |
| T-04-04 | Tampering | Replay of expired session via stale cookie | mitigate | `expires_at < now()` check deletes the row and clears the cookie; DB-side expiry is authoritative (RESEARCH §5 — two clocks avoided by using URLSafeSerializer not URLSafeTimedSerializer) |
| T-04-05 | Information Disclosure | Storing `ip` or `user_agent` on session rows would let an attacker correlate sessions with users via DB compromise | accept | D-07 explicitly excludes `ip`, `user_agent`, `device_label` — minimal storage footprint is the mitigation by design; consequence: no "sign out everywhere" UI (D-09) |
| T-04-06 | Denial of Service | Naive implementation issues one UPDATE per request (HTMX-staggered home page = 6+ writes/page) | mitigate | `refresh_last_seen` only fires when `now - last_seen > 300s`; ~98% write reduction during active sessions per RESEARCH §5 |
| T-04-07 | Tampering | `BaseHTTPMiddleware` adoption by future contributor would break contextvars + lose `request_id` correlation on session-related log lines | mitigate | Module docstring + `app/middleware/__init__.py` warn against BaseHTTPMiddleware; pitfall 13.1 captured in ADR (Plan 10) |
| T-04-08 | Tampering | Cookie parser misinterprets malformed `Cookie` header and grants access | mitigate | Custom parser is strict (split on `; ` then `=`, ignore empties); never delegates to `http.cookies.SimpleCookie` which has historical edge-case bugs; on any parse error, no `signed_value` is extracted → request is treated as unauthenticated |
</threat_model>

<verification>
1. `python -c "from app.middleware.session import SessionMiddleware; from app.services.sessions import regenerate_session, build_session_cookie, build_session_clear_cookie; from app.signing import sign_session_id, load_session_id; from app.models.session import Session"` exits 0
2. `cd C:\Claude\Coffee-Snobbery && python -m pytest tests/middleware/test_session.py tests/services/test_sessions.py --co -q` exits 0 (tests collect)
3. `ruff check app/middleware/session.py app/services/sessions.py app/services/__init__.py app/signing.py app/models/session.py app/migrations/versions/p1_sessions_table.py` exits 0
4. Source grep: `BaseHTTPMiddleware` does not appear in `app/middleware/session.py`
5. Source grep: `app/middleware/session.py` parses cookies via the raw header path (`scope["headers"]`), NOT via `Request.cookies`
6. Column inventory: `python -c "from app.models.session import Session; print({c.name for c in Session.__table__.columns})"` returns exactly `{"session_id", "user_id", "last_seen", "expires_at", "created_at"}` — no `ip`, `user_agent`, or `device_label`
</verification>

<success_criteria>
- `sessions` table migration lands with EXACTLY the D-07 columns (no additions)
- `Session` SQLAlchemy model uses typed `Mapped[...]` columns and `DeclarativeBase` parent
- `SessionMiddleware` is pure ASGI; cookie parser is custom; `last_seen` write-throttled to 5-min granularity
- `regenerate_session` performs an atomic delete+insert with a single commit
- Cookie attributes match D-10 exactly: `HttpOnly; Secure; SameSite=Lax; Max-Age=2592000; Path=/`
- Wave 0 session tests collect cleanly; will flip to green once Plan 09 wires `session_factory` into the middleware constructor
- All new files lint-clean under ruff
</success_criteria>

<output>
After completion, create `.planning/phases/01-middleware/01-04-SUMMARY.md` documenting:
- Final schema (column names + types + constraints) — for Phase 2 to reference when building User-Session joins
- Whether `app/models/base.py` was created by this plan (if Phase 0 did not yet ship it)
- The `down_revision` value chosen in the Alembic migration (and any cleanup needed once Phase 0 lands)
- TODO list for Phase 2: replace the stub `request.state.user = {"user_id": ...}` dict with a real User row lookup
- Status of Wave 0 session + service tests (collect/skip/pass/fail)
</output>
