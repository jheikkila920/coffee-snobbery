---
phase: quick-260519-ql9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/roasters.py
  - app/services/flavor_notes.py
  - app/routers/roasters.py
  - app/routers/flavor_notes.py
  - tests/phase_04/test_routers_roasters.py
  - tests/phase_04/test_routers_flavor_notes.py
  - Makefile
  - app/csrf.py
autonomous: true
requirements: [CODEX-BUCKET-A]
must_haves:
  truths:
    - "Submitting a duplicate roaster name returns a friendly inline 'Name already exists.' error, not HTTP 500"
    - "Submitting a duplicate flavor-note name returns the same friendly inline error, not HTTP 500"
    - "Renaming a roaster or flavor note onto an existing name returns the friendly error, not HTTP 500"
    - "make smoke asserts the live Alembic head and does not break when a new migration lands"
    - "app/csrf.py CSRF_SENSITIVE_COOKIES docstring describes the actual enforcement scope (authenticated sessions only)"
  artifacts:
    - path: "app/services/roasters.py"
      provides: "IntegrityError handling on create + update with rollback and a duplicate-name signal"
    - path: "app/services/flavor_notes.py"
      provides: "IntegrityError handling on create + update with rollback and a duplicate-name signal"
    - path: "Makefile"
      provides: "smoke target asserting the live migration head"
    - path: "app/csrf.py"
      provides: "accurate CSRF enforcement-scope docstring"
  key_links:
    - from: "app/routers/roasters.py"
      to: "errors_by_field re-render path"
      via: "DuplicateNameError caught and mapped to {'name': 'Name already exists.'} via _normalize_errors at status 200"
      pattern: "DuplicateName"
    - from: "app/routers/flavor_notes.py"
      to: "errors_by_field re-render path"
      via: "DuplicateNameError caught and mapped to {'name': 'Name already exists.'} via _normalize_errors at status 200"
      pattern: "DuplicateName"
---

<objective>
Fix three independent, verified "bucket-A" findings from a Codex code review of the shipped Phase 0-4 code. Surgical fixes only — no refactors, no scope creep.

Purpose: Eliminate a real user-facing 500 (duplicate catalog names), stop a stale smoke-test assertion from silently rotting, and correct a factually wrong CSRF docstring so the next reader does not trust a false security claim.

Output: Three atomic commits — `fix:` (duplicate-name handling + test), `chore:` (Makefile smoke head assertion), `docs:` (CSRF docstring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Existing seams the executor must MATCH, not reinvent. Extracted from the codebase. -->

app/services/form_validation.py — the only validation-error pivot helper:
```python
def errors_by_field(exc: ValidationError) -> dict[str, str]:
    # pivots a pydantic ValidationError into {field_name: message}
    # falls back to "_form" sentinel when loc has no usable leaf
```

app/routers/roasters.py — the EXISTING error re-render path both POST handlers use:
```python
_FORM_FIELDS = {"name", "location", "website", "notes"}
def _normalize_errors(errors: dict[str, str]) -> dict[str, str]: ...
# On pydantic ValidationError, create_roaster / update_roaster_handler do:
return templates.TemplateResponse(
    request=request,
    name="fragments/roaster_form.html",
    context={"values": raw, "errors": _normalize_errors(errors_by_field(exc)),
             "mode": ..., "roaster_id": ...},  # roaster_id only on update
    status_code=200,
)
```

app/routers/flavor_notes.py — identical shape, fragment "fragments/flavor_note_form.html",
context also carries "categories": FLAVOR_NOTE_CATEGORIES, and "flavor_note_id" on the update path.
_FORM_FIELDS = {"name", "category"}.

app/services/roasters.py — write functions that need IntegrityError handling:
```python
def create_roaster(db, *, name, location, website, notes, by_user_id) -> Roaster
def update_roaster(db, *, roaster_id, name, location, website, notes, by_user_id) -> Roaster
```
create uses db.add/flush/commit; update uses core update().values(...) then commit. Both raise
sqlalchemy.exc.IntegrityError on the UNIQUE CITEXT name collision.

app/services/flavor_notes.py — parallel:
```python
def create_flavor_note(db, *, name, category, by_user_id) -> FlavorNote
def update_flavor_note(db, *, flavor_note_id, name, category, by_user_id) -> FlavorNote
```

app/models/roaster.py / app/models/flavor_note.py — both have:
```python
name: Mapped[str] = mapped_column(CITEXT(), nullable=False, unique=True)
```
This UNIQUE CITEXT is what raises IntegrityError on duplicate (and case-variant) names.

Test conventions (tests/phase_04/test_routers_roasters.py + conftest.py):
- authed_client fixture + _prime_csrf(authed_client) before any POST
- _require_postgres() and _require_p4_migration_applied() guards at the top of DB tests
- _seed_roaster(name=...) helper inserts via the service; clean_roasters fixture wipes rows
- Tests assert response status + body substrings ("text-red-700" marks the error styling)
</interfaces>

<makefile>
Current Makefile smoke target (lines 86-94) hardcodes the stale head:
```make
smoke:
	docker compose down -v
	docker compose up -d --build
	@echo "Waiting ~30s for DB healthcheck + migrations to settle..."
	sleep 30
	curl -fsS http://127.0.0.1:8080/healthz
	@echo ""
	docker compose exec coffee-snobbery alembic current | grep -E '^0001_initial \(head\)'
	@echo "smoke: OK"
```
The live head is now `p4_shared_catalog` (app/migrations/versions/p4_shared_catalog.py,
down_revision = "p3_api_credentials", and nothing points to it as a down_revision → it is head).
</makefile>

<csrf_docstring>
app/csrf.py lines 46-50 (the wrong claim — "still gets checked because the double-submit
pattern remains intact" is FALSE):
```python
#: Cookies whose presence triggers CSRF enforcement. The session cookie is the
#: only one listed: an unauthenticated POST that nevertheless carries the
#: ``csrftoken`` cookie (always present after the first safe GET) still gets
#: checked because the double-submit pattern remains intact.
CSRF_SENSITIVE_COOKIES: set[str] = {"session_id"}
```
NOTE: app/static/js/htmx-listeners.js (lines 30-33) ALREADY states the behavior correctly
("an unauthenticated request without a CSRF token is not enforced") — verify it, do not change it
unless it actually repeats the wrong claim.
</csrf_docstring>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map duplicate-name IntegrityError to a friendly inline form error (roasters + flavor notes, create + update)</name>
  <files>app/services/roasters.py, app/services/flavor_notes.py, app/routers/roasters.py, app/routers/flavor_notes.py, tests/phase_04/test_routers_roasters.py, tests/phase_04/test_routers_flavor_notes.py</files>
  <behavior>
    - POST /roasters with a name that already exists (any case — CITEXT) → HTTP 200, roaster_form.html re-render, body contains the "text-red-700" error styling and "Name already exists." (NOT a 500).
    - POST /roasters/{id} renaming onto another existing roaster's name → same friendly 200 re-render with the name error.
    - POST /flavor-notes with a duplicate name → HTTP 200, flavor_note_form.html re-render with "Name already exists." (NOT a 500). Re-render must still pass the categories context so the <select> renders.
    - POST /flavor-notes/{id} renaming onto an existing name → same friendly 200 re-render.
    - On the duplicate path the service must have rolled back (no partial/poisoned session); a subsequent valid create in the same session still succeeds.
    - Existing happy-path and blank-name tests continue to pass unchanged.
  </behavior>
  <action>
Pick the smallest seam that matches the codebase's existing service-raises / router-catches convention used for ValidationError. Implement a typed sentinel exception the router maps onto the EXISTING errors_by_field re-render path — do NOT invent a new error surface or template.

Service layer (app/services/roasters.py and app/services/flavor_notes.py):
- Define one shared typed exception. Add it to app/services/form_validation.py (the existing shared catalog helper module) as `class DuplicateNameError(Exception)` and export it in that module's `__all__`. Reason: form_validation.py is already the shared seam both catalog routers import from, so the routers can `from app.services.form_validation import DuplicateNameError, errors_by_field` without a new import source. Keep type hints + `from __future__ import annotations` per CLAUDE.md.
- In create_roaster and create_flavor_note: wrap the db.add/flush/commit in try/except sqlalchemy.exc.IntegrityError. On IntegrityError call db.rollback() then `raise DuplicateNameError from exc`. Do not log the audit success event on the failure path (the existing log.info call already sits after commit, so it is naturally skipped — confirm it stays after).
- In update_roaster and update_flavor_note: wrap the core update().values(...) + commit in the same try/except IntegrityError → db.rollback() → `raise DuplicateNameError from exc`. The re-fetch + log.info must stay after the commit so they are skipped on the failure path.
- Import IntegrityError as `from sqlalchemy.exc import IntegrityError` (top-of-file import, alphabetized with the existing sqlalchemy imports).
- Do NOT broaden the catch to a generic Exception — only IntegrityError. Other DB errors must keep surfacing (a generic 500 is correct for unexpected failures).

Router layer (app/routers/roasters.py and app/routers/flavor_notes.py):
- Import DuplicateNameError alongside errors_by_field.
- Wrap the service create/update calls (the ones AFTER the ValidationError block, where `roasters_service.create_roaster(...)` etc. are invoked) in try/except DuplicateNameError. On catch, re-render the SAME form fragment with status_code=200 and the SAME context shape the ValidationError branch uses, but with `errors=_normalize_errors({"name": "Name already exists."})` and `values=raw`. Preserve mode (create vs edit vs modal) and the id/categories context keys exactly as the ValidationError branch does for that handler.
- For roasters create: as_modal path — keep `mode="modal" if as_modal else "create"`. For roasters update: include `roaster_id`, `mode="edit"`. For flavor notes: include `categories=FLAVOR_NOTE_CATEGORIES` in every re-render, plus `flavor_note_id`/`mode="edit"` on update.
- This means the four handlers (create_roaster, update_roaster_handler, create_flavor_note, update_flavor_note_handler) each gain a small try/except around the existing service call. Smallest possible diff — do not restructure the handlers.

Tests (match tests/phase_04 conventions — _require_postgres + _require_p4_migration_applied guards, _prime_csrf, clean fixtures, assert status + "text-red-700" + "Name already exists." substrings):
- tests/phase_04/test_routers_roasters.py: add `test_create_roaster_duplicate_name_returns_friendly_error` (seed "Onyx", POST name="Onyx" → 200 + error body, assert NOT 500) and `test_update_roaster_duplicate_name_returns_friendly_error` (seed two roasters, rename one onto the other → 200 + error). Use the existing _seed_roaster helper and _prime_csrf.
- tests/phase_04/test_routers_flavor_notes.py: add the parallel two tests. Read that file first to reuse its existing seed helper / fixture names (it mirrors the roasters test file; match whatever it actually defines — do not assume a helper name).
- Tests assert the friendly outcome AND assert resp.status_code == 200 (a regression that re-raises the 500 would fail here).

NOTE on execution: pytest is NOT in the production image (CLAUDE.md). You cannot run these tests in this environment. Write them to match existing conventions, run ruff format + ruff check on every touched .py file, and state explicitly in the SUMMARY that the new tests were authored but executed only via the documented container path (docker compose cp tests/ + pip install --user pytest + python -m pytest) which must be run on a host with the stack up.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/phase_04/test_routers_roasters.py tests/phase_04/test_routers_flavor_notes.py -k duplicate</automated>
  </verify>
  <done>Both catalog services catch IntegrityError, rollback, and raise DuplicateNameError; both routers map it to a 200 'Name already exists.' inline error on the name field via the existing _normalize_errors/errors_by_field re-render path for BOTH create and update; four new tests authored matching phase_04 conventions; ruff format + ruff check clean on all touched files. Commit: `fix: return friendly inline error on duplicate catalog name instead of 500`.</done>
</task>

<task type="auto">
  <name>Task 2: Make the make smoke migration-head assertion query the live head</name>
  <files>Makefile</files>
  <action>
The smoke target asserts `alembic current | grep -E '^0001_initial \(head\)'` but the live head is now `p4_shared_catalog`. Fix so it cannot go stale on the next migration.

Replace the hardcoded grep with a dynamic assertion that compares `alembic current` against `alembic heads` inside the container. Concretely, in the smoke recipe replace the single `alembic current | grep ...` line with a step that:
- runs `alembic heads` to get the live head revision id(s),
- runs `alembic current` to get the applied revision,
- asserts the applied revision IS the head (i.e. the running DB is fully migrated), failing non-zero otherwise.

Implementation approach (keep it consistent with the file's existing `docker compose exec coffee-snobbery ...` style and `set -e`/`&&` chaining):
- Prefer the simplest robust form: assert `alembic current` reports the `(head)` marker. `alembic current` appends ` (head)` to the revision line only when the applied revision equals the live head, so a marker-based grep that does NOT hardcode the revision id is both dynamic and self-updating:
  `docker compose exec coffee-snobbery alembic current | grep -E '\(head\)'`
  This stays green across future migrations because it never names the revision — it only asserts "DB is at head".
- Add a one-line comment above it explaining the assertion checks the DB is migrated to the live head (revision-id-agnostic, so it does not need bumping per migration), replacing the now-removed comment paragraph about `^0001_initial`.
- Keep the surrounding `curl /healthz`, `@echo`, and `smoke: OK` lines intact. Do not touch any other target.

Do NOT hardcode `p4_shared_catalog`. The whole point is that it never goes stale again.
  </action>
  <verify>
    <automated>grep -nE 'alembic current' Makefile | grep -v '0001_initial' && ! grep -qE '0001_initial' Makefile && echo MARKER_OK</automated>
  </verify>
  <done>The smoke target asserts the DB is at the live Alembic head via a revision-id-agnostic `(head)` marker check (no hardcoded `0001_initial`, no hardcoded `p4_shared_catalog`); a comment explains it does not need bumping per migration; all other targets unchanged. Commit: `chore: assert live alembic head in make smoke instead of stale revision id`.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the misleading CSRF enforcement-scope docstring (docs only, no behavior change)</name>
  <files>app/csrf.py</files>
  <action>
Rewrite the CSRF_SENSITIVE_COOKIES docstring comment (app/csrf.py lines 46-50) so it states the ACTUAL behavior. The current claim that an unauthenticated POST carrying the csrftoken cookie "still gets checked because the double-submit pattern remains intact" is factually wrong: starlette-csrf only enforces when a cookie listed in `sensitive_cookies` (here `session_id`) is present. Unauthenticated POSTs to /login and /setup carry no session_id cookie and are therefore NOT CSRF-enforced.

Rewrite the comment to state, accurately:
- Enforcement is scoped to requests carrying the `session_id` cookie (i.e. authenticated sessions). starlette-csrf checks CSRF only when a sensitive cookie is present in the request.
- Unauthenticated POSTs (/login, /setup) carry no session_id cookie and are intentionally NOT CSRF-enforced.
- This is a deliberate design choice: `sensitive_cookies` scopes enforcement to authenticated sessions. The residual login-CSRF exposure is an ACCEPTED, low-impact exception for this household-scale threat model (no public registration; admin-provisioned users only).

Constraints:
- DOCUMENTATION ONLY. Do not change the value of CSRF_SENSITIVE_COOKIES, do not remove or add cookies, do not touch csrf_middleware_kwargs or any runtime code. The set stays `{"session_id"}`.
- Keep the `#:` Sphinx-attribute-comment style so the docstring still attaches to the constant.
- Run ruff format + ruff check on app/csrf.py after editing (comment-only change should be a no-op for ruff, but confirm it stays clean).

Also: scan app/static/js/htmx-listeners.js for any comment repeating the same wrong claim. Lines 30-33 ALREADY state the correct behavior ("an unauthenticated request without a CSRF token is not enforced") — leave them as-is. Only edit the JS if you find a comment that actually asserts the wrong thing. Note the outcome (changed / verified-already-correct) in the SUMMARY.
  </action>
  <verify>
    <automated>grep -nE 'sensitive_cookies|session_id' app/csrf.py && ! grep -qE 'still gets checked because the double-submit pattern remains intact' app/csrf.py && echo DOCSTRING_FIXED</automated>
  </verify>
  <done>app/csrf.py CSRF_SENSITIVE_COOKIES docstring accurately describes session-scoped enforcement, names /login + /setup as intentionally not CSRF-enforced, and frames the residual exposure as an accepted household-scale exception; CSRF_SENSITIVE_COOKIES value unchanged (`{"session_id"}`); no runtime code touched; htmx-listeners.js verified (or corrected only if it repeated the wrong claim); ruff clean. Commit: `docs: correct CSRF enforcement-scope docstring for sensitive_cookies`.</done>
</task>

</tasks>

<verification>
- Duplicate roaster/flavor-note name (create AND update, including case-variant collisions) returns a 200 inline "Name already exists." on the name field, never a 500. The session is rolled back so a subsequent valid write succeeds.
- `make smoke`'s head assertion is revision-id-agnostic — no `0001_initial`, no `p4_shared_catalog`, and it will not need editing when the next migration lands.
- app/csrf.py docstring tells the truth about CSRF enforcement scope; runtime CSRF behavior is byte-for-byte unchanged (CSRF_SENSITIVE_COOKIES still `{"session_id"}`).
- ruff format + ruff check clean on every touched .py file (CLAUDE.md: warnings as errors).
- Three atomic commits with conventional-commit prefixes (fix / chore / docs).
</verification>

<success_criteria>
- All three Codex bucket-A findings closed with surgical, scope-contained changes.
- No schema migration, no new env var, no dependency change, no CSRF behavior change.
- New duplicate-name tests authored to match tests/phase_04 conventions (executed only via the documented container path — noted in SUMMARY since pytest is not in this environment's image).
- No refactors beyond the three items.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-ql9-codex-bucket-a-fixes/260519-ql9-SUMMARY.md`.
The SUMMARY MUST explicitly state: (a) the duplicate-name tests were authored but not executed here (pytest not in the prod image), with the exact container command to run them; (b) whether htmx-listeners.js needed a change or was verified already-correct; (c) confirmation that CSRF runtime behavior was not altered.
</output>
