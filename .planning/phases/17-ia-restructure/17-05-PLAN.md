---
phase: 17-ia-restructure
plan: 05
type: execute
wave: 4
depends_on: ['17-01', '17-02', '17-03', '17-04']
files_modified: []
autonomous: false
requirements: [IA-05]
must_haves:
  truths:
    - "IA-05 (Nav + asset changes reach installed PWAs without manual cache clear): after `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`, the `__BUILD_HASH__` substitution in sw.js produces a NEW cache name; John's installed iPhone PWA shows the new Home / Log / AI / Config bottom nav within one navigation cycle WITHOUT \"Clear site data\" or re-install"
    - "Full container test suite green after all four prior plans landed: `docker compose exec coffee-snobbery python -m pytest tests/ -q -rs --ignore=tests/e2e` exits 0 with no NEW failures"
    - "Phase-end style gates: `ruff format --check .` AND `ruff check .` both exit 0 — CI gates BOTH per project memory `executors-skip-ruff-ci-gates-both`"
    - "Build hash bumped: the new `tailwind.<hash>.css` filename + the new `snobbery-v<hash>` SW cache name differ from the pre-Phase-17 baseline (verified via `docker compose exec coffee-snobbery cat /app/app/static/css/<hash>.css | head -1` and `docker compose exec coffee-snobbery grep CACHE_NAME /app/app/static/js/sw.js`)"
    - "375px mobile layout sweep recorded: during the IA-05 same-browser-session check (Task 4), the verifier also confirms at 375x667 that (a) bottom nav fits as Home/Log/AI/Config, (b) home action row + greeting + Top Coffees render without horizontal scroll, (c) DIST-07 banner spans container width for admin+no-key, (d) /ai page renders cleanly in each of below-gate / above-gate-no-key-admin / above-gate-no-key-non-admin / above-gate-with-key states; result recorded in 17-VERIFICATION.md"
  artifacts:
    - path: ".planning/phases/17-ia-restructure/17-VERIFICATION.md"
      provides: "Human-recorded IA-05 on-device verification: pre-deploy nav screenshot, post-deploy nav screenshot, iOS Safari version, build hash before/after, pass/fail verdict"
  key_links: []
---

<objective>
Phase 17 close: rebuild the Docker image so the new templates + JS + Tailwind utilities + SW cache hash are baked, run the full container test suite as the cumulative gate, run the manual IA-05 PWA cache-freshness check on John's iPhone, and record the verification evidence in `.planning/phases/17-ia-restructure/17-VERIFICATION.md` for the verifier agent.

Purpose: IA-05 cannot be verified by pytest — service-worker cache invalidation requires a real installed PWA. This is the one human-required step in Phase 17. Pairing the manual check with a full container suite ensures any test regression from the wave-1/2/3 plans is caught here before `/gsd-verify-work` runs.

Output: A green full container test run, ruff format/check both exit 0, and a `17-VERIFICATION.md` file recording the on-device PWA evidence (pre + post screenshots, build hashes, verdict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/17-ia-restructure/17-CONTEXT.md
@.planning/phases/17-ia-restructure/17-RESEARCH.md
@.planning/phases/17-ia-restructure/17-VALIDATION.md
@.planning/phases/17-ia-restructure/17-01-SUMMARY.md
@.planning/phases/17-ia-restructure/17-02-SUMMARY.md
@.planning/phases/17-ia-restructure/17-03-SUMMARY.md
@.planning/phases/17-ia-restructure/17-04-SUMMARY.md

<interfaces>
SW cache name mechanism (app/static/js/sw.js line 5): `const CACHE_NAME = 'snobbery-v__BUILD_HASH__';`. The substitution happens in `app/routers/pwa.py` at request time using `_BUILD_HASH` derived from content-hashed Tailwind CSS filename (per project memory `c9-sw-cache-content-deterministic`). Any change to a template (base.html lines from plan 17-01, home.html from plan 17-02, pages/ai.html from plan 17-04) or JS file (nav-bar.js from 17-01, banner-dismiss.js from 17-03) will bump the build hash and therefore the SW cache name.

Build commands (per CLAUDE.md "Working with the code"):
- `docker compose build coffee-snobbery` — rebakes the image with new code + Tailwind rebuild + SW hash substitution.
- `docker compose up -d coffee-snobbery` — restarts container on new image.

Full container test suite expectations (per project memory `snobbery-test-gate-runtime`):
- ~939+ passing tests as of Phase 12 baseline; plans 17-01..17-04 add ~24 new tests (5 + 7 + 5 + 7).
- e2e tests at `tests/e2e/` are excluded from the standard CI gate per project memory (they need a virgin-DB temp app + Playwright at /ms-playwright); they are NOT a Phase 17 gate.
- A clean run baseline before this plan: `docker compose exec coffee-snobbery python -m pytest tests/ -q -rs --ignore=tests/e2e` → all green.

IA-05 acceptance environment per CONTEXT.md / RESEARCH.md "Validation Architecture > Manual / On-Device Validation Steps": John's installed iPhone PWA. The test must be done WITHOUT "Clear site data" and WITHOUT re-installing — the goal is to confirm the SW cache-bust mechanism works for normal users who never touch DevTools.

Verification template structure (write to .planning/phases/17-ia-restructure/17-VERIFICATION.md):
```
# Phase 17 Verification

## IA-05 — PWA picks up nav changes without manual cache clear

**Test environment:** John's iPhone PWA (installed via Safari "Add to Home Screen")
**Date:** YYYY-MM-DD
**iOS Safari version:** <version>
**Pre-deploy build hash:** <hash from sw.js before this phase>
**Post-deploy build hash:** <hash from sw.js after Phase 17 deploy>

**Steps performed:**
1. Pre-deploy: open the installed PWA. Confirm bottom nav shows Home / Log / Config / Admin. Screenshot saved at: <path or "captured-but-not-committed">
2. Deploy: `git pull && docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`. Healthcheck waited for.
3. Re-visit: tap any tab to navigate. No "Clear site data", no re-install.
4. Confirm: bottom nav shows Home / Log / AI / Config. Screenshot saved at: <path>

**Verdict:** PASS / FAIL
**Notes:** <any observations — e.g. "took two nav cycles for the SW to activate", which is acceptable per RESEARCH.md>

## Cumulative test suite

**Command:** `docker compose exec coffee-snobbery python -m pytest tests/ -q -rs --ignore=tests/e2e`
**Result:** <pass count> passed, <skip count> skipped, 0 failed
**ruff format --check .:** PASS
**ruff check .:** PASS

## Phase 17 close
- All four prior plans (17-01, 17-02, 17-03, 17-04) merged.
- All requirements (IA-01..06, DIST-07, AIX-08) covered by passing tests except IA-05 which is recorded above.
- Ready for `/gsd-verify-work`.
```

Project memory `verifier-overwrites-human-verification-ledger`: this 17-VERIFICATION.md is HUMAN-AUTHORED; the verifier agent must write to `17-VERIFICATION-AUTO.md` instead. Surface this constraint in the SUMMARY so the orchestrator passes the right flag to the verifier.

Project memory `commit_docs false leaves review docs untracked`: the project's `commit_docs` is false. `gsd-sdk query commit` will refuse to commit `.planning/*.md` files. The 17-VERIFICATION.md must be staged + committed via plain `git add` + `git commit`, NOT via the GSD commit harness. Note this in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build new image, restart container, confirm health</name>
  <files>
    (no source changes — commands only)
  </files>
  <read_first>
    - CLAUDE.md "Working with the code" (docker compose build + up + logs commands)
    - .planning/phases/17-ia-restructure/17-RESEARCH.md "Runtime State Inventory" (build artifacts row)
  </read_first>
  <acceptance_criteria>
    - `docker compose build coffee-snobbery` exits 0. Build log shows the Tailwind stage rebuilds (new content-hashed CSS filename).
    - `docker compose up -d coffee-snobbery` brings the container up.
    - `docker compose logs -f coffee-snobbery` (followed for ~30s until startup completes) shows lifespan startup + migrations applied + no startup errors.
    - `docker compose exec coffee-snobbery grep CACHE_NAME /app/app/static/js/sw.js` returns the substituted line `const CACHE_NAME = 'snobbery-v<hash>';` with `<hash>` populated (not the literal `__BUILD_HASH__`).
    - The hash in the new image differs from the pre-Phase-17 baseline (compare to git history if available, or just record the new hash and trust the rebuild signal).
  </acceptance_criteria>
  <action>From the project root on the host machine: Step 1: `docker compose build coffee-snobbery` (~2-5 min). Step 2: `docker compose up -d coffee-snobbery`. Step 3: tail logs with `docker compose logs --tail 60 coffee-snobbery` and confirm a clean startup (migrations applied, lifespan ready). Step 4: capture the new SW cache name with `docker compose exec coffee-snobbery grep CACHE_NAME /app/app/static/js/sw.js` and note it for the 17-VERIFICATION.md file. Step 5: capture the Tailwind CSS hash via `docker compose exec coffee-snobbery ls /app/app/static/css/`. If `docker compose build` fails, fix the underlying issue (likely a Jinja template syntax error from an earlier plan) and re-run; do NOT proceed to Task 2 until the build is green.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose exec coffee-snobbery grep -o "snobbery-v[a-z0-9]*" /app/app/static/js/sw.js</automated>
  </verify>
  <done>Image rebuilt successfully. Container is healthy. SW cache name is substituted (not the literal `__BUILD_HASH__`). New Tailwind CSS hash + SW hash recorded for the 17-VERIFICATION.md file.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full container test suite (non-e2e) — cumulative gate for Phase 17</name>
  <files>
    (no source changes — commands only)
  </files>
  <read_first>
    - .planning/phases/17-ia-restructure/17-VALIDATION.md Sampling Rate
    - CLAUDE.md "Working with the code"
    - project memory `full-suite-test-isolation-gaps` — DB cross-pollution baselines
  </read_first>
  <acceptance_criteria>
    - `docker compose exec coffee-snobbery python -m pytest tests/ -q -rs --ignore=tests/e2e` exits 0.
    - Counted pass count >= pre-phase baseline + 24 (the new tests from plans 17-01..17-04: 5 + 7 + 5 + 7).
    - Skip count is acceptable only if every skip is tied to a documented `_require_*()` gate (per project memory `tests-pass-by-skip-mask-green` — `-rs` surfaces skips for inspection).
    - Zero NEW failures attributable to Phase 17 changes. Pre-existing flakes documented in project memory `full-suite-test-isolation-gaps` may persist (the `test_setup_concurrent_race` test in particular — that's a known pre-existing flake) — surface and ignore them.
  </acceptance_criteria>
  <action>Run `docker compose exec coffee-snobbery python -m pytest tests/ -q -rs --ignore=tests/e2e` from the host. Capture the summary line (e.g., `939 passed, 12 skipped in 118.23s`). Compare new pass count vs baseline + 24 expected new tests. If a failure occurs:
    - If the failure is in a Phase 17-touched file, the executor must fix the source. Do NOT skip or xfail.
    - If the failure is in a pre-existing module flagged by `full-suite-test-isolation-gaps`, capture and continue (the verifier will adjudicate).
    Do NOT run e2e tests in this task — they require Playwright + virgin-DB orchestration documented separately.
    Record the final summary line for the 17-VERIFICATION.md file.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose exec coffee-snobbery python -m pytest tests/ -q -rs --ignore=tests/e2e 2>&1 | tail -5</automated>
  </verify>
  <done>Full non-e2e suite green (or only pre-existing flakes failing). Final summary line captured. New tests from Phase 17 (24) all collected and passing.</done>
</task>

<task type="auto">
  <name>Task 3: ruff format + check phase-end gate</name>
  <files>
    (no source changes — commands only)
  </files>
  <read_first>
    - project memory `executors-skip-ruff-ci-gates-both`
  </read_first>
  <acceptance_criteria>
    - `ruff format --check .` exits 0. If it fails, run `ruff format .` and commit the diff as `style(17): ruff format pass`.
    - `ruff check .` exits 0. If it fails, fix the violations (do NOT add `# noqa` unless the lint exception is principled and documented inline).
  </acceptance_criteria>
  <action>From the project root host shell: Step 1: `ruff format --check .`. If it reports formatting drift, run `ruff format .`, stage + commit the resulting diff with subject `style(17): apply ruff format`. Step 2: `ruff check .`. If lint failures appear, edit the source to fix them (do NOT silence with `# noqa`). Re-run both gates after fixes.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && ruff format --check . && ruff check .</automated>
  </verify>
  <done>Both ruff gates exit 0. If a `style(17): ` commit was needed, it sits on the branch alongside the feat commits from 17-01..17-04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (CHECKPOINT): IA-05 on-device PWA cache-freshness verification on John's iPhone</name>
  <files>
    (no source changes — verification only; evidence recorded in .planning/phases/17-ia-restructure/17-VERIFICATION.md by Task 5)
  </files>
  <action>Pause for John to perform the on-device check documented in the `<how-to-verify>` block below and return one of PASS / FAIL / DEFER (with notes). The orchestrator MUST NOT proceed to Task 5 until a verdict is recorded.</action>
  <verify>
    <automated>echo "Manual checkpoint — no automated verify. Resume signal will be recorded inline in 17-VERIFICATION.md by Task 5."</automated>
  </verify>
  <done>John has returned PASS (with build hashes + screenshot description) OR FAIL (with observed behavior + DevTools state) OR DEFER (with reason + revisit date) for IA-05. Additionally, the 375px mobile layout sweep (sub-steps a/b/c/d) has a recorded verdict — bottom nav, home composition, DIST-07 banner, /ai page composition. Both verdicts are captured for Task 5 to write into 17-VERIFICATION.md.</done>
  <what-built>
    Phase 17 has rebuilt the Docker image with a new Tailwind CSS hash and new SW cache name (`snobbery-v<new-hash>`). The bottom nav HTML in base.html has changed (Admin slot removed, AI slot added). The PWA service worker is content-deterministic per project memory `c9-sw-cache-content-deterministic`: ANY change to the baked-in template content bumps the cache name. The expectation is that an installed PWA, on its next visit, fetches the new cache via the SW activate-on-fetch lifecycle and renders the new nav — WITHOUT the user touching "Clear site data" or re-installing.
  </what-built>
  <how-to-verify>
    Before triggering this checkpoint, the executor must have completed Tasks 1-3 (image rebuilt, container green, full suite passing). The actual on-device check requires John:

    1. **Pre-deploy snapshot:** (if not already captured before today) on John's iPhone, open the installed Snobbery PWA. Confirm the current bottom nav shows the OLD layout — Home / Log / Config / Admin (pre-Phase-17). Take a screenshot. If the PWA already shows the NEW nav (because the deploy was done earlier and the PWA already polled), record that fact and skip steps 2-4 — the verification is implicitly already PASS. Otherwise continue.

    2. **Deploy (already done by Task 1):** the local container should already be running the new image. For the VPS environment: on the VPS, `git pull && docker compose build coffee-snobbery && docker compose up -d coffee-snobbery` and wait for `docker compose logs -f coffee-snobbery` to show the lifespan startup message. If verifying against the production VPS, this step needs SSH access.

    3. **Re-visit the PWA:** on John's iPhone, tap any bottom-nav tab to trigger an in-app navigation. Do NOT use "Clear site data" in Safari Settings. Do NOT remove + re-add to home screen. Do NOT force-quit the PWA.

    4. **Confirm:** within one navigation cycle (sometimes two, which is also acceptable per RESEARCH.md — the SW activate step runs on the next fetch), the bottom nav updates to the NEW layout — Home / Log / AI / Config. Take a post-deploy screenshot.

    5. **Record evidence:** capture both screenshots' filenames (or paste-friendly descriptions), the iOS Safari version, the pre-deploy SW cache name, and the post-deploy SW cache name (from Task 1's verify output). Write everything into `.planning/phases/17-ia-restructure/17-VERIFICATION.md` using the template from `<interfaces>` above.

    6. **375px mobile layout sweep (extends this same checkpoint — same browser session):** In Chrome DevTools (or Safari Web Inspector) at 375×667, hit each of the following and confirm no horizontal scroll + the documented composition. The PWA on-iPhone visit covers (a) implicitly; (b)-(d) need DevTools because the on-device PWA may not reach every branch in a single session (e.g., John has an AI key configured in prod):
       (a) **Bottom nav (IA-01 / IA-02 / D-01 / D-02):** four tabs — Home / Log / AI / Config — each `flex-1`, single row, AI label + icon fit; no Admin tab.
       (b) **Home action row + greeting + Top Coffees (IA-04 / D-06 / D-10):** GET / — three-button action row (Guided Brew / Log session / Quick rate) wraps cleanly; greeting H1 reads; Top Coffees renders eagerly; no `hx-get=/home/cards/top-coffees` placeholder visible.
       (c) **DIST-07 banner (D-19):** as admin with no AI key (use a test session or temporarily disable the key — recovery noted in 17-VERIFICATION.md), GET / — banner at top of `<main>`, headline + "Go to Admin" + × button all reachable; spans container width.
       (d) **/ai page composition (IA-02 / IA-03 / AIX-08 / D-13..D-16 / D-20):** GET /ai in each state — below-gate cold-start (meter + D-14 explainer + Log session CTA); above-gate-no-key as admin (AIX-08 amber callout + Go to Admin button + DIST-07 banner above); above-gate-no-key as non-admin ("AI is not set up" copy, NO admin link, NO banner); above-gate-with-key (AI hero + Preference Profile + Flavor Descriptors + Sweet Spots + AI tools + research stub). Each state renders without horizontal scroll; `min-h-[14rem]` parity holds between cold-start, admin callout, and non-admin callout sections.
       Record verdict (PASS / FAIL / DEFER) per sub-step (a/b/c/d) in 17-VERIFICATION.md alongside the IA-05 record.

    **If FAIL:** PWA still shows Admin tab after 2 navigation cycles — check `caches.keys()` in Safari Web Inspector (Settings → Advanced → Web Inspector → Storage → Service Workers); inspect whether the new SW activated. If not, that's a SW lifecycle issue worth deeper investigation; raise it as a gap in 17-VERIFICATION.md and the verifier will decide whether to gate phase close on it.
  </how-to-verify>
  <resume-signal>Reply with "PASS" + the recorded build hashes and a screenshot description, or "FAIL" + observed behavior + DevTools cache state. If verification is being deferred (e.g., John not at his iPhone right now), reply "DEFER" and the verifier can re-open this checkpoint later — but Phase 17 close should not be claimed until the on-device check is recorded.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write .planning/phases/17-ia-restructure/17-VERIFICATION.md and commit it</name>
  <files>.planning/phases/17-ia-restructure/17-VERIFICATION.md</files>
  <read_first>
    - The template structure documented in `<interfaces>` above
    - project memory `verifier-overwrites-human-verification-ledger` — this file is HUMAN-authored; the verifier writes to `17-VERIFICATION-AUTO.md` instead
    - project memory `commit_docs false leaves review docs untracked` — stage + commit via plain `git add` (the GSD harness will refuse)
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/17-ia-restructure/17-VERIFICATION.md` exists and contains: IA-05 section with pre/post build hashes + iOS Safari version + verdict, 375px mobile layout sweep section with per-sub-step (a/b/c/d) verdicts, cumulative test summary from Task 2, ruff verdict from Task 3, Phase 17 close roll-up.
    - File is committed via plain `git add .planning/phases/17-ia-restructure/17-VERIFICATION.md && git commit -m "docs(17): record Phase 17 verification (IA-05 on-device + full suite + ruff)"`.
    - The orchestrator should pass `--manual-verification 17-VERIFICATION.md` (or equivalent flag) to the verifier so it knows the canonical human ledger is present and writes any auto verification to `17-VERIFICATION-AUTO.md` (per project memory).
  </acceptance_criteria>
  <action>Write `.planning/phases/17-ia-restructure/17-VERIFICATION.md` per the template in `<interfaces>`. Fill in: the build hash captured in Task 1, the test suite summary captured in Task 2, the ruff verdict from Task 3, and the IA-05 verdict from the Task 4 checkpoint. If Task 4 was DEFERRED, mark the IA-05 verdict as `PENDING` and note the date the deferral was made — the Phase 17 SUMMARY will flag this as the only open item. Stage with plain `git add` (NOT through the GSD commit harness, which the project config disables) and commit with the message above. Do NOT push.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && test -f .planning/phases/17-ia-restructure/17-VERIFICATION.md && grep -q "IA-05" .planning/phases/17-ia-restructure/17-VERIFICATION.md && git log --oneline -1 | grep "docs(17):"</automated>
  </verify>
  <done>17-VERIFICATION.md exists, contains the IA-05 verdict (PASS/FAIL/PENDING), the full-suite summary, and the ruff verdict. The file is committed on main with a `docs(17):` subject. The orchestrator now has a canonical human-authored ledger to point the verifier at.</done>
</task>

</tasks>

<verification>
1. **Image rebuilt with new hash:** `docker compose exec coffee-snobbery grep CACHE_NAME /app/app/static/js/sw.js` shows a non-template hash value distinct from any pre-Phase-17 baseline.

2. **Full non-e2e suite green:** the suite passes; new test count from Phase 17 is +24 minimum.

3. **Style gates:** ruff format --check . and ruff check . both exit 0.

4. **IA-05 on-device verdict recorded:** 17-VERIFICATION.md contains a PASS / FAIL / PENDING IA-05 section with build hashes + iOS Safari version.

5. **17-VERIFICATION.md committed:** `git log --oneline -1` shows a `docs(17):` commit on main.

6. **Phase 17 close-ready signals:**
   - All requirements (IA-01, IA-02, IA-03, IA-04, IA-05, IA-06, DIST-07, AIX-08) have either passing tests OR recorded human verification in 17-VERIFICATION.md.
   - All 21 D-NN decisions are covered by at least one plan's must_haves.truths frontmatter (decision-coverage gate; project memory `decision-coverage-gate-scans-musthaves`).
   - Working tree clean.
</verification>

<success_criteria>
- Docker image rebuilt; SW cache name reflects new content hash.
- Full non-e2e pytest suite green inside the running container.
- ruff format/check both pass.
- John has verified (or has deferred with a recorded reason) that his installed iPhone PWA picks up the new Home / Log / AI / Config bottom nav without a manual cache clear.
- `.planning/phases/17-ia-restructure/17-VERIFICATION.md` exists, captures all evidence, and is committed.
- Phase 17 is ready for `/gsd-verify-work` (which will read this file and write its own `17-VERIFICATION-AUTO.md`).
</success_criteria>

<output>
After completion, create `.planning/phases/17-ia-restructure/17-05-SUMMARY.md` with:
- Confirmation that the build was clean, the suite ran green, ruff passed, and the IA-05 on-device verdict was recorded (PASS / FAIL / PENDING).
- The pre-vs-post build hash diff (proves the SW cache-bust mechanism fired).
- Notes for the verifier:
  - The canonical human verification ledger is `17-VERIFICATION.md`; verifier MUST write its auto verification to `17-VERIFICATION-AUTO.md` (project memory `verifier-overwrites-human-verification-ledger`).
  - If IA-05 was deferred, surface this as the ONE open item — the rest of Phase 17 is closed-ready.
- Final phase-17 commit list (the four feat commits from 17-01..17-04, plus the docs(17) commit from this plan, plus any style(17) commit from Task 3 if ruff drift was found).
</output>
