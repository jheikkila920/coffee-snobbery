---
phase: 17-ia-restructure
plan: 04
type: execute
wave: 3
depends_on: ['17-01', '17-02', '17-03']
files_modified:
  - app/routers/ai.py
  - app/templates/pages/ai.html
  - app/templates/fragments/ai/_cold_start.html
  - app/templates/fragments/home/_cold_start.html
  - app/templates/fragments/ai/_no_key_admin_callout.html
  - app/templates/fragments/ai/_no_key_non_admin_callout.html
  - app/templates/fragments/research_coming_soon.html
  - tests/routers/test_ai_router.py
autonomous: true
requirements: [IA-02, IA-03, AIX-08]
complexity_note: "8 tasks intentional — each task is a single small file create or edit; splitting would force a sub-wave with no quality gain"
must_haves:
  truths:
    - "D-03 (AI tab always visible — covered by plan 17-01; this plan ensures GET /ai responds 200 for ANY authenticated user regardless of admin / cold-start / key state): `pytest tests/routers/test_ai_router.py::test_get_ai_page_returns_200` passes for both admin and non-admin fixtures"
    - "D-12 (URL is /ai; existing /ai/paste-rank, /ai/wishlist, /ai/refresh, /ai/equipment, /ai/wishlist/* routes keep their URLs and templates — UNCHANGED by this plan): grep `app/routers/ai.py` shows all five sub-routes intact after the new GET '' handler is added"
    - "D-13 (Above-gate, key-present users see: AI hero, Preference Profile, Top Flavor Descriptors, Sweet Spots, AI tools, Research-coming-soon stub on /ai): pages/ai.html renders the four card sections with hx-get mounts of /home/cards/ai-recommendation, /home/cards/preference-profile, /home/cards/flavor-descriptors, /home/cards/sweet-spots PLUS the AI tools section (Rank these → /ai/paste-rank link, Analyze setup → /ai/equipment form) PLUS the {% include \"fragments/research_coming_soon.html\" %}"
    - "D-14 (Below-gate users on /ai see: cold-start meter + explainer line + Log session CTA): pages/ai.html renders `{% include \"fragments/ai/_cold_start.html\" %}` when `not gate.gate_open`; the moved fragment's body includes the meter, the line `AI personalization activates after 3 sessions and 5 distinct flavor notes.`, and the existing `<a href=\"/brew/new\">Log session</a>` CTA"
    - "D-15 (Above-gate, no AI key, ADMIN users see: distinct callout — different headline `AI keys needed`, different icon, primary `Go to Admin` button, same container size as cold-start card so layout doesn't jump): pages/ai.html renders `{% include \"fragments/ai/_no_key_admin_callout.html\" %}` for is_admin AND not ai_key_present AND gate_open; fragment has min-h matched to cold-start to honor Pitfall F"
    - "D-16 (Above-gate, no AI key, NON-ADMIN users see: \"AI is not set up. Ask the household admin to configure an API key.\" — no Admin link, no Notify button): pages/ai.html renders `{% include \"fragments/ai/_no_key_non_admin_callout.html\" %}` for not is_admin AND not ai_key_present AND gate_open; fragment contains zero `href=\"/admin\"` references"
    - "D-20 (DIST-07 banner and AIX-08 callout coexist for admins — banner at top, callout in place of AI hero): pages/ai.html includes `{% include \"fragments/ai_key_setup_banner.html\" %}` at the top of <main> AND renders the AIX-08 admin callout in the hero slot when both conditions hold; non-admins see only the non-admin callout (banner self-gates to admins)"
    - "IA-02 (AI tab opens page shell): `GET /ai` returns 200 for any authenticated user (above or below gate, with or without key); 401 for anonymous"
    - "IA-03 (AI surfaces consolidated on /ai): pages/ai.html mounts /home/cards/ai-recommendation, /home/cards/preference-profile, /home/cards/flavor-descriptors, /home/cards/sweet-spots when above-gate AND key-present — these endpoints stay at their /home/cards/* URLs (RESEARCH Open Question #3 deferred); the AI tools section (paste-rank + equipment) mirrors what was removed from home.html in plan 17-02"
    - "AIX-08 (no-key state distinct from not-enough-data): the admin no-key callout has DIFFERENT headline text (`AI keys needed` vs cold-start's `Build your taste profile.`), DIFFERENT icon (key/lock vs progress bar), DIFFERENT primary action (Go to Admin button vs Log session CTA). Below-gate users see cold-start, NEVER the no-key callout — gate_open check precedes ai_key_present check"
  artifacts:
    - path: "app/routers/ai.py"
      provides: "New `GET /ai` handler (mounts at prefix root `/ai`) inside the existing router; computes gate + ai_key_present; renders pages/ai.html with context"
      contains: "@router.get(\"\", response_class=HTMLResponse)"
    - path: "app/templates/pages/ai.html"
      provides: "AI page shell with three-branch composition (below-gate / above-gate-no-key / above-gate-key-present); top-of-page banner include; research-coming-soon stub on the key-present branch"
      min_lines: 80
    - path: "app/templates/fragments/ai/_cold_start.html"
      provides: "MOVED via git mv from fragments/home/_cold_start.html; updates the secondary explainer line to D-14 copy"
      contains: "AI personalization activates after 3 sessions and 5 distinct flavor notes."
    - path: "app/templates/fragments/home/_cold_start.html"
      provides: "DELETED via git mv (the file no longer exists; home.html's include was removed in plan 17-02 so no orphan reference)"
    - path: "app/templates/fragments/ai/_no_key_admin_callout.html"
      provides: "D-15 admin no-key callout: amber-accented section, key icon, AI keys needed headline, Go to Admin primary button"
      contains: "AI keys needed"
    - path: "app/templates/fragments/ai/_no_key_non_admin_callout.html"
      provides: "D-16 non-admin no-key callout: AI is not set up + Ask the household admin copy; no admin link, no notify button"
      contains: "Ask the household admin"
    - path: "app/templates/fragments/research_coming_soon.html"
      provides: "D-13 disabled stub card: heading 'Research a coffee', one-line copy mentioning Phase 19, disabled button labeled 'Coming soon'"
      contains: "Coming in Phase 19"
    - path: "tests/routers/test_ai_router.py"
      provides: "Seven new tests covering D-13/D-14/D-15/D-16/D-20/IA-02/AIX-08 — every branch of pages/ai.html plus the auth gate"
  key_links:
    - from: "app/routers/ai.py"
      to: "app/services/analytics.py"
      via: "analytics.get_cold_start_counts(db, user.id) call inside the new shell handler"
      pattern: "get_cold_start_counts"
    - from: "app/routers/ai.py"
      to: "app/services/credentials.py"
      via: "credentials_service.get_provider_credential(db, provider) for anthropic + openai"
      pattern: "get_provider_credential"
    - from: "app/templates/pages/ai.html"
      to: "app/templates/fragments/ai_key_setup_banner.html"
      via: "{% include %} at top of <main>"
      pattern: "include \"fragments/ai_key_setup_banner.html\""
    - from: "app/templates/pages/ai.html"
      to: "/home/cards/ai-recommendation"
      via: "hx-get mount inside the above-gate + key-present branch"
      pattern: "hx-get=\"/home/cards/ai-recommendation\""
    - from: "app/templates/fragments/ai/_no_key_admin_callout.html"
      to: "/admin/credentials"
      via: "Go to Admin primary button"
      pattern: "href=\"/admin/credentials\""
---

<objective>
Build the AI page shell at `/ai` and the AIX-08 no-key callouts. The page is the consolidation point for AI surfaces (hero + Preference Profile + Top Flavor Descriptors + Sweet Spots + AI tools + a Phase 19 research stub) when both the cold-start gate is open and an AI key is configured; otherwise it renders one of three distinct empty states: the cold-start meter (below gate), the AIX-08 admin callout (above gate + no key + admin), or the AIX-08 non-admin callout (above gate + no key + non-admin). The DIST-07 banner from plan 17-03 is included at the top of the page for admins-with-no-key (D-20 coexistence).

Purpose: Closes the IA-02 (AI page shell present + working), IA-03 (AI surfaces consolidated), and AIX-08 (distinct no-key state) requirements. Phase 19 will replace the research-coming-soon stub with the real research/predict UI without changing the page composition contract built here.

Output: One new GET handler in app/routers/ai.py, one new page template, one moved fragment (cold-start), two new callout fragments, one new stub fragment, and seven new pytest assertions covering the three-branch composition + auth gate + banner coexistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-ia-restructure/17-CONTEXT.md
@.planning/phases/17-ia-restructure/17-RESEARCH.md
@.planning/phases/17-ia-restructure/17-PATTERNS.md
@.planning/phases/17-ia-restructure/17-VALIDATION.md
@.planning/phases/17-ia-restructure/17-01-SUMMARY.md
@.planning/phases/17-ia-restructure/17-02-SUMMARY.md
@.planning/phases/17-ia-restructure/17-03-SUMMARY.md

<interfaces>
Existing app/routers/ai.py prefix (line 47): `router = APIRouter(prefix="/ai")`. The new `@router.get("", response_class=HTMLResponse)` handler MUST mount at the empty path so it serves `/ai` exactly (prefix concatenation). Insert IMMEDIATELY AFTER line 47 (before the existing `get_paste_rank_page` handler at line 54).

Existing imports at lines 30-40 of app/routers/ai.py:
```python
from fastapi import APIRouter, BackgroundTasks, Depends, HTTPException, Request
from fastapi.responses import HTMLResponse, Response
from sqlalchemy.orm import Session
from app.dependencies.auth import require_user
from app.dependencies.db import get_session
from app.events import AI_THROTTLE_BLOCK, AI_URL_VERIFY
from app.models.user import User
from app.services import ai_service
from app.services import wishlist as wishlist_service
from app.templates_setup import templates
```
ADD: `from app.services import analytics`, `from app.services import credentials as credentials_service`. The new GET handler's body mirrors the pattern from app/routers/home.py (post-Phase-17-02): get gate, compute ai_key_present from two `get_provider_credential` calls, render `pages/ai.html` with context `{"gate": gate, "ai_key_present": ai_key_present, "user": user}`. The `user` is already available from `Depends(require_user)`; passing it explicitly into context lets the template branch on `user.is_admin`.

Seed strategy for AI-key presence/absence in tests (pinned for plans 17-03 and 17-04, symmetric):
- `_with_ai_key` fixture: monkeypatch `credentials_service.get_provider_credential` to return a sentinel object (e.g., `object()` or a `MagicMock`) for either "anthropic" OR "openai" (planner picks anthropic by default) and `None` for the other provider.
- `_without_ai_key` fixture: monkeypatch `credentials_service.get_provider_credential` to return `None` for BOTH providers.
- Rationale: respx is already in the test deps; monkeypatch sidesteps Fernet encryption round-trip noise; the test is about UI/template rendering on the basis of the helper's return value, NOT about whether the encryption layer works (that has its own coverage in tests/services/test_credentials.py). Symmetric across plans 17-03 and 17-04 so the same approach reads identically in both modules.
- Concrete signature: use pytest's `monkeypatch` fixture and a side-effect function:
  ```python
  def _with_ai_key(monkeypatch, *, provider: str = "anthropic"):
      from app.services import credentials as credentials_service
      def fake(_db, p):
          return object() if p == provider else None
      monkeypatch.setattr(credentials_service, "get_provider_credential", fake)

  def _without_ai_key(monkeypatch):
      from app.services import credentials as credentials_service
      monkeypatch.setattr(credentials_service, "get_provider_credential", lambda _db, _p: None)
  ```
- Do NOT insert real `ApiCredential` rows in tests. Do NOT round-trip Fernet ciphertext for these UI/template tests.

Existing `card_ai_recommendation` in app/routers/home.py (lines 220-288) — the canonical reference for the AI-key check pattern. Use the SAME pattern in the new /ai handler: `anthropic_cred = credentials_service.get_provider_credential(db, "anthropic")` + `openai_cred = ...` + `ai_key_present = anthropic_cred is not None or openai_cred is not None`.

`pages/ai.html` composition skeleton (per 17-PATTERNS.md "NEW pages/ai.html"):

```
{% extends "base.html" %}
{% block page_title %}AI{% endblock %}
{% block content %}
  <main class="mx-auto max-w-6xl px-6 py-12">
    {% include "fragments/ai_key_setup_banner.html" %}    {# DIST-07 banner; self-gates on is_admin + not ai_key_present #}
    <header class="mb-6">
      <h1 class="text-2xl font-semibold">AI</h1>
    </header>

    <div class="space-y-8">
      {% if not gate.gate_open %}
        {% include "fragments/ai/_cold_start.html" %}
      {% elif not ai_key_present %}
        {% if request.state.user.is_admin %}
          {% include "fragments/ai/_no_key_admin_callout.html" %}
        {% else %}
          {% include "fragments/ai/_no_key_non_admin_callout.html" %}
        {% endif %}
      {% else %}
        {# AI hero (mount existing /home/cards/ai-recommendation endpoint — URL retained per RESEARCH Open Question #3) #}
        {# Preference Profile, Flavor Descriptors, Sweet Spots — same lazy-mount shape #}
        {# AI tools: paste-rank link + equipment form #}
        {% include "fragments/research_coming_soon.html" %}
      {% endif %}
    </div>
  </main>
{% endblock %}
```

`fragments/home/_cold_start.html` will be moved (`git mv`) to `fragments/ai/_cold_start.html`. The variable contract (`gate.sessions_needed`, `gate.notes_needed`, `gate.distinct_notes`, `gate.sessions`) is unchanged. The body text on line 41-43 of the source fragment ("In the meantime, your recent brews and catalog are below.") is REPLACED with the D-14 explainer "AI personalization activates after 3 sessions and 5 distinct flavor notes." The remaining body (progress meter + branched copy + Log session CTA) stays.

AI hero / Preference / Flavor / Sweet Spots lazy mounts use the canonical home pattern (17-RESEARCH.md Pattern 3) with the staggered delays specified there:
- AI hero (id="ai-rec-hero"): `hx-get="/home/cards/ai-recommendation" hx-trigger="load delay:100ms" hx-swap="outerHTML"` (drop from 600ms — hero is now the first card on /ai)
- Preference Profile: `hx-get="/home/cards/preference-profile" hx-trigger="load delay:200ms" hx-swap="innerHTML"`
- Flavor Descriptors: `hx-get="/home/cards/flavor-descriptors" hx-trigger="load delay:300ms" hx-swap="innerHTML"`
- Sweet Spots: `hx-get="/home/cards/sweet-spots" hx-trigger="load delay:500ms" hx-swap="innerHTML"`

AI tools section copy-paste from home.html (pre-Phase-17-02 lines 87-115 — the executor can recover it via `git log -p -- app/templates/pages/home.html` against commit `feat(17-02):`). The section heading is "AI tools"; contains a `<a href="/ai/paste-rank">Rank these for me</a>` link styled as `bg-espresso-700`, a `<a href="/ai/wishlist">Wishlist</a>` link styled as the bordered button, then the `<form hx-post="/ai/equipment" ...>Analyze my setup</form>` block with its CSRF hidden input and `<div id="equipment-rec-result">` swap target. NOTE: the Wishlist link inside AI tools is a duplicate affordance (Wishlist also has its own card on home per D-06). Both surfaces are intentional per CONTEXT.md; do NOT remove either.

D-15 (admin no-key callout) container shape mirrors the cold-start card's outer wrapper for layout-parity per Pitfall F. Add `min-h-[14rem]` on BOTH the cold-start fragment (after the move) and the admin callout fragment so the slot is the same height across state transitions. The amber palette (`amber-50` bg, `amber-300` border, `amber-700` text, `amber-900/20` dark bg) is intentional — different visual treatment than cold-start's neutral palette.

D-16 (non-admin no-key callout) uses the same NEUTRAL `bg-cream-100 dark:bg-espresso-900 border-espresso-200 dark:border-espresso-700` palette as cold-start (no amber) — the non-admin state is informational, not actionable. Same `min-h-[14rem]` for layout parity. Zero `href="/admin"` references; the only outbound link in this fragment is optional (`/cafe-logs/new` or `/brew/new` as a soft suggestion — planner's discretion; safest is to omit any external link and keep the copy alone, per D-16's "no Admin link, no Notify admin button" wording).

`fragments/research_coming_soon.html` per 17-PATTERNS.md "NEW research_coming_soon.html" — single section card with `opacity-60`, headline "Research a coffee", one-line body mentioning Phase 19, a `<button type="button" disabled>Coming soon</button>` with `cursor-not-allowed`.

The existing `fragments/home/ai_rec_not_configured.html` is the legacy fragment served by `/home/cards/ai-recommendation` when no key is configured. Plan 17-04 does NOT modify it. The endpoint is still mounted on /ai (key-present branch); but if the endpoint internally finds no key, it returns this fragment as a defensive fallback. This is double-protection: the page-level branch in pages/ai.html shows the AIX-08 callout BEFORE the hero would even mount; if a race condition fires the hero mount anyway, the endpoint returns the legacy "not configured" fragment. That's acceptable; we don't change `card_ai_recommendation`.

The existing test file tests/routers/test_ai_router.py is present (see Bash output during planning); extend it in place per RESEARCH §"Wave 0 Gaps".

The existing `_require_nav_wired()` skip-gate pattern from tests/test_nav.py is not adequate here because the AI page depends on /ai router import; mirror the skip-gate pattern from tests/routers/test_home.py or tests/routers/test_ai_router.py (whichever the existing module uses).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/routers/test_ai_router.py with seven new tests covering D-13/D-14/D-15/D-16/D-20/IA-02/AIX-08</name>
  <files>tests/routers/test_ai_router.py</files>
  <read_first>
    - tests/routers/test_ai_router.py (full file — skip-gate + fixture-usage pattern)
    - tests/test_dist07_banner.py (plan 17-03 product — re-use the "with key" / "no key" seed strategy chosen there)
    - tests/conftest.py (seeded_admin_user, seeded_regular_user)
    - .planning/phases/17-ia-restructure/17-VALIDATION.md Per-Task Verification Map (IA-02 / IA-03 / AIX-08 rows)
  </read_first>
  <behavior>
    Add seven test functions to tests/routers/test_ai_router.py:

    - `test_get_ai_page_returns_200`: any authenticated user GET /ai → 200. Run twice (admin + non-admin fixtures) or parametrize. Confirms IA-02 + D-03.
    - `test_get_ai_page_returns_401_for_anonymous`: GET /ai without session → 401. Standard `require_user` gate.
    - `test_ai_page_below_gate_shows_cold_start_not_no_key`: seed an authed user with ZERO brews + ZERO cafe logs + NO AI key configured. GET /ai → response contains the literal `AI personalization activates after 3 sessions and 5 distinct flavor notes.` (D-14 explainer) AND does NOT contain `AI keys needed` (the AIX-08 admin headline) AND does NOT contain `AI is not set up` (the non-admin headline). Confirms the branch ordering: gate check fires BEFORE key check.
    - `test_ai_page_shows_admin_callout_above_gate_no_key`: seed an admin user with enough brews+cafe logs+flavor notes to clear the cold-start gate (use `_seed_analytics_scenario` or hand-seed >=3 brews + >=5 distinct flavor notes), but NO ApiCredential rows. GET /ai → response contains `AI keys needed` AND `href="/admin/credentials"` AND does NOT contain `AI personalization activates after`. Confirms D-15.
    - `test_ai_page_shows_non_admin_callout_above_gate_no_key`: same seed (above-gate + no key) but with non-admin user. GET /ai → contains `Ask the household admin` AND does NOT contain `href="/admin"` (no admin link for non-admins per D-16). Confirms D-16.
    - `test_ai_page_above_gate_with_key_shows_hero`: seed an admin user above gate AND with a working ApiCredential (reuse the strategy from plan 17-03's `test_home_hides_dist07_banner_when_admin_has_key`). GET /ai → response contains `hx-get="/home/cards/ai-recommendation"` AND `hx-get="/home/cards/preference-profile"` AND `hx-get="/home/cards/flavor-descriptors"` AND `hx-get="/home/cards/sweet-spots"` AND the literal `Coming in Phase 19` (research stub). Confirms D-13.
    - `test_ai_page_shows_dist07_banner_for_admin_with_no_key`: seed admin + no key (above OR below gate — banner gate is independent of cold-start). GET /ai → response contains `bannerDismiss` AND `Welcome — add your AI API key in Admin`. Confirms D-20 (banner coexists on /ai for admins-no-key).

    All seven tests collect (`--collect-only`) and currently FAIL until Tasks 2-7 land.
  </behavior>
  <action>Open tests/routers/test_ai_router.py. Read the existing skip-gate helpers and seed-helper patterns at the top of the file. Append seven new test functions per `<behavior>`. For the "above gate" seed, reuse `_seed_analytics_scenario` from tests/services/test_analytics.py if compatible; otherwise write a small per-test helper that inserts the minimum required brew_sessions + flavor_note observations. For the "no key" seed, reuse the strategy from plan 17-03 (`tests/test_dist07_banner.py`) — read that file first to discover whether the chosen strategy was real ApiCredential rows or `monkeypatch` on `get_provider_credential`; reuse the same strategy here for consistency. Add `import re` and any needed datetime imports to the top-of-file. Tests added BEFORE implementation tasks (TDD red).</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && grep -c "def test_get_ai_page_returns_200\|def test_get_ai_page_returns_401_for_anonymous\|def test_ai_page_below_gate_shows_cold_start_not_no_key\|def test_ai_page_shows_admin_callout_above_gate_no_key\|def test_ai_page_shows_non_admin_callout_above_gate_no_key\|def test_ai_page_above_gate_with_key_shows_hero\|def test_ai_page_shows_dist07_banner_for_admin_with_no_key" tests/routers/test_ai_router.py</automated>
  </verify>
  <done>The test module contains all seven new functions per `<behavior>`. `grep -c` returns 7. Tests collect without import errors; they currently fail (RED) because tasks 2-7 are not yet done.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: git mv fragments/home/_cold_start.html → fragments/ai/_cold_start.html and update the explainer line</name>
  <files>app/templates/fragments/home/_cold_start.html, app/templates/fragments/ai/_cold_start.html</files>
  <read_first>
    - app/templates/fragments/home/_cold_start.html (full file)
    - .planning/phases/17-ia-restructure/17-PATTERNS.md "MOD app/templates/fragments/home/_cold_start.html" section
    - .planning/phases/17-ia-restructure/17-RESEARCH.md Alternatives Considered → "Move to fragments/ai/_cold_start.html"
  </read_first>
  <behavior>
    - After the move, fragments/home/_cold_start.html no longer exists.
    - fragments/ai/_cold_start.html exists with all of the original meter / branched copy / Log session CTA preserved.
    - Line 42 of the original file ("In the meantime, your recent brews and catalog are below.") is REPLACED with "AI personalization activates after 3 sessions and 5 distinct flavor notes." (D-14 explainer copy).
    - Pitfall F (matching container size with the admin callout): the section wrapper gains `min-h-[14rem]` Tailwind class — added to the existing `<section ... class="rounded-lg border ...">` opening tag.
    - The blame-preserving move is `git mv` (NOT delete + create). If working in Windows-without-git-mv-awareness, the executor uses `git rm fragments/home/_cold_start.html` followed by `git add fragments/ai/_cold_start.html` and verifies via `git status` that git detects the rename (R100 or R98 — ≥ 50% similarity threshold).
    - The new directory `app/templates/fragments/ai/` is created by this task (first file in it). Git tracks directories implicitly via tracked files; no `.gitkeep` needed.
  </behavior>
  <action>Step 1: ensure `app/templates/fragments/ai/` exists — `mkdir app/templates/fragments/ai` (will no-op if already there from a prior plan). Step 2: `git mv app/templates/fragments/home/_cold_start.html app/templates/fragments/ai/_cold_start.html`. Step 3: edit the new file at `app/templates/fragments/ai/_cold_start.html`: replace the `<p class="text-sm text-espresso-600 dark:text-cream-300 mb-4">In the meantime, your recent brews and catalog are below.</p>` line with `<p class="text-sm text-espresso-600 dark:text-cream-300 mb-4">AI personalization activates after 3 sessions and 5 distinct flavor notes.</p>`. Step 4: add `min-h-[14rem]` to the opening `<section ... class="rounded-lg border border-espresso-200 bg-cream-100 p-4 dark:bg-espresso-900 dark:border-espresso-800">` element (append `min-h-[14rem]` to the class list). Step 5: confirm via `git status` that git detected the move (renamed: app/templates/fragments/home/_cold_start.html → app/templates/fragments/ai/_cold_start.html, with a small content modification).</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && test ! -f app/templates/fragments/home/_cold_start.html && test -f app/templates/fragments/ai/_cold_start.html && grep -q "AI personalization activates after 3 sessions and 5 distinct flavor notes." app/templates/fragments/ai/_cold_start.html && grep -q "min-h-\[14rem\]" app/templates/fragments/ai/_cold_start.html && echo "cold-start move + edits OK"</automated>
  </verify>
  <done>fragments/home/_cold_start.html no longer exists. fragments/ai/_cold_start.html exists, contains the D-14 explainer copy, has `min-h-[14rem]` on the section wrapper, and otherwise preserves the meter + branched countdown copy + Log session CTA. `git status` shows a rename, not a delete+add.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create fragments/ai/_no_key_admin_callout.html (D-15 distinct callout for above-gate + admin + no key)</name>
  <files>app/templates/fragments/ai/_no_key_admin_callout.html</files>
  <read_first>
    - .planning/phases/17-ia-restructure/17-PATTERNS.md "NEW fragments/ai/_no_key_admin_callout.html" section
    - .planning/phases/17-ia-restructure/17-RESEARCH.md Pitfall F (layout-parity with cold-start)
    - app/templates/fragments/ai/_cold_start.html (after Task 2 — match the wrapper shape for layout parity)
  </read_first>
  <behavior>
    Fragment structure (per 17-PATTERNS.md code example):
    - `<section aria-labelledby="no-key-admin-heading" class="rounded-lg border border-amber-300 bg-amber-50 p-4 dark:bg-amber-900/20 dark:border-amber-700 min-h-[14rem]">` — outer wrapper, AMBER palette (visually distinct from cold-start's neutral cream/espresso) + matching `min-h-[14rem]` for Pitfall F layout parity.
    - Inner row `<div class="flex items-start gap-3">`.
    - 32x32 inline SVG icon, key/lock outline (`d="M15 7a2 2 0 11-4 0 2 2 0 014 0zm6 1a8 8 0 11-16 0 8 8 0 0116 0z"`), `stroke="currentColor"`, `stroke-width="2"`, color `text-amber-700`, `aria-hidden="true"`. The KEY shape is intentional — visually distinct from the cold-start's progress bar.
    - Content `<div class="flex-1">`:
      - `<h2 id="no-key-admin-heading" class="text-xl font-semibold mb-2">AI keys needed</h2>` — distinct headline from cold-start's "Build your taste profile."
      - `<p class="text-base mb-4">Add an API key in Admin to unlock AI recommendations for the household.</p>`
      - `<a href="/admin/credentials" class="inline-flex items-center rounded bg-espresso-700 px-4 py-2 text-base font-semibold text-cream-50 hover:bg-espresso-800 min-h-[44px]">Go to Admin</a>` — PRIMARY button (filled bg-espresso-700 vs cold-start's same button — that's intentional; the action button across the app is consistent), `min-h-[44px]` touch target.
  </behavior>
  <action>Create app/templates/fragments/ai/_no_key_admin_callout.html with the markup documented in `<behavior>` and 17-PATTERNS.md. The fragment is NOT wrapped in any `{% if %}` — pages/ai.html handles the conditional inclusion. No CSRF / no form / no inline script. No `request.state.user` references — the parent template gates inclusion. Total file size ~20 lines including comment header. Add a one-line Jinja comment header `{# D-15 / AIX-08: admin no-key callout. Renders on /ai when gate_open AND not ai_key_present AND is_admin. #}`.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && python -c "t = open('app/templates/fragments/ai/_no_key_admin_callout.html', encoding='utf-8').read(); assert 'AI keys needed' in t, 'headline missing'; assert 'href=\"/admin/credentials\"' in t, 'admin link missing'; assert 'min-h-[14rem]' in t, 'layout-parity min-h missing (Pitfall F)'; assert 'amber' in t, 'amber palette missing for visual distinction'; assert 'Go to Admin' in t, 'CTA label missing'; print('admin callout OK')"</automated>
  </verify>
  <done>Fragment file exists with the documented structure: AI keys needed headline, key/lock icon, Go to Admin filled-button linking to /admin/credentials, amber-palette wrapper, `min-h-[14rem]` for layout parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create fragments/ai/_no_key_non_admin_callout.html (D-16: non-admin no-key state)</name>
  <files>app/templates/fragments/ai/_no_key_non_admin_callout.html</files>
  <read_first>
    - .planning/phases/17-ia-restructure/17-PATTERNS.md "NEW fragments/ai/_no_key_non_admin_callout.html" section
    - .planning/phases/17-ia-restructure/17-RESEARCH.md D-16 explicit constraints
  </read_first>
  <behavior>
    Fragment structure (per 17-PATTERNS.md):
    - `<section aria-labelledby="no-key-non-admin-heading" class="rounded-lg border border-espresso-200 bg-cream-100 p-4 dark:bg-espresso-900 dark:border-espresso-800 min-h-[14rem]">` — NEUTRAL cream/espresso palette (different from admin callout's amber), same `min-h-[14rem]` for Pitfall F.
    - Inner row with same key icon (32x32, stroke-currentColor, `text-espresso-700 dark:text-cream-100` — neutral, not amber).
    - `<h2 id="no-key-non-admin-heading" class="text-xl font-semibold mb-2">AI is not set up</h2>` — D-16 headline.
    - `<p class="text-base text-espresso-700 dark:text-cream-200">Ask the household admin to configure an API key.</p>` — D-16 social-action copy.
    - NO `<a href="/admin">` references (non-admins get 403).
    - NO "Notify admin" button (no notification system v1).
    - Optional: a one-line Jinja comment inline above the closing tag explaining the deliberate omissions.
  </behavior>
  <action>Create app/templates/fragments/ai/_no_key_non_admin_callout.html with the structure in `<behavior>`. Add a Jinja comment header `{# D-16 / AIX-08: non-admin no-key callout. NO Admin link, NO Notify button. Names the social action honestly. Renders on /ai when gate_open AND not ai_key_present AND not is_admin. #}`. Verify zero `href="/admin"` occurrences in the file.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && python -c "t = open('app/templates/fragments/ai/_no_key_non_admin_callout.html', encoding='utf-8').read(); assert 'AI is not set up' in t, 'headline missing'; assert 'Ask the household admin' in t, 'social-action copy missing'; assert 'href=\"/admin\"' not in t, 'D-16 forbids any admin link'; assert 'min-h-[14rem]' in t, 'layout-parity min-h missing'; assert 'Notify' not in t, 'no notify button per D-16'; print('non-admin callout OK')"</automated>
  </verify>
  <done>Fragment file exists. Contains the D-16 headline + social-action copy. Zero `href="/admin"` references. Zero "Notify" references. `min-h-[14rem]` for layout parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create fragments/research_coming_soon.html (D-13 stub card for the Phase 19 surface)</name>
  <files>app/templates/fragments/research_coming_soon.html</files>
  <read_first>
    - .planning/phases/17-ia-restructure/17-PATTERNS.md "NEW research_coming_soon.html" section
  </read_first>
  <behavior>
    Fragment structure:
    - `<section aria-labelledby="research-stub-heading" class="rounded-lg border border-espresso-200 bg-cream-100 p-4 dark:bg-espresso-900 dark:border-espresso-800 opacity-60">` — standard home/AI card shape PLUS `opacity-60` to signal disabled state.
    - `<h2 id="research-stub-heading" class="text-xl font-semibold mb-2">Research a coffee</h2>`.
    - `<p class="text-base text-espresso-600 dark:text-cream-300">Coming in Phase 19 — paste a coffee URL or name and we'll surface roaster history, flavor profile, and a predicted rating.</p>`.
    - `<button type="button" disabled class="mt-3 inline-flex items-center rounded border border-espresso-300 dark:border-espresso-600 px-4 py-2 text-base font-semibold min-h-[44px] cursor-not-allowed text-espresso-400 dark:text-espresso-500">Coming soon</button>`.
    - Lives in `fragments/research_coming_soon.html` (NOT under `fragments/ai/`) to leave room for plan 19 to share it with potentially another page if needed; the file path is the planner's discretion call.
  </behavior>
  <action>Create app/templates/fragments/research_coming_soon.html with the documented structure. No `request.state.user` references; the parent template controls when this is included. Add a Jinja comment header `{# D-13: Phase 19 research/predict surface stub. Disabled button + Coming in Phase 19 copy. Single-line message; minimal styling — Phase 21 owns visual polish, Phase 19 replaces this stub with real research/predict UI. #}`.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && python -c "t = open('app/templates/fragments/research_coming_soon.html', encoding='utf-8').read(); assert 'Research a coffee' in t, 'headline missing'; assert 'Coming in Phase 19' in t, 'Phase 19 reference missing'; assert 'disabled' in t and 'Coming soon' in t, 'disabled button missing'; assert 'opacity-60' in t, 'disabled visual cue missing'; print('research stub OK')"</automated>
  </verify>
  <done>Fragment file exists with the documented disabled stub. Contains "Coming in Phase 19", a disabled button labeled "Coming soon", and `opacity-60` styling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Create app/templates/pages/ai.html (three-branch composition; mounts existing /home/cards/* endpoints; includes banner + stub)</name>
  <files>app/templates/pages/ai.html</files>
  <read_first>
    - .planning/phases/17-ia-restructure/snippets/ai-surfaces-pre-17-02.html (CANONICAL recovery source for the AI hero + AI tools + lazy-mount markup — staged by plan 17-02 Task 4)
    - app/templates/pages/home.html (current state after plan 17-02) for the `<main>` wrapper + heading shape
    - .planning/phases/17-ia-restructure/17-PATTERNS.md "NEW pages/ai.html" section + 17-RESEARCH.md Pattern 3 (staggered delays)
  </read_first>
  <behavior>
    Page composition (per the `<interfaces>` skeleton above and 17-PATTERNS.md "NEW pages/ai.html"):

    1. `{% extends "base.html" %}` + `{% block page_title %}AI{% endblock %}` + `{% block content %}` ... `{% endblock %}`.

    2. Inside `<main class="mx-auto max-w-6xl px-6 py-12">`:
       - `{% include "fragments/ai_key_setup_banner.html" %}` at the very top (D-20 — banner self-gates on is_admin AND not ai_key_present).
       - `<header class="mb-6"><h1 class="text-2xl font-semibold">AI</h1></header>`.
       - `<div class="space-y-8">` wrapping the branched content.

    3. Three-branch composition:
       - **Below gate** (`{% if not gate.gate_open %}`): single `{% include "fragments/ai/_cold_start.html" %}` only.
       - **Above gate, no key** (`{% elif not ai_key_present %}`):
         - Admin: `{% include "fragments/ai/_no_key_admin_callout.html" %}`.
         - Non-admin (`{% else %}`): `{% include "fragments/ai/_no_key_non_admin_callout.html" %}`.
       - **Above gate, key present** (final `{% else %}`): five sections, in order:
         1. AI hero `<section aria-labelledby="ai-rec-heading">` with `<div id="ai-rec-hero" hx-get="/home/cards/ai-recommendation" hx-trigger="load delay:100ms" hx-swap="outerHTML">` + skeleton placeholder.
         2. Preference Profile section with `hx-get="/home/cards/preference-profile" hx-trigger="load delay:200ms" hx-swap="innerHTML"`.
         3. Top Flavor Descriptors section with `hx-get="/home/cards/flavor-descriptors" hx-trigger="load delay:300ms"`.
         4. Sweet Spots section with `hx-get="/home/cards/sweet-spots" hx-trigger="load delay:500ms"`.
         5. AI tools section (heading "AI tools") containing the paste-rank link (`<a href="/ai/paste-rank">Rank these for me</a>`), the wishlist link (`<a href="/ai/wishlist">Wishlist</a>`), AND the equipment form (`<form hx-post="/ai/equipment" hx-target="#equipment-rec-result" hx-swap="innerHTML">` with CSRF hidden input + submit + result `<div id="equipment-rec-result">`) — copy this verbatim from the snippet at `.planning/phases/17-ia-restructure/snippets/ai-surfaces-pre-17-02.html` (staged by plan 17-02 Task 4 — see Task 6 <action>).
         6. `{% include "fragments/research_coming_soon.html" %}` — the D-13 stub.
  </behavior>
  <action>Create app/templates/pages/ai.html per the composition in `<behavior>`. Read `.planning/phases/17-ia-restructure/snippets/ai-surfaces-pre-17-02.html` (staged by plan 17-02 Task 4 as a stable recovery source) to recover the AI hero + AI tools + lazy-mount markup that this page mounts at the new location. Do NOT use `git show <commit-sha>:...` — commit SHAs drift across branches and rebases; the snippet under `.planning/snippets/` is the canonical source. Adjust the AI hero `hx-trigger` delay to `load delay:100ms` (down from 600ms — the hero is now the first card on /ai, the staggering rationale changes). Use the canonical home card section shape (`rounded-lg border border-espresso-200 bg-cream-100 p-4 dark:bg-espresso-900 dark:border-espresso-800`) for the four lazy-mount sections + AI tools section. The branched skeleton must use Jinja's `{% if %} {% elif %} {% else %} {% endif %}` cleanly — order matters: gate_open check first, then ai_key_present, then the admin/non-admin split inside the no-key elif. Total file size ~120 lines.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && python -c "t = open('app/templates/pages/ai.html', encoding='utf-8').read(); assert '{% extends \"base.html\" %}' in t; assert '{% include \"fragments/ai_key_setup_banner.html\" %}' in t; assert 'hx-get=\"/home/cards/ai-recommendation\"' in t; assert 'hx-get=\"/home/cards/preference-profile\"' in t; assert 'hx-get=\"/home/cards/flavor-descriptors\"' in t; assert 'hx-get=\"/home/cards/sweet-spots\"' in t; assert '{% include \"fragments/research_coming_soon.html\" %}' in t; assert '{% if not gate.gate_open %}' in t; assert '{% elif not ai_key_present %}' in t; assert '{% include \"fragments/ai/_cold_start.html\" %}' in t; assert '{% include \"fragments/ai/_no_key_admin_callout.html\" %}' in t; assert '{% include \"fragments/ai/_no_key_non_admin_callout.html\" %}' in t; print('pages/ai.html composition OK')"</automated>
  </verify>
  <done>pages/ai.html exists with the three-branch composition. Contains every required include and hx-get mount listed in `<behavior>`. The branch order (gate → key → admin-vs-non-admin) matches D-13/D-14/D-15/D-16. Banner include is at the top of `<main>`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Add `GET /ai` page-shell handler to app/routers/ai.py</name>
  <files>app/routers/ai.py</files>
  <read_first>
    - app/routers/ai.py lines 1-100 (imports + router declaration + first existing handler)
    - app/routers/home.py lines 220-288 (canonical AI-key-check pattern from card_ai_recommendation)
    - .planning/phases/17-ia-restructure/17-RESEARCH.md "Pattern 1: Add a route to an existing FastAPI APIRouter"
    - .planning/phases/17-ia-restructure/17-PATTERNS.md MOD app/routers/ai.py section
  </read_first>
  <behavior>
    - Add imports if not already present (line 30-40 region): `from app.services import analytics`, `from app.services import credentials as credentials_service`. Each goes on its own line, following the existing import-block style.
    - New handler signature (mount immediately after `router = APIRouter(prefix="/ai")` on line 47, BEFORE `get_paste_rank_page` at line 54):
      ```
      @router.get("", response_class=HTMLResponse)
      def get_ai_page(
          request: Request,
          user: User = Depends(require_user),
          db: Session = Depends(get_session),
      ) -> Response:
      ```
    - Handler body: compute `gate = analytics.get_cold_start_counts(db, user.id)`. Compute `anthropic_cred = credentials_service.get_provider_credential(db, "anthropic")` and `openai_cred = credentials_service.get_provider_credential(db, "openai")`. Set `ai_key_present = anthropic_cred is not None or openai_cred is not None`. Return `templates.TemplateResponse(request=request, name="pages/ai.html", context={"gate": gate, "ai_key_present": ai_key_present, "user": user})`. Pass `user` so the template's `request.state.user.is_admin` branch is consistent with the request-scoped user object (also matches existing pattern in card_ai_recommendation line 254).
    - Docstring covers the three rendered branches with one-liners per D-13/D-14/D-15/D-16.
    - The new handler MUST mount at the EXACTLY-EMPTY path `""` so the router's prefix `/ai` makes it `/ai`. A path of `/` would mount at `/ai/`, breaking the URL contract.
  </behavior>
  <action>Edit app/routers/ai.py. Step 1: extend the import block at lines 30-40 with `from app.services import analytics` and `from app.services import credentials as credentials_service` (place alphabetically among the existing `from app.services import ...` lines). Step 2: between line 47 (`router = APIRouter(prefix="/ai")`) and line 49 (the `# -----` separator comment), insert the new `get_ai_page` handler per `<behavior>`. The mount path is the empty string `""`. Add a section separator comment block ABOVE the new handler with format consistent with the existing separators (`# -----` line, blank line, `# GET /ai — page shell (Phase 17, IA-02)` comment, blank line, separator line). Do NOT modify ANY other handler in ai.py. Do NOT change the existing throttle window constant or any other module-level value.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose cp app/routers/ai.py coffee-snobbery:/app/app/routers/ai.py && docker compose cp app/templates/pages/ai.html coffee-snobbery:/app/app/templates/pages/ai.html && docker compose cp app/templates/fragments/ai/_cold_start.html coffee-snobbery:/app/app/templates/fragments/ai/_cold_start.html && docker compose cp app/templates/fragments/ai/_no_key_admin_callout.html coffee-snobbery:/app/app/templates/fragments/ai/_no_key_admin_callout.html && docker compose cp app/templates/fragments/ai/_no_key_non_admin_callout.html coffee-snobbery:/app/app/templates/fragments/ai/_no_key_non_admin_callout.html && docker compose cp app/templates/fragments/research_coming_soon.html coffee-snobbery:/app/app/templates/fragments/research_coming_soon.html && docker compose cp tests/routers/test_ai_router.py coffee-snobbery:/app/tests/routers/test_ai_router.py && docker compose exec coffee-snobbery rm -f /app/app/templates/fragments/home/_cold_start.html && docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py -q -rs -k "get_ai_page or ai_page_below_gate or ai_page_shows or ai_page_above_gate"</automated>
  </verify>
  <done>app/routers/ai.py contains the new `get_ai_page` handler mounted at `""` (resolves to `/ai`). The handler computes `gate` and `ai_key_present` and renders `pages/ai.html`. All seven new tests in tests/routers/test_ai_router.py pass.</done>
</task>

<task type="auto">
  <name>Task 8: Full focused suite + cumulative regression sweep + ruff + commit</name>
  <files>
    (commands only)
  </files>
  <read_first>
    - CLAUDE.md "Working with the code"
    - .planning/phases/17-ia-restructure/17-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py tests/test_nav.py tests/test_dist07_banner.py tests/routers/test_home.py tests/services/test_analytics.py -q -rs` exits 0; all new tests green; no regression.
    - `docker compose exec coffee-snobbery python -m pytest tests/ -q -rs --ignore=tests/e2e` exits 0 (modulo project-memory documented flakes; NO new failures from this plan).
    - `ruff format --check .` exits 0.
    - `ruff check .` exits 0.
    - Commit message: `feat(17-04): /ai page shell — IA-02 shell, IA-03 consolidation, AIX-08 admin + non-admin callouts, D-13..D-16 + D-20`.
    - Staged files exactly match `files_modified` (the `git mv` of _cold_start.html counts as TWO file-path entries in `files_modified`).
  </acceptance_criteria>
  <action>Step 1: verify all changes are copied into the container (the Task 7 verify command already did most of this; re-run for any drift). Step 2: run the cumulative IA test sweep covering plans 17-01..17-04. Step 3: run the full non-e2e suite. If any pre-existing flake is hit, check against project memory `full-suite-test-isolation-gaps` — only block on NEW failures. Step 4: ruff format/check from host. Step 5: stage exactly the files in `files_modified` (the `git rm fragments/home/_cold_start.html` from Task 2 is part of this commit) and commit. The commit will show:
      - 1 file deleted (fragments/home/_cold_start.html)
      - 1 file added (fragments/ai/_cold_start.html) — git detects as RENAME
      - 3 new fragments (admin callout, non-admin callout, research stub)
      - 1 new page (pages/ai.html)
      - 1 modified router (app/routers/ai.py)
      - 1 modified test module (tests/routers/test_ai_router.py)
    Do NOT push.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py tests/test_nav.py tests/test_dist07_banner.py tests/routers/test_home.py tests/services/test_analytics.py -q -rs && ruff check . && git log --oneline -1 | grep "feat(17-04):"</automated>
  </verify>
  <done>All 7 new ai-router tests pass; plans 17-01/02/03 tests still pass. Full non-e2e suite green (no new failures). ruff format/check exit 0. Commit subject begins with `feat(17-04):`. Working tree clean after commit (modulo `.planning/` files which the orchestrator commits separately).</done>
</task>

</tasks>

<verification>
1. **Auth gate (IA-02):** `pytest tests/routers/test_ai_router.py::test_get_ai_page_returns_200 tests/routers/test_ai_router.py::test_get_ai_page_returns_401_for_anonymous -v` — both pass.

2. **Three-branch composition:** `pytest tests/routers/test_ai_router.py::test_ai_page_below_gate_shows_cold_start_not_no_key tests/routers/test_ai_router.py::test_ai_page_shows_admin_callout_above_gate_no_key tests/routers/test_ai_router.py::test_ai_page_shows_non_admin_callout_above_gate_no_key tests/routers/test_ai_router.py::test_ai_page_above_gate_with_key_shows_hero -v` — all four pass.

3. **DIST-07 banner coexistence (D-20):** `pytest tests/routers/test_ai_router.py::test_ai_page_shows_dist07_banner_for_admin_with_no_key -v` — passes.

4. **No existing /ai/* route broken (D-12):** `pytest tests/routers/test_ai_router.py -q -rs` — all pre-existing tests (paste-rank, wishlist, refresh, equipment) still pass.

5. **Pitfall F (layout-parity):** grep both fragment files (`fragments/ai/_cold_start.html` and `fragments/ai/_no_key_admin_callout.html`) for `min-h-[14rem]` — both contain it.

6. **D-16 non-admin no-key copy contract:** `grep "Ask the household admin\|href=\"/admin\"" app/templates/fragments/ai/_no_key_non_admin_callout.html` — first match present, second match ABSENT.

7. **/admin/credentials link target:** `grep "href=\"/admin/credentials\"" app/templates/fragments/ai/_no_key_admin_callout.html app/templates/fragments/ai_key_setup_banner.html` — present in both.

8. **No SW changes:** `git diff HEAD~4..HEAD --name-only -- app/static/js/sw.js` returns nothing.

9. **No /setup change:** `git diff HEAD~4..HEAD --name-only -- app/routers/auth.py` returns nothing.

10. **Style gates:** `ruff format --check .` and `ruff check .` both exit 0.
</verification>

<success_criteria>
- `GET /ai` returns 200 for any authenticated user (admin or not, above gate or not, with or without a key); 401 for anonymous.
- pages/ai.html renders one of three distinct states per request: cold-start meter (below gate, with D-14 explainer); AIX-08 admin or non-admin no-key callout (above gate, no key); the consolidated AI surface (above gate, key present — hero + Preference Profile + Flavor Descriptors + Sweet Spots + AI tools + research stub).
- Above-gate admin with no key sees the DIST-07 banner at the TOP of /ai PLUS the AIX-08 admin callout in the hero slot (D-20 coexistence).
- /home/cards/* endpoint URLs are unchanged; pages/ai.html mounts them at their existing paths.
- All seven new ai-router tests pass; the existing /ai/paste-rank, /ai/wishlist, /ai/refresh, /ai/equipment tests still pass; full non-e2e suite has zero new failures.
- ruff format/check pass; commit lands on main.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ia-restructure/17-04-SUMMARY.md` with:
- Files added (5 new fragments/templates) + files modified (ai.py, test_ai_router.py) + 1 file moved (cold-start)
- The three-branch state machine for `/ai` documented as a small ASCII flowchart or table
- Confirmation that the AI hero `hx-trigger` delay dropped from 600ms (home pre-17-02) to 100ms (the hero is now the first card on /ai)
- Confirmation that the `card_ai_recommendation` endpoint at app/routers/home.py:220-288 is UNCHANGED (the page-level branch on /ai prevents the endpoint from being hit when no key is configured; defense-in-depth — if it IS hit, the existing `ai_rec_not_configured.html` fragment fires)
- Note that AIX-08 admin link target is `/admin/credentials` — confirmed during planning via grep of app/routers/admin/credentials.py
- Note that Pitfall F (layout-parity between cold-start + admin callout) is honored via `min-h-[14rem]` on BOTH section wrappers; ALSO added to the non-admin callout for consistency
- Phase 17 close prerequisites: plan 17-05 will handle the container rebuild + full suite + IA-05 on-device PWA verification
</output>
