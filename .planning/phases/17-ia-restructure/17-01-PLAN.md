---
phase: 17-ia-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/base.html
  - app/static/js/alpine-components/nav-bar.js
  - app/templates/pages/config_hub.html
  - tests/test_nav.py
autonomous: true
requirements: [IA-01, IA-02]
must_haves:
  truths:
    - "D-01 (Bottom nav slot order Home / Log / AI / Config; Admin removed from bottom): the bottom nav block in base.html contains exactly four `<a>` slots in that order, no `<a href=\"/admin\">` lives inside the `<nav x-data=\"navBar\">` bottom-nav region for any user role"
    - "D-02 (AI tab label is \"AI\" with sparkle/star inline SVG): the new bottom-nav slot renders `<span class=\"text-xs\">AI</span>` and an inline 24x24 stroke-currentColor SVG icon — no new image asset, no new icon library"
    - "D-04 (Config tab label retained): the bottom-nav and top-nav Config link label still reads `Config`, route stays `/config`"
    - "D-05 (nav-bar.js activeTab gets /ai branch BEFORE /admin removal): the activeTab getter returns `'ai'` for any pathname starting with `/ai` and no longer returns `'admin'` (the /admin pathname branch is removed)"
    - "D-17 (Admin entry on /config below catalog grid, above mobile sign-out, is_admin-gated, visible on mobile AND desktop): `pages/config_hub.html` renders a new section between catalog grid close and the `md:hidden` sign-out block, guarded by `{% if request.state.user.is_admin %}`, NOT itself `md:hidden`, containing a single tile linking to `/admin`"
    - "D-18 (Top-nav Admin link at >=768px stays): the top-nav `<nav>` block in base.html still contains `{% if request.state.user.is_admin %}<a href=\"/admin\">Admin</a>{% endif %}` after the slot reshape"
    - "IA-01 (Admin reachable from Config button, not bottom nav): an admin's GET / response contains no `href=\"/admin\"` in the bottom-nav `<nav x-data=\"navBar\">` block; an admin's GET /config response contains `href=\"/admin\"` inside the new Administration section"
    - "IA-02 (AI tab present in bottom nav): every authenticated user's GET / response contains `<a href=\"/ai\">` inside the `<nav x-data=\"navBar\">` block"
  artifacts:
    - path: "app/templates/base.html"
      provides: "Bottom-nav AI slot insertion; bottom-nav Admin slot removal; top-nav AI link added between Log and Config; top-nav Admin link preserved"
      contains: "href=\"/ai\""
    - path: "app/static/js/alpine-components/nav-bar.js"
      provides: "activeTab returns 'ai' for /ai*; /admin branch removed"
      contains: "p.startsWith('/ai')"
    - path: "app/templates/pages/config_hub.html"
      provides: "Admin entry tile, is_admin-gated, between catalog grid and mobile sign-out"
      contains: "Administration"
    - path: "tests/test_nav.py"
      provides: "Five new test functions asserting IA-01 + IA-02 wiring across home + config + non-admin home"
      min_lines: 200
  key_links:
    - from: "app/templates/base.html"
      to: "app/static/js/alpine-components/nav-bar.js"
      via: ":class=\"activeTab === 'ai' ? ...\""
      pattern: "activeTab === 'ai'"
    - from: "app/templates/pages/config_hub.html"
      to: "/admin"
      via: "<a href=\"/admin\"> tile inside is_admin-gated Administration section"
      pattern: "Administration[\\s\\S]+?href=\"/admin\""
---

<objective>
Reshape the persistent nav so the bottom tab bar reflects daily-use priority — Admin is removed from the bottom nav and reachable via a dedicated card on the Config page, an AI tab is inserted between Log and Config, the activeTab Alpine getter is updated to route `/ai/*` to the new tab, and the desktop top-nav mirrors the new slot order while keeping the existing Admin link (D-18). Lands IA-01 (Admin off bottom nav, on Config) and IA-02 (AI tab present) in a single self-contained plan with no file overlap with 17-02.

Purpose: Phase 17's nav reshuffle must land before any AI page consumer or DIST-07 banner can target the new tab. Solving nav + Config admin entry first keeps the file-modification surface tight and parallel to 17-02 (home composition).

Output: Updated base.html top + bottom nav blocks, updated nav-bar.js activeTab getter, new Administration section on pages/config_hub.html, five new tests in tests/test_nav.py asserting the IA-01 + IA-02 wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-ia-restructure/17-CONTEXT.md
@.planning/phases/17-ia-restructure/17-RESEARCH.md
@.planning/phases/17-ia-restructure/17-PATTERNS.md
@.planning/phases/17-ia-restructure/17-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Use these directly — no exploration needed. -->

base.html bottom nav block lives at lines 244-291; current order is Home (251-258), Log (260-267), Config (269-277), Admin (279-289 gated by is_admin). The wrapper is `<nav x-data="navBar" class="fixed bottom-0 ... md:hidden">` and each tab is an `<a>` with `:class="activeTab === '<slot>' ? 'text-espresso-700 dark:text-cream-100' : 'text-espresso-400 dark:text-espresso-400'"`.

base.html top nav block lives at lines 91-98; current order is Home / Log / Config / Admin (is_admin-gated). Same `<a class="text-sm font-semibold text-espresso-600 ...">` shape per link.

base.html bottom-nav slot pattern to copy for the AI tab (per 17-PATTERNS.md and 17-RESEARCH.md "Adding the AI bottom-nav slot"):
- `<a href="/ai" class="flex flex-col items-center justify-center gap-1 flex-1 min-h-[44px] min-w-[44px]" :class="activeTab === 'ai' ? 'text-espresso-700 dark:text-cream-100' : 'text-espresso-400 dark:text-espresso-400'">`
- 24x24 inline SVG, `stroke="currentColor"`, `stroke-width="2"`, `aria-hidden="true"` — use the Heroicons sparkles outline path (`d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"`)
- `<span class="text-xs">AI</span>`

nav-bar.js current getter (lines 19-33):
```javascript
get activeTab() {
  const p = window.location.pathname;
  if (p === '/' || p.startsWith('/home')) return 'home';
  if (p.startsWith('/brew')) return 'brew';
  if (p.startsWith('/config') || p.startsWith('/coffees') ||
      p.startsWith('/equipment') || p.startsWith('/recipes') ||
      p.startsWith('/roasters') || p.startsWith('/flavor-notes')) return 'config';
  if (p.startsWith('/admin')) return 'admin';
  return '';
},
```

config_hub.html structure: catalog grid at lines 14-52, mobile sign-out block (the `md:hidden` wrapper) opens at line 56. New Administration section is inserted at the boundary (after line 52 closing `</div>`, before line 56 opening `<div class="md:hidden ...">`). Section is NOT `md:hidden`. Uses the existing catalog-card shape (`flex items-center gap-3 rounded-xl border border-espresso-200 dark:border-espresso-700 bg-cream-100 dark:bg-espresso-900 px-4 py-4 min-h-[44px] hover:bg-cream-200 dark:hover:bg-espresso-800 transition-colors`). Shield icon path comes from base.html:285 (`d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"`).

tests/test_nav.py: existing module with five tests using `seeded_admin_user` + `seeded_regular_user` fixtures from `tests/conftest.py:574,600`. New tests follow the same shape as `test_admin_home_has_admin_link` and `test_non_admin_home_has_no_admin_link`. Use a `re` import to scope assertions to the bottom-nav `<nav x-data="navBar">` block (the top-nav Admin link stays per D-18, so a bare `'href="/admin"' in r.text` assertion is no longer valid for IA-01 on the home page — must scope to the bottom-nav region).

Admin router prefix confirmed via grep: `app/routers/admin/__init__.py:35` — `router = APIRouter(prefix="/admin")`. Admin sub-pages including `/admin/credentials` are mounted under that prefix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IA-01 + IA-02 nav assertions to tests/test_nav.py (Wave 0 test scaffold)</name>
  <files>tests/test_nav.py</files>
  <read_first>
    - tests/test_nav.py (full file — pattern to mirror)
    - tests/conftest.py lines 574-610 (seeded_admin_user + seeded_regular_user fixtures)
    - .planning/phases/17-ia-restructure/17-PATTERNS.md (Test Pattern section)
    - .planning/phases/17-ia-restructure/17-VALIDATION.md (Per-Task Verification Map — IA-01/IA-02 test names)
  </read_first>
  <behavior>
    - Test 1 `test_admin_home_has_no_admin_bottom_nav_tab`: admin GET / → response has no `href="/admin"` inside the bottom-nav `<nav x-data="navBar">...</nav>` block (use a regex `re.search(r'<nav x-data="navBar".*?</nav>', r.text, re.DOTALL)` and assert `href="/admin"` not in match group). The TOP-nav `href="/admin"` stays per D-18 — do NOT use a bare `'href="/admin"' not in r.text` assertion.
    - Test 2 `test_admin_config_page_has_admin_entry`: admin GET /config → response contains the literal substring `Administration` AND `href="/admin"` (the new entry section).
    - Test 3 `test_non_admin_config_page_has_no_admin_entry`: non-admin GET /config → response contains no `href="/admin"` (no top-nav admin, no Administration section).
    - Test 4 `test_home_has_ai_bottom_nav_tab`: any authed user (admin or non-admin) GET / → bottom-nav block contains `href="/ai"` AND the literal text `>AI<` (the `<span class="text-xs">AI</span>` label).
    - Test 5 `test_top_nav_still_has_admin_link_for_admin`: admin GET / → response contains `href="/admin"` somewhere (regression guard for D-18; the top-nav link stays).

    All five tests use the existing `client`, `seeded_admin_user`, `seeded_regular_user` fixtures and the `_require_nav_wired()` skip gate already in the file. Place new tests after the existing `test_authenticated_home_has_nav_bar_component` (line 126).
  </behavior>
  <action>Open tests/test_nav.py and append five new test functions per the `<behavior>` block above, after the existing `test_authenticated_home_has_nav_bar_component`. Add `import re` to the top-of-file imports (currently absent). For tests 1 and 4, scope the assertion to the bottom-nav region by extracting `<nav x-data="navBar".*?</nav>` with `re.search(..., re.DOTALL)` and asserting on the matched substring — a bare `r.text` scan is wrong because the top-nav Admin link survives per D-18. All tests call `_require_nav_wired()` first, mirroring existing tests. Tests MUST be added BEFORE the implementation in Task 2/3/4 so they go RED first (TDD per CLAUDE.md and Phase 17 plan structure). Do NOT install pytest in the container yet — the test-run step in Task 5 handles that. Do NOT modify the existing five tests.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && grep -c "def test_admin_home_has_no_admin_bottom_nav_tab\|def test_admin_config_page_has_admin_entry\|def test_non_admin_config_page_has_no_admin_entry\|def test_home_has_ai_bottom_nav_tab\|def test_top_nav_still_has_admin_link_for_admin" tests/test_nav.py</automated>
  </verify>
  <done>tests/test_nav.py contains five new test functions matching the names in `<behavior>`. `grep -c` returns 5. `import re` is present at module top. Running `pytest tests/test_nav.py --collect-only -q` (after pytest install in Task 5) reports >=11 collected tests (6 old + 5 new) — confirms no name collisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reshape base.html top + bottom nav (remove Admin bottom slot, add AI tab, mirror top-nav order)</name>
  <files>app/templates/base.html</files>
  <read_first>
    - app/templates/base.html lines 85-100 (top-nav block) and 240-295 (bottom-nav block)
    - .planning/phases/17-ia-restructure/17-PATTERNS.md MOD app/templates/base.html section
    - .planning/phases/17-ia-restructure/17-RESEARCH.md "Adding the AI bottom-nav slot in base.html" + "Code Examples"
  </read_first>
  <behavior>
    - Bottom nav rendered order after edit (left to right): Home / Log / AI / Config. The `{% if request.state.user.is_admin %}` Admin slot block (lines 279-289) is deleted in full.
    - AI tab slot uses the Heroicons sparkles outline SVG path (`d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"`), `<span class="text-xs">AI</span>`, `:class="activeTab === 'ai' ? 'text-espresso-700 dark:text-cream-100' : 'text-espresso-400 dark:text-espresso-400'"`, and is OUTSIDE any `{% if request.state.user.is_admin %}` (D-03: AI tab always visible).
    - Top nav (lines 91-98) gains `<a href="/ai" class="text-sm font-semibold text-espresso-600 dark:text-espresso-300 hover:text-espresso-900 dark:hover:text-cream-100">AI</a>` inserted between the Log link and the Config link. The existing `{% if request.state.user.is_admin %}<a href="/admin">Admin</a>{% endif %}` block stays at the end (D-18).
    - The comment at line 76 (`Bottom tab nav (<768px): Home / Log / Config / Admin tabs.`) is updated to read `Bottom tab nav (<768px): Home / Log / AI / Config tabs.` to keep documentation in sync.
    - No `<script>` tags added or removed in this task (Task 4 of plan 17-03 adds banner-dismiss.js). No new CSS files. No `nonce` changes.
  </behavior>
  <action>Edit app/templates/base.html. Step 1: in the top nav `<nav class="flex items-center gap-5">` block at lines 91-98, insert a new `<a href="/ai" ...>AI</a>` line between the existing `<a href="/brew">Log</a>` and `<a href="/config">Config</a>` links, using the same `text-sm font-semibold text-espresso-600 dark:text-espresso-300 hover:text-espresso-900 dark:hover:text-cream-100` classes. Leave the `{% if request.state.user.is_admin %}<a href="/admin">Admin</a>{% endif %}` block intact (D-18). Step 2: in the bottom-nav block (lines 244-291), delete the entire `{# Admin tab — hidden for non-admins (MOB-02) #}` block from `{% if request.state.user.is_admin %}` through `{% endif %}` (lines 279-289 inclusive). Insert a new AI tab slot between the Log tab close (line 268) and the Config tab open (line 269), using the exact slot shape from 17-PATTERNS.md "Bottom-nav slot pattern to copy" — the Heroicons sparkles outline path noted in `<behavior>`, `<span class="text-xs">AI</span>`, `:class="activeTab === 'ai' ? 'text-espresso-700 dark:text-cream-100' : 'text-espresso-400 dark:text-espresso-400'"`. The slot is NOT wrapped in any `is_admin` gate (D-03). Step 3: update the documentation comment at line 76 to reflect the new bottom-nav order `Home / Log / AI / Config tabs.`. Do NOT touch the iOS install banner (lines 310-326) or any other section.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && python -c "import re; t = open('app/templates/base.html', encoding='utf-8').read(); m = re.search(r'<nav x-data=\"navBar\".*?</nav>', t, re.DOTALL); assert m, 'bottom-nav block not found'; b = m.group(0); assert 'href=\"/ai\"' in b, 'AI tab missing from bottom nav'; assert '>AI<' in b, 'AI label missing'; assert 'href=\"/admin\"' not in b, 'Admin link must NOT appear in bottom nav (IA-01)'; assert 'activeTab === \\'ai\\'' in b, 'activeTab binding missing'; print('base.html bottom-nav OK')"</automated>
  </verify>
  <done>The bottom-nav block in base.html contains an AI tab slot with `href="/ai"`, `<span class="text-xs">AI</span>`, and the activeTab binding. The bottom-nav block contains zero `href="/admin"` references. The top-nav block at lines 91-98 contains a new `<a href="/ai">AI</a>` between Log and Config plus the unchanged `is_admin`-gated Admin link. The doc comment at line 76 reads `Home / Log / AI / Config tabs.`. The Python verify command above exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update nav-bar.js activeTab — add /ai branch, remove /admin branch</name>
  <files>app/static/js/alpine-components/nav-bar.js</files>
  <read_first>
    - app/static/js/alpine-components/nav-bar.js (full file)
    - .planning/phases/17-ia-restructure/17-PATTERNS.md MOD nav-bar.js section
    - .planning/phases/17-ia-restructure/17-RESEARCH.md "Adding /ai to the activeTab getter"
  </read_first>
  <behavior>
    - After edit, the `activeTab` getter returns `'ai'` for any pathname starting with `/ai` (e.g., `/ai`, `/ai/paste-rank`, `/ai/wishlist`, `/ai/wishlist/123/purchase`).
    - The `/ai` branch sits BEFORE the `/config` branch so that `/ai*` does NOT accidentally fall through to `'config'` (no path collision today, but D-05 spells the order explicitly).
    - The `/admin` branch (current line 29) is removed entirely. With the bottom-nav admin tab gone, there is no `:class="activeTab === 'admin' ..."` consumer; the top-nav admin link is a plain `<a>` and ignores `activeTab` (verified — `base.html:95-97`).
    - No other lines change. No new imports, no new methods, no new state.
  </behavior>
  <action>Edit app/static/js/alpine-components/nav-bar.js. Inside the `get activeTab()` getter (lines 22-31), insert a new line `if (p.startsWith('/ai')) return 'ai';` between the existing `if (p.startsWith('/brew')) return 'brew';` line and the `if (p.startsWith('/config') || ...) return 'config';` line. Delete the existing `if (p.startsWith('/admin')) return 'admin';` line (current line 29). Do NOT change the leading `home` branch, the `brew` branch, the `config` branch, or the trailing `return '';` fallback. Do NOT add a comment block (keep the inline pattern terse). The file ends at line 33; after edit it remains ~33 lines (one branch added, one removed).</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && python -c "t = open('app/static/js/alpine-components/nav-bar.js', encoding='utf-8').read(); assert \"p.startsWith('/ai')\" in t, '/ai branch missing'; assert \"return 'ai'\" in t, 'return ai missing'; assert \"p.startsWith('/admin')\" not in t, '/admin branch must be removed (D-05)'; assert \"return 'admin'\" not in t, 'return admin must be removed'; print('nav-bar.js OK')"</automated>
  </verify>
  <done>nav-bar.js contains exactly one `p.startsWith('/ai')` branch that returns `'ai'`, and zero references to `/admin` or `'admin'`. The branch order (home → brew → ai → config → fallback) matches D-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Append Administration section to pages/config_hub.html (D-17)</name>
  <files>app/templates/pages/config_hub.html</files>
  <read_first>
    - app/templates/pages/config_hub.html (full file — note line 52 closing `</div>` of catalog grid, line 54-56 start of `md:hidden` sign-out block)
    - .planning/phases/17-ia-restructure/17-PATTERNS.md MOD config_hub.html section + "Adding the admin entry" code example in RESEARCH
    - app/templates/base.html lines 279-289 (the shield-icon SVG path being reused — note this block is being deleted in Task 2; the path itself is preserved in the new fragment)
  </read_first>
  <behavior>
    - New section sits AFTER the closing `</div>` of the catalog grid (current line 52) and BEFORE the `<div class="md:hidden mt-8 ...">` mobile sign-out block (current line 56).
    - Section is wrapped in `{% if request.state.user.is_admin %}` … `{% endif %}` (D-17 — admin-gated).
    - Section is NOT itself `md:hidden`: admins on desktop reach Admin from this entry too.
    - Heading is `<h2 class="text-xl font-semibold mb-4 text-espresso-900 dark:text-cream-100">Administration</h2>`.
    - Content is a single `<a href="/admin">` tile reusing the catalog-card classes verbatim (`flex items-center gap-3 rounded-xl border border-espresso-200 dark:border-espresso-700 bg-cream-100 dark:bg-espresso-900 px-4 py-4 min-h-[44px] hover:bg-cream-200 dark:hover:bg-espresso-800 transition-colors`).
    - Shield SVG is the same 24x24 stroke-currentColor outline path used in the existing base.html admin tab (path `d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944..."`). Span label is `<span class="text-base font-semibold text-espresso-900 dark:text-cream-100">Admin</span>`.
    - Grid wrapper uses `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` matching the catalog grid above so the tile sits in a column-aligned grid even though only one entry exists today.
    - No badge text ("Admin only") — Claude's discretion in CONTEXT.md notes the `is_admin` gate already hides it from non-admins.
    - Page `<h1>` (line 10) stays as `Catalog` — D-04 says the planner MAY change it to `Config` but to minimize churn, leave it. (If a follow-up cycle wants the heading aligned with the route name, that's a one-line edit; not required for IA-01.)
  </behavior>
  <action>Edit app/templates/pages/config_hub.html. Between the closing `</div>` of the catalog grid at line 52 and the opening `<div class="md:hidden mt-8 ...">` at line 56, insert a new admin-gated section per the `<behavior>` block. The exact shape is documented in 17-RESEARCH.md "Adding the admin entry to config_hub.html" and 17-PATTERNS.md MOD config_hub.html section — reuse the shield SVG path from `base.html:285` (the path string in `<behavior>`). Add a leading `<div class="mt-8">` wrapper around the heading + grid so spacing is consistent with the existing `mt-8 border-t pt-6` mobile sign-out block but does NOT add a border-top (Admin entry is a primary section, not an afterthought). The `Administration` heading uses `text-espresso-900 dark:text-cream-100` so it matches the catalog page heading dark-mode treatment. Do NOT modify the existing catalog grid (lines 14-52) or the mobile sign-out block (lines 56-97).</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && python -c "import re; t = open('app/templates/pages/config_hub.html', encoding='utf-8').read(); assert 'Administration' in t, 'Administration heading missing'; assert re.search(r'{% if request\\.state\\.user\\.is_admin %}.*?Administration.*?href=\"/admin\".*?{% endif %}', t, re.DOTALL), 'Admin section not properly is_admin-gated (D-17)'; assert 'md:hidden' not in re.search(r'{% if request\\.state\\.user\\.is_admin %}.*?{% endif %}', t, re.DOTALL).group(0), 'Admin section must NOT be md:hidden (D-17)'; print('config_hub.html OK')"</automated>
  </verify>
  <done>config_hub.html contains a new `{% if request.state.user.is_admin %}` … `{% endif %}` block sitting between the catalog grid close and the `md:hidden` mobile sign-out block. The block contains the literal string `Administration` and exactly one `<a href="/admin">` tile. The block itself is NOT `md:hidden`. The page `<h1>` still reads `Catalog` (no churn).</done>
</task>

<task type="auto">
  <name>Task 5: Install pytest in container, copy test changes, run the new test_nav.py assertions, run ruff format + check, commit</name>
  <files>
    (no source files — runs commands inside the running coffee-snobbery container; reads project memory `docker-cp-into-container-nesting` + `snobbery-test-gate-runtime` + `executors-skip-ruff-ci-gates-both`)
  </files>
  <read_first>
    - CLAUDE.md "Working with the code" section (pytest install + docker compose cp file-level requirements)
    - .planning/phases/17-ia-restructure/17-VALIDATION.md "Test Infrastructure"
  </read_first>
  <acceptance_criteria>
    - `docker compose ps` shows coffee-snobbery healthy (pre-flight).
    - `docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx` exits 0 (idempotent if already installed).
    - `docker compose cp tests/test_nav.py coffee-snobbery:/app/tests/test_nav.py` succeeds (file-level cp, NOT dir-level — project memory `docker-cp-into-container-nesting` warns dir-level nests).
    - `docker compose cp app/templates/base.html coffee-snobbery:/app/app/templates/base.html`, same for `app/templates/pages/config_hub.html`, `app/static/js/alpine-components/nav-bar.js` — all four files copied individually.
    - `docker compose exec coffee-snobbery python -m pytest tests/test_nav.py -q -rs` collects >=11 tests and all five new test functions PASS. No test is skipped without an `_require_nav_wired()` reason. Exit 0.
    - `ruff format app/templates app/static/js/alpine-components tests/test_nav.py` (or `ruff format .` if simpler) — Python files only; templates and JS are untouched by ruff. `ruff format --check .` exits 0.
    - `ruff check .` exits 0 (project memory `executors-skip-ruff-ci-gates-both` — CI gates BOTH).
    - `git add app/templates/base.html app/static/js/alpine-components/nav-bar.js app/templates/pages/config_hub.html tests/test_nav.py && git commit` lands with message `feat(17-01): IA-01/IA-02 nav reshape — drop bottom Admin tab, add AI tab, add Config Admin entry`.
  </acceptance_criteria>
  <action>Pre-flight: verify the stack is up with `docker compose ps`. If down, `docker compose up -d` first. Step 1: ensure pytest + deps are present in the container — `docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx` (idempotent; reuses cache if installed). Step 2: copy the four modified files into the container one at a time via `docker compose cp <local> coffee-snobbery:<container>` (file-level, NOT dir-level — per project memory `docker-cp-into-container-nesting`). Step 3: run the focused test command `docker compose exec coffee-snobbery python -m pytest tests/test_nav.py -q -rs` and confirm all ten (5 pre-existing + 5 new) tests pass. If a new test fails, fix the source (Tasks 2/3/4) and re-run — do NOT loosen the test. Step 4: run `ruff format .` and `ruff check .` from the project root (host shell, not container — ruff is a dev tool). Address any formatting diffs. Step 5: stage exactly the four files this plan modifies and commit with the conventional-commit subject line above. Do NOT commit `.planning/` files here (the orchestrator handles `commit_docs` separately). Do NOT push.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose exec coffee-snobbery python -m pytest tests/test_nav.py -q -rs 2>&1 | tail -20 && ruff check . && git log --oneline -1</automated>
  </verify>
  <done>All eleven tests in tests/test_nav.py pass (6 pre-existing + 5 new). `ruff check .` exits 0. `ruff format --check .` exits 0. The latest commit's subject begins with `feat(17-01):` and includes the four files listed in `files_modified`.</done>
</task>

</tasks>

<verification>
After all five tasks complete:

1. **Bottom-nav reshape (IA-02 + D-01/D-02/D-03/D-05):** `docker compose cp` the four files (if not already), then `docker compose exec coffee-snobbery python -m pytest tests/test_nav.py::test_home_has_ai_bottom_nav_tab tests/test_nav.py::test_admin_home_has_no_admin_bottom_nav_tab -x -v` — both pass.

2. **Top-nav (D-18):** `pytest tests/test_nav.py::test_top_nav_still_has_admin_link_for_admin -x` — passes (regression guard).

3. **Config admin entry (IA-01 + D-17):** `pytest tests/test_nav.py::test_admin_config_page_has_admin_entry tests/test_nav.py::test_non_admin_config_page_has_no_admin_entry -x` — both pass.

4. **activeTab routing (D-05):** spot-check by `grep -c "p.startsWith('/ai')\|p.startsWith('/admin')" app/static/js/alpine-components/nav-bar.js` → expects `1` (only the /ai branch present).

5. **Full nav module isolation:** `docker compose exec coffee-snobbery python -m pytest tests/test_nav.py -q -rs` — all eleven tests pass, zero skips outside the `_require_nav_wired()` skip gate.

6. **No collateral test damage:** `docker compose exec coffee-snobbery python -m pytest tests/routers/test_home.py -q -rs` — expect zero new failures (this plan does not touch home — but the bottom-nav change rides in `base.html` which home extends; the existing home tests do not assert on bottom-nav content, only that the navBar component is mounted, so they should pass).

7. **Style gates:** `ruff format --check .` and `ruff check .` — both exit 0.

The on-device PWA cache verification (IA-05) is handled by plan 17-05 after all wave-2 templates land — do not attempt it from this plan.
</verification>

<success_criteria>
- The bottom nav at <768px shows exactly four tabs in order Home / Log / AI / Config, with no admin tab visible to any user role.
- The top nav at >=768px shows Home / Log / AI / Config plus an admin-gated Admin link (D-18).
- `/config` shows a new admin-gated `Administration` section between the catalog grid and the mobile sign-out block; the section is visible on both mobile and desktop for admins, invisible to non-admins.
- nav-bar.js routes any `/ai*` pathname to the active 'ai' tab styling; the `/admin` branch is fully removed.
- All five new tests in tests/test_nav.py pass alongside the five pre-existing tests.
- ruff format/check both pass; commit lands on main.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ia-restructure/17-01-SUMMARY.md` with:
- Files touched (4 source + 1 test)
- Five test functions added with brief descriptions
- Confirmation that D-01 / D-02 / D-04 / D-05 / D-17 / D-18 are all reflected in the rendered nav
- Note that the IA-05 on-device PWA cache check is deferred to plan 17-05 (cannot be verified until ALL phase 17 template/JS changes land + container rebuilds with a fresh `__BUILD_HASH__`)
- Note that the `<h1>Catalog</h1>` heading was NOT changed to `Config` (D-04 left this to planner; chose minimum churn; a later cycle can rename if desired)
</output>
