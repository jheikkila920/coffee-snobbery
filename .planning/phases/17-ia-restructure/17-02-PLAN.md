---
phase: 17-ia-restructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/analytics.py
  - app/routers/home.py
  - app/templates/pages/home.html
  - tests/services/test_analytics.py
  - tests/routers/test_home.py
autonomous: true
requirements: [IA-03, IA-04, IA-06]
must_haves:
  truths:
    - "D-06 (Home sections in order: action row → Recent brews → Not tried yet → Top Coffees eager → \"See AI recommendations →\" link → Wishlist link/card): home.html renders the six sections in exactly that order; action row contains Guided Brew + Log session + Quick rate, NO Admin button"
    - "D-07 (Removed from home: ai-recommendation hero, AI tools section, Preference Profile, Top Flavor Descriptors, Sweet Spots, cold-start meter, Admin button): home.html no longer contains `hx-get=\"/home/cards/ai-recommendation\"`, `hx-get=\"/home/cards/preference-profile\"`, `hx-get=\"/home/cards/flavor-descriptors\"`, `hx-get=\"/home/cards/sweet-spots\"`, no AI tools section, no `{% include \"fragments/home/_cold_start.html\" %}`, no `<a href=\"/admin\">` inside the action row header"
    - "D-08 (Top Coffees eager on home, not lazy): home.html renders Top Coffees inline via `{% include %}` of a new `fragments/home/top_coffees_eager.html` (or by directly inlining the top_coffees fragment's body) using rows passed in view context; no `hx-get=\"/home/cards/top-coffees\"` on home"
    - "D-09 (Top Coffees on home uses no-floor variant): `home_shell` in routers/home.py calls `analytics.get_top_coffees(db, user.id, min_sessions=0)`; the new keyword arg defaults to `2` so the existing `/home/cards/top-coffees` fragment endpoint keeps the >=2 floor"
    - "D-10 (Home <h1> is personalized greeting): home.html renders `<h1>{{ greeting }}</h1>`; the greeting is derived server-side from `app.config.settings.APP_TIMEZONE` (NOT a new `APP_TZ` env var — APP_TIMEZONE already exists in Settings); buckets are 05:00-11:59 morning, 12:00-16:59 afternoon, 17:00-04:59 evening; fallback to `Home` if `user` is missing"
    - "D-11 (Cold-start meter does NOT live on home): home.html removes the entire `{% if not gate.gate_open %} … {% else %} … {% endif %}` block; both above-gate and below-gate home users see the same composition; the gate dict stays available for plan 17-04 to consume on /ai"
    - "IA-03 (AI surfaces removed from home): the four `hx-get=\"/home/cards/<ai-fragment>\"` references and the AI tools section are all gone from home.html (the fragment endpoints in routers/home.py remain — they'll be re-mounted from pages/ai.html in plan 17-04)"
    - "IA-04 (Home simplified to primary action affordances + lightweight recency): home.html shows greeting + three-button action row + Recent brews + Not tried yet + Top Coffees (eager) + \"See AI recommendations →\" link + Wishlist card"
    - "IA-06 (Top Coffees: top 5 by rating, no minimum-star / minimum-session floor): the no-floor query returns up to 5 coffees ranked by avg rating where ANY rated session exists (single-session coffees included); `get_top_coffees(db, user_id, min_sessions=0)` returns coffees with `session_count == 1`"
  artifacts:
    - path: "app/services/analytics.py"
      provides: "get_top_coffees gains keyword-only `min_sessions: int = 2` parameter; HAVING clause applied only when min_sessions > 0; default preserves the existing /home/cards/top-coffees floor"
      contains: "def get_top_coffees(db: Session, user_id: int, *, min_sessions: int = 2)"
    - path: "app/routers/home.py"
      provides: "home_shell adds top_coffees + greeting to view context; calls get_top_coffees with min_sessions=0; greeting helper derive_greeting + APP_TIMEZONE-aware now()"
      contains: "min_sessions=0"
    - path: "app/templates/pages/home.html"
      provides: "Full composition rewrite per D-06; eager Top Coffees; personalized greeting; no AI surfaces; no cold-start meter; no Admin button"
      min_lines: 80
    - path: "tests/services/test_analytics.py"
      provides: "Three new tests for the min_sessions parameter (no-floor includes single-session, no-floor caps at 5, default behavior preserved)"
    - path: "tests/routers/test_home.py"
      provides: "Seven new tests covering D-06 composition, D-07 removals, D-08 eager render, D-09 no-floor in home, D-10 greeting, D-11 home invariant across gate states, IA-04 see-AI link"
  key_links:
    - from: "app/routers/home.py"
      to: "app/services/analytics.py"
      via: "analytics.get_top_coffees(db, user.id, min_sessions=0) call inside home_shell"
      pattern: "get_top_coffees\\(.*min_sessions=0"
    - from: "app/templates/pages/home.html"
      to: "app/routers/home.py"
      via: "view context keys: greeting (str), top_coffees (list[Row]), top_coffees_all_unrated (bool — passed to detect 'no rated coffees yet' UI state)"
      pattern: "\\{\\{ greeting \\}\\}"
    - from: "app/templates/pages/home.html"
      to: "/ai"
      via: "<a href=\"/ai\"> link with 'See AI recommendations' text below Top Coffees"
      pattern: "href=\"/ai\"[^>]*>[^<]*See AI recommendations"
    - from: "app/templates/pages/home.html"
      to: "/cafe-logs/new"
      via: "<a href=\"/cafe-logs/new\">Quick rate</a> action button (symmetric with /brew header from Phase 16 D-09)"
      pattern: "href=\"/cafe-logs/new\""
    - from: "app/templates/pages/home.html"
      to: "/ai/wishlist"
      via: "Wishlist link/card at bottom of home"
      pattern: "href=\"/ai/wishlist\""
---

<objective>
Rewrite the home page composition per D-06..D-11: strip every AI surface and the cold-start meter, add a personalized server-side greeting, render Top Coffees eagerly with the no-floor variant, and replace the AI hero + AI tools blocks with a single small "See AI recommendations →" link + a Wishlist card. Touches three source files (analytics.py, home.py, home.html) and extends two test modules. No file overlap with 17-01 — plans run in parallel.

Purpose: IA-03 / IA-04 / IA-06 are the home-page reduction half of Phase 17. The AI page shell in 17-04 will re-mount the same fragment endpoints (URLs unchanged), but home gets there first by dropping them, so the page reflects the new "primary action affordances + lightweight recency" identity before any /ai page exists.

Output: A simpler home.html (greeting + action row + Recent brews + Not tried yet + Top Coffees eager + See AI link + Wishlist), a backward-compatible no-floor parameter on `analytics.get_top_coffees`, a small `derive_greeting` helper, and a Wave-0 test suite that pins the new composition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-ia-restructure/17-CONTEXT.md
@.planning/phases/17-ia-restructure/17-RESEARCH.md
@.planning/phases/17-ia-restructure/17-PATTERNS.md
@.planning/phases/17-ia-restructure/17-VALIDATION.md

<interfaces>
analytics.get_top_coffees CURRENT signature (app/services/analytics.py:48):
```python
def get_top_coffees(db: Session, user_id: int) -> list[Row]:
    # returns rows with .id, .name, .avg_rating, .session_count; min 2 rated sessions; max 5; tie-broken by avg DESC then count DESC
```
Currently called from ONE site: `app/routers/home.py:145` `card_top_coffees` (positional args only — `analytics.get_top_coffees(db, user.id)`).

Existing seed helpers in tests/services/test_analytics.py (verified during planning):
- `def _seed_analytics_scenario(db, *, username: str) -> tuple[int, int, int]` at line 56 — returns (user_id, coffee3_id, archived_coffee_id); seeds 4 coffees, 6 rated brew sessions, 6 distinct flavor notes; user is gate-cleared.
- `def _seed_cold_start(db, *, username: str) -> int` at line 235 — seeds 1 session + 2 distinct notes (below gate). Returns user_id.
- `def _seed_all_unrated(db, *, username: str) -> int` at line 285 — seeds user with unrated-only coffees.
- `def _seed_cafe_into_scenario(...)` at line 348 — extends a scenario with cafe-log data.

The three new no-floor tests need a NEW dedicated helper because none of the existing ones produce the specific shape "N coffees each with exactly one rated brew session". Name the new helper `def _seed_top_coffees_no_floor(db, *, username: str, coffees: list[tuple[str, float]]) -> int` — accepts a list of (coffee_name, rating) tuples, creates one User + one Roaster + N Coffees + N rated BrewSessions (one per coffee), commits, returns user_id. Place the helper alongside the existing seed helpers (~line 348+). Keep `_seed_analytics_scenario` untouched for backward compatibility with the existing analytics tests.

analytics.get_cold_start_counts return shape (app/services/analytics.py:378):
```python
{
  "sessions": int,
  "distinct_notes": int,
  "sessions_needed": int,
  "notes_needed": int,
  "gate_open": bool,
}
```
The gate dict is still computed and passed into the home view context (plans 17-03 / 17-04 may need it later) — only the TEMPLATE BRANCH on `gate.gate_open` is removed from home.html per D-11.

home_shell view context (app/routers/home.py:40-66) CURRENT:
```python
return templates.TemplateResponse(
    request=request,
    name="pages/home.html",
    context={"gate": gate, "recent_brews": recent_brews, "unrated_coffees": unrated_coffees},
)
```

Settings.APP_TIMEZONE (app/config.py:55) EXISTS:
```python
APP_TIMEZONE: str = "America/Chicago"  # IANA; consumed by APScheduler (Phase 8)
```
Reuse this. Do NOT introduce a new `APP_TZ` env var (research recommendation accepted as deviation: APP_TIMEZONE is already there and consumed by APScheduler; one source of truth).

Phase 16 brew header has a Quick rate button linking to `/cafe-logs/new` (16-D-09); home's action row mirrors that target so the entry is symmetric.

Existing fragments that home.html currently includes:
- `fragments/home/recent_brews.html` (eager) — KEEP, unchanged
- `fragments/home/_cold_start.html` (eager when below gate) — REMOVE the include from home; the file itself is moved by plan 17-04, NOT this plan
- `fragments/home/unrated_coffees.html` (lazy, via /home/cards/unrated-coffees) — KEEP

Fragment endpoints in app/routers/home.py to LEAVE INTACT (plan 17-04 reuses them from /ai):
- /home/cards/top-coffees (lines 133-151) — keeps existing >=2 floor when called without kwarg
- /home/cards/preference-profile (lines 154-172)
- /home/cards/flavor-descriptors (lines 175-191)
- /home/cards/sweet-spots (lines 194-217)
- /home/cards/ai-recommendation (lines 220-288)
- /home/cards/recent-brews (lines 69-92), /home/cards/unrated-coffees (lines 95-112)

`fragments/home/top_coffees.html` (existing, used by /home/cards/top-coffees) is the row-rendering template. For the eager render on home, you can EITHER:
- (A) include it directly in home.html with `{% include "fragments/home/top_coffees.html" with rows=top_coffees, all_unrated=top_coffees_all_unrated %}`, OR
- (B) inline the rows block in home.html.
Choice (A) is preferred — single source of truth for the row markup, no template-rendering drift.

The greeting helper should live where home_shell can use it cleanly. Options:
- (A) Inline a small `derive_greeting(username: str | None, *, now: datetime | None = None) -> str` function at module-bottom of app/routers/home.py.
- (B) New file `app/utils/greeting.py`.
Choice (A) is preferred — single consumer, no new module overhead, easy to find at code review.

The `derive_greeting` signature MUST accept an injectable `now` parameter (no-default) so tests can freeze time without monkeypatching `datetime.datetime.now`.

Wishlist card on home links to `/ai/wishlist` (existing route, app/routers/ai.py:76 — unchanged by Phase 17). The card uses the standard home card section shape (`rounded-lg border border-espresso-200 bg-cream-100 p-4 dark:bg-espresso-900 dark:border-espresso-800`) with `<h2>Wishlist</h2>` heading + a one-line copy + a button-styled `<a>` link.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/services/test_analytics.py + tests/routers/test_home.py with the Wave-0 assertions for the new home composition + no-floor variant</name>
  <files>tests/services/test_analytics.py, tests/routers/test_home.py</files>
  <read_first>
    - tests/services/test_analytics.py (existing get_top_coffees tests — find via `grep -n "get_top_coffees" tests/services/test_analytics.py`)
    - tests/routers/test_home.py lines 1-50 (skip-gate + seed pattern)
    - tests/conftest.py lines 574-610 (fixtures)
    - .planning/phases/17-ia-restructure/17-VALIDATION.md Per-Task Verification Map (rows IA-03 / IA-04 / IA-06)
  </read_first>
  <behavior>
    In tests/services/test_analytics.py, add three tests:
    - `test_get_top_coffees_no_floor_includes_single_session_coffees`: seed a user with one coffee + one rated brew (rating=4.5). Call `get_top_coffees(db, user_id, min_sessions=0)` — expect 1 row returned (single-session coffee included). Call `get_top_coffees(db, user_id)` (default) — expect 0 rows (the >=2 floor excludes it). Confirms D-09 backward compatibility.
    - `test_get_top_coffees_no_floor_caps_at_5`: seed a user with 7 coffees each with one rated brew. `get_top_coffees(db, user_id, min_sessions=0)` returns exactly 5 rows. Confirms IA-06 still caps at 5.
    - `test_get_top_coffees_default_still_enforces_min_two`: seed a user with two coffees, coffee A has 1 rated brew, coffee B has 2 rated brews. `get_top_coffees(db, user_id)` returns 1 row (B only). Regression guard.

    In tests/routers/test_home.py, add seven tests:
    - `test_home_renders_personalized_greeting`: any authed user GET / — the response body matches a regex like `<h1[^>]*>Good (morning|afternoon|evening), [^<]+</h1>`. Time-of-day branch coverage is covered by the service-level unit tests below — this test just asserts the H1 follows the expected shape.
    - `test_home_does_not_mount_ai_cards`: GET / response contains zero `hx-get="/home/cards/ai-recommendation"`, `hx-get="/home/cards/preference-profile"`, `hx-get="/home/cards/flavor-descriptors"`, `hx-get="/home/cards/sweet-spots"` substrings. Confirms D-07 / IA-03.
    - `test_home_renders_top_coffees_eagerly`: seed an admin user with two rated coffees (each two sessions) — GET / response contains the coffee names rendered inline (NOT inside an `hx-get="/home/cards/top-coffees"` placeholder). The body MUST NOT contain `hx-get="/home/cards/top-coffees"`. Confirms D-08.
    - `test_home_top_coffees_no_floor_integration`: seed an admin with ONE coffee + ONE rated brew (rating=4.0) — GET / response contains that coffee's name in the Top Coffees section. Confirms IA-06 / D-09 wired end-to-end.
    - `test_home_action_row_has_no_admin_button_for_admin`: admin GET / — assert that within the `<header>` block at the top of `<main>`, there is no `<a href="/admin">`. (Use `re.search(r'<header[\s\S]*?</header>', body, ...)` then assert.) Confirms D-07 Admin button removal from home action row. (Note: the top-nav admin link still appears OUTSIDE the `<main>` element per D-18.)
    - `test_home_action_row_has_quick_rate`: GET / response `<header>` block contains `<a href="/cafe-logs/new">Quick rate</a>` (or anchor text "Quick rate" with the cafe-logs href). Confirms D-06.
    - `test_home_has_see_ai_recommendations_link`: GET / response contains `<a href="/ai">` with anchor text containing `See AI recommendations`. Confirms D-06 / IA-04 discovery breadcrumb.

    All tests use existing fixtures and skip-gate patterns from the surrounding tests in each file. For seed helpers, reuse `_seed_analytics_scenario` from tests/services/test_analytics.py (per test_home.py's own header comment line 16). For time-frozen assertions on the greeting H1, you do NOT need to freeze time — the regex matches any of the three buckets.

    A unit test for `derive_greeting` itself lives ALSO in tests/routers/test_home.py (or a new tests/routers/test_greeting.py — planner's call; keep it in test_home.py to avoid a new module):
    - `test_derive_greeting_returns_morning_before_noon`, `test_derive_greeting_returns_afternoon_between_12_and_17`, `test_derive_greeting_returns_evening_at_or_after_17`, `test_derive_greeting_returns_evening_before_5am`, `test_derive_greeting_falls_back_to_home_when_username_missing`. Each constructs a frozen datetime with `tzinfo=ZoneInfo("America/Chicago")` and asserts the return string. Five sub-tests; pack into one test function with `pytest.mark.parametrize` if it tightens the suite — planner's choice.
  </behavior>
  <action>Open tests/services/test_analytics.py — locate the existing `get_top_coffees` test block (find via grep; tests live near the top after skip-gate helpers). Append the three new test functions per `<behavior>`. For the three no-floor tests, ADD a new seed helper at the bottom of the helpers block (~line 348+) with the exact signature `def _seed_top_coffees_no_floor(db, *, username: str, coffees: list[tuple[str, float]]) -> int` per the <interfaces> block above — it creates one User + one Roaster + len(coffees) Coffees + one rated BrewSession per coffee (with the supplied rating) and returns user_id. Do NOT modify the existing `_seed_analytics_scenario` (line 56) — it remains the single source for the other analytics tests. Open tests/routers/test_home.py — append the seven home composition tests after the last existing test function. Add `from datetime import datetime` and `from zoneinfo import ZoneInfo` to imports (zoneinfo may already be available via Python 3.12 stdlib). For the `test_home_action_row_has_no_admin_button_for_admin` test, scope the assertion to the `<main>...</main>` block OR the first `<header>` block — explicitly NOT a bare `r.text` scan (the top-nav admin link is outside `<main>` and D-18 preserves it). Use the same `_require_nav_wired` style skip gate from test_nav.py (or the existing `_require_postgres` + `_require_analytics_tables` from test_home.py). Pack the five `derive_greeting` unit tests into a single parametrized function `test_derive_greeting_buckets` if the executor prefers; otherwise five functions are fine. These tests will FAIL until Tasks 2-4 land (TDD red).</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && grep -c "def test_get_top_coffees_no_floor_includes_single_session_coffees\|def test_get_top_coffees_no_floor_caps_at_5\|def test_get_top_coffees_default_still_enforces_min_two" tests/services/test_analytics.py && grep -c "def test_home_renders_personalized_greeting\|def test_home_does_not_mount_ai_cards\|def test_home_renders_top_coffees_eagerly\|def test_home_top_coffees_no_floor_integration\|def test_home_action_row_has_no_admin_button_for_admin\|def test_home_action_row_has_quick_rate\|def test_home_has_see_ai_recommendations_link" tests/routers/test_home.py</automated>
  </verify>
  <done>tests/services/test_analytics.py has 3 new test functions named per `<behavior>` (grep -c == 3). tests/routers/test_home.py has 7 new test functions per `<behavior>` (grep -c == 7). At least 1 `derive_greeting` unit test (parametrized or split) exists in tests/routers/test_home.py. Running `--collect-only` reports both modules collect the new tests without name collisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `min_sessions: int = 2` keyword-only parameter to analytics.get_top_coffees</name>
  <files>app/services/analytics.py</files>
  <read_first>
    - app/services/analytics.py lines 43-75 (current get_top_coffees implementation)
    - .planning/phases/17-ia-restructure/17-PATTERNS.md MOD analytics.py section
    - .planning/phases/17-ia-restructure/17-RESEARCH.md "Adding the no-floor parameter"
  </read_first>
  <behavior>
    - New signature: `def get_top_coffees(db: Session, user_id: int, *, min_sessions: int = 2) -> list[Row]:`
    - The `*` marker makes `min_sessions` keyword-only, preserving the positional call shape used by the existing `card_top_coffees` caller at app/routers/home.py:145.
    - The HAVING clause `.having(func.count(BrewSession.id) >= min_sessions)` is applied ONLY when `min_sessions > 0` (per `<interfaces>`/RESEARCH). When `min_sessions == 0`, the HAVING clause is omitted entirely, so coffees with one rated session are eligible.
    - The docstring updates to document the new parameter, the IA-06 / D-09 use case, and the unchanged tie-break behavior.
    - All other behavior preserved: limit 5, NULL ratings excluded, tie-broken by avg DESC then count DESC, no UNION with cafe logs.
  </behavior>
  <action>Edit app/services/analytics.py `get_top_coffees` (lines 48-74). Change the signature to `def get_top_coffees(db: Session, user_id: int, *, min_sessions: int = 2) -> list[Row]:`. Update the docstring to mention the new parameter and reference IA-06 / D-09 / phase 17. Restructure the SELECT chain so `.having(func.count(BrewSession.id) >= min_sessions)` is conditional — build the `stmt` without the HAVING, then `if min_sessions > 0: stmt = stmt.having(func.count(BrewSession.id) >= min_sessions)` BEFORE the `.order_by(...).limit(5)` chain (HAVING must come before ORDER BY in SQL). Do NOT touch any other function in analytics.py. Do NOT change the return type. The existing caller in home.py (line 145) keeps working because positional args + keyword default preserves the >=2 floor.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose cp app/services/analytics.py coffee-snobbery:/app/app/services/analytics.py && docker compose cp tests/services/test_analytics.py coffee-snobbery:/app/tests/services/test_analytics.py && docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py -q -rs -k "get_top_coffees"</automated>
  </verify>
  <done>analytics.get_top_coffees signature matches the new shape with a keyword-only `min_sessions: int = 2`. The three new tests from Task 1 pass: `test_get_top_coffees_no_floor_includes_single_session_coffees`, `test_get_top_coffees_no_floor_caps_at_5`, `test_get_top_coffees_default_still_enforces_min_two`. All pre-existing get_top_coffees tests still pass (no regression). The HAVING clause is gated on `min_sessions > 0`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add derive_greeting helper + wire greeting + top_coffees into home_shell view context</name>
  <files>app/routers/home.py</files>
  <read_first>
    - app/routers/home.py full file (40-line home_shell + the rest for import patterns)
    - app/config.py lines 1-70 (Settings class, note APP_TIMEZONE already exists)
    - .planning/phases/17-ia-restructure/17-RESEARCH.md "Personalized greeting (server-side, view context)" + "Pitfall A"
    - .planning/phases/17-ia-restructure/17-PATTERNS.md "Personalized greeting — server-side template context"
  </read_first>
  <behavior>
    - New helper `derive_greeting(username: str | None, *, now: datetime | None = None) -> str` lives at the bottom of app/routers/home.py (or as a private module-level function above `home_shell`, planner's discretion — keeping it inside home.py avoids a new module).
    - Defaults: `now=None` → `datetime.now(ZoneInfo(settings.APP_TIMEZONE))`. Buckets: hour in `[5, 12) → morning`, `[12, 17) → afternoon`, otherwise → evening. Username missing/empty → return literal `"Home"` (D-10 defensive fallback).
    - Return strings: `f"Good morning, {username}"`, `f"Good afternoon, {username}"`, `f"Good evening, {username}"`. Username is passed straight through — Jinja autoescapes at render-time per the project invariant; the helper does NOT pre-escape.
    - `home_shell` view context gains: `greeting=derive_greeting(user.username)`, `top_coffees=analytics.get_top_coffees(db, user.id, min_sessions=0)` (D-09 no-floor), `top_coffees_all_unrated=(not top_coffees and not _has_rated_sessions(db, user.id))` (mirror the all-unrated detection in `card_top_coffees`).
    - The existing `gate` context value STAYS — plan 17-04 will move the cold-start meter to /ai and plan 17-03 will read gate for the banner conditional if needed. Removing it from home_shell would break those plans' shared shape. Just because home.html no longer renders the meter doesn't mean the context value disappears.
    - Imports: add `from datetime import datetime` and `from zoneinfo import ZoneInfo` to the top-of-file imports; add `from app.config import settings`.
    - The `ai_key_present` context value is NOT added in this plan — plan 17-03 (DIST-07 banner) wires it. Keeping the concern separated minimizes the diff for review.
  </behavior>
  <action>Edit app/routers/home.py. Step 1: add imports near the existing import block (lines 22-35): `from datetime import datetime` and `from zoneinfo import ZoneInfo` (`zoneinfo` is stdlib in Python 3.12), `from app.config import settings`. Step 2: at module bottom (after the last route handler at line 289 or earlier as a private helper), add the `derive_greeting` function per `<behavior>`. Step 3: in `home_shell` (lines 40-66), after the existing `unrated_coffees = analytics.get_unrated_coffees(...)` line, add the three new lines: `top_coffees = analytics.get_top_coffees(db, user.id, min_sessions=0)`, `top_coffees_all_unrated = not top_coffees and not _has_rated_sessions(db, user.id)`, `greeting = derive_greeting(user.username)`. Step 4: extend the `context={...}` dict passed to `templates.TemplateResponse` with `"top_coffees": top_coffees, "top_coffees_all_unrated": top_coffees_all_unrated, "greeting": greeting`. Do NOT remove `gate` from the context. Do NOT change any other handler in home.py.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose cp app/routers/home.py coffee-snobbery:/app/app/routers/home.py && docker compose cp tests/routers/test_home.py coffee-snobbery:/app/tests/routers/test_home.py && docker compose exec coffee-snobbery python -m pytest tests/routers/test_home.py -q -rs -k "derive_greeting or personalized_greeting or top_coffees_no_floor_integration"</automated>
  </verify>
  <done>app/routers/home.py contains the new `derive_greeting` helper with the four time-bucket cases. `home_shell` passes `greeting`, `top_coffees`, `top_coffees_all_unrated` into the render context. Settings.APP_TIMEZONE is the TZ source. The five (or 1-parametrized) `derive_greeting` unit tests pass. `test_home_renders_personalized_greeting` and `test_home_top_coffees_no_floor_integration` pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Rewrite pages/home.html composition — greeting H1, action row, eager Top Coffees, See-AI link, Wishlist card; remove all AI surfaces and cold-start meter</name>
  <files>app/templates/pages/home.html</files>
  <read_first>
    - app/templates/pages/home.html full file (current composition)
    - app/templates/fragments/home/top_coffees.html (the row-rendering fragment to include eagerly)
    - .planning/phases/17-ia-restructure/snippets/ai-surfaces-pre-17-02.html (the AI markup blocks recovered for plan 17-04 — verify presence before deletion)
    - .planning/phases/17-ia-restructure/17-PATTERNS.md MOD home.html section
    - .planning/phases/17-ia-restructure/17-RESEARCH.md "Home reduction" + Pattern 3 + Pattern 4
  </read_first>
  <behavior>
    Final home.html composition (top to bottom, inside `<main class="mx-auto max-w-6xl px-6 py-12">`):

    1. `<header class="flex flex-wrap items-center justify-between gap-4 mb-6">`
       - `<h1 class="text-2xl font-semibold">{{ greeting }}</h1>`
       - `<div class="flex items-center gap-3">` containing exactly three action buttons (no `is_admin` Admin button — removed per D-07):
         - `<a href="/recipes">Guided Brew</a>` with the existing flex-1 border-button shape
         - `<a href="/brew/new">Log session</a>` with the existing flex-1 espresso-700 filled-button shape
         - NEW `<a href="/cafe-logs/new">Quick rate</a>` using the SAME flex-1 border-button shape as Guided Brew (matches Phase 16 D-09 brew header)

    2. `<div class="space-y-8">` wrapping all main content sections in order:
       - `<section aria-labelledby="recent-brews-heading" ...>` Recent brews — eager include of `fragments/home/recent_brews.html` (UNCHANGED from current home.html lines 41-45).
       - `<section aria-labelledby="unrated-heading" ...>` Not tried yet — lazy `hx-get="/home/cards/unrated-coffees"` (UNCHANGED from current lines 47-59).
       - `<section aria-labelledby="top-coffees-heading" ...>` Top Coffees — EAGER include of `fragments/home/top_coffees.html` with `rows=top_coffees` and `all_unrated=top_coffees_all_unrated`. No `hx-get` placeholder. Below this section's closing tag, a small inline link: `<p class="mt-3 text-sm"><a href="/ai" class="text-espresso-700 dark:text-cream-200 hover:underline">See AI recommendations →</a></p>` — small text, not a banner, per D-06.
       - `<section aria-labelledby="wishlist-heading" ...>` Wishlist — NEW card with the standard home card section shape. H2 `<h2 id="wishlist-heading" class="text-xl font-semibold mb-4">Wishlist</h2>`, one-line copy `<p class="text-base mb-4">Coffees to try next.</p>`, button-styled `<a href="/ai/wishlist" class="rounded border border-espresso-300 dark:border-espresso-600 px-4 py-2 text-base font-semibold min-h-[44px] inline-flex items-center hover:bg-espresso-100 dark:hover:bg-espresso-800">Open wishlist</a>`. Wishlist link target is `/ai/wishlist` (existing route per Phase 7 / app/routers/ai.py:76).

    Removed (D-07):
    - The `{% if not gate.gate_open %} {% include "_cold_start.html" %} {% else %} ... {% endif %}` wrapper (lines 61-173 current) — gone entirely. NO `{% include "fragments/home/_cold_start.html" %}` remains anywhere in home.html.
    - The AI hero `hx-get="/home/cards/ai-recommendation"` section (current lines 71-83).
    - The AI tools section with paste-rank / wishlist links + equipment form (current lines 85-115). Note Wishlist returns as its OWN card per D-06 (item 6) — that's NOT a duplicate, it's a relocated affordance.
    - The Preference Profile / Top Flavor Descriptors / Sweet Spots lazy cards (current lines 133-170).
    - The Admin button inside the action row (current lines 17-23).

    No DIST-07 banner include in this task — plan 17-03 adds the include. This task SHOULD leave a Jinja comment placeholder at the top of `<main>` reading `{# DIST-07 banner include — plan 17-03 #}` to signal where the banner will land. Plan 17-03 will replace this comment with `{% include "fragments/ai_key_setup_banner.html" %}` once that fragment exists. The placeholder comment costs nothing and avoids a merge conflict.
  </behavior>
  <action>Step 0 (pre-deletion side-effect, NO commit): the snippet file `.planning/phases/17-ia-restructure/snippets/ai-surfaces-pre-17-02.html` is staged by the planner during plan-phase as a stable recovery source for plan 17-04 Task 6 (it contains the AI hero + AI tools + Preference Profile + Flavor Descriptors + Sweet Spots blocks being deleted here). Confirm the file exists with `ls .planning/phases/17-ia-restructure/snippets/ai-surfaces-pre-17-02.html` before proceeding. If the file is missing (e.g., partial revision), copy the relevant blocks from the current home.html into that path before the deletion — plan 17-04 reads from it, NOT from git history. Do NOT commit the snippet (it is a planning working file under .planning/snippets/).

Rewrite app/templates/pages/home.html per the `<behavior>` composition above. Preserve the existing `{% extends "base.html" %}` and `{% block page_title %}Home{% endblock %}` lines (top-of-file structure unchanged). Replace the header block (lines 9-35) with the new greeting H1 + three-button action row. Replace the `<div class="space-y-8">` body (lines 37-175) with the four sections (Recent brews, Not tried yet, Top Coffees eager + See-AI link, Wishlist). The Top Coffees eager include uses the existing `fragments/home/top_coffees.html` template — pass `rows` and `all_unrated` via `{% include "fragments/home/top_coffees.html" with context %}` (Jinja's `with context` semantic) OR by relying on the parent context (`top_coffees` and `top_coffees_all_unrated` are in scope from home_shell — pass them as `rows` and `all_unrated` using `{% with rows=top_coffees, all_unrated=top_coffees_all_unrated %}{% include "fragments/home/top_coffees.html" %}{% endwith %}` if the fragment templates expect those exact variable names). Verify the fragment's variable names by reading `fragments/home/top_coffees.html` BEFORE writing the include. Insert a Jinja comment `{# DIST-07 banner include — plan 17-03 will replace this comment with {% include "fragments/ai_key_setup_banner.html" %} #}` at the top of `<main>` (right after the `<main ...>` opening tag, before the `<header>`). Do NOT add ANY `<script>` tags or inline Alpine `x-data` directives in this rewrite. Do NOT modify `base.html`, `recent_brews.html`, `unrated_coffees.html`, `top_coffees.html`, or any other file.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose cp app/templates/pages/home.html coffee-snobbery:/app/app/templates/pages/home.html && docker compose exec coffee-snobbery python -m pytest tests/routers/test_home.py -q -rs -k "home_renders or home_does_not or home_action_row or home_has_see_ai"</automated>
  </verify>
  <done>home.html has been rewritten per the composition in `<behavior>`. The file contains `{{ greeting }}`, `href="/cafe-logs/new"`, `href="/ai"`, `href="/ai/wishlist"`. The file contains NONE of the following substrings: `hx-get="/home/cards/ai-recommendation"`, `hx-get="/home/cards/preference-profile"`, `hx-get="/home/cards/flavor-descriptors"`, `hx-get="/home/cards/sweet-spots"`, `hx-get="/home/cards/top-coffees"`, `{% include "fragments/home/_cold_start.html" %}`, `{% if not gate.gate_open %}`. There is exactly one DIST-07 placeholder comment at the top of `<main>`. The seven home-composition tests in tests/routers/test_home.py pass.</done>
</task>

<task type="auto">
  <name>Task 5: Run full focused test suite, ruff format + check, commit</name>
  <files>
    (commands only — no source edits)
  </files>
  <read_first>
    - CLAUDE.md "Working with the code" (docker compose cp file-level + pytest install hint)
    - .planning/phases/17-ia-restructure/17-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py tests/routers/test_home.py -q -rs` exits 0 with no new failures and all newly-added tests collected and passing.
    - `docker compose exec coffee-snobbery python -m pytest tests/ -q -rs -x --ignore=tests/e2e` runs the non-e2e suite green; document any pre-existing flake but a NEW failure caused by this plan blocks the commit.
    - `ruff format --check .` exits 0.
    - `ruff check .` exits 0.
    - Commit message: `feat(17-02): home composition rewrite — greeting, eager Top Coffees no-floor, drop AI surfaces (IA-03/04/06, D-06..D-11)`.
    - Staged files: exactly the five listed in `files_modified`.
  </acceptance_criteria>
  <action>Step 1: copy any not-yet-copied files into the container (`docker compose cp app/services/analytics.py coffee-snobbery:/app/app/services/analytics.py`, same for `app/routers/home.py`, `app/templates/pages/home.html`, `tests/services/test_analytics.py`, `tests/routers/test_home.py`). Step 2: run the focused suite first (`docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py tests/routers/test_home.py -q -rs`). Fix any failures by editing the source — do NOT loosen tests. Step 3: run the non-e2e full suite (`docker compose exec coffee-snobbery python -m pytest tests/ -q -rs -x --ignore=tests/e2e`). If a pre-existing test fails for reasons unrelated to this plan, capture the failure and check whether it's listed in project memory `full-suite-test-isolation-gaps`; only block on NEW failures. Step 4: from the host shell, `ruff format .` and `ruff check .`. Address any drift. Step 5: stage only the five files and commit with the conventional-commit subject above. Do NOT push.</action>
  <verify>
    <automated>cd C:/Claude/Coffee-Snobbery && docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py tests/routers/test_home.py -q -rs && ruff check . && git log --oneline -1 | grep "feat(17-02):"</automated>
  </verify>
  <done>The focused test set for this plan is green. The non-e2e suite has no NEW failures attributable to plan 17-02. ruff format/check exit 0. The latest commit's subject begins with `feat(17-02):` and the diff touches exactly the five files in `files_modified`.</done>
</task>

</tasks>

<verification>
1. **No-floor variant (IA-06):** `pytest tests/services/test_analytics.py -k "get_top_coffees" -v` — all top-coffees tests pass; specifically the no-floor includes single-session coffees, caps at 5, and default still enforces >=2.

2. **Greeting (D-10):** `pytest tests/routers/test_home.py -k "derive_greeting or personalized_greeting" -v` — all bucket cases pass.

3. **Home composition (D-06..D-11 + IA-03/IA-04):** `pytest tests/routers/test_home.py -k "home_renders or home_does_not or home_action_row or home_has_see_ai or home_top_coffees_no_floor" -v` — all seven home composition tests pass.

4. **Regression sweep on neighboring tests:** `pytest tests/routers/test_home.py -q -rs` — pre-existing fragment-endpoint tests (`/home/cards/recent-brews`, `/home/cards/unrated-coffees`, etc.) still pass; this plan does NOT change those endpoints.

5. **Source assertion — home.html cleanliness (IA-03):** the file MUST NOT contain any of `hx-get="/home/cards/(ai-recommendation|preference-profile|flavor-descriptors|sweet-spots)"`, `{% include "fragments/home/_cold_start.html" %}`, `{% if not gate.gate_open %}`. One DIST-07 placeholder comment is allowed and expected.

6. **Source assertion — home_shell context:** `grep -n "min_sessions=0\|derive_greeting\|top_coffees" app/routers/home.py` shows all three plumbed.

7. **Style gates:** `ruff format --check .` and `ruff check .` both exit 0.
</verification>

<success_criteria>
- analytics.get_top_coffees accepts `min_sessions: int = 2` keyword-only; default preserves existing >=2 floor; min_sessions=0 returns coffees with one rated session.
- home_shell view context contains `greeting`, `top_coffees`, `top_coffees_all_unrated` alongside the existing `gate`, `recent_brews`, `unrated_coffees`.
- home.html renders a personalized greeting `<h1>` (D-10), a three-button action row (Guided Brew / Log session / Quick rate — NO Admin), Recent brews + Not tried yet + Top Coffees (eager, no-floor) + "See AI recommendations →" link + Wishlist card.
- Zero `hx-get="/home/cards/(ai-recommendation|preference-profile|flavor-descriptors|sweet-spots)"` references in home.html.
- The DIST-07 banner placeholder comment sits at the top of `<main>` ready for plan 17-03.
- ruff format/check pass; commit lands on main.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ia-restructure/17-02-SUMMARY.md` with:
- The before/after composition of home.html as a small diff summary
- Confirmation that `gate` context is preserved (plan 17-04 needs it for /ai)
- Confirmation that `APP_TIMEZONE` is reused rather than introducing `APP_TZ` (deviation from RESEARCH open question #1; rationale: APP_TIMEZONE already exists in Settings and APScheduler consumes it — single source of truth)
- Note that the DIST-07 banner include is a placeholder comment in home.html; plan 17-03 replaces it with the live include
- Note that the `/home/cards/*` fragment endpoint URLs were KEPT unchanged (RESEARCH Open Question #3 resolution: minimize SW cache churn; plan 17-04 re-mounts them from /ai using the existing URLs)
- Confirmation that `.planning/phases/17-ia-restructure/snippets/ai-surfaces-pre-17-02.html` was verified present before the home.html rewrite (the snippet is plan 17-04 Task 6's recovery source for the AI hero + AI tools + lazy-mount markup; replaces the brittle git-show approach)
</output>
