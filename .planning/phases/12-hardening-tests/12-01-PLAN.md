---
phase: 12-hardening-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - pyproject.toml
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "A second consecutive `pytest tests/` run no longer trips the phase_04 DELETE FROM coffees RESTRICT FK"
    - "test_setup_concurrent_race passes in a full-suite run, not just in isolation"
    - "With SNOB_CI=1 set, a critical-path test that cannot reach Postgres FAILS instead of skipping"
    - "Without SNOB_CI, host-only unit runs still skip Postgres-dependent tests cleanly"
    - "The destructive teardown refuses to run against any database whose name lacks 'test'"
  artifacts:
    - path: "tests/conftest.py"
      provides: "module-scoped catalog/brew TRUNCATE teardown + SNOB_CI skip-to-fail helper"
      contains: "_reset_catalog_tables"
    - path: "pyproject.toml"
      provides: "gate-friendly addopts (no -x so all failures surface)"
      contains: "addopts"
  key_links:
    - from: "tests/conftest.py _reset_catalog_tables"
      to: "app.services.settings._cache"
      via: "_cache.clear() after TRUNCATE"
      pattern: "_cache\\.clear"
    - from: "tests/conftest.py critical-path fixtures"
      to: "SNOB_CI env flag"
      via: "_require_postgres() helper"
      pattern: "SNOB_CI"
---

<objective>
Fix the root-conftest full-suite isolation gap (D-01) and add the skip-enforcement gate (D-02) so `pytest tests/` runs green as one honest batch and a hollow "green-by-skip" run becomes a FAILURE under the ship gate.

Purpose: This is the spine of Phase 12. STATE's deferred T-INFRA-1 is THIS task. Every downstream plan (the TEST-01 smoke, the verify-and-extend coverage check, CI) depends on the corrected fixtures and the skip gate landing first.
Output: A modified root `tests/conftest.py` (catalog teardown + SNOB_CI helper applied to critical-path fixtures) and a `pyproject.toml` addopts change so the gate run surfaces all failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-hardening-tests/12-CONTEXT.md
@.planning/phases/12-hardening-tests/12-RESEARCH.md
@.planning/phases/12-hardening-tests/12-PATTERNS.md

<interfaces>
<!-- Verified against the codebase. Use these directly — no exploration needed. -->

app/services/settings.py:
  - `_cache: dict[str, _CachedSetting] = {}`  (module-level dict, line 78)
  - `_cache.clear()`  — full reset (faster than per-key invalidate)
  - `invalidate(key: str) -> None`  — single-key eviction (line 275), calls `_cache.pop(key, None)`

tests/conftest.py existing symbols to reuse (DO NOT re-implement):
  - `_postgres_reachable() -> bool`  — 0.5s TCP probe (line 358)
  - the safety interlock idiom: parse DATABASE_URL → if "test" not in db name, bail (lines 319-332)
  - autouse `fresh_db` — DELETEs users+sessions, resets setup_completed (lines 276-355); the new catalog teardown is ADDITIVE, do not modify fresh_db's users/sessions handling
  - `app.db.engine` (sync engine) for `engine.begin()` + `text()` TRUNCATE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module-scoped catalog/brew TRUNCATE teardown to root conftest (D-01)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the WHOLE file — you are extending it; study `fresh_db` lines 276-355, the safety interlock lines 319-332, and `_postgres_reachable` line 358)
    - app/services/settings.py (confirm `_cache` dict at line 78 and `invalidate`/`_cache.clear` surface)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the `_reset_catalog_tables` pattern block, lines 150-190)
  </read_first>
  <action>
    Add a new `@pytest.fixture(scope="module", autouse=True)` named `_reset_catalog_tables` to tests/conftest.py. It yields first (teardown-only), then on teardown: (1) return early if `_postgres_reachable()` is False; (2) replicate the existing safety interlock VERBATIM — parse `settings.DATABASE_URL` (replace `postgresql+psycopg://` with `postgresql://`), extract the db name, and bail if `"test"` is not in its lowercased name; (3) open `app.db.engine.begin()` and execute, in this exact FK-safe order honoring the RESTRICT constraints (brew_sessions.coffee_id/bag_id RESTRICT coffees/bags, bags.coffee_id RESTRICT coffees): `TRUNCATE brew_sessions RESTART IDENTITY CASCADE`, then `TRUNCATE bags ...`, then `TRUNCATE coffees ...`, then `TRUNCATE equipment ...`, then `TRUNCATE recipes ...`, then `TRUNCATE roasters ...`, then `TRUNCATE flavor_notes ...`. Do NOT TRUNCATE or CASCADE `users` (that wipes the 19-row app_settings seed via the updated_by_user_id SET NULL FK — fresh_db already handles users via DELETE). (4) After the TRUNCATE block, clear the in-memory settings cache via `import app.services.settings as _svc; _svc._cache.clear()` wrapped in try/except — this closes test_setup_concurrent_race (the in-memory `setup_completed` cache surviving across modules). Wrap the SQL block in try/except so a not-yet-migrated test DB is a no-op, mirroring fresh_db. Module scope (not function scope) keeps per-test cost near zero while guaranteeing each test MODULE starts with an empty catalog.
  </action>
  <verify>
    <automated>python -m pytest tests/ --ignore=tests/e2e -rs --tb=short -p no:cacheprovider -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py defines `_reset_catalog_tables` as `scope="module", autouse=True`
    - The fixture TRUNCATEs in the order brew_sessions → bags → coffees → equipment → recipes → roasters → flavor_notes and NEVER TRUNCATEs users
    - The fixture calls `app.services.settings._cache.clear()` on teardown
    - The `"test" not in <db>.lower()` safety interlock is present and bails before any TRUNCATE
    - Running the full suite TWICE in one process (`pytest tests/ --ignore=tests/e2e` then re-run) does not trip a `DELETE FROM coffees` RESTRICT FK error
    - `tests/test_migrations.py::test_app_settings_seeded_with_19_rows` still passes (proves users was not CASCADE-truncated)
    - `test_setup_concurrent_race` passes in the full-suite run
  </acceptance_criteria>
  <done>Full `pytest tests/ --ignore=tests/e2e` runs green as one batch; test_setup_concurrent_race and test_app_settings_seeded_with_19_rows both pass; no RESTRICT-FK error on a second consecutive run.</done>
</task>

<task type="auto">
  <name>Task 2: Add SNOB_CI skip-enforcement helper and apply to critical-path fixtures (D-02)</name>
  <files>tests/conftest.py, pyproject.toml</files>
  <read_first>
    - tests/conftest.py (the Postgres-dependent fixtures `fresh_db` line 276, `sync_db` line 548, `seeded_admin_user` line 434, `seeded_regular_user` line 458, and every `pytest.skip(...)` call tied to a missing DB)
    - pyproject.toml lines 36-48 (`[tool.pytest.ini_options]`: `addopts = "-x --tb=short"`, `asyncio_mode`, `testpaths`)
    - .planning/phases/12-hardening-tests/12-PATTERNS.md (the "Skip → Fail Gate" shared pattern, lines 628-639)
    - .planning/phases/12-hardening-tests/12-RESEARCH.md (Pattern 2 Option A, lines 237-258; Pitfall 5 + Pitfall 6, lines 577-585)
  </read_first>
  <action>
    Add at the top of tests/conftest.py (module level, after imports): `_CI_MODE = os.environ.get("SNOB_CI") == "1"` and a helper `def _require_postgres(reason: str) -> None:` that calls `pytest.fail(f"SNOB_CI=1 but Postgres unreachable: {reason}")` when `_CI_MODE` is True, else `pytest.skip(reason)`. Then in the Postgres-dependent CRITICAL-PATH fixtures — `sync_db`, `seeded_admin_user`, `seeded_regular_user` — replace the `pytest.skip("Postgres not reachable")` / `_postgres_reachable()`-guarded skips with `_require_postgres(...)`. For `fresh_db` (autouse, runs for EVERY test including pure-unit tests): do NOT make it fail on unreachable Postgres (that would break legitimate host-only unit runs even under SNOB_CI for tests that don't need a DB); instead leave fresh_db's reachability bail as a `yield; return` but rely on the per-fixture `_require_postgres` for tests that actually request a DB fixture. In pyproject.toml, change `addopts = "-x --tb=short"` to `addopts = "--tb=short"` (drop `-x`) so the gate run surfaces ALL failures at once per D-02 / Pitfall 5; the gate command passes `-rs` explicitly. Add a short comment in pyproject.toml noting `-x` was removed for the Phase 12 ship gate and can be passed on the CLI for fast dev one-shots. Do NOT add `filterwarnings = ["error"]` (explicitly deferred, CONTEXT Deferred Ideas).
  </action>
  <verify>
    <automated>SNOB_CI=1 python -m pytest tests/test_phase02_smoke.py -rs --tb=short -p no:cacheprovider -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py defines `_CI_MODE = os.environ.get("SNOB_CI") == "1"` and `_require_postgres(reason)`
    - `_require_postgres` calls `pytest.fail(...)` when `_CI_MODE` else `pytest.skip(...)`
    - `sync_db`, `seeded_admin_user`, `seeded_regular_user` route their DB-missing path through `_require_postgres` (not a bare `pytest.skip`)
    - pyproject.toml `addopts` no longer contains `-x`
    - With Postgres reachable, `SNOB_CI=1 pytest tests/ --ignore=tests/e2e -rs` reports zero unexpected skips on the critical path (run after Task 1; record any skip that becomes a failure as a real finding per Pitfall 6 — fix the source, do not loosen)
    - Without SNOB_CI on a host with no Postgres, the same critical-path fixtures still `pytest.skip` cleanly
  </acceptance_criteria>
  <done>`SNOB_CI=1 pytest tests/ --ignore=tests/e2e -rs` runs with no unexpected skips on the critical path; addopts no longer carries `-x`; host-only runs without SNOB_CI still skip DB tests cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → ship-gate verdict | A green pytest run is the artifact a human trusts before deploying; a hollow green (skip-as-green) crosses this boundary as a false "safe to ship" signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Repudiation | full-suite gate (skip-as-green) | mitigate | D-02 `SNOB_CI=1` flag converts critical-path skips into failures via `_require_postgres`; the gate command runs `-rs` so every skip reason is reported and auditable |
| T-12-02 | Tampering | destructive teardown vs live DB | mitigate | Safety interlock replicated verbatim: refuse any TRUNCATE when `"test"` is absent from the resolved DB name; reuse the existing `_postgres_reachable()` probe |
</threat_model>

<verification>
- After this plan, `pytest tests/ --ignore=tests/e2e -rs` is green as a single batch on a repeated run.
- `SNOB_CI=1 pytest tests/ --ignore=tests/e2e -rs` is green with no unexpected skips on the critical path.
- `test_app_settings_seeded_with_19_rows` and `test_setup_concurrent_race` both pass in the full-suite run.
</verification>

<success_criteria>
- D-01 closed: full-suite isolation teardown lands; no cross-module catalog pollution; settings cache cleared between modules.
- D-02 closed: SNOB_CI skip-enforcement helper applied to critical-path fixtures; addopts drops `-x` for the gate.
- Safety interlock preserved verbatim; users table never CASCADE-truncated.
</success_criteria>

<output>
After completion, create `.planning/phases/12-hardening-tests/12-01-SUMMARY.md`
</output>
