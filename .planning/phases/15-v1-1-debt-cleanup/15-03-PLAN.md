---
phase: 15-v1-1-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - .planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md
autonomous: false
requirements: [DEBT-03, DEBT-04, DEBT-05]
user_setup: []

must_haves:
  truths:
    - "Every authenticated page shows persistent nav with visible user identity and a working sign-out, confirmed on a physical device"
    - "Every outstanding v1.1 human-UAT scenario (Phases 01/02/07/10/11) is executed and recorded with an outcome"
    - "Every outstanding human_needed verification (Phases 01/02/07/09/10/11) is closed-with-evidence or re-deferred-with-written-reason + target phase"
    - "The iOS bottom-nav safe-area fix (commit 982c0e6) is verified on-device in the same session (D-13), unblocking Phases 20/21"
    - "Closure is auditable: 15-VERIFICATION.md records pass/fail/deferred for every item, no hollow green"
    - "Honors locked decisions D-08 (correctness-only nav/sign-out), D-09 (no visual redesign / no IA change), D-10 (live interactive John+Claude session), D-11 (record every outcome), D-12 (re-defer with written reason + target phase)"
  artifacts:
    - path: ".planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md"
      provides: "Phase 15 closure ledger: per-item outcome for DEBT-01..05 + D-13"
      contains: "Phase 15 Closure Ledger"
  key_links:
    - from: "15-VERIFICATION.md DEBT-04/05 sections"
      to: "the as-found ledger in 15-RESEARCH.md (Human UAT Ledger)"
      via: "each catalogued item gets a recorded result"
      pattern: "deferred|pass|fail"
    - from: "15-VERIFICATION.md D-13 section"
      to: "commit 982c0e6 safe-area fix"
      via: "on-device confirmation recorded with a date"
      pattern: "982c0e6"
---

<objective>
Close the three human-gated debt items in a single live device session (John + Claude),
and record every outcome in an auditable ledger.

- DEBT-03: confirm persistent nav + visible user identity + working sign-out on every
  authenticated page, on a physical device (correctness-only; fix only if broken — NO
  redesign, NO IA changes; Phase 17/21 own design).
- DEBT-04: execute and record every outstanding v1.1 human-UAT scenario
  (Phases 01/02/07/10/11). NOTE: the "Phase 14 375px search-sheet UAT" items actually live
  in Phase 10's VERIFICATION.md — point the ledger there (RESEARCH Open Question #3).
- DEBT-05: resolve every outstanding `human_needed` verification (Phases 01/02/07/09/10/11)
  with evidence OR re-defer with a written reason + target phase (D-12).
- D-13: fold the iOS bottom-nav safe-area on-device check (commit 982c0e6) into the SAME
  session so John picks up the phone once; this unblocks Phases 20/21.

Purpose: ROADMAP success criteria #3, #4, #5. The base must be VERIFIED, not just patched,
before any v1.2 feature work begins.
Output: `15-VERIFICATION.md` — the Phase 15 closure ledger with a recorded outcome for
every item.

DEPENDS ON Plans 01 + 02 landing first: the device session verifies the deployed app, and
several UAT items want the DEBT-01 clean-deploy fix present; the DEBT-01 and DEBT-02
automated evidence is cited in the ledger's DEBT-01/02 sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-v1-1-debt-cleanup/15-CONTEXT.md
@.planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md
@.planning/phases/15-v1-1-debt-cleanup/15-PATTERNS.md
@.planning/phases/15-v1-1-debt-cleanup/15-01-SUMMARY.md
@.planning/phases/15-v1-1-debt-cleanup/15-02-SUMMARY.md

<interfaces>
<!-- The as-found inventory the ledger must close. Source: 15-RESEARCH.md "Human UAT
     Ledger (as-found state for DEBT-04/05)" lines 358-410. -->

DEBT-04 outstanding human-UAT (close or re-defer each):
  - 01-middleware UAT-1: real NGINX reverse-proxy e2e (curl https://<host>/debug/proxy)  [needs VPS]
  - 01-middleware UAT-2: browser CSP nonce wiring (DevTools Network)                      [local OK]
  - 01-middleware UAT-3: HTMX CSRF double-submit on 2nd fragment swap                     [local OK]
  - 02-auth      UAT-1: mobile 375px visual smoke for /setup,/login,/admin,/             [local OK]
  - 07-ai        UAT-1: hero card e2e with live provider key                             [needs VPS + AI creds]
  - 07-ai        UAT-2: 375px hero "generated"/"try-again" states                        [needs VPS + AI creds]
  - 10-search    VERIF-1: responsive layout smoke, icon -> full-screen sheet (375px)      [local OK]
  - 10-search    VERIF-2: debounce + hx-sync in-flight cancellation                       [local OK]
  - 10-search    VERIF-3: p95 < 100ms latency on seeded dataset                           [needs VPS-scale data]
  - 11-pwa       UAT-1: iOS Safari installability (MOB-12)                                [needs VPS + iPhone]
  - 11-pwa       UAT-2: Android Chrome installability (Lighthouse)                        [needs VPS + Chrome]
  - 11-pwa       UAT-3: Guided Brew wake lock on real device (BREW-13)                    [needs real devices]

DEBT-05 outstanding human_needed:
  - 01 (3 items) = same as 01-UAT-1/2/3
  - 02 (1 item)  = same as 02-UAT-1
  - 07 (2 items) = same as 07-UAT-1/2
  - 09-admin item 6: AI refresh respect/force modes with real data ("seems good" / partial)
                     -> close if VPS has live AI creds; else re-defer with reason
  - 10 (3 items) = same as 10-VERIF-1/2/3
  - 11 (3 items) = same as 11-UAT-1/2/3

D-13: commit 982c0e6 iOS bottom-nav safe-area fix — UNVERIFIED on-device. Also check Phase
13 NEW-13 (bottom nav still raised on the Log/sessions page) — was it resolved by the 100dvh
min-height fix?

Ledger format analog: .planning/milestones/v1.1-phases/01-middleware/01-HUMAN-UAT.md +
01-VERIFICATION.md. Recommended single-file ledger shape: 15-RESEARCH.md lines 518-532 and
15-PATTERNS.md lines 331-401.

DEBT-03 automated coverage already green (cite, do not rebuild):
  tests/test_nav.py — 5 tests (config_hub mobile sign-out form; home navBar; admin link
  visibility by role; config hub 200). Sign-out CSRF form pattern: base.html lines 161-166.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the 15-VERIFICATION.md closure ledger</name>
  <files>.planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-v1-1-debt-cleanup/15-PATTERNS.md § "15-VERIFICATION.md (new ledger artifact)" (lines 331-401, the recommended structure + per-item result vocabulary)
    - .planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md § "Human UAT Ledger (as-found state)" (lines 358-410) and § "Verification Ledger Shape" (lines 518-532)
    - .planning/milestones/v1.1-phases/01-middleware/01-HUMAN-UAT.md (format analog — frontmatter + per-item sections)
  </read_first>
  <action>
    Create `15-VERIFICATION.md` as the Phase 15 closure ledger with `status: in-progress`
    frontmatter (phase: 15-v1-1-debt-cleanup). Title it `Phase 15 Closure Ledger`. Pre-seed
    one section per requirement with a row/subsection for EVERY catalogued item from the
    as-found inventory (see the `<interfaces>` block and RESEARCH lines 358-410), each marked
    `pending` initially:
    - `## DEBT-01: Volume Ownership Fix` — paste the automated smoke evidence from
      15-01-SUMMARY.md (stat UID -> 1000, process UID 1000, write test, stop time).
    - `## DEBT-02: Test Isolation Double-Run` — paste the run 1 / run 2 pass counts and the
      `test_setup_concurrent_race` PASSED line from 15-02-SUMMARY.md.
    - `## DEBT-03: Nav / Sign-Out On-Device` — a per-page checklist (/, /brew, /brew/guided,
      /coffees, /roasters, /equipment, /recipes, /flavor-notes, /config, /admin/*) at 375px
      and >=768px: nav present, user identity visible, sign-out works. Cite the green
      tests/test_nav.py automated coverage as the structural baseline.
    - `## DEBT-04: Human UAT Closure` — subsections Phase 01 / 02 / 07 / 10 / 11 with the
      exact items above. Explicitly note that the "Phase 14 375px search-sheet UAT" maps to
      Phase 10's VERIFICATION.md items (RESEARCH Open Question #3) — do not invent a Phase 14
      item set.
    - `## DEBT-05: human_needed Closure` — subsections Phase 01 / 02 / 07 / 09 / 10 / 11.
    - `## D-13: Safe-Area Commit 982c0e6` — on-device iOS PWA standalone check + the NEW-13
      Log/sessions bottom-nav follow-up.
    Each item's result MUST be one of: `pending`, `pass: <evidence>`, `fail: <details>`, or
    `deferred: Phase XX — <reason>` (D-11/D-12). Do NOT mark anything pass yet — this task
    only scaffolds; the device session (Tasks 2-4) fills outcomes. This file is created and
    edited directly with the Write/Edit tool. Note (project memory): `commit_docs: false`
    means `gsd-sdk query commit` refuses doc commits — this ledger is committed manually
    (`git add` + `git commit`) at phase close, not via the SDK helper.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md && grep -q 'Phase 15 Closure Ledger' .planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md && grep -q 'DEBT-03' .planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md && grep -q 'DEBT-04' .planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md && grep -q 'DEBT-05' .planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md && grep -q '982c0e6' .planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md && echo "ledger scaffold ok"</automated>
  </verify>
  <acceptance_criteria>
    - `15-VERIFICATION.md` exists with `status: in-progress` frontmatter and the title `Phase 15 Closure Ledger`
    - It has sections for DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05, and D-13
    - DEBT-04 lists every catalogued UAT item (Phases 01/02/07/10/11) each marked `pending`
    - DEBT-05 lists every catalogued `human_needed` item (Phases 01/02/07/09/10/11) each marked `pending`
    - The D-13 section references commit `982c0e6` and the NEW-13 Log/sessions follow-up
    - DEBT-01 and DEBT-02 sections contain the automated evidence pasted from the Plan 01 / Plan 02 summaries
    - No item is marked `pass` yet (the device session has not run)
  </acceptance_criteria>
  <done>The closure ledger exists with a pending row for every DEBT-03/04/05 + D-13 item and the DEBT-01/02 automated evidence captured, ready for the device session to fill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device session — DEBT-03 nav/sign-out + D-13 safe-area</name>
  <files>.planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md § "DEBT-03 — Nav/Sign-Out Coverage Map" (lines 258-280) and § "D-13: Safe-Area Commit Verification" (lines 390-394) and Pitfall 6 (lines 325-328, SW stale cache)
    - .planning/phases/15-v1-1-debt-cleanup/15-PATTERNS.md § "app/templates/base.html (DEBT-03)" (lines 298-327, the CSRF sign-out form pattern to copy IF a fix is needed)
    - 15-VERIFICATION.md (the scaffold from Task 1 — you will fill the DEBT-03 and D-13 sections)
  </read_first>
  <what-built>
    Plan 01 landed the entrypoint privilege fix and Plan 02 closed test isolation. Sign-out
    forms and nav already exist (verified green by tests/test_nav.py). This task is the live
    on-device CORRECTNESS verification — NOT a redesign. Claude drives, John confirms on his
    physical iPhone (and a >=768px viewport for the desktop nav path).
  </what-built>
  <how-to-verify>
    PREP (Claude): ensure the app under test reflects the latest build. Because the service
    worker SWRs the app shell (project memory: SW stale cache confounds UI verify), instruct
    John to "Clear site data" (Safari/Chrome) before trusting any verdict — a hard refresh is
    not enough. If a template fix becomes necessary, a rebuild is required to bump the SW
    cache name.
    DEBT-03 (per authenticated page: /, /brew, /brew/guided, /coffees, /roasters,
    /equipment, /recipes, /flavor-notes, /config, /admin/*):
      1. Confirm persistent nav is present (bottom nav <768px; top nav >=768px).
      2. Confirm the user's identity (username) is visible/reachable in the nav chrome.
      3. Confirm sign-out actually works end-to-end: at <768px via the config-hub mobile
         sign-out form; at >=768px via the desktop user-dropdown form (base.html:161). After
         submit, the session is cleared and the user lands unauthenticated.
      If any page is missing nav/identity/working sign-out, that is the ONLY case a fix is
      authorized — copy the base.html:161 CSRF form pattern to the missing surface, rebuild,
      and re-verify. Do NOT restyle or restructure (D-08/D-09).
    D-13 (same session, John's iPhone, installed PWA standalone mode):
      4. Confirm the bottom nav clears the iOS home indicator (safe-area inset correct) —
         verify commit 982c0e6 on-device.
      5. Check Phase 13 NEW-13: is the bottom nav still raised on the Log/sessions page, or
         did the 100dvh min-height fix resolve it? Record which.
    Claude records each result into 15-VERIFICATION.md as `pass: <evidence/date>` or
    `fail: <details>` as John confirms.
  </how-to-verify>
  <action>
    Manual-only (documented Nyquist exception). Claude drives the live on-device session per
    the steps in `<how-to-verify>` and writes each per-page DEBT-03 result and the D-13
    safe-area / NEW-13 result into the corresponding `15-VERIFICATION.md` sections as John
    confirms. The ONLY code change permitted is a correctness-only copy of the base.html:161
    CSRF sign-out form to a page found missing one, followed by a rebuild and re-verify — no
    restyle, no IA change (D-08/D-09).
  </action>
  <verify>
    <automated>MISSING — manual-only checkpoint (on-device human verification per D-08/D-13). Evidence captured in 15-VERIFICATION.md DEBT-03 + D-13 sections; structural baseline is the already-green `pytest tests/test_nav.py -rs`.</automated>
  </verify>
  <acceptance_criteria>
    - Every authenticated page has a recorded DEBT-03 result (nav + identity + working sign-out) at both 375px and >=768px in 15-VERIFICATION.md
    - Sign-out is confirmed to actually clear the session (lands unauthenticated), not just visually present
    - Any DEBT-03 fix made is correctness-only (a copied base.html CSRF form), with NO restyle/IA change, and is re-verified after a rebuild
    - The D-13 safe-area result for commit 982c0e6 is recorded `pass`/`fail` with a date, on John's physical iPhone in PWA standalone mode
    - The NEW-13 Log/sessions bottom-nav follow-up has a recorded outcome
  </acceptance_criteria>
  <done>Every authenticated page's nav/identity/sign-out result and the D-13 safe-area + NEW-13 result are recorded in 15-VERIFICATION.md with John's on-device confirmation.</done>
  <resume-signal>John types "approved" (all nav/sign-out/safe-area confirmed) or describes the specific page(s)/behavior that failed so Claude can apply a correctness-only fix and re-verify.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device/live session — DEBT-04 human-UAT closure</name>
  <files>.planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md § "DEBT-04: Outstanding Human UAT" table (lines 362-377), § "Items That Can Close Without VPS" (lines 396-410), § "Environment Availability" (lines 536-544)
    - 15-VERIFICATION.md DEBT-04 section (the scaffold from Task 1)
  </read_first>
  <what-built>
    The DEBT-04 inventory (12 catalogued UAT items across Phases 01/02/07/10/11) is pending
    in the ledger. This task executes each item LIVE with John and records the outcome. Some
    close locally (Chrome DevTools + a running instance); some need the VPS and/or live AI
    creds and/or a real device.
  </what-built>
  <how-to-verify>
    Claude drives each catalogued item in turn; John confirms. Group by environment:
    LOCAL (close now via DevTools + running instance): 01-UAT-2 (CSP nonce wiring in Network
      tab), 01-UAT-3 (HTMX CSRF double-submit on 2nd swap), 02-UAT-1 (375px visual smoke for
      /setup,/login,/admin,/), 10-VERIF-1 (responsive search icon -> full-screen sheet at
      375px), 10-VERIF-2 (debounce + hx-sync in-flight cancel in Network panel).
    VPS / live-env (close if the VPS session is available this sitting, else re-defer with a
      written reason + target phase): 01-UAT-1 (real NGINX proxy chain via /debug/proxy),
      07-UAT-1/2 (hero card e2e + 375px states with a live provider key), 10-VERIF-3 (p95
      <100ms on a seeded dataset), 11-UAT-1/2/3 (iOS Safari + Android Chrome installability,
      Guided Brew wake lock on real devices).
    For EACH item, record in 15-VERIFICATION.md exactly one of:
      `pass: <evidence — what was observed + date>` | `fail: <details>` |
      `deferred: Phase XX — <reason>` (e.g. "Phase 22 — VPS AI creds not active this session").
    Note (RESEARCH Open Question #3): the "Phase 14 375px search-sheet UAT" = Phase 10's
    VERIF-1/2/3 — record under the Phase 10 subsection, not a fabricated Phase 14 entry.
    No item may be left `pending` — every one closes or re-defers with a reason (D-11/D-12).
  </how-to-verify>
  <action>
    Manual-only (documented Nyquist exception). Claude walks John through each catalogued
    DEBT-04 UAT item per `<how-to-verify>`, attempting the local-closable items this session
    and re-deferring VPS/device-only items with a written reason + target phase, then writes
    every outcome (`pass`/`fail`/`deferred: Phase XX — <reason>`) into the DEBT-04 section of
    15-VERIFICATION.md. The Phase 14 search-sheet items are recorded under Phase 10.
  </action>
  <verify>
    <automated>MISSING — manual-only checkpoint (live human-UAT execution per D-10/D-11). Evidence captured per-item in 15-VERIFICATION.md DEBT-04 section.</automated>
  </verify>
  <acceptance_criteria>
    - All 12 catalogued DEBT-04 UAT items have a recorded result of `pass`, `fail`, or `deferred: Phase XX — <reason>` — none left `pending`
    - Every `pass` has concrete evidence (what was observed) and a date
    - Every `deferred` names a target phase AND a written reason (no bare "deferred")
    - The local-closable items (01-UAT-2/3, 02-UAT-1, 10-VERIF-1/2) are attempted this session (they need no VPS)
    - The Phase 14 search-sheet items are recorded under Phase 10 (not a fabricated Phase 14 set)
  </acceptance_criteria>
  <done>Every DEBT-04 UAT item has a recorded pass/fail/deferred outcome in 15-VERIFICATION.md, with local items attempted this session and VPS/device items re-deferred with reasons.</done>
  <resume-signal>John types "approved" once every DEBT-04 item is recorded as pass/fail/deferred, or flags an item he wants re-run before sign-off.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-device/live session — DEBT-05 human_needed closure + phase ledger finalize</name>
  <files>.planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md § "DEBT-05: Outstanding human_needed Verifications" table (lines 379-388) and Open Question #4 (Phase 09 item 6, lines 352-354)
    - 15-VERIFICATION.md DEBT-05 section (the scaffold from Task 1) and all sections filled by Tasks 2-3
  </read_first>
  <what-built>
    The DEBT-05 `human_needed` inventory (Phases 01/02/07/09/10/11) is pending. Most items
    overlap the DEBT-04 UAT items already exercised in Tasks 2-3; the distinct one is Phase
    09 item 6 (AI refresh respect/force modes with real data). This task resolves each and
    finalizes the ledger.
  </what-built>
  <how-to-verify>
    For DEBT-05 items that are the SAME behavior as a DEBT-04 item already closed in Tasks
    2-3, cross-reference the recorded outcome (do not re-test; cite the DEBT-04 result and the
    date). For the DISTINCT item:
      Phase 09 item 6 (AI refresh respect/force with real data): attempt during this session
      IF the VPS has live AI credentials + an eligible cold-start-passed user. If creds are
      active, exercise both respect (signature unchanged -> no regen) and force (manual
      refresh -> regen) and record `pass: <evidence>`. If creds are NOT active this session,
      `deferred: Phase XX — VPS AI credentials not active` (RESEARCH Open Question #4).
    Record every DEBT-05 item as `pass`/`fail`/`deferred: Phase XX — <reason>` (D-12). Then
    FINALIZE the ledger: set frontmatter `status` to `complete` (or `partial` if any item is
    legitimately re-deferred), and add a one-line phase-summary stating how many items closed
    vs re-deferred. Confirm the DEBT-01 and DEBT-02 automated-evidence sections are present
    and accurate. The ledger + any DEBT-03 correctness fix are committed MANUALLY at phase
    close (project memory: `commit_docs: false` makes `gsd-sdk query commit` refuse doc
    commits — use `git add` + `git commit` directly).
  </how-to-verify>
  <action>
    Manual-only (documented Nyquist exception). Claude resolves each DEBT-05 `human_needed`
    item per `<how-to-verify>` — citing the already-recorded DEBT-04 outcome for overlapping
    behaviors and live-testing or re-deferring Phase 09 item 6 — then finalizes
    15-VERIFICATION.md (frontmatter `status` to `complete`/`partial`, closed-vs-deferred
    count, DEBT-01/02 evidence confirmed) and commits the ledger MANUALLY via `git add` +
    `git commit` (not `gsd-sdk query commit`, which refuses doc commits under
    `commit_docs: false`).
  </action>
  <verify>
    <automated>MISSING — manual-only checkpoint (live human_needed resolution per D-12). Ledger finalize is asserted by: `grep -L pending .planning/phases/15-v1-1-debt-cleanup/15-VERIFICATION.md` (no remaining `pending`) and `status: complete`/`partial` in frontmatter.</automated>
  </verify>
  <acceptance_criteria>
    - Every DEBT-05 `human_needed` item (Phases 01/02/07/09/10/11) has a recorded `pass`/`fail`/`deferred: Phase XX — <reason>` — none `pending`
    - Phase 09 item 6 (AI refresh respect/force) is either closed with evidence or re-deferred with a written reason naming why (no live AI creds)
    - DEBT-05 items duplicating DEBT-04 behaviors cite the already-recorded DEBT-04 outcome (no redundant re-test, but a result is present)
    - 15-VERIFICATION.md frontmatter `status` is `complete` (or `partial` with a stated reason) and includes a closed-vs-deferred count
    - No section in the ledger remains `pending`
    - The ledger is committed to git manually (not via `gsd-sdk query commit`)
  </acceptance_criteria>
  <done>Every DEBT-05 item is recorded, Phase 09 item 6 is closed or re-deferred with a reason, and 15-VERIFICATION.md is finalized with a closed-vs-deferred count and committed manually.</done>
  <resume-signal>John types "approved" once every DEBT-05 item is recorded and the ledger is finalized + committed, or flags the last item(s) he wants addressed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser -> app (authenticated) | Sign-out must actually terminate the server-side session; on-device verification confirms the existing CSRF logout form clears state, not just renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Spoofing / session persistence | logout flow on authenticated pages | mitigate (verify) | DEBT-03 confirms on-device that sign-out terminates the session (lands unauthenticated) on every page — a present-but-broken sign-out would leave a session usable after a user believes they logged out. This plan verifies the existing control rather than adding one; any fix reuses the existing CSRF POST logout form (base.html:161). |

This plan authors NO new code paths (it verifies existing behavior and records outcomes); the only permitted code change is a correctness-only copy of the existing CSRF sign-out form if a page is found missing one. No new auth/crypto/input surface is introduced.
</threat_model>

<verification>
- DEBT-03: every authenticated page has a recorded on-device result (nav + identity +
  working sign-out) at 375px and >=768px; sign-out confirmed to clear the session.
- DEBT-04: all 12 catalogued UAT items recorded pass/fail/deferred-with-reason; Phase 14
  search items recorded under Phase 10.
- DEBT-05: all `human_needed` items (incl. Phase 09 item 6) recorded; ledger finalized
  with a closed-vs-deferred count and committed manually.
- D-13: commit 982c0e6 safe-area verified on John's physical iPhone (PWA standalone), with
  the NEW-13 follow-up recorded — unblocking Phases 20/21.
</verification>

<success_criteria>
ROADMAP Phase 15 success criteria #3, #4, #5 are met: persistent nav + identity + working
sign-out confirmed on a physical device; all outstanding v1.1 human-UAT scenarios executed
and recorded; every `human_needed` verification closed-with-evidence or re-deferred-with-
reason. The safe-area gate (D-13) for Phases 20/21 is closed in the same sitting. The
closure ledger is auditable — no hollow green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-v1-1-debt-cleanup/15-03-SUMMARY.md`.
Summarize how many items closed vs re-deferred (with target phases), confirm D-13 outcome,
and confirm `15-VERIFICATION.md` is finalized and committed.
</output>
