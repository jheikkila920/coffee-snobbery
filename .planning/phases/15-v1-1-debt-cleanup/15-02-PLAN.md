---
phase: 15-v1-1-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/routers/test_auth.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [DEBT-02]
user_setup: []

must_haves:
  truths:
    - "test_setup_concurrent_race passes deterministically inside a full pytest tests/ run"
    - "pytest tests/ runs green twice in a row against the SAME test DB (residue from run 1 does not break run 2)"
    - "CI fails if the double-run isolation regresses (a second consecutive full-suite step exists)"
    - "The conftest safety interlocks (test-DB-name guard + Postgres-reachability probe) are unchanged"
    - "Honors locked decisions D-05 (prove-and-lock; no conftest fixture rewrite), D-06 (root-cause fix for test_setup_concurrent_race, no skip/xfail/quarantine), D-07 (green twice against the SAME DB + CI double-run guard)"
  artifacts:
    - path: "tests/routers/test_auth.py"
      provides: "test_setup_concurrent_race with an in-test settings-cache clear guard"
      contains: "_cache.clear()"
    - path: ".github/workflows/ci.yml"
      provides: "A second consecutive full-suite pytest step against the same DB"
      contains: "Pytest isolation double-run"
  key_links:
    - from: "test_setup_concurrent_race"
      to: "app.services.settings._cache"
      via: "in-test _svc._cache.clear() before async_client lifespan prewarm"
      pattern: "_cache\\.clear\\(\\)"
    - from: ".github/workflows/ci.yml double-run step"
      to: "the same Postgres service (no drop/recreate)"
      via: "second python -m pytest tests/ invocation reusing the session DB"
      pattern: "python -m pytest tests/"
---

<objective>
Close T-INFRA-1 (DEBT-02). The full-suite cross-module isolation mechanism already exists
in `tests/conftest.py` (`_reset_catalog_tables` TRUNCATE teardown + `_svc._cache.clear()`)
— this plan is PROVE-AND-LOCK, not rebuild. Two changes: (1) fix the lone full-suite
failure `test_setup_concurrent_race` at its root cause (a stale `setup_completed` settings
cache leaking across tests), and (2) add a CI guard that runs the full suite a SECOND time
against the SAME database so teardown residue can never silently regress.

Purpose: `pytest tests/` must run green twice in a row with zero cross-module isolation
failures (ROADMAP success criterion #2). A clean, double-run-proof suite is the base every
later v1.2 phase builds tests on.
Output: A minimal in-test cache-clear guard in `tests/routers/test_auth.py` and a new
"Pytest isolation double-run" step in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-v1-1-debt-cleanup/15-CONTEXT.md
@.planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md
@.planning/phases/15-v1-1-debt-cleanup/15-PATTERNS.md

<interfaces>
<!-- DO NOT rewrite conftest fixtures (D-05). They carry safety interlocks. -->

tests/conftest.py — interlocks that MUST remain intact (read-only reference):
  - fresh_db (autouse, function-scoped): guards on `"test" not in _active_db.lower()` then
    yields-and-returns; DELETEs users/sessions; resets setup_completed=false per test.
  - _reset_catalog_tables (autouse, module-scoped, teardown-only): same DB-name guard,
    a _postgres_reachable() probe, TRUNCATEs catalog+brew tables, then:
        import app.services.settings as _svc
        _svc._cache.clear()   # the exact pattern the test fix mirrors
  - _provision_test_db (session-scoped): idempotent — checks existence before creating;
    does NOT drop/recreate. This is why the double-run reuses the same DB.

tests/routers/test_auth.py — target test (current lines 137-182):
  async def test_setup_concurrent_race(async_client):
      _require_auth_router()
      primer = await async_client.get("/setup")   # <-- async_client lifespan -> prewarm_cache
      ...two concurrent POST /setup, assert locations == ["/", "/login"], one user row.
  Root cause: async_client depends only on `app`, not fresh_db; its lifespan prewarm_cache
  can read a stale setup_completed=true left in _svc._cache by an earlier test that set it
  via raw engine.begin() (bypassing set_setting(), which would have invalidated the cache).

.github/workflows/ci.yml — existing "Pytest full suite" step (lines 60-69, last step in file):
  env block: DATABASE_URL / POSTGRES_* / APP_SECRET_KEY / APP_ENCRYPTION_KEY / SNOB_CI: "1"
  run: python -m pytest tests/ -rs --tb=short --ignore=tests/e2e
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix test_setup_concurrent_race with an in-test settings-cache clear</name>
  <files>tests/routers/test_auth.py</files>
  <behavior>
    - After the fix, `pytest tests/ -k test_setup_concurrent_race` passes when run as part of the full collection (not just when the module runs alone).
    - The test still asserts exactly `["/", "/login"]` from the two concurrent POSTs and exactly one user row — assertion logic is unchanged.
    - A full `pytest tests/ --ignore=tests/e2e` run that previously surfaced this race failure now passes.
  </behavior>
  <read_first>
    - tests/routers/test_auth.py (read lines 90-185 — both test_setup_blocked_after_completion, which sets setup_completed via raw engine.begin(), and the target test_setup_concurrent_race)
    - .planning/phases/15-v1-1-debt-cleanup/15-PATTERNS.md § "tests/routers/test_auth.py (DEBT-02: minimal fix...)" (lines 122-202) and § "Cache-Clear Guard" shared pattern (lines 415-427)
    - .planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md § "DEBT-02 — test_setup_concurrent_race Root Cause and Fix" (lines 197-256, especially the deterministic fix at 223-247)
  </read_first>
  <action>
    Insert a single minimal guard immediately after `_require_auth_router()` (current line
    140) and before the primer `async_client.get("/setup")` call. The guard imports the
    settings module under a non-shadowing alias and clears the in-memory cache, wrapped in
    try/except so an import failure never breaks the test:
    import `app.services.settings` as `_svc_mod`, call `_svc_mod._cache.clear()`, swallow
    exceptions. This mirrors the EXISTING module-teardown pattern in conftest.py (the
    `_svc._cache.clear()` at the end of `_reset_catalog_tables`) — D-06 root-cause fix, not
    a workaround. Add a short comment explaining WHY: `test_setup_blocked_after_completion`
    sets `setup_completed='true'` via raw `engine.begin()` (bypassing `set_setting()`, so the
    cache is not invalidated); without this clear, `async_client`'s lifespan `prewarm_cache`
    can pick up the stale `true` and make both concurrent POSTs redirect to `/login`.
    Do NOT xfail, skip, mark, or quarantine the test (D-06). Do NOT touch any conftest
    fixture (D-05). Leave the rest of the test body — primer GET, the two `asyncio.gather`
    POSTs, the `locations == ["/", "/login"]` assertion, and the one-user-row assertion —
    exactly as-is. Note: the test currently has a `pytest.skip("CSRF cookie not primed...")`
    guard for a missing primer cookie; leave that line unchanged (it is a genuine
    environment guard, not a pass-by-skip of the assertion under test).
    Finally, before committing, run `ruff format tests/routers/test_auth.py` then
    `ruff check tests/routers/test_auth.py` and make both clean — CI gates on
    `ruff format --check .` and `ruff check .`, and executors have historically
    committed non-ruff-clean code that only fails on CI (project memory). ruff is in
    `requirements-dev.txt` and the baked dev/test image, with config in `pyproject.toml`.
  </action>
  <verify>
    <automated>docker compose run --rm coffee-snobbery-test tests/ --ignore=tests/e2e -rs -k test_setup_concurrent_race</automated>
  </verify>
  <acceptance_criteria>
    - `tests/routers/test_auth.py` contains `_cache.clear()` inside `test_setup_concurrent_race`, positioned after `_require_auth_router()` and before the primer GET
    - The clear is wrapped in try/except (import-failure safe) and uses a non-shadowing alias for the settings module
    - The file contains NO new `pytest.mark.xfail`, `pytest.mark.skip`, or `@pytest.mark.skipif` on `test_setup_concurrent_race` (D-06)
    - `pytest tests/ --ignore=tests/e2e -k test_setup_concurrent_race -rs` exits 0 with the test reported PASSED (not skipped) — confirm there is no `s`/skip in the `-rs` summary for this test
    - The two original assertions (`locations == ["/", "/login"]` and one-user-row) are unchanged
    - `git diff tests/conftest.py` is empty (no conftest changes)
    - `ruff format --check tests/routers/test_auth.py` and `ruff check tests/routers/test_auth.py` both exit 0 (matches the CI ruff gate so the commit does not fail CI)
  </acceptance_criteria>
  <done>test_setup_concurrent_race passes inside the full collection via a minimal in-test cache clear that mirrors the existing conftest teardown pattern; no fixtures changed; no skip/xfail introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CI double-run isolation guard</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (read the whole file; the existing "Pytest full suite" step is lines 60-69 and is the LAST step)
    - .planning/phases/15-v1-1-debt-cleanup/15-PATTERNS.md § ".github/workflows/ci.yml (DEBT-02: append double-run guard step)" (lines 256-294) and § "CI Step Env Block" shared pattern (lines 442-446)
    - .planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md § "CI double-run guard" code example (lines 497-514)
  </read_first>
  <action>
    Append ONE new step named `Pytest isolation double-run` immediately after the existing
    `Pytest full suite` step (after line 69, at the end of the `steps:` list). Copy the
    existing step's `env:` block VERBATIM — same `DATABASE_URL`,
    `POSTGRES_USER/PASSWORD/DB`, the `APP_SECRET_KEY` / `APP_ENCRYPTION_KEY` `secrets.* ||
    'fallback'` expressions, and `SNOB_CI: "1"` (SNOB_CI converts skip-as-green into
    failures — required for honest results). The `run:` command is the SAME full-suite
    invocation as the existing step: `python -m pytest tests/ -rs --tb=short
    --ignore=tests/e2e`, with a comment that this is the SECOND run against the SAME
    Postgres service (no drop/recreate between runs — D-07; `_provision_test_db` is
    idempotent and reuses the existing DB, so teardown residue from run 1 surfaces here).
    Do NOT add a fresh Postgres service, do NOT drop/recreate the DB, and do NOT add a
    `DROP DATABASE` step — that would defeat the residue proof (RESEARCH Pitfall 5).
    Keep YAML indentation consistent with the surrounding steps (two-space step indent).
  </action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); steps=d['jobs']['test']['steps']; names=[s.get('name') for s in steps]; assert 'Pytest isolation double-run' in names, names; i=names.index('Pytest full suite'); j=names.index('Pytest isolation double-run'); assert j==i+1, (i,j); ds=steps[j]; assert ds['env'].get('SNOB_CI')=='1'; assert 'pytest tests/' in ds['run']; assert 'drop' not in ds['run'].lower(); print('ci double-run step ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML
    - A step named exactly `Pytest isolation double-run` exists and is positioned IMMEDIATELY after `Pytest full suite`
    - The new step's `env` block includes `SNOB_CI: "1"` and the same `DATABASE_URL` / `POSTGRES_*` / `APP_SECRET_KEY` / `APP_ENCRYPTION_KEY` keys as the existing full-suite step
    - The new step's `run` invokes `python -m pytest tests/ ... --ignore=tests/e2e` and contains NO `DROP DATABASE` / drop / recreate logic
    - The Postgres `services:` block is unchanged (still one Postgres service; the double-run reuses it)
  </acceptance_criteria>
  <done>CI runs the full suite twice against the same Postgres service; the second run will fail if teardown leaks cross-module residue, locking the isolation fix against regression.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the same-DB double-run is green</name>
  <files>tests/routers/test_auth.py, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/15-v1-1-debt-cleanup/15-RESEARCH.md § "Double-run proof — the correct test command" (lines 249-256) and § "Common Pitfalls" Pitfall 4-5 (lines 317-323)
    - .planning/phases/15-v1-1-debt-cleanup/15-VALIDATION.md § "Sampling Rate" (lines 36-41, the per-wave double-run gate)
    - CLAUDE.md § "Working with the code" (tests run in the BAKED image — rebuild or `docker compose cp` changed files first)
  </read_first>
  <action>
    No source edits — this is the DEBT-02 double-run proof and it is the real gate (D-07).
    Because tests run in the baked image with no source bind-mount (CLAUDE.md), first make
    the Task-1 edit reach the test runner: rebuild the dev/test image
    (`docker compose build coffee-snobbery-test` or the project's equivalent) OR
    `docker compose cp` the changed `tests/routers/test_auth.py` into the running test
    container — confirm which mechanism actually exercises the edit (file-level cp to avoid
    the dir-nesting trap noted in project memory). Then run the FULL suite TWICE in
    sequence against the SAME test database, NOT dropping/recreating between runs:
    run 1 `... pytest tests/ --ignore=tests/e2e -rs --tb=short`, then run 2 with the
    identical command immediately after. Both runs must exit 0 and report
    `test_setup_concurrent_race` as PASSED (never skipped) in the `-rs` summary. Treat any
    skip of a behavior-bearing test as a failure (project memory: pass-by-skip masks green).
    Record both runs' summary lines (pass/skip/fail counts) in the SUMMARY so Plan 03's
    ledger can cite the DEBT-02 evidence (the expected clean baseline is ~939 unit/integration
    passing per project memory; confirm and record the actual counts).
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery-test && docker compose run --rm coffee-snobbery-test tests/ --ignore=tests/e2e -rs --tb=short && docker compose run --rm coffee-snobbery-test tests/ --ignore=tests/e2e -rs --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Run 1 of `pytest tests/ --ignore=tests/e2e -rs` exits 0
    - Run 2 (same DB, immediately after, no drop/recreate) exits 0 — residue from run 1 did not break run 2
    - `test_setup_concurrent_race` is reported PASSED in BOTH runs' `-rs` summaries (not in the skipped list)
    - No behavior-bearing test is silently skipped (review the `-rs` skip section; document any legitimate environment skip)
    - SUMMARY records the pass/skip/fail counts for both runs
  </acceptance_criteria>
  <done>The full suite is green twice in a row against the same test DB with the race test passing both times — T-INFRA-1 is proven closed, not just patched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> database | Destructive teardown (TRUNCATE/DELETE) runs only against a database whose name contains "test"; the interlock prevents a misconfigured run from wiping a real DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | conftest destructive fixtures (`fresh_db`, `_reset_catalog_tables`) | mitigate (preserve) | The existing `"test" not in _active_db.lower()` guard and the `_postgres_reachable()` probe are NON-NEGOTIABLE safety interlocks (D-05). This plan must NOT modify either fixture; Task 1 asserts `git diff tests/conftest.py` is empty. The control is preserved, not added. |

This plan changes only test code and CI config — no production code path, no auth/session/crypto/input-validation surface. No new threats are introduced beyond preserving the existing Tampering control above.
</threat_model>

<verification>
- `test_setup_concurrent_race` passes inside a full `pytest tests/ --ignore=tests/e2e` collection (not just in isolation) and is never skipped.
- The full suite runs green twice in a row against the SAME test DB (Task 3) — residue proof.
- `.github/workflows/ci.yml` has a `Pytest isolation double-run` step immediately after the full-suite step, same env, no DB drop/recreate.
- `tests/conftest.py` is unchanged (`git diff` empty) — safety interlocks preserved per D-05.
</verification>

<success_criteria>
ROADMAP Phase 15 success criterion #2 is met: `pytest tests/` runs green twice in a row
with zero cross-module isolation failures (T-INFRA-1 closed), and CI enforces the double-run
so the isolation cannot silently regress.
</success_criteria>

<output>
After completion, create `.planning/phases/15-v1-1-debt-cleanup/15-02-SUMMARY.md`.
Record both double-run pass/skip/fail counts and confirm `test_setup_concurrent_race`
passed (not skipped) in each, so Plan 03's verification ledger can cite the DEBT-02 evidence.
</output>
