---
phase: 15.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/coffee.py
  - app/models/coffee_origin.py
  - app/schemas/coffee.py
  - app/routers/coffees.py
  - app/services/coffees.py
  - app/templates/fragments/coffee_form.html
  - app/templates/fragments/coffee_origin_row.html
  - app/templates/fragments/coffee_row.html
  - app/templates/pages/coffee_detail.html
  - app/templates/pages/coffees.html
  - app/migrations/versions/p15_1_multi_origin.py
  - tests/test_coffee_origins.py
autonomous: true
requirements: [CATALOG-02, CATALOG-03]
must_haves:
  truths:
    - "Per D-01: a new coffee_origins table exists with (id, coffee_id FK CASCADE, country NOT NULL, region NULL, sort_order NOT NULL); is_blend is derived from COUNT(coffee_origins) > 1 and is NOT a stored column"
    - "Per D-02-orig: coffee_origins has NO percent column in v1"
    - "Per D-05: every existing coffee with country or origin populated has exactly one coffee_origins row after migration, with country = COALESCE(NULLIF(coffees.country,''), NULLIF(coffees.origin,''), 'Unknown') and region = NULL and sort_order = 0; coffees.country and coffees.origin columns are dropped in the same migration"
    - "Per D-22: the coffee form renders at least one origin row plus an Add another origin button; there is NO Single-origin/Blend toggle in the UI; clicking the button appends another row server-side via HTMX; the form supports >=1 origins and submitting one row yields a single-origin coffee while submitting >=2 rows yields a blend (is_blend derived from row count)"
    - "The /coffees filter bar uses an origin-derived option list sourced from SELECT DISTINCT country FROM coffee_origins; coffees.country is not queried by any router, service, or template after this plan"
    - "coffees.country and coffees.origin do not appear in any source file under app/ (excluding migration files in app/migrations/versions/) after this plan"
  artifacts:
    - path: app/models/coffee_origin.py
      provides: "CoffeeOrigin SQLAlchemy model"
      contains: "class CoffeeOrigin"
    - path: app/migrations/versions/p15_1_multi_origin.py
      provides: "coffee_origins table + data move + drop country/origin columns"
      contains: "p15_1_multi_origin"
    - path: app/templates/fragments/coffee_origin_row.html
      provides: "Single origin row fragment for HTMX server-side append"
      contains: "data-origin-row"
    - path: tests/test_coffee_origins.py
      provides: "Wave 0 test stubs + multi-origin behavior coverage"
  key_links:
    - from: app/templates/fragments/coffee_form.html
      to: app/templates/fragments/coffee_origin_row.html
      via: "Jinja include in origin-rows-container plus Add-another button hx-get /coffees/origin-row-template"
      pattern: "id=\"origin-rows-container\""
    - from: app/routers/coffees.py
      to: app/services/coffees.py
      via: "_parse_form_payload zips form_data.getlist('origins_country') with getlist('origins_region') and passes origins=[(country, region), ...] to coffee create/update service"
      pattern: "getlist\\(\"origins_country\""
---

<objective>
Replace the `coffees.country` + `coffees.origin` free-text columns with a structured `coffee_origins` join table (D-01, D-02-orig, D-05) and update the coffee form to render >=1 origin rows with a server-side HTMX "Add another origin" button and no Single-origin/Blend toggle (D-22).

Purpose: Enables true multi-origin coffees (blends) with structured data so downstream phases (16 Cafe, 19 AI) can query origins reliably. Removes free-text drift between country and origin columns.
Output: New `coffee_origins` model + migration, restructured `CoffeeCreate` schema, updated coffees router for `origins[]` form payload, new `coffee_origin_row.html` fragment, updated `coffee_form.html` rendering origin rows, updated `coffee_row.html` display, updated `/coffees` filter bar, full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15.1-catalog-session-polish/15.1-CONTEXT.md
@.planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md
@.planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md
@.planning/phases/15.1-catalog-session-polish/15.1-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Write failing test stubs for multi-origin (Wave 0)</name>
  <files>tests/test_coffee_origins.py</files>
  <read_first>
    - tests/services/test_analytics.py (skip-gate `_require_postgres` pattern - PATTERNS.md "Test DB skip gate pattern" lines 998-1009)
    - tests/conftest.py (existing fixtures + DB session pattern)
    - .planning/phases/15.1-catalog-session-polish/15.1-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md (sections "CATALOG-02/03: app/models/coffee_origin.py" and "CATALOG-02/03: Multi-origin migration")
  </read_first>
  <action>
    Create `tests/test_coffee_origins.py` with failing test stubs (no implementation behind them yet) covering: (1) `test_coffee_origins_table_exists` - SELECT 1 FROM coffee_origins succeeds; (2) `test_country_origin_columns_dropped` - SELECT country FROM coffees raises UndefinedColumn; (3) `test_data_move_country_priority` - a coffee created pre-migration with country='Ethiopia' origin=None has one coffee_origins row with country='Ethiopia'; (4) `test_data_move_origin_fallback` - a coffee with country=None origin='Kenya' yields coffee_origins.country='Kenya'; (5) `test_data_move_country_wins_when_both_set` - coffee with country='Ethiopia' origin='Yirgacheffe' yields coffee_origins.country='Ethiopia' (D-05 semantics: "origin field 'wins' semantically when both columns are populated -- the more specific value carries forward" - and per D-05 the migration uses COALESCE country first; the test asserts the COALESCE order); (6) `test_is_blend_derived_single` - coffee with one coffee_origins row → is_blend=False; (7) `test_is_blend_derived_blend` - coffee with two coffee_origins rows → is_blend=True; (8) `test_cascade_delete_removes_origins` - deleting a coffee removes its coffee_origins rows; (9) `test_form_renders_one_origin_row_by_default` - GET /coffees/new returns coffee_form.html with at least one `data-origin-row` element; (10) `test_origin_row_template_endpoint_returns_fragment` - GET /coffees/origin-row-template returns the coffee_origin_row.html fragment with `removable=True` markup; (11) `test_post_coffee_with_two_origins_creates_blend` - POST /coffees with origins_country=['Ethiopia','Kenya'] and origins_region=['Yirgacheffe',''] yields a coffee whose origin count is 2; (12) `test_filter_bar_uses_coffee_origins_country` - GET /coffees passes a `countries` (or `origins`) context value sourced from coffee_origins not coffees.country. Use the postgres skip-gate `_require_postgres` from `tests/services/test_analytics.py` at the top of each test that hits the DB. All tests MUST start in a failing-or-skipped state since implementation lands in later tasks; do NOT mark them xfail.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/test_coffee_origins.py --collect-only 2>&1 | grep -c "test_" </automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_coffee_origins.py` exists.
    - `pytest --collect-only tests/test_coffee_origins.py` collects exactly 12 tests (count from action above).
    - Running the file yields all-failing or all-skipped results before Tasks 1-5 land (no green tests in Wave 0).
    - Skip-gate matches the pattern used in `tests/services/test_analytics.py` (no novel skip syntax).
  </acceptance_criteria>
  <done>Test stubs exist, fail or skip cleanly, and exercise the contract Tasks 1-5 will deliver.</done>
</task>

<task type="auto">
  <name>Task 1: Add CoffeeOrigin model + migration that creates table, moves data, and drops country/origin columns</name>
  <files>app/models/coffee_origin.py, app/migrations/versions/p15_1_multi_origin.py, app/models/coffee.py</files>
  <read_first>
    - app/models/flavor_note.py (CITEXT + Identity + TIMESTAMP imports pattern, PATTERNS.md lines 269-278)
    - app/models/bag.py (FK with ondelete=CASCADE pattern)
    - app/models/coffee.py (current country line 67, origin line 68, varietal line 71 to drop; relationships block where the new origins relationship attaches)
    - app/migrations/versions/p4_shared_catalog.py (autogenerate baseline header lines 50-64, GIN op.execute pattern at lines 26-28 for reference only, CHECK pattern; the docstring at lines 32-34 stating migrations do NOT import from app.models)
    - app/migrations/versions/p11_brew_time_seconds.py (verify the current head; this plan's migration sets down_revision = "p11_brew_time_seconds")
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md (sections "CATALOG-02/03: app/models/coffee_origin.py" and "CATALOG-02/03: Multi-origin migration")
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 2 "Data Migration Steps (D-05)"
  </read_first>
  <action>
    (a) Create `app/models/coffee_origin.py` defining `CoffeeOrigin(Base)` per D-01: table `coffee_origins`; columns `id` BigInteger Identity primary key, `coffee_id` BigInteger ForeignKey("coffees.id", ondelete="CASCADE") nullable=False, `country` Text nullable=False (plain Text, NOT CITEXT — per PATTERNS.md "country is plain Text, NOT CITEXT UNIQUE"), `region` Text nullable=True, `sort_order` Integer nullable=False server_default text("0"); add Index "ix_coffee_origins_coffee_id" on coffee_id via __table_args__. NO `percent` column (D-02-orig). Mirror the imports pattern from `app/models/flavor_note.py`. (b) Add the relationship `origins: Mapped[list["CoffeeOrigin"]] = relationship(..., cascade="all, delete-orphan", order_by="CoffeeOrigin.sort_order")` to `app/models/coffee.py` (use the same relationship style already in coffee.py for any existing relationships; if none, import `relationship` from `sqlalchemy.orm` and add it cleanly). Remove the `country` and `origin` column declarations from `app/models/coffee.py` (lines 67-68). Leave `varietal: Mapped[str | None]` for now — Plan 03 owns that. Update the `coffees.__table_args__` if it referenced country/origin (it does not per RESEARCH but verify). (c) Create migration `app/migrations/versions/p15_1_multi_origin.py` with `revision = "p15_1_multi_origin"`, `down_revision = "p11_brew_time_seconds"`. `upgrade()` does in order: (1) `op.create_table("coffee_origins", ...)` with the column shape from D-01; (2) `op.create_index("ix_coffee_origins_coffee_id", "coffee_origins", ["coffee_id"])`; (3) `op.execute(...)` with the data-move INSERT per D-05 (see RESEARCH lines 286-293 for the exact SQL); (4) `op.drop_column("coffees", "country")`; (5) `op.drop_column("coffees", "origin")`. `downgrade()` is forward-only-policy: define an `op.execute("SELECT 1")` no-op + a comment "forward-only per project policy" (the same convention the existing p4_shared_catalog.py uses if applicable; otherwise match project convention for downgrade noops). Migration body MUST NOT import from `app.models` per PATTERNS.md "Migration file — no app.models imports" rule. The migration head is `p11_brew_time_seconds` (confirmed via grep of app/migrations/versions/).
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery alembic upgrade head && docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d $POSTGRES_DB -c "\d coffee_origins" && docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d $POSTGRES_DB -c "SELECT column_name FROM information_schema.columns WHERE table_name='coffees' AND column_name IN ('country','origin')" | grep -c "0 rows"</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/coffee_origin.py` exists with class `CoffeeOrigin` whose `__tablename__ == "coffee_origins"` and column set matches D-01 exactly.
    - `app/models/coffee.py` no longer contains the literal strings `Mapped[str | None] = mapped_column(Text, nullable=True)` on lines that were `country` or `origin`; `grep -n "country\|origin" app/models/coffee.py | grep -v "coffee_origins\|^#"` returns no matches for column declarations (the relationship line is fine because it uses `CoffeeOrigin` and `origins`).
    - `alembic upgrade head` completes without error.
    - psql `\d coffees` shows neither `country` nor `origin` column.
    - psql `\d coffee_origins` shows: `id bigint`, `coffee_id bigint NOT NULL`, `country text NOT NULL`, `region text`, `sort_order integer NOT NULL DEFAULT 0`, FK `coffee_origins_coffee_id_fkey` with `ON DELETE CASCADE`, index `ix_coffee_origins_coffee_id`.
    - A coffee row with pre-migration `country='Ethiopia'` and `origin='Yirgacheffe'` has exactly one matching `coffee_origins` row with `country='Ethiopia'` (COALESCE precedence per D-05) and `region IS NULL`.
    - A coffee row with pre-migration `country IS NULL` and `origin='Kenya'` has one `coffee_origins` row with `country='Kenya'`.
    - Migration file has NO `from app.models` or `import app.models` line: `grep -E "^from app\\.models|^import app\\.models" app/migrations/versions/p15_1_multi_origin.py` returns nothing.
    - The Wave 0 tests `test_coffee_origins_table_exists`, `test_country_origin_columns_dropped`, `test_data_move_country_priority`, `test_data_move_origin_fallback`, `test_data_move_country_wins_when_both_set`, `test_is_blend_derived_single`, `test_is_blend_derived_blend`, `test_cascade_delete_removes_origins` pass.
  </acceptance_criteria>
  <done>Schema migrated, model in place, country/origin columns gone, data-move tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure CoffeeCreate schema + coffees router to accept origins[] form payload</name>
  <files>app/schemas/coffee.py, app/routers/coffees.py, app/services/coffees.py</files>
  <read_first>
    - app/schemas/coffee.py (current `CoffeeCreate`, `extra="forbid"`, lines 30-57)
    - app/routers/coffees.py (_FORM_FIELDS lines 71-80, _EMPTY_TO_NONE_FIELDS lines 88-94, _NON_SCHEMA_FORM_KEYS lines 101-105, `_parse_form_payload`, `_hydrate_form_context` lines 184-236, create + update handlers, `list_distinct_countries` consumer)
    - app/services/coffees.py (current create/update signatures; where `country` and `origin` are written)
    - app/templates/pages/coffees.html (filter bar lines 43-50 querying country)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md sections "CATALOG-02/03: app/schemas/coffee.py restructuring" and "Special Pattern Notes 2"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 2 "Origin-Row HTMX Pattern (D-22)" + "Wire format" subsection + Pitfall 3
  </read_first>
  <action>
    (a) In `app/schemas/coffee.py`: drop the `country` and `origin` fields from `CoffeeCreate`. Do NOT add an origins field to the Pydantic schema — origins bypass Pydantic the same way `advertised_flavor_note_ids` does (PATTERNS.md "Special Pattern Notes 2"). Leave the existing `extra="forbid"` config intact. Leave `varietal` for Plan 03. (b) In `app/routers/coffees.py`: remove `"country"` and `"origin"` from `_FORM_FIELDS` and `_EMPTY_TO_NONE_FIELDS`. In `_parse_form_payload`, before constructing `CoffeeCreate`, call `origins_country = form_data.getlist("origins_country")` and `origins_region = form_data.getlist("origins_region")` and zip them into `origins = [(c.strip(), (r.strip() or None)) for c, r in zip(origins_country, origins_region) if c.strip()]`. Strip empty country entries (don't persist a blank-country row). Pass `origins` to the service as a kwarg separate from the validated `CoffeeCreate`. Add to `_NON_SCHEMA_FORM_KEYS` any of the origin-row helper keys if your collector tries to forbid them. Update `_hydrate_form_context` to add an `origins` key in the return dict: for `mode == "edit"` load the coffee's current `origins` list (with `region` falling through); for create return `[(None, None)]` (one empty row). Update `list_distinct_countries` (or rename to `list_distinct_origin_countries`) in `app/services/coffees.py` to query `SELECT DISTINCT country FROM coffee_origins ORDER BY country` — keep the function name stable to minimize template churn but update the query body and docstring. (c) Update the create + update service in `app/services/coffees.py` to accept `origins: list[tuple[str, str | None]] | None = None` kwarg; on create, after inserting the Coffee, call `db.add_all([CoffeeOrigin(coffee_id=c.id, country=country, region=region, sort_order=i) for i, (country, region) in enumerate(origins or [])])` then `db.commit()`. On update, replace strategy: delete all existing `coffee_origins` rows for `coffee_id`, then insert the new set (D-22 implicit single-origin/blend promotion is achieved by row count). The replace happens within the same transaction as the coffee update. (d) Update the `GET /coffees` filter handler to query by `coffee_origins.country` instead of `coffees.country` (per RESEARCH open question 2 recommendation): the URL param name `country` STAYS for URL stability, but the filter query joins coffees to coffee_origins on coffee_id and filters by `coffee_origins.country = :country`. The `countries` context value passed to `pages/coffees.html` is sourced from `list_distinct_countries()` (updated above).
  </action>
  <verify>
    <automated>docker compose cp app/schemas/coffee.py coffee-snobbery:/app/app/schemas/coffee.py && docker compose cp app/routers/coffees.py coffee-snobbery:/app/app/routers/coffees.py && docker compose cp app/services/coffees.py coffee-snobbery:/app/app/services/coffees.py && docker compose restart coffee-snobbery && docker compose exec coffee-snobbery python -m pytest -q tests/test_coffee_origins.py::test_post_coffee_with_two_origins_creates_blend tests/test_coffee_origins.py::test_filter_bar_uses_coffee_origins_country</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^\\s+country\\s*[:=]|^\\s+origin\\s*[:=]" app/schemas/coffee.py | grep -v "advertised_flavor_note_ids\\|^#"` returns no matches in CoffeeCreate.
    - `grep -n '"country"\|"origin"' app/routers/coffees.py | grep -E "_FORM_FIELDS|_EMPTY_TO_NONE"` returns no matches (those constants no longer mention country or origin).
    - `_parse_form_payload` reads `form_data.getlist("origins_country")` and `form_data.getlist("origins_region")` (grep confirms the literal strings).
    - `list_distinct_countries` (or renamed equivalent) queries `coffee_origins.country` (grep `FROM coffee_origins` in `app/services/coffees.py` finds at least one match in this function).
    - POSTing a coffee form with two `origins_country` values yields a Coffee with `len(coffee.origins) == 2`.
    - Tests `test_post_coffee_with_two_origins_creates_blend` and `test_filter_bar_uses_coffee_origins_country` pass.
    - `extra="forbid"` is unchanged (line stays).
  </acceptance_criteria>
  <done>Form posts with origin rows persist, filter bar pulls from coffee_origins, schema rejects no extras yet accepts origin row keys via the getlist bypass.</done>
</task>

<task type="auto">
  <name>Task 3: Build coffee_form origin-rows-container + Add another origin button + origin row fragment endpoint</name>
  <files>app/templates/fragments/coffee_form.html, app/templates/fragments/coffee_origin_row.html, app/routers/coffees.py</files>
  <read_first>
    - app/templates/fragments/coffee_form.html (current country/origin inputs at lines 115-135 to replace; existing `data-initial-chips='{{ ...|tojson }}'` at line 209 single-quoted attribute pattern; flavor_notes chip-builder Alpine scope so the origin block stays OUTSIDE it per RESEARCH assumption A2)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md sections "CATALOG-02/03: coffee_form.html" + "app/templates/fragments/coffee_origin_row.html (NEW)" + "Special Pattern Notes 1"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md Pitfalls 5 (tojson + single quotes) and 7 (htmx-indicator), sub-area 2 "Origin-Row HTMX Pattern (D-22)"
    - app/routers/coffees.py (analog endpoint location — pick the natural spot next to existing GET helpers like `/coffees/empty-form` so the route ordering stays grouped)
  </read_first>
  <action>
    (a) Create `app/templates/fragments/coffee_origin_row.html` containing one origin row: `<div class="flex gap-2 items-end" data-origin-row>` with two `<label>` blocks (Country required, Region optional) using input names `origins_country` and `origins_region` (plural — `form_data.getlist()` reads these). If context `removable` is truthy, append a Remove button using `hx-on:click="this.closest('[data-origin-row]').remove()"` (HTMX 2.x CSP-safe syntax — NO inline `onclick`). All input min-height `min-h-[44px]` for mobile touch targets where applicable. Match the Tailwind class set from `coffee_form.html` country input (line 117 area) for visual parity. (b) In `app/templates/fragments/coffee_form.html`, REPLACE the current country + origin inputs (the block currently rendering lines ~115-135 per CONTEXT canonical_refs) with a `<div id="origin-rows-container" class="flex flex-col gap-2">` that uses `{% for origin in origins %}{% set country = origin[0] %}{% set region = origin[1] %}{% set removable = not loop.first %}{% include "fragments/coffee_origin_row.html" with context %}{% endfor %}`. After the container, place the Add button: `<button type="button" hx-get="/coffees/origin-row-template" hx-target="#origin-rows-container" hx-swap="beforeend" class="text-sm text-espresso-700 hover:underline dark:text-cream-200">+ Add another origin</button>`. The origin block must NOT be inside any `x-data` scope (RESEARCH assumption A2). Do not introduce any Alpine state for the row list. (c) In `app/routers/coffees.py`, add a new route handler `GET /coffees/origin-row-template` decorated with `@router.get` returning a `TemplateResponse` for `fragments/coffee_origin_row.html` with context `{"country": "", "region": "", "removable": True}`. Gate it with `user: User = Depends(require_user)` for defense in depth (PATTERNS.md "Special Pattern Notes 1"). Place it near `/coffees/empty-form`. Do not require CSRF (it's GET only). (d) On the desktop edit path: existing edit-coffee already passes `origins` because Task 2 added it to `_hydrate_form_context`. The first row uses `removable = not loop.first` so the first row never has a Remove button — preventing removing down to zero origins (D-22 "removing rows back to 1 returns the coffee to single-origin").
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest -q tests/test_coffee_origins.py::test_form_renders_one_origin_row_by_default tests/test_coffee_origins.py::test_origin_row_template_endpoint_returns_fragment</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/fragments/coffee_origin_row.html` exists.
    - File contains the literal string `data-origin-row` (verifiable: `grep -c "data-origin-row" app/templates/fragments/coffee_origin_row.html` ≥ 1).
    - File contains `hx-on:click` for the remove button (NOT inline `onclick`).
    - `grep -c "onclick=" app/templates/fragments/coffee_origin_row.html` returns 0.
    - `coffee_form.html` contains `id="origin-rows-container"` and `hx-get="/coffees/origin-row-template"` (greppable literals).
    - `coffee_form.html` no longer renders standalone `name="country"` or `name="origin"` text inputs: `grep -nE 'name="country"|name="origin"' app/templates/fragments/coffee_form.html` returns no matches.
    - The new endpoint `GET /coffees/origin-row-template` returns 200 with body containing `data-origin-row` (verify via integration test).
    - GET /coffees/new returns a body containing at least one `data-origin-row` element.
    - Tests `test_form_renders_one_origin_row_by_default` and `test_origin_row_template_endpoint_returns_fragment` pass.
  </acceptance_criteria>
  <done>Coffee form renders origins as repeating rows, Add another origin appends via HTMX, no Alpine state involved.</done>
</task>

<task type="auto">
  <name>Task 4: Update coffee_row + coffees page filter bar + coffee_detail page to render origins from new shape</name>
  <files>app/templates/fragments/coffee_row.html, app/templates/pages/coffees.html, app/templates/pages/coffee_detail.html</files>
  <read_first>
    - app/templates/fragments/coffee_row.html (current country + origin display lines 102-106 with " · " separator; both desktop `<tr>` mode and card mode)
    - app/templates/pages/coffees.html (filter bar lines 43-50, currently `name="country"` select feeding `coffees.country`)
    - app/templates/pages/coffee_detail.html (existing read-side display of country/origin/varietal)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md sections "Pattern Assignments" entries for `coffee_row.html`, `coffee_detail.html`, `pages/coffees.html`
  </read_first>
  <action>
    (a) In `app/templates/fragments/coffee_row.html`, replace the `coffee.country` and `coffee.origin` references (both desktop `<tr>` shape and card mode) with a `{% for origin in coffee.origins %}` loop joined by " · " separator (use a Jinja `{% if not loop.first %} · {% endif %}` pattern). Render each origin as `{{ origin.country }}{% if origin.region %} ({{ origin.region }}){% endif %}`. Show "—" or nothing if `coffee.origins` is empty (RESEARCH landmine on data move: a coffee can have zero origins after the data move). (b) In `app/templates/pages/coffees.html`, keep the filter bar `<select name="country">` URL stability but populate its `<option>` list from the new `countries` context variable that Task 2 made source from `coffee_origins.country`. Label the select "Origin country" instead of just "Country" (the filter is now origin-based). (c) In `app/templates/pages/coffee_detail.html`, locate the read-side display of `coffee.country` / `coffee.origin` and replace with the same `{% for origin in coffee.origins %}` loop pattern used in `coffee_row.html`. If the page currently shows a "Country" + "Origin" label pair, replace with a single "Origins" section listing one origin per line with `Country (Region)` formatting. (d) Make sure no template under `app/templates/` references `coffee.country` or `coffee.origin` after this task.
  </action>
  <verify>
    <automated>docker compose cp app/templates/fragments/coffee_row.html coffee-snobbery:/app/app/templates/fragments/coffee_row.html && docker compose cp app/templates/pages/coffees.html coffee-snobbery:/app/app/templates/pages/coffees.html && docker compose cp app/templates/pages/coffee_detail.html coffee-snobbery:/app/app/templates/pages/coffee_detail.html && docker compose restart coffee-snobbery && grep -rnE "coffee\\.country|coffee\\.origin[^s]" app/templates/ | grep -v "coffee\\.origins" | grep -v "^#"</automated>
  </verify>
  <acceptance_criteria>
    - Bash command: `grep -rnE "coffee\\.country|coffee\\.origin[^s]" app/templates/ | grep -v "coffee\\.origins"` returns empty (0 matches; allowed: `coffee.origins`).
    - `coffee_row.html` contains `{% for origin in coffee.origins %}` or equivalent loop syntax (greppable).
    - `pages/coffees.html` filter `<select>` still has `name="country"` (URL stability) but the dropdown options come from the new `countries` context value sourced from `coffee_origins`.
    - `pages/coffee_detail.html` no longer shows separate Country and Origin label rows; instead shows an Origins section (visible string change verifiable by grep).
    - The /coffees page renders without 500 errors when a coffee has zero origins (renders "—" or empty).
  </acceptance_criteria>
  <done>All read-side surfaces render origins from the new shape; no template references coffee.country or coffee.origin.</done>
</task>

<task type="auto">
  <name>Task 5: Full-suite test pass + remove stray app/ references to coffee.country/origin (excluding migrations)</name>
  <files>app/, tests/test_coffee_origins.py</files>
  <read_first>
    - tests/test_coffee_origins.py (the stubs from Task 0)
    - .planning/phases/15.1-catalog-session-polish/15.1-VALIDATION.md ("Sampling Rate" section)
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 2 "Landmine on the country filter"
  </read_first>
  <action>
    (a) Audit `app/` for stray references to `coffees.country`, `coffees.origin`, `.country` and `.origin` as Coffee attributes via: `grep -rnE "Coffee\\.country|Coffee\\.origin[^s]|coffee\\.country|coffee\\.origin[^s]" app/ --include="*.py" --include="*.html" | grep -v "app/migrations/versions" | grep -v "coffee_origins\\|coffee\\.origins"`. Fix any stragglers. (b) Promote any Wave 0 stubs from Task 0 that are still skipped/failing into green tests now that Tasks 1-4 have shipped — remove pytest.skip guards or implement the bodies. (c) Run the full test suite inside the container: `python -m pytest tests/ -q -rs`. Treat skips as suspicious per project memory "tests-pass-by-skip-mask-green" — every skip on `tests/test_coffee_origins.py` must have a concrete reason (e.g., "postgres not reachable"). Fix any failures introduced by the schema change (likely candidates: existing `tests/phase_04/test_models_catalog.py` or `tests/phase_04/test_routers_coffees.py` referencing `country`/`origin` fields directly). Do NOT introduce new app-code changes to mask test failures — fix the test data instead.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest tests/ -q -rs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Bash command: `grep -rnE "Coffee\\.country|Coffee\\.origin[^s]|coffee\\.country|coffee\\.origin[^s]" app/ --include="*.py" --include="*.html" | grep -v "app/migrations/versions" | grep -v "coffee_origins\\|coffee\\.origins" | grep -v "^#"` returns empty.
    - All 12 tests in `tests/test_coffee_origins.py` pass (no skips on the post-migration tests).
    - Full pytest suite passes (exit 0). Skips reported by `-rs` are pre-existing patterns from Phase 12 (postgres reachability gate) — NOT new skips introduced by this plan.
    - `ruff check app/ tests/test_coffee_origins.py` and `ruff format --check app/ tests/test_coffee_origins.py` both pass (run before commit per project memory "executors-skip-ruff-ci-gates-both").
  </acceptance_criteria>
  <done>Multi-origin lands cleanly, no stray Coffee.country references in app/, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/coffees/origin-row-template GET | Authenticated user requests an HTML fragment to append to their form; CSRF not required for GET but auth IS required |
| client→/coffees POST + /coffees/{id} POST | Authenticated user submits origin rows via form-array keys `origins_country[]` / `origins_region[]` |
| migration p15_1_multi_origin | One-time data move from coffees.country/origin into coffee_origins; irreversible (drops columns) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-01 | Tampering (data integrity) | p15_1_multi_origin migration | mitigate | Use `INSERT INTO coffee_origins SELECT ...` from a single transactional Alembic upgrade so the data move and column drops are atomic; pg_dump backup is the recovery story (CONTEXT D-16 same posture); Task 1 acceptance criteria verifies data move semantics for both country-only and origin-only source rows |
| T-15.1-02 | Information Disclosure | /coffees/origin-row-template GET endpoint | mitigate | Gate with `Depends(require_user)` (defense in depth); endpoint returns static HTML scaffolding only, no per-user data |
| T-15.1-03 | Tampering (input validation) | _parse_form_payload zip of origins_country/origins_region | mitigate | Strip blank-country rows in the zip; reject submissions with zero non-blank origins via a router-level guard returning the form re-rendered with `{"errors": {"origins": "At least one origin required"}}`; pydantic `extra="forbid"` blocks unknown keys reaching CoffeeCreate |
| T-15.1-04 | Denial of Service | unbounded origin rows from a malicious client | accept | Household-scale (2 users); a malicious user adding 10k origin rows is a no-op via the form UI (the Remove button limits practical use); if abuse appears, add a per-coffee row-count cap later. Not in v1.2 scope. |
| T-15.1-05 | Repudiation | who-changed-which-origin audit | accept | Inherits the existing catalog last-write-wins philosophy (D-09 mirror — applies to flavor_notes; same household-trust model applies to origins); no per-origin provenance |
| T-15.1-06 | Spoofing (CSRF on origin row append) | GET origin-row-template | accept | GET is read-only HTML scaffolding; no state change; CSRF middleware exempts GET per starlette-csrf defaults |
</threat_model>

<verification>
- Migration upgrades cleanly on container start: `docker compose exec coffee-snobbery alembic upgrade head` exit 0.
- `psql \d coffees` shows no `country` or `origin` columns; `\d coffee_origins` shows expected shape.
- A round-trip test: create a 2-origin coffee via POST `/coffees` with form fields `origins_country=Ethiopia&origins_country=Kenya&origins_region=Yirgacheffe&origins_region=`, then GET `/coffees/{id}` and verify both origins render.
- `pytest tests/test_coffee_origins.py -q` all green.
- Full suite green: `pytest tests/ -q`.
- No `coffee.country` or `coffee.origin` (non-`origins`) string anywhere under `app/` excluding `app/migrations/versions/`.
</verification>

<success_criteria>
- coffee_origins join table exists with FK CASCADE, indexed on coffee_id.
- coffees.country and coffees.origin columns are gone.
- Every pre-migration coffee with country or origin populated has exactly one coffee_origins row with the COALESCE precedence.
- Coffee form renders >=1 origin rows + an Add another origin button; the button appends a new row via HTMX server-side; no Single-origin/Blend toggle.
- is_blend is derived from `len(coffee.origins) > 1` (no stored column).
- /coffees filter bar uses coffee_origins.country values.
- Full pytest suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-catalog-session-polish/15.1-01-SUMMARY.md` documenting: migration revision id, files modified, test counts, any deviations from the plan.
</output>
