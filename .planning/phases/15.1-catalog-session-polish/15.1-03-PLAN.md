---
phase: 15.1
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/models/coffee.py
  - app/models/varietal.py
  - app/schemas/coffee.py
  - app/services/coffees.py
  - app/services/varietals.py
  - app/routers/coffees.py
  - app/events.py
  - app/templates/fragments/coffee_form.html
  - app/templates/pages/coffee_detail.html
  - app/migrations/versions/p15_1_roast_level_enum.py
  - app/migrations/versions/p15_1_varietal_m2m.py
  - tests/test_roast_level_enum.py
  - tests/test_varietals.py
  - tests/services/test_varietals.py
autonomous: true
requirements: [CATALOG-04, CATALOG-05]
must_haves:
  truths:
    - "Per CATALOG-04 (Claude's discretion): coffees_roast_level_check accepts 'ultra-light', 'nordic-light', 'light', 'medium-light', 'medium', 'medium-dark', 'dark', 'unknown' (plus NULL); existing data with the legacy six values remains valid"
    - "Per D-02: varietals table exists with (id BIGINT PK, name CITEXT UNIQUE NOT NULL, created_at) and a coffee_varietals join table with (coffee_id ondelete=CASCADE, varietal_id ondelete=RESTRICT); coffees.varietal text column is dropped in the varietal migration"
    - "Per D-03: any authenticated user can create a new varietal on the fly via the autocomplete UI — typing a name not in the list shows an 'Add X as a new varietal?' affordance; CITEXT UNIQUE prevents 'Geisha'/'geisha'/'GEISHA' duplicates"
    - "Per D-04: the migration's op.bulk_insert seeds exactly these 14 varietals in order: Bourbon, Typica, Caturra, Catuai, Geisha, Pacamara, SL28, SL34, Mundo Novo, Pacas, Heirloom, Maragogype, Castillo, Catimor"
    - "The coffee form's varietal field is a multi-select chip-builder with autocomplete that mirrors the flavor_notes autocomplete UX pattern (single-quoted data-* JSON attribute, server-rendered chip seed, HTMX prefix search endpoint)"
    - "COFFEE_ROAST_LEVELS tuple is (ultra-light, nordic-light, light, medium-light, medium, medium-dark, dark, unknown) in that exact order; CoffeeCreate.roast_level regex matches the same eight values"
  artifacts:
    - path: app/models/varietal.py
      provides: "Varietal model + coffee_varietals join Table"
      contains: "class Varietal"
    - path: app/services/varietals.py
      provides: "create_varietal + search_by_prefix API mirroring flavor_notes.py"
      contains: "def create_varietal"
    - path: app/migrations/versions/p15_1_roast_level_enum.py
      provides: "CHECK constraint widening migration"
      contains: "p15_1_roast_level_enum"
    - path: app/migrations/versions/p15_1_varietal_m2m.py
      provides: "varietals + coffee_varietals tables + 14-row seed + drop coffees.varietal"
      contains: "p15_1_varietal_m2m"
  key_links:
    - from: app/services/varietals.py
      to: app/services/flavor_notes.py
      via: "Mirror create + search_by_prefix API surface exactly (PATTERNS.md Pattern 4)"
      pattern: "DuplicateNameError"
    - from: app/templates/fragments/coffee_form.html
      to: app/routers/coffees.py
      via: "Varietal autocomplete fragment via hx-get /coffees/varietal-autocomplete?q=..."
      pattern: "varietal-autocomplete"
---

<objective>
Two requirements bundled in one plan because they share contended source files (coffee.py / schemas/coffee.py / services/coffees.py / coffee_form.html):
- CATALOG-04: widen the `coffees_roast_level_check` CHECK constraint with `ultra-light` and `nordic-light` (Claude's discretion).
- CATALOG-05: replace the single-text `coffees.varietal` column with a `varietals` + `coffee_varietals` m2m via join table, seed 14 common varietals, build an autocomplete chip-builder UX that mirrors flavor_notes (D-02, D-03, D-04).

Purpose: Future-proofs roast labels and turns varietal into structured data with create-on-the-fly UX. Both changes share the same files so sequencing them in one plan minimizes coffee_form.html merge churn.
Output: One enum-widening migration + one varietal m2m migration (with bulk seed) + new Varietal model + new varietals service mirroring flavor_notes + extended CoffeeCreate schema (varietal_ids[]) + updated coffees router for the varietal payload + updated coffee_form.html with the chip-builder + new autocomplete endpoint + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15.1-catalog-session-polish/15.1-CONTEXT.md
@.planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md
@.planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md
@.planning/phases/15.1-catalog-session-polish/15.1-01-PLAN.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Write failing tests for roast enum + varietal m2m (Wave 0)</name>
  <files>tests/test_roast_level_enum.py, tests/test_varietals.py, tests/services/test_varietals.py</files>
  <read_first>
    - tests/services/test_analytics.py (postgres skip-gate)
    - tests/services/test_flavor_notes.py if it exists, else `app/services/flavor_notes.py` (analog API shape for varietal service tests)
    - .planning/phases/15.1-catalog-session-polish/15.1-VALIDATION.md (Wave 0 test list)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md sections "CATALOG-04" + "CATALOG-05"
  </read_first>
  <action>
    Three files. (a) `tests/test_roast_level_enum.py`: failing stubs (1) `test_check_constraint_accepts_ultra_light` - INSERT with roast_level='ultra-light' succeeds; (2) `test_check_constraint_accepts_nordic_light`; (3) `test_check_constraint_rejects_invalid` - 'extra-bold' raises IntegrityError; (4) `test_existing_six_values_still_valid`; (5) `test_coffee_roast_levels_tuple_order` - tuple equals the eight-value sequence in CATALOG-04 order; (6) `test_pydantic_accepts_ultra_light` - CoffeeCreate(roast_level='ultra-light') validates; (7) `test_pydantic_rejects_invalid`. (b) `tests/test_varietals.py`: failing stubs (1) `test_varietals_table_exists`; (2) `test_coffee_varietals_join_table_exists`; (3) `test_seed_inserted_14_varietals` - SELECT COUNT(*) FROM varietals returns 14; (4) `test_seed_includes_expected_names` - names ('Bourbon','Typica','Caturra','Catuai','Geisha','Pacamara','SL28','SL34','Mundo Novo','Pacas','Heirloom','Maragogype','Castillo','Catimor') all present; (5) `test_coffee_varietal_column_dropped` - coffees.varietal column gone; (6) `test_citext_uniqueness_collapses_case` - inserting 'geisha' after 'Geisha' raises IntegrityError; (7) `test_coffee_with_two_varietals` - POST /coffees with varietal_ids=[1,2] yields a coffee whose `varietals` list has length 2; (8) `test_coffee_with_zero_varietals` - submitting no varietal_ids creates a coffee with empty `varietals`; (9) `test_cascade_delete_removes_join_rows` - deleting a coffee removes its coffee_varietals rows; (10) `test_varietal_restrict_on_delete` - deleting a varietal still referenced by a coffee_varietals row raises IntegrityError; (11) `test_autocomplete_endpoint_returns_prefix_matches` - GET /coffees/varietal-autocomplete?q=Bou returns a fragment containing 'Bourbon'; (12) `test_create_varietal_on_the_fly` - POST /coffees/varietals with name='NewVar' creates a varietal and returns it. (c) `tests/services/test_varietals.py`: service-layer unit tests (1) `test_create_varietal_basic` - create_varietal returns a Varietal with the given name; (2) `test_create_varietal_duplicate_raises_DuplicateNameError` - second create with same name raises DuplicateNameError; (3) `test_search_by_prefix_returns_matches` - search_by_prefix(query='Bou') returns Bourbon. Use postgres skip-gate on DB-touching tests.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/test_roast_level_enum.py tests/test_varietals.py tests/services/test_varietals.py --collect-only 2>&1 | grep -c "test_"</automated>
  </verify>
  <acceptance_criteria>
    - Three test files exist.
    - Combined collect count: 7 + 12 + 3 = 22 tests.
    - All 22 tests fail or skip before Tasks 1-5 land.
  </acceptance_criteria>
  <done>Test stubs cover CHECK constraint, varietal model + seed + autocomplete + service-layer behavior.</done>
</task>

<task type="auto">
  <name>Task 1: Create roast-enum-widening migration + update model/schema/service constants</name>
  <files>app/migrations/versions/p15_1_roast_level_enum.py, app/models/coffee.py, app/schemas/coffee.py, app/services/coffees.py</files>
  <read_first>
    - app/models/coffee.py (CheckConstraint at lines 94-98 — note Plan 01 may have left it unchanged; verify)
    - app/schemas/coffee.py (roast_level pattern regex line 42-46)
    - app/services/coffees.py (COFFEE_ROAST_LEVELS tuple line 80-87)
    - app/migrations/versions/p4_shared_catalog.py (migration header pattern)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md section "CATALOG-04: app/migrations/versions/p15_1_roast_level_enum.py (NEW)"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 4 "Migration Approach"
  </read_first>
  <action>
    (a) Create `app/migrations/versions/p15_1_roast_level_enum.py` with `revision = "p15_1_roast_level_enum"` and `down_revision = "p15_1_drop_roast_date"` (chained after Plan 02's migration; the chain is: p11_brew_time_seconds → p15_1_multi_origin → p15_1_drop_roast_date → p15_1_roast_level_enum → p15_1_varietal_m2m). `upgrade()` uses raw op.execute: `op.execute("ALTER TABLE coffees DROP CONSTRAINT coffees_roast_level_check")` then `op.execute("ALTER TABLE coffees ADD CONSTRAINT coffees_roast_level_check CHECK (roast_level IS NULL OR roast_level IN ('ultra-light','nordic-light','light','medium-light','medium','medium-dark','dark','unknown'))")`. `downgrade()` reverts to the six-value list. No app.models imports. (b) `app/services/coffees.py`: replace `COFFEE_ROAST_LEVELS` tuple with the 8-value sequence in CATALOG-04 order. (c) `app/schemas/coffee.py`: update roast_level Field pattern to `r"^(ultra-light|nordic-light|light|medium-light|medium|medium-dark|dark|unknown)$"`. (d) `app/models/coffee.py`: update the literal CheckConstraint string at lines 94-98 to include the two new values, matching the migration so Alembic autogenerate doesn't drift.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery alembic upgrade head && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest -q tests/test_roast_level_enum.py</automated>
  </verify>
  <acceptance_criteria>
    - All 7 tests in `tests/test_roast_level_enum.py` pass.
    - psql `\d coffees` shows the CHECK constraint with eight values.
    - `grep -nE "ultra-light\\|nordic-light" app/schemas/coffee.py` finds the regex.
    - `grep -nE 'ultra-light","nordic-light"' app/services/coffees.py` or equivalent tuple syntax matches the new ordering.
    - No app.models imports in the migration file.
  </acceptance_criteria>
  <done>Roast enum widened in DB + schema + tuple + model; all 7 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Create Varietal model + coffee_varietals join Table + varietal migration with seed + drop coffees.varietal</name>
  <files>app/models/varietal.py, app/models/coffee.py, app/migrations/versions/p15_1_varietal_m2m.py</files>
  <read_first>
    - app/models/flavor_note.py (exact analog: CITEXT UNIQUE + Identity + TIMESTAMP imports + table_args style)
    - app/models/coffee.py (current `varietal: Mapped[str | None]` line 71 to be dropped + the existing relationships block; how `advertised_flavor_note_ids` is declared as ARRAY for the contrast — varietals uses a join Table not an array)
    - app/migrations/versions/p4_shared_catalog.py (table creation + op.bulk_insert pattern around lines 95-118)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md sections "CATALOG-05: app/models/varietal.py (NEW)" + "CATALOG-05: Varietal migration with bulk seed"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 3 (full)
  </read_first>
  <action>
    (a) Create `app/models/varietal.py` with: `class Varietal(Base)` — table `varietals`, columns `id` BigInteger Identity primary key, `name` CITEXT() nullable=False unique=True, `created_at` TIMESTAMP(timezone=True) nullable=False server_default=func.now(). No `archived` column (varietals are managed via the join table, not soft-deleted — PATTERNS.md note). Below the Varietal class, declare the association table: `coffee_varietals = Table("coffee_varietals", Base.metadata, Column("coffee_id", BigInteger, ForeignKey("coffees.id", ondelete="CASCADE"), primary_key=True), Column("varietal_id", BigInteger, ForeignKey("varietals.id", ondelete="RESTRICT"), primary_key=True))`. Mirror the imports pattern from `app/models/flavor_note.py`. (b) In `app/models/coffee.py`: add a `varietals` relationship to the Coffee class: `varietals: Mapped[list["Varietal"]] = relationship(secondary="coffee_varietals", lazy="selectin", order_by="Varietal.name")`. Remove the `varietal: Mapped[str | None] = mapped_column(Text, nullable=True)` declaration at line 71. (c) Create `app/migrations/versions/p15_1_varietal_m2m.py` with `revision = "p15_1_varietal_m2m"` and `down_revision = "p15_1_roast_level_enum"`. `upgrade()` body: (1) `op.create_table("varietals", ...)` with the columns above (use `postgresql.CITEXT()` for name); (2) `op.create_table("coffee_varietals", ...)` with the FK + cascade/restrict semantics; (3) `op.bulk_insert(varietals_table, [...])` seeding the 14 names from D-04 in the exact order specified: Bourbon, Typica, Caturra, Catuai, Geisha, Pacamara, SL28, SL34, Mundo Novo, Pacas, Heirloom, Maragogype, Castillo, Catimor — each with `"created_at": sa.func.now()`; (4) `op.drop_column("coffees", "varietal")`. `downgrade()` is forward-only no-op per project convention. No app.models imports in the migration body.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery alembic upgrade head && docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d $POSTGRES_DB -c "SELECT COUNT(*) FROM varietals" && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest -q tests/test_varietals.py::test_varietals_table_exists tests/test_varietals.py::test_seed_inserted_14_varietals tests/test_varietals.py::test_seed_includes_expected_names tests/test_varietals.py::test_coffee_varietal_column_dropped tests/test_varietals.py::test_citext_uniqueness_collapses_case tests/test_varietals.py::test_cascade_delete_removes_join_rows tests/test_varietals.py::test_varietal_restrict_on_delete</automated>
  </verify>
  <acceptance_criteria>
    - psql `\d varietals` shows id (bigint NOT NULL Identity), name (CITEXT NOT NULL with UNIQUE constraint), created_at (TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT now()).
    - psql `\d coffee_varietals` shows composite PK on (coffee_id, varietal_id), FK on coffee_id with ON DELETE CASCADE, FK on varietal_id with ON DELETE RESTRICT.
    - `SELECT COUNT(*) FROM varietals` returns 14.
    - `SELECT name FROM varietals ORDER BY id` returns the 14 names in the D-04 order.
    - `SELECT column_name FROM information_schema.columns WHERE table_name='coffees' AND column_name='varietal'` returns 0 rows.
    - Inserting a second varietal with name 'geisha' after 'Geisha' exists raises IntegrityError (CITEXT UNIQUE).
    - All 7 named tests pass.
  </acceptance_criteria>
  <done>Varietal m2m schema in place, seed loaded, coffees.varietal dropped, CITEXT uniqueness verified.</done>
</task>

<task type="auto">
  <name>Task 3: Create varietals service (mirror flavor_notes) + autocomplete + create-on-the-fly endpoints</name>
  <files>app/services/varietals.py, app/routers/coffees.py, app/events.py</files>
  <read_first>
    - app/services/flavor_notes.py (full file — `create_flavor_note` lines 56-101, `search_by_prefix` lines 202-219, `DuplicateNameError` import + structlog event constants)
    - app/events.py (existing CATALOG_FLAVOR_NOTE_CREATED constant pattern; add CATALOG_VARIETAL_CREATED)
    - app/routers/coffees.py (analog autocomplete endpoint location near flavor_notes autocomplete; find the existing `/coffees/flavor-note-autocomplete` or similar route — RESEARCH section "Flavor Notes Pattern (Template to Mirror)")
    - app/services/coffees.py (where the varietal_ids would be persisted in create/update)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md sections "CATALOG-05: app/services/varietals.py (NEW)" + "DuplicateNameError handling" + "Structlog audit events"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 3
  </read_first>
  <action>
    (a) Add event constant `CATALOG_VARIETAL_CREATED = "catalog.varietal.created"` to `app/events.py` (or matching style; grep `CATALOG_FLAVOR_NOTE_CREATED` to find the file). (b) Create `app/services/varietals.py` mirroring `app/services/flavor_notes.py` API surface exactly: imports include `DuplicateNameError` from wherever flavor_notes imports it; structlog logger; `create_varietal(db, *, name, by_user_id, commit=True) -> Varietal` with the IntegrityError → DuplicateNameError pattern (PATTERNS.md "DuplicateNameError handling"); `search_by_prefix(db, *, query, limit=50) -> list[Varietal]` using `select(Varietal).where(Varietal.name.ilike(f"{query}%")).order_by(Varietal.name).limit(limit)` (no archived filter — Varietal has no archived column). Use the create_flavor_note pattern's SAVEPOINT semantics for concurrent inserts. (c) In `app/routers/coffees.py`, add two new endpoints: (i) `GET /coffees/varietal-autocomplete?q=<prefix>` returning a TemplateResponse for `fragments/varietal_autocomplete.html` (create this fragment too — small list of `<button>` chips matching the existing flavor_note autocomplete fragment shape). If `q` is empty or <2 chars, return an empty list. Gate with `Depends(require_user)`. (ii) `POST /coffees/varietals` for create-on-the-fly: form body `name=<name>`, CSRF-protected, calls `varietals.create_varietal(...)`, returns the new varietal as a chip fragment to append to the chip-builder. (d) In `app/services/coffees.py`, extend `create_coffee` and `update_coffee` to accept `varietal_ids: list[int] | None = None`. On create, after the coffee is inserted, do `coffee.varietals = db.execute(select(Varietal).where(Varietal.id.in_(varietal_ids or []))).scalars().all()` (this uses the secondary table). On update, replace the existing set (same pattern as advertised_flavor_note_ids). Same transaction as the coffee update.
  </action>
  <verify>
    <automated>docker compose cp app/services/varietals.py coffee-snobbery:/app/app/services/varietals.py && docker compose cp app/routers/coffees.py coffee-snobbery:/app/app/routers/coffees.py && docker compose cp app/services/coffees.py coffee-snobbery:/app/app/services/coffees.py && docker compose cp app/events.py coffee-snobbery:/app/app/events.py && docker compose restart coffee-snobbery && docker compose exec coffee-snobbery python -m pytest -q tests/services/test_varietals.py tests/test_varietals.py::test_autocomplete_endpoint_returns_prefix_matches tests/test_varietals.py::test_create_varietal_on_the_fly</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/varietals.py` exists and defines `create_varietal` + `search_by_prefix`.
    - `grep -nE "DuplicateNameError" app/services/varietals.py` finds the IntegrityError catch + raise.
    - `app/events.py` defines `CATALOG_VARIETAL_CREATED`.
    - GET /coffees/varietal-autocomplete?q=Bou returns 200 with body containing 'Bourbon' (greppable in response body).
    - POST /coffees/varietals with form name='TestVar' + CSRF token returns 200 with the new chip; calling it a second time with 'testvar' raises DuplicateNameError → re-rendered as a form error.
    - All 3 service-layer tests and 2 autocomplete/create-on-the-fly integration tests pass.
  </acceptance_criteria>
  <done>Varietal service + autocomplete + create-on-the-fly endpoints work; tests green.</done>
</task>

<task type="auto">
  <name>Task 4: Restructure CoffeeCreate schema + coffees router to accept varietal_ids[]; replace single-varietal text input in coffee_form with chip-builder</name>
  <files>app/schemas/coffee.py, app/routers/coffees.py, app/templates/fragments/coffee_form.html, app/templates/pages/coffee_detail.html</files>
  <read_first>
    - app/schemas/coffee.py (current `varietal: str | None = Field(None, max_length=120)` line 39 area — drop and replace with `varietal_ids: list[int]`)
    - app/routers/coffees.py (_FORM_FIELDS, _NON_SCHEMA_FORM_KEYS, _hydrate_form_context — note Plan 01 added `origins` to context; this task ADDS `selected_varietals`)
    - app/templates/fragments/coffee_form.html (existing flavor_notes chip-builder pattern — same shape applies to varietals; current single `varietal` text input area to replace; `data-initial-chips='{{ ...|tojson }}'` single-quoted pattern at line 209)
    - app/templates/pages/coffee_detail.html (read-side display of varietal)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md sections "CATALOG-02/03: app/schemas/coffee.py restructuring" (subsection on varietal_ids) + "Special Pattern Notes 2"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 3 "Wire format for varietal_ids"
  </read_first>
  <action>
    (a) In `app/schemas/coffee.py`: REMOVE the `varietal: str | None = Field(None, max_length=120)` field. ADD `varietal_ids: list[int] = Field(default_factory=list)` (same pattern as `advertised_flavor_note_ids`). (b) In `app/routers/coffees.py`: remove `"varietal"` from `_FORM_FIELDS` and `_EMPTY_TO_NONE_FIELDS`. Add `"varietal_query"` to `_NON_SCHEMA_FORM_KEYS` (the autocomplete text input). In `_parse_form_payload`, read `varietal_ids = [int(x) for x in form_data.getlist("varietal_ids") if x]` and pass it to the service. In `_hydrate_form_context`, add `selected_varietals` to the returned dict: for edit mode load `coffee.varietals` (the ORM list); for create return `[]`. (c) In `app/templates/fragments/coffee_form.html`: REPLACE the current single varietal text input with a chip-builder mirroring the flavor_notes chip-builder block. The chip-builder must: render `selected_varietals` as seed chips via a single-quoted `data-initial-chips='{{ selected_varietals|tojson }}'` (Pitfall 5 — single quotes required); hidden inputs named `varietal_ids` (one per selected chip) so `form_data.getlist("varietal_ids")` works server-side; a text input named `varietal_query` (autocomplete query) wired to `hx-get="/coffees/varietal-autocomplete"` with `hx-trigger="keyup changed delay:200ms"` + `hx-target="<adjacent-autocomplete-list>"`; a dropdown showing matches; for unmatched queries an "Add 'X' as a new varietal?" button posting to `/coffees/varietals`. The new fragment may live inline in `coffee_form.html` or be split into `fragments/varietal_chip_builder.html` if length justifies — planner's discretion. The Alpine x-data scope MUST NOT collide with the existing flavor_notes chip-builder's scope (use a distinct x-data variable like `varietalChips` not `flavorNoteChips`). (d) In `app/templates/pages/coffee_detail.html`: replace the single varietal display with `{% for v in coffee.varietals %}{{ v.name }}{% if not loop.last %}, {% endif %}{% endfor %}` (or "—" if empty).
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest -q tests/test_varietals.py::test_coffee_with_two_varietals tests/test_varietals.py::test_coffee_with_zero_varietals</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "varietal:\\s*str" app/schemas/coffee.py` returns no matches (the single-text field is gone).
    - `grep -nE "varietal_ids:\\s*list\\[int\\]" app/schemas/coffee.py` finds the new field.
    - `grep -nE "varietal_query" app/routers/coffees.py` finds the _NON_SCHEMA_FORM_KEYS entry.
    - `grep -nE 'name="varietal_ids"' app/templates/fragments/coffee_form.html` finds the hidden chip inputs.
    - `grep -nE "data-initial-chips='" app/templates/fragments/coffee_form.html` returns ≥ 2 matches (existing flavor_notes + new varietals; both single-quoted).
    - `grep -cE 'data-initial-chips="' app/templates/fragments/coffee_form.html` returns 0 (no double-quoted forms — Pitfall 5).
    - `app/templates/fragments/coffee_form.html` no longer contains a standalone `name="varietal"` text input: `grep -cE 'name="varietal"[^_]' app/templates/fragments/coffee_form.html` returns 0.
    - POST /coffees with `varietal_ids=1&varietal_ids=2` creates a coffee with `len(coffee.varietals) == 2`.
    - POST /coffees with no varietal_ids creates a coffee with `coffee.varietals == []`.
    - Coffee detail page renders varietals correctly.
    - Both named tests pass.
  </acceptance_criteria>
  <done>Schema + router + form + detail page all use varietal_ids[]; chip-builder works; tests green.</done>
</task>

<task type="auto">
  <name>Task 5: Full-suite test pass + stray Coffee.varietal/Coffee.country reference audit</name>
  <files>app/, tests/</files>
  <read_first>
    - tests/test_varietals.py + tests/services/test_varietals.py + tests/test_roast_level_enum.py (the stubs from Task 0)
    - .planning/phases/15.1-catalog-session-polish/15.1-VALIDATION.md sampling rate
  </read_first>
  <action>
    (a) Promote any remaining Wave 0 stubs to green by completing service/router wiring if any task left gaps. (b) Audit: `grep -rnE "Coffee\\.varietal[^s]|coffee\\.varietal[^s]" app/ --include="*.py" --include="*.html" | grep -v "coffee_varietals\\|coffee\\.varietals" | grep -v "app/migrations/versions"`. Fix any stragglers. (c) Audit existing tests for stale `varietal=...` references: `grep -rnE "varietal\\s*=" tests/ --include="*.py" | grep -v "varietal_ids\\|varietals\\." `. Update test fixtures that pass `varietal="..."` to `Coffee(...)` or to schema constructors — they should now pass `varietal_ids=[...]` or omit the field. (d) Drop snobbery_test DB and run the full suite: `python -m pytest tests/ -q -rs`. Treat skips as suspect. (e) Run `ruff check app/ tests/` and `ruff format --check app/ tests/`; commit any style fixes separately.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d postgres -c "DROP DATABASE IF EXISTS snobbery_test" && docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest tests/ -q -rs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "Coffee\\.varietal[^s]|coffee\\.varietal[^s]" app/ --include="*.py" --include="*.html" | grep -v "coffee_varietals\\|coffee\\.varietals" | grep -v "app/migrations/versions"` returns empty.
    - All 22 tests across the three new test files pass.
    - Full pytest suite passes (exit 0). Skips are the pre-existing postgres-reachability gate.
    - `ruff check` and `ruff format --check` both pass.
  </acceptance_criteria>
  <done>Varietal m2m + roast enum land cleanly; no stray varietal text references; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/coffees/varietal-autocomplete GET | Authenticated user requests autocomplete fragment; q is the only user-supplied parameter |
| client→/coffees/varietals POST | Authenticated user creates a new varietal on the fly; CSRF-protected |
| client→/coffees POST with varietal_ids[] | varietal_ids[] is a list of integers; the service casts them via int() in _parse_form_payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-15 | Tampering (input validation) | varietal_ids[] | mitigate | `[int(x) for x in form_data.getlist("varietal_ids") if x]` strict cast in _parse_form_payload rejects non-integer values; SQLAlchemy FK constraint rejects nonexistent varietal_id at write time |
| T-15.1-16 | Tampering (CITEXT collision) | create_varietal | mitigate | DuplicateNameError pattern: IntegrityError caught after db.flush(), SAVEPOINT rollback, re-query for the existing varietal so concurrent inserts don't both succeed-then-fail |
| T-15.1-17 | Tampering (CHECK constraint replacement atomicity) | p15_1_roast_level_enum migration | mitigate | Drop + recreate inside a single Alembic upgrade transaction; existing six values explicitly tested as still-valid in Wave 0 |
| T-15.1-18 | Information Disclosure | varietal autocomplete prefix query | accept | varietals are shared catalog data (household model); no per-user filtering needed; auth-gated to prevent unauthenticated enumeration |
| T-15.1-19 | DoS | Unbounded autocomplete result size | mitigate | search_by_prefix uses .limit(50) (mirroring flavor_notes); empty q returns no rows |
| T-15.1-20 | Repudiation | varietal creation provenance | mitigate | structlog `CATALOG_VARIETAL_CREATED` event emits varietal_id + user_id at create time (PATTERNS.md "Structlog audit events") |
| T-15.1-21 | CSRF on POST /coffees/varietals | session forgery | mitigate | CSRF token via the existing starlette-csrf middleware; the inline-create chip-builder fragment must embed the CSRF token same as other state-changing forms |
</threat_model>

<verification>
- Migration chain runs cleanly: `alembic upgrade head` exits 0, leaving p15_1_varietal_m2m as head.
- psql `SELECT COUNT(*) FROM varietals` returns 14.
- POST /coffees with varietal_ids=[1,2] yields a coffee whose `coffee.varietals` list has length 2.
- Creating 'Geisha' then 'geisha' raises DuplicateNameError.
- Coffee form chip-builder renders with seed chips on edit; autocomplete returns matches; "Add as a new varietal?" affordance works.
- Coffee detail page renders varietals as a comma-separated list.
- coffees.varietal column is gone; no source file under app/ references `coffee.varietal` (single text) except the deleted varietal column.
- COFFEE_ROAST_LEVELS tuple is the 8-value sequence.
- Full pytest suite green.
</verification>

<success_criteria>
- Roast enum widened (CATALOG-04).
- varietals + coffee_varietals tables created, 14 seeded, coffees.varietal dropped (CATALOG-05).
- Varietal service mirrors flavor_notes API.
- Coffee form chip-builder with autocomplete + create-on-the-fly works.
- Coffee detail page renders multi-varietal correctly.
- Full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-catalog-session-polish/15.1-03-SUMMARY.md` recording: migration revisions in chain, files modified, test counts, any deviations.
</output>
