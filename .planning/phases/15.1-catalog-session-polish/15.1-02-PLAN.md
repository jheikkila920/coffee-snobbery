---
phase: 15.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/bag.py
  - app/schemas/bag.py
  - app/schemas/brew_csv.py
  - app/services/analytics.py
  - app/services/bags.py
  - app/services/csv_io.py
  - app/services/ai_service.py
  - app/routers/bags.py
  - app/routers/home.py
  - app/templates/fragments/bag_form.html
  - app/templates/fragments/bag_row.html
  - app/templates/pages/home.html
  - app/templates/fragments/home/roast_freshness.html
  - app/migrations/versions/p15_1_drop_roast_date.py
  - tests/test_freshness_removal.py
  - tests/services/test_analytics.py
  - tests/services/test_brew_csv.py
  - tests/routers/test_brew_list_csv.py
  - tests/phase_04/test_schemas_form_validation.py
  - tests/phase_04/test_routers_bags.py
autonomous: true
requirements: [CATALOG-07]
must_haves:
  truths:
    - "Per D-16: the bags.roast_date column is dropped via a single Alembic migration with no data preservation; pg_dump backups are the recovery story"
    - "Per D-17: app/services/analytics.py compute_input_signature no longer reads Bag.roast_date — removing it from the signature input forces the next nightly @ 00:00 AI regen to detect a signature mismatch and regenerate per-user (no startup force-regen, no immediate wipe)"
    - "Per D-18: app/services/ai_service.py and any prompt-builder module contains zero references to the strings 'freshness', 'roast date', 'roast_date', 'days off roast', or 'resting' (planner QA gate; treat zero matches as a pass since RESEARCH found no current matches but the audit is mandatory)"
    - "Per D-19: freshness_buckets() is removed from app/services/analytics.py entirely; app/templates/fragments/home/roast_freshness.html is deleted; the router call card_roast_freshness in app/routers/home.py is deleted; the home page section for freshness is deleted"
    - "Per D-20: app/services/csv_io.py drops roast_date from EXPORT_FIELDNAMES and _HEADER_ALIASES; the _resolve_bag(coffee_id, roast_date_raw) function is deleted; all imported brew sessions get bag_id=None; the export writer no longer reads Bag.roast_date.isoformat(); app/schemas/brew_csv.py drops the bag_label field"
    - "After this plan, grep -rnE 'roast_date|freshness|days off roast|days_off_roast' app/ --include='*.py' --include='*.html' (excluding app/migrations/versions/) returns ZERO matches (with the bag_row.html label replaced per the Open Question 1 resolution below)"
    - "Bag row display open question resolved: bag_row.html primary label uses weight + opened/finished status when present, falling back to 'Bag #{id}' when both are absent (planner's discretion per RESEARCH open question 1, recommended option per RESEARCH)"
  artifacts:
    - path: app/migrations/versions/p15_1_drop_roast_date.py
      provides: "Migration that drops bags.roast_date column"
      contains: "p15_1_drop_roast_date"
    - path: tests/test_freshness_removal.py
      provides: "Wave 0 test stubs + greppable-absence assertions + signature-regen test"
  key_links:
    - from: app/services/analytics.py
      to: app/services/ai_service.py
      via: "compute_input_signature feeds the nightly AI cache key; removing Bag.roast_date from the select changes the signature and triggers regen on next nightly run"
      pattern: "compute_input_signature"
    - from: app/services/csv_io.py
      to: app/services/brew_sessions.py
      via: "import_brews now sets bag_id=None on every imported row; brew_sessions accept the row with no bag resolution"
      pattern: "bag_id = None"
---

<objective>
Remove roast-freshness tracking app-wide: drop `bags.roast_date` via a no-preservation migration (D-16), remove `freshness_buckets()` from analytics + its home-page surface (D-19), strip freshness/roast_date references from AI prompts (D-18 audit), trust nightly cadence to invalidate AI cache via signature change (D-17), and clean up CSV import/export (D-20).

Purpose: The feature is being removed on purpose. Removing the freshness signal simplifies analytics + AI prompts + UI. Backups (pg_dump nightly) are the recovery story for the data.
Output: One migration + ~15 source file edits + freshness fragment deletion + AI prompt audit + 6 test updates + 1 new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15.1-catalog-session-polish/15.1-CONTEXT.md
@.planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md
@.planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Write failing tests for freshness removal (Wave 0)</name>
  <files>tests/test_freshness_removal.py</files>
  <read_first>
    - tests/services/test_analytics.py (skip-gate `_require_postgres` pattern lines 25-48; current `test_roast_freshness_buckets` at line 457 to be deleted in Task 5)
    - tests/conftest.py (DB fixtures)
    - .planning/phases/15.1-catalog-session-polish/15.1-VALIDATION.md (Wave 0 requirements)
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 5 "Tests to Update" table
  </read_first>
  <action>
    Create `tests/test_freshness_removal.py` with failing stubs covering: (1) `test_bags_roast_date_column_gone` - `SELECT column_name FROM information_schema.columns WHERE table_name='bags' AND column_name='roast_date'` returns 0 rows; (2) `test_freshness_card_endpoint_returns_404` - GET /home/cards/roast-freshness returns 404; (3) `test_freshness_fragment_template_does_not_exist` - `os.path.exists("app/templates/fragments/home/roast_freshness.html")` is False; (4) `test_analytics_module_has_no_freshness_buckets` - `assert not hasattr(analytics, "get_roast_freshness_buckets")`; (5) `test_compute_input_signature_omits_bag_roast_date` - signature builder for a user with sessions does not include any element shaped like an ISO date string from a Bag; (6) `test_signature_changes_after_freshness_removal` - prior captured signature (from a baked test fixture or recomputed pre-migration) differs from current signature for the same input data; (7) `test_csv_export_fieldnames_has_no_roast_date` - `assert "roast_date" not in csv_io.EXPORT_FIELDNAMES`; (8) `test_csv_header_aliases_has_no_roast_date` - `assert "roast_date" not in csv_io._HEADER_ALIASES`; (9) `test_resolve_bag_function_gone` - `assert not hasattr(csv_io, "_resolve_bag")`; (10) `test_grep_app_for_roast_date_returns_zero` - shell out to grep over app/ (excluding app/migrations/versions/) and assert exit_code==1 (grep returns 1 when zero matches); (11) `test_grep_ai_service_for_freshness_returns_zero` - same shape but on `app/services/ai_service.py` and the strings "freshness", "roast date", "days off roast", "resting" — D-18 QA gate; (12) `test_bag_form_template_has_no_roast_date_input` - `grep -c "name=\"roast_date\"" app/templates/fragments/bag_form.html` returns 0; (13) `test_bag_row_template_label_replaced` - the file no longer contains the literal string "Roasted {{" or "Roast date unknown"; (14) `test_home_page_section_removed` - `grep -c "freshness-heading" app/templates/pages/home.html` returns 0. Use postgres skip-gate on DB-touching tests.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/test_freshness_removal.py --collect-only 2>&1 | grep -c "test_"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_freshness_removal.py` exists.
    - `pytest --collect-only` collects exactly 14 tests.
    - All 14 tests fail or skip before Tasks 1-5 land.
  </acceptance_criteria>
  <done>Test stubs collect cleanly; will go green after Tasks 1-5.</done>
</task>

<task type="auto">
  <name>Task 1: Create migration to drop bags.roast_date + update Bag model/schema</name>
  <files>app/migrations/versions/p15_1_drop_roast_date.py, app/models/bag.py, app/schemas/bag.py</files>
  <read_first>
    - app/models/bag.py (current roast_date column line 54; date import line 29; Date import in sqlalchemy imports line 31)
    - app/schemas/bag.py (current roast_date field line 41; date import line 29; docstring line 12)
    - app/migrations/versions/p11_brew_time_seconds.py (current head; this plan's migration depends on it OR on plan 01's migration — see action note below)
    - app/migrations/versions/p4_shared_catalog.py (header pattern + downgrade convention)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md sections "CATALOG-07: app/models/bag.py and app/schemas/bag.py surgery"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 5 model/schema surgeries
  </read_first>
  <action>
    (a) Create `app/migrations/versions/p15_1_drop_roast_date.py` with `revision = "p15_1_drop_roast_date"` and `down_revision = "p11_brew_time_seconds"`. Note: Plan 01's migration is `p15_1_multi_origin` with `down_revision = "p11_brew_time_seconds"` too — this creates a branch. Resolve by setting THIS plan's `down_revision = "p15_1_multi_origin"` instead. Coordination: Plan 01 owns the migration just above; this plan stacks on top. If executors run Plan 02 before Plan 01 lands, this migration's down_revision cannot point at a non-existent revision; therefore Plan 02's source code changes (model + schema + service edits) ship independently but the migration revision file is created with `down_revision = "p15_1_multi_origin"` — execution order is enforced by the migration chain at `alembic upgrade head` time. Upgrade body: `op.drop_column("bags", "roast_date")`. Downgrade: forward-only no-op (`op.execute("SELECT 1")` + comment "forward-only per project policy"). No app.models imports. (b) In `app/models/bag.py`: line 29 change `from datetime import date, datetime` → `from datetime import datetime`; line 31 remove `Date` from the `from sqlalchemy import ...` line; line 54 DELETE the `roast_date: Mapped[date | None] = mapped_column(Date, nullable=True)` declaration entirely. (c) In `app/schemas/bag.py`: line 12 remove the docstring line `* ``roast_date``: optional date.`; line 29 change `from datetime import date, datetime` → `from datetime import datetime`; line 41 DELETE `roast_date: date | None = None` field.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery alembic upgrade head && docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d $POSTGRES_DB -c "SELECT column_name FROM information_schema.columns WHERE table_name='bags' AND column_name='roast_date'" | grep -c "0 rows"</automated>
  </verify>
  <acceptance_criteria>
    - `psql \d bags` does NOT show a `roast_date` column.
    - `grep -nE "roast_date" app/models/bag.py app/schemas/bag.py` returns no matches (excluding any docstring footnotes that have ALSO been cleaned).
    - `grep -nE "^from datetime import.*\\bdate\\b" app/models/bag.py app/schemas/bag.py` returns no matches (the `date` name should be unimported).
    - Migration file has no `from app.models` line.
    - Migration `down_revision = "p15_1_multi_origin"` (string-matches Plan 01's migration revision id).
    - Test `test_bags_roast_date_column_gone` passes.
  </acceptance_criteria>
  <done>Column dropped, model + schema surgical edits clean, migration chains after Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: Remove freshness_buckets from analytics.py + scrub Bag.roast_date from signature builder</name>
  <files>app/services/analytics.py</files>
  <read_first>
    - app/services/analytics.py (lines 165-217 `get_roast_freshness_buckets` function; lines 371/388-391/405/411 in `compute_input_signature` + `_serialize_row`; line 28 Bag import)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md section "CATALOG-07: app/services/analytics.py — freshness removal"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 5 inventory for analytics.py
  </read_first>
  <action>
    (a) DELETE the entire `get_roast_freshness_buckets` function and its preceding HOME-04 section comment (lines 165-217 per CONTEXT canonical_refs / RESEARCH inventory). (b) In `compute_input_signature` (lines 371-411 area): remove `Bag.roast_date` from the SELECT list, remove the `.outerjoin(Bag, BrewSession.bag_id == Bag.id)` join, remove the `Bag.roast_date.isoformat() if row.roast_date else None` element from `_serialize_row`. (c) After removing the Bag join, the `Bag` import at line 28 of `analytics.py` may become unused — verify by grepping the file for any remaining `Bag` reference; if none, remove the import. (d) Update any docstring text that mentions `roast_date` (line 371 "bag roast_date isoformat-or-None") — delete the corresponding bullet. (e) Drop the "Pitfall 4 (reads bags.roast_date ONLY)" comment from the analytics module per D-19 if present (grep `Pitfall 4` in analytics.py to find).
  </action>
  <verify>
    <automated>docker compose cp app/services/analytics.py coffee-snobbery:/app/app/services/analytics.py && docker compose restart coffee-snobbery && grep -nE "roast_date|freshness|freshness_buckets" app/services/analytics.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "roast_date|freshness|freshness_buckets|get_roast_freshness_buckets" app/services/analytics.py` returns 0.
    - `compute_input_signature` no longer references `Bag` (greppable).
    - Test `test_analytics_module_has_no_freshness_buckets`, `test_compute_input_signature_omits_bag_roast_date`, and `test_signature_changes_after_freshness_removal` pass.
    - The signature for a known fixture set differs from a pre-migration baked baseline (validated by test 6 in Task 0).
  </acceptance_criteria>
  <done>Analytics module fully scrubbed of freshness; signature simpler; AI cache regen will fire on next nightly cadence (D-17).</done>
</task>

<task type="auto">
  <name>Task 3: Remove freshness card route + home-page section + delete fragment file</name>
  <files>app/routers/home.py, app/templates/pages/home.html, app/templates/fragments/home/roast_freshness.html</files>
  <read_first>
    - app/routers/home.py (lines 13 doc comment, 195-211 `card_roast_freshness` handler)
    - app/templates/pages/home.html (lines 159-170 freshness section)
    - app/templates/fragments/home/roast_freshness.html (file to delete)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md "CATALOG-07: app/routers/home.py — freshness card removal"
  </read_first>
  <action>
    (a) In `app/routers/home.py`: delete the `card_roast_freshness` route handler entirely (lines 195-211 per RESEARCH inventory); delete the doc comment line 13 `GET /home/cards/roast-freshness`; remove any unused imports left behind (likely `analytics.get_roast_freshness_buckets` was already removed in Task 2). (b) In `app/templates/pages/home.html`: delete the entire `<section aria-labelledby="freshness-heading">` block (lines 159-170 per RESEARCH); ensure no dangling `<section>` closing tag mismatches remain. (c) Delete the file `app/templates/fragments/home/roast_freshness.html` entirely via `git rm`. Use the actual git removal so it shows up in the commit, not just file deletion.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest -q tests/test_freshness_removal.py::test_freshness_card_endpoint_returns_404 tests/test_freshness_removal.py::test_freshness_fragment_template_does_not_exist tests/test_freshness_removal.py::test_home_page_section_removed</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "card_roast_freshness\\|roast-freshness" app/routers/home.py` returns no matches.
    - `git ls-files app/templates/fragments/home/roast_freshness.html` returns empty (file is tracked-as-deleted).
    - `grep -n "freshness-heading\\|roast_freshness" app/templates/pages/home.html` returns no matches.
    - GET /home/cards/roast-freshness returns 404 (verified by test).
    - Loading the home page returns 200 with no template errors (Jinja resolves cleanly).
  </acceptance_criteria>
  <done>Home page no longer shows or fetches a freshness card; the fragment file is git-deleted.</done>
</task>

<task type="auto">
  <name>Task 4: Strip roast_date from bag service/router/forms and rewrite bag_row label</name>
  <files>app/services/bags.py, app/routers/bags.py, app/templates/fragments/bag_form.html, app/templates/fragments/bag_row.html</files>
  <read_first>
    - app/services/bags.py (`create_bag` lines 71-92, `update_bag` lines 133-149)
    - app/routers/bags.py (lines 85, 97, 210, 245, 302 per RESEARCH inventory)
    - app/templates/fragments/bag_form.html (lines 28-36 roast_date label block — DELETE)
    - app/templates/fragments/bag_row.html (lines 14-46 current display; lines 20-32 current "Roasted YYYY-MM-DD"/"Roast date unknown" + weight branch; rewrite primary label)
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md "Open Question 1: Bag row display after roast_date removal" — recommendation: weight-based label, fall back to opened/Bag #{id}
  </read_first>
  <action>
    (a) In `app/services/bags.py`: remove `roast_date` from `create_bag` signature + body (drop the kwarg + the pass-through to the `Bag(...)` constructor); same for `update_bag`. (b) In `app/routers/bags.py`: remove `"roast_date"` from `_EMPTY_TO_NONE_FIELDS` or similar field sets (per RESEARCH lines 85+97); remove `roast_date=form.roast_date` from service call at line 210; remove `"roast_date": bag.roast_date.isoformat() if bag.roast_date else ""` at line 245; remove `roast_date=form.roast_date` at line 302. (c) In `app/templates/fragments/bag_form.html`: DELETE the label block at lines 28-36 (the `<label>` with the `name="roast_date"` input). Leave weight_grams (line 38), opened_at (line 51), finished_at (line 62), notes (line 71) intact — RESEARCH explicit. (d) In `app/templates/fragments/bag_row.html`: REPLACE the current `{% if bag.roast_date %}Roasted...{% else %}Roast date unknown{% endif %}` block (lines 20-25 area) with a new primary label per the planner's discretion resolution: render `{% if bag.weight_grams %}{{ bag.weight_grams }}g bag{% elif bag.opened_at %}Bag opened {{ bag.opened_at.strftime('%Y-%m-%d') }}{% else %}Bag #{{ bag.id }}{% endif %}` followed by the existing `{% if is_finished %}<span>Finished</span>{% endif %}` pill. The secondary line (Opened/Not yet opened/finished) below stays unchanged.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest -q tests/test_freshness_removal.py::test_bag_form_template_has_no_roast_date_input tests/test_freshness_removal.py::test_bag_row_template_label_replaced tests/phase_04/test_routers_bags.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'roast_date' app/services/bags.py app/routers/bags.py app/templates/fragments/bag_form.html app/templates/fragments/bag_row.html` returns no matches.
    - `bag_row.html` no longer contains the literal string `Roasted ` or `Roast date unknown` (greppable).
    - `bag_form.html` no longer contains `name="roast_date"`.
    - GET /bags renders without 500 errors for: a bag with weight_grams only, a bag with opened_at only, a bag with both, a bag with neither (renders "Bag #{id}").
    - Tests `test_bag_form_template_has_no_roast_date_input` and `test_bag_row_template_label_replaced` pass.
    - Existing `tests/phase_04/test_routers_bags.py` passes (after the test fixtures are updated in Task 6 — this verify command may flag existing test failures if the fixtures still send `roast_date`; that's expected and fixed in Task 6).
  </acceptance_criteria>
  <done>Bag CRUD surface fully scrubbed; new bag label renders sensibly without roast_date.</done>
</task>

<task type="auto">
  <name>Task 5: CSV export/import cleanup (D-20) + AI prompt audit (D-18)</name>
  <files>app/services/csv_io.py, app/schemas/brew_csv.py, app/services/ai_service.py</files>
  <read_first>
    - app/services/csv_io.py (EXPORT_FIELDNAMES line 97, _HEADER_ALIASES line 129, _resolve_bag lines 239-261, call sites 369-374 + 394, export writer 583-612, _build_name_caches 625-644 bag cache sub-block)
    - app/schemas/brew_csv.py (bag_label field line 38)
    - app/services/ai_service.py (RESEARCH confirms ZERO current matches but D-18 mandates audit)
    - .planning/phases/15.1-catalog-session-polish/15.1-PATTERNS.md section "CATALOG-07: app/services/csv_io.py — D-20 cleanup"
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 5 csv_io inventory + ai_service.py note
  </read_first>
  <action>
    (a) `app/services/csv_io.py` per RESEARCH inventory: remove `"roast_date"` from `EXPORT_FIELDNAMES` (line 97); remove `"roast_date": "roast_date"` from `_HEADER_ALIASES` (line 129); DELETE the entire `_resolve_bag` function (lines 239-261); replace the `bag_id, bag_reason = _resolve_bag(...)` call + its if-block at lines 369-374 with `bag_id = None  # D-20: roast_date removed; imports always set bag_id=None`; remove the `bag_label=row.get("roast_date", "")` keyword from the `BrewCsvRow` construction at line 394; remove `"roast_date": bag_roast` from the export writer dict at line 593; remove the `bag_roast` variable at line 585; in `_build_name_caches` delete the entire `bag` sub-block (lines 625-644 area) including the `bag_ids` set construction and the `bag: dict[int, str]` dict (the bag cache only existed for `roast_date`; verify no other consumer remains). (b) `app/schemas/brew_csv.py`: remove the `bag_label` field (line 38) from `BrewCsvRow`. `extra="forbid"` stays. (c) `app/services/ai_service.py` D-18 audit: run `grep -cniE "freshness|roast.date|days.off.roast|resting" app/services/ai_service.py` and also against `app/services/ai_schemas.py` if it exists. If zero matches (RESEARCH confirms this is expected), the audit is a no-op and a comment `# D-18: freshness/roast_date audit complete (no references at time of removal)` may be added near the top of the prompt-builder function for future grep traceability. If ANY matches are found, scrub them: remove the offending sentence/clause from the prompt text. The Wave 0 test `test_grep_ai_service_for_freshness_returns_zero` enforces this. (d) Audit export filename patterns per CONTEXT Claude's Discretion: `grep -nE 'roast_date' app/services/csv_io.py` after the above — confirm no export filename template uses roast_date.
  </action>
  <verify>
    <automated>docker compose cp app/services/csv_io.py coffee-snobbery:/app/app/services/csv_io.py && docker compose cp app/schemas/brew_csv.py coffee-snobbery:/app/app/schemas/brew_csv.py && docker compose cp app/services/ai_service.py coffee-snobbery:/app/app/services/ai_service.py && docker compose restart coffee-snobbery && docker compose exec coffee-snobbery python -m pytest -q tests/test_freshness_removal.py::test_csv_export_fieldnames_has_no_roast_date tests/test_freshness_removal.py::test_csv_header_aliases_has_no_roast_date tests/test_freshness_removal.py::test_resolve_bag_function_gone tests/test_freshness_removal.py::test_grep_ai_service_for_freshness_returns_zero</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "roast_date" app/services/csv_io.py app/schemas/brew_csv.py app/services/ai_service.py` returns no matches.
    - `grep -nE "(freshness|roast.date|days.off.roast|resting)" app/services/ai_service.py` returns no matches (case-insensitive).
    - `_resolve_bag` function is gone (`grep -n "def _resolve_bag" app/services/csv_io.py` returns no matches).
    - CSV export no longer emits a `roast_date` column header — `assert "roast_date" not in csv_io.EXPORT_FIELDNAMES`.
    - Importing a CSV that still carries a `roast_date` column does NOT crash (the column is ignored because `_HEADER_ALIASES` no longer maps it; extra unknown columns get ignored by the existing import logic).
    - All four named verify-tests pass.
  </acceptance_criteria>
  <done>CSV surface clean, AI prompt audited, all greppable D-18 strings absent.</done>
</task>

<task type="auto">
  <name>Task 6: Update existing tests + full-suite green</name>
  <files>tests/services/test_analytics.py, tests/services/test_brew_csv.py, tests/routers/test_brew_list_csv.py, tests/phase_04/test_schemas_form_validation.py, tests/phase_04/test_routers_bags.py</files>
  <read_first>
    - tests/services/test_analytics.py (current `test_roast_freshness_buckets` at line 457, `_seed_analytics_scenario` with `Bag(..., roast_date=...)` fixtures)
    - tests/services/test_brew_csv.py (row fixtures using `roast_date: "2026-05-01"` at line 257, "named bag not found" test at lines 239-243, `_seed_bag(...)` calls with `roast_date`)
    - tests/routers/test_brew_list_csv.py (header at line 367 with `roast_date`)
    - tests/phase_04/test_schemas_form_validation.py (bag form validation tests referencing `roast_date`)
    - tests/phase_04/test_routers_bags.py (fixtures for create/edit bags)
    - .planning/phases/15.1-catalog-session-polish/15.1-RESEARCH.md sub-area 5 "Tests to Update" table
  </read_first>
  <action>
    (a) `tests/services/test_analytics.py`: DELETE the entire `test_roast_freshness_buckets` test function (line 457 area). In `_seed_analytics_scenario`, remove any `roast_date=...` kwarg from `Bag(...)` instantiations. (b) `tests/services/test_brew_csv.py`: in `_seed_bag(...)` calls, drop the `roast_date` kwarg if present; in the row 4 "named bag not found" case at lines 239-243, the test specifically checked `bag_reason` when `_resolve_bag` failed — after D-20 (`bag_id` is always None), this test needs to be rewritten or deleted. Replace with `test_csv_import_always_sets_bag_id_none` asserting that `BrewSession.bag_id is None` for all imported rows. Update the row dict at line 257 that uses `roast_date: "2026-05-01"` — drop the key or rename to a different valid column. (c) `tests/routers/test_brew_list_csv.py`: at line 367 in `_csv_bytes()` header, remove `roast_date` from the header tuple and the row data. (d) `tests/phase_04/test_schemas_form_validation.py`: remove any test asserting validation behavior on `BagCreate.roast_date` (the field no longer exists). (e) `tests/phase_04/test_routers_bags.py`: remove `roast_date=...` from bag create/edit test fixtures. (f) Run the full suite: `python -m pytest tests/ -q -rs`. Drop snobbery_test DB before the full run per project memory `full-suite-test-isolation-gaps`. Fix any remaining failures introduced by the schema change. Skips are suspect — confirm any skip is the pre-existing postgres-reachability gate from `tests/services/test_analytics.py`.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d postgres -c "DROP DATABASE IF EXISTS snobbery_test" && docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery pip install --user pytest pytest-asyncio respx && docker compose exec coffee-snobbery python -m pytest tests/ -q -rs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "roast_date" tests/ --include="*.py" | grep -v "tests/test_freshness_removal.py"` returns no matches (excluding the freshness-removal test file itself, which references the string in greppable assertions).
    - Full pytest suite passes (exit 0). Any reported skips are the pre-existing postgres-reachability gate.
    - All 14 tests in `tests/test_freshness_removal.py` pass.
    - `ruff check app/ tests/` and `ruff format --check app/ tests/` both pass.
    - Bash grep gate: `grep -rnE "roast_date|freshness|days off roast|days_off_roast" app/ --include="*.py" --include="*.html" | grep -v "app/migrations/versions"` returns no matches.
  </acceptance_criteria>
  <done>Freshness removal lands cleanly; all existing tests updated; full suite green; no stray roast_date strings in app/ source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration p15_1_drop_roast_date | Irreversible column drop; pg_dump nightly backup is the recovery story |
| nightly AI regen scheduler → analytics.compute_input_signature | Removing Bag.roast_date from the signature changes the cache key; the next nightly run regenerates per-user AI recs |
| client→/home/cards/roast-freshness | Endpoint deleted; clients hitting old URLs receive 404 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-10 | Tampering (data loss) | bags.roast_date column drop | accept (D-16) | The feature is being removed deliberately; pg_dump nightly backups are the recovery path; D-16 explicitly accepts this. The migration is atomic and runs once at container start. |
| T-15.1-11 | Information Disclosure (stale cached AI recs) | ai_recommendations rows with embedded freshness wording | mitigate | D-17: removing Bag.roast_date from compute_input_signature changes the signature hash, forcing the next nightly @ 00:00 cadence to regenerate per-user AI recs. Old cached recs remain in the DB until that regen overwrites them (worst case: one day of stale wording). |
| T-15.1-12 | Tampering (input validation) | CSV import with stale roast_date column | mitigate | After D-20, the CSV importer ignores any `roast_date` column from a legacy file (the header alias is gone); imported sessions always get bag_id=None. The import path is auth-gated. |
| T-15.1-13 | DoS (404 burst on deleted endpoint) | /home/cards/roast-freshness | accept | Endpoint deleted; FastAPI returns 404; existing rate-limit middleware applies; no per-user cost. |
| T-15.1-14 | Repudiation (AI prompt audit traceability) | ai_service.py D-18 audit | mitigate | Wave 0 test `test_grep_ai_service_for_freshness_returns_zero` enforces the absence at every CI run, providing ongoing regression coverage. |
</threat_model>

<verification>
- `alembic upgrade head` runs cleanly; `psql \d bags` shows no `roast_date` column.
- `grep -rnE "roast_date|freshness|days off roast|days_off_roast" app/ --include="*.py" --include="*.html" | grep -v "app/migrations/versions"` is empty.
- GET /home/cards/roast-freshness returns 404.
- Home page renders without the freshness section.
- Full pytest suite green.
- D-18 grep audit returns zero matches in ai_service.py.
- Signature regen verified by test_signature_changes_after_freshness_removal.
</verification>

<success_criteria>
- bags.roast_date column dropped.
- freshness_buckets function removed; home card route deleted; fragment file git-rm'd.
- CSV export/import cleaned (D-20).
- AI prompt audit complete (D-18) — zero greppable freshness strings in ai_service.py.
- Existing tests updated, full suite green.
- Bag row template displays a sensible non-roast_date label.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-catalog-session-polish/15.1-02-SUMMARY.md` recording migration revision, files modified, test counts, the bag_row label chosen, and any deviations.
</output>
