---
phase: 0
plan_id: 03
wave: 2
depends_on: [01]
requirements: FOUND-05, FOUND-06, CAT-04, AI-02
files_modified:
  - app/db.py
  - app/models/__init__.py
  - app/models/base.py
  - app/models/user.py
  - app/models/bag.py
  - app/models/wishlist_entry.py
  - app/models/ai_recommendation.py
  - app/models/app_setting.py
  - alembic.ini
  - app/migrations/__init__.py
  - app/migrations/env.py
  - app/migrations/script.py.mako
  - app/migrations/versions/__init__.py
  - app/migrations/versions/0001_initial.py
  - tests/test_migrations.py
autonomous: true
must_haves:
  truths:
    - "alembic upgrade head against a fresh Postgres 16 database installs citext, pg_trgm, and unaccent extensions before creating any tables (FOUND-06; CONTEXT D-03)"
    - "Five tables exist after the first migration: users, bags, wishlist_entries, ai_recommendations, app_settings (FOUND-05, CAT-04, AI-02, CONTEXT D-01/D-02)"
    - "ai_recommendations carries every cost-observability column from day one: tokens_input, tokens_output, tokens_input_search, web_search_count, provider_used, model_used, tool_version, url_verified, duration_ms, generated_by, error_status (AI-02, COST-1)"
    - "bags table ships with coffee_id as bigint NOT NULL with NO FK constraint; Phase 4 adds the FK when coffees exists (CAT-04, CONTEXT <Claude's Discretion>)"
    - "app_settings is seeded with exactly 19 documented rows including setup_completed=false and the AI tool-version rows (CONTEXT D-17)"
    - "SQLAlchemy engine is constructed with explicit pool knobs: pool_size=10, max_overflow=5, pool_timeout=5, pool_pre_ping=True, pool_recycle=300 (CONTEXT D-10, PITFALL SH-2)"
    - "alembic env.py imports app.models so DeclarativeBase.metadata is populated before autogenerate compares (Plan-only — autogenerate is not used for the first migration, but later phases depend on this wiring)"
  artifacts:
    - path: "app/db.py"
      provides: "SQLAlchemy 2.0 sync engine + sessionmaker + DeclarativeBase"
      contains: "create_engine"
    - path: "app/models/base.py"
      provides: "DeclarativeBase subclass exposed as `Base`"
      contains: "class Base(DeclarativeBase)"
    - path: "app/models/user.py"
      provides: "User SQLAlchemy 2.0 model"
      contains: "class User"
    - path: "app/models/bag.py"
      provides: "Bag SQLAlchemy 2.0 model (coffee_id without FK in Phase 0)"
      contains: "class Bag"
    - path: "app/models/wishlist_entry.py"
      provides: "WishlistEntry model"
      contains: "class WishlistEntry"
    - path: "app/models/ai_recommendation.py"
      provides: "AIRecommendation model with all cost-observability columns"
      contains: "tokens_input_search"
    - path: "app/models/app_setting.py"
      provides: "AppSetting model"
      contains: "class AppSetting"
    - path: "app/migrations/env.py"
      provides: "Alembic env.py for SQLAlchemy 2.0 + psycopg 3 + pydantic-settings DATABASE_URL"
      contains: "target_metadata"
    - path: "app/migrations/versions/0001_initial.py"
      provides: "The single initial migration: 3 extensions, 5 tables, 19 app_settings seed rows"
      contains: "CREATE EXTENSION IF NOT EXISTS citext"
    - path: "alembic.ini"
      provides: "Alembic config with script_location pointing at app/migrations"
      contains: "script_location"
    - path: "tests/test_migrations.py"
      provides: "Schema + seed-row introspection tests (run against a live Postgres in CI)"
      contains: "pg_extension"
  key_links:
    - from: "entrypoint.sh (Plan 04)"
      to: "app/migrations/versions/0001_initial.py"
      via: "alembic upgrade head before exec uvicorn"
      pattern: "alembic upgrade head"
    - from: "app/migrations/env.py"
      to: "app/db.py:Base"
      via: "from app.db import Base; target_metadata = Base.metadata"
      pattern: "target_metadata\\s*=\\s*Base\\.metadata"
    - from: "app/migrations/env.py"
      to: "app/config.py:settings"
      via: "config.set_main_option('sqlalchemy.url', settings.DATABASE_URL)"
      pattern: "set_main_option.*sqlalchemy\\.url"
    - from: "app/db.py engine"
      to: "Postgres 16 (coffee-snobbery-db)"
      via: "postgresql+psycopg:// driver URL"
      pattern: "postgresql\\+psycopg"
---

<objective>
Land the database layer — SQLAlchemy 2.0 sync engine + sessionmaker with the locked pool knobs, the five Phase 0 models, Alembic configuration, and the single mega-migration `0001_initial.py` that installs three Postgres extensions, creates the five tables, and seeds 19 documented `app_settings` rows.

Purpose: FOUND-05 (migrations auto-run on container start), FOUND-06 (citext / pg_trgm / unaccent), CAT-04 (bags table), AI-02 (full cost-observability column set). This plan is the load-bearing schema bet — `bags` and `ai_recommendations` land NOW so Phase 4 and Phase 7 do not retrofit columns onto populated tables (PROJECT.md Key Decisions row 11; PITFALL COST-1). The migration is hand-authored, not autogenerated, because autogenerate does not emit `CREATE EXTENSION` and gets column ordering wrong for cross-table FKs (RESEARCH §Implementation Strategy Step 4).

Output: One engine module, five model modules + base, complete Alembic configuration, one migration file, and a test file that introspects `pg_extension` + `information_schema.columns` to prove every column with the right type and nullability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-foundation/00-CONTEXT.md
@.planning/phases/00-foundation/00-RESEARCH.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@app/config.py
@CLAUDE.md

<interfaces>
<!--
Plan 03 publishes:

  from app.db import Base, engine, SessionLocal, dispose_engine
  from app.models import (User, Bag, WishlistEntry, AIRecommendation, AppSetting)

Engine contract (locked — do not deviate, PITFALL SH-2 / CONTEXT D-10):
  pool_size=10, max_overflow=5, pool_timeout=5, pool_pre_ping=True, pool_recycle=300, echo=False
  URL: settings.DATABASE_URL (form: postgresql+psycopg://USER:PASS@HOST:5432/DB)

Schema (from RESEARCH §Schema Design For First Migration, verbatim):

  Extensions (run first, idempotent):
    CREATE EXTENSION IF NOT EXISTS citext
    CREATE EXTENSION IF NOT EXISTS pg_trgm
    CREATE EXTENSION IF NOT EXISTS unaccent

  users (referenced by ai_recommendations.user_id, wishlist_entries.user_id):
    id              BIGINT PK (IDENTITY)
    username        CITEXT UNIQUE NOT NULL
    email           CITEXT NULL                  + partial unique idx WHERE email IS NOT NULL
    password_hash   TEXT NOT NULL
    is_admin        BOOLEAN NOT NULL DEFAULT false
    is_active       BOOLEAN NOT NULL DEFAULT true
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
    last_login_at   TIMESTAMPTZ NULL

  bags (NB: coffee_id has NO FK; Phase 4 adds it):
    id              BIGINT PK (IDENTITY)
    coffee_id       BIGINT NOT NULL              + btree idx
    roast_date      DATE NULL
    weight_grams    INTEGER NULL
    opened_at       TIMESTAMPTZ NULL
    finished_at     TIMESTAMPTZ NULL
    notes           TEXT NOT NULL DEFAULT ''
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()

  wishlist_entries (CONTEXT D-01 — present from day one):
    id              BIGINT PK (IDENTITY)
    user_id         BIGINT NOT NULL FK users.id ON DELETE CASCADE  + btree idx
    coffee_name     TEXT NOT NULL
    roaster_name    TEXT NULL
    source_url      TEXT NULL
    source          TEXT NULL                    -- "ai_recommendation"|"manual"
    notes           TEXT NOT NULL DEFAULT ''
    added_at        TIMESTAMPTZ NOT NULL DEFAULT now()
    purchased_at    TIMESTAMPTZ NULL

  ai_recommendations (AI-02 + COST-1):
    id                    BIGINT PK (IDENTITY)
    user_id               BIGINT NOT NULL FK users.id ON DELETE CASCADE
    recommendation_type   TEXT NOT NULL          -- 'coffee'|'equipment'|'paste_rank'|'sweet_spots'
    input_signature       TEXT NOT NULL          + btree idx (single-column)
    response_json         JSONB NOT NULL
    provider_used         TEXT NOT NULL          -- 'anthropic'|'openai'
    model_used            TEXT NOT NULL
    tool_version          TEXT NULL              -- e.g. 'web_search_20250305'
    tokens_input          INTEGER NOT NULL DEFAULT 0   (COST-1)
    tokens_output         INTEGER NOT NULL DEFAULT 0   (COST-1)
    tokens_input_search   INTEGER NOT NULL DEFAULT 0   (COST-1 — web-search billed input tokens)
    web_search_count      INTEGER NOT NULL DEFAULT 0   (COST-1)
    url_verified          BOOLEAN NULL                 (COST-1 — null until URL-verification task runs)
    duration_ms           INTEGER NULL                 (COST-1)
    generated_at          TIMESTAMPTZ NOT NULL DEFAULT now()
    generated_by          TEXT NOT NULL                -- 'scheduler'|'manual_refresh'
    error_status          TEXT NULL                    (populated only on failure)
    Indexes:
      ix_ai_recs_input_signature (input_signature)
      ix_ai_recs_user_type_generated (user_id, recommendation_type, generated_at DESC)

  app_settings (CONTEXT D-17):
    key                   TEXT PK
    value                 TEXT NULL                    -- string form or NULL
    value_type            TEXT NOT NULL                -- 'string'|'int'|'float'|'bool'|'json'|'null'
    description           TEXT NOT NULL DEFAULT ''
    updated_at            TIMESTAMPTZ NOT NULL DEFAULT now()
    updated_by_user_id    BIGINT NULL FK users.id ON DELETE SET NULL

  app_settings seed rows (19 — verbatim list in RESEARCH §Schema Design For First Migration §step 7).

Downgrade leaves extensions in place (RESEARCH §Schema Design §downgrade — dropping
extensions could break sibling databases on the same cluster).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQLAlchemy engine, DeclarativeBase, and the five model classes</name>
  <files>
    app/db.py,
    app/models/base.py,
    app/models/user.py,
    app/models/bag.py,
    app/models/wishlist_entry.py,
    app/models/ai_recommendation.py,
    app/models/app_setting.py,
    app/models/__init__.py
  </files>
  <read_first>
    app/config.py (Plan 01 — see how `settings.DATABASE_URL` is exposed),
    app/models/__init__.py (Plan 01 — currently a docstring-only file; this task fills it),
    .planning/phases/00-foundation/00-RESEARCH.md §Pattern 3 (the SQLAlchemy 2.0 sync engine + locked pool knobs — verbatim),
    .planning/phases/00-foundation/00-RESEARCH.md §Schema Design for First Migration (column-by-column shape for all five tables — read in full; the model classes must mirror it 1:1),
    .planning/phases/00-foundation/00-RESEARCH.md §Notes on schema choices (the bigint-vs-uuid, citext, FK-deferred decisions),
    .planning/phases/00-foundation/00-CONTEXT.md §Decisions (D-10 pool knobs; D-11 model layout),
    .planning/phases/00-foundation/00-CONTEXT.md §Claude's Discretion (column-type details for users — username citext UNIQUE NOT NULL, password_hash text NOT NULL, etc.),
    .planning/research/PITFALLS.md SH-2 (pool exhaustion rationale),
    CLAUDE.md §"Code conventions" (SQLAlchemy 2.0 style — typed `Mapped[...]` columns, `select()` constructs, never legacy Query)
  </read_first>
  <action>
    Author `app/db.py` exporting (per RESEARCH §Pattern 3, verbatim):
      - `engine = create_engine(settings.DATABASE_URL, pool_size=10, max_overflow=5, pool_timeout=5, pool_pre_ping=True, pool_recycle=300, echo=False)`
      - `SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)`
      - `def dispose_engine() -> None: engine.dispose()`
      - Re-export `Base` from `app.models.base` so callers can `from app.db import Base, engine, SessionLocal, dispose_engine`.

    Author `app/models/base.py` containing only `class Base(DeclarativeBase): pass` with `from __future__ import annotations` and `from sqlalchemy.orm import DeclarativeBase`.

    Author each model in its own module under `app/models/`, all SQLAlchemy 2.0 style with `Mapped[...]` typed columns and `mapped_column(...)`. Each model includes `__tablename__`, all columns from the Interfaces block above, server defaults for `created_at`/`updated_at` (where applicable) via `server_default=func.now()`, and any indexes via `__table_args__`. Specific shapes (use these as exact field names — do NOT improvise):

    - `app/models/user.py` — class `User`, `__tablename__ = "users"`. Use `from sqlalchemy.dialects.postgresql import CITEXT` for the `username` and `email` columns (psycopg-3-compatible). `username` is `Mapped[str] = mapped_column(CITEXT, unique=True, nullable=False)`. `email` is `Mapped[str | None] = mapped_column(CITEXT, nullable=True)`. Define the partial unique index on email via `Index("ix_users_email", "email", unique=True, postgresql_where=text("email IS NOT NULL"))` in `__table_args__`. Booleans use `server_default=text("false")` / `text("true")` for portability.

    - `app/models/bag.py` — class `Bag`, `__tablename__ = "bags"`. `coffee_id: Mapped[int] = mapped_column(BigInteger, nullable=False)` — NO `ForeignKey(...)` (Phase 4 adds it). `notes: Mapped[str] = mapped_column(Text, nullable=False, server_default="")`. Single index in `__table_args__`: `Index("ix_bags_coffee_id", "coffee_id")`.

    - `app/models/wishlist_entry.py` — class `WishlistEntry`, `__tablename__ = "wishlist_entries"`. `user_id: Mapped[int] = mapped_column(BigInteger, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)`. Single index `ix_wishlist_entries_user_id`.

    - `app/models/ai_recommendation.py` — class `AIRecommendation`, `__tablename__ = "ai_recommendations"`. Use `from sqlalchemy.dialects.postgresql import JSONB`. `response_json: Mapped[dict] = mapped_column(JSONB, nullable=False)`. Every cost-observability column listed in the Interfaces block MUST be present with the right nullable + server_default. Two indexes in `__table_args__`:
        `Index("ix_ai_recs_input_signature", "input_signature")`
        `Index("ix_ai_recs_user_type_generated", "user_id", "recommendation_type", text("generated_at DESC"))`

    - `app/models/app_setting.py` — class `AppSetting`, `__tablename__ = "app_settings"`. `key: Mapped[str] = mapped_column(Text, primary_key=True)`. `updated_by_user_id: Mapped[int | None] = mapped_column(BigInteger, ForeignKey("users.id", ondelete="SET NULL"), nullable=True)`.

    All `id` PKs across the five tables are `Mapped[int] = mapped_column(BigInteger, Identity(always=False), primary_key=True)` (Postgres-native IDENTITY column — RESEARCH §Notes on schema choices).

    Update `app/models/__init__.py` to re-export every model so Alembic's metadata discovery is complete:
      `from app.models.base import Base`
      `from app.models.user import User`
      `from app.models.bag import Bag`
      `from app.models.wishlist_entry import WishlistEntry`
      `from app.models.ai_recommendation import AIRecommendation`
      `from app.models.app_setting import AppSetting`
      `__all__ = ["Base", "User", "Bag", "WishlistEntry", "AIRecommendation", "AppSetting"]`

    DO NOT read `os.environ` anywhere in `app/db.py` or the model modules — use `settings.DATABASE_URL` (FOUND-10).
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'.'); from app.models import Base, User, Bag, WishlistEntry, AIRecommendation, AppSetting; tables = set(Base.metadata.tables.keys()); expected = {'users','bags','wishlist_entries','ai_recommendations','app_settings'}; missing = expected - tables; assert not missing, f'missing tables: {missing}'; ai_cols = set(Base.metadata.tables['ai_recommendations'].columns.keys()); cost_cols = {'tokens_input','tokens_output','tokens_input_search','web_search_count','provider_used','model_used','tool_version','url_verified','duration_ms','generated_by','error_status'}; missing_cost = cost_cols - ai_cols; assert not missing_cost, f'missing cost-obs columns: {missing_cost}'; bag_cols = set(Base.metadata.tables['bags'].columns.keys()); bag_expected = {'id','coffee_id','roast_date','weight_grams','opened_at','finished_at','notes','created_at','updated_at'}; assert bag_expected.issubset(bag_cols), f'bag missing: {bag_expected - bag_cols}'; print('Models OK; 5 tables, cost-obs columns + bag columns present')"</automated>
  </verify>
  <done>
    All five model classes exist; importing `app.models` populates `Base.metadata.tables` with exactly the five expected table names. Every cost-observability column listed in the Interfaces block is present on `AIRecommendation` (introspectable via `Base.metadata.tables['ai_recommendations'].columns.keys()`). `bags.coffee_id` is `BigInteger NOT NULL` with NO `ForeignKey` constraint (introspectable: `[fk for fk in Base.metadata.tables['bags'].columns['coffee_id'].foreign_keys]` returns an empty list). `app/db.py` exposes `engine`, `SessionLocal`, `Base`, and `dispose_engine`. FOUND-10 not regressed (`tests/test_no_direct_env.py` still green).
  </done>
</task>

<task type="auto">
  <name>Task 2: Alembic configuration + the single 0001_initial.py migration</name>
  <files>
    alembic.ini,
    app/migrations/__init__.py,
    app/migrations/env.py,
    app/migrations/script.py.mako,
    app/migrations/versions/__init__.py,
    app/migrations/versions/0001_initial.py
  </files>
  <read_first>
    app/models/__init__.py (Task 1 — read so executor knows Base.metadata is populated by `import app.models`),
    app/db.py (Task 1 — `from app.db import Base` is the canonical metadata source),
    .planning/phases/00-foundation/00-RESEARCH.md §Pattern 4 (env.py for SQLAlchemy 2.0 + psycopg 3 — verbatim),
    .planning/phases/00-foundation/00-RESEARCH.md §Schema Design For First Migration (the verbatim `upgrade()` and `downgrade()` body),
    .planning/phases/00-foundation/00-RESEARCH.md §Implementation Strategy Step 4 (do NOT use autogenerate for the first migration — hand-author),
    .planning/phases/00-foundation/00-CONTEXT.md §Decisions D-02 (one mega-migration) and D-03 (CREATE EXTENSION IF NOT EXISTS via op.execute) and D-17 (19 seed rows — verbatim list with `key`, `value`, `value_type`, `description`),
    .planning/REQUIREMENTS.md FOUND-05, FOUND-06, CAT-04, AI-02 verbatim
  </read_first>
  <action>
    Run `alembic init app/migrations` ONCE in a scratch environment to generate the stock `script.py.mako` and `env.py` skeleton; the planner-authored `env.py` will REPLACE Alembic's generated one — only keep `script.py.mako` (verbatim from the alembic template) and the directory shape.

    Author `alembic.ini` at the repo root (one file, ~30 lines) with these settings:
      - `[alembic]` section: `script_location = app/migrations`, `sqlalchemy.url =` (empty — overridden at runtime by `env.py`), `prepend_sys_path = .`, `version_path_separator = os`, `truncate_slug_length = 40`.
      - NO `[loggers]` / `[handlers]` / `[formatters]` blocks (we use structlog via `app/logging.py`; Alembic's stdlib logging is fine for migration runs and inherits from `logging.basicConfig` when env.py omits `fileConfig`).

    Author `app/migrations/__init__.py` as an empty file (Alembic doesn't require it, but Python tooling does).

    Author `app/migrations/env.py` per RESEARCH §Pattern 4, verbatim. Key points:
      - `from app.config import settings` (FOUND-10 compliance: this is the only env read path).
      - `from app.db import Base`
      - `import app.models  # noqa: F401` (forces every model module to load so `Base.metadata` is complete).
      - `config.set_main_option("sqlalchemy.url", settings.DATABASE_URL)` — runtime URL override.
      - `target_metadata = Base.metadata`.
      - Both `run_migrations_offline()` and `run_migrations_online()` defined per the pattern; `poolclass=pool.NullPool` for online.

    Author `app/migrations/script.py.mako` — paste the verbatim Alembic generic template (the file `alembic init` generates). It is referenced by `alembic revision` in future plans (Phase 1+).

    Author `app/migrations/versions/__init__.py` as an empty file.

    Author `app/migrations/versions/0001_initial.py` per RESEARCH §Schema Design For First Migration, verbatim — one migration file with `revision = "0001_initial"`, `down_revision = None`, `branch_labels = None`, `depends_on = None`, and `upgrade() / downgrade()` functions. Critical content:

    1. Three `op.execute("CREATE EXTENSION IF NOT EXISTS <ext>")` calls FIRST, in order: `citext`, `pg_trgm`, `unaccent` (FOUND-06; CONTEXT D-03).

    2. `users` table — every column from the Interfaces block, using `postgresql.CITEXT` for `username` (UNIQUE NOT NULL) and `email` (NULL); `password_hash TEXT NOT NULL`; boolean defaults via `sa.false()` / `sa.true()`; timestamp defaults via `sa.func.now()`. Create `ix_users_email` partial unique index after the table.

    3. `bags` table — every column from the Interfaces block. `coffee_id` is `sa.BigInteger, nullable=False` with NO `sa.ForeignKey(...)`. Add a comment in the migration file: `# bags.coffee_id FK constraint is deferred to Phase 4 when coffees table exists`. Create `ix_bags_coffee_id` btree.

    4. `wishlist_entries` table — every column from the Interfaces block. `user_id` is `sa.BigInteger, sa.ForeignKey("users.id", ondelete="CASCADE"), nullable=False`. Create `ix_wishlist_entries_user_id` btree.

    5. `ai_recommendations` table — every column from the Interfaces block, INCLUDING all 11 cost-observability columns (PITFALL COST-1 — this is the load-bearing assertion of this plan). `response_json` is `postgresql.JSONB, nullable=False`. Create both indexes:
       `op.create_index("ix_ai_recs_input_signature", "ai_recommendations", ["input_signature"])`
       `op.create_index("ix_ai_recs_user_type_generated", "ai_recommendations", ["user_id", "recommendation_type", sa.text("generated_at DESC")])`

    6. `app_settings` table — `key` is PK (TEXT), `value_type` is TEXT NOT NULL, `updated_by_user_id` FK to users.id ON DELETE SET NULL.

    7. Seed 19 rows into `app_settings` via `op.bulk_insert(app_settings_table, [...])`. The `app_settings_table` is built via `sa.table(...)` at the top of the `upgrade()` function so the migration is self-contained and does NOT depend on the model classes (Alembic-safe pattern). Use the verbatim row list from RESEARCH §Schema Design §step 7 — all 19 rows, with their `key`, `value`, `value_type`, and `description` strings exactly as listed. CRITICAL rows that downstream phases depend on:
       - `setup_completed = "false"` (Phase 2 reads under SELECT ... FOR UPDATE; SEC-5)
       - `ai_tool_version_anthropic = "web_search_20250305"` (Phase 7 / AI-5)
       - `ai_tool_version_openai = "web_search"` (Phase 7 / AI-5)
       - `min_sessions_for_ai = "3"` and `min_flavor_notes_for_ai = "5"` (AI-7 cold-start)
       - `ai_primary_max_searches = "5"` and `ai_broadened_max_searches = "3"` (COST-5)
       - `last_ai_run_status = "never_run"` and `last_backup_status = "never_run"` (Phase 9 health panel)
       - `last_backup_at` with `value = None` and `value_type = "null"`
       - All four `home_*` rows for Phase 6 analytics
       - `photo_max_bytes = "5242880"` (Phase 4) and `csv_import_max_rows = "5000"` (Phase 5)
       - `recommendation_region = "US"` (Phase 7) and `ai_provider_default = "anthropic"`

    `downgrade()` drops the five tables in REVERSE creation order (`app_settings`, `ai_recommendations`, `wishlist_entries`, `bags`, `users`) and explicitly DOES NOT drop the three extensions (RESEARCH §Schema Design §downgrade — extensions are cluster-shared).

    DO NOT use Alembic autogenerate to create this file — the entire file is hand-authored from the RESEARCH §Schema Design section because autogenerate emits neither `CREATE EXTENSION` nor index ordering correctly across tables.
  </action>
  <verify>
    <automated>python -c "import importlib.util, pathlib; spec = importlib.util.spec_from_file_location('m', 'app/migrations/versions/0001_initial.py'); mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); src = pathlib.Path('app/migrations/versions/0001_initial.py').read_text(); assert 'CREATE EXTENSION IF NOT EXISTS citext' in src; assert 'CREATE EXTENSION IF NOT EXISTS pg_trgm' in src; assert 'CREATE EXTENSION IF NOT EXISTS unaccent' in src; assert mod.revision == '0001_initial'; assert mod.down_revision is None; for k in ['setup_completed', 'ai_tool_version_anthropic', 'min_sessions_for_ai', 'ai_primary_max_searches', 'last_ai_run_status', 'last_backup_status', 'last_backup_at', 'home_recent_brews_limit', 'home_top_coffees_limit', 'home_top_coffees_min_sessions', 'home_top_flavors_min_rating', 'home_sweetspot_min_sessions', 'photo_max_bytes', 'csv_import_max_rows', 'recommendation_region', 'ai_provider_default', 'min_flavor_notes_for_ai', 'ai_broadened_max_searches', 'ai_tool_version_openai']: assert k in src, f'seed key missing: {k}'; print('Migration source OK; 19 seed keys + 3 extensions referenced')"</automated>
  </verify>
  <done>
    `alembic.ini` exists with `script_location = app/migrations`. `app/migrations/env.py` exists; importing it does not raise (env vars are stub-set by Plan 01's conftest in tests, or real `.env` in production). `app/migrations/versions/0001_initial.py` exists; static-source inspection finds: three `CREATE EXTENSION` lines, all five table definitions, every cost-observability column on `ai_recommendations`, and all 19 seed keys named in CONTEXT D-17 present in `app_settings` after the migration applies. `downgrade()` drops tables in reverse order and does NOT call `DROP EXTENSION`.
  </done>
</task>

<task type="auto">
  <name>Task 3: tests/test_migrations.py — schema + seed introspection against a live Postgres</name>
  <files>tests/test_migrations.py</files>
  <read_first>
    app/models/__init__.py + the five model files (Task 1),
    app/migrations/versions/0001_initial.py (Task 2),
    tests/conftest.py (Plan 01 — read so executor sees existing env-var stubs),
    .planning/phases/00-foundation/00-RESEARCH.md §Validation Architecture (the unit tests `test_bags_columns`, `test_ai_recommendations_columns`),
    .planning/phases/00-foundation/00-RESEARCH.md §Sampling Rate (transactional pytest fixture pointing at the compose DB with rollback),
    .planning/phases/00-foundation/00-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
    Author `tests/test_migrations.py` containing the schema introspection tests required by RESEARCH §Validation Architecture. Implementation approach:

    1. **Test fixture strategy:** Add a session-scoped pytest fixture `pg_session` to `tests/test_migrations.py` (NOT to `conftest.py` — these are integration tests that require a running Postgres; placing them here lets unit tests in `test_logging.py` / `test_env_example.py` run without DB). The fixture:
       - Reads `DATABASE_URL` from `app.config.settings` (which the conftest autouse fixture already stubbed; in CI / docker compose, the real URL is set by the environment).
       - Constructs a fresh SQLAlchemy engine, opens a connection, and yields a connection.
       - On teardown, closes the connection and disposes the engine.

    2. **Skip behavior when DB unavailable:** Wrap the connection attempt in a `try/except` for `psycopg.OperationalError` (and `sqlalchemy.exc.OperationalError`). On failure, call `pytest.skip("Postgres not reachable — run via `make smoke` or `docker compose up -d`")`. This keeps the suite green in pure-unit pytest runs and red in the integration smoke run (Plan 05 wires `make smoke`).

    3. **Tests to author:**

       - `test_three_extensions_installed(pg_session)` — `SELECT extname FROM pg_extension WHERE extname IN ('citext','pg_trgm','unaccent')` returns 3 rows. Asserts exact set match.

       - `test_five_tables_exist(pg_session)` — `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('users','bags','wishlist_entries','ai_recommendations','app_settings')` returns 5 rows.

       - `test_bags_columns(pg_session)` — `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='bags'` returns all 9 expected columns with the right `data_type` and `is_nullable` per the Interfaces block. CAT-04 traceability comment in the docstring.

       - `test_bags_coffee_id_has_no_foreign_key(pg_session)` — `SELECT count(*) FROM information_schema.table_constraints WHERE table_name='bags' AND constraint_type='FOREIGN KEY'` returns 0 (CONTEXT — coffee_id FK is deferred to Phase 4).

       - `test_ai_recommendations_columns(pg_session)` — every column in the Interfaces block is present. CRITICAL assertion: `{'tokens_input','tokens_output','tokens_input_search','web_search_count','provider_used','model_used','tool_version','url_verified','duration_ms','generated_by','error_status'}` is a subset of the column names. AI-02 / COST-1 traceability comment.

       - `test_ai_recommendations_indexes(pg_session)` — `SELECT indexname FROM pg_indexes WHERE tablename='ai_recommendations'` includes `ix_ai_recs_input_signature` and `ix_ai_recs_user_type_generated`.

       - `test_app_settings_seeded_with_19_rows(pg_session)` — `SELECT count(*) FROM app_settings` returns ≥19 (use ≥ rather than == in case future phases add seed rows; document this in a comment).

       - `test_app_settings_critical_keys_present(pg_session)` — `SELECT key FROM app_settings WHERE key IN (...)` against the critical-key list: `setup_completed`, `ai_tool_version_anthropic`, `min_sessions_for_ai`, `min_flavor_notes_for_ai`, `ai_primary_max_searches`, `last_ai_run_status`, `recommendation_region`. Asserts all returned. CONTEXT D-17 traceability.

       - `test_app_settings_setup_completed_is_false(pg_session)` — `SELECT value, value_type FROM app_settings WHERE key='setup_completed'` returns `('false', 'bool')`. SEC-5 traceability — Phase 2's setup-race defense depends on this exact value.

    4. **Imports:** `from __future__ import annotations`, `import pytest`, `from sqlalchemy import create_engine, text`, `from app.config import settings`.
  </action>
  <verify>
    <automated>pytest tests/test_migrations.py -x --tb=short -k "not pg_session" || pytest tests/test_migrations.py -x --tb=short --co -q</automated>
  </verify>
  <done>
    `tests/test_migrations.py` exists with all eight test functions listed. Collection succeeds: `pytest tests/test_migrations.py --co -q` lists every test. When run against a Postgres without the migration applied, the tests skip (psycopg.OperationalError caught) rather than error. When run inside the compose stack after `alembic upgrade head` (i.e. Plan 05's `make smoke`), every test passes. The pg_session fixture is local to this file, not in `conftest.py`, so other unit tests still run without a DB.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | Migration files run with the database role's privileges; `CREATE EXTENSION` requires superuser — the `postgres` role in the alpine image is the default superuser, which is fine for v1 single-tenant deployment |
| `settings.DATABASE_URL` → `app/migrations/env.py` | The URL crosses from pydantic-settings into Alembic — single read path preserves FOUND-10 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03-01 | Tampering | A future autogenerate run silently drops `bags` or `wishlist_entries` because they have no model counterpart yet | mitigate | All five tables HAVE matching `Mapped[...]` models in `app/models/`; `app/models/__init__.py` imports them all; Alembic metadata is complete. CLAUDE.md "ask first" applies for lossy migrations regardless. |
| T-00-03-02 | Denial of Service | Pool exhaustion under load (HTMX home page + AI call in flight; PITFALL SH-2) | mitigate | Explicit pool knobs locked in `app/db.py`: `pool_size=10, max_overflow=5, pool_timeout=5, pool_pre_ping=True, pool_recycle=300`. `pool_timeout=5` is deliberate fail-fast vs. 30s default. |
| T-00-03-03 | Information Disclosure | API keys logged via SQLAlchemy `echo=True` | mitigate | `engine` is created with `echo=False`. Plan 02 default `LOG_LEVEL=INFO` keeps SQLAlchemy bound parameters out of structlog stream. |
| T-00-03-04 | Tampering | Schema drift between models and migration | accept in Phase 0 (smoke gates) | Phase 0 hand-authors the migration; Phase 12 adds a CI check that `alembic check` shows no drift. `tests/test_migrations.py` introspects the live DB so any drift surfaces in `make smoke`. |
| T-00-03-05 | Information Disclosure | `response_json` JSONB column eventually stores AI prose that might include user-supplied coffee names — XSS via re-rendering | accept here; mitigate Phase 7 | Jinja autoescape ON (CLAUDE.md) and HX-6 grep ban on `|safe` (Phase 12) cover the rendering side. Schema-level: no `|safe` ever applied to `response_json` content. |
| T-00-03-06 | Repudiation | Migration history corrupted by future operator hand-edit | mitigate | CLAUDE.md "never edit a committed migration" rule. Phase 0 establishes this as policy. |
| T-00-03-07 | Spoofing | `users.username` lookup is case-sensitive → "Admin" vs "admin" collide differently | mitigate | `username` is `CITEXT` UNIQUE — case-insensitive comparison + uniqueness. FOUND-06 (citext extension) is the load-bearing dependency. |
</threat_model>

<verification>
1. **Static checks (no DB required):**
   - `python -c "from app.models import Base; assert set(Base.metadata.tables.keys()) == {'users','bags','wishlist_entries','ai_recommendations','app_settings'}"` returns exit 0.
   - `python -c "import pathlib; src = pathlib.Path('app/migrations/versions/0001_initial.py').read_text(); assert all(k in src for k in ['CREATE EXTENSION IF NOT EXISTS citext','CREATE EXTENSION IF NOT EXISTS pg_trgm','CREATE EXTENSION IF NOT EXISTS unaccent','tokens_input_search','setup_completed'])"` returns exit 0.

2. **FOUND-10 not regressed:**
   - `pytest tests/test_no_direct_env.py -x` returns exit 0 (proves the new modules didn't introduce a stray `os.environ` read).

3. **Test collection works without a DB:**
   - `pytest tests/test_migrations.py --co -q` lists all eight tests without import errors.

4. **Wave 0 unit tests still green:**
   - `pytest tests/test_logging.py tests/test_env_example.py tests/test_no_direct_env.py -x` returns exit 0.

5. **Live DB integration (gated behind Plan 05's `make smoke`; documented here for completeness):**
   - After `docker compose up -d --build` (Plan 04 + 05) and `alembic upgrade head`:
     - `docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d $POSTGRES_DB -tAc "SELECT count(*) FROM pg_extension WHERE extname IN ('citext','pg_trgm','unaccent')"` returns `3`.
     - `... -tAc "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('users','bags','wishlist_entries','ai_recommendations','app_settings')"` returns `5`.
     - `... -tAc "SELECT count(*) FROM app_settings"` returns `>= 19`.
     - `... -tAc "SELECT value FROM app_settings WHERE key='setup_completed'"` returns `false`.
     - `pytest tests/test_migrations.py -x` returns exit 0 inside the web container.
</verification>

<success_criteria>
- `app/db.py` constructs `engine` with the LOCKED pool knobs (CONTEXT D-10).
- All five model classes exist with SQLAlchemy 2.0 `Mapped[...]` typed columns; `Base.metadata` contains all five tables.
- `ai_recommendations` model carries all 11 cost-observability columns (AI-02, COST-1).
- `bags.coffee_id` is `BigInteger NOT NULL` with NO `ForeignKey` constraint.
- `app/migrations/env.py` imports `app.models` and uses `settings.DATABASE_URL` (no `os.environ`).
- `app/migrations/versions/0001_initial.py` installs three extensions, creates five tables, seeds 19 `app_settings` rows including all the load-bearing keys.
- `tests/test_migrations.py` collects cleanly without DB; runs green when DB is up.
- `tests/test_no_direct_env.py` still passes after this plan lands.
- FOUND-05, FOUND-06, CAT-04, AI-02 traceability comments appear in either the migration file or the test file.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-03-SUMMARY.md` documenting:
- The exact final table list with column counts.
- The exact final `app_settings` seed row count (19) and verbatim key list.
- The `bags.coffee_id` "no FK" decision and Phase 4 follow-up note.
- The Alembic config layout (script_location, env.py shape).
- Note for Plan 04: `entrypoint.sh` runs `alembic upgrade head`; `app/main.py` lifespan opens `engine` and uses it for `/healthz`.
- Note for Plan 04: the `/healthz` route imports `SessionLocal` from `app.db` and runs `SELECT 1` with a 2-second pool_timeout (already configured in `app/db.py`).
- Note for future phases: `app/models/__init__.py` is now the registry — every new model must be re-exported there for Alembic to discover it.
</output>
