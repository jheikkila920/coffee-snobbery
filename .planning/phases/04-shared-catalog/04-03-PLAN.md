---
phase: 04-shared-catalog
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/models/coffee.py
  - app/models/roaster.py
  - app/models/flavor_note.py
  - app/models/equipment.py
  - app/models/recipe.py
  - app/models/bag.py
  - app/models/__init__.py
  - app/migrations/versions/p4_shared_catalog.py
  - tests/phase_04/test_models_catalog.py
  - tests/phase_04/test_migration.py
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-05
  - CAT-06
  - CAT-08
tags:
  - models
  - alembic
  - migration
  - schema

must_haves:
  truths:
    - "Five new SQLAlchemy 2.0 `Mapped[...]` models exist: `Roaster`, `FlavorNote`, `Coffee`, `Equipment`, `Recipe` — all using BigInteger Identity + audit timestamps."
    - "`app/models/bag.py` modified to add `ForeignKey('coffees.id', ondelete='RESTRICT')` on `coffee_id` + new `photo_filename: Mapped[str | None]` column (CAT-08 storage)."
    - "Single Alembic migration `p4_shared_catalog` (down_revision = `p3_api_credentials`) creates the five new tables + the FK constraint + the `photo_filename` column on `bags`, plus the GIN index on `coffees.advertised_flavor_note_ids` (hand-edited; autogenerate misses it)."
    - "CITEXT unique on `roasters.name` and `flavor_notes.name`; CITEXT (not unique) on `coffees.name` (different roasters can share names)."
    - "CHECK constraints land on `coffees.process`, `coffees.roast_level`, `flavor_notes.category`, `equipment.type` — matching the Pydantic regex from plan 04-02 for defense in depth."
    - "`recipes.steps` is JSONB with default `'[]'::jsonb`; `coffees.advertised_flavor_note_ids` is `ARRAY(BigInteger)` with default `'{}'::bigint[]`."
    - "Alembic upgrade + downgrade round-trip succeeds on a fresh DB (test_migration.py)."
    - "`app/models/__init__.py` re-exports all five new models so Alembic autogenerate / future migrations see them (per the existing file-level comment)."
  artifacts:
    - path: "app/models/coffee.py"
      provides: "Coffee model with CITEXT name + ARRAY advertised_flavor_note_ids + process/roast_level CHECK constraints."
      contains: "class Coffee"
    - path: "app/models/roaster.py"
      provides: "Roaster model with CITEXT unique name + Text website."
      contains: "class Roaster"
    - path: "app/models/flavor_note.py"
      provides: "FlavorNote model with CITEXT unique name + category CHECK."
      contains: "class FlavorNote"
    - path: "app/models/equipment.py"
      provides: "Equipment model with type CHECK + usage_count denormalized default 0."
      contains: "class Equipment"
    - path: "app/models/recipe.py"
      provides: "Recipe model with JSONB steps + dose/water/temp/grind columns."
      contains: "class Recipe"
    - path: "app/models/bag.py"
      provides: "Bag model MODIFIED: FK to coffees + photo_filename column."
      contains: "photo_filename"
    - path: "app/migrations/versions/p4_shared_catalog.py"
      provides: "Single alembic migration; revision='p4_shared_catalog'; down_revision='p3_api_credentials'."
      contains: "revision: str = \"p4_shared_catalog\""
  key_links:
    - from: "app/migrations/versions/p4_shared_catalog.py"
      to: "GIN index on coffees.advertised_flavor_note_ids"
      via: "op.execute(\"CREATE INDEX ix_coffees_advertised_flavor_note_ids ON coffees USING GIN (advertised_flavor_note_ids)\")"
      pattern: "USING GIN"
    - from: "app/models/bag.py"
      to: "coffees.id"
      via: "ForeignKey('coffees.id', ondelete='RESTRICT')"
      pattern: "ondelete=.RESTRICT"
    - from: "app/models/__init__.py"
      to: "five new models"
      via: "import + __all__ re-export"
      pattern: "from app.models.coffee import Coffee"
---

<objective>
Ship the five new `Mapped[...]` models + the bag-table modification + the single Alembic migration that lands all of Phase 4's schema in one logical change. Without these, Wave 2 entity routers cannot import their models.

Purpose: CAT-01 (roasters), CAT-02 (flavor_notes), CAT-03 (coffees + array of flavor note IDs), CAT-05 (equipment), CAT-06 (recipes + JSONB steps), and CAT-08 (bag photo_filename column + FK) all share one migration per Phase 0 D-02 "one migration per logical change." The GIN index on `coffees.advertised_flavor_note_ids` is hand-edited into the migration (autogenerate misses `USING GIN` per 04-RESEARCH.md Pitfall 3).

Output:
  - Six model files (5 new + bag.py modified), `app/models/__init__.py` re-exports.
  - One migration file `p4_shared_catalog.py` with both `upgrade()` and `downgrade()`.
  - Two real test modules: `test_models_catalog.py` (model roundtrip + CHECK constraint behavior) and `test_migration.py` (alembic upgrade/downgrade round-trip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-shared-catalog/04-CONTEXT.md
@.planning/phases/04-shared-catalog/04-RESEARCH.md
@.planning/phases/04-shared-catalog/04-PATTERNS.md
@.planning/phases/04-shared-catalog/04-VALIDATION.md
@app/models/user.py
@app/models/api_credential.py
@app/models/bag.py
@app/models/__init__.py
@app/models/base.py
@app/migrations/versions/0001_initial.py
@app/migrations/versions/p3_api_credentials.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the five new Mapped[...] models + modify bag.py + register in __init__.py</name>
  <files>
    app/models/coffee.py,
    app/models/roaster.py,
    app/models/flavor_note.py,
    app/models/equipment.py,
    app/models/recipe.py,
    app/models/bag.py,
    app/models/__init__.py
  </files>
  <read_first>
    app/models/user.py,
    app/models/api_credential.py,
    app/models/bag.py,
    app/models/__init__.py,
    app/models/base.py,
    .planning/phases/04-shared-catalog/04-PATTERNS.md,
    .planning/phases/04-shared-catalog/04-RESEARCH.md
  </read_first>
  <action>
Write the five new model modules + modify bag.py + extend `__init__.py`. Follow 04-PATTERNS.md exactly for each model shape. Each file starts with `from __future__ import annotations`, the standard Phase 0 imports, and ends with no `__all__` (the package `__init__.py` is the registry, per the existing convention).

**A. `app/models/roaster.py`** — class `Roaster(Base)`, `__tablename__ = "roasters"`. Columns:
- `id: Mapped[int]` — `mapped_column(BigInteger, Identity(always=False), primary_key=True)`
- `name: Mapped[str]` — `mapped_column(CITEXT(), nullable=False, unique=True)`
- `location: Mapped[str | None]` — `mapped_column(Text, nullable=True)`
- `website: Mapped[str | None]` — `mapped_column(Text, nullable=True)` (HttpUrl validated at schema layer only)
- `notes: Mapped[str]` — `mapped_column(Text, nullable=False, server_default="")`
- `archived: Mapped[bool]` — `mapped_column(Boolean, nullable=False, server_default=text("false"))`
- `created_at`, `updated_at`: TIMESTAMP(timezone=True) with `server_default=func.now()`

Module docstring: "Shared catalog roaster (CAT-01) — household-shared, no user_id." Imports from `sqlalchemy`, `sqlalchemy.dialects.postgresql.CITEXT`, `sqlalchemy.dialects.postgresql.TIMESTAMP`, and `app.models.base.Base` — match the import shape of `app/models/user.py`.

**B. `app/models/flavor_note.py`** — class `FlavorNote(Base)`, `__tablename__ = "flavor_notes"`. Columns:
- Standard id + audit cols.
- `name: Mapped[str]` — `mapped_column(CITEXT(), nullable=False, unique=True)`
- `category: Mapped[str]` — `mapped_column(Text, nullable=False)`
- `archived: Mapped[bool]` — as roaster.

`__table_args__ = (CheckConstraint("category IN ('fruit','floral','sweet','chocolate','nutty','spice','savory','fermented','other')", name="flavor_notes_category_check"),)`.

Module docstring: "Shared catalog flavor note (CAT-02). 9-value category CHECK matches Pydantic schema regex for defense in depth."

**C. `app/models/coffee.py`** — class `Coffee(Base)`, `__tablename__ = "coffees"`. Columns:
- Standard id + audit cols.
- `name: Mapped[str]` — `mapped_column(CITEXT(), nullable=False)` (NOT unique — different roasters can share names).
- `roaster_id: Mapped[int | None]` — `mapped_column(BigInteger, ForeignKey("roasters.id", ondelete="SET NULL"), nullable=True)` (per 04-PATTERNS.md §coffee — a coffee can survive its roaster being hard-deleted).
- `country: Mapped[str | None]` — `mapped_column(Text, nullable=True)`
- `origin: Mapped[str | None]` — `mapped_column(Text, nullable=True)`
- `process: Mapped[str | None]` — `mapped_column(Text, nullable=True)`
- `roast_level: Mapped[str | None]` — `mapped_column(Text, nullable=True)`
- `varietal: Mapped[str | None]` — `mapped_column(Text, nullable=True)`
- `notes: Mapped[str]` — `mapped_column(Text, nullable=False, server_default="")`
- `advertised_flavor_note_ids: Mapped[list[int]]` — `mapped_column(ARRAY(BigInteger), nullable=False, server_default=text("'{}'::bigint[]"))`
- `archived: Mapped[bool]` — as roaster.

`__table_args__`:
- `CheckConstraint("process IS NULL OR process IN ('washed','natural','honey','anaerobic','experimental','unknown')", name="coffees_process_check")` — note the `IS NULL OR` because the column is nullable, per 04-RESEARCH.md model excerpt.
- `CheckConstraint("roast_level IS NULL OR roast_level IN ('light','medium-light','medium','medium-dark','dark','unknown')", name="coffees_roast_level_check")`
- `Index("ix_coffees_roaster_id", "roaster_id")`
- `Index("ix_coffees_archived", "archived")`

The GIN index on `advertised_flavor_note_ids` is NOT declared here — it goes in the migration via raw SQL because autogenerate cannot emit `USING GIN`.

Imports: `ARRAY` from `sqlalchemy.dialects.postgresql`; `ForeignKey`, `CheckConstraint`, `Index` from `sqlalchemy`.

**D. `app/models/equipment.py`** — class `Equipment(Base)`, `__tablename__ = "equipment"`. Columns:
- Standard id + audit cols.
- `type: Mapped[str]` — `mapped_column(Text, nullable=False)`
- `brand: Mapped[str]` — `mapped_column(Text, nullable=False)`
- `model: Mapped[str]` — `mapped_column(Text, nullable=False)`
- `notes: Mapped[str]` — `mapped_column(Text, nullable=False, server_default="")`
- `usage_count: Mapped[int]` — `mapped_column(Integer, nullable=False, server_default=text("0"))` (denormalized; ships at 0 in Phase 4 per CONTEXT specifics; incremented by Phase 5 service-layer per the open question)
- `archived: Mapped[bool]` — as roaster.

`__table_args__ = (CheckConstraint("type IN ('brewer','grinder','kettle','scale','water_filter','other')", name="equipment_type_check"), Index("ix_equipment_type", "type"))`.

**E. `app/models/recipe.py`** — class `Recipe(Base)`, `__tablename__ = "recipes"`. Columns:
- Standard id + audit cols.
- `name: Mapped[str]` — `mapped_column(Text, nullable=False)`
- `dose_grams: Mapped[int]` — `mapped_column(Integer, nullable=False)`
- `water_grams: Mapped[int]` — `mapped_column(Integer, nullable=False)`
- `water_temp_c: Mapped[int]` — `mapped_column(Integer, nullable=False)`
- `grind_setting: Mapped[str]` — `mapped_column(Text, nullable=False, server_default="")`
- `steps: Mapped[list[dict]]` — `mapped_column(JSONB, nullable=False, server_default=text("'[]'::jsonb"))`
- `archived: Mapped[bool]` — as roaster.

Import `JSONB` from `sqlalchemy.dialects.postgresql`.

**F. `app/models/bag.py` (MODIFY)** — read current state first.

Modifications (preserve every existing column):
1. Change `coffee_id` declaration to include `ForeignKey("coffees.id", ondelete="RESTRICT")` (per 04-PATTERNS.md §`app/models/bag.py` + 04-RESEARCH.md "FK ondelete choice — RESTRICT is the right pick"). Update imports to include `ForeignKey`.
2. Add a new column line after `notes`: `photo_filename: Mapped[str | None] = mapped_column(Text, nullable=True)` (CAT-08 storage; serving route in plan 04-10 reads from this).

Update the file-level docstring's "FK deferred to Phase 4" comment to reflect the now-landed FK ("Phase 4 added the FK constraint per CONTEXT canonical_refs"). Keep the existing `__table_args__` `Index("ix_bags_coffee_id", "coffee_id")` line in place.

**G. `app/models/__init__.py` (MODIFY)** — add five new imports (alphabetical) + extend `__all__`:

Insert (alphabetical) into the existing imports between `Bag` and `Base` etc.:
- `from app.models.coffee import Coffee`
- `from app.models.equipment import Equipment`
- `from app.models.flavor_note import FlavorNote`
- `from app.models.recipe import Recipe`
- `from app.models.roaster import Roaster`

Extend `__all__` to include the new names (alphabetical). Preserve the existing file-level comment (lines 1-11 explain why this re-export matters for Alembic autogenerate).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -c "from app.models import Coffee, Roaster, FlavorNote, Equipment, Recipe, Bag; from app.models.bag import Bag as B; assert hasattr(B, 'photo_filename'); print('ok')"</automated>
  </verify>
  <done>
    All six model files import cleanly; the smoke `python -c` prints `ok`. `grep -c "ForeignKey..coffees.id...ondelete=.RESTRICT" app/models/bag.py` returns 1. `grep -c "ARRAY(BigInteger)" app/models/coffee.py` returns 1. `grep -c "JSONB" app/models/recipe.py` returns 1. `grep -c "CheckConstraint" app/models/coffee.py` returns ≥ 2. `grep -c "Coffee\|Roaster\|FlavorNote\|Equipment\|Recipe" app/models/__init__.py` returns ≥ 10 (5 imports + 5 in __all__).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the alembic migration p4_shared_catalog + alembic round-trip test + model integration tests</name>
  <files>
    app/migrations/versions/p4_shared_catalog.py,
    tests/phase_04/test_models_catalog.py,
    tests/phase_04/test_migration.py
  </files>
  <read_first>
    app/migrations/versions/0001_initial.py,
    app/migrations/versions/p3_api_credentials.py,
    app/models/coffee.py,
    app/models/roaster.py,
    app/models/flavor_note.py,
    app/models/equipment.py,
    app/models/recipe.py,
    app/models/bag.py,
    .planning/phases/04-shared-catalog/04-PATTERNS.md,
    .planning/phases/04-shared-catalog/04-RESEARCH.md
  </read_first>
  <action>
Author the migration + the two model tests.

**A. `app/migrations/versions/p4_shared_catalog.py`** — follow 04-PATTERNS.md §Migration template + `p3_api_credentials.py` for the header shape.

Header:
- `"""Phase 4 shared catalog: roasters + flavor_notes + coffees + equipment + recipes + bag FK/photo."""`
- `from __future__ import annotations`
- `from collections.abc import Sequence`
- `from alembic import op`
- `import sqlalchemy as sa`
- `from sqlalchemy.dialects import postgresql`
- `revision: str = "p4_shared_catalog"`
- `down_revision: str | Sequence[str] | None = "p3_api_credentials"` (verified: that's the current head per `app/migrations/versions/` listing)
- `branch_labels: str | Sequence[str] | None = None`
- `depends_on: str | Sequence[str] | None = None`

Alembic-safe rule (verbatim from `p3_api_credentials.py`): the migration MUST NOT import from `app.models`. Use `sa.Column(...)` + `op.create_table(...)` directly. No model imports.

**`upgrade()` body** (creation order matters because of FKs):

1. `roasters` first (no FK deps): `op.create_table("roasters", ...)` with columns from 04-PATTERNS.md §Migration §roasters. After: `op.create_index("ix_roasters_archived", "roasters", ["archived"])`.

2. `flavor_notes`: `op.create_table("flavor_notes", ...)` with the CheckConstraint inline. No additional index.

3. `coffees` (FK → roasters): `op.create_table("coffees", ...)` with ALL columns. ForeignKey shape: `sa.Column("roaster_id", sa.BigInteger, sa.ForeignKey("roasters.id", ondelete="SET NULL"), nullable=True)`. CheckConstraints for process + roast_level (with `IS NULL OR` form for nullable columns). ARRAY column: `sa.Column("advertised_flavor_note_ids", postgresql.ARRAY(sa.BigInteger), nullable=False, server_default=sa.text("'{}'::bigint[]"))`. Then:
   - `op.create_index("ix_coffees_roaster_id", "coffees", ["roaster_id"])`
   - `op.create_index("ix_coffees_archived", "coffees", ["archived"])`
   - `op.execute("CREATE INDEX ix_coffees_advertised_flavor_note_ids ON coffees USING GIN (advertised_flavor_note_ids)")` — REQUIRED hand-edit per 04-RESEARCH.md Pitfall 3.

4. `equipment`: `op.create_table("equipment", ...)` with CheckConstraint inline. After: `op.create_index("ix_equipment_type", "equipment", ["type"])`.

5. `recipes`: `op.create_table("recipes", ...)` with `postgresql.JSONB` on steps + `server_default=sa.text("'[]'::jsonb")`.

6. Modify `bags`:
   - `op.add_column("bags", sa.Column("photo_filename", sa.Text, nullable=True))`
   - `op.create_foreign_key("fk_bags_coffee_id", "bags", "coffees", ["coffee_id"], ["id"], ondelete="RESTRICT")`

**`downgrade()` body** (reverse order, strictly):

1. `op.drop_constraint("fk_bags_coffee_id", "bags", type_="foreignkey")`
2. `op.drop_column("bags", "photo_filename")`
3. `op.drop_table("recipes")`
4. `op.drop_index("ix_equipment_type", table_name="equipment")` then `op.drop_table("equipment")`
5. `op.execute("DROP INDEX IF EXISTS ix_coffees_advertised_flavor_note_ids")`
6. `op.drop_index("ix_coffees_archived", table_name="coffees")`
7. `op.drop_index("ix_coffees_roaster_id", table_name="coffees")`
8. `op.drop_table("coffees")`
9. `op.drop_table("flavor_notes")`
10. `op.drop_index("ix_roasters_archived", table_name="roasters")`
11. `op.drop_table("roasters")`

Note: Postgres extensions (citext, pg_trgm, unaccent) are already installed in `0001_initial.py:59-61` — do NOT re-create or drop them. Run a quick sanity check on the file: `op.create_table` for the 5 tables, `op.execute` once for the GIN index, `op.add_column` once, `op.create_foreign_key` once.

**B. `tests/phase_04/test_models_catalog.py`** — author real tests (replace Wave-0 stub).

Cases (using `fresh_db` from `tests/conftest.py` and a sync `SessionLocal()` session):

1. `test_roaster_round_trip` — insert a Roaster with `name="Onyx"`, commit; query by id, assert `name == "Onyx"`.
2. `test_roaster_name_unique_citext` — insert two roasters with names `"Onyx"` and `"onyx"`; the second raises IntegrityError (CITEXT case-insensitive unique).
3. `test_flavor_note_category_check` — insert with `category="metallic"`; raises IntegrityError (CheckConstraint).
4. `test_coffee_advertised_flavor_note_ids_round_trip` — insert a Coffee with `advertised_flavor_note_ids=[1, 2, 3]`, commit; query; assert the list survives as a list of ints.
5. `test_coffee_process_check_allows_null` — `process=None` accepted.
6. `test_coffee_process_check_rejects_unknown` — `process="cold_brewed"` raises.
7. `test_equipment_type_check` — `type="grinder_v2"` raises.
8. `test_equipment_usage_count_defaults_zero` — insert without usage_count; query; `usage_count == 0`.
9. `test_recipe_steps_jsonb_round_trip` — insert with `steps=[{"water_grams": 50, "time_seconds": 30, "label": "Bloom"}]`, commit, re-query, assert `steps[0]["water_grams"] == 50` (order preserved).
10. `test_bag_coffee_fk_restrict` — insert a Coffee + a Bag referencing it, commit; attempt to delete the Coffee row; expect IntegrityError ("violates foreign key constraint").
11. `test_bag_photo_filename_optional` — insert a Bag with `photo_filename=None`; commit; query; assert `photo_filename is None`.
12. `test_bag_photo_filename_persists` — insert with `photo_filename="abc.jpg"`; query; assert match.

**C. `tests/phase_04/test_migration.py`** — alembic upgrade/downgrade round-trip:

1. `test_alembic_upgrade_head_idempotent` — run `alembic upgrade head` twice; second call is a no-op (returns 0; head already at p4_shared_catalog).
2. `test_alembic_downgrade_p4_then_upgrade` — `alembic downgrade -1` to revert p4_shared_catalog; assert `coffees` table no longer exists; `alembic upgrade head` re-creates it.
3. `test_alembic_p4_gin_index_present` — after `alembic upgrade head`, query `pg_indexes` for `indexname = 'ix_coffees_advertised_flavor_note_ids'`; assert the result includes the `USING gin` substring (sanity that the hand-edited GIN index landed).

Implementation: use a subprocess call to `alembic` or import `alembic.command` + `alembic.config.Config`. Reference existing test infrastructure in `tests/` to see whether the project has a migration test fixture; if not, write it as a focused integration test that:
- Calls `alembic.command.downgrade(config, "-1")` then asserts the schema diff
- Calls `alembic.command.upgrade(config, "head")` to restore
- Verifies via raw `SELECT FROM information_schema.tables WHERE table_name = 'coffees'`

Use `tmp_path` for a fresh DB if the existing test fixtures don't already isolate. Test runtime budget: <10s combined for the migration suite.

Helpful raw SQL for verification: `SELECT indexdef FROM pg_indexes WHERE indexname = 'ix_coffees_advertised_flavor_note_ids';` returns a row containing `USING gin (advertised_flavor_note_ids)`.

No fenced code blocks beyond the small directive snippets above; the executor reads `0001_initial.py` and `p3_api_credentials.py` for the migration boilerplate.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery alembic upgrade head && docker compose exec coffee-snobbery pytest -q tests/phase_04/test_models_catalog.py tests/phase_04/test_migration.py -x</automated>
  </verify>
  <done>
    `alembic upgrade head` runs cleanly to `p4_shared_catalog`. `tests/phase_04/test_models_catalog.py` exits 0 with ≥ 12 tests covering CITEXT unique, CHECK constraints, ARRAY round-trip, JSONB round-trip, FK RESTRICT, photo_filename optional. `tests/phase_04/test_migration.py` exits 0 with the upgrade/downgrade round-trip + GIN-index presence. `docker compose exec coffee-snobbery psql -U $POSTGRES_USER $POSTGRES_DB -c "\\dt" | grep -E "roasters|flavor_notes|coffees|equipment|recipes"` shows all five tables. `docker compose exec coffee-snobbery psql -c "SELECT indexdef FROM pg_indexes WHERE indexname = 'ix_coffees_advertised_flavor_note_ids'"` shows `USING gin`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → Postgres | One-way schema change; downgrade tested in CI. |
| ORM models → routes | Strong typing via `Mapped[...]`; routes consume via dependency-injected `Session`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (data integrity) | Tampering | `coffees.process`, `coffees.roast_level`, `flavor_notes.category`, `equipment.type` | mitigate | CHECK constraint at DB layer + Pydantic regex at schema layer = defense in depth. A direct SQL INSERT bypassing the schema still fails the CHECK. |
| (cascade destruction) | Data loss | `bags.coffee_id` | mitigate | `ondelete="RESTRICT"` per CONTEXT canonical_refs — hard-delete of a referenced coffee fails loudly with IntegrityError. Combined with archive-only policy, coffees are never hard-deleted in practice. |
</threat_model>

<verification>
- 5 new tables exist with correct columns + constraints + indexes (verified via `psql \d`).
- GIN index on `coffees.advertised_flavor_note_ids` exists with `USING gin` (verified via `pg_indexes`).
- `bags.coffee_id` has FK to `coffees.id` with `RESTRICT` (verified via `\d bags`).
- Alembic round-trip clean (upgrade → downgrade → upgrade returns to head).
- All Phase 4 model files import via `from app.models import ...`.
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion #1 "users can create a roaster, a flavor note, a coffee, an equipment item, and a recipe" is unblocked at the model layer.
- Wave 2 routers (04-04 through 04-08) can import their models without touching the schema.
- The CAT-08 photo_filename column lands so plan 04-09 (bag photo upload) and plan 04-10 (photos router) have a place to read from.
</success_criteria>

<output>
Create `.planning/phases/04-shared-catalog/04-03-SUMMARY.md` summarizing the model shapes, migration footprint, GIN index, FK choice, and any deviations from 04-PATTERNS.md.
</output>
