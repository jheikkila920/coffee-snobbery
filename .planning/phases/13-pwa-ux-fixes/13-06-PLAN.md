---
phase: 13-pwa-ux-fixes
plan: 06
type: execute
wave: 2
depends_on: [05]
files_modified:
  - app/templates/pages/home.html
  - app/templates/pages/sessions.html
  - app/templates/fragments/session_list.html
  - app/templates/pages/data_tools.html
  - app/templates/pages/config_hub.html
  - app/routers/brew.py
  - tests/templates/test_recipe_row.py
autonomous: false
requirements: [C5, C8, D-05, D-06]
must_haves:
  truths:
    - "A 'Guided Brew' action is reachable from the Home page and the Log/sessions page; both route to /recipes (C5, D-05)"
    - "A regression test covers recipe_row.html: with steps renders a /brew/guided link, without steps renders a /recipes/{id}/edit link (C5, D-05 mandated test)"
    - "CSV Export + Import live on a dedicated page linked from the config hub; the log/sessions view no longer carries the inline export/import controls (C8, D-06)"
    - "The routes /brew/export and /brew/import are UNCHANGED — only the entry point moved (C8, D-06)"
  artifacts:
    - path: "app/templates/pages/data_tools.html"
      provides: "Dedicated Export/Import page (D-06)"
      contains: "/brew/import"
    - path: "tests/templates/test_recipe_row.py"
      provides: "C5 mandated enabled-vs-no-steps regression test"
      exports: ["test functions"]
    - path: "app/templates/pages/home.html"
      provides: "Guided Brew action -> /recipes (C5)"
      contains: "/recipes"
    - path: "app/templates/pages/config_hub.html"
      provides: "Export/Import link in account section (D-06)"
  key_links:
    - from: "home.html + sessions.html Guided Brew action"
      to: "/recipes"
      via: "<a href=/recipes>"
      pattern: "/recipes"
    - from: "config_hub.html account section"
      to: "/data-tools"
      via: "<a href=/data-tools>"
      pattern: "data-tools"
    - from: "data_tools.html import form"
      to: "/brew/import"
      via: "hx-post=/brew/import with CSRF token (unchanged route)"
      pattern: "/brew/import"
---

<objective>
Fix C5 (Guided Brew reachability + mandated regression test) and C8 (relocate Export/Import).

C5 (D-05): Guided Brew Mode requires a recipe-WITH-steps, and neither Home nor Log has a recipe pre-selected, so add a "Guided Brew" action on Home and the Log/sessions page that routes to /recipes — the recipe list, where each steps-bearing recipe already carries the (eafc6e3-fixed) "Start guided brew" control and no-steps recipes link to the editor. PLUS the criterion-mandated regression test: cover recipe_row.html enabled-vs-no-steps rendering (the coverage gap that let the dead-span bug ship).

C8 (D-06): Move CSV Export + Import to a dedicated page (/data-tools) linked from the config-hub account section. The log/sessions view (GET /brew) loses its inline export/import controls. The routes /brew/export and /brew/import are UNCHANGED — only the entry point moves.

Wave 2 / depends_on 05: this plan edits config_hub.html (the data-tools link) and Plan 05 also edits config_hub.html (the dark toggle), both in the same md:hidden account section. Running after Plan 05 avoids the file conflict.

Purpose: Make Guided Brew discoverable, close the recipe_row coverage gap, and declutter the log view.
Output: Guided Brew links on Home + Log, a new /data-tools page + route + config-hub link, removed inline export/import controls, and the mandated recipe_row regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pwa-ux-fixes/13-RESEARCH.md
@.planning/phases/13-pwa-ux-fixes/13-PATTERNS.md
@.planning/debug/resolved/guided-brew-does-nothing.md

<interfaces>
<!-- Verified from source -->
app/templates/fragments/recipe_row.html (read-only — already eafc6e3-fixed; the C5 test target):
  - mode="card" branch (lines 20-60): {% if recipe.steps %} <a href="/brew/guided?recipe_id={{ recipe.id }}"> ... {% else %} <a href="/recipes/{{ recipe.id }}/edit" ...> Start guided brew <span>(add steps)</span> {% endif %}
  - mode="row" branch (lines ~85-115): same {% if recipe.steps %} ... {% else %} ... {% endif %} pattern
  - selected by a `mode` context var; uses recipe.steps (truthy/falsy) and recipe.id

app/templates/pages/home.html:
  - header actions div (lines 11-23): {% if ...is_admin %}<a href="/admin">Admin</a>{% endif %} <a href="/brew/new">Log session</a>. Add a "Guided Brew" -> /recipes <a> here.

app/templates/pages/sessions.html:
  - header (lines 17-33): <a id="export-csv-link" href="/brew/export...">Export CSV</a>, <a href="/brew/import">Import sessions</a>, <a href="/brew/new">Log session</a>.
    C5: add a "Guided Brew" -> /recipes link. C8: REMOVE the Export CSV + Import sessions links; keep Log session.

app/templates/fragments/session_list.html:
  - lines ~20-24: {% if is_fragment %}<a id="export-csv-link" hx-swap-oob="true" href="/brew/export...">Export CSV</a>{% endif %} — the OOB export-link sync. C8: REMOVE (no longer needed once export moves off this page).

app/routers/brew.py:
  - existing GET /brew/export (filtered CSV download) and POST /brew/import (multipart, CSRF-enforced) — UNCHANGED.
  - the GET /brew (sessions list) handler builds export_query / is_fragment for the OOB sync — after C8, the export_query/OOB plumbing is dead for the list page; the executor may leave the export_query context var (harmless) but must ensure the list still renders without the removed OOB anchor.
  - ADD a GET /data-tools route returning pages/data_tools.html (require_user; same auth posture as /brew). Place near the brew page routes.

app/templates/pages/brew_import.html: the role-match analog for data_tools.html (extends base.html, max-w-2xl px-6 py-12, CSRF token on the import form, hx-post="/brew/import" hx-target="#import-results").

app/templates/pages/config_hub.html: md:hidden account section lines 54-66. Plan 05 adds the dark toggle here; this plan adds the "Export / Import sessions" -> /data-tools link in the same section.

Test analog: tests/templates/test_autoescape.py — renders via Jinja Environment (app/templates dir, autoescape ON). Use env.get_template("fragments/recipe_row.html").render(mode=..., recipe=SimpleNamespace(id=1, steps=[...]|[]), ...). Provide any other context vars the fragment references with safe defaults.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: C5 mandated regression test — recipe_row enabled-vs-no-steps</name>
  <files>tests/templates/test_recipe_row.py</files>
  <read_first>
    - app/templates/fragments/recipe_row.html (full — both mode="card" and mode="row" branches; note ALL context vars it references so the test supplies them)
    - tests/templates/test_autoescape.py (the Jinja Environment render pattern — how to obtain the env from app.templates_setup or Jinja2Templates(directory="app/templates"))
    - .planning/debug/resolved/guided-brew-does-nothing.md (the dead-span bug this test guards against)
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "recipe_row.html — C5" + "Tests (NEW files)" test_recipe_row.py excerpt
    - C:\Users\John\.claude\projects\C--Claude-Coffee-Snobbery\memory\tests-pass-by-skip-mask-green.md (do NOT pytest.skip silently if the env is available; assert real branches)
  </read_first>
  <behavior>
    - test_recipe_with_steps_renders_guided_brew_link (mode="card"): render recipe_row.html with a recipe having truthy steps; output contains `href="/brew/guided?recipe_id=<id>"` and does NOT contain `/recipes/<id>/edit` as the guided-brew control target.
    - test_recipe_no_steps_renders_edit_link (mode="card"): render with empty/falsy steps; output contains `href="/recipes/<id>/edit"` for the guided-brew control AND the "(add steps)" hint; does NOT contain `/brew/guided`.
    - Repeat both assertions for mode="row" (two more tests, or parametrize over mode) so BOTH shapes are covered (the bug shipped in a shape the original tests missed).
    - The guided-brew control text "Start guided brew" appears in both branches (it was the dead <span> before eafc6e3).
  </behavior>
  <action>Create tests/templates/test_recipe_row.py. Obtain the Jinja Environment the same way test_autoescape.py does (try app.templates_setup.templates, fall back to fastapi Jinja2Templates(directory="app/templates").env). Use SimpleNamespace (or a small fake) for the recipe with id and steps; for the with-steps case give steps a non-empty list, for the no-steps case an empty list. Render env.get_template("fragments/recipe_row.html") with mode + recipe (+ any other vars the fragment needs, with minimal safe defaults). Assert the href branches per the behavior block. Parametrize over mode in ("card","row"). These tests are the C5-mandated regression coverage — they must assert real output, not skip when the env is present.</action>
  <verify>
    <automated>docker compose run --rm coffee-snobbery-test tests/templates/test_recipe_row.py -rs --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - tests/templates/test_recipe_row.py exists with steps-present and no-steps tests covering BOTH mode="card" and mode="row".
    - With steps -> asserts /brew/guided?recipe_id link; without steps -> asserts /recipes/{id}/edit link + "(add steps)".
    - Tests assert real rendered output (no silent skip when the Jinja env is available).
    - `docker compose run --rm coffee-snobbery-test tests/templates/test_recipe_row.py -rs` exits 0.
  </acceptance_criteria>
  <done>The mandated recipe_row enabled-vs-no-steps regression test exists and is green for both fragment shapes.</done>
</task>

<task type="auto">
  <name>Task 2: C5 — Guided Brew action on Home + Log (-> /recipes)</name>
  <files>app/templates/pages/home.html, app/templates/pages/sessions.html</files>
  <read_first>
    - app/templates/pages/home.html (header actions div lines 11-23)
    - app/templates/pages/sessions.html (header lines 17-33)
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "home.html + session_list.html — C5" (the Guided Brew <a> insertion excerpts)
    - .planning/phases/13-pwa-ux-fixes/13-CONTEXT.md D-05 (route to /recipes; reuse existing screens)
  </read_first>
  <action>home.html: in the header actions div (lines 11-23), add a "Guided Brew" link `<a href="/recipes" class="rounded border border-espresso-300 dark:border-espresso-600 px-3 py-1 text-sm font-semibold hover:bg-espresso-100 dark:hover:bg-espresso-800">Guided Brew</a>` placed before the existing "Log session" button. sessions.html: add the same "Guided Brew" -> /recipes link in the header (lines 17-33), using the secondary-button styling consistent with that header. Keep tap targets comfortable; do not introduce a new picker UI (D-05 routes to the existing recipe list).</action>
  <verify>
    <automated>python -c "h=open('app/templates/pages/home.html',encoding='utf-8').read(); s=open('app/templates/pages/sessions.html',encoding='utf-8').read(); assert 'Guided Brew' in h and 'href=\"/recipes\"' in h, 'home guided-brew link missing'; assert 'Guided Brew' in s and 'href=\"/recipes\"' in s, 'sessions guided-brew link missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - home.html contains a "Guided Brew" link with href="/recipes".
    - sessions.html contains a "Guided Brew" link with href="/recipes".
    - No new picker/component introduced.
  </acceptance_criteria>
  <done>Guided Brew is reachable from Home and Log, both routing to /recipes.</done>
</task>

<task type="auto">
  <name>Task 3: C8 — /data-tools page + route + config-hub link; remove inline export/import</name>
  <files>app/templates/pages/data_tools.html, app/routers/brew.py, app/templates/pages/config_hub.html, app/templates/pages/sessions.html, app/templates/fragments/session_list.html</files>
  <read_first>
    - app/templates/pages/brew_import.html (the role-match page analog — extends base.html, max-w-2xl, CSRF token, hx-post="/brew/import" hx-target="#import-results")
    - app/templates/pages/sessions.html (export/import links lines 17-33 to remove; keep Log session + the new Guided Brew link)
    - app/templates/fragments/session_list.html (the is_fragment OOB export-link block lines ~20-24 to remove)
    - app/routers/brew.py (the existing GET /brew/export + POST /brew/import handlers — DO NOT change them; the GET /brew list handler's export_query/is_fragment plumbing; where page routes are registered — add GET /data-tools there with require_user)
    - app/templates/pages/config_hub.html (account section lines 54-66 — add the /data-tools link AFTER Plan 05's dark toggle; depends_on 05)
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "data_tools.html (NEW) — C8", "config_hub.html — C4, C8", "sessions.html + session_list.html — C8"
    - .planning/phases/13-pwa-ux-fixes/13-CONTEXT.md D-06 (routes unchanged; only entry point moves)
  </read_first>
  <action>
data_tools.html (NEW): create a page extending base.html (max-w-2xl px-6 py-12) titled "Export / Import sessions" that combines: (a) an Export section with `<a href="/brew/export" class="...">Export CSV</a>` (full export — the dedicated page is not filtered by the live list filters, per D-06 the routes are unchanged but the entry point is generic; if a filtered export is desired the user still has the unchanged /brew/export route), and (b) an Import section that reuses the brew_import.html form unchanged — hx-post="/brew/import" hx-encoding="multipart/form-data" hx-target="#import-results", with the CSRF token hidden input `<input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken','') }}">`. Include the #import-results container.

brew.py: ADD a GET /data-tools route (require_user, same auth posture as the brew pages) returning pages/data_tools.html. Do NOT modify /brew/export or /brew/import. The GET /brew list handler: ensure it still renders after the OOB export anchor is removed (the export_query context var may remain unused/harmless).

sessions.html: REMOVE the "Export CSV" (id="export-csv-link") and "Import sessions" links from the header (lines 17-33); keep "Log session" and the new "Guided Brew" link from Task 2.

session_list.html: REMOVE the {% if is_fragment %}...export-csv-link hx-swap-oob...{% endif %} block (lines ~20-24) — the OOB sync is dead once export leaves this page.

config_hub.html: in the md:hidden account section (after Plan 05's dark toggle, before the section's closing </div>), add `<a href="/data-tools" class="mt-4 inline-flex items-center rounded border border-espresso-300 px-4 py-2 text-base text-espresso-800 hover:bg-espresso-50 dark:border-espresso-700 dark:text-cream-200 dark:hover:bg-espresso-900 min-h-[44px]">Export / Import sessions</a>`.
  </action>
  <verify>
    <automated>docker compose run --rm coffee-snobbery-test tests/routers/test_brew_list_csv.py tests/routers/test_brew_router.py -rs --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - app/templates/pages/data_tools.html exists, extends base.html, contains hx-post="/brew/import" + the CSRF token input + an Export CSV link to /brew/export.
    - app/routers/brew.py has a GET /data-tools route (require_user) returning pages/data_tools.html; /brew/export and /brew/import handlers are unchanged (git diff shows no logic change to those handlers).
    - sessions.html no longer contains "Export CSV" / "Import sessions" header links (id="export-csv-link" gone); it still has Log session + Guided Brew.
    - session_list.html no longer contains the is_fragment OOB export-csv-link block.
    - config_hub.html contains an `<a href="/data-tools">Export / Import sessions</a>` in the account section.
    - The CSV export/import router tests still pass (routes unchanged).
  </acceptance_criteria>
  <done>Dedicated /data-tools page + route created and linked from the config hub; inline export/import removed from the log view; export/import routes unchanged; CSV tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual — Guided Brew reach + Export/Import relocation</name>
  <files>(verification only — no files modified)</files>
  <action>Manual verification checkpoint — perform the steps in <how-to-verify>. No source files are modified by this task.</action>
  <verify>Manual: follow <how-to-verify>; the gate is met when <acceptance_criteria> hold.</verify>
  <done>See <acceptance_criteria> / <resume-signal>.</done>
  <what-built>Guided Brew links on Home/Log -> /recipes (C5); dedicated /data-tools page linked from config hub, inline export/import removed from the log view (C8).</what-built>
  <how-to-verify>
    Navigation/UI placement is a manual check. Rebuild first: `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`. "Clear site data" before checking (SW caching).
    1. C5: On the Home page and the Log/sessions page, a "Guided Brew" action is visible; tapping it lands on /recipes. From a steps-bearing recipe there, "Start guided brew" launches GBM; a no-steps recipe links to its editor (eafc6e3 behavior).
    2. C8: The Log/sessions view (/brew) NO LONGER shows inline Export CSV / Import controls. The config hub shows an "Export / Import sessions" link; tapping it opens /data-tools. On /data-tools: "Export CSV" downloads a CSV (route /brew/export unchanged) and the Import form uploads a CSV (route /brew/import unchanged, CSRF still enforced — an import without a valid token is rejected).
    Expected: Guided Brew reachable from both surfaces; export/import only on /data-tools (linked from config hub), routes still work with CSRF.
  </how-to-verify>
  <acceptance_criteria>
    - Guided Brew action present on Home + Log; routes to /recipes.
    - Log view has no inline export/import; /data-tools (via config hub) handles both with CSRF intact.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what's missing/broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /data-tools | Authenticated page route (require_user) — moved entry point, not a new unprotected route |
| browser → POST /brew/import (via /data-tools form) | Authenticated, CSRF-protected multipart upload (unchanged route) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Spoofing (CSRF) | /data-tools import form | mitigate | The import form reuses the unchanged POST /brew/import route and carries the double-submit CSRF token input; CSRF enforcement is unchanged (RESEARCH Security Domain). |
| T-13-15 | Elevation / Access Control | new GET /data-tools route | mitigate | Route is gated by require_user (same posture as /brew); it is a moved entry point, not a new public route. Task 3 asserts require_user. |
| T-13-16 | Tampering | CSV import pipeline | accept | Unchanged — the existing header-driven importer + Content-Length pre-check + single-transaction guards (Phase 5) are reused as-is; this plan only moves the entry point. |

No HIGH-severity threats. Routes + CSRF + auth posture preserved; only the entry point relocated.
</threat_model>

<verification>
- tests/templates/test_recipe_row.py green (C5 mandated test, both shapes).
- CSV router tests (test_brew_list_csv.py, test_brew_router.py) green — export/import routes unchanged.
- Per-wave full suite shows no new failures.
- Manual (Task 4): Guided Brew reach + export/import relocation with CSRF intact.
</verification>

<success_criteria>
- C5 satisfied: Guided Brew reachable from Home + Log (-> /recipes); recipe_row enabled-vs-no-steps regression test green for both fragment shapes (D-05).
- C8 satisfied: Export/Import moved to /data-tools, linked from config hub, removed from the log view; /brew/export + /brew/import unchanged (D-06).
- Auth + CSRF posture preserved on the moved entry point.
</success_criteria>

<output>
After completion, create `.planning/phases/13-pwa-ux-fixes/13-06-SUMMARY.md`
</output>
