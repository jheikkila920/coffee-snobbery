---
phase: 13-pwa-ux-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.js
  - app/static/css/tailwind.src.css
  - app/templates/base.html
  - app/static/js/alpine-components/dark-toggle.js
  - app/templates/pages/config_hub.html
autonomous: false
requirements: [C1, C4, D-01, D-02]
must_haves:
  truths:
    - "A 3-state Auto/Light/Dark toggle exists on the config hub near the Signed-in-as / Sign-out block; Auto is the default and preserves today's system-preference behavior (C4, D-01)"
    - "The choice persists in localStorage snobbery:theme and applies via a .dark class on <html>; Tailwind dark: utilities key off .dark (C4)"
    - "No flash-of-wrong-theme on load — a nonce'd, eval-free, synchronous inline <head> script sets the .dark class before first paint (C4, D-01)"
    - "An explicit Light choice on a dark-system device wins (the two hand-written @media prefers-color-scheme:dark blocks are now .dark-class-scoped) (C4, D-01)"
    - "Login + setup remain always-dark regardless of the toggle (D-02)"
    - "The iOS standalone mobile top strip carries env(safe-area-inset-top) padding so the header is not obscured by the status bar (C1)"
  artifacts:
    - path: "tailwind.config.js"
      provides: "darkMode: 'selector' (v3, NOT @custom-variant) (C4)"
      contains: "selector"
    - path: "app/static/css/tailwind.src.css"
      provides: ".dark-class-scoped form/anchor rules (was @media) + .top-safe-area rule (C4, C1)"
      contains: ".dark"
    - path: "app/static/js/alpine-components/dark-toggle.js"
      provides: "darkToggle Alpine CSP component, snobbery:theme localStorage (C4)"
      contains: "snobbery:theme"
    - path: "app/templates/base.html"
      provides: "No-FOUC nonce'd inline head script + dark-toggle script tag + top safe-area on mobile strip (C4, C1)"
      contains: "snobbery:theme"
  key_links:
    - from: "base.html <head> no-FOUC script"
      to: "<html>.classList .dark"
      via: "localStorage snobbery:theme or matchMedia, synchronous, nonce'd"
      pattern: "snobbery:theme"
    - from: "config_hub.html darkToggle buttons"
      to: "dark-toggle.js setTheme"
      via: "x-data=darkToggle + x-on:click=setTheme(...)"
      pattern: "setTheme"
    - from: "tailwind.config.js darkMode selector"
      to: ".dark on <html>"
      via: "Tailwind v3 selector strategy"
      pattern: "darkMode"
    - from: "base.html mobile top strip"
      to: "env(safe-area-inset-top)"
      via: "pt-[env(safe-area-inset-top)] / .top-safe-area"
      pattern: "safe-area-inset-top"
---

<objective>
Fix C4 (manual dark-mode toggle) and C1 (iOS status-bar safe-area-top) — grouped because both touch base.html + tailwind.src.css.

C4 (D-01/D-02): Add a 3-state Auto/Light/Dark toggle on the config hub. Auto (default) follows system preference — today's exact behavior, so existing users see no change. Mechanism (RESEARCH-VERIFIED, SUPERSEDES the v4 guidance in CONTEXT.md): this project is Tailwind v3.4.17 (standalone CLI, JS config), NOT v4. Change tailwind.config.js darkMode 'media' to 'selector'. DO NOT use @custom-variant dark — that is v4 syntax and will silently no-op / break the v3 build (RESEARCH Pitfall 1). Rewrite the two hand-written @media (prefers-color-scheme: dark) blocks in tailwind.src.css to .dark-class-scoped selectors so an explicit Light override wins on a dark-system device. Persist the choice in localStorage snobbery:theme. Prevent FOUC with a synchronous, nonce'd, eval-free inline head script that sets .dark on html before first paint. Login + setup stay always-dark (D-02) — already satisfied because they hardcode bg-espresso-950, not the dark: variant.

C1: Add env(safe-area-inset-top) padding to the mobile top strip (and the full-screen mobile search sheet) so the iOS standalone header isn't hidden under the status bar, mirroring the bottom-nav technique from commit 982c0e6. NOTE: that technique is UNVERIFIED on-device — the on-device check is a MANUAL verification, not an automated pass.

Purpose: User-controllable theme without FOUC; unobscured iOS standalone header.
Output: One-line config change, class-scoped CSS, the no-FOUC script + toggle component + toggle UI, and the safe-area-top padding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pwa-ux-fixes/13-RESEARCH.md
@.planning/phases/13-pwa-ux-fixes/13-PATTERNS.md
@docs/decisions/0001-csp-strict-no-unsafe-eval.md

<interfaces>
<!-- Verified from source. RESEARCH.md supersedes CONTEXT.md on the Tailwind version. -->
tailwind.config.js: module.exports = { darkMode: 'media', ... } (line ~27) — v3 JS config. Change 'media' -> 'selector'.

app/static/css/tailwind.src.css:
  - lines 19-49: existing OUTSIDE-@layer safe-area rules (.nav-safe-area, .nav-safe-area-extend) — the proven bottom pattern to mirror for C1
  - line 62: @layer base {
  - lines 88-97 (inside @layer base): @media (prefers-color-scheme: dark) { input,select,textarea {...}; ::placeholder {...} } -> rewrite to .dark input,...
  - lines 111-116 (inside @layer base): @media (prefers-color-scheme: dark) { a {...}; a:hover {...} } -> rewrite to .dark a {...}

app/templates/base.html:
  - line 3: <html lang="en">  (the .dark class target)
  - line 21: <link rel="stylesheet" href="{{ tailwind_css_path }}">  — the no-FOUC script must go BEFORE this
  - lines 26-42: Alpine.data component <script defer ... nonce> registrations (load BEFORE @alpinejs/csp core line 51); add dark-toggle.js here
  - line 59: existing nonce'd inline SW-registration script — the EXACT pattern the no-FOUC script copies (synchronous, IIFE, nonce, no defer)
  - line 70: desktop top header (hidden md:flex h-14) — normal flow, NOT fixed
  - line 164: mobile top strip (md:hidden flex h-14 ...) — normal flow, NOT fixed; C1 target
  - line 187: full-screen mobile search sheet (fixed inset-0 z-50) — covers status-bar area; also needs top safe-area padding
  - <body> line 63: bg-cream-50 ... dark:bg-espresso-950 ... (dark: utilities already present)

app/templates/pages/config_hub.html:
  - lines 54-66: the md:hidden account section ("Signed in as" + username + Sign out form). The dark toggle attaches AFTER the Sign out form, before the closing </div>.

app/templates/pages/login.html: extends base.html; hardcodes bg-espresso-950 (line 7) — NOT dark: variant. So the .dark toggle is visually a no-op on login (D-02 satisfied). setup.html likewise extends base.html.

app/static/js/alpine-components/ios-banner.js: the localStorage + Alpine.data CSP registration analog for dark-toggle.js.
docs/decisions/0001: strict CSP — script-src 'self' 'nonce-...'; no unsafe-inline/eval; no x-model; no inline hx-on:.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: C4 — darkMode selector + class-scoped CSS + dark-toggle component</name>
  <files>tailwind.config.js, app/static/css/tailwind.src.css, app/static/js/alpine-components/dark-toggle.js</files>
  <read_first>
    - tailwind.config.js (full — confirm v3 JS-config shape, darkMode line ~27)
    - app/static/css/tailwind.src.css (lines 19-49 safe-area block, line 62 @layer base, lines 88-97 + 111-116 the two @media dark blocks)
    - app/static/js/alpine-components/ios-banner.js (the alpine:init + Alpine.data + localStorage try/catch pattern to copy)
    - .planning/phases/13-pwa-ux-fixes/13-RESEARCH.md Patterns 1, 2, 4 + Pitfall 1 (NO @custom-variant) + the dark-toggle.js Code Example
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "tailwind.config.js — C4", "tailwind.src.css — C1, C4", "dark-toggle.js (NEW component) — C4"
    - planning_context critical_conflict_resolution (RESEARCH supersedes CONTEXT on Tailwind version)
  </read_first>
  <action>
tailwind.config.js: change darkMode 'media' to 'selector' (v3.4.1+ canonical; if the standalone binary misbehaves with 'selector', fall back to 'class' per RESEARCH Open Question 3 — note which was used). Do NOT add any @custom-variant directive anywhere (v4-only, Pitfall 1).

tailwind.src.css: rewrite the @media (prefers-color-scheme: dark) block at lines 88-97 to .dark-scoped rules — .dark input, .dark select, .dark textarea with color #F4EFE6 / background-color #21150C / border-color #3D2817, and .dark input::placeholder, .dark textarea::placeholder with color #DACBAE. Rewrite the block at lines 111-116 to .dark a { color:#E3D5C9 } and .dark a:hover { color:#F4EFE6 }. Keep them inside @layer base (RESEARCH A1 — works in v3; if the compiled CSS drops them, move outside @layer and note it). All CSS stays in tailwind.src.css (MX-1 — NO custom.css). No @media (prefers-color-scheme: dark) blocks remain after this change.

dark-toggle.js (NEW): create app/static/js/alpine-components/dark-toggle.js following the ios-banner.js pattern exactly — an alpine:init listener registering Alpine.data('darkToggle', function(){ return { theme, setTheme(val), isActive(val) } }). theme initializes from localStorage.getItem('snobbery:theme') || 'auto'. setTheme: 'auto' -> removeItem('snobbery:theme') then add/remove .dark on document.documentElement per window.matchMedia('(prefers-color-scheme: dark)'); 'dark' -> setItem 'dark' + classList.add('dark'); 'light' -> setItem 'light' + classList.remove('dark'). isActive(val) returns this.theme === val (for :class active styling). Wrap localStorage writes in try/catch (private mode/quota). No x-model, no eval (CSP/ADR 0001).
  </action>
  <verify>
    <automated>python scripts/check_c4_dark.py</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js darkMode is 'selector' (or 'class' fallback, noted); NO @custom-variant anywhere.
    - tailwind.src.css contains `.dark input` and `.dark a` rules; contains NO `prefers-color-scheme: dark` (both @media blocks rewritten).
    - dark-toggle.js registers Alpine.data('darkToggle'), references snobbery:theme, contains no eval and no x-model.
    - All CSS still lives in tailwind.src.css; no custom.css created.
  </acceptance_criteria>
  <done>v3 selector strategy set; @media dark blocks now .dark-scoped; darkToggle component created; no v4 syntax, no eval.</done>
</task>

<task type="auto">
  <name>Task 2: C4 — no-FOUC head script + toggle UI; C1 — top safe-area padding</name>
  <files>app/templates/base.html, app/templates/pages/config_hub.html</files>
  <read_first>
    - app/templates/base.html (line 21 stylesheet link, lines 26-42 Alpine component script tags, line 59 nonce'd SW-reg inline script PATTERN, line 164 mobile top strip, line 187 mobile search sheet)
    - app/templates/pages/config_hub.html (lines 54-66 account section attach point)
    - app/static/js/alpine-components/dark-toggle.js (the component from Task 1 — confirm setTheme/isActive API)
    - .planning/phases/13-pwa-ux-fixes/13-RESEARCH.md Pattern 3 (no-FOUC inline script), Pattern 6 + Pitfall 6 (safe-area-top, h-14 too short), Pitfall 3/4 (nonce required, no defer/async on no-FOUC)
    - .planning/phases/13-pwa-ux-fixes/13-PATTERNS.md "base.html — C1, C4, C10" (no-FOUC excerpt + script-tag insertion + mobile-strip safe-area) and "config_hub.html — C4, C8" (toggle insertion excerpt)
    - app/templates/pages/login.html (confirm hardcoded bg-espresso-950 so D-02 needs no extra handling)
  </read_first>
  <action>
base.html C4: (a) INSERT a synchronous nonce'd inline IIFE script in <head> BEFORE the stylesheet link (line 21) that reads localStorage 'snobbery:theme' and adds .dark to document.documentElement when value is 'dark' OR (value is null AND matchMedia prefers-color-scheme:dark) — copy the exact nonce + IIFE shape from the SW-registration script at line 59. NO defer, NO async (Pitfall 4). (b) ADD a `<script defer src="/static/js/alpine-components/dark-toggle.js" nonce="{{ csp_nonce(request) }}"></script>` in the Alpine component block (between line 42 ios-banner.js and the @alpinejs/csp core at line 51) so the registration exists before Alpine boots.

base.html C1: change the mobile top strip (line 164) from `h-14` to `min-h-14` and add `pt-[env(safe-area-inset-top)]` (or the .top-safe-area utility from Task 3) so the strip's content clears the status bar; bump any matching content top-spacer if present (Pitfall 6). Also add top safe-area padding to the full-screen mobile search sheet (line 187, fixed inset-0) so its header isn't under the status bar in standalone. Desktop header (line 70) is normal-flow and not the iOS standalone obstruction target — leave it unless the executor confirms it's the obscured element.

config_hub.html C4: in the md:hidden account section (after the Sign out form, before line 66 closing </div>), add an `x-data="darkToggle"` block titled "Display" with three buttons — Auto / Light / Dark — each `x-on:click="setTheme('auto'|'light'|'dark')"` and `:class` driven by `isActive('...')` for an active highlight, each `min-h-[44px]`. String x-data reference only (CSP). No x-model, no inline hx-on:.
  </action>
  <verify>
    <automated>python scripts/check_c4_dark.py --templates</automated>
  </verify>
  <acceptance_criteria>
    - base.html <head> contains a nonce'd inline script (no defer/async) referencing localStorage 'snobbery:theme' and document.documentElement.classList, placed BEFORE the tailwind stylesheet link.
    - base.html includes a defer+nonce script tag for /static/js/alpine-components/dark-toggle.js loaded before the @alpinejs/csp core.
    - base.html mobile top strip carries env(safe-area-inset-top) padding and min-h (not fixed h-14); the mobile search sheet also carries top safe-area padding.
    - config_hub.html account section has an x-data="darkToggle" block with Auto/Light/Dark buttons calling setTheme(...), each >= 44px, no x-model / no inline hx-on:.
    - No |safe filter, no inline hx-on:, no eval introduced (CSP grep clean).
  </acceptance_criteria>
  <done>No-FOUC nonce'd script in head; dark-toggle script registered; toggle UI on config hub; mobile top strip + search sheet carry top safe-area padding.</done>
</task>

<task type="auto">
  <name>Task 3: C1 — .top-safe-area CSS utility (if used) + CSP grep guard</name>
  <files>app/static/css/tailwind.src.css</files>
  <read_first>
    - app/static/css/tailwind.src.css (lines 19-49 — the existing .nav-safe-area / .nav-safe-area-extend OUTSIDE-@layer rules to mirror)
    - .planning/phases/13-pwa-ux-fixes/13-RESEARCH.md Pattern 6 (the .top-safe-area rule example, placed outside @layer to prevent purge)
    - tests/ci/test_csp_nonce.py + tests/ci/test_no_unsafe_jinja.py (the existing CSP/|safe grep guards that must still pass after the base.html edits)
  </read_first>
  <action>If Task 2 used the `.top-safe-area` utility class (rather than the arbitrary `pt-[env(safe-area-inset-top)]` value), add a `.top-safe-area { padding-top: env(safe-area-inset-top); }` rule to tailwind.src.css OUTSIDE any @layer block (mirroring the existing .nav-safe-area rules at lines 19-49) so it is never purged. If Task 2 used the arbitrary-value utility `pt-[env(safe-area-inset-top)]` instead (which Tailwind generates directly), this CSS rule is unnecessary — note that and skip the addition. Either way, run the existing CSP and |safe grep guards to confirm the new no-FOUC inline script and dark-toggle additions did not introduce a CSP violation.</action>
  <verify>
    <automated>docker compose run --rm coffee-snobbery-test tests/ci/ -rs --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - If a utility class was used: tailwind.src.css contains `.top-safe-area { padding-top: env(safe-area-inset-top); }` outside @layer; if the arbitrary value was used instead, no rule needed and the omission is documented in the SUMMARY.
    - tests/ci/ (CSP nonce + no-unsafe-jinja grep guards) pass — the no-FOUC inline script is nonce'd and there is no new |safe / inline hx-on: / unsafe-eval.
  </acceptance_criteria>
  <done>Top safe-area CSS resolved (utility rule or arbitrary value); CSP/|safe guards green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual — dark toggle (no FOUC), login stays dark, iOS safe-area-top</name>
  <files>(verification only — no files modified)</files>
  <action>Manual verification checkpoint — perform the steps in <how-to-verify>. No source files are modified by this task.</action>
  <verify>Manual: follow <how-to-verify>; the gate is met when <acceptance_criteria> hold.</verify>
  <done>See <acceptance_criteria> / <resume-signal>.</done>
  <what-built>3-state dark toggle with no-FOUC script (C4), login always-dark (D-02), and env(safe-area-inset-top) on the mobile top strip (C1).</what-built>
  <how-to-verify>
    Theme timing, dark-system override, and iOS safe-area are not pytest-verifiable. Rebuild first (no bind-mount, Jinja caches templates): `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`. Do a browser "Clear site data" (memory sw-stale-cache-confounds-ui-verify) before each check.
    C4 toggle + persistence (DevTools, 375px):
    1. Open the config hub; the Display toggle shows Auto/Light/Dark with Auto active by default.
    2. Choose Dark — the app goes dark immediately; reload — still dark, no flash of light first (no FOUC).
    3. Choose Light — app goes light; reload — still light.
    4. Choose Auto — app follows system; toggle the OS/DevTools "prefers-color-scheme" and confirm the app follows.
    C4 dark-system Light override:
    5. Set the system to dark, then choose Light in the toggle — the app must stay LIGHT (proves the @media blocks are now .dark-class-scoped, not system-driven).
    D-02 login always-dark:
    6. Sign out and view /login (and /setup if reachable) — they remain espresso-950 dark regardless of the toggle / system setting.
    C1 iOS safe-area-top (REQUIRES a real iPhone in standalone PWA — UNVERIFIED technique, ROADMAP C1 note):
    7. On an installed iOS PWA, confirm the top strip (logo + search) is NOT obscured by the status bar / Dynamic Island. If it IS still obscured, the safe-area technique (shared with the unverified bottom fix 982c0e6) needs a revised approach for BOTH top and bottom — report that.
    Expected: instant theme switch, no FOUC, Light wins on dark system, login stays dark, iOS top strip clear of the status bar.
  </how-to-verify>
  <acceptance_criteria>
    - Toggle switches theme instantly with no flash on reload; choice persists.
    - Light override wins on a dark-preference system.
    - Login + setup stay always-dark.
    - iOS standalone top strip is clear of the status bar (or the failure is reported for a revised safe-area approach).
  </acceptance_criteria>
  <resume-signal>Type "approved" (note the iOS on-device result), or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ inline no-FOUC script | Runs under strict nonce-CSP; reads localStorage, mutates <html> class |
| browser ↔ dark-toggle Alpine component | Client-only; writes localStorage snobbery:theme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Tampering / XSS | no-FOUC inline <head> script | mitigate | Script carries nonce="{{ csp_nonce(request) }}", is eval-free (IIFE, no new Function), reads only localStorage + matchMedia — no user input interpolated. Honors ADR 0001 (no unsafe-inline/eval). The tests/ci CSP grep guard (Task 3) confirms no violation. |
| T-13-12 | Tampering | dark-toggle.js | mitigate | Alpine CSP build, string x-data, named methods only, no eval / no x-model (ADR 0001); served from 'self' with nonce. |
| T-13-13 | Information Disclosure | localStorage snobbery:theme | accept | Theme preference is non-sensitive; no encryption needed (RESEARCH Security Domain). INFO residual. |

No HIGH-severity threats. CSP/nonce posture preserved; grep guards enforce it.
</threat_model>

<verification>
- scripts/check_c4_dark.py (Tasks 1-2) green: selector config, .dark-scoped CSS, no @custom-variant, no-FOUC script nonce'd, toggle UI present, top safe-area present.
- tests/ci/ green (Task 3): CSP nonce + no-unsafe-jinja guards still pass.
- Manual (Task 4): no-FOUC theme switch, Light-on-dark-system override, login always-dark, iOS safe-area-top.
</verification>

<success_criteria>
- C4 satisfied: 3-state Auto/Light/Dark toggle on config hub, Auto default, snobbery:theme persistence, no FOUC, Light override wins, login always-dark (D-01/D-02).
- C1 satisfied: mobile top strip + search sheet carry env(safe-area-inset-top) (on-device verification flagged manual/unverified).
- Tailwind v3 selector strategy only — NO @custom-variant; CSP/nonce posture intact.
</success_criteria>

<output>
After completion, create `.planning/phases/13-pwa-ux-fixes/13-05-SUMMARY.md`.
Note: Task 1/2 reference a helper `scripts/check_c4_dark.py` — the executor must create this tiny grep-based checker as part of Task 1 (it asserts the acceptance_criteria strings: darkMode selector/class, no @custom-variant, .dark rules present, no leftover @media dark, dark-toggle component shape; with --templates it adds the base.html no-FOUC nonce script + safe-area + config_hub toggle assertions). Keeping it as a committed script makes the structural checks reproducible and CI-friendly.
</output>
