---
phase: 19-ai-page-research-predict
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app/services/ai_schemas.py
  - app/models/ai_coffee_research_cache.py
  - app/models/ai_rating_prediction.py
  - app/models/__init__.py
  - app/migrations/versions/p19_ai_research_predict.py
  - tests/services/test_ai_research.py
  - tests/services/test_brew_improve.py
  - tests/services/test_preference_prose.py
  - tests/test_migrations.py
autonomous: true
requirements: [AIX-01, AIX-02, AIX-04, AIX-09, AIX-11, AIX-12]
must_haves:
  truths:
    - "D-06: ai_coffee_research_cache table exists with cache_key PK, response_json JSONB, cited_sources JSONB, created_at, expires_at, and an expires_at index, shared across all users"
    - "D-07: ai_rating_predictions table exists per-user with UNIQUE(user_id, research_cache_key), predicted_low/high NUMERIC(3,2), confidence TEXT, reasoning TEXT, input_signature TEXT, 7-day expires_at"
    - "D-11: RecipeSuggestionSchema no longer has a no_match field; ratio/temp_c/grind_hint are required; constructing it with no_match raises ValidationError"
    - "D-02: RatingPredictionSchema carries predicted_low, predicted_high, confidence (Low|Medium|High), reasoning — a range, never a single number"
    - "AIX-01: CoffeeResearchSchema validates a complete payload and rejects extra fields (extra=forbid)"
    - "AIX-09: PreferenceProfileProseSchema has a single summary_prose field with extra=forbid"
    - "AIX-12: BrewImproveSchema + BrewParameterChangeSchema validate and reject extra fields"
    - "sse-starlette>=3.4,<4 is pinned in requirements.txt"
    - "D-08: migration INSERTs app_settings rows ai.research_daily_quota=20 and ai.improve_brew_daily_quota=20 (value_type int)"
  artifacts:
    - path: "app/models/ai_coffee_research_cache.py"
      provides: "D-06 world-view research cache model (typed Mapped columns)"
      contains: "class AICoffeeResearchCache"
    - path: "app/models/ai_rating_prediction.py"
      provides: "D-07 per-user prediction model"
      contains: "class AIRatingPrediction"
    - path: "app/migrations/versions/p19_ai_research_predict.py"
      provides: "both tables + quota app_settings rows + indexes"
      contains: "p19_ai_research_predict"
    - path: "app/services/ai_schemas.py"
      provides: "5 new/modified Pydantic schemas"
      contains: "class CoffeeResearchSchema"
  key_links:
    - from: "app/migrations/versions/p19_ai_research_predict.py"
      to: "p16_cafe_logs"
      via: "down_revision chain"
      pattern: "down_revision.*p16_cafe_logs"
    - from: "app/models/__init__.py"
      to: "AICoffeeResearchCache, AIRatingPrediction"
      via: "model registration import"
      pattern: "ai_coffee_research_cache|ai_rating_prediction"
---

<objective>
Lay the Phase 19 data + contract foundation: install sse-starlette, define all five new/changed Pydantic AI schemas (D-02/D-11/AIX-01/AIX-09/AIX-12), create the two new SQLAlchemy models (D-06/D-07), author the migration that creates both tables and seeds the two quota app_settings rows (D-08), and scaffold the Wave 0 test files so downstream plans implement against red tests.

Purpose: Every later plan (service flows, routes, templates) consumes these schemas and tables. Defining the contracts first prevents the executor scavenger-hunt anti-pattern and makes the no_match removal (D-11) a compile-time ValidationError rather than a runtime surprise.
Output: requirements.txt pin, ai_schemas.py changes, two model files, one migration, three new test files + one migration-test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ai-page-research-predict/19-CONTEXT.md
@.planning/phases/19-ai-page-research-predict/19-RESEARCH.md
@.planning/phases/19-ai-page-research-predict/19-VALIDATION.md
@app/services/ai_schemas.py
@app/models/ai_recommendation.py
@app/migrations/versions/p16_cafe_logs.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New + modified AI schemas (D-02/D-11/AIX-01/AIX-09/AIX-12)</name>
  <read_first>
    - app/services/ai_schemas.py (full file — the analog: every schema uses ConfigDict(extra="forbid"); RecipeSuggestionSchema lives at lines 34-48 today with the no_match field to delete; summary_prose convention on every top-level schema)
    - app/models/ai_recommendation.py (recommendation_type is free TEXT — new rec_type strings need no enum migration)
    - .planning/phases/19-ai-page-research-predict/19-RESEARCH.md § "Pattern 7: New Schema Definitions" (exact field names/types)
  </read_first>
  <behavior>
    - RecipeSuggestionSchema: no_match field REMOVED; ratio:str, temp_c:int, grind_hint:str all required; recipe_id:int|None and recipe_name:str|None stay nullable; summary:str stays. Constructing with no_match=True raises ValidationError (extra=forbid).
    - CoffeeResearchSchema: coffee_name:str, roaster_name:str|None, origin:str|None, process:str|None, roast_level:str|None, tasting_notes:list[str], buy_url:str|None, sources:list[str], summary_prose:str. extra=forbid rejects injected fields.
    - RatingPredictionSchema: predicted_low:float (ge=0,le=5), predicted_high:float (ge=0,le=5), confidence:Literal["Low","Medium","High"], reasoning:str. No single-number form exists.
    - BrewParameterChangeSchema: parameter:Literal["grind","ratio","temp_c","brewer","recipe"], suggested_value:str, rationale:str.
    - BrewImproveSchema: summary_prose:str, unchanged_parameters:list[str], next_try:list[BrewParameterChangeSchema].
    - PreferenceProfileProseSchema: summary_prose:str only.
    - All new top-level schemas exported in __all__.
  </behavior>
  <action>
    Modify RecipeSuggestionSchema per D-11: delete the no_match Field, add required ratio (str, description "e.g. '1:15' coffee-to-water ratio"), temp_c (int, description "Brew water temperature in Celsius"), grind_hint (str, description "e.g. 'medium-fine, ~22 clicks Encore'"). Add the five new schemas exactly as shaped in RESEARCH § Pattern 7, each with model_config = ConfigDict(extra="forbid") per the T-07-02 prompt-injection invariant. Use `from typing import Literal` for confidence/parameter enums. Carry summary_prose on every new top-level schema (AI-18 convention). Add all new class names to __all__ alphabetically. Do NOT add a url_verified field to any schema (server-side only — see the module docstring note lines 18-22).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -c "from app.services.ai_schemas import CoffeeResearchSchema, RatingPredictionSchema, BrewImproveSchema, PreferenceProfileProseSchema, RecipeSuggestionSchema; RecipeSuggestionSchema(summary='x', ratio='1:15', temp_c=94, grind_hint='medium'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/services/test_ai_research.py::test_coffee_research_schema_validates -x` passes
    - `pytest tests/services/test_ai_research.py::test_rating_prediction_schema -x` passes
    - `pytest tests/services/test_preference_prose.py::test_preference_prose_schema -x` passes
    - `pytest tests/services/test_brew_improve.py::test_brew_improve_schema -x` passes
    - Constructing RecipeSuggestionSchema(no_match=True, ...) raises pydantic.ValidationError (source assertion: no_match attribute absent from model_fields)
  </acceptance_criteria>
  <done>All five schemas validate complete payloads, reject extra fields, and no_match no longer exists on RecipeSuggestionSchema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New models + migration (D-06/D-07/D-08)</name>
  <read_first>
    - app/models/ai_recommendation.py (typed Mapped[...] column style, BigInteger Identity PK, JSONB usage, server_default text(), TIMESTAMPTZ generated_at — copy this style verbatim)
    - app/models/__init__.py (how models are registered/imported for Alembic autogenerate + Base.metadata)
    - app/migrations/versions/p16_cafe_logs.py (current head: revision="p16_cafe_logs"; copy the op.create_table / op.create_index style + the Sequence import block)
    - app/models/app_setting.py (key/value/value_type column shape for the INSERT statements)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-06/D-07 (exact column lists + TTLs)
  </read_first>
  <action>
    Create app/models/ai_coffee_research_cache.py (class AICoffeeResearchCache, __tablename__ "ai_coffee_research_cache"): cache_key Mapped[str] mapped_column(Text, primary_key=True); response_json Mapped[dict[str,Any]] JSONB nullable=False; cited_sources Mapped[list[dict[str,Any]]] JSONB nullable=False; created_at TIMESTAMPTZ server_default now(); expires_at TIMESTAMPTZ nullable=False. Add an index on expires_at via __table_args__ or Index().
    Create app/models/ai_rating_prediction.py (class AIRatingPrediction, __tablename__ "ai_rating_predictions"): id BigInteger Identity PK; user_id BigInteger FK users.id ondelete=CASCADE nullable=False; research_cache_key Text FK ai_coffee_research_cache.cache_key ondelete=CASCADE nullable=False; predicted_low/predicted_high Numeric(3,2) nullable=False; confidence Text nullable=False; reasoning Text nullable=False; input_signature Text nullable=False; created_at TIMESTAMPTZ server_default now(); expires_at TIMESTAMPTZ nullable=False. Add UniqueConstraint("user_id","research_cache_key").
    Register both in app/models/__init__.py following the existing import + __all__ pattern.
    Create app/migrations/versions/p19_ai_research_predict.py: revision="p19_ai_research_predict", down_revision="p16_cafe_logs". upgrade() creates both tables (op.create_table with sa.Numeric(3,2), JSONB via postgresql.JSONB, TIMESTAMPTZ via sa.DateTime(timezone=True)), the expires_at index, and INSERTs two app_settings rows via op.execute / bulk_insert: ('ai.research_daily_quota','20','int') and ('ai.improve_brew_daily_quota','20','int') matching the app_settings column names. downgrade() drops predictions table first (FK), then cache table, then DELETEs the two settings rows.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery alembic upgrade head && docker compose exec coffee-snobbery python -c "from app.models import AICoffeeResearchCache, AIRatingPrediction; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` applies p19_ai_research_predict with no error; `alembic downgrade -1` then `upgrade head` round-trips cleanly
    - `pytest tests/test_migrations.py -x` passes (both tables present; UNIQUE(user_id, research_cache_key) enforced)
    - CLI: `psql -c "SELECT value FROM app_settings WHERE key='ai.research_daily_quota'"` returns 20
    - Source assertion: down_revision == "p16_cafe_logs" (migration head chain intact)
  </acceptance_criteria>
  <done>Both tables created with correct columns/constraints/indexes; quota settings seeded; models import and register for metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test scaffolds for schema + migration coverage</name>
  <read_first>
    - tests/services/test_ai_service.py:520-545 (the two no_match assertions to relocate — Task is NOT to fix these here; plan 19-02 owns test_ai_service.py edits. Do NOT touch test_ai_service.py in this plan.)
    - tests/conftest.py (catalog-TRUNCATE + settings-cache-clear fixtures — project memory full-suite-test-isolation-gaps; reuse the db session fixture)
    - tests/test_migrations.py (existing migration-presence assertion style)
    - .planning/phases/19-ai-page-research-predict/19-VALIDATION.md § "Per-Task Verification Map" (exact test names to create)
  </read_first>
  <action>
    Create tests/services/test_ai_research.py with: test_coffee_research_schema_validates (complete payload validates, extra field raises ValidationError), test_rating_prediction_schema (range fields present; rejects extra). Leave clearly-marked xfail/skip placeholders OR minimal stubs for the cache/SSE/quota tests that later plans (19-03) fill: test_cache_key_normalization, test_expired_cache_eviction, test_cache_miss_calls_llm, test_cache_hit_skips_llm, test_sse_event_contract, test_advisory_lock_blocks_duplicate, test_duration_ms_written — mark these `@pytest.mark.skip(reason="filled in 19-03")` so the file collects green now.
    Create tests/services/test_preference_prose.py with test_preference_prose_schema (summary_prose present, extra=forbid).
    Create tests/services/test_brew_improve.py with test_brew_improve_schema (validates; BrewParameterChangeSchema parameter Literal enforced; extra rejected); skip-stub test for prior-sessions loading filled by 19-04.
    Extend tests/test_migrations.py with assertions that ai_coffee_research_cache and ai_rating_predictions tables exist and the UNIQUE constraint is present after upgrade head.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_ai_research.py tests/services/test_preference_prose.py tests/services/test_brew_improve.py tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The four test files collect and run with zero failures (skips allowed for later-plan placeholders)
    - `pytest tests/services/test_ai_research.py tests/services/test_preference_prose.py tests/services/test_brew_improve.py -rs -q` shows skip reasons referencing the owning downstream plan (no silent pass-by-skip masking — project memory tests-pass-by-skip-mask-green)
  </acceptance_criteria>
  <done>Wave 0 test files exist, schema tests pass, downstream placeholders are explicitly skipped with owning-plan reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_use output → Pydantic schema | Untrusted model/web-search output crosses into validated app objects |
| Migration DDL → runtime | New tables become app-trusted storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | CoffeeResearchSchema / BrewImproveSchema / all new schemas | mitigate | ConfigDict(extra="forbid") on every new schema (T-07-02 prompt-injection defence reused verbatim); injected fields raise ValidationError |
| T-19-02 | Tampering | ai_rating_predictions cross-user rows | mitigate | user_id FK ondelete=CASCADE + UNIQUE(user_id, research_cache_key); writes are user-scoped in 19-03 (no user_id from form) |
| T-19-03 | Info Disclosure | research cache shared across users | accept | D-06: the world's-view of a coffee is non-personal (origin/process/notes/buy_url) — intentionally shared; no per-user data in this table |
| T-19-SC | Tampering | sse-starlette pip install | mitigate | RESEARCH § Package Legitimacy Audit: sse-starlette [OK] approved (github.com/sysid/sse-starlette, production-stable, BSD-3). Pinned >=3.4,<4 |
</threat_model>

<verification>
- `pytest tests/services/test_ai_research.py tests/services/test_preference_prose.py tests/services/test_brew_improve.py tests/test_migrations.py -q` green
- `alembic upgrade head` clean; downgrade/upgrade round-trips
- `ruff format --check . && ruff check .` clean on all new/modified files (project memory executors-skip-ruff-ci-gates-both)
</verification>

<success_criteria>
Both tables exist, all five schemas are correct, no_match is gone, quota settings seeded, sse-starlette pinned, Wave 0 test files green.
</success_criteria>

<output>
Create `.planning/phases/19-ai-page-research-predict/19-01-SUMMARY.md` when done.
</output>
