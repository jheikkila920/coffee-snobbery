---
phase: 19-ai-page-research-predict
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - app/services/ai_quota.py
  - app/services/ai_research.py
  - app/services/analytics.py
  - tests/services/test_ai_research.py
  - tests/services/test_ai_quota.py
autonomous: true
requirements: [AIX-01, AIX-02, AIX-03, AIX-04, AIX-05, AIX-07, AIX-13]
must_haves:
  truths:
    - "AIX-04/D-06: cache key is lowercased+trimmed coffee_name + '|' + roaster_name (empty string when roaster missing); a repeat lookup within 30 days returns the cached row without firing the LLM and does NOT decrement quota"
    - "AIX-04/D-06: expired cache rows are lazily evicted at read time"
    - "AIX-05/D-08: research quota is a DB-backed COUNT over ai_recommendations(rec_type='coffee_research', error_status IS NULL, generated_at within rolling 24h) keyed to user_id; cap read from app_settings ai.research_daily_quota with a 20 fallback"
    - "AIX-05/D-09: a reset time is computable from the oldest non-expired call in the window"
    - "AIX-07/D-16: the research SSE generator yields event:message prose deltas then event:complete (rendered HTML) on success, or event:error on validation failure; two-phase pattern reuses _project_tool_use_input on get_final_message()"
    - "AIX-03: research consults get_cold_start_counts and is gated when the cold-start gate is closed"
    - "AIX-13: generate_coffee_research writes an ai_recommendations row with duration_ms and rec_type='coffee_research'; the function carries a p95 ≤ 30s comment"
    - "AIX-02/D-07: per-user prediction is signature-versioned — stale on signature mismatch OR past 7-day expiry, and regenerated without re-firing the world-view web search"
    - "Reconnect safety: the existing advisory lock (_try_advisory_lock / _get_lock) prevents a double-charge on EventSource reconnect"
  artifacts:
    - path: "app/services/ai_quota.py"
      provides: "rolling-24h COUNT, reset-time, admin cap reader"
      contains: "count_research_calls_last_24h"
    - path: "app/services/ai_research.py"
      provides: "cache lookup/eviction, normalize key, SSE two-phase generator, prediction tying"
      contains: "_research_sse_generator"
    - path: "app/services/analytics.py"
      provides: "count_research_calls_in_last_24h helper (or delegated to ai_quota)"
      contains: "research"
  key_links:
    - from: "app/services/ai_research.py"
      to: "ai_service._project_tool_use_input"
      via: "structured-output extraction on final message"
      pattern: "_project_tool_use_input"
    - from: "app/services/ai_research.py"
      to: "analytics.get_cold_start_counts, analytics.compute_input_signature"
      via: "gate check + prediction signature"
      pattern: "get_cold_start_counts|compute_input_signature"
    - from: "app/services/ai_quota.py"
      to: "ai_recommendations"
      via: "DB COUNT rolling 24h"
      pattern: "func.count"
---

<objective>
Build the research service layer: the rolling-24h quota math (D-08), the world-view cache with lazy TTL eviction + normalized key (D-06/AIX-04), the signature-versioned per-user prediction (D-07/AIX-02), and the two-phase SSE generator that streams prose then validates structured output and renders the result fragment (D-16/AIX-07). Gated by the existing cold-start counts (AIX-03). Writes telemetry to ai_recommendations with the new 'coffee_research' rec_type (AIX-13).

Purpose: This is the core new flow — the v1.2 differentiator. It reuses every security-critical primitive (citation projector, advisory lock, client builders) verbatim and adds only the cache/quota/prediction/SSE orchestration.
Output: ai_quota.py, ai_research.py, an analytics helper, filled test_ai_research.py + test_ai_quota.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-ai-page-research-predict/19-CONTEXT.md
@.planning/phases/19-ai-page-research-predict/19-RESEARCH.md
@.planning/phases/19-ai-page-research-predict/19-VALIDATION.md
@app/services/ai_service.py
@app/models/ai_recommendation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ai_quota.py — rolling-24h quota math + admin cap reader (AIX-05/D-08/D-09)</name>
  <read_first>
    - app/models/ai_recommendation.py (generated_at TIMESTAMPTZ, error_status, recommendation_type columns the COUNT filters on)
    - app/services/settings.py:147-200 (get_int reader + SettingNotFoundError — use `get_int("ai.research_daily_quota") or 20` defensive fallback per RESEARCH Pitfall 8)
    - .planning/phases/19-ai-page-research-predict/19-RESEARCH.md § "Pattern 3: Rolling 24h Quota Math" (exact query shape)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-08/D-09 (separate buckets for research vs improve-brew; cache hits do not decrement)
  </read_first>
  <behavior>
    - count_calls_last_24h(db, user_id, rec_type) returns the COUNT of ai_recommendations rows for that user+rec_type where error_status IS NULL and generated_at >= now-24h.
    - get_quota_reset_time(db, user_id, rec_type) returns oldest-in-window generated_at + 24h, or None when window empty.
    - get_quota_cap(rec_type) reads app_settings ('ai.research_daily_quota' or 'ai.improve_brew_daily_quota') via settings_service.get_int with a 20 fallback.
    - remaining(db, user_id, rec_type) = max(cap - count, 0).
  </behavior>
  <action>
    Create app/services/ai_quota.py with count_calls_last_24h, get_quota_reset_time, get_quota_cap, and remaining. Use SQLAlchemy 2.0 select(func.count(...)) / select(func.min(...)) with timezone-aware datetime.now(UTC). Map rec_type→settings-key with a small dict ('coffee_research'→'ai.research_daily_quota', 'brew_improvement'→'ai.improve_brew_daily_quota'). The reset "Hh Mm" string is computed in the router/template, not here — this module returns the datetime only. Add a thin analytics.count_research_calls_in_last_24h delegating to this module (CONTEXT names this helper on analytics.py; keep it a one-line delegation to avoid duplicating the query).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_ai_quota.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/services/test_ai_quota.py::test_quota_count -x` passes (counts only successful, in-window rows; excludes error_status rows and rows older than 24h)
    - `pytest tests/services/test_ai_quota.py::test_reset_time_computation -x` passes
    - `pytest tests/services/test_ai_quota.py::test_quota_cap_fallback -x` passes (returns 20 when setting absent)
    - Source assertion: research and improve-brew map to different settings keys (separate buckets, D-08)
  </acceptance_criteria>
  <done>Quota count, reset-time, and admin-configurable cap (with fallback) work; research and improve-brew are separate buckets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ai_research.py — cache, prediction, two-phase SSE generator (AIX-01/02/03/04/07/13)</name>
  <read_first>
    - app/services/ai_service.py:107-130 (_get_lock), :131-160 (_project_tool_use_input — call on get_final_message().content, reuse verbatim), :269-300 (_advisory_key / _try_advisory_lock), :1095-1145 (_write_recommendation_row signature — call it to write the 'coffee_research' telemetry row), :1204+ (regenerate — the orchestration pattern to mirror: lock → generate → validate → write → return)
    - app/services/ai_service._build_anthropic_client / _build_openai_client (provider abstraction — reuse; do not build new clients)
    - app/services/analytics.py:387 (get_cold_start_counts), :456 (compute_input_signature)
    - app/models/ai_coffee_research_cache.py + app/models/ai_rating_prediction.py (from 19-01)
    - .planning/phases/19-ai-page-research-predict/19-RESEARCH.md § "Pattern 1" (two-phase streaming), § "Pattern 4" (lazy TTL eviction), § "Pattern 5" (signature-versioned prediction), § "Open Questions" #1 (OpenAI fallback = NON-streaming structured call, do not stream the fallback)
  </read_first>
  <behavior>
    - normalize_cache_key(coffee_name, roaster_name) → lower().strip() of coffee_name + "|" + (roaster or "").lower().strip(). Same derivation read+write.
    - get_cached_research(db, cache_key) deletes any expired row for that key then returns the live row or None.
    - generate_coffee_research is the SSE entry: checks cold-start gate (AIX-03) and quota (raise/return a sentinel for 429 — the route owns the HTTP 429), checks cache (hit → emit event:complete instantly, no LLM, no quota decrement, cached badge flag), miss → acquire advisory lock → two-phase Anthropic stream (text deltas as event:message → get_final_message → _project_tool_use_input → CoffeeResearchSchema.model_validate) → on ValidationError emit event:error → write cache row + ai_recommendations telemetry (duration_ms, rec_type='coffee_research') → tie/refresh prediction → render research_result.html → emit event:complete.
    - get_or_refresh_prediction regenerates the per-user prediction when missing/expired/signature-mismatch, WITHOUT re-firing the web search (reuses cache_row facts + a cheap prediction call); writes/updates the ai_rating_predictions row (UNIQUE upsert).
    - OpenAI fallback path uses a non-streaming structured call (RESEARCH Open Q#1).
  </action>
  <action>
    Create app/services/ai_research.py implementing normalize_cache_key, get_cached_research (lazy delete-then-select per RESEARCH Pattern 4), generate_coffee_research (async generator yielding sse_starlette ServerSentEvent), and get_or_refresh_prediction (RESEARCH Pattern 5). Reuse ai_service._project_tool_use_input, _get_lock, _try_advisory_lock, _build_anthropic_client, _write_recommendation_row, the WEB_SEARCH_TOOL + a structure_output tool, and the for-sale-only prompt clause from 19-02. Add the D-15 comment `# p95 target: <= 30s` above generate_coffee_research. Do NOT call _verify_buy_url inside the generator (RESEARCH anti-pattern: run it as a BackgroundTask after stream close — the route schedules it); render the result with a "buy URL not verified" chip flag the route can set. SSE error events emit a short user-facing message only — log the real exception via structlog (V7 / no stack-trace leak). Fill the previously-skipped tests in test_ai_research.py.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_ai_research.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/services/test_ai_research.py::test_cache_key_normalization -x` passes (case + whitespace fold to same key)
    - `pytest tests/services/test_ai_research.py::test_expired_cache_eviction -x` passes (expired row deleted on read, returns None)
    - `pytest tests/services/test_ai_research.py -k "cache_miss or cache_hit" -x` passes (miss fires mocked LLM; hit skips it and does not decrement quota)
    - `pytest tests/services/test_ai_research.py::test_sse_event_contract -x` passes (message events precede a single complete event; validation failure yields error event)
    - `pytest tests/services/test_ai_research.py::test_advisory_lock_blocks_duplicate -x` passes
    - `pytest tests/services/test_ai_research.py::test_duration_ms_written -x` passes (rec_type='coffee_research', duration_ms non-null)
    - Source assertion: `grep -n "p95 target: <= 30s" app/services/ai_research.py`
  </acceptance_criteria>
  <done>Research cache+prediction+SSE generator work end to end against mocks; reuse the security primitives verbatim; telemetry + latency comment present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client POST → research flow | untrusted coffee_name/roaster + quota-bypass attempts cross here |
| LLM tool_use + web search → CoffeeResearchSchema | prompt-injection crosses into validated objects |
| SSE generator → browser | error events must not leak internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Elevation | research quota bypass via direct POST | mitigate | quota COUNT keyed to user_id only (never form/query); DB-backed not in-memory (V4) |
| T-19-08 | Tampering | CoffeeResearchSchema from web-search results | mitigate | _project_tool_use_input on get_final_message + extra=forbid (T-07-02 reused) |
| T-19-09 | Info Disclosure | SSE event:error leaking stack trace | mitigate | error event emits short user-facing string; real exception logged via structlog (V7) |
| T-19-10 | DoS / double-charge | EventSource reconnect re-fires LLM | mitigate | _try_advisory_lock + _get_lock held across generator; second concurrent attempt returns locked → route 429 (RESEARCH Pitfall 2) |
| T-19-11 | Spoofing/SSRF | research buy_url | mitigate | _verify_buy_url (extended in 19-02) run as BackgroundTask post-stream; never inside generator |
</threat_model>

<verification>
- `pytest tests/services/test_ai_research.py tests/services/test_ai_quota.py -x -q` green
- `pytest tests/ -x -q` green at wave merge (project memory full-suite-test-isolation-gaps — drop snobbery_test first for a clean full run)
- `ruff format --check . && ruff check .` clean
</verification>

<success_criteria>
Quota math, cache eviction, signature-versioned prediction, and the two-phase SSE generator all pass their unit tests; all security primitives reused verbatim; telemetry written with the new rec_type.
</success_criteria>

<output>
Create `.planning/phases/19-ai-page-research-predict/19-03-SUMMARY.md` when done.
</output>
