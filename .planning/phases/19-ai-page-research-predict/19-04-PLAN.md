---
phase: 19-ai-page-research-predict
plan: 04
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - app/services/ai_service.py
  - app/services/scheduler.py
  - tests/services/test_brew_improve.py
  - tests/services/test_preference_prose.py
autonomous: true
requirements: [AIX-09, AIX-12, AIX-13]
must_haves:
  truths:
    - "AIX-12/D-12: generate_brew_improvement loads ALL of the user's brew sessions for the session's coffee_id, serializes them as prompt input so the LLM avoids already-tried parameters, and validates BrewImproveSchema; it counts against the improve_brew quota bucket (separate from research)"
    - "AIX-12/D-16: generate_brew_improvement is an SSE two-phase flow (prose stream → structured validate → complete fragment) reusing _project_tool_use_input + advisory lock"
    - "AIX-09/D-10: generate_preference_profile_prose builds prompt from get_preference_profile + get_flavor_descriptors + brew/cafe rating distribution + varietal preferences, validates PreferenceProfileProseSchema, and writes ai_recommendations rec_type='preference_profile_prose' (one row per user+signature)"
    - "AIX-13/D-15: both new functions carry p95 latency comments (brew_improvement ≤ 20s, preference_profile_prose ≤ 30s) and write duration_ms telemetry"
    - "Scheduler nightly job iterates rec_types including 'preference_profile_prose' (signature-driven regen); 'brew_improvement' is on-demand and NOT in the nightly loop; research cache + predictions are NOT regenerated nightly"
  artifacts:
    - path: "app/services/ai_service.py"
      provides: "generate_brew_improvement + generate_preference_profile_prose flows"
      contains: "generate_brew_improvement"
    - path: "app/services/scheduler.py"
      provides: "nightly rec_type loop extended with preference_profile_prose"
      contains: "preference_profile_prose"
  key_links:
    - from: "app/services/ai_service.generate_brew_improvement"
      to: "brew_sessions_service prior-sessions-for-coffee"
      via: "prompt input load"
      pattern: "coffee_id"
    - from: "app/services/ai_service.generate_preference_profile_prose"
      to: "analytics.get_preference_profile, get_flavor_descriptors"
      via: "prompt input"
      pattern: "get_preference_profile|get_flavor_descriptors"
    - from: "app/services/scheduler.py run_nightly_ai_refresh"
      to: "preference_profile_prose regen"
      via: "rec_type iteration"
      pattern: "preference_profile_prose"
---

<objective>
Build the two remaining new AI service flows: improve-this-brew (AIX-12, SSE, prior-session-aware, own quota bucket) and the preference-profile prose rewrite (AIX-09, replaces structured rows), and extend the nightly scheduler to regenerate preference_profile_prose on signature change.

Purpose: AIX-12 is the "coach a brew" intelligence — it must see prior sessions for the same coffee so it proposes parameters NOT yet tried. AIX-09 turns the Preference Profile from pills into an in-depth narrative cross-cutting flavor × process × origin × varietal × rating. Both reuse the SSE/telemetry primitives.
Output: two new functions in ai_service.py, scheduler.py rec_type loop extension, filled test_brew_improve.py + test_preference_prose.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-ai-page-research-predict/19-CONTEXT.md
@.planning/phases/19-ai-page-research-predict/19-RESEARCH.md
@.planning/phases/19-ai-page-research-predict/19-VALIDATION.md
@app/services/ai_service.py
@app/services/scheduler.py
@app/services/analytics.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generate_brew_improvement SSE flow (AIX-12/D-12/D-16)</name>
  <read_first>
    - app/services/ai_service.py:131-160 (_project_tool_use_input), :107-130 (_get_lock), :282-300 (_try_advisory_lock), :1095-1145 (_write_recommendation_row), the two-phase generator pattern established in 19-03 ai_research.py (mirror it)
    - app/services/brew_sessions.py (the list/get helpers — find the one that returns a user's sessions filtered by coffee_id; reuse by_user_id-scoped queries, never trust a client coffee_id without user scope)
    - app/services/ai_quota.py (from 19-03 — count against 'brew_improvement' bucket)
    - app/services/ai_schemas.py BrewImproveSchema / BrewParameterChangeSchema (from 19-01)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-12 (server pre-loads ALL sessions for the coffee_id; unchanged_parameters is a sanity-check the LLM saw prior dials)
  </read_first>
  <behavior>
    - generate_brew_improvement(db, user_id, session_id) loads the session (user-scoped — 404/None on cross-user), loads ALL of the user's brew sessions for that session's coffee_id, serializes their dial settings (grind/ratio/temp/brewer/recipe/rating) into the prompt, runs the SSE two-phase flow, validates BrewImproveSchema, writes ai_recommendations rec_type='brew_improvement' + duration_ms, emits event:complete with the rendered improve_result fragment (rendered by the route in 19-05; service emits the validated object or HTML per the established contract).
    - Quota counted against the improve_brew bucket (separate from research).
    - p95 comment ≤ 20s above the function.
  </behavior>
  <action>
    Add generate_brew_improvement to ai_service.py mirroring the 19-03 two-phase SSE pattern (text deltas → get_final_message → _project_tool_use_input → BrewImproveSchema.model_validate → telemetry → complete). Load prior sessions via the user-scoped brew_sessions service helper filtered by coffee_id. The prompt instructs the model to propose parameters NOT already in unchanged_parameters. Use the OpenAI non-streaming fallback path (RESEARCH Open Q#1). Fill the prior-sessions-loading test in test_brew_improve.py.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_brew_improve.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/services/test_brew_improve.py::test_brew_improve_schema -x` passes
    - `pytest tests/services/test_brew_improve.py -k prior -x` passes (mock asserts all sessions for the coffee_id are serialized into the prompt)
    - Source assertion: `grep -n "p95 target: <= 20s" app/services/ai_service.py` (brew_improvement)
    - Source assertion: brew_improvement quota uses the improve_brew bucket, not research
  </acceptance_criteria>
  <done>generate_brew_improvement loads prior sessions, validates BrewImproveSchema via the SSE two-phase pattern, and writes its own-bucket telemetry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate_preference_profile_prose + scheduler rec_type loop (AIX-09/D-10/D-15)</name>
  <read_first>
    - app/services/analytics.py:91 (get_preference_profile), :216 (get_flavor_descriptors), :266 (get_sweet_spots), :456 (compute_input_signature) — CONSUME unchanged; serialize their output to JSON for the prompt
    - app/services/ai_service.py:1204+ (regenerate — how the coffee/sweet_spots bundle is generated + written; mirror for preference_profile_prose), :1095 (_write_recommendation_row)
    - app/services/scheduler.py:245-340 (run_nightly_ai_refresh — today it calls regenerate(uid, "scheduler"); extend to also regenerate preference_profile_prose per RESEARCH Pitfall 6)
    - app/services/ai_schemas.py PreferenceProfileProseSchema (from 19-01)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-10 (one row per user+signature; prompt input shape; manual refresh button is wired in templates plan 19-06)
  </read_first>
  <behavior>
    - generate_preference_profile_prose(db, user_id) builds prompt from get_preference_profile + get_flavor_descriptors + brew/cafe rating distribution + varietal preferences (all JSON-serialized), validates PreferenceProfileProseSchema, writes ai_recommendations rec_type='preference_profile_prose' (signature-keyed, one row per user+signature), with duration_ms.
    - The nightly scheduler regenerates preference_profile_prose when the signature changed; brew_improvement is NOT in the nightly loop; research cache/predictions are NOT regenerated nightly.
    - p95 comment ≤ 30s above the function.
  </behavior>
  <action>
    Add generate_preference_profile_prose to ai_service.py (non-SSE — it's a signature-driven card refresh, not a slow streamed flow per D-16's three-flow scope; structured output is fine). Extend run_nightly_ai_refresh in scheduler.py to also fire preference_profile_prose regeneration in the per-user loop, gated on signature change like the coffee bundle. Add the latency comment. Fill the preference-prose service test (beyond the schema test from 19-01: assert it consumes the analytics helpers and writes the correct rec_type).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_preference_prose.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/services/test_preference_prose.py -x -q` green (schema + flow consumes get_preference_profile/get_flavor_descriptors + writes rec_type='preference_profile_prose')
    - Source assertion: `grep -n "preference_profile_prose" app/services/scheduler.py` shows it in the nightly loop
    - Source assertion: `grep -n "brew_improvement" app/services/scheduler.py` returns nothing (on-demand only — RESEARCH Pitfall 6)
    - Source assertion: `grep -n "p95 target: <= 30s" app/services/ai_service.py` (preference_profile_prose)
  </acceptance_criteria>
  <done>Preference-prose flow consumes the analytics helpers and writes the new rec_type; scheduler regenerates it nightly on signature change; brew_improvement stays on-demand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client session_id → improve-brew | IDOR: cross-user session access crosses here |
| LLM tool_use → BrewImproveSchema / PreferenceProfileProseSchema | prompt-injection crosses into validated objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Elevation/IDOR | generate_brew_improvement session load | mitigate | session loaded with by_user_id=user_id; returns None/404 on cross-user (V4) — route enforces the 404 |
| T-19-13 | Tampering | BrewImproveSchema / PreferenceProfileProseSchema | mitigate | _project_tool_use_input + extra=forbid (T-07-02 reused) |
| T-19-14 | Elevation | improve-brew quota bypass | mitigate | quota keyed to user_id, separate improve_brew DB-backed bucket (V4) |
| T-19-15 | DoS | nightly regen cost growth | accept | preference_profile_prose regenerates only on signature change (bounded); brew_improvement on-demand; research cache TTL-driven (cost control invariant preserved) |
</threat_model>

<verification>
- `pytest tests/services/test_brew_improve.py tests/services/test_preference_prose.py -x -q` green
- `pytest tests/ -x -q` green at wave merge
- `ruff format --check . && ruff check .` clean
</verification>

<success_criteria>
Both new flows validate their schemas, brew-improve is prior-session-aware and SSE, preference-prose consumes the analytics helpers, and the scheduler regenerates preference_profile_prose nightly while keeping brew_improvement on-demand.
</success_criteria>

<output>
Create `.planning/phases/19-ai-page-research-predict/19-04-SUMMARY.md` when done.
</output>
