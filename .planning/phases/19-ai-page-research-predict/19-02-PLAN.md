---
phase: 19-ai-page-research-predict
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - app/services/ai_service.py
  - tests/services/test_ai_service.py
  - tests/services/test_archived_retry.py
autonomous: true
requirements: [AIX-11, AIX-13]
must_haves:
  truths:
    - "D-11/AIX-11: the what-to-buy-next flow never returns no_match; the recipe prompt instructs the LLM to generate a recipe from scratch (recipe_id=null) when no catalog recipe fits, always populating ratio/temp_c/grind_hint"
    - "D-14: _verify_buy_url treats HTTP 404/410 as a verification failure (in addition to the existing non-200 + SSRF gates)"
    - "D-14: _generate_coffee_rec retries ONCE with a broadened-search instruction when the first candidate's buy_url fails verification, before falling through to the existing no-recommendation fragment"
    - "D-15/AIX-13: each AI flow function in ai_service.py carries a one-line p95 latency-target comment at its definition"
    - "AIX-11: existing no_match tests in test_ai_service.py are updated to assert ratio/temp_c/grind_hint and that no_match raises ValidationError"
  artifacts:
    - path: "app/services/ai_service.py"
      provides: "archived-coffee verification + recipe-prompt update + latency comments"
      contains: "broaden"
    - path: "tests/services/test_archived_retry.py"
      provides: "404/410 rejection + retry-on-archived coverage"
      contains: "test_verify_url_rejects_404"
  key_links:
    - from: "app/services/ai_service.py _generate_coffee_rec"
      to: "_verify_buy_url"
      via: "archived-coffee retry decision"
      pattern: "_verify_buy_url"
---

<objective>
Apply the service-layer AIX-11 fix and the D-14 archived-coffee hardening to the existing what-to-buy-next flow, and stamp the D-15 latency targets onto every AI flow function. This plan owns the test_ai_service.py edits that plan 19-01's schema change (no_match removal) breaks.

Purpose: AIX-11 reclassifies no_match=true as a bug — the schema deletion in 19-01 makes it a ValidationError, but the PROMPT must also stop the model from ever wanting to say "no recipe" by instructing it to generate one. D-14 closes the 07-UAT-1 archived-coffee finding. D-15 documents latency targets as code comments (single source of truth at the function).
Output: ai_service.py prompt + verifier + retry + comments; updated test_ai_service.py; new test_archived_retry.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-ai-page-research-predict/19-CONTEXT.md
@.planning/phases/19-ai-page-research-predict/19-RESEARCH.md
@app/services/ai_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _verify_buy_url for 404/410 + archived-coffee retry in _generate_coffee_rec (D-14)</name>
  <read_first>
    - app/services/ai_service.py:161-211 (_verify_buy_url — the SSRF-hardened verifier: https-only, _assert_public_host, 64KB Range cap, 5s timeout; ranged GET + 200-class check. Extend, do NOT rewrite the security gates)
    - app/services/ai_service.py:212-268 (_assert_public_host — reuse verbatim, no change)
    - app/services/ai_service.py around _generate_coffee_rec + _build_coffee_rec_prompt (the tiered search flow; locate where buy_url verification / search_tier is decided)
    - app/services/ai_service.py:1204+ (regenerate — the entry that drives _generate_coffee_rec; understand where a retry would slot)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-14 (exact retry semantics + the research-flow "buy URL not verified" chip is NOT built here — research flow lives in 19-03)
  </read_first>
  <behavior>
    - _verify_buy_url returns False when the ranged GET status is 404 or 410 (currently only non-200-class fails — make 404/410 explicit). All existing SSRF/redirect/scheme gates unchanged.
    - Optional cheap check: if "Sold Out" / "sold out" appears in the first 64KB Range body, treat as failure (only if it does not complicate the existing 64KB read; otherwise skip per D-14 "if cheap to detect").
    - _generate_coffee_rec: when the first candidate's buy_url fails verification, fire ONE more LLM call with the broadened-search instruction ("Try again with a broader search; the first candidate appears archived.") before returning the existing "no good recommendation today" path. Does not loop more than once.
  </behavior>
  <action>
    In _verify_buy_url add an explicit `if resp.status_code in (404, 410): return False` (and keep the existing non-200 fallthrough). Add the D-14 archived-coffee prompt clause to _build_coffee_rec_prompt and the research prompt builder it shares ("Only recommend coffees that are currently for sale at the roaster's website. Avoid archived, sold-out, or discontinued lots."). In _generate_coffee_rec, wrap the buy_url verification result so a verification failure on the first pick triggers one retry pass with the broadened-search system instruction, then falls through unchanged. Reuse _project_tool_use_input and the existing Pydantic validation/retry path verbatim — do not reimplement them.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_archived_retry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/services/test_archived_retry.py::test_verify_url_rejects_404 -x` passes (404 and 410 both return False)
    - `pytest tests/services/test_archived_retry.py::test_archived_retry_logic -x` passes (mock: first buy_url fails → second LLM call fired with broadened instruction → success)
    - Source assertion: the broadened-search instruction string is present in ai_service.py
  </acceptance_criteria>
  <done>_verify_buy_url rejects 404/410; the coffee-rec flow retries once on archived first pick; prompts instruct for-sale-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update no_match tests + add D-15 latency-target comments (AIX-11/AIX-13)</name>
  <read_first>
    - tests/services/test_ai_service.py:157 (test_projector_no_match_raises — this is a citation-projector test, NOT the recipe schema; verify whether it needs changing — likely leave unless it constructs RecipeSuggestionSchema)
    - tests/services/test_ai_service.py:515-545 (test asserting result.no_match is False / is True — these MUST change: rewrite to assert ratio/temp_c/grind_hint populated and recipe generated when no catalog match)
    - app/services/ai_service.py (locate every AI flow function definition: _generate_coffee_rec, generate_equipment_rec, generate_sweet_spots_prose, rank_pasted_coffees — and note the new ones will be added in 19-03/19-04)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-15 (the seven p95 targets)
  </read_first>
  <behavior>
    - test_ai_service.py no longer references no_match on RecipeSuggestionSchema; instead asserts the recipe suggestion carries ratio, temp_c, grind_hint and that a generated recipe sets recipe_id=None.
    - A test asserts constructing RecipeSuggestionSchema(no_match=True) raises ValidationError.
    - Each existing AI flow function has a one-line comment above its def stating its p95 target.
  </behavior>
  <action>
    Run `grep -rn "no_match" tests/` first to confirm scope (expected: only test_ai_service.py lines ~520/525-545 plus the unrelated flavor_notes datalist test which is NOT touched). Rewrite the no_match suggest-recipe tests to the new contract: add test_recipe_schema_no_match_rejected (ValidationError on no_match) and test_recipe_schema_required_fields (ratio/temp_c/grind_hint required) per VALIDATION.md names. Add D-15 latency-target comments: `_generate_coffee_rec` p95 ≤ 60s, `generate_equipment_rec` p95 ≤ 20s, `generate_sweet_spots_prose` p95 ≤ 30s, `rank_pasted_coffees` p95 ≤ 45s. (The three new-flow comments — generate_coffee_research ≤30s, generate_brew_improvement ≤20s, generate_preference_profile_prose ≤30s — are added by 19-03/19-04 when those functions are created; note this in the SUMMARY.)
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_ai_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/services/test_ai_service.py -x -q` green (no no_match references remain)
    - `pytest tests/services/test_ai_service.py::test_recipe_schema_no_match_rejected tests/services/test_ai_service.py::test_recipe_schema_required_fields -x` passes
    - `grep -n "p95" app/services/ai_service.py` shows the four existing-flow latency comments
  </acceptance_criteria>
  <done>test_ai_service.py reflects the new recipe contract and the four existing AI flows carry latency-target comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-provided buy_url → outbound HTTP | SSRF + archived-lot risk crosses to network |
| web-search results → recipe recommendation | prompt-injection / stale-lot tampering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Spoofing/SSRF | _verify_buy_url buy_url fetch | mitigate | Existing _assert_public_host scheme+IP gate + no cross-host redirect + 64KB Range + 5s timeout reused verbatim; extended to reject 404/410 (T-07-01) |
| T-19-05 | Tampering | recipe recommendation prompt | mitigate | for-sale-only clause + _project_tool_use_input citation projector (T-07-02) unchanged; broadened retry bounded to one extra call (no amplification) |
| T-19-06 | DoS | archived-retry amplification | mitigate | Retry is single-shot (one extra LLM call max); bounded by existing advisory lock + manual-refresh throttle |
</threat_model>

<verification>
- `pytest tests/services/test_ai_service.py tests/services/test_archived_retry.py -x -q` green
- `ruff format --check . && ruff check .` clean
- No `no_match` token remains in tests/ except the unrelated flavor_notes datalist test
</verification>

<success_criteria>
404/410 rejected, single archived retry works, for-sale-only prompt in place, no_match tests rewritten, four latency comments added.
</success_criteria>

<output>
Create `.planning/phases/19-ai-page-research-predict/19-02-SUMMARY.md` when done.
</output>
