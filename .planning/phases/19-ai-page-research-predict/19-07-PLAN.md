---
phase: 19-ai-page-research-predict
plan: 07
type: execute
wave: 6
depends_on: ["19-06"]
files_modified:
  - CONTRIBUTING.md
  - .planning/phases/19-ai-page-research-predict/19-VERIFICATION.md
  - app/templates/pages/admin_settings.html
  - tests/routers/test_admin_settings.py
autonomous: false
requirements: [AIX-05, AIX-13]
must_haves:
  truths:
    - "AIX-13/D-15: the p50/p95 latency investigation is run against ai_recommendations.duration_ms grouped by recommendation_type (last 30 days); each flow is compared to its documented p95 target and any regression is fixed or has a written justification in 19-VERIFICATION.md"
    - "D-16 (research correction): the NPM Advanced-config proxy_buffering off block is documented in CONTRIBUTING.md and the end-to-end SSE-through-NPM check is recorded as an operator manual verification"
    - "AIX-05/D-08: both quota settings (ai.research_daily_quota, ai.improve_brew_daily_quota) are editable from the Admin Settings page (the generic settings editor renders DB rows; verify they appear and save)"
    - "The full test suite is green twice (project memory full-suite-test-isolation-gaps) and ruff format+check both pass before phase close"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "NPM proxy_buffering off SSE block + operator SSE smoke procedure"
      contains: "proxy_buffering off"
    - path: ".planning/phases/19-ai-page-research-predict/19-VERIFICATION.md"
      provides: "latency investigation results + per-flow target verdicts"
      contains: "p95"
  key_links:
    - from: "CONTRIBUTING.md NPM section"
      to: "X-Accel-Buffering:no SSE responses"
      via: "operator config requirement"
      pattern: "proxy_buffering off"
---

<objective>
Close Phase 19: run the AIX-13 latency investigation and record per-flow verdicts, document the NPM proxy_buffering off requirement (the D-16 research correction), confirm the two quota settings are admin-editable (D-08), and run the full-suite + ruff gates before close.

Purpose: D-15 mandates a one-time p50/p95 capture comparing each flow to its documented target. The NPM/SSE buffering correction means streaming UX requires an operator config block that must be documented and smoke-tested. These are the non-code closeout obligations of the phase.
Output: CONTRIBUTING.md NPM block, 19-VERIFICATION.md latency ledger, admin-settings verification, green-suite confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-ai-page-research-predict/19-CONTEXT.md
@.planning/phases/19-ai-page-research-predict/19-RESEARCH.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Latency investigation + NPM SSE documentation (AIX-13/D-15, D-16 correction)</name>
  <read_first>
    - .planning/phases/19-ai-page-research-predict/19-RESEARCH.md § "Latency Investigation Query (D-15)" (the PERCENTILE_CONT SQL), § "Pitfall 1" (NPM proxy_buffering off block: proxy_buffering off; proxy_http_version 1.1; proxy_read_timeout 300s; proxy_send_timeout 300s)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-15 (seven p95 targets), D-16 (SSE scope)
    - CONTRIBUTING.md (carved out in Phase 18 — the dev/ops guide where the NPM block belongs; find the deploy / reverse-proxy section)
    - project memory snobbery-vps-npm-reverse-proxy + project memory commit-docs-false-untracked-review-docs (commit_docs:false — 19-VERIFICATION.md must be git add + committed manually)
  </read_first>
  <action>
    Run the p50/p95 PERCENTILE_CONT query against ai_recommendations grouped by recommendation_type (last 30 days, error_status IS NULL, duration_ms NOT NULL) using the same query helper added in 19-05. Record results in a new .planning/phases/19-ai-page-research-predict/19-VERIFICATION.md table: per recommendation_type, call_count, p50_ms, p95_ms, documented target, verdict (within target / needs fix / fundamental-with-justification). For any flow over target, either note a fix or write the justification (e.g., web-search-tool dominates). Note: new flows may have little/no data yet at close — record "insufficient data, re-measure at Phase 22" where applicable (Phase 22 reruns this as a smoke check). Add to CONTRIBUTING.md a reverse-proxy SSE subsection documenting the NPM Advanced-config proxy_buffering off block (verbatim from RESEARCH Pitfall 1) and noting the backend already emits X-Accel-Buffering:no; include the operator SSE smoke procedure (trigger a research call from a real browser through NPM, confirm prose streams incrementally not bursted).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics_perf.py::test_latency_percentile_query -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 19-VERIFICATION.md contains a per-recommendation_type p50/p95 table with a verdict column referencing the D-15 targets
    - Source assertion: `grep -n "proxy_buffering off" CONTRIBUTING.md`
    - CONTRIBUTING.md contains the operator SSE smoke procedure
  </acceptance_criteria>
  <done>Latency ledger written with per-flow verdicts; NPM proxy_buffering off block + SSE smoke procedure documented.</done>
</task>

<task type="auto">
  <name>Task 2: Admin-editable quota settings verification (AIX-05/D-08)</name>
  <read_first>
    - app/routers/admin/settings_editor.py:60-220 (the settings editor renders ALL non-read-only app_settings rows generically and edits in place by value_type; the two new int rows seeded by the 19-01 migration should appear automatically — verify, do not duplicate)
    - app/templates/pages/admin_settings.html + app/templates/fragments/admin_setting_row.html (the generic row render + the int input_kind)
    - tests/routers/test_admin_settings.py (existing settings-editor test style)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-08 (admin-configurable, default 20 each)
  </read_first>
  <action>
    Confirm ai.research_daily_quota and ai.improve_brew_daily_quota render as editable int inputs on /admin (the generic editor handles this; only add a _READ_ONLY_KEYS exclusion check or input_kind tweak if they render wrong). Add a test asserting both keys appear in the settings list and a POST update to ai.research_daily_quota persists and is read back by ai_quota.get_quota_cap (settings cache invalidation on commit). Do NOT add bespoke template controls if the generic editor already covers them — note that in the SUMMARY.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/routers/test_admin_settings.py -k quota -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/routers/test_admin_settings.py -k quota -x` passes (both quota keys editable; update persists; cap reader reflects the new value)
    - Source/behavioral assertion: editing ai.research_daily_quota to 5 then calling get_quota_cap('coffee_research') returns 5
  </acceptance_criteria>
  <done>Both quota settings are admin-editable and changes take effect in the quota reader.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-close gate — full suite + ruff + operator NPM/SSE smoke</name>
  <action>Pause for the phase-close human gate. No code changes — the human runs the full suite + ruff gates and the operator NPM/SSE smoke, then approves or reports failures.</action>
  <what-built>
    All Phase 19 surfaces (research+predict, improve-brew, preference prose, charts, quotas, SSE) plus the latency ledger and NPM SSE documentation.
  </what-built>
  <how-to-verify>
    1. Full suite twice (project memory full-suite-test-isolation-gaps): drop snobbery_test, then `docker compose exec coffee-snobbery python -m pytest tests/ -q` twice — both runs green (a clean run is the established baseline; only the known concurrent-race test may flake per memory). Run `-rs` and treat any skip-as-green as a gap.
    2. `ruff format --check . && ruff check .` both clean (project memory executors-skip-ruff-ci-gates-both — commit a style() fix if needed before close).
    3. Operator step (you, on the VPS): apply the proxy_buffering off block to the Snobbery NPM proxy host Advanced config, deploy via git pull + rebuild, then trigger a research call from a real browser through NPM and confirm prose streams incrementally (not bursted at close). Record the result in 19-VERIFICATION.md.
    4. Confirm 19-VERIFICATION.md and any other planning docs are git add + committed manually (commit_docs:false — project memory commit-docs-false-untracked-review-docs).
  </how-to-verify>
  <resume-signal>Type "approved" or report the failing suite/ruff/SSE-smoke result.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator NPM config → SSE delivery | misconfig degrades streaming (availability, not security) |
| admin settings → quota cap | privileged change to cost-control bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-25 | Tampering | admin lowering/raising quota cap | accept | admin-only route (require admin); audited via ADMIN_APP_SETTING_CHANGED; intended privileged control (D-08) |
| T-19-26 | DoS | misapplied NPM config buffers SSE | accept | functional fallback (events arrive at close); documented operator step + smoke test; X-Accel-Buffering:no is defense-in-depth |
| T-19-27 | Info Disclosure | latency ledger exposing internals | accept | 19-VERIFICATION.md is a planning artifact, not user-facing; no secrets recorded |
</threat_model>

<verification>
- `pytest tests/ -q` green twice
- `ruff format --check . && ruff check .` clean
- 19-VERIFICATION.md latency ledger + NPM doc complete; operator SSE smoke recorded
- Human-check (Task 3) approved
</verification>

<success_criteria>
Latency investigated and recorded, NPM SSE documented + smoke-tested by the operator, quota settings admin-editable, full suite green twice, ruff clean.
</success_criteria>

<output>
Create `.planning/phases/19-ai-page-research-predict/19-07-SUMMARY.md` when done.
</output>
