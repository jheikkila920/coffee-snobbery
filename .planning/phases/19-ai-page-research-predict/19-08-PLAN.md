---
phase: 19-ai-page-research-predict
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/ai_research.py
  - app/services/ai_service.py
  - app/services/ai_schemas.py
  - app/models/ai_coffee_research_cache.py
  - app/migrations/versions/p19_ai_research_predict.py
  - tests/services/test_ai_research.py
  - tests/services/test_brew_improve.py
autonomous: true
requirements: [AIX-02, AIX-04, AIX-07, AIX-12, AIX-13]
gap_closure: true

must_haves:
  truths:
    - "The research event:complete payload is the rendered fragments/ai/research_result.html, not an f-string (CR-01)"
    - "An adversarial coffee_name containing <script>/onerror markup is returned HTML-escaped in the research event:complete payload, never raw (CR-01)"
    - "The improve-brew event:complete payload is the rendered fragments/brew/improve_result.html, not a JSON string (CR-02)"
    - "An adversarial summary_prose containing markup is returned HTML-escaped in the improve-brew event:complete payload (CR-02)"
    - "No AI-derived prose is rendered into HTML via Python f-string anywhere in ai_research.py or ai_service.py (CR-01/CR-02)"
    - "research_result.html context-variable contract (result/is_cached/buy_url_unverified) agrees with what the generator passes (IN-01)"
    - "cited_sources contract (schema sources type, model annotation, migration comment) is internally consistent (WR-06)"
  artifacts:
    - path: "app/services/ai_research.py"
      provides: "Template-rendered research result via Jinja autoescape; dead f-string stub removed"
    - path: "app/services/ai_service.py"
      provides: "Template-rendered improve-brew result via Jinja autoescape"
    - path: "tests/services/test_ai_research.py"
      provides: "Regression test: research payload is rendered HTML and escapes adversarial coffee_name"
    - path: "tests/services/test_brew_improve.py"
      provides: "Regression test: improve-brew payload is rendered HTML and escapes adversarial prose"
  key_links:
    - from: "app/services/ai_research.py"
      to: "fragments/ai/research_result.html"
      via: "templates.get_template(...).render(...)"
      pattern: "research_result\\.html"
    - from: "app/services/ai_service.py"
      to: "fragments/brew/improve_result.html"
      via: "templates.get_template(...).render(...)"
      pattern: "improve_result\\.html"
---

<objective>
Close the two reopen BLOCKERs from 19-REVIEW.md by routing both AI-prose SSE
result payloads through their autoescaped Jinja templates instead of hand-built
strings, and reconcile the cited_sources data contract (WR-06) that lives in the
same research write path.

- CR-01 (stored XSS): the research `event:complete` payload is currently a raw
  f-string (`_render_research_result`, ai_research.py ~:654-675) that interpolates
  LLM/web-search-derived `coffee_name` unescaped, then HTMX swaps it into the live
  DOM via `innerHTML`. This violates CLAUDE.md's "never bypass autoescape for AI
  prose" invariant. Replace the f-string with a render of
  `fragments/ai/research_result.html` (autoescape ON).
- CR-02 (broken feature + contract violation): the improve-brew `event:complete`
  payload is raw JSON (ai_service.py ~:2156-2162), so the coaching card never
  renders. Render `fragments/brew/improve_result.html` server-side and emit the
  HTML.
- WR-06: `CoffeeResearchSchema.sources` is `list[str]`, written verbatim into
  `cited_sources`, but the model annotation and migration comment say
  `list[dict[str, Any]]`. Reconcile to one shape.

Purpose: Re-establish the autoescape boundary on every AI-prose render path (the
non-negotiable security invariant) and make the headline "Coach a brew" feature
actually render.
Output: Template-rendered SSE payloads, the dead f-string stub removed, a
consistent cited_sources contract, and regression tests proving escaping + HTML
render against the baked test image.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ai-page-research-predict/19-REVIEW.md
@.planning/phases/19-ai-page-research-predict/19-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render research result through autoescaped Jinja template (CR-01 + IN-01 + IN-03)</name>
  <files>app/services/ai_research.py, tests/services/test_ai_research.py</files>
  <read_first>
    - app/services/ai_research.py (the f-string sink `_render_research_result` :654-675; the three call sites that pass `cache_row`/`prediction`/`cached` at :428-433, :458-459, :623-628; cache-hit branches :420-460; the credential resolution at :407-412)
    - app/templates/fragments/ai/research_result.html (target template; expects context vars `result`, `prediction`, `is_cached`, `buy_url_unverified`; reads `request.cookies.get('csrftoken')` at :45 for the wishlist CSRF field)
    - app/templates_setup.py (the shared `templates = Jinja2Templates(...)` instance with `templates.env.autoescape = select_autoescape(...)` at :40-47)
    - app/services/ai_schemas.py (CoffeeResearchSchema :221-260 — the shape of `result`: coffee_name, roaster_name, buy_url, sources, etc.)
    - CLAUDE.md (the autoescape / AI-prose invariant)
  </read_first>
  <action>
    Rewrite `_render_research_result` in ai_research.py to render
    `fragments/ai/research_result.html` via the shared `templates` instance
    (`from app.templates_setup import templates`; `templates.get_template("fragments/ai/research_result.html").render(...)`). Do NOT build HTML with an
    f-string — the AI-derived `coffee_name`/`roaster_name`/`summary` strings MUST
    pass through Jinja autoescape (CLAUDE.md invariant; this is the CR-01 fix).

    Reconcile the context-variable contract (IN-01): the template expects
    `result` (a CoffeeResearchSchema-shaped object exposing `.coffee_name`,
    `.roaster_name`, `.buy_url`), `prediction` (AIRatingPrediction | None),
    `is_cached` (bool), and `buy_url_unverified` (bool). Inside the renderer,
    build `result` by validating `cache_row.response_json` with
    `CoffeeResearchSchema.model_validate(...)`, map the existing `cached` param to
    the template's `is_cached`, and pass `buy_url_unverified=False` for now
    (the buy-URL verifier runs as a background task per T-19-11; default to
    unverified=False at render time). Keep the `_render_research_result` keyword
    signature usable by all three existing call sites (cache-hit at :428, lock
    re-check at :458, miss path at :623) — they already pass
    `cache_row=`, `prediction=`, `cached=`; add the new defaults so callers need
    no change unless a real Request is threaded.

    CSRF token: `research_result.html:45` reads
    `request.cookies.get('csrftoken')`. Since the generator has no `Request`,
    pass `request=None` to the render and make the template tolerate a None
    request by defaulting the token to empty string (the template already uses
    `request.cookies.get('csrftoken', '')`; guard it so `request=None` does not
    AttributeError — render the hidden field value as empty when `request` is
    falsy). The wishlist POST is itself CSRF-protected by CSRFMiddleware via the
    cookie+header double-submit, so an empty hidden field is acceptable; the
    client form already carries the token. Choose this contract (empty-on-None)
    and apply it in the template guard — do NOT thread a Request through the async
    generator signature (avoids a wider refactor).

    Delete the stale "stub added in 19-04 / sufficient for service-layer tests"
    docstring framing (IN-03) and replace it with an accurate docstring stating
    this is the live render path and renders through autoescape.

    Tests first (tdd): in tests/services/test_ai_research.py add a regression test
    that drives `generate_coffee_research` (or directly calls the rewritten
    `_render_research_result`) with a `cache_row.response_json` whose
    `coffee_name` is `<script>alert(1)</script>` / `"><img src=x onerror=alert(1)>`,
    and asserts (a) the returned payload contains the rendered template marker
    (the template's outer wrapper / `Add to wishlist` text or a stable class), and
    (b) the raw `<script>`/`onerror=` substring is NOT present while its escaped
    form (`&lt;script&gt;` / `&gt;`) IS present. Assert the f-string artifact
    `<div id="research-result">` is gone.
  </action>
  <behavior>
    - Render returns HTML produced by Jinja (contains the template's static markup, e.g. the wishlist button text), not an f-string div
    - coffee_name "&lt;script&gt;alert(1)&lt;/script&gt;" appears escaped, raw "<script>" does not appear
    - prediction range still renders when prediction is not None; renders without prediction block when None
    - cached=True path renders the "cached" badge; cached=False omits it
  </behavior>
  <verify>
    <automated>docker compose build coffee-snobbery-test && docker compose run --rm coffee-snobbery-test python -m pytest tests/services/test_ai_research.py -x -rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' app/services/ai_research.py | grep -c 'f.<div id=.research-result'` returns 0 (the f-string sink is gone)
    - ai_research.py contains a `templates.get_template("fragments/ai/research_result.html")` render call
    - New escaping regression test passes against the baked `coffee-snobbery-test` image (rebuild required — no bind-mount); test collects >=1 (not skipped)
    - `ruff format --check app/services/ai_research.py` and `ruff check app/services/ai_research.py` both clean
  </acceptance_criteria>
  <done>Research SSE event:complete renders fragments/ai/research_result.html through autoescape; adversarial coffee_name is escaped; the dead f-string stub and its stale docstring are removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render improve-brew result through autoescaped Jinja template (CR-02)</name>
  <files>app/services/ai_service.py, tests/services/test_brew_improve.py</files>
  <read_first>
    - app/services/ai_service.py (the JSON emit at :2156-2162; the generator signature `generate_brew_improvement` at :1926-1931 which has `session_id`; the success log just above at :2146-2154)
    - app/templates/fragments/brew/improve_result.html (target template; expects context vars `result` (BrewImproveSchema: summary_prose, unchanged_parameters[], next_try[]) and `session_id` (int); outer id is `improve-brew-result`)
    - app/templates/pages/brew_form.html (the improve-brew mount :339-364 — sse-swap="complete" with hx-swap="innerHTML"; comment states it expects the improve_result.html fragment)
    - app/templates_setup.py (shared `templates` instance)
    - app/services/ai_schemas.py (BrewImproveSchema + BrewParameterChangeSchema shapes)
  </read_first>
  <action>
    Replace the terminal JSON emit in `generate_brew_improvement` (ai_service.py
    :2156-2162, the `_json.dumps(result_schema.model_dump())` ServerSentEvent)
    with a server-side render of `fragments/brew/improve_result.html` and emit the
    rendered HTML as the `event="complete"` payload. Use the shared templates
    instance: `from app.templates_setup import templates` then
    `templates.get_template("fragments/brew/improve_result.html").render(result=result_schema, session_id=session_id)`.
    Emit `ServerSentEvent(data=html, event="complete")`. Remove the now-dead
    `import json as _json` if it is unused after this change (check it is not used
    elsewhere in the function first). Apply the same autoescape discipline as
    Task 1 — the template already does the right thing (no `|safe`); just route
    through it. This makes the headline "Coach a brew" feature render the card
    instead of a raw JSON blob.

    Tests first (tdd): in tests/services/test_brew_improve.py add a regression
    test that drives `generate_brew_improvement` to completion (mock the provider
    call so the validated BrewImproveSchema has a `summary_prose` containing
    `<script>alert(1)</script>` or an `onerror=` payload) and asserts the
    `event:complete` data (a) contains `id="improve-brew-result"` (the template's
    outer id, the rendered-HTML marker), and (b) the raw `<script>`/`onerror=`
    substring is absent while the escaped form is present. Assert the payload does
    NOT parse as a bare JSON object (it is HTML now).
  </action>
  <behavior>
    - event:complete data contains `id="improve-brew-result"` and the "Improvement suggestions" heading text from the template
    - summary_prose with markup is HTML-escaped in the payload; raw "<script>" absent
    - next_try items render parameter / suggested_value / rationale rows
    - payload is no longer a JSON string (json.loads on it does not yield the schema dict)
  </behavior>
  <verify>
    <automated>docker compose build coffee-snobbery-test && docker compose run --rm coffee-snobbery-test python -m pytest tests/services/test_brew_improve.py -x -rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_json.dumps(result_schema.model_dump())' app/services/ai_service.py` returns 0 in the improve-brew generator path
    - ai_service.py contains a `templates.get_template("fragments/brew/improve_result.html")` render call inside `generate_brew_improvement`
    - New HTML-render + escaping regression test passes against the baked `coffee-snobbery-test` image; collects >=1 (not skipped)
    - `ruff format --check app/services/ai_service.py` and `ruff check app/services/ai_service.py` both clean
  </acceptance_criteria>
  <done>Improve-brew SSE event:complete renders fragments/brew/improve_result.html through autoescape; the coaching card displays instead of raw JSON; adversarial prose is escaped.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile cited_sources contract to list[str] (WR-06)</name>
  <files>app/models/ai_coffee_research_cache.py, app/migrations/versions/p19_ai_research_predict.py, app/services/ai_schemas.py</files>
  <read_first>
    - app/services/ai_schemas.py (CoffeeResearchSchema.sources at :244 — currently `list[str]`; this is the producer / source of truth for the shape)
    - app/models/ai_coffee_research_cache.py (cited_sources annotation at :43 — currently `Mapped[list[dict[str, Any]]]`)
    - app/migrations/versions/p19_ai_research_predict.py (the cited_sources column definition at ~:65-67 and its comment)
    - app/services/ai_research.py (the write at :591 `cited_sources = result_schema.sources or []` — a list[str]; do not change the write)
  </read_first>
  <action>
    The write path stores `result_schema.sources` (a `list[str]`) into
    `cited_sources`, but the model annotation and migration comment claim
    `list[dict[str, Any]]`. Pick the shape that matches what is actually written
    (`list[str]`) — this is a pure annotation/comment reconciliation, no stored
    data shape change, so NO new migration is needed (JSONB already holds the
    strings; existing rows are already list[str]).

    Change the model annotation in ai_coffee_research_cache.py:43 from
    `Mapped[list[dict[str, Any]]]` to `Mapped[list[str]]` and update its
    "Cited source URLs extracted at write time (D-06)" comment to state the shape
    is a list of URL strings. Update the migration's `cited_sources` column
    comment in p19_ai_research_predict.py to say `list[str]` (URL strings) rather
    than a list of `{"url","title"}` dicts. Add a one-line comment at the model
    field noting "shape matches CoffeeResearchSchema.sources (list[str]); no
    migration needed — annotation-only reconciliation per WR-06." Remove the now
    unused `dict`/`Any` import from the model only if nothing else in the file
    uses it.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery-test && docker compose run --rm coffee-snobbery-test python -m pytest tests/services/test_ai_research.py tests/migrations -x -rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'cited_sources' app/models/ai_coffee_research_cache.py` shows `Mapped[list[str]]`
    - Migration comment for cited_sources references `list[str]` (URL strings), not a list of dicts
    - No new Alembic migration file was created (annotation-only change)
    - Existing research + migration tests still pass against the baked test image
    - `ruff format --check` and `ruff check` clean on both edited Python files
  </acceptance_criteria>
  <done>Schema, model annotation, and migration comment all agree that cited_sources is list[str]; no data-shape migration introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web search → LLM → SSE payload → browser DOM | LLM output is grounded in adversarial web-search content; it crosses into the victim's authenticated DOM via HTMX `hx-swap="innerHTML"` on `event:complete`. This is the CR-01 sink. |
| client form → POST /ai/research, POST /ai/improve-brew | CSRF-protected state-changing POSTs; double-submit cookie + header enforced by CSRFMiddleware. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08-01 | Tampering/Info-disclosure (Stored XSS) | `_render_research_result` in ai_research.py emitting AI-derived `coffee_name`/prose into `event:complete` | mitigate | Render through `fragments/ai/research_result.html` (autoescape ON); delete the f-string. Regression test asserts injected `<script>`/`onerror` coffee name is escaped. (Task 1) |
| T-19-08-02 | Tampering (raw-HTML SSE emission) | `generate_brew_improvement` terminal emit in ai_service.py | mitigate | Render through `fragments/brew/improve_result.html` (autoescape ON); emit HTML not JSON. Regression test asserts injected markup in `summary_prose` is escaped. (Task 2) |
| T-19-08-03 | Tampering (residual raw-HTML SSE path) | any remaining f-string/`|safe` AI-prose render in ai_research.py / ai_service.py | mitigate | Acceptance grep confirms zero `f"<div id="research-result"` and zero `_json.dumps(...model_dump())` complete-event emits remain; must_have asserts no AI prose is rendered to HTML via f-string. |
| T-19-08-04 | Spoofing/Tampering (CSRF on wishlist add) | wishlist-add form rendered with `request=None` → empty CSRF hidden field | accept | The wishlist POST is independently protected by CSRFMiddleware double-submit (cookie+header); the client form carries the token. Empty hidden field at render time is acceptable at household scale. Documented in Task 1 action. |

Any remaining raw-HTML SSE emission of AI-derived prose is treated as HIGH severity and is closed by Tasks 1+2 plus the must_have "No AI-derived prose is rendered into HTML via Python f-string anywhere."
</threat_model>

<verification>
- Both regression tests (research escaping, improve-brew HTML render + escaping) pass against the baked `coffee-snobbery-test` image after rebuild.
- `grep` confirms the f-string sink and the JSON complete-emit are both gone.
- cited_sources annotation/comment reconciled; no new migration.
- `ruff format --check` and `ruff check` clean on all edited Python files.
- Full AI test slice green: `tests/services/test_ai_research.py tests/services/test_brew_improve.py tests/routers/test_ai_research.py tests/routers/test_ai_improve.py`.
</verification>

<success_criteria>
- Research and improve-brew `event:complete` payloads are rendered Jinja fragments (autoescape ON), with zero raw-HTML AI-prose emission anywhere in the two services.
- An injected `<script>`/`onerror` coffee name and an injected markup `summary_prose` both come back HTML-escaped (proven by tests).
- The "Coach a brew" feature renders the coaching card instead of a JSON blob.
- cited_sources contract is internally consistent (schema = model = migration = list[str]).
- All edits are ruff-clean and the AI test slice is green on the baked test image.
</success_criteria>

<output>
Create `.planning/phases/19-ai-page-research-predict/19-08-SUMMARY.md` when done.
</output>
