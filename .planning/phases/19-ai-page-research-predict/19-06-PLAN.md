---
phase: 19-ai-page-research-predict
plan: 06
type: execute
wave: 5
depends_on: ["19-05"]
files_modified:
  - app/templates/base.html
  - app/static/css/tailwind.src.css
  - app/templates/pages/ai.html
  - app/templates/fragments/ai/research_form.html
  - app/templates/fragments/ai/research_result.html
  - app/templates/fragments/ai/research_quota_exhausted.html
  - app/templates/fragments/ai/preference_profile_prose.html
  - app/templates/fragments/ai/trends_card.html
  - app/templates/fragments/ai/coach_brew_picker.html
  - app/templates/fragments/brew/improve_result.html
  - app/static/js/alpine-components/chart-trends.js
  - app/routers/home.py
  - app/routers/brew.py
  - tests/templates/test_ai_page_phase19.py
autonomous: false
requirements: [AIX-06, AIX-09, AIX-10, AIX-12, VIZ-01]
must_haves:
  truths:
    - "D-01/D-03/D-04/D-05/AIX-01/AIX-06: a compact single-column Research card is pinned at the TOP of /ai (above the What-to-buy-next hero) with two free-text fields (coffee_name required + roaster_name optional, no catalog autocomplete per D-01), quota counter, SSE result mount, predicted-rating block (range+confidence+reasoning), cited sources, and an Add-to-wishlist button reusing the existing POST /ai/wishlist/add with prefilled coffee_name/roaster_name/source_url per D-05"
    - "AIX-09/D-10: the Top Flavor Descriptors lazy mount is DELETED from /ai; the Preference Profile card renders AI prose from the preference_profile_prose flow; /home/cards/preference-profile returns the new prose template"
    - "VIZ-01/D-17: a Trends card mounts last on /ai with two Chart.js v4 canvases (rating-over-time line + flavor-distribution bar); Chart.js + htmx-ext-sse load via CDN in base.html with csp_nonce; the chart-trends.js Alpine component re-themes on .dark toggle; canvas sizing uses a CSS class (no inline style attr) with maintainAspectRatio:false"
    - "AIX-10/D-13: every user-triggered AI button uses hx-indicator + htmx-request with spinner styles defined in tailwind.src.css (NOT auto-injected — project memory strict-csp-blocks-htmx-indicator); button disables during the request"
    - "AIX-12/D-12: an improve-brew button sits next to Edit on /brew/{id}/edit and renders the inline improve_result card via SSE; the Coach-a-brew link on /ai opens an inline Alpine x-show session picker"
    - "fragments/research_coming_soon.html include is removed from ai.html"
  artifacts:
    - path: "app/templates/fragments/ai/research_form.html"
      provides: "research input + quota counter + SSE result mount (D-03/D-04/D-09)"
      contains: "sse-connect"
    - path: "app/static/js/alpine-components/chart-trends.js"
      provides: "CSP-clean Chart.js init + dark-mode MutationObserver"
      contains: "chartTrends"
    - path: "app/static/css/tailwind.src.css"
      provides: ".htmx-indicator spinner styles"
      contains: "htmx-indicator"
  key_links:
    - from: "app/templates/base.html"
      to: "Chart.js + htmx-ext-sse CDN"
      via: "script defer nonce"
      pattern: "chart.js@4.5.1|htmx-ext-sse@2.2.4"
    - from: "app/templates/pages/ai.html"
      to: "fragments/ai/research_form.html (top) + trends_card.html (last)"
      via: "include / lazy mount"
      pattern: "research_form|trends_card"
    - from: "app/routers/home.py /home/cards/preference-profile"
      to: "preference_profile_prose.html"
      via: "response shape change"
      pattern: "preference_profile_prose"
---

<objective>
Build the entire Phase 19 UI: load Chart.js v4 + htmx-ext-sse via CDN with nonce, define the CSP-clean .htmx-indicator spinner styles, restructure /ai (Research card pinned top, Top Flavor Descriptors deleted, Preference Profile → prose, Trends card last), build the research result card + quota states + SSE wiring, wire the improve-brew button on the edit page + coach picker, and ship the CSP-clean Chart.js Alpine component.

Purpose: This is where every Phase 19 service/route becomes a usable surface. It is the highest CSP/SW-cache-risk plan (new CDN scripts, inline indicator styles, Chart.js canvas sizing, SSE client) so it ends with a blocking human verification of the live page at 375px + dark mode.
Output: base.html, tailwind.src.css, ai.html, six new ai/brew fragments, chart-trends.js, home.py + brew.py endpoint/template wiring, ai-page template test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-ai-page-research-predict/19-CONTEXT.md
@.planning/phases/19-ai-page-research-predict/19-RESEARCH.md
@app/templates/pages/ai.html
@app/templates/base.html
@app/static/css/tailwind.src.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: base.html CDN scripts + tailwind.src.css indicator + chart-trends.js (VIZ-01/AIX-10/D-13/D-17)</name>
  <read_first>
    - app/templates/base.html:26-75 (the script-loading block: theme bootstrap nonce script, the Alpine component files load BEFORE @alpinejs/csp core at line 65, htmx at line 67, sw register at line 73 — every tag carries nonce="{{ csp_nonce(request) }}". Add Chart.js + htmx-ext-sse + chart-trends.js following this exact pattern and ordering rules)
    - app/static/css/tailwind.src.css (full file — where .htmx-indicator + .chart-canvas classes go; this is the Tailwind v3 source compiled by the CLI, NOT v4 — project memory tailwind-v3-not-v4, never @custom-variant)
    - app/static/js/alpine-components/dark-toggle.js (existing dark component — how .dark is set on <html>; the chart component watches the same class)
    - .planning/phases/19-ai-page-research-predict/19-RESEARCH.md § "Pattern 6" (Chart.js CSP-clean init), § "Pitfall 3" (canvas sizing via CSS class not inline style), § "Pitfall 5" (load order — chart-trends.js registers Alpine.data; Chart.js CDN before @alpinejs/csp core)
    - project memory strict-csp-blocks-htmx-indicator (indicator styles MUST be in tailwind.src.css)
  </read_first>
  <action>
    In base.html add three CDN/component script tags, each with nonce="{{ csp_nonce(request) }}" and defer, placed so chart-trends.js loads with the other Alpine component files (before @alpinejs/csp at line 65) and the Chart.js CDN tag precedes @alpinejs/csp: chart-trends.js (local), https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.min.js, https://cdn.jsdelivr.net/npm/htmx-ext-sse@2.2.4/dist/sse.js (after htmx core). In tailwind.src.css add .htmx-indicator rules (hidden by default; shown via .htmx-request per HTMX 2.x) and a .chart-canvas class (display:block; max-width:100%; width:100%) so no inline style attr is needed on the canvas. Create app/static/js/alpine-components/chart-trends.js per RESEARCH Pattern 6: Alpine.data('chartTrends', (ratingsData, flavorsData) => ({...})) with init() creating both charts (maintainAspectRatio:false, explicit width/height attrs), a MutationObserver on document.documentElement class for dark re-theming (light: espresso-700 on cream-100; dark: cream-200 on espresso-900), and no eval. After CSS edits, rebuild Tailwind (the project's CLI build step) so the new classes compile.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery python -m pytest tests/templates/test_ai_page_phase19.py -k "cdn or indicator" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "chart.js@4.5.1" app/templates/base.html` and `grep -n "htmx-ext-sse@2.2.4" app/templates/base.html`, both with csp_nonce on the same tag
    - Source assertion: `grep -n "htmx-indicator" app/static/css/tailwind.src.css` and `grep -n "chart-canvas" app/static/css/tailwind.src.css`
    - Source assertion: chart-trends.js contains no `eval` and registers Alpine.data('chartTrends', ...)
    - Browser/CSP: no CSP violation in console for script-src or style-src when /ai renders the Trends card (verified in Task 4 human-check)
  </acceptance_criteria>
  <done>Chart.js + htmx-ext-sse load nonce-tagged; indicator + canvas styles live in tailwind.src.css; chart-trends.js is CSP-clean and dark-aware.</done>
</task>

<task type="auto">
  <name>Task 2: /ai restructure + research/prefs/trends fragments + preference-profile endpoint (AIX-01/06/09/10/D-03/D-04/D-10/D-17)</name>
  <read_first>
    - app/templates/pages/ai.html (full file: the above-gate-key-present branch lines 37-136 — Research stub include at line 135 to REPLACE; Top Flavor Descriptors section lines 72-84 to DELETE; Preference Profile mount lines 58-70; stagger delays 100/200/300/500ms; the equipment form at 117-131 shows the CSRF hidden-field + hx-target pattern)
    - app/templates/fragments/ai/paste_rank_results.html (existing ai/ fragment style for the result card)
    - app/routers/home.py (/home/cards/preference-profile handler — change its response to render preference_profile_prose.html from the new flow's latest row)
    - app/routers/ai.py /ai/research + /ai/research/quota + /ai/coach (from 19-05 — the URLs the fragments mount/post to)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-03 (compact card layout), D-04 (pinned top, cached badge), D-09 (quota counter + exhausted state), D-10 (final card stack order: Research → hero → Preference Profile prose → Sweet Spots → AI tools → Trends)
    - .planning/phases/19-ai-page-research-predict/19-RESEARCH.md § "Pattern 2" (htmx-ext-sse client: hx-ext="sse", sse-connect, sse-swap="complete", sse-close)
  </read_first>
  <action>
    Restructure ai.html above-gate-key-present branch to the D-10 card order: include research_form.html at the TOP (before the hero section), keep the hero, change the Preference Profile mount to render the new prose, DELETE the Top Flavor Descriptors section entirely, keep Sweet Spots + AI tools, add the Trends card last (lazy hx-get 700ms stagger). Remove the research_coming_soon.html include. Create research_form.html (two free-text fields coffee_name required + roaster_name optional, CSRF hidden field, quota counter eager-rendered, hx-ext="sse" result mount with sse-swap="complete"/sse-close, hx-indicator spinner span), research_result.html (D-03 compact single-column: title row, metadata row with optional ·cached and "buy URL not verified" chips, tasting-note chips, predicted-rating block range+confidence+"Why:" reasoning, footnote sources, Add-to-wishlist button posting to /ai/wishlist/add with prefilled coffee_name/roaster_name/source_url), research_quota_exhausted.html (friendly inline 429 fragment), preference_profile_prose.html (renders summary_prose, autoescaped, newlines→<br> via |replace never |safe), trends_card.html (wrapper with x-data="chartTrends(...)" and two <canvas class="chart-canvas"> with explicit width/height attrs + a refresh affordance), coach_brew_picker.html (inline Alpine x-show expandable list of last ~20 sessions linking to /brew/{id}/edit with the result auto-requested). Update /home/cards/preference-profile in home.py to return preference_profile_prose.html. Add hx-indicator + disabled-during-request to the existing equipment/paste-rank/refresh buttons (AIX-10 uniformity). Rebuild after template changes.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery python -m pytest tests/templates/test_ai_page_phase19.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/templates/test_ai_page_phase19.py -x -q` green (research card present at top; Top Flavor Descriptors absent; trends card present; research_coming_soon include gone)
    - Source assertion: `grep -L "research_coming_soon" app/templates/pages/ai.html` (string absent)
    - Source assertion: research_result.html renders predicted_low–predicted_high + confidence + reasoning (never a lone number) and never uses |safe
    - Source assertion: wishlist button posts to /ai/wishlist/add with coffee_name/roaster_name/source_url fields (AIX-06)
  </acceptance_criteria>
  <done>/ai shows the correct card stack with the Research card top, prose Preference Profile, deleted descriptors, and Trends last; all new fragments exist; wishlist reuse wired.</done>
</task>

<task type="auto">
  <name>Task 3: Improve-brew button + inline result on the edit page (AIX-12/D-12)</name>
  <read_first>
    - app/routers/brew.py:973-1042 (edit_brew_form — the page + context; locate the existing Edit/Save button row in the rendered template to place the improve-brew button next to it)
    - app/templates/pages/brew_form.html (the edit form template — where the button + inline result card mount)
    - app/routers/ai.py POST /ai/improve-brew/{session_id} + GET /ai/coach (from 19-05)
    - .planning/phases/19-ai-page-research-predict/19-CONTEXT.md D-12 (button next to Edit; inline result card below the session detail; coach link navigates here with hx-trigger="load")
  </read_first>
  <action>
    Create fragments/brew/improve_result.html (renders BrewImproveSchema: summary_prose, unchanged_parameters chips, next_try list of parameter/suggested_value/rationale; autoescaped, no |safe). Add a "Suggest improvements" button on the edit page (brew_form.html in edit mode) next to Edit, wired hx-ext="sse" sse-connect="/ai/improve-brew/{session_id}" with hx-indicator spinner, rendering improve_result.html on event:complete into an inline card below the form. Support the coach-picker entry: when arriving from /ai/coach the result is auto-requested via hx-trigger="load". CSRF on the POST. Rebuild after template changes.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery python -m pytest tests/templates/test_ai_page_phase19.py -k improve -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: brew_form.html (edit mode) contains a "Suggest improvements" affordance targeting /ai/improve-brew/{session_id}
    - Source assertion: improve_result.html renders next_try rationale and never uses |safe
    - `pytest tests/templates/test_ai_page_phase19.py -k improve -x` passes
  </acceptance_criteria>
  <done>The edit page exposes the improve-brew button with an inline SSE result card; the coach picker entry auto-requests it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live 375px + dark-mode + SSE + CSP verification</name>
  <action>Pause for human verification of the live /ai page and brew edit page after rebuild. No code changes in this task — the human executes the steps in how-to-verify and approves or reports issues.</action>
  <what-built>
    The full /ai page (Research card top, prose Preference Profile, deleted Top Flavor Descriptors, Trends card with two Chart.js canvases), the improve-brew button + inline result on the brew edit page, Chart.js + htmx-ext-sse CDN scripts with nonce, and the CSP-clean indicator + canvas styles.
  </what-built>
  <how-to-verify>
    1. Rebuild + restart: `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`. In the browser, do a full "Clear site data" (not just Bypass) before loading — the service worker SWRs / and /static (project memory sw-stale-cache-confounds-ui-verify).
    2. Load /ai at 375px viewport (DevTools device mode). Confirm: Research card pinned at top, no horizontal scroll, quota counter shows "X/20 ... remaining today", both Trends charts fit the card width, no Top Flavor Descriptors card.
    3. Submit a research lookup (coffee name). Confirm prose streams incrementally (not one burst), then the compact result card renders with predicted range + confidence + "Why:" + sources + Add to wishlist. Repeat the SAME lookup — confirm it returns instantly with a ·cached badge and the quota counter does NOT drop.
    4. Toggle dark mode on /ai — confirm both charts re-theme (light: espresso lines on cream; dark: cream lines on espresso) without reload.
    5. Open a brew session's edit page, click Suggest improvements — confirm the inline SSE result card renders unchanged_parameters + next_try suggestions.
    6. Open DevTools console — confirm ZERO CSP violations (script-src or style-src) on /ai and the edit page.
    Note: end-to-end SSE-through-NPM (no buffering) is verified separately in 19-07 once the operator applies the NPM config.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (horizontal scroll, CSP violation, charts not theming, SSE bursting locally, cached badge wrong, quota decrementing on cache hit).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI prose → rendered HTML | XSS via injected prose crosses here |
| CDN scripts → page CSP | third-party script execution boundary |
| client → wishlist add | source_url scheme boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-21 | Tampering/XSS | AI prose rendered in research_result / preference_profile_prose / improve_result | mitigate | Jinja2 autoescape; never |safe; newlines via |replace('\n','<br>') filter only (V5) |
| T-19-22 | Tampering | Chart.js/htmx-ext-sse CDN scripts under strict CSP | mitigate | nonce="{{ csp_nonce(request) }}" on every tag; no eval (Chart.js UMD); canvas sized via CSS class not inline style (RESEARCH Pitfall 3) |
| T-19-23 | Tampering | wishlist source_url from research (javascript:/data:) | mitigate | existing /ai/wishlist/add drops non-https source_url (CR-01, reused verbatim) |
| T-19-24 | Info Disclosure | stale SW cache serving old /ai | mitigate | content-deterministic SW cache bumps on template/CSS/JS change (project memory c9); human-check does full Clear site data |
</threat_model>

<verification>
- `pytest tests/templates/test_ai_page_phase19.py -x -q` green
- `pytest tests/ -x -q` green at wave merge
- `ruff format --check . && ruff check .` clean; Tailwind rebuilt; container rebuilt before the human-check
- Human-check (Task 4) approved: 375px + dark mode + SSE local streaming + zero CSP violations
</verification>

<success_criteria>
The /ai page and brew edit page expose all Phase 19 surfaces, CSP-clean, mobile-first at 375px, with working local SSE streaming and dark-aware charts, confirmed by the human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/19-ai-page-research-predict/19-06-SUMMARY.md` when done.
</output>
