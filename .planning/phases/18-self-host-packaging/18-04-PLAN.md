---
phase: 18-self-host-packaging
plan: 04
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements: [DIST-02]
tags: [ci, release, ghcr, multi-arch, packaging]
must_haves:
  truths:
    - "D-09: Release workflow triggers on `push: tags: ['v*']` only — no workflow_dispatch fallback"
    - "D-10: A `test` job runs the full CI gate (ruff format check, ruff lint, pytest full suite + isolation double-run with SNOB_CI=1) and the `build-push` job has `needs: test` so a failing gate blocks the publish"
    - "D-11: `docker/metadata-action@v6` with `flavor: latest=auto` causes pre-release tags like `v1.2.0-rc1` to publish under the exact tag only — `latest`/`1.2`/`1` are not touched"
    - "D-13: Multi-arch build uses `docker/setup-qemu-action@v4` + `docker/setup-buildx-action@v4` + `docker/build-push-action@v7` with `platforms: linux/amd64,linux/arm64`"
    - "D-12: `build-args: APP_VERSION=${{ github.ref_name }}` passes the git tag into the Dockerfile ARG (wired by Plan 02), stamping it into the runtime ENV and OCI labels"
    - "D-03: The `tags:` matrix publishes `v1.2.0` (raw), `1.2` (major.minor), `1` (major, only on v1+), and `latest` (via flavor=auto) for stable tags only"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered two-job workflow: test gate → multi-arch GHCR publish"
      contains: "linux/amd64,linux/arm64"
  key_links:
    - from: "git tag v1.2.0 push"
      to: "ghcr.io/jheikkila54/coffee-snobbery:v1.2.0 (+ :1.2 :1 :latest)"
      via: ".github/workflows/release.yml test + build-push jobs"
      pattern: "ghcr.io/jheikkila54/coffee-snobbery"
    - from: "build-push job APP_VERSION build-arg"
      to: "Dockerfile ARG APP_VERSION (Plan 02)"
      via: "build-args: APP_VERSION=${{ github.ref_name }}"
      pattern: "APP_VERSION="
    - from: "build-push job docker/login-action"
      to: "ghcr.io"
      via: "GITHUB_TOKEN with `permissions: { packages: write }`"
      pattern: "packages: write"
---

<objective>
Create `.github/workflows/release.yml` as a two-job workflow triggered by `v*` tag pushes. The `test` job is a verbatim copy of `.github/workflows/ci.yml`'s test gate (Tailwind v3.4.17 build, ruff format check, ruff lint, pytest full suite + isolation double-run with SNOB_CI=1) — memory `executors-skip-ruff-ci-gates-both` mandates both ruff steps, memory `tailwind-v3-not-v4` pins the Tailwind binary version, memory `full-suite-test-isolation-gaps` is satisfied by mirroring ci.yml's idempotent double-run pattern. The `build-push` job (with `needs: test`) sets up QEMU + Buildx, logs into GHCR with the workflow's `GITHUB_TOKEN`, runs `docker/metadata-action@v6` with `flavor: latest=auto` and the D-03 tag matrix, then `docker/build-push-action@v7` with multi-arch (`linux/amd64,linux/arm64`), `target: runtime`, GHA cache, and `APP_VERSION=${{ github.ref_name }}` build-arg.

Purpose: Make DIST-02 true. A `git tag v1.2.0 && git push --tags` after this plan ships produces — atomically — a tested, versioned, multi-arch image at `ghcr.io/jheikkila54/coffee-snobbery:v1.2.0` (plus `:1.2`, `:1`, `:latest`), with the git tag stamped into the in-container `APP_VERSION` env var and the `org.opencontainers.image.version` label.

Output: A single committed `.github/workflows/release.yml` file (~120 lines) that mirrors `ci.yml`'s test gate and adds the build-push job verbatim from RESEARCH.md's verified skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-self-host-packaging/18-CONTEXT.md
@.planning/phases/18-self-host-packaging/18-RESEARCH.md
@.planning/phases/18-self-host-packaging/18-PATTERNS.md
@.planning/phases/18-self-host-packaging/18-VALIDATION.md
@.github/workflows/ci.yml
@.planning/phases/18-self-host-packaging/18-02-SUMMARY.md
@Dockerfile
@CLAUDE.md

<interfaces>
<!-- Verified GitHub Action versions (RESEARCH.md § "Release CI Architecture", lines 120-128): -->
<!-- docker/metadata-action     @v6  -->
<!-- docker/build-push-action   @v7  -->
<!-- docker/setup-qemu-action   @v4  -->
<!-- docker/setup-buildx-action @v4  -->
<!-- docker/login-action        @v4  -->
<!-- actions/checkout           @v4  -->
<!-- actions/setup-python       @v5  -->

<!-- Dockerfile ARG (from Plan 02): -->
<!--   ARG APP_VERSION=dev  ; ENV APP_VERSION=${APP_VERSION}  -->
<!-- Build-args: APP_VERSION=${{ github.ref_name }} stamps the git tag end-to-end -->

<!-- ci.yml test gate steps (lines 24-86) MUST be mirrored verbatim, including: -->
<!--   - Tailwind v3.4.17 binary build (memory: tailwind-v3-not-v4) -->
<!--   - Ruff format check AND ruff lint (memory: executors-skip-ruff-ci-gates-both) -->
<!--   - Both pytest runs with SNOB_CI=1 (memory: full-suite-test-isolation-gaps) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/release.yml — test job mirroring ci.yml</name>
  <read_first>
    - .github/workflows/ci.yml (current — full file, lines 1-86; the test gate is copied verbatim)
    - .planning/phases/18-self-host-packaging/18-PATTERNS.md § ".github/workflows/release.yml" (lines 29-161) — exact YAML blocks
    - .planning/phases/18-self-host-packaging/18-RESEARCH.md § "Release CI Architecture" (lines 118-360) — full release.yml skeleton, action-version table, and landmines
    - .planning/phases/18-self-host-packaging/18-CONTEXT.md — D-09, D-10
    - .planning/phases/18-self-host-packaging/18-VALIDATION.md — V18-04 structural validator
  </read_first>
  <action>
    Create `.github/workflows/release.yml` using the Write tool with LF line endings. The file has two jobs (`test` and `build-push`); this task writes the workflow header + the `test` job (Task 2 appends `build-push`).

    Implementation strategy: write the COMPLETE workflow in one Write call (both jobs together) since the two jobs share frontmatter and the file is short (~120 lines total). The "two tasks" boundary is logical, not file-fragment — the second task verifies the build-push portion specifically, but the file is created here in one pass.

    Workflow body:

    ```yaml
    name: Release

    # D-09: Single trigger — push of a tag matching v*. No workflow_dispatch fallback.
    # Re-running a failed release is `git tag -d vX.Y.Z && git push origin :refs/tags/vX.Y.Z
    # && git tag vX.Y.Z && git push --tags`.
    on:
      push:
        tags:
          - 'v*'

    jobs:
      # ---------------------------------------------------------------------------
      # Test gate — verbatim copy of .github/workflows/ci.yml's test job.
      # Memory: executors-skip-ruff-ci-gates-both (both ruff steps run).
      # Memory: tailwind-v3-not-v4 (Tailwind v3.4.17 binary, NOT v4).
      # Memory: full-suite-test-isolation-gaps (double-run with SNOB_CI=1).
      # D-10: build-push is gated on this job via `needs: test`.
      # ---------------------------------------------------------------------------
      test:
        runs-on: ubuntu-latest

        services:
          postgres:
            image: postgres:16-alpine
            env:
              POSTGRES_USER: test
              POSTGRES_PASSWORD: test
              POSTGRES_DB: snobbery
            options: >-
              --health-cmd pg_isready
              --health-interval 5s
              --health-timeout 3s
              --health-retries 10
            ports:
              - 5432:5432

        steps:
          - uses: actions/checkout@v4

          - uses: actions/setup-python@v5
            with:
              python-version: "3.12"
              cache: pip

          - name: Install deps
            run: pip install -r requirements-dev.txt

          # Tailwind v3.4.17 — memory `tailwind-v3-not-v4`. Mirrors Dockerfile stage 1.
          - name: Build Tailwind CSS
            run: |
              set -euo pipefail
              curl -fsSL \
                "https://github.com/tailwindlabs/tailwindcss/releases/download/v3.4.17/tailwindcss-linux-x64" \
                -o /usr/local/bin/tailwindcss
              chmod +x /usr/local/bin/tailwindcss
              HASH="$(sha256sum app/static/css/tailwind.src.css | cut -c1-8)"
              tailwindcss \
                -i app/static/css/tailwind.src.css \
                -o "app/static/css/tailwind.${HASH}.css" \
                --minify

          # Memory `executors-skip-ruff-ci-gates-both` — BOTH steps required.
          - name: Ruff format check
            run: ruff format --check .

          - name: Ruff lint
            run: ruff check .

          - name: Pytest full suite
            env:
              DATABASE_URL: postgresql+psycopg://test:test@localhost:5432/snobbery
              POSTGRES_USER: test
              POSTGRES_PASSWORD: test
              POSTGRES_DB: snobbery
              APP_SECRET_KEY: ${{ secrets.APP_SECRET_KEY || 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx' }}
              APP_ENCRYPTION_KEY: ${{ secrets.APP_ENCRYPTION_KEY || '0123456789abcdef0123456789abcdef0123456789a=' }}
              SNOB_CI: "1"
            run: python -m pytest tests/ -rs --tb=short --ignore=tests/e2e

          # Isolation double-run — memory `full-suite-test-isolation-gaps`.
          # Same DB, no drop/recreate; teardown residue from run 1 surfaces here.
          - name: Pytest isolation double-run
            env:
              DATABASE_URL: postgresql+psycopg://test:test@localhost:5432/snobbery
              POSTGRES_USER: test
              POSTGRES_PASSWORD: test
              POSTGRES_DB: snobbery
              APP_SECRET_KEY: ${{ secrets.APP_SECRET_KEY || 'xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx' }}
              APP_ENCRYPTION_KEY: ${{ secrets.APP_ENCRYPTION_KEY || '0123456789abcdef0123456789abcdef0123456789a=' }}
              SNOB_CI: "1"
            run: python -m pytest tests/ -rs --tb=short --ignore=tests/e2e

      # ---------------------------------------------------------------------------
      # Build + push — multi-arch GHCR publish, gated on `needs: test`.
      # D-13: docker/setup-qemu + setup-buildx + build-push-action.
      # D-11: docker/metadata-action `flavor: latest=auto` handles pre-release filter.
      # D-12: APP_VERSION build-arg consumed by Dockerfile ARG (Plan 02).
      # ---------------------------------------------------------------------------
      build-push:
        needs: test
        runs-on: ubuntu-latest
        permissions:
          contents: read
          packages: write

        steps:
          - uses: actions/checkout@v4

          - name: Set up QEMU
            uses: docker/setup-qemu-action@v4

          - name: Set up Docker Buildx
            uses: docker/setup-buildx-action@v4

          - name: Log in to GHCR
            uses: docker/login-action@v4
            with:
              registry: ghcr.io
              username: ${{ github.actor }}
              password: ${{ secrets.GITHUB_TOKEN }}

          - name: Docker metadata
            id: meta
            uses: docker/metadata-action@v6
            with:
              images: ghcr.io/jheikkila54/coffee-snobbery
              # D-03 tag matrix:
              #   - {{raw}}        : exact verbatim tag (v1.2.0 or v1.2.0-rc1)
              #   - {{major.minor}}: 1.2  (auto-skipped on pre-release per metadata-action docs)
              #   - {{major}}      : 1    (disabled on v0.x to avoid false stable signal)
              # latest is added by flavor=auto on stable tags only (D-11 pre-release filter).
              tags: |
                type=semver,pattern={{raw}}
                type=semver,pattern={{major}}.{{minor}}
                type=semver,pattern={{major}},enable=${{ !startsWith(github.ref, 'refs/tags/v0.') }}
              flavor: |
                latest=auto
              labels: |
                org.opencontainers.image.title=Snobbery
                org.opencontainers.image.description=Self-hosted household coffee log for pour-over enthusiasts
                org.opencontainers.image.licenses=Proprietary

          - name: Build and push
            uses: docker/build-push-action@v7
            with:
              context: .
              # target: runtime is mandatory — without it, the `dev` stage runs pytest
              # as ENTRYPOINT (the operator image would restart-loop on `up -d`).
              target: runtime
              platforms: linux/amd64,linux/arm64
              push: true
              tags: ${{ steps.meta.outputs.tags }}
              labels: ${{ steps.meta.outputs.labels }}
              # D-12: stamp the git tag into the Dockerfile ARG APP_VERSION (Plan 02).
              build-args: |
                APP_VERSION=${{ github.ref_name }}
              # GHA cache backend speeds up reruns ~50% by caching the Tailwind builder
              # stage + pip layer; 10 GB limit per repo, ample for household scale.
              cache-from: type=gha
              cache-to: type=gha,mode=max
    ```

    File system path: `.github/workflows/release.yml`. Use forward slashes. Ensure the `.github/workflows/` directory exists (it does — `ci.yml` is there). LF line endings.

    Landmines (PATTERNS.md §"Landmines" for release.yml):
    - `permissions:` ONLY on the `build-push` job, NOT on `test`. The test job does not need package-write access.
    - `target: runtime` is non-negotiable.
    - Tailwind URL must use `v3.4.17` (memory `tailwind-v3-not-v4`).
    - Both ruff steps must appear (memory `executors-skip-ruff-ci-gates-both`).
    - Both pytest runs must appear (memory `full-suite-test-isolation-gaps`).
    - `flavor: latest=auto` is the D-11 pre-release filter mechanism — do NOT replace with a conditional.
    - The build-arg name `APP_VERSION` must match the Dockerfile ARG name added by Plan 02.

    Validation hook (D-discretion — `tailwind-builder` stage caching): GHA cache covers the Dockerfile stage cache (Tailwind builder + pip layer) via `cache-to: type=gha,mode=max`. No additional per-stage config needed.

    Discretionary deferrals per CONTEXT.md "Claude's Discretion":
    - DO NOT generate a GitHub Release with auto-notes inside this workflow — that's a "nice-to-have if 5-line add" per CONTEXT.md, and `actions/create-release@v1` is deprecated. If wanted later, `gh release create` is the modern path, but it's not load-bearing for DIST-02. Skip for now to keep the workflow lean.
    - DO NOT add a `workflow_dispatch:` trigger — explicitly deferred per CONTEXT.md.
    - DO NOT add GHCR retention automation — documented as a manual periodic task in CONTRIBUTING.md (Plan 05) instead.
  </action>
  <verify>
    <automated>python -c "import yaml; w=yaml.safe_load(open('.github/workflows/release.yml')); jobs=w['jobs']; assert 'test' in jobs, 'test job missing'; assert 'build-push' in jobs, 'build-push job missing'; assert jobs['build-push'].get('needs')=='test' or 'test' in jobs['build-push'].get('needs',[]), 'needs: test missing'; print('V18-04 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .github/workflows/release.yml`
    - V18-04 (structural validator) passes — see Verify command
    - YAML parses cleanly: `python -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0
    - V18-05 platforms: `grep -q 'linux/amd64,linux/arm64' .github/workflows/release.yml`
    - V18-06 permissions: `grep -A2 'permissions:' .github/workflows/release.yml | grep -q 'packages: write'`
    - V18-07 pre-release filter: `grep -q 'latest=auto' .github/workflows/release.yml`
    - V18-16 build-arg: `grep -q 'APP_VERSION=' .github/workflows/release.yml`
    - Tag trigger pattern: `grep -E "^\s*-\s*'v\*'" .github/workflows/release.yml` exits 0
    - Two pytest steps present (full + double-run): `grep -c 'python -m pytest tests/' .github/workflows/release.yml` returns 2
    - Both ruff steps present: `grep -q 'ruff format --check' .github/workflows/release.yml` AND `grep -q 'ruff check' .github/workflows/release.yml`
    - Tailwind v3.4.17 pinned: `grep -q 'v3.4.17/tailwindcss-linux-x64' .github/workflows/release.yml`
    - `target: runtime` in build-push: `grep -E "target:\\s*runtime" .github/workflows/release.yml` exits 0
    - Action versions verified:
      - `grep -q 'docker/metadata-action@v6' .github/workflows/release.yml`
      - `grep -q 'docker/build-push-action@v7' .github/workflows/release.yml`
      - `grep -q 'docker/setup-qemu-action@v4' .github/workflows/release.yml`
      - `grep -q 'docker/setup-buildx-action@v4' .github/workflows/release.yml`
      - `grep -q 'docker/login-action@v4' .github/workflows/release.yml`
    - `permissions:` NOT on the `test` job: `python -c "import yaml; j=yaml.safe_load(open('.github/workflows/release.yml'))['jobs']; assert 'permissions' not in j['test'], 'permissions leaked onto test job'; assert 'permissions' in j['build-push'], 'permissions missing from build-push'; print('perms OK')"` exits 0
    - LF-only line endings: `python -c "b=open('.github/workflows/release.yml','rb').read(); assert b'\r\n' not in b, 'CRLF detected'"` exits 0
  </acceptance_criteria>
  <done>`.github/workflows/release.yml` exists, parses as YAML, has exactly the two jobs `test` and `build-push` with the latter `needs: test`, mirrors ci.yml's gate (verified versions: Tailwind v3.4.17, both ruff steps, both pytest steps with SNOB_CI=1), and the build-push job has multi-arch platforms, latest=auto flavor, APP_VERSION build-arg, and only the `build-push` job carries the `packages: write` permission.</done>
</task>

<task type="auto">
  <name>Task 2: Validate release.yml against the full V18 validator set (no commit required)</name>
  <read_first>
    - .planning/phases/18-self-host-packaging/18-VALIDATION.md § "Validator Table" (V18-04..V18-07, V18-16)
    - .github/workflows/release.yml (just written in Task 1)
  </read_first>
  <action>
    Run the full set of release-workflow validators (V18-04, V18-05, V18-06, V18-07, V18-16) and verify all exit 0. This is a verification-only task to catch any missed memory/landmine before merging — produces no file changes.

    If any validator fails, edit `release.yml` to fix and re-run. Document the fix in the SUMMARY.

    Validator commands (run each, all must exit 0):

    ```bash
    # V18-04 — structural shape
    python -c "
    import yaml
    w = yaml.safe_load(open('.github/workflows/release.yml'))
    jobs = w['jobs']
    assert 'test' in jobs, 'test job missing'
    assert 'build-push' in jobs, 'build-push job missing'
    needs = jobs['build-push'].get('needs', [])
    assert needs == 'test' or 'test' in needs, f'needs: test missing, got {needs}'
    assert 'permissions' in jobs['build-push'], 'build-push needs permissions block'
    assert jobs['build-push']['permissions'].get('packages') == 'write', 'packages: write missing'
    assert 'permissions' not in jobs['test'], 'permissions should not be on test job'
    print('V18-04 OK')
    "

    # V18-05 — multi-arch platforms
    grep -q 'linux/amd64,linux/arm64' .github/workflows/release.yml && echo "V18-05 OK"

    # V18-06 — packages:write permission
    grep -A2 'permissions:' .github/workflows/release.yml | grep -q 'packages: write' && echo "V18-06 OK"

    # V18-07 — pre-release filter (D-11)
    grep -q 'latest=auto' .github/workflows/release.yml && echo "V18-07 OK"

    # V18-16 — APP_VERSION build-arg (D-12 wires to Plan 02)
    grep -q 'APP_VERSION=' .github/workflows/release.yml && echo "V18-16 OK"
    ```

    Additionally run a YAML lint check via `actionlint` if available (free check; do NOT install it on the host if absent):
    ```bash
    command -v actionlint > /dev/null 2>&1 && actionlint .github/workflows/release.yml || echo "actionlint not installed — skip"
    ```

    Cross-check against Plan 02's ARG name: verify the `release.yml` build-arg name matches the Dockerfile ARG:
    ```bash
    DF_ARG=$(grep -oE 'ARG APP_VERSION' Dockerfile | head -1)
    WF_ARG=$(grep -oE 'APP_VERSION=' .github/workflows/release.yml | head -1)
    [ -n "$DF_ARG" ] && [ -n "$WF_ARG" ] && echo "ARG name reconciled across Plan 02 + Plan 04"
    ```

    If `Dockerfile` does not yet contain `ARG APP_VERSION` (Plan 02 not yet executed in the same wave), note this in the SUMMARY as a wave-2-dependency-met check and treat it as acceptance criteria #2 below. Wave assignment ensures Plan 02 lands first.
  </action>
  <verify>
    <automated>python -c "import yaml; w=yaml.safe_load(open('.github/workflows/release.yml')); j=w['jobs']; assert 'test' in j and 'build-push' in j; assert j['build-push'].get('needs')=='test' or 'test' in j['build-push'].get('needs',[]); print('V18-04 OK')" && grep -q 'linux/amd64,linux/arm64' .github/workflows/release.yml && grep -A2 'permissions:' .github/workflows/release.yml | grep -q 'packages: write' && grep -q 'latest=auto' .github/workflows/release.yml && grep -q 'APP_VERSION=' .github/workflows/release.yml && echo "V18-04 + V18-05 + V18-06 + V18-07 + V18-16 all OK"</automated>
  </verify>
  <acceptance_criteria>
    - All five validator commands (V18-04, V18-05, V18-06, V18-07, V18-16) exit 0
    - Dockerfile ARG name reconciliation: `grep -q 'ARG APP_VERSION' Dockerfile` AND `grep -q 'APP_VERSION=' .github/workflows/release.yml` both exit 0
    - No actionlint errors (if available); if not installed, document the skip in SUMMARY
    - V18-04 strict assertions (in Verify command) pass:
      - both jobs present
      - `build-push` has `needs: test`
      - `build-push` has `permissions.packages: write`
      - `test` job does NOT have a `permissions:` block (least-privilege)
  </acceptance_criteria>
  <done>All structural and content validators for `release.yml` pass. The workflow is ready to fire on the first `git tag v1.2.0 && git push --tags`. The post-first-push manual GHCR public-visibility flip (V18-22 equivalent — documented in Plan 05's CONTRIBUTING.md section) is the one remaining human step before DIST-02 is fully exercised end-to-end.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Structural shape (V18-04)
python -c "
import yaml
w = yaml.safe_load(open('.github/workflows/release.yml'))
j = w['jobs']
assert 'test' in j and 'build-push' in j
assert j['build-push'].get('needs') == 'test' or 'test' in j['build-push'].get('needs', [])
assert 'permissions' in j['build-push']
assert j['build-push']['permissions'].get('packages') == 'write'
assert 'permissions' not in j['test']
print('V18-04 OK')
"

# Memory-anchored assertions
grep -q 'v3.4.17/tailwindcss-linux-x64' .github/workflows/release.yml && echo "tailwind-v3 pin OK"
grep -q 'ruff format --check' .github/workflows/release.yml && grep -q 'ruff check' .github/workflows/release.yml && echo "both-ruff-gates OK"
[ "$(grep -c 'python -m pytest tests/' .github/workflows/release.yml)" -eq 2 ] && echo "double-run OK"

# Plan 02 → Plan 04 contract handshake
grep -q 'ARG APP_VERSION' Dockerfile && grep -q 'APP_VERSION=' .github/workflows/release.yml && echo "ARG handshake OK"

# Full validator set
for v in V18-04 V18-05 V18-06 V18-07 V18-16; do echo "checking $v..."; done
```
</verification>

<success_criteria>
- DIST-02 is unblocked: a future `git tag v1.2.0 && git push --tags` will fire the workflow.
- The workflow gate cannot publish a flaky commit — `build-push` is `needs: test`.
- Multi-arch and metadata-action tag matrix are correctly wired.
- The build-arg handshake with Plan 02's Dockerfile ARG is verified before the first real run.
- One remaining human step (documented in Plan 05's CONTRIBUTING.md): set GHCR package to Public after the first push.
</success_criteria>

<output>
After completion, create `.planning/phases/18-self-host-packaging/18-04-SUMMARY.md` per the standard summary template, including:
- The single file created (`.github/workflows/release.yml`)
- All five validator results (V18-04, V18-05, V18-06, V18-07, V18-16) with pass/fail
- Confirmation of the ARG handshake with Plan 02's Dockerfile
- Note that the first `v*` tag push will publish to GHCR as PRIVATE by default; Plan 05's CONTRIBUTING.md documents the one-time public-visibility flip
- Deferrals: no workflow_dispatch (D-09 explicit), no GitHub Release auto-notes (CONTEXT.md discretion deferred), no actions/create-release@v1 (deprecated)
</output>
