---
phase: 18-self-host-packaging
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
autonomous: true
requirements: [DIST-06]
tags: [env-vars, docs, self-host]
must_haves:
  truths:
    - "D-17: `.env.example` documents every required env var with a generation hint inline for the four secrets (POSTGRES_PASSWORD, APP_SECRET_KEY, APP_ENCRYPTION_KEY, DATABASE_URL password slot)"
    - "D-17: `TRUSTED_PROXY_IPS` comment is updated to note `set to * when using Nginx Proxy Manager` so the NPM topology (project memory `snobbery-vps-npm-reverse-proxy`) is reachable from the env file alone"
    - "DIST-06 parity invariant: `.env.example` keys match `app/config.py` `Settings.model_fields` exactly — `tests/test_env_example.py` stays green; no new keys added"
    - "D-17: `POSTGRES_USER` and `POSTGRES_DB` defaults are visible inline so an operator reading only `.env.example` knows the values out of the box"
  artifacts:
    - path: ".env.example"
      provides: "Operator-facing env-var inventory with all 11 keys, generation hints for secrets, and the NPM-aware TRUSTED_PROXY_IPS guidance"
      contains: "TRUSTED_PROXY_IPS"
  key_links:
    - from: ".env.example var keys"
      to: "app/config.py Settings.model_fields"
      via: "tests/test_env_example.py strict-equality assertion"
      pattern: "Settings"
---

<objective>
Audit `.env.example` against `app/config.py` `Settings` per D-17, tightening prose so a new operator can copy it, fill in four secrets, and reach a working deploy without referencing anything else. The current file already documents 11 vars matching `Settings` — this plan is verify-and-polish, NOT add-or-remove. The key surgical change is updating the `TRUSTED_PROXY_IPS` comment so the NPM topology guidance lives in the env file (memory `snobbery-vps-npm-reverse-proxy` is non-negotiable for the NPM walkthrough Plan 05 will write).

Purpose: Make DIST-06 demonstrably true — every required env var has a clear purpose line and (for secrets) an in-place copy-paste generation command.

Output: A polished `.env.example` with the same 11 keys, sharpened prose, and an explicit NPM note on `TRUSTED_PROXY_IPS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-self-host-packaging/18-CONTEXT.md
@.planning/phases/18-self-host-packaging/18-RESEARCH.md
@.planning/phases/18-self-host-packaging/18-PATTERNS.md
@.planning/phases/18-self-host-packaging/18-VALIDATION.md
@.env.example
@app/config.py
@tests/test_env_example.py
@CLAUDE.md

<interfaces>
<!-- The 11 var keys MUST stay exactly in sync with app/config.py Settings.model_fields -->
<!-- (verified in RESEARCH.md § ".env.example Audit Checklist"): -->
<!-- POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, DATABASE_URL,
     APP_SECRET_KEY, APP_ENCRYPTION_KEY,
     TRUSTED_PROXY_IPS, APP_TIMEZONE, BACKUP_RETENTION_DAYS,
     LOG_LEVEL, LOG_FORMAT -->
<!-- DO NOT add APP_VERSION here. Plan 02 adds it as a Dockerfile ARG/ENV, NOT a Settings field. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit .env.example against Settings, sharpen prose, add NPM TRUSTED_PROXY_IPS note</name>
  <read_first>
    - .env.example (current — 39 lines)
    - app/config.py (Settings class — confirm the 11 field names so the parity check is grounded)
    - tests/test_env_example.py (current — confirm the parity assertion is on key-set equality, so prose changes are safe)
    - .planning/phases/18-self-host-packaging/18-PATTERNS.md § ".env.example (config, prose polish)" (lines 425-440)
    - .planning/phases/18-self-host-packaging/18-RESEARCH.md § ".env.example Audit Checklist" (lines 765-792) — the inventory table + D-17 prose-polish checklist
    - .planning/phases/18-self-host-packaging/18-CONTEXT.md — D-17
  </read_first>
  <action>
    Apply targeted Edit operations on `.env.example` — do NOT rewrite the whole file. Each change has a precise anchor.

    1. **Header comment (lines 1-8) — keep verbatim.** The current header already explains the procedure and the parity invariant. No edit.

    2. **`POSTGRES_USER=snobbery` (line 11) — append inline default note.** Edit anchor: the line above (`# --- Postgres ---`). Add one comment line directly above `POSTGRES_USER=snobbery`:
       ```
       # default: snobbery — operator can pick anything for their household
       POSTGRES_USER=snobbery
       ```

    3. **`POSTGRES_DB=snobbery` (line 14) — append inline default note.** Add one comment line directly above:
       ```
       # default: snobbery — must match the database name in DATABASE_URL below
       POSTGRES_DB=snobbery
       ```

    4. **`TRUSTED_PROXY_IPS=127.0.0.1` (currently line 28, with comment line 27) — rewrite the comment block to cover NPM.** Edit anchor: the comment `# comma-separated list of upstream IPs uvicorn will trust for X-Forwarded-* headers` (line 27).

       Replace the two-line block (lines 27-28) with:
       ```
       # Comma-separated list of upstream IPs uvicorn will trust for X-Forwarded-*
       # headers. Defaults:
       #   - NGINX on the same VPS as Docker (host-port-bind topology): 127.0.0.1
       #   - Nginx Proxy Manager (NPM) on the shared docker network: *
       # The NPM container's IP is allocated by Docker and can change; * is the
       # documented setting (project memory: snobbery-vps-npm-reverse-proxy).
       # Setting this wrong breaks Secure cookies — see README "Reverse proxy".
       TRUSTED_PROXY_IPS=127.0.0.1
       ```

    5. **`APP_SECRET_KEY` block (lines 18-20) — keep verbatim.** The existing generation hint (`python -c "import secrets; print(secrets.token_urlsafe(64))"`) is correct. No edit.

    6. **`APP_ENCRYPTION_KEY` block (lines 22-24) — keep verbatim.** The existing two-line comment about comma-separated rotation is correct. No edit.

    7. **`POSTGRES_PASSWORD` generation hint (line 12) — keep verbatim** (`openssl rand -hex 32`).

    8. **`DATABASE_URL` (line 16) — keep verbatim.**

    9. **`APP_TIMEZONE`, `BACKUP_RETENTION_DAYS`, `LOG_LEVEL`, `LOG_FORMAT` blocks (lines 29-38) — keep verbatim.** Existing prose is sufficient.

    **Hard rules:**
    - Do NOT add `APP_VERSION` to this file. It's a Dockerfile ARG/ENV (Plan 02), not a Settings field. The parity test `tests/test_env_example.py` would fail.
    - Do NOT remove or rename any of the 11 existing keys. Plan 02 confirmed the parity test will be exercised after both plans complete.
    - Preserve LF line endings (memory `Windows-CRLF-on-pathlib-write-text`). Use Edit tool, which respects existing line endings.

    Post-edit sanity:
    - Run `ruff format --check .` and `ruff check .` (memory `executors-skip-ruff-ci-gates-both`). `.env.example` is not Python, but the broader project gate should still pass.
    - Run `docker compose exec coffee-snobbery python -m pytest tests/test_env_example.py -x` (V18-02).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/test_env_example.py -x 2>&1 | tail -3 | grep -E "passed|^OK" && grep -q 'set to \*' .env.example || grep -q 'Nginx Proxy Manager (NPM)' .env.example && [ "$(grep -cE '^[A-Z_]+=' .env.example)" -eq 11 ] && echo "V18-02 OK"</automated>
  </verify>
  <acceptance_criteria>
    - V18-02 parity test passes: `docker compose exec coffee-snobbery python -m pytest tests/test_env_example.py -x` exits 0. (If the running image predates this plan, rebuild first.)
    - Exact 11 key=value lines remain: `grep -cE '^[A-Z_]+=' .env.example` returns 11
    - Specific keys all present (each grep exits 0):
      - `grep -q '^POSTGRES_USER=' .env.example`
      - `grep -q '^POSTGRES_PASSWORD=' .env.example`
      - `grep -q '^POSTGRES_DB=' .env.example`
      - `grep -q '^DATABASE_URL=' .env.example`
      - `grep -q '^APP_SECRET_KEY=' .env.example`
      - `grep -q '^APP_ENCRYPTION_KEY=' .env.example`
      - `grep -q '^TRUSTED_PROXY_IPS=' .env.example`
      - `grep -q '^APP_TIMEZONE=' .env.example`
      - `grep -q '^BACKUP_RETENTION_DAYS=' .env.example`
      - `grep -q '^LOG_LEVEL=' .env.example`
      - `grep -q '^LOG_FORMAT=' .env.example`
    - NPM note added: `grep -q 'Nginx Proxy Manager' .env.example` exits 0
    - Default notes added: `grep -q 'default: snobbery' .env.example` exits 0 (two hits expected — POSTGRES_USER + POSTGRES_DB)
    - APP_VERSION NOT added: `grep -c '^APP_VERSION=' .env.example` returns 0
    - Generation hints retained (each grep exits 0):
      - `grep -q 'openssl rand -hex 32' .env.example`
      - `grep -q 'secrets.token_urlsafe(64)' .env.example`
      - `grep -q 'Fernet.generate_key' .env.example`
    - LF-only line endings: `python -c "b=open('.env.example','rb').read(); assert b'\r\n' not in b, 'CRLF detected'"` exits 0
  </acceptance_criteria>
  <done>`.env.example` keeps its 11 keys (parity test green), adds the NPM-aware `TRUSTED_PROXY_IPS` block, and surfaces inline defaults for `POSTGRES_USER`/`POSTGRES_DB`. No new keys, no `APP_VERSION` leak, no broken generation hints.</done>
</task>

</tasks>

<verification>
After the task:

```bash
# V18-02 — parity test (the load-bearing assertion)
docker compose exec coffee-snobbery python -m pytest tests/test_env_example.py -x

# Manual key-count audit
grep -cE '^[A-Z_]+=' .env.example   # expect 11

# Prose checks
grep -q 'Nginx Proxy Manager' .env.example && echo "NPM note OK"
grep -c 'default: snobbery' .env.example  # expect 2 (POSTGRES_USER + POSTGRES_DB)
grep -c 'APP_VERSION' .env.example         # expect 0 (must NOT leak)
```
</verification>

<success_criteria>
- DIST-06 holds: every required env var has a clear purpose line and (for the four secrets) a generation hint readable inline.
- The parity invariant — `.env.example` keys ↔ `Settings.model_fields` — remains intact.
- An operator who reads only `.env.example` knows to set `TRUSTED_PROXY_IPS=*` when running behind NPM.
</success_criteria>

<output>
After completion, create `.planning/phases/18-self-host-packaging/18-03-SUMMARY.md` per the standard summary template, including:
- The five precise edits and their anchors
- Confirmation that the parity test exited 0
- Carry-forward: Plan 05 (README) cross-references the env file's NPM note in the Reverse Proxy section so the two stay aligned
</output>
