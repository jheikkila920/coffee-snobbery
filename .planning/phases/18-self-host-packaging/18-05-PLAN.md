---
phase: 18-self-host-packaging
plan: 05
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - README.md
  - CONTRIBUTING.md
  - CLAUDE.md
autonomous: true
requirements: [DIST-03, DIST-04, DIST-05]
tags: [docs, readme, contributing, self-host, npm]
must_haves:
  truths:
    - "D-14: README leads with operator content — Prerequisites → Quickstart → Environment variables → Reverse proxy (NPM primary, plain NGINX secondary) → Single-worker warning → Upgrade → Restore → Troubleshooting → License. Dev content moves to CONTRIBUTING.md."
    - "D-15: NPM walkthrough is a field-list + gotchas (no screenshots) — Domain Names, Scheme http, Forward Hostname `coffee-snobbery`, Forward Port 8000, Block Common Exploits on, SSL via Let's Encrypt, Advanced tab `/sw.js` passthrough; plus the load-bearing TRUSTED_PROXY_IPS=* + shared docker network gotchas"
    - "D-16: Plain-nginx server-block snippet from the original README is preserved verbatim as a secondary section under the NPM walkthrough"
    - "D-19: Upgrade walkthrough is exactly three lines: edit `image:` tag → `docker compose pull` → `docker compose up -d`; migrations run automatically via entrypoint.sh"
    - "D-20: Troubleshooting carries forward the existing entries (healthcheck failing, cookies dropping, migrations didn't run, pg_dump version mismatch) AND adds a new 'Image pull fails / 403 from ghcr.io' entry"
    - "D-18: DIST-05 fresh-install smoke procedure (docker compose down -v && pull && up -d → /healthz returns ok → / redirects unauthenticated user to /setup) is documented step-by-step in the README Upgrade/Quickstart neighborhood and verifiable via V18-19 + V18-20"
    - "D-07: Operator quickstart uses raw `docker compose` commands only — no `make` prereq on the host; Makefile + make-target reference moves to CONTRIBUTING.md"
    - "D-14: CONTRIBUTING.md is new and carries make-target table, fast-iteration recipe (file-level `docker compose cp` per memory `docker-cp-into-container-nesting`), ruff invocations, conventional commits, release ritual, GHCR public-visibility one-time flip, GHCR retention note"
    - "D-02: GHCR image visibility is public — CONTRIBUTING.md `After First Release` section documents the one-time package-settings flip (private->public) so `docker pull ghcr.io/jheikkila54/coffee-snobbery:vX.Y.Z` works without `docker login`; README troubleshooting cross-links the flip as the resolution for 403 errors"
    - "Three-place single-worker warning system survives — README, entrypoint.sh, app/services/scheduler.py all retain `--workers 1` / `single worker` references; audit grep returns ≥3 (V18-03)"
  artifacts:
    - path: "README.md"
      provides: "Operator-first walkthrough: prereqs → quickstart → env → reverse proxy (NPM + plain) → single-worker → upgrade → restore → troubleshooting → license"
      contains: "ghcr.io/jheikkila54/coffee-snobbery"
    - path: "CONTRIBUTING.md"
      provides: "Dev-facing guide: make targets, fast iteration, ruff, tests, conventional commits, release ritual, GHCR public-visibility one-time flip"
      contains: "docker compose cp"
    - path: "CLAUDE.md"
      provides: "Updated 'Files worth knowing' table with one-line pointers to CONTRIBUTING.md and docker-compose.override.yml.example"
      contains: "CONTRIBUTING.md"
  key_links:
    - from: "README Quickstart"
      to: "ghcr.io/jheikkila54/coffee-snobbery:v1.2.0"
      via: "`docker compose up -d` (no build) using committed compose pin"
      pattern: "docker compose up -d"
    - from: "README NPM walkthrough"
      to: "TRUSTED_PROXY_IPS=* + coffee-snobbery-net shared network"
      via: "operator-facing field list + gotcha block"
      pattern: "TRUSTED_PROXY_IPS=\\*"
    - from: "README Upgrade section"
      to: "docker compose pull"
      via: "explicit three-line procedure"
      pattern: "docker compose pull"
    - from: "CONTRIBUTING.md Releasing section"
      to: "Plan 04's release.yml workflow + GHCR package settings"
      via: "git tag v1.2.0 && git push --tags ritual + one-time visibility flip"
      pattern: "git push --tags"
---

<objective>
Restructure `README.md` per D-14 to lead with operator content, carve dev content into a new `CONTRIBUTING.md` per D-14, add a minimal pointer in `CLAUDE.md` so future agents can find the split. The README rewrite preserves three load-bearing blocks verbatim (single-worker warning per memory three-place-system, plain-nginx server snippet per D-16, restore-from-backup procedure) and adds three new sections (NPM walkthrough per D-15, Upgrade per D-19, GHCR 403 troubleshooting entry per D-20). The README also documents the DIST-05 fresh-install smoke procedure (D-18, V18-19, V18-20) so the operator can self-verify on first deploy.

Purpose: Make DIST-03 (complete from-zero walkthrough), DIST-04 (NPM step-by-step), and DIST-05 (verify-only fresh-install boot path documented) all true in a single coordinated docs pass.

Output: Rewritten `README.md`, new `CONTRIBUTING.md`, one-line `CLAUDE.md` update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-self-host-packaging/18-CONTEXT.md
@.planning/phases/18-self-host-packaging/18-RESEARCH.md
@.planning/phases/18-self-host-packaging/18-PATTERNS.md
@.planning/phases/18-self-host-packaging/18-VALIDATION.md
@.planning/phases/18-self-host-packaging/18-01-SUMMARY.md
@README.md
@CLAUDE.md
@Makefile
@entrypoint.sh
@app/services/scheduler.py
@.env.example
@docker-compose.yml

<interfaces>
<!-- README content to PRESERVE verbatim (do NOT rewrite): -->
<!-- 1. Single-worker warning block (current lines 63-84) — location #3 of three-place system. -->
<!--    Audit grep must return ≥3 hits after rewrite. -->
<!-- 2. Plain NGINX server-block snippet (current lines 86-135). -->
<!-- 3. Restore-from-backup procedure (current lines 209-225). -->
<!-- 4. iOS Wake Lock caveat (current lines 239-243). -->

<!-- README content to MOVE to CONTRIBUTING.md: -->
<!-- 1. "Working with the code" make-target table (current lines 41-59). -->
<!-- 2. "Deploying a change" dev/VPS flow (current lines 139-150). -->
<!-- 3. One-time root-owned volumes fix (current lines 152-159) — operational, dev-context. -->

<!-- README content to ADD: -->
<!-- 1. NPM walkthrough field-list + gotchas (D-15) — see RESEARCH.md § "NPM Walkthrough Reference" -->
<!--    Tables: Details tab + SSL tab + Advanced tab passthrough snippet. -->
<!--    Gotchas: TRUSTED_PROXY_IPS=*, shared docker network, X-Forwarded-Proto. -->
<!-- 2. Upgrade section (D-19) — three lines: edit tag, pull, up -d. -->
<!-- 3. Troubleshooting GHCR 403 entry (D-20). -->
<!-- 4. DIST-05 fresh-install smoke procedure (D-18) — documented in Quickstart "verify it worked". -->

<!-- CONTRIBUTING.md new file — outline from RESEARCH.md lines 667-761: -->
<!--   Development Prerequisites -->
<!--   Local Dev Loop (make table) -->
<!--   Fast Per-File Iteration (docker compose cp — FILE LEVEL per memory docker-cp-into-container-nesting) -->
<!--   Linting and Formatting (both ruff steps) -->
<!--   Running the Test Suite (docker compose run --rm coffee-snobbery-test gate) -->
<!--   Committing (conventional commits) -->
<!--   Deploying a Change (dev → VPS flow) -->
<!--   Releasing (git tag v1.2.0 && git push --tags → release.yml fires) -->
<!--   After First Release (one-time GHCR Public visibility flip — RESEARCH.md § "GHCR Public Visibility Flow") -->
<!--   GHCR Package Maintenance (manual untagged cleanup) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md operator-first (Prerequisites → Quickstart → Env vars → Reverse proxy → Single-worker → Upgrade → Restore → Troubleshooting → License)</name>
  <read_first>
    - README.md (current — full file, 251 lines)
    - .planning/phases/18-self-host-packaging/18-PATTERNS.md § "README.md (docs, restructure)" (lines 443-530)
    - .planning/phases/18-self-host-packaging/18-RESEARCH.md § "README Operator-First Rewrite" (lines 517-577) AND § "NPM Walkthrough Reference" (lines 579-661) AND § "DIST-05 Fresh-Install Smoke Procedure" (lines 795-845)
    - .planning/phases/18-self-host-packaging/18-CONTEXT.md — D-04, D-08, D-14, D-15, D-16, D-18, D-19, D-20
    - .env.example (just polished in Plan 03 — verify TRUSTED_PROXY_IPS prose alignment with README NPM section)
    - entrypoint.sh AND app/services/scheduler.py (top-of-file comment blocks — required for three-place audit grep validation)
    - docker-compose.yml (just rewritten in Plan 01 — confirm image pin to v1.2.0 is the version cited in the README quickstart)
  </read_first>
  <action>
    Restructure `README.md` via a full rewrite (use the Write tool with LF line endings since this is a major rewrite — the file structure changes substantially enough that Edit-style patches would be more error-prone than a clean replacement). PRESERVE the three load-bearing blocks verbatim by lifting their exact text from the current file:

    1. **Single-worker warning block (current lines 63-84)** — entire block, verbatim.
    2. **Plain NGINX server-block snippet (current lines 86-135)** — entire block, verbatim.
    3. **Restore-from-backup section (current lines 209-225)** — entire block, verbatim.
    4. **iOS Wake Lock caveat (current lines 239-243)** — entire block, verbatim.

    New section order (D-14):

    ```markdown
    # Snobbery

    Self-hosted household coffee log — FastAPI + PostgreSQL 16 + HTMX, deployed via Docker Compose.

    ## What this is

    Snobbery is a self-hosted household coffee log for pour-over enthusiasts who care about beans, grind, water, and ratio. Multiple users share a household catalog (coffees, equipment, recipes, roasters, flavor notes) but keep separate brew session logs and AI-driven recommendations. Designed for phone-in-hand use at the kettle.

    ## Stack

    [Preserve the current Stack list — Python 3.12 + FastAPI, Postgres 16, SQLAlchemy 2.0 + Alembic + psycopg 3, Jinja2 + HTMX 2.x + Tailwind v3.4.17 + Alpine.js, argon2-cffi + Fernet, structlog, APScheduler, Docker Compose two-service. Update the "(standalone CLI v3.4.17)" line if needed.]

    ## Prerequisites

    - Docker and Docker Compose v2 on the host. Nothing else.
    - The published image bakes Python 3.12, the Tailwind v3.4.17 CLI, and `postgresql-client-16` from PGDG — no Python or Node required on the host.
    - A domain name pointing to the VPS (for HTTPS via your reverse proxy).

    ## Quickstart

    ```bash
    # 1. Get the compose file + env template
    git clone https://github.com/jheikkila54/coffee-snobbery.git
    cd coffee-snobbery

    # 2. Configure secrets (each .env.example line has a generation hint)
    cp .env.example .env
    # POSTGRES_PASSWORD     openssl rand -hex 32
    # APP_SECRET_KEY        python -c "import secrets; print(secrets.token_urlsafe(64))"
    # APP_ENCRYPTION_KEY    python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
    # DATABASE_URL          patch the POSTGRES_PASSWORD value into the URL
    # TRUSTED_PROXY_IPS     set to * if running behind Nginx Proxy Manager (see Reverse proxy below)
    ${EDITOR:-vi} .env

    # 3. Pull and start
    docker compose up -d
    docker compose logs -f coffee-snobbery
    # Expect: [alembic] Running upgrade ... -> 0001_initial, ... then
    #         INFO uvicorn.main Application startup complete.

    # 4. Verify the boot
    curl -fsS http://127.0.0.1:8080/healthz
    # Expect: {"status":"ok"}

    # 5. Visit https://<your-domain> (configured in your reverse proxy below) —
    #    on a fresh install with zero users you land on /setup to create the first admin.
    ```

    ## Environment variables

    [Reproduce the existing 11-row table from current README lines 189-207 verbatim. Update only the `TRUSTED_PROXY_IPS` row to note "Set to `*` when behind Nginx Proxy Manager — see Reverse proxy." This matches the .env.example comment Plan 03 added.]

    For full inline generation hints and per-var notes see `.env.example`. The 4-step procedure for adding a new var is in `CONTRIBUTING.md`.

    ## Reverse proxy

    ### Nginx Proxy Manager (recommended)

    NPM (`jc21/nginx-proxy-manager`) is the recommended reverse proxy for self-hosted Snobbery — it ships with the Let's Encrypt integration and reaches the Snobbery container by name on a shared Docker network.

    **One-time: connect NPM to the Snobbery network.**

    Both containers must share a Docker network so NPM can resolve `coffee-snobbery:8000` by name. From the host that runs NPM:

    ```bash
    docker network connect coffee-snobbery-net <npm-container-name>
    # Confirm: docker network inspect coffee-snobbery-net | grep -A2 Containers
    # You should see both the NPM container and coffee-snobbery in the Containers list.
    ```

    If NPM lives on a different host you'll need the secondary plain-NGINX path below — NPM cannot reach the container by name across hosts.

    **NPM Proxy Host fields:**

    *Details tab:*

    | Field | Value | Notes |
    |-------|-------|-------|
    | Domain Names | `snobbery.example.com` | Your FQDN; A record must point to the VPS |
    | Scheme | `http` | Snobbery listens on plain HTTP inside Docker; NPM terminates TLS |
    | Forward Hostname / IP | `coffee-snobbery` | Container name on the shared `coffee-snobbery-net` network |
    | Forward Port | `8000` | The in-container uvicorn port (NOT 8080) |
    | Cache Assets | Off | Snobbery sets its own cache headers; leave NPM caching off |
    | Block Common Exploits | On | Recommended |
    | Websockets Support | Off | Not needed in v1.2 (SSE is planned for a later phase) |

    *SSL tab:*

    | Field | Value |
    |-------|-------|
    | SSL Certificate | Let's Encrypt — Request new certificate |
    | Force SSL | On |
    | HTTP/2 Support | On |
    | HSTS Enabled | On |
    | HSTS Subdomains | Off (unless you control subdomains) |

    *Advanced tab — Custom Nginx Configuration:* paste this `/sw.js` location block to make the service worker survive the proxy without the wrong cache header (PWA-7 invariant — the app already sends `Cache-Control: no-cache` on `/sw.js`; do NOT add a cache override):

    ```nginx
    location = /sw.js {
        proxy_pass http://coffee-snobbery:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        add_header Service-Worker-Allowed "/" always;
        # Do NOT add Cache-Control here — the app already sends no-cache (PWA-7).
    }
    ```

    **Load-bearing gotchas (NPM topology):**

    1. **`TRUSTED_PROXY_IPS=*` is non-negotiable** behind NPM. Set this in `.env` — uvicorn trusts X-Forwarded-* headers only from the listed upstream IPs, and NPM's container IP is allocated by Docker and can change. Without `*`, `Secure` session cookies silently break on login.
    2. **Shared docker network is mandatory.** See the `docker network connect` step above.
    3. **`X-Forwarded-Proto: https`** is sent automatically by NPM when SSL is active. Combined with `TRUSTED_PROXY_IPS=*`, uvicorn rewrites `request.url.scheme` to `https` so the `Secure` cookie flag works.
    4. **Port mapping is optional under NPM.** The `127.0.0.1:8080:8000` line in `docker-compose.yml` exists for the plain-NGINX fallback below; NPM operators can comment it out for a cleaner posture, but leaving it is harmless.

    ### Plain NGINX (secondary)

    If you run NGINX directly on the host instead of NPM, the compose stack binds to `127.0.0.1:8080` and NGINX proxies to that. Canonical server block (SEC-04):

    [PRESERVE VERBATIM the current README lines 86-135 — the `server { ... }` block plus the surrounding comment about HSTS, /sw.js cache, proxy_buffering off, and the four proxy_set_header lines.]

    The four `proxy_set_header` lines feed uvicorn's `--proxy-headers --forwarded-allow-ips=$TRUSTED_PROXY_IPS` so `request.url.scheme` is rewritten to `https` and `request.client.host` is rewritten to the real upstream IP. Without `proxy_set_header X-Forwarded-Proto $scheme`, cookies marked `Secure` silently break. With plain NGINX on the same VPS, `TRUSTED_PROXY_IPS=127.0.0.1` is the default and is correct.

    ### Operational smoke check

    After deploying — or after any reverse-proxy config change — run:

    ```bash
    curl -i https://snobbery.example.com/debug/proxy
    ```

    A correctly configured stack returns JSON with `"scheme": "https"` and `"headers_honored": true`. If `headers_honored` is `false`, verify the reverse proxy is sending `X-Forwarded-Proto $scheme` AND verify `TRUSTED_PROXY_IPS` in `.env` matches the upstream IP (or `*` for NPM). Restart the web container after changing `TRUSTED_PROXY_IPS`: `docker compose up -d coffee-snobbery`.

    ## Single uvicorn worker — DO NOT change

    [PRESERVE VERBATIM the current README lines 65-84 single-worker warning block. The grep `grep -RIn -E '\-\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py` must return ≥3 hits after this rewrite — location #3 of the three-place system lives here.]

    ## Upgrade

    Snobbery releases publish to GHCR as multi-arch images (amd64 + arm64). Upgrade by bumping the pinned tag in `docker-compose.yml`:

    ```bash
    # 1. Edit docker-compose.yml — update the image: line to the new semver
    #    image: ghcr.io/jheikkila54/coffee-snobbery:v1.3.0
    docker compose pull
    docker compose up -d
    # Migrations run automatically on container start via entrypoint.sh.
    docker compose logs -f coffee-snobbery
    # Confirm: alembic upgrade completes, then `Application startup complete.`
    ```

    The four published tag forms (per release) — pick the float you want:

    - `:v1.2.0` — exact, immutable. Recommended default.
    - `:1.2` — mutable, follows the latest patch in the 1.2 line.
    - `:1` — mutable, follows the latest minor in the 1.x line.
    - `:latest` — mutable, latest stable. Pre-releases (`v*-rc1`, `v*-beta1`) do NOT bump `:latest`.

    ## Restore from backup

    [PRESERVE VERBATIM the current README lines 209-225, including the warning about not restoring by copying raw Postgres data files.]

    ## Troubleshooting

    [PRESERVE VERBATIM the current 4 troubleshooting entries — healthcheck failing, cookies dropping, migrations didn't run, pg_dump version mismatch — current README lines 227-235.]

    **Image pull fails / 403 from ghcr.io.** Snobbery images are public — `docker pull ghcr.io/jheikkila54/coffee-snobbery:v1.2.0` should work without authentication. If you see a 403:
    1. Verify the image is public: visit `https://github.com/jheikkila54/coffee-snobbery/pkgs/container/coffee-snobbery`.
    2. If the image was just published, wait 1–2 minutes for GHCR's CDN to propagate.
    3. If you have stale credentials in Docker's credential store, clear them: `docker logout ghcr.io && docker pull ghcr.io/jheikkila54/coffee-snobbery:v1.2.0`.
    4. If the image is private (one-time setup after the first release tag), the maintainer must flip visibility — see `CONTRIBUTING.md` § "After first release".

    ## Known caveats

    ### iOS Wake Lock — silent-audio fallback

    [PRESERVE VERBATIM the current README lines 239-243 iOS Wake Lock caveat.]

    ## Project history

    This project was bootstrapped by **GSD** (an agentic planner + builder) from the spec in `docs/snobbery-gsd-prompt.md`. That file is the historical product brief — useful reference for the original intent, but **the code is the source of truth** wherever they conflict. Operational conventions, stack invariants, and the never-do-silently list live in `CLAUDE.md`. Developer setup (Makefile, fast iteration, tests, releases) lives in `CONTRIBUTING.md`.

    ## License

    Private. Household use.
    ```

    **Hard rules:**
    - Three-place audit MUST still return ≥3 hits after rewrite — `grep -RIn -E '\-\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py | wc -l` ≥3. The README section header `## Single uvicorn worker — DO NOT change` counts only if the surrounding text retains `--workers 1` or `single worker` strings (the verbatim block does).
    - LF line endings only.
    - All four PRESERVED blocks must contain the original text byte-for-byte where reasonable (minor leading-comment polish is fine, but the meat — the bash commands, the NGINX server-block, the warning rationale — stays).
    - Do NOT reference the Makefile in the operator quickstart (D-07).
    - Do NOT include `make up` anywhere in the README operator content (D-07 moves Makefile to CONTRIBUTING.md).
  </action>
  <verify>
    <automated>for h in 'Prerequisites' 'Quickstart' 'Environment variables' 'Reverse proxy' 'Upgrade' 'Restore from backup' 'Troubleshooting' 'License'; do grep -q "## $h" README.md || { echo "MISSING: $h"; exit 1; }; done && grep -q 'ghcr.io/jheikkila54/coffee-snobbery' README.md && grep -q 'TRUSTED_PROXY_IPS=\*' README.md && grep -q 'docker compose pull' README.md && grep -q 'docker compose up -d' README.md && (grep -q '403 from ghcr' README.md || grep -q 'Image pull fails' README.md) && [ "$(grep -RIn -E '\-\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py | wc -l)" -ge 3 ] && echo "V18-03 + V18-08 + V18-09 + V18-10 + V18-11 + V18-12 all OK"</automated>
  </verify>
  <acceptance_criteria>
    - V18-08 required headers — all 8 grep checks pass: Prerequisites, Quickstart, Environment variables, Reverse proxy, Upgrade, Restore from backup, Troubleshooting, License (each `grep -q "## $h" README.md` exits 0)
    - V18-09 image pin reference: `grep -q 'ghcr.io/jheikkila54/coffee-snobbery' README.md`
    - V18-10 NPM `TRUSTED_PROXY_IPS=*` callout: `grep -q 'TRUSTED_PROXY_IPS=\*' README.md`
    - V18-11 upgrade two lines: `grep -q 'docker compose pull' README.md` AND `grep -q 'docker compose up -d' README.md`
    - V18-12 GHCR troubleshooting entry: `grep -q '403 from ghcr.io' README.md` OR `grep -q 'Image pull fails' README.md`
    - V18-03 three-place single-worker audit: `[ "$(grep -RIn -E '\-\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py | wc -l)" -ge 3 ]`
    - PRESERVED blocks intact (each grep exits 0):
      - `grep -q 'add_header Strict-Transport-Security' README.md` (HSTS line from NGINX block)
      - `grep -q 'pg_dump version mismatch' README.md` OR `grep -q 'postgresql-client-15' README.md` (pg_dump troubleshooting)
      - `grep -q 'NoSleep.js' README.md` OR `grep -q 'Wake Lock' README.md` (iOS caveat)
      - `grep -q 'docker compose exec -T coffee-snobbery-db psql' README.md` (restore procedure)
    - D-07 enforced (no `make up` in operator quickstart): the line containing `## Quickstart` does NOT precede a `make ` reference within the same section. Audit: `awk '/^## /{section=$0} /make /{print section": "$0}' README.md | grep -v '^##\(.*\)CONTRIBUTING' | grep -c 'make '` returns 0 (Make references are gone from README operator content). NOTE: it's OK if "Makefile" appears in the Project history section as a pointer to CONTRIBUTING.md.
    - NPM section presence: `grep -q 'docker network connect coffee-snobbery-net' README.md` (the shared-network step)
    - NPM Advanced tab snippet: `grep -q 'proxy_pass http://coffee-snobbery:8000' README.md` (the /sw.js block)
    - DIST-05 smoke documented: `grep -q '"status":"ok"' README.md` AND `grep -q '/setup' README.md` (both verify steps named)
    - Quickstart references the new compose pin: `grep -q 'docker compose up -d' README.md` AND `grep -q 'docker compose logs' README.md`
    - LF-only line endings: `python -c "b=open('README.md','rb').read(); assert b'\r\n' not in b, 'CRLF detected'"` exits 0
  </acceptance_criteria>
  <done>README.md is rewritten operator-first; all 8 required H2 headers are present; PRESERVED blocks (single-worker, plain-nginx, restore, Wake Lock) are intact; the new NPM walkthrough + Upgrade + GHCR-403 sections are added; the three-place single-worker audit still returns ≥3 hits; no `make` references appear inside operator sections.</done>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md — dev-facing carve-out (make targets, fast iteration, ruff, tests, release ritual, GHCR first-time public flip)</name>
  <read_first>
    - README.md (the just-rewritten version — to verify the carve-out content was actually moved, not duplicated)
    - Makefile (current — for accurate make-target references; lines 36-98)
    - .planning/phases/18-self-host-packaging/18-PATTERNS.md § "CONTRIBUTING.md (docs, new file)" (lines 534-563)
    - .planning/phases/18-self-host-packaging/18-RESEARCH.md § "CONTRIBUTING.md Outline" (lines 667-761) AND § "GHCR Public Visibility Flow" (lines 850-870)
    - .planning/phases/18-self-host-packaging/18-CONTEXT.md — D-07, D-14
  </read_first>
  <action>
    Create `CONTRIBUTING.md` at the repo root with the Write tool, LF line endings. Structure per RESEARCH.md outline. Body:

    ```markdown
    # Contributing to Snobbery

    Operator docs live in `README.md`. This file is for working on Snobbery itself — local dev loop, tests, lint, release ritual.

    ## Development Prerequisites

    - Docker + Docker Compose v2
    - Git
    - `cp docker-compose.override.yml.example docker-compose.override.yml`

    Compose auto-merges `docker-compose.override.yml` (gitignored). With the override in place, `docker compose up -d` builds the image locally instead of pulling from GHCR.

    ## Local Dev Loop

    The Makefile is convenience over `docker compose` — raw commands work identically.

    | Command | What it does |
    |---------|-------------|
    | `make up` | `docker compose up -d` — start the two-service stack |
    | `make down` | `docker compose down` — stop, keep volumes |
    | `make logs` | Tail the web container logs |
    | `make logs-db` | Tail the Postgres container logs |
    | `make psql` | Open a `psql` shell inside the db container |
    | `make migrate` | Run `alembic upgrade head` (migrations also run on container start) |
    | `make revision MSG="add foo column"` | Generate a new alembic autogenerate revision |
    | `make test` | Run `pytest -x` inside the web container |
    | `make smoke` | Cold-start gate: `down -v && up -d --build && /healthz` |
    | `make shell` | Open a `bash` shell inside the web container |
    | `make build` | Rebuild the web image (`docker compose build coffee-snobbery`) |
    | `make fmt` | Run `ruff format .` inside the web container |
    | `make lint` | Run `ruff check .` inside the web container |

    ## Fast Per-File Iteration

    The container has no source bind-mount — code changes don't reach the running container until you either rebuild or copy them in. For one-file Python edits during a test loop:

    ```bash
    # CORRECT — file-level copy (no directory-nesting trap).
    docker compose cp app/routers/foo.py coffee-snobbery:/app/app/routers/foo.py
    docker compose exec coffee-snobbery python -m pytest tests/test_foo.py -x

    # WRONG — directory-level copy nests (creates /app/app/routers/app/routers/foo.py).
    # docker compose cp app/ coffee-snobbery:/app/app/   # DO NOT DO THIS
    ```

    Template and static-file edits ALSO require either a rebuild or a `docker compose cp` — Jinja caches templates in-process and the image bakes the static directory.

    For a full rebuild after broader changes: `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`.

    ## Linting and Formatting

    CI gates on **both** ruff steps. Run both before pushing:

    ```bash
    ruff format --check .
    ruff check .
    ```

    Or fix in place:

    ```bash
    ruff format .
    ruff check --fix .
    ```

    Inside the container (matches CI): `make fmt && make lint`.

    ## Running the Test Suite

    Quick targeted run during dev:

    ```bash
    docker compose exec coffee-snobbery python -m pytest tests/test_foo.py -x
    ```

    Full gate (matches CI — requires the dev override active so `coffee-snobbery-test` exists):

    ```bash
    docker compose build coffee-snobbery-test
    docker compose run --rm coffee-snobbery-test
    # Or: make test (with --build alternative — see Makefile)
    ```

    The test gate runs the baked tree, not your source directory. Rebuild `coffee-snobbery-test` before each run or use `docker compose cp` to push individual files into the live container.

    ## Committing

    Use [Conventional Commits](https://www.conventionalcommits.org/):

    - `feat:`, `fix:`, `chore:`, `refactor:`, `docs:`, `style:`
    - Short, imperative, present tense ("add backup retention config")

    Branches: small changes go straight to `main`; anything touching schema, auth, encryption, AI scheduling, or deployment topology goes on a feature branch with a PR.

    ## Deploying a Change to the VPS

    From the repo root on the VPS:

    ```bash
    git pull
    docker compose build coffee-snobbery
    docker compose up -d coffee-snobbery
    docker compose logs -f coffee-snobbery
    # Confirm: alembic upgrade completes, then `Application startup complete.`
    ```

    Note: this is the **dev → VPS direct-deploy** path. The **release** path goes through CI and a tag push (see below) and produces a versioned multi-arch image on GHCR that any operator can pull.

    ### One-time: fix root-owned volumes on an existing VPS deployment (G-01)

    Volumes created before the Phase 15 entrypoint fix are owned by `root`, which blocks the `app` user (UID 1000) from writing backups and photo uploads. Fresh deploys do NOT need this step — the Phase 15 entrypoint runs `chown -R app:app /app/data` at startup as root, then drops privileges via `gosu`.

    If you deployed before Phase 15 and backups or photo uploads fail with permission errors:

    ```bash
    docker compose run --rm -u root coffee-snobbery chown -R app:app /app/data
    ```

    ## Releasing

    The release ritual is a single tag push:

    ```bash
    git tag v1.2.0
    git push --tags
    ```

    This fires `.github/workflows/release.yml`:

    1. `test` job — verbatim CI gate (Tailwind v3.4.17 build → `ruff format --check .` → `ruff check .` → `pytest tests/ -rs --ignore=tests/e2e` → isolation double-run).
    2. `build-push` job (gated on `needs: test`) — QEMU + Buildx, multi-arch (`linux/amd64,linux/arm64`), pushes to `ghcr.io/jheikkila54/coffee-snobbery` with the following tag matrix:
       - `:v1.2.0` (exact)
       - `:1.2` (mutable major.minor — stable tags only)
       - `:1` (mutable major — stable tags only, disabled on v0.x)
       - `:latest` (mutable — stable tags only, pre-releases skip this via `flavor: latest=auto`)

    Pre-release tags (`v1.2.0-rc1`, `v1.2.0-beta1`) publish under the exact tag only — they do NOT bump `:latest`, `:1.2`, or `:1`, so production operators floating on those tags are safe.

    The image is stamped with the git tag via `APP_VERSION=${{ github.ref_name }}` build-arg → `Dockerfile ARG APP_VERSION` → `ENV APP_VERSION=` → `app/routers/admin/system.py` displays it on `/admin`. `org.opencontainers.image.version` label also carries the tag.

    ### Re-running a failed release

    The workflow may fail mid-push (e.g., one arch built, the other failed). To re-run, delete and re-push the tag:

    ```bash
    git tag -d v1.2.0
    git push origin :refs/tags/v1.2.0
    git tag v1.2.0
    git push --tags
    ```

    GHCR overwrites matching tags on push — no manual cleanup required.

    ## After First Release — One-Time GHCR Public Visibility Flip

    GHCR publishes packages as **private by default**. After the first `v*` tag push, the package will exist at `https://github.com/users/jheikkila54/packages/container/coffee-snobbery` but `docker pull` will fail with 403 without `docker login ghcr.io`.

    To make the image public (DIST-02 requires it):

    1. Visit `https://github.com/users/jheikkila54/packages/container/coffee-snobbery/settings`.
    2. Scroll to "Danger Zone" → "Change visibility" → select **Public** → confirm.
    3. Verify: `docker pull ghcr.io/jheikkila54/coffee-snobbery:v1.2.0` (without `docker login`).

    This is a **one-way** operation — public → private is not supported by GitHub once any download has occurred. Subsequent pushes to the same package keep the public visibility.

    ## GHCR Package Maintenance

    Untagged image versions accumulate over time (each release creates and orphans the previous untagged platform manifests). Clean up periodically via the GitHub UI:

    > GitHub → Profile → Packages → coffee-snobbery → Manage versions → delete untagged

    Automation via `actions/delete-package-versions` is possible but deferred — manual periodic cleanup is sufficient at household release cadence.

    ## Adding a New Env Var

    Four steps:

    1. Add the key to `.env.example` with a generation hint comment.
    2. Add the key to the `environment:` section of `docker-compose.yml` (and the override example if dev-only).
    3. Add the field to the `Settings` class in `app/config.py` (pydantic-settings) — never read `os.environ` directly elsewhere.
    4. Document in `README.md` → "Environment variables" table if operator-facing.

    `tests/test_env_example.py` enforces step 1 ↔ step 3 parity — it will fail if you miss either side.

    ## Files Worth Knowing

    See `CLAUDE.md` § "Files worth knowing" for the per-file role table.
    ```

    Landmines:
    - Fast Per-File Iteration MUST include the file-level vs directory-level caveat (memory `docker-cp-into-container-nesting`).
    - GHCR public-visibility flip MUST be present — without it, DIST-02's "operator can pull without auth" promise is broken.
    - Tailwind version reference MUST be v3.4.17 (memory `tailwind-v3-not-v4`).
    - LF line endings only.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -q 'make smoke' CONTRIBUTING.md && grep -q 'ruff' CONTRIBUTING.md && grep -q 'docker compose cp' CONTRIBUTING.md && grep -q 'git push --tags' CONTRIBUTING.md && grep -q 'Change visibility' CONTRIBUTING.md && echo "V18-13 OK"</automated>
  </verify>
  <acceptance_criteria>
    - V18-13 file exists + key content: `test -f CONTRIBUTING.md && grep -q 'make smoke' CONTRIBUTING.md && grep -q 'ruff' CONTRIBUTING.md && grep -q 'docker compose cp' CONTRIBUTING.md` all true
    - Conventional Commits referenced: `grep -q 'Conventional Commits' CONTRIBUTING.md`
    - Release ritual present: `grep -q 'git push --tags' CONTRIBUTING.md` AND `grep -q 'v1.2.0' CONTRIBUTING.md`
    - GHCR public visibility flip present: `grep -q 'Change visibility' CONTRIBUTING.md` AND `grep -q 'Public' CONTRIBUTING.md`
    - File-level cp caveat present (memory `docker-cp-into-container-nesting`): `grep -q 'DO NOT DO THIS' CONTRIBUTING.md` OR `grep -q 'directory-nesting' CONTRIBUTING.md` OR `grep -q 'file-level' CONTRIBUTING.md`
    - Both ruff commands referenced: `grep -q 'ruff format --check' CONTRIBUTING.md` AND `grep -q 'ruff check' CONTRIBUTING.md`
    - Tailwind v3.4.17 referenced (no v4 leak): `grep -q 'v3.4.17' CONTRIBUTING.md` AND `grep -c 'v4' CONTRIBUTING.md` returns 0 (or only matches `v4.x` for unrelated content — verify manually)
    - Add-new-env-var procedure carried over: `grep -q 'Settings' CONTRIBUTING.md`
    - Required H2 headers (each `grep -q "## $h" CONTRIBUTING.md` exits 0):
      - Development Prerequisites
      - Local Dev Loop
      - Linting and Formatting
      - Running the Test Suite
      - Committing
      - Releasing
    - LF-only line endings: `python -c "b=open('CONTRIBUTING.md','rb').read(); assert b'\r\n' not in b, 'CRLF detected'"` exits 0
    - Carve-out is real (not duplicated in README): `grep -c '| .make smoke. |' README.md` returns 0 (the make-target table is no longer in README)
  </acceptance_criteria>
  <done>`CONTRIBUTING.md` exists at repo root with make-target table, fast-iteration recipe (with directory-nesting caveat), both ruff invocations, release ritual, GHCR public-visibility flip, and conventional-commits guidance. README no longer contains the make-target table.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md "Files worth knowing" table with operator/dev split pointers</name>
  <read_first>
    - CLAUDE.md (current — find the "Files worth knowing" table section; verify it exists and locate the exact closing line)
    - README.md and CONTRIBUTING.md (just written — to verify pointer accuracy)
    - .planning/phases/18-self-host-packaging/18-PATTERNS.md § "CLAUDE.md (docs, optional pointer)" (lines 567-579)
    - .planning/phases/18-self-host-packaging/18-CONTEXT.md — Claude's Discretion note re: CLAUDE.md pointer
  </read_first>
  <action>
    Locate the "## Files worth knowing" section in `CLAUDE.md`. It's a markdown table with `| File | What it does |` columns. Find the closing row of the table (one of: `.env.example`, `docker-compose.yml`, or similar — read CLAUDE.md to locate the precise final table row).

    Apply ONE Edit to append two new rows to the bottom of the table. Edit anchor: the final existing table row.

    New rows to append (in order):
    ```
    | `CONTRIBUTING.md` | Dev-facing guide: local loop, fast iteration, tests, lint, release ritual, GHCR public-visibility flip. Carved from README in Phase 18. |
    | `docker-compose.override.yml.example` | Dev override template — `cp` to `docker-compose.override.yml` (gitignored) to enable the local `build:` block + the `coffee-snobbery-test` service. |
    | `.github/workflows/release.yml` | Tag-triggered (`v*`) two-job release CI: test gate → multi-arch GHCR push (added Phase 18). |
    ```

    Also: if there is any operator-vs-dev split context that fits in an existing section (e.g., a "Working with the code" note), add a one-line pointer like `Operator-facing docs live in README.md; dev docs in CONTRIBUTING.md.` But this is optional polish — the primary requirement is the table rows.

    No other CLAUDE.md changes. Do NOT touch unrelated sections.
  </action>
  <verify>
    <automated>grep -q 'CONTRIBUTING.md' CLAUDE.md && grep -q 'docker-compose.override.yml.example' CLAUDE.md && grep -q 'release.yml' CLAUDE.md && echo "CLAUDE.md pointer rows OK"</automated>
  </verify>
  <acceptance_criteria>
    - All three new references present (each grep exits 0):
      - `grep -q '`CONTRIBUTING.md`' CLAUDE.md` (rendered as inline code in the table)
      - `grep -q 'docker-compose.override.yml.example' CLAUDE.md`
      - `grep -q '.github/workflows/release.yml' CLAUDE.md` OR `grep -q 'release.yml' CLAUDE.md`
    - The existing table content is intact: pick a known existing row (e.g., `docs/snobbery-gsd-prompt.md` or `app/main.py`) and verify it's still present: `grep -q 'snobbery-gsd-prompt.md' CLAUDE.md` AND `grep -q 'app/main.py' CLAUDE.md`
    - LF line endings preserved (Edit-tool respects existing endings — should pass automatically; spot-check anyway)
    - No other sections inadvertently modified: spot-check `grep -c '## ' CLAUDE.md` returns the same count as before the edit (CLAUDE.md headers unchanged)
  </acceptance_criteria>
  <done>CLAUDE.md "Files worth knowing" table has three new rows pointing to CONTRIBUTING.md, docker-compose.override.yml.example, and .github/workflows/release.yml. No other sections touched.</done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
# V18-03 — three-place single-worker audit (load-bearing — must survive README rewrite)
[ "$(grep -RIn -E '\-\-workers 1|single worker' README.md entrypoint.sh app/services/scheduler.py | wc -l)" -ge 3 ] && echo "V18-03 OK"

# V18-08 — all 8 required README H2 headers
for h in 'Prerequisites' 'Quickstart' 'Environment variables' 'Reverse proxy' 'Upgrade' 'Restore from backup' 'Troubleshooting' 'License'; do
  grep -q "## $h" README.md || { echo "FAIL: $h"; exit 1; }
done
echo "V18-08 OK"

# V18-09 — README references GHCR image path
grep -q 'ghcr.io/jheikkila54/coffee-snobbery' README.md && echo "V18-09 OK"

# V18-10 — NPM TRUSTED_PROXY_IPS=* callout
grep -q 'TRUSTED_PROXY_IPS=\*' README.md && echo "V18-10 OK"

# V18-11 — upgrade three-line procedure
grep -q 'docker compose pull' README.md && grep -q 'docker compose up -d' README.md && echo "V18-11 OK"

# V18-12 — GHCR 403 troubleshooting
(grep -q '403 from ghcr' README.md || grep -q 'Image pull fails' README.md) && echo "V18-12 OK"

# V18-13 — CONTRIBUTING.md content
test -f CONTRIBUTING.md && grep -q 'make smoke' CONTRIBUTING.md && grep -q 'ruff' CONTRIBUTING.md && grep -q 'docker compose cp' CONTRIBUTING.md && echo "V18-13 OK"

# CLAUDE.md pointer rows
grep -q 'CONTRIBUTING.md' CLAUDE.md && grep -q 'docker-compose.override.yml.example' CLAUDE.md && echo "CLAUDE.md OK"

# DIST-05 smoke procedure documented (V18-19, V18-20 manual)
grep -q '"status":"ok"' README.md && grep -q '/setup' README.md && echo "DIST-05 docs OK"

# Carve-out reality check: make-target table should NOT be in README anymore
[ "$(grep -c '`make smoke`' README.md)" -eq 0 ] && echo "make-target table successfully moved to CONTRIBUTING.md"
```

**Manual smoke (V18-19 + V18-20 — DIST-05):** After all five plans land, execute the documented quickstart on a clean volume:
```bash
docker compose down -v
docker compose pull   # pulls ghcr.io/jheikkila54/coffee-snobbery:v1.2.0
docker compose up -d
sleep 20
curl -fsS http://127.0.0.1:8080/healthz | grep '"status":"ok"'   # V18-19
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8080/setup  # V18-20: expect 200 with zero users
```
Note: V18-19 + V18-20 require the GHCR image to already be published (Plan 04 → first `v*` tag push → public-flip per CONTRIBUTING.md). If the image is not yet published, substitute the local build via `docker-compose.override.yml.example` for the smoke run, but the canonical DIST-05 verification is against the pulled GHCR image.
</verification>

<success_criteria>
- DIST-03 satisfied: README is a complete from-zero walkthrough — prereqs, quickstart, env vars, reverse proxy, upgrade, restore, troubleshooting — without referencing the Makefile.
- DIST-04 satisfied: NPM walkthrough is step-by-step with Details/SSL/Advanced field lists + the three load-bearing gotchas (shared network, TRUSTED_PROXY_IPS=*, X-Forwarded-Proto).
- DIST-05 documented: the fresh-install smoke procedure (clean volume → up -d → /healthz ok → / redirects to /setup) is explicit in the README; manual run on first deploy validates V18-19 + V18-20.
- CONTRIBUTING.md carries the dev content the operator no longer needs.
- CLAUDE.md points future agents at both files.
- The three-place single-worker warning system survives the README rewrite (V18-03 ≥3).
</success_criteria>

<output>
After completion, create `.planning/phases/18-self-host-packaging/18-05-SUMMARY.md` per the standard summary template, including:
- Files modified (3): README.md (rewrite), CLAUDE.md (3 table rows), and files created (1): CONTRIBUTING.md
- Validators run: V18-03, V18-08, V18-09, V18-10, V18-11, V18-12, V18-13 — all pass
- DIST-05 status: documented but MANUAL VERIFICATION PENDING (V18-19, V18-20 require a clean-volume run + first `v*` tag push). Execute-phase wave 3 should include a checkpoint to run the smoke manually.
- Carry-forward to Phase 22 (Verification & Release): the first real `v*` tag push will exercise V18-19, V18-20, V18-22 (GHCR public-visibility flip per CONTRIBUTING.md "After First Release" section), and the full release.yml workflow end-to-end. Phase 22 owns the v1.2.0 tag bump per CONTEXT.md D-04.
</output>
