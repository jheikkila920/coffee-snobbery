---
phase: 20-guided-brew-polish
plan: 05
type: execute
wave: 3
depends_on: ["20-02", "20-03", "20-04"]
files_modified:
  - app/static/js/alpine-components/guided-brew-mode.js
  - app/templates/pages/brew_guided.html
autonomous: true
requirements: [GBREW-01, GBREW-02, GBREW-03]
must_haves:
  truths:
    - "Elapsed time is computed from Date.now() minus a persisted _startTimestamp (not an accumulated setInterval counter), and _resync() runs on visibilitychange so the timer self-corrects and silently advances past missed phase transitions on wake from screen sleep (D-15, GBREW-01)"
    - "The timer screen is a full coach view: step type badge + auto-composed coaching line (Bloom / Pour N to Xg / Wait / Action label) + per-step note + per-step temp + remaining countdown + cumulative water target + total elapsed + next-step preview, with a 3-2-1 pre-cue countdown before each transition and audio+vibration+visual cue at the transition (D-08, D-09, D-10, D-11, GBREW-02)"
    - "A 'Mark first drip' tap-to-mark button records first_drip_seconds from brew start, bloom_time_seconds auto-derives when the Bloom-type step transitions, and finishBrewing() carries first_drip + bloom_time + brew_time into /brew/new (D-12, D-13, D-14, GBREW-03)"
  artifacts:
    - path: "app/static/js/alpine-components/guided-brew-mode.js"
      provides: "wall-clock timer (_startTimestamp/_resync/_syncStateFromElapsed), coaching getters, bloom auto-derive, markFirstDrip, finishBrewing timing params"
      contains: "_syncStateFromElapsed"
    - path: "app/templates/pages/brew_guided.html"
      provides: "full coach view, pre-cue countdown, tap-to-mark, bloom indicator, safe-area"
      contains: "coachingLine"
  key_links:
    - from: "app/static/js/alpine-components/guided-brew-mode.js"
      to: "step.time_seconds (cumulative offsets)"
      via: "_syncStateFromElapsed walks steps vs elapsed"
      pattern: "_syncStateFromElapsed"
    - from: "app/templates/pages/brew_guided.html"
      to: "guidedBrewMode getters"
      via: "x-text coachingLine/stepTypeBadge/preCueCountdown"
      pattern: "x-text=\"(coachingLine|preCueCountdown)"
    - from: "guided-brew-mode.js finishBrewing()"
      to: "/brew/new"
      via: "query params first_drip/bloom_time/brew_time"
      pattern: "first_drip=|bloom_time="
---

<objective>
Rework the Guided Brew Mode timer and screen into a purpose-built mobile brewing coach:
a wall-clock-truth timer that survives iOS screen sleep, a full coach view (type badge,
auto-composed coaching line, per-step note/temp, countdown, cumulative water, elapsed,
next-step preview), a 3-2-1 pre-cue countdown, audio+vibration+visual transition cues,
live first-drip tap-to-mark, and auto-derived bloom time, all carried into the brew form
on finish.

Purpose: This is the heart of the phase: GBREW-01 (accurate-on-wake timer, D-15),
GBREW-02 (coached phase stepping, D-08/09/10/11), and the live-capture half of GBREW-03
(D-12/13/14). It consumes the extended step shape from 20-02/20-04 and feeds the timing
fields wired in 20-03.
Output: rewritten timer internals + coaching getters in guided-brew-mode.js and the
redesigned coach view in brew_guided.html with safe-area + touch targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-guided-brew-polish/20-PATTERNS.md
@.planning/phases/20-guided-brew-polish/20-UI-SPEC.md
@.planning/phases/20-guided-brew-polish/20-RESEARCH.md
@.planning/phases/20-guided-brew-polish/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: guided-brew-mode.js wall-clock timer + coaching getters + capture</name>
  <read_first>
    - app/static/js/alpine-components/guided-brew-mode.js (entire file ~374 lines: state lines 17-34, destroy 54-61, pause 125-129, resume 131-135, _startTimer 146-149, _tick 158-168, _stepDuration 99-109, _setupVisibilityReacquire 282-289, requestWakeLock/_releaseWakeLock, playChime ~215, triggerVibration, formattedElapsed getter, finishBrewing 357-365)
    - app/schemas/recipe.py (StepSchema fields type/note/water_temp_c the getters read, from 20-02)
    - .planning/phases/20-guided-brew-polish/20-RESEARCH.md (Pattern 1 wall-clock timer, Pattern 4 coachingLine, Pattern 5 bloom auto-derive; Pitfalls 1/2/5/7 cumulative offsets + pause resync guard + AudioContext)
    - .planning/phases/20-guided-brew-polish/20-PATTERNS.md (guided-brew-mode.js section, exact method replacements)
    - .planning/phases/20-guided-brew-polish/20-UI-SPEC.md (section 4 coach view, copywriting, pre-cue color)
  </read_first>
  <action>
    State additions: _startTimestamp null, _pausedOffset 0, _pausedAt null, firstDripSeconds null, bloomTimeSeconds null.
    Replace _startTimer(): if no _startTimestamp, set it to Date.now() minus elapsedTotalSeconds*1000 and persist to localStorage key 'snobbery:gbm:start'; then start the 1s interval.
    Replace _tick(): elapsed = floor((Date.now() - _startTimestamp)/1000) - _pausedOffset; set elapsedTotalSeconds; call _syncStateFromElapsed(elapsed). (Never increment a counter, Pitfall 1.)
    Add _resync(): guard `if (!_startTimestamp || !isRunning || isPaused) return;` (Pitfall 7), recompute elapsed, set elapsedTotalSeconds, call _syncStateFromElapsed.
    Add _syncStateFromElapsed(elapsed): walk steps treating step.time_seconds as CUMULATIVE offset from start (Pitfall 5); compute stepIdx as count of steps whose time_seconds <= elapsed; if stepIdx >= steps.length transition to done (stop timer, isRunning false, isDone true, release wake lock) without double-cue; else set currentStepIndex and remainingSeconds = max(0, steps[stepIdx].time_seconds - elapsed); when stepIdx > prevIndex: if the completed step type is 'Bloom' set bloomTimeSeconds = elapsedTotalSeconds (D-13), then fire chime/vibrate per cuePrefs (only for newly crossed transitions).
    Extend _setupVisibilityReacquire(): on visible + running + not paused, call _resync() FIRST then await requestWakeLock() (resync before wake-lock per RESEARCH).
    Extend pause(): record _pausedAt = Date.now(); stop timer. Extend resume(): add floor((Date.now()-_pausedAt)/1000) to _pausedOffset, clear _pausedAt, restart timer.
    Extend destroy(): localStorage.removeItem('snobbery:gbm:start').
    Add computed getters: coachingLine (Bloom -> 'Bloom' or 'Bloom - Wg'; Pour -> 'Pour N - to Wg' where N is 1-indexed count of Pour steps up to current; Wait -> 'Wait - ' + formatted time; Action -> step.label; default-at-read type 'Pour'); stepTypeBadge (uppercased type); stepNote (step.note or ''); stepWaterTemp ('at TC' when water_temp_c present); preCueCountdown (returns remainingSeconds when 1-3 else 0); isPreCue (preCueCountdown > 0).
    Add markFirstDrip() (set firstDripSeconds = elapsedTotalSeconds only if currently null, D-14 from brew start) and clearFirstDrip() (set null).
    Extend finishBrewing(): append &first_drip= and &bloom_time= to the /brew/new URL when those values are non-null, alongside the existing &brew_time=.
    Pre-cue is visual only; the chime fires at the transition in _syncStateFromElapsed, NOT during the pre-cue display (Pitfall 2).
  </action>
  <verify>
    <automated>docker compose cp app/static/ coffee-snobbery:/app/app/static/ && docker compose exec coffee-snobbery sh -c "grep -q _syncStateFromElapsed /app/app/static/js/alpine-components/guided-brew-mode.js && grep -q _startTimestamp /app/app/static/js/alpine-components/guided-brew-mode.js && grep -q coachingLine /app/app/static/js/alpine-components/guided-brew-mode.js && grep -q markFirstDrip /app/app/static/js/alpine-components/guided-brew-mode.js && grep -q 'first_drip=' /app/app/static/js/alpine-components/guided-brew-mode.js"</automated>
  </verify>
  <acceptance_criteria>
    - _tick() computes elapsed from Date.now() - _startTimestamp (no `elapsedTotalSeconds++` increment remains; grep for `elapsedTotalSeconds++` returns 0).
    - _resync() exists, guards on isPaused, and is called from the visibilitychange handler before requestWakeLock.
    - _syncStateFromElapsed treats step.time_seconds as cumulative and sets bloomTimeSeconds when a Bloom step completes.
    - coachingLine, stepTypeBadge, stepNote, stepWaterTemp, preCueCountdown, isPreCue getters and markFirstDrip/clearFirstDrip methods exist.
    - finishBrewing() appends first_drip and bloom_time query params; the grep verify command exits 0.
  </acceptance_criteria>
  <done>The timer is wall-clock-truth with visibilitychange resync, coaching getters compose per step type, bloom auto-derives, first-drip is tap-markable, and finish carries all timing to the form. Timer-accuracy-on-wake is the documented manual-only verification (20-VALIDATION.md), confirmed at the 20-06 device checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: brew_guided.html full coach view + pre-cue + tap-to-mark + safe-area</name>
  <read_first>
    - app/templates/pages/brew_guided.html (data-steps line 33 single-quote tojson; start-screen step preview lines 72-80; timer screen block x-show isRunning && !isDone lines 136-228; wake-lock indicator 141-149; step counter 151-154; current step card 167-173; progress bar; pause/next buttons; cue toggles; bottom cancel 220-227)
    - app/static/js/alpine-components/guided-brew-mode.js (the getters/methods from Task 1, the binding contract)
    - .planning/phases/20-guided-brew-polish/20-PATTERNS.md (brew_guided.html section, exact markup blocks for pre-cue, coach card, tap-to-mark, bloom indicator, safe-area)
    - .planning/phases/20-guided-brew-polish/20-UI-SPEC.md (section 4 layout hierarchy, color contract espresso-700 accent, touch targets 56/44, copywriting, Safe-Area Application, Accessibility aria-live)
  </read_first>
  <action>
    Start-screen step preview (lines 72-80): show the step type alongside the label (step.type or 'Pour' then label).
    Timer screen: insert the pre-cue countdown block BEFORE the main countdown, x-show isPreCue, aria-live polite, 'Get ready...' (text-espresso-600 dark:text-cream-300) + the 3-2-1 digit x-text preCueCountdown (text-5xl font-semibold tabular-nums).
    Replace the current-step card (lines 167-173) with the full coach card: bg-espresso-700 text-cream-50 rounded-lg px-4 py-5, aria-live assertive; a type badge x-text stepTypeBadge (text-xs uppercase tracking-wide opacity-70); coaching line x-text coachingLine (text-xl font-semibold); per-step note x-show stepNote x-text stepNote; per-step temp x-show stepWaterTemp x-text stepWaterTemp.
    Keep the existing remaining countdown (text-7xl tabular-nums), progress bar, and add/confirm the cumulative water target line ('Wg / TOTALg total', tabular-nums espresso-700 font-semibold), total elapsed line ('Elapsed: M:SS' text-sm), and next-step preview line ('Next: ...' text-sm espresso-600).
    Insert the first-drip tap-to-mark: x-show firstDripSeconds === null a full-width primary button min-h-[56px] bg-espresso-700 text-cream-50 'Mark first drip' x-on:click markFirstDrip() aria-label 'Mark first drip time'; x-show firstDripSeconds !== null a display tile showing 'First drip: ' + _formatTime(firstDripSeconds) with a 'Clear' link (min-h-[44px], aria-label 'Clear first drip time') x-on:click clearFirstDrip().
    Insert the bloom indicator: x-show when current step type is Bloom show 'Bloom will auto-record'; x-show bloomTimeSeconds !== null show 'Bloom: ' + _formatTime(bloomTimeSeconds).
    Bottom cancel area: apply pb-[max(env(safe-area-inset-bottom),_16px)] to the mt-auto section per the Phase 15 safe-area technique (commit 982c0e6); the coach view is fixed inset-0 z-50 and owns its own safe-area padding (NOT .content-nav-safe-area).
    Keep data-steps single-quoted with |tojson (notes may contain quotes, memory: tojson attr quoting). No |safe, no inline script without nonce. All Tailwind classes carry dark: variants per UI-SPEC. espresso-700 accent reserved to the elements UI-SPEC lists.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery python -m pytest -q tests/test_phase20_mobile.py::test_brew_guided_loads -x</automated>
  </verify>
  <acceptance_criteria>
    - brew_guided.html contains x-text="coachingLine", x-text="stepTypeBadge", x-text="preCueCountdown", a 'Mark first drip' button calling markFirstDrip(), and a bloom auto-record indicator.
    - The bottom action area uses pb-[max(env(safe-area-inset-bottom),_16px)] (safe-area, D-16).
    - data-steps stays single-quoted with |tojson; no |safe in the template; pre-cue container has aria-live="polite" and the coach card aria-live="assertive".
    - test_brew_guided_loads passes against the rebuilt image (guided page returns 200 with guidedBrewMode root).
    - First-drip is a min-h-[56px] primary button; Clear is a min-h-[44px] link.
  </acceptance_criteria>
  <done>The Guided Brew timer screen is a full coach view with pre-cue countdown, tap-to-mark first drip, auto-recorded bloom, cumulative/elapsed/next-step context, and correct safe-area + touch targets; the smoke test is GREEN. Coach-feel and 375px layout are the documented manual verifications closed at 20-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored recipe steps (note/label) -> data-steps attr -> coach view | User-typed step note/label rendered client-side |
| guided-brew JS -> /brew/new query params | Client-computed first_drip/bloom_time/brew_time integers cross to the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-15 | Tampering | XSS via step note/label in coach view | mitigate | x-text (not x-html) for coachingLine/stepNote; data-steps via |tojson in SINGLE-quoted attr (memory: tojson attr quoting); Jinja autoescape ON; no |safe |
| T-20-16 | Tampering | tampered first_drip/bloom_time/brew_time in /brew/new query | mitigate | Server-side BrewSessionCreate bounds (ge=0, le=86400) validate the seeded values on the subsequent POST (plan 20-03); query params only pre-fill the form, never persist directly |
| T-20-17 | Spoofing | guided brew page access | mitigate | Existing brew_guided route require_user unchanged; this plan touches JS/template only |
| T-20-18 | DoS | localStorage timer key abuse | accept | localStorage key is per-origin client state; cleared on destroy(); no server impact |
</threat_model>

<verification>
- test_brew_guided_loads green against the rebuilt image (template/JS change requires rebuild or cp, baked image + Jinja cache).
- Full suite python -m pytest -q green after wave merge.
- ruff/prettier not applicable to JS here, but no CSP violations (no inline script without nonce, no eval/new Function).
- Manual-only (closed at 20-06): timer accuracy across screen sleep on iPhone PWA, coach-feel/pre-cue timing, 375px polish.
</verification>

<success_criteria>
Guided Brew Mode is a purpose-built coach: a sleep-proof wall-clock timer with silent
catch-up, step-by-step coaching with pre-cue + cues, live first-drip capture, auto bloom
time, and timing carried into the brew form. GBREW-01, GBREW-02, and the live half of
GBREW-03 are delivered (manual device verifications pending at 20-06).
</success_criteria>

<output>
Create `.planning/phases/20-guided-brew-polish/20-05-SUMMARY.md` when done.
</output>
