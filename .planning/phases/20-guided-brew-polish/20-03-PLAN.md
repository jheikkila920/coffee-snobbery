---
phase: 20-guided-brew-polish
plan: 03
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - app/templates/fragments/brew_prefill_fields.html
  - app/static/js/alpine-components/water-profile-select.js
  - app/templates/pages/brew_form.html
  - app/routers/brew.py
  - app/services/brew_sessions.py
autonomous: true
requirements: [GBREW-03, GBREW-04]
must_haves:
  truths:
    - "On the brew form the water field is a named-profile <select> (not freetext) with inline 'Add new...' create that POSTs to /water-profiles and auto-selects the new profile via the water-profile-created HX-Trigger (D-02, D-04 water profiles)"
    - "A user can record first-drip (s) and bloom time (s) on any brew session via optional number inputs under a 'Brew timing (optional)' fieldset — including manually-logged non-guided sessions (D-12, GBREW-03)"
    - "The GBM completion redirect ?first_drip= and ?bloom_time= query params seed the brew form, and water_profile_id + the two timing fields persist on create and edit (D-12, D-14)"
  artifacts:
    - path: "app/static/js/alpine-components/water-profile-select.js"
      provides: "waterProfileSelect Alpine component (select-or-create, HX-Trigger listener)"
      contains: "waterProfileSelect"
    - path: "app/templates/fragments/brew_prefill_fields.html"
      provides: "water_profile_id select + inline create + timing fieldset"
      contains: "water_profile_id"
  key_links:
    - from: "app/templates/fragments/brew_prefill_fields.html"
      to: "/water-profiles"
      via: "htmx.ajax POST in saveProfile()"
      pattern: "htmx\\.ajax\\('POST', '/water-profiles'"
    - from: "app/routers/brew.py"
      to: "water_profiles (context)"
      via: "_hydrate_form_context injects water_profiles list"
      pattern: "water_profiles"
    - from: "app/services/brew_sessions.py"
      to: "BrewSession columns"
      via: "create/update accept water_profile_id, first_drip_seconds, bloom_time_seconds"
      pattern: "water_profile_id"
---

<objective>
Wire the new water-profile catalog and brew-timing fields into the brew session form
end-to-end: replace the freetext water_type input with an Alpine select-or-create profile
picker, add the optional first-drip / bloom-time inputs (usable on any session), inject the
water_profiles list into the form context, ingest the GBM ?first_drip=/?bloom_time= query
params, and persist all three new fields through create + edit.

Purpose: Delivers GBREW-04 (named water profiles instead of freetext) and the form half of
GBREW-03 (editable first-drip/bloom on any session, including non-guided) — the data the
guided-brew tap-to-mark (plan 20-05) feeds back into.
Output: new water-profile-select.js, modified brew_prefill_fields.html + brew_form.html,
brew router context + form parsing, brew service create/update signatures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-guided-brew-polish/20-PATTERNS.md
@.planning/phases/20-guided-brew-polish/20-UI-SPEC.md
@.planning/phases/20-guided-brew-polish/20-CONTEXT.md
@app/routers/brew.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: water_profiles context + form parsing + service persistence (brew.py, brew_sessions.py)</name>
  <read_first>
    - app/routers/brew.py (_hydrate_form_context lines 278-358 — context choke point for /brew/new, /brew/prefill, edit, re-render; _selectables; new_brew_form lines 766-836 ?brew_time= seeding lines 782-796; create_brew lines 923-965; edit_brew_form values dict lines 989-1010; update_brew ~line 1067-1078)
    - app/services/brew_sessions.py (create_brew_session lines 188-237; _CARRYABLE_FIELDS lines 98-118; update_brew_session signature; water_type handling)
    - app/services/water_profiles.py (list_water_profiles — built in 20-02)
    - app/schemas/brew_session.py (BrewSessionCreate water_profile_id/first_drip_seconds/bloom_time_seconds — built in 20-02)
    - .planning/phases/20-guided-brew-polish/20-RESEARCH.md (Pitfall 6: empty-string water_profile_id → None before Pydantic; finishBrewing query-param contract)
  </read_first>
  <action>
    In app/routers/brew.py _hydrate_form_context: add `"water_profiles": [{"id": p.id, "name": p.name} for p in water_profiles_service.list_water_profiles(db)]` to the returned context dict so brew_prefill_fields.html can render the <select> options and seed data-initial-profiles. Import water_profiles service. This single helper feeds /brew/new, /brew/prefill, edit, and error re-render — no per-handler duplication.
    In new_brew_form (the GET /brew/new handler): alongside the existing ?brew_time= seeding (~line 782-796), parse `first_drip = _int_or_none(qp.get("first_drip"))` and `bloom_time = _int_or_none(qp.get("bloom_time"))` and seed values["first_drip_seconds"]/values["bloom_time_seconds"] as str when present (D-12, D-14 — GBM completion path).
    In edit_brew_form values dict: add "water_profile_id" (str(session.water_profile_id) or ""), "first_drip_seconds", "bloom_time_seconds" (_num_str pattern) so edit pre-fills the new fields.
    In create_brew and update_brew form parsing: before constructing BrewSessionCreate, coerce empty-string water_profile_id to None (`raw["water_profile_id"] = raw.get("water_profile_id") or None`) per Pitfall 6.
    In app/services/brew_sessions.py create_brew_session and update_brew_session: add keyword params water_profile_id, first_drip_seconds, bloom_time_seconds and assign them onto the BrewSession. Keep writing water_type for backward compat OR set it from the selected profile name — set water_type from profile name is NOT required; leave water_type as-is from form (deprecated). In brew.py create_brew/update_brew calls, pass form.water_profile_id, form.first_drip_seconds, form.bloom_time_seconds.
    Add the three new fields to _CARRYABLE_FIELDS only if they should carry across prefill — first_drip/bloom should NOT carry (they are per-attempt), water_profile_id MAY carry; default to NOT adding them to _CARRYABLE_FIELDS unless an existing test requires it.
  </action>
  <verify>
    <automated>docker compose cp app/ coffee-snobbery:/app/app/ && docker compose exec coffee-snobbery python -m pytest -q tests/test_phase20_brew_session.py::test_gbm_finish_url_has_brew_time tests/test_phase20_mobile.py::test_brew_form_loads -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/brew.py _hydrate_form_context` returns a `water_profiles` key (list of {id, name}).
    - GET /brew/new?gbm=1&brew_time=120&first_drip=20&bloom_time=45 seeds first_drip_seconds and bloom_time_seconds into the form (assert via test_gbm_finish_url_has_brew_time).
    - `create_brew_session` and `update_brew_session` accept and persist water_profile_id, first_drip_seconds, bloom_time_seconds.
    - Empty-string water_profile_id from the form is coerced to None before BrewSessionCreate (no ValidationError on unselected).
    - `ruff check app/routers/brew.py app/services/brew_sessions.py` clean.
  </acceptance_criteria>
  <done>The form context carries water_profiles, GBM query params seed timing fields, and all three new fields persist on create + edit; the finish-URL and form-load tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: water-profile select-or-create UI + timing fieldset (brew_prefill_fields.html, water-profile-select.js, brew_form.html)</name>
  <read_first>
    - app/templates/fragments/brew_prefill_fields.html (water_type block lines ~120-138 to replace; dark-mode + label patterns; data-steps tojson single-quote convention)
    - app/static/js/alpine-components/flavor-tag-input.js (observedFlavorNotes init/destroy + _onCreated HX-Trigger listener lines 79-103; createFromQuery htmx.ajax lines 175-180)
    - app/templates/pages/brew_form.html (head_extra / script-load block — where guided-brew/recipe component scripts are loaded with nonce; where brew_prefill_fields is included)
    - app/static/js/htmx-listeners.js (X-CSRF-Token injection on htmx.ajax — confirm no manual header needed)
    - .planning/phases/20-guided-brew-polish/20-PATTERNS.md (brew_prefill_fields section — full select-or-create block + waterProfileSelect component excerpt)
    - .planning/phases/20-guided-brew-polish/20-UI-SPEC.md (§1 Water Profile + §3 timing inputs + Touch Target + Copywriting + Safe-Area + CSP/Dark-mode invariants)
  </read_first>
  <action>
    Replace the water_type <label> block in brew_prefill_fields.html (~lines 124-138) with the Alpine x-data="waterProfileSelect" container: data-initial-profiles='{{ water_profiles | tojson }}' (single-quoted — memory: tojson attr quoting) and data-initial-value='{{ values.get("water_profile_id", "") }}'. Inside: a native <select name="water_profile_id"> x-show="!showCreate" listing x-for profiles with a leading "—" empty option and trailing value="__new__" "Add new…" option; an x-show="showCreate" inline create block with name input (maxlength=80, placeholder "Profile name (e.g. Third Wave Water)"), notes <textarea rows=2>, a createError <p class="text-sm text-red-600">, a "Save profile" button (min-h-[44px], bg-espresso-700 text-cream-50), and a Cancel link (min-h-[44px]). All inputs text-base (16px, no iOS zoom). Dark-mode dark: variants per UI-SPEC.
    Add a "Brew timing (optional)" <fieldset> with two optional number inputs: name="first_drip_seconds" label "First drip (s)" and name="bloom_time_seconds" label "Bloom time (s)" — w-24, text-base tabular-nums, inputmode=numeric, min=0. Pre-fill from values (D-12 — editable on any session including non-guided).
    Create app/static/js/alpine-components/water-profile-select.js: Alpine.data('waterProfileSelect', () => ({ profiles, profileId, showCreate, createError, init(), destroy(), onSelectChange(v), saveProfile() })). init() parses this.$root.dataset.initialProfiles + initialValue, registers document.body 'water-profile-created' listener that de-dupes then pushes {id,name}, sets profileId, hides create form. saveProfile() validates non-empty name (else createError "Profile name is required."), then htmx.ajax('POST','/water-profiles',{values:{name,notes},swap:'none'}) — CSRF injected by htmx-listeners.js, NO manual fetch/header. onSelectChange toggles showCreate when value==="__new__". destroy() removes the listener. Mirror flavor-tag-input.js lifecycle exactly.
    In brew_form.html: load water-profile-select.js with <script defer src=... nonce="{{ csp_nonce(request) }}"> in the same block as the other Alpine component scripts (before Alpine boots). No inline script without nonce; no |safe on user data.
    Rebuild required after template/JS change (baked image, Jinja template cache) — verify in a freshly built/cp'd container.
  </action>
  <verify>
    <automated>docker compose cp app/ coffee-snobbery:/app/app/ && docker compose exec coffee-snobbery python -m pytest -q tests/test_phase20_mobile.py::test_brew_form_loads tests/test_phase20_water_profiles.py::test_create_water_profile -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/fragments/brew_prefill_fields.html` contains `name="water_profile_id"` and `data-initial-profiles='{{ water_profiles | tojson }}'` (single-quoted attr) and no longer renders a freetext water_type input as the primary water field.
    - `app/static/js/alpine-components/water-profile-select.js` defines `Alpine.data('waterProfileSelect'` with init/destroy/onSelectChange/saveProfile and a `water-profile-created` body listener.
    - `saveProfile()` calls `htmx.ajax('POST', '/water-profiles', { ... swap: 'none' })` (no manual fetch, no manual CSRF header).
    - brew_form.html loads water-profile-select.js via `<script defer ... nonce="{{ csp_nonce(request) }}">`.
    - The "Brew timing (optional)" fieldset renders `name="first_drip_seconds"` and `name="bloom_time_seconds"` number inputs (text-base, ≥16px → no iOS zoom).
    - test_brew_form_loads (asserts name="water_profile_id") and test_create_water_profile pass.
  </acceptance_criteria>
  <done>The brew form uses a named water-profile select with inline CSRF-safe create that auto-selects via HX-Trigger, plus optional first-drip/bloom inputs available on any session; tests GREEN and no CSP/tojson violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /water-profiles (htmx.ajax) | Untrusted name/notes; CSRF-protected |
| browser → POST /brew (create/update) | water_profile_id + timing ints crossing into DB |
| stored profile name → brew form template | Rendered into <option> text and tojson attr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Tampering | CSRF on htmx.ajax POST /water-profiles | mitigate | htmx-listeners.js injects X-CSRF-Token on every htmx.ajax; endpoint not exempt from starlette-csrf |
| T-20-09 | Tampering | XSS via water profile name in <option>/attr | mitigate | Jinja2 autoescape ON; data-initial-profiles uses |tojson inside SINGLE-quoted attr (memory: tojson attr quoting); x-text (not x-html) for option labels; no |safe |
| T-20-10 | Tampering | water_profile_id / timing tampering | mitigate | BrewSessionCreate ge=1 / ge=0 bounds + FK constraint; empty-string coerced to None |
| T-20-11 | Information disclosure | cross-user water profile leak | accept | water profiles are household-shared by design (ASVS V4) — list is intentionally shared, not per-user |
</threat_model>

<verification>
- `pytest tests/test_phase20_water_profiles.py tests/test_phase20_mobile.py` green.
- Full suite `python -m pytest -q` green after wave merge (no regressions in existing brew_form tests).
- `ruff check` + `ruff format --check` clean on all modified files.
- Manual (deferred to 20-06 checkpoint): inline create + auto-select + 375px layout on iPhone PWA.
</verification>

<success_criteria>
The brew form selects water from the named profiles catalog with inline create, and first-drip/bloom time are recordable on any brew session including non-guided ones; GBREW-04 and the form half of GBREW-03 are delivered and tested.
</success_criteria>

<output>
Create `.planning/phases/20-guided-brew-polish/20-03-SUMMARY.md` when done.
</output>
