---
phase: 20-guided-brew-polish
plan: 04
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - app/static/js/alpine-components/recipe-step-builder.js
  - app/templates/fragments/recipe_step_builder.html
  - tests/test_phase20_step_schema.py
autonomous: true
requirements: [GBREW-06]
must_haves:
  truths:
    - "The recipe step builder lets each step pick a type (Bloom/Pour/Wait/Action, default Pour), and choosing Wait or Action dims and clears the Water (g) field so the step can be a pure timed action with no water (D-04, D-07, GBREW-06)"
    - "Each step supports an optional per-step note (<=200 chars, e.g. 'Hario Switch closed for immersion') revealed via an 'Add note' affordance, and an optional per-step water temperature (50-100) (D-05, D-06, GBREW-06)"
    - "stepsJson serializes type/note/water_temp_c so the extended StepSchema persists them, and old recipes without these fields still render (default-at-read type='Pour')"
  artifacts:
    - path: "app/static/js/alpine-components/recipe-step-builder.js"
      provides: "setType/setNote/setWaterTemp setters + showNote state + extended addStep/init defaults"
      contains: "setType"
    - path: "app/templates/fragments/recipe_step_builder.html"
      provides: "Type select + Temp input + per-step note textarea + Wait/Action dimming"
      contains: "setType(idx"
  key_links:
    - from: "app/templates/fragments/recipe_step_builder.html"
      to: "recipe-step-builder.js setters"
      via: "x-on change/input bindings"
      pattern: "set(Type|Note|WaterTemp)"
    - from: "app/static/js/alpine-components/recipe-step-builder.js"
      to: "RecipeCreate.steps (StepSchema)"
      via: "stepsJson JSON.stringify includes new fields"
      pattern: "stepsJson"
---

<objective>
Extend the recipe step builder so each step carries an explicit type (Bloom/Pour/Wait/Action),
an optional per-step note, and an optional per-step water temperature, and so Wait/Action
steps can exist with no water. These are the recipe-authoring half of GBREW-06; the guided-brew
coaching (plan 20-05) consumes the resulting step shape.

Purpose: Recipes become coachable. Bloom/pour phases are explicit, drawdown/switch actions
become first-class timed steps, and per-step notes flow into the Guided Brew coach view.
Output: extended recipe-step-builder.js (setters + state), recipe_step_builder.html (Type
select, Temp input, note affordance, Wait/Action dimming), and a round-trip test proving a
Wait/Action step with note + temp validates through RecipeCreate/StepSchema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-guided-brew-polish/20-PATTERNS.md
@.planning/phases/20-guided-brew-polish/20-UI-SPEC.md
@.planning/phases/20-guided-brew-polish/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: recipe-step-builder.js type/note/temp state + setters</name>
  <read_first>
    - app/static/js/alpine-components/recipe-step-builder.js (entire file ~174 lines: init lines 41-44 default step, addStep lines 46-58, existing setters setWater/setTime ~line 60-91, stepsJson getter line 137, timelineSegments getter lines 141-172)
    - app/schemas/recipe.py (extended StepSchema field names/bounds from 20-02; setters must produce a matching shape)
    - .planning/phases/20-guided-brew-polish/20-PATTERNS.md (recipe-step-builder.js section, exact setter excerpts + showNote state)
    - .planning/phases/20-guided-brew-polish/20-UI-SPEC.md (section 2, default Pour, Wait/Action clears water, temp 50-100)
  </read_first>
  <action>
    Add to the component state object: showNote as an empty object dict keyed by step index (toggles the note textarea visibility).
    Extend init() default step (when steps.length is 0) to seed the new fields on the default Bloom step: type 'Bloom', water_grams 50, time_seconds 45, label 'Bloom', note null, water_temp_c null.
    Extend addStep() to push a step with type 'Pour', note null, water_temp_c null in addition to the existing cumulative water_grams/time_seconds defaults.
    Add setter methods after the existing setters: setType(i, v) sets steps[i].type to v or 'Pour', and when v is 'Wait' or 'Action' sets steps[i].water_grams to null (D-07). setNote(i, v) sets steps[i].note to v.trim() or null. setWaterTemp(i, v) parses an int and sets steps[i].water_temp_c to the finite value or null.
    Update the timelineSegments label fallback to include type so Wait/Action segments label sensibly (label falls back to step.label, then step.type, then 'Step N').
    Do NOT change stepsJson; JSON.stringify(this.steps) already serializes the new fields automatically. Confirm the new fields round-trip through stepsJson.
  </action>
  <verify>
    <automated>docker compose cp app/static/ coffee-snobbery:/app/app/static/ && docker compose exec coffee-snobbery sh -c "grep -q setType /app/app/static/js/alpine-components/recipe-step-builder.js && grep -q setWaterTemp /app/app/static/js/alpine-components/recipe-step-builder.js && grep -q showNote /app/app/static/js/alpine-components/recipe-step-builder.js"</automated>
  </verify>
  <acceptance_criteria>
    - recipe-step-builder.js defines setType, setNote, setWaterTemp methods and a showNote state object.
    - setType('Wait')/setType('Action') sets the step's water_grams to null.
    - addStep() and init() seed type/note/water_temp_c on created steps.
    - grep for setType, setWaterTemp, showNote in the cp'd file all succeed (verify command exits 0).
  </acceptance_criteria>
  <done>The step-builder component manages step type, per-step note, and per-step temp; Wait/Action clears water; stepsJson serializes all new fields.</done>
</task>

<task type="auto">
  <name>Task 2: recipe_step_builder.html Type/Temp/note fields + Wait-Action dimming + round-trip test</name>
  <read_first>
    - app/templates/fragments/recipe_step_builder.html (step row template x-for lines 29-88: flex row lines 49-77 Water(g)/Time(s) inputs, reorder w-11 h-11 buttons lines 36-46/80-85, formatDelta paragraph line 78)
    - app/static/js/alpine-components/recipe-step-builder.js (the setters from Task 1, the binding contract)
    - app/templates/pages/recipe_form.html (how recipe_step_builder is included + how the component script is loaded with nonce)
    - tests/test_phase20_step_schema.py (existing round-trip test scaffold from 20-01 to extend)
    - .planning/phases/20-guided-brew-polish/20-PATTERNS.md (recipe_step_builder.html section, full field excerpts + showNote note block)
    - .planning/phases/20-guided-brew-polish/20-UI-SPEC.md (section 2 styling, widths w-32/w-24, touch targets, dark mode, copywriting)
  </read_first>
  <action>
    In the step row flex (flex flex-wrap gap-3 items-end): insert a Type select (w-32, label 'Type') BEFORE the Label input, options Pour/Bloom/Wait/Action, value bound to step.type or 'Pour', x-on:change calls setType(idx, value), aria-label 'Step type for step N'.
    Modify the Water (g) field (lines 59-67): add :class dimming opacity-40 when step.type is Wait or Action, and :disabled when step.type is Wait or Action (D-07).
    Insert a Temp (degC) number input (w-24, label 'Temp (degC)', min 50 max 100, inputmode decimal, placeholder dash) after the Time (s) field, value bound to step.water_temp_c or '', x-on:input calls setWaterTemp(idx, value).
    Insert a per-step note block (after the formatDelta paragraph, inside the flex-1 column): an 'Add note' text trigger (text-sm underline, min-h-[44px], x-show when no note and not showNote[idx], x-on:click sets showNote[idx]=true) and a textarea rows=2 maxlength=200 (x-show when step.note or showNote[idx], value step.note or '', x-on:input setNote(idx, value), placeholder 'Optional step note (e.g. Hario Switch closed for immersion)', w-full text-base).
    All inputs text-base (16px, no iOS zoom). Dark-mode dark: variants per UI-SPEC. Keep existing w-11 h-11 reorder/remove buttons unchanged. No |safe; autoescape on note. Ensure recipe_form.html loads recipe-step-builder.js with nonce (it already does; verify).
    Extend tests/test_phase20_step_schema.py with test_recipe_roundtrip_wait_action: build a RecipeCreate-shaped steps list containing a Bloom step, a Pour step with water_temp_c, a Wait step (water_grams None), and an Action step with a note; assert it validates and the Action step's note + the Pour step's water_temp_c survive (this proves the UI-produced shape persists through StepSchema).
  </action>
  <verify>
    <automated>docker compose cp app/ coffee-snobbery:/app/app/ && docker compose cp tests/ coffee-snobbery:/app/tests/ && docker compose exec coffee-snobbery python -m pytest -q tests/test_phase20_step_schema.py -x</automated>
  </verify>
  <acceptance_criteria>
    - recipe_step_builder.html contains a Type select calling setType(idx, ...), a Temp input calling setWaterTemp(idx, ...), and a note textarea calling setNote(idx, ...).
    - The Water (g) field is dimmed (opacity-40) and disabled when step.type is Wait or Action.
    - The note textarea has maxlength 200 and the 'Add note' affordance is keyed by showNote[idx].
    - tests/test_phase20_step_schema.py defines test_recipe_roundtrip_wait_action and the full file passes (pytest exits 0).
    - ruff check on the test file is clean; no |safe in the template; all inputs text-base.
  </acceptance_criteria>
  <done>The step builder UI exposes type/note/temp per step with Wait/Action dimming, and a round-trip test confirms the produced shape validates through StepSchema; GBREW-06 authoring is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser step builder -> POST /recipes (stepsJson) | Untrusted JSONB step array (type/note/temp) crosses into DB |
| stored JSONB note -> recipe/coach templates | User-typed note rendered to the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | Tampering | step type/note/temp via stepsJson | mitigate | StepSchema validates server-side: type is a Literal (Bloom/Pour/Wait/Action), note max_length=200, water_temp_c 50-100, water_grams le=2000; ConfigDict(extra="forbid") rejects unknown keys |
| T-20-13 | Tampering | XSS via per-step note in templates | mitigate | Jinja2 autoescape ON; note rendered as plain text (x-text in coach view, autoescaped in builder); no |safe, no x-html |
| T-20-14 | Tampering | CSRF on recipe save | mitigate | Existing recipe form CSRF unchanged; this plan adds fields, not endpoints |
</threat_model>

<verification>
- pytest tests/test_phase20_step_schema.py green (including the new round-trip test).
- Full suite python -m pytest -q green after wave merge (no recipe-form regressions).
- ruff check + ruff format --check clean on all modified files.
- Manual (deferred to 20-06 checkpoint): step-builder usability + 375px layout on device.
</verification>

<success_criteria>
The recipe step builder supports typed steps (Bloom/Pour/Wait/Action), waterless timed
Wait/Action steps, per-step notes, and per-step temperature, all persisting through the
extended StepSchema; GBREW-06 authoring is delivered and tested.
</success_criteria>

<output>
Create `.planning/phases/20-guided-brew-polish/20-04-SUMMARY.md` when done.
</output>
