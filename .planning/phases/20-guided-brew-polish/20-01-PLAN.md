---
phase: 20-guided-brew-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase20_water_profiles.py
  - tests/test_phase20_step_schema.py
  - tests/test_phase20_brew_session.py
  - tests/test_phase20_mobile.py
  - tests/conftest.py
autonomous: true
requirements: [GBREW-01, GBREW-02, GBREW-03, GBREW-04, GBREW-05, GBREW-06]
must_haves:
  truths:
    - "Running the four Phase 20 test files collects tests and they fail/error (RED) because the production code does not exist yet — proving the tests assert real behavior, not pass-by-skip (D-04..D-15 behaviors are encoded as assertions)"
    - "No Phase 20 test uses pytest.skip on missing seed data (memory: tests pass-by-skip mask green) — every test either asserts or xfails with an explicit reason"
  artifacts:
    - path: "tests/test_phase20_water_profiles.py"
      provides: "GBREW-04 migration seed/link/null + POST /water-profiles + HX-Trigger assertions (D-02, D-03)"
    - path: "tests/test_phase20_step_schema.py"
      provides: "GBREW-06 StepSchema optional water_grams, type literal, temp range, note, backward-compat (D-04..D-07)"
    - path: "tests/test_phase20_brew_session.py"
      provides: "GBREW-03 timing-field schema + columns + GBREW-01 finish URL (D-12, D-14, D-15)"
    - path: "tests/test_phase20_mobile.py"
      provides: "GBREW-05 smoke: guided brew + brew form pages load (D-16)"
  key_links:
    - from: "tests/test_phase20_*.py"
      to: "app.schemas.recipe.StepSchema / app.models.water_profile / app.schemas.brew_session"
      via: "import + instantiate"
      pattern: "from app\\.(schemas|models)"
---

<objective>
Create the Wave 0 test scaffolding for Phase 20 per 20-VALIDATION.md. These four test
files encode every automatable GBREW-01..06 behavior as assertions BEFORE any production
code exists, so the implementation waves have a feedback loop and the Nyquist sampling
contract is satisfiable.

Purpose: Lock the behavioral contract for water profiles, the extended StepSchema, the
new brew-session timing fields, and the migration data-seed/link/null logic. Tests start
RED (imports of not-yet-existing modules fail) and turn GREEN as Waves 1-3 land.
Output: tests/test_phase20_water_profiles.py, tests/test_phase20_step_schema.py,
tests/test_phase20_brew_session.py, tests/test_phase20_mobile.py, and any conftest factory
additions needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-guided-brew-polish/20-VALIDATION.md
@.planning/phases/20-guided-brew-polish/20-RESEARCH.md
@.planning/phases/20-guided-brew-polish/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + brew-session test files (test_phase20_step_schema.py, test_phase20_brew_session.py)</name>
  <read_first>
    - tests/conftest.py (existing transactional fixtures + factories; do NOT duplicate session/client fixtures)
    - app/schemas/recipe.py (current StepSchema shape to assert against post-extension)
    - app/schemas/brew_session.py (current BrewSessionCreate shape)
    - .planning/phases/20-guided-brew-polish/20-VALIDATION.md (Per-Task Verification Map — exact test names)
    - tests/test_brew_form.py or equivalent existing brew test (TestClient + auth-session fixture usage pattern)
  </read_first>
  <action>
    Create tests/test_phase20_step_schema.py with these named tests (exact names from VALIDATION.md so the per-task verify commands resolve):
    test_backward_compat_no_type — construct StepSchema from the OLD dict {"water_grams": 100, "time_seconds": 45, "label": "Bloom"}; assert it validates and resulting .type == "Pour" (default-at-read, no backfill — D-04, A3).
    test_wait_step_no_water — StepSchema(type="Wait", time_seconds=30, label="Drawdown") validates with water_grams is None (D-07, GBREW-06).
    test_step_water_temp_range — StepSchema with water_temp_c=94 validates; water_temp_c=49 and water_temp_c=101 raise ValidationError (D-06, ge=50 le=100).
    test_coaching_line_by_type — assert the StepSchema-level invariants that drive the JS coachingLine: a Bloom step keeps type "Bloom", a Pour step keeps water_grams as cumulative target, a Wait step accepts water_grams None. (Coaching string composition itself lives in JS and is manually verified; this test pins the data contract the JS reads — D-09.)
    Also add a test asserting ConfigDict(extra="forbid") still rejects an unknown key (mass-assignment guard — threat T-V5).
    Create tests/test_phase20_brew_session.py with:
    test_timing_fields_schema — BrewSessionCreate accepts first_drip_seconds and bloom_time_seconds as optional ints; negative values raise; absent → None (D-12, D-14).
    test_gbm_finish_url_has_brew_time — assert (via the GET /brew/new handler with ?gbm=1&brew_time=T&first_drip=X&bloom_time=Y) that the rendered form seeds those values; this pins the finishBrewing() query-param contract (D-15, GBREW-01). Use the existing authed TestClient fixture.
    test for water_profile_id: BrewSessionCreate accepts water_profile_id (ge=1) and rejects 0 (D-02, threat water_profile_id tampering).
    A timing_columns test: assert brew_sessions table has columns water_profile_id, first_drip_seconds, bloom_time_seconds after migrations run (introspect via SQLAlchemy inspect on the test engine).
    Do NOT use pytest.skip for missing data — if a fixture is needed, create it. These tests are EXPECTED to fail RED now (StepSchema lacks type/note/water_temp_c; BrewSessionCreate lacks the new fields). That is correct Wave 0 behavior.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/test_phase20_step_schema.py tests/test_phase20_brew_session.py --collect-only 2>&1 | grep -v '^#' | grep -c 'test_' </automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only tests/test_phase20_step_schema.py tests/test_phase20_brew_session.py` collects ≥ 8 tests (no collection errors that yield 0).
    - tests/test_phase20_step_schema.py defines functions named test_backward_compat_no_type, test_wait_step_no_water, test_step_water_temp_range, test_coaching_line_by_type.
    - tests/test_phase20_brew_session.py defines test_timing_fields_schema and test_gbm_finish_url_has_brew_time.
    - No occurrence of `pytest.skip(` in either file (grep returns 0).
  </acceptance_criteria>
  <done>Both test files exist, collect cleanly, and assert the locked schema/timing contracts; running them now produces RED (not skip), which is the correct pre-implementation state.</done>
</task>

<task type="auto">
  <name>Task 2: Water-profile + mobile-smoke test files (test_phase20_water_profiles.py, test_phase20_mobile.py)</name>
  <read_first>
    - app/routers/flavor_notes.py (POST handler + HX-Trigger header pattern to mirror in assertions)
    - app/migrations/versions/p15_1_multi_origin.py (data-migration test analog — how migration behavior is asserted elsewhere)
    - tests/conftest.py (authed TestClient fixture, DB engine fixture, how migrations are applied to the test DB)
    - .planning/phases/20-guided-brew-polish/20-RESEARCH.md (Pattern 3 migration SQL: INITCAP(TRIM(...)) normalization, NULL/blank handling)
    - .planning/phases/20-guided-brew-polish/20-VALIDATION.md (Highest-value tests list)
  </read_first>
  <action>
    Create tests/test_phase20_water_profiles.py with:
    test_create_water_profile — authed POST /water-profiles with form {name:"Third Wave Water", notes:"remineralized"} returns 200 and an HX-Trigger header whose JSON contains key "water-profile-created" with water_profile_id and name (mirror flavor_notes.py assertion). CSRF must be satisfied via the test client's existing CSRF helper (T-V4 / CSRF threat).
    test_create_water_profile_duplicate — second POST of the same name returns 422 with errors.name set (DuplicateNameError path).
    test_create_water_profile_blank — POST with empty name returns 422 (WaterProfileCreate min_length=1).
    test_migration_seeds_profiles — insert synthetic brew_sessions rows with water_type values ["Tap", "tap", "FILTERED", "Third Wave Water"], run the p20 migration logic, assert water_profiles has rows ["Tap","Filtered","Third Wave Water"] (INITCAP+TRIM dedup, "tap"/"Tap" collapse — D-03).
    test_migration_links_sessions — assert each seeded session's water_profile_id points to the profile whose name == INITCAP(TRIM(water_type)).
    test_migration_null_water_type — sessions with water_type NULL or "" or "   " get water_profile_id IS NULL and NO "Unknown"/"Unspecified" profile is created (A2, D-03).
    For the migration tests, prefer driving alembic upgrade/downgrade against a scratch DB OR asserting the post-`alembic upgrade head` end state (follow whatever pattern tests/test_migrations.py uses — read it first; reuse, do not invent).
    Create tests/test_phase20_mobile.py with:
    test_brew_guided_loads — authed GET of the guided-brew page returns 200 and contains the guidedBrewMode Alpine root (smoke; 375px layout itself is manual per Manual-Only Verifications — D-16).
    test_brew_form_loads — authed GET /brew/new returns 200 and contains name="water_profile_id" (proves the water_type→profile select swap landed; will be RED until Wave 2).
    No pytest.skip for missing seed data. These start RED.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/test_phase20_water_profiles.py tests/test_phase20_mobile.py --collect-only 2>&1 | grep -v '^#' | grep -c 'test_'</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` for both files collects ≥ 8 tests with no collection error.
    - tests/test_phase20_water_profiles.py defines test_create_water_profile, test_migration_seeds_profiles, test_migration_links_sessions, test_migration_null_water_type.
    - tests/test_phase20_mobile.py defines test_brew_guided_loads and asserts on name="water_profile_id" in the brew form.
    - No `pytest.skip(` in either file (grep returns 0).
    - Migration tests follow the existing tests/test_migrations.py harness pattern (no bespoke alembic invocation if a shared helper exists).
  </acceptance_criteria>
  <done>Both test files exist, collect cleanly, encode the GBREW-04 migration + endpoint + GBREW-05 smoke contracts, and are RED pending implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Tests exercise real schemas/routers; no new untrusted input surface introduced by the tests themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | Test fixtures relaxing schema to pass | mitigate | Tests assert the LOCKED bounds (ge=50/le=100 temp, ge=1 water_profile_id, min_length=1 name); they fail RED rather than loosening production schema (memory: executor loosens schema for bad fixtures) |
| T-20-SC | Tampering | npm/pip installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A). pytest/respx already documented in CLAUDE.md test loop |
</threat_model>

<verification>
- `pytest --collect-only tests/test_phase20_*.py` collects all files with zero collection errors.
- No `pytest.skip(` anywhere in the four new files.
- Running the suite now yields RED/errors (imports of not-yet-built modules / missing fields), confirming the tests assert real behavior.
</verification>

<success_criteria>
Four Phase 20 test files exist, collect cleanly, encode every automatable GBREW-01..06 behavior with correct test names from 20-VALIDATION.md, use no pass-by-skip, and are RED pending implementation waves.
</success_criteria>

<output>
Create `.planning/phases/20-guided-brew-polish/20-01-SUMMARY.md` when done.
</output>
