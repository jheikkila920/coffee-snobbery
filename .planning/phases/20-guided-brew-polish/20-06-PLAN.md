---
phase: 20-guided-brew-polish
plan: 06
type: execute
wave: 4
depends_on: ["20-03", "20-04", "20-05"]
files_modified:
  - .planning/phases/20-guided-brew-polish/20-VERIFICATION.md
autonomous: false
requirements: [GBREW-01, GBREW-02, GBREW-05]
must_haves:
  truths:
    - "The full pytest suite runs green twice in a row in the baked container (no pass-by-skip masking, no cross-module isolation failures), and ruff check + ruff format --check are both clean before any push (memory: executors skip ruff, CI gates both)"
    - "On a physical iPhone PWA, the Guided Brew timer stays accurate across screen sleep with silent catch-up of missed transitions (D-15, GBREW-01), the 3-2-1 pre-cue + coach view feel purpose-built mid-pour (D-10/D-11, GBREW-02), and all new UI passes the 375px bar with correct touch targets, safe-area, and no horizontal scroll (D-16, GBREW-05)"
  artifacts:
    - path: ".planning/phases/20-guided-brew-polish/20-VERIFICATION.md"
      provides: "recorded results of the three manual device verifications + full-suite/ruff gate evidence"
  key_links:
    - from: "guided-brew coach view + water-profile create + step builder + tap-to-mark"
      to: "iPhone PWA at 375px"
      via: "on-device human verification"
      pattern: "manual"
---

<objective>
Phase-close gate for Phase 20: run the full test suite green twice in the baked container,
pass both ruff gates, and execute the three documented manual-only device verifications
(timer accuracy across screen sleep, coach-feel/pre-cue, 375px polish) on John's physical
iPhone PWA. Record results in 20-VERIFICATION.md.

Purpose: GBREW-01 (timer accurate on wake), GBREW-02 (coach feel), and GBREW-05 (375px
polish bar) cannot be proven by the headless harness; they require on-device confirmation.
This plan also catches the recurring "tests green by skip" and "executors skip ruff, CI
gates both" traps before the work is declared done.
Output: 20-VERIFICATION.md with evidence; a clean full suite + ruff state ready for
/gsd-verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-guided-brew-polish/20-VALIDATION.md
@.planning/phases/20-guided-brew-polish/20-UI-SPEC.md
@.planning/phases/20-guided-brew-polish/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + ruff gate (automated, run before the human checkpoint)</name>
  <files>.planning/phases/20-guided-brew-polish/20-VERIFICATION.md</files>
  <read_first>
    - CLAUDE.md (test loop: pytest not baked, cp vs rebuild, baked-tree gotcha; ruff format + ruff check both gated by CI)
    - .planning/phases/20-guided-brew-polish/20-VALIDATION.md (full-suite command, sampling, run-twice isolation expectation)
    - memory: tests-pass-by-skip-mask-green, full-suite-test-isolation-gaps, executors-skip-ruff-ci-gates-both, snobbery-test-gate-runtime
  </read_first>
  <action>
    Rebuild the image so the baked tree reflects all Phase 20 changes (docker compose build coffee-snobbery && docker compose up -d coffee-snobbery), then install the test deps into the running container (pip install --user pytest pytest-asyncio respx) or use the coffee-snobbery-test profile per CLAUDE.md.
    Run the full suite with -rs to surface skips: python -m pytest -q -rs. Confirm NO Phase 20 test is skipped (skips that hide missing behavior are gaps, memory: tests pass-by-skip). Investigate any skip/xfail and either fix or document.
    Run the full suite a SECOND time to catch cross-module isolation pollution (memory: full-suite-test-isolation-gaps; drop snobbery_test DB first if needed); both runs must be green.
    Run both ruff gates exactly as CI does: ruff format --check . and ruff check . ; fix any findings and commit a style() change if needed (memory: executors skip ruff, CI gates both).
    Record the suite pass counts and ruff results into 20-VERIFICATION.md under an "Automated Gate" section.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q -rs && docker compose exec coffee-snobbery sh -c "ruff format --check . && ruff check ."</automated>
  </verify>
  <acceptance_criteria>
    - Full suite is green on two consecutive runs in the baked container; no Phase 20 test is skipped (pytest -rs shows no phase20 skips).
    - ruff format --check . and ruff check . both exit 0.
    - 20-VERIFICATION.md records the pass counts and ruff results.
  </acceptance_criteria>
  <done>The full suite is green twice with no pass-by-skip, both ruff gates pass, and the evidence is recorded; the automated half of the phase gate is closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: On-device manual verification (timer accuracy, coach feel, 375px polish, water profile)</name>
  <files>.planning/phases/20-guided-brew-polish/20-VERIFICATION.md</files>
  <action>
    Hand the build to John for on-device verification on his physical iPhone PWA and record the
    PASS/FAIL outcome of each of the four checks below into 20-VERIFICATION.md. This task is the
    human gate for the three manual-only verifications in 20-VALIDATION.md (timer accuracy on
    wake, coach feel, 375px polish) plus a water-profile round-trip. Do not auto-advance; any
    FAIL routes to a gap-closure plan via /gsd-plan-phase 20 --gaps.
  </action>
  <what-built>
    Phase 20 Guided Brew polish: wall-clock-truth timer (sleep-proof), full coach view with
    3-2-1 pre-cue + audio/vibration/visual cues, live first-drip tap-to-mark + auto bloom,
    named water-profile inline select-or-create on the brew form, typed recipe steps
    (Bloom/Pour/Wait/Action) with per-step note + temp, and optional first-drip/bloom inputs
    on any session. All automated tests + ruff are green (Task 1).
  </what-built>
  <how-to-verify>
    On John's physical iPhone, using the INSTALLED PWA (do a "Clear site data" / fresh install
    first so the service worker serves the rebuilt assets, memory: SW stale cache confounds UI verify):

    1. TIMER ACCURACY (GBREW-01 / D-15): Start a Guided Brew on a multi-step recipe. During the
       bloom, press the side button to sleep the screen for ~30s, then wake. Confirm: elapsed
       equals real wall-clock time (no ~30s deficit), the current step has advanced if its time
       passed, and there is no double-count or skipped/duplicated cue.
    2. COACH FEEL (GBREW-02 / D-10/D-11): Run the recipe through all steps. Confirm: the 3-2-1
       pre-cue appears a few seconds before each transition; audio tone + vibration + visual
       change all fire AT the transition; the coach view shows step type, coaching line
       (e.g. "Pour 2 - to 250g"), per-step note/temp when present, cumulative water target,
       total elapsed, and next-step preview; bloom auto-records; "Mark first drip" records and
       can be cleared.
    3. 375px POLISH (GBREW-05 / D-16): At 375px and on the PWA, inspect the coach view, the
       recipe step builder (type/note/temp fields, Wait/Action dimming), the water-profile
       inline create, and the tap-to-mark buttons. Confirm: 44px secondary / 56px primary touch
       targets, px-6 padding, Phase 15 safe-area correct (bottom action clear of the home
       indicator), and NO horizontal scroll on any of these surfaces.
    4. WATER PROFILE (GBREW-04): On the brew form, pick "Add new...", create a profile, confirm
       it auto-selects; reload and confirm it persists in the select and a saved session keeps it.

    Record PASS/FAIL per item (with any defects) into 20-VERIFICATION.md.
  </how-to-verify>
  <acceptance_criteria>
    Each item is a discrete PASS/FAIL recorded in 20-VERIFICATION.md; a "clean pass" requires ALL four:
    - TIMER (GBREW-01/D-15): after a ~30s screen sleep mid-bloom, on-wake elapsed matches wall-clock within 1s, the active step advanced if its time passed, no double-count or skipped/duplicated cue. FAIL if any visible deficit or drift.
    - COACH (GBREW-02/D-10/D-11): 3-2-1 pre-cue fires before every transition; audio + vibration + visual all fire AT the transition; coach view shows step type, coaching line, per-step note/temp when present, cumulative water target, total elapsed, next-step preview; bloom auto-records; first-drip tap records and clears. FAIL if any cue missing or any coach-view element absent.
    - 375px POLISH (GBREW-05/D-16): on coach view, step builder, inline water-profile create, and tap-to-mark — 44px secondary / 56px primary targets, px-6 padding, Phase 15 safe-area clear of the home indicator, and ZERO horizontal scroll on every surface. FAIL on any clipped target, unsafe inset, or horizontal scroll.
    - WATER PROFILE (GBREW-04): inline "Add new..." creates + auto-selects a profile, it persists across reload, and a saved session retains it. FAIL if create, auto-select, or persistence breaks.
    Any FAIL routes to /gsd-plan-phase 20 --gaps; do not mark the phase complete on a partial pass.
  </acceptance_criteria>
  <verify>
    <human-check>All four on-device checks recorded PASS in 20-VERIFICATION.md (timer-accuracy, coach-feel, and 375px-polish are the manual-only verifications named in 20-VALIDATION.md).</human-check>
  </verify>
  <done>The three manual-only verifications plus the water-profile round-trip are confirmed on a physical iPhone PWA and recorded PASS in 20-VERIFICATION.md, or defects are routed to gap closure.</done>
  <resume-signal>Type "approved" if all four pass, or describe the defects to route into a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device/PWA -> verification | Human-in-the-loop confirmation of behaviors not reproducible headless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-19 | Repudiation | unverified manual claims | mitigate | Results recorded per-item in 20-VERIFICATION.md with PASS/FAIL + defect notes (no blanket "looks good") |
| T-20-SC | Tampering | npm/pip installs | accept | No new packages; only the documented pytest/respx test deps installed into the container per CLAUDE.md |
</threat_model>

<verification>
- Full suite green twice in the baked container; no phase20 skips; both ruff gates pass.
- Three manual device verifications recorded PASS in 20-VERIFICATION.md (or defects routed to gap closure).
- Service worker refreshed (Clear site data) before trusting any on-device UI verdict.
</verification>

<success_criteria>
Phase 20 is verifiably complete: the full suite is green twice with no pass-by-skip, both
ruff gates pass, and the timer-accuracy, coach-feel, and 375px-polish manual verifications
are confirmed on a physical iPhone PWA and recorded.
</success_criteria>

<output>
Create `.planning/phases/20-guided-brew-polish/20-06-SUMMARY.md` when done.
Record manual results in `.planning/phases/20-guided-brew-polish/20-VERIFICATION.md`.
</output>
