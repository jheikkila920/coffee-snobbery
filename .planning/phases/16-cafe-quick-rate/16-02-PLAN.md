---
phase: 16-cafe-quick-rate
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/schemas/cafe_log.py
  - app/services/cafe_logs.py
  - app/routers/cafe_logs.py
  - app/main.py
  - tests/services/test_cafe_logs.py
  - tests/routers/test_cafe_logs.py
autonomous: true
requirements: [CAFE-01, CAFE-02, CAFE-03, CAFE-06]
tags: [snobbery, fastapi, pydantic, sqlalchemy, csrf, idor, htmx, autocomplete]

must_haves:
  truths:
    - "Per D-10: routes exist at GET /cafe-logs/new, POST /cafe-logs, GET /cafe-logs/{id}/edit, POST /cafe-logs/{id} (update), GET /cafe-logs/origin-country-autocomplete"
    - "Per D-10 + CAFE-06: POST /cafe-logs/{id} with hidden _method=DELETE form field removes the row and returns 200 (HTMX 2.x convention per CLAUDE.md § 3.2; cross-user delete returns 404 — IDOR non-leak)"
    - "Per D-03: GET /cafe-logs/origin-country-autocomplete returns merged distinct values from coffee_origins.country + seeded country list (NO + Create new affordance)"
    - "Per CAFE-06 + RESEARCH.md Pattern 4: cross-user reads of /cafe-logs/{id}/edit and cross-user POST update return HTTP 404 (existence non-leak, NEVER 403)"
    - "Per ASVS V5 + RESEARCH.md Pattern 3: CafeLogCreate and CafeLogUpdate declare model_config = ConfigDict(extra=\"forbid\"); user_id and photo_filename are NOT schema fields (mass-assignment defense T-05-16)"
    - "Per CAFE-01: POST /cafe-logs with payload {cafe_name, rating} succeeds (only those two fields are required-ish)"
    - "Per CAFE-02: POST /cafe-logs accepts roaster_id, origin_country, brew_method, flavor_note_ids, notes, logged_at, and an optional photo UploadFile"
    - "Validation error path returns HTTP 200 + re-render of pages/cafe_log_form.html (NEVER 422 — HTMX swaps only 2xx) per RESEARCH.md Pattern 4 / app/routers/brew.py:981-1005"
  artifacts:
    - path: "app/schemas/cafe_log.py"
      provides: "CafeLogCreate + CafeLogUpdate Pydantic v2 schemas with extra=\"forbid\""
      contains: "class CafeLogCreate"
    - path: "app/services/cafe_logs.py"
      provides: "create_cafe_log / get_cafe_log / list_cafe_logs / update_cafe_log / delete_cafe_log — all taking by_user_id kwarg"
      contains: "def create_cafe_log"
    - path: "app/routers/cafe_logs.py"
      provides: "APIRouter with the five page/state routes + origin-country autocomplete; reuses _parse_form_payload + _hydrate_form_context structure from brew.py"
      contains: "router = APIRouter(prefix=\"/cafe-logs\")"
    - path: "app/main.py"
      provides: "app.include_router(cafe_logs_router.router) line registering the new router"
      contains: "cafe_logs_router"
    - path: "tests/services/test_cafe_logs.py"
      provides: "service-layer CRUD + IDOR + photo orchestration tests, gated on _require_cafe_logs_table()"
      contains: "def test_create_cafe_log"
    - path: "tests/routers/test_cafe_logs.py"
      provides: "router CSRF + IDOR + multipart + layout query-param tests"
      contains: "def test_create_minimal_payload"
  key_links:
    - from: "app/routers/cafe_logs.py"
      to: "app/services/cafe_logs.py"
      via: "service function calls (create_cafe_log/get_cafe_log/list_cafe_logs/update_cafe_log/delete_cafe_log)"
      pattern: "cafe_logs_service\\.(create|get|list|update|delete)_cafe_log"
    - from: "app/routers/cafe_logs.py"
      to: "app/services/photos.py"
      via: "photos.process_and_save(raw_bytes) call from POST handlers"
      pattern: "photos\\.process_and_save"
    - from: "app/services/cafe_logs.py"
      to: "app/models/cafe_log.py"
      via: "from app.models.cafe_log import CafeLog"
      pattern: "from app.models.cafe_log import CafeLog"
    - from: "app/main.py"
      to: "app/routers/cafe_logs.py"
      via: "app.include_router(cafe_logs_router.router) at module init"
      pattern: "include_router.*cafe_logs"
---

<objective>
Stand up the service + router + Pydantic schema layer for cafe logs, mirroring the brew_sessions slice verbatim. This plan ships the API contract that the form template (16-03) and the list/tab view (16-04) will consume. It also wires the new origin-country autocomplete endpoint (the only net-new endpoint shape in Phase 16).

Purpose: Without service + router scaffolds, the form template has nothing to POST to; without the autocomplete endpoint, the form's origin country field has no suggestions. Lands CSRF, IDOR-404, and mass-assignment defenses (RESEARCH.md Security Domain) once so 16-03 and 16-04 don't re-derive them.
Output: A self-contained `/cafe-logs` router registered in main.py, a Pydantic schema with extra="forbid", a service layer that takes by_user_id kwargs on every call, and a paired test pair (services + routers) that gates on `_require_cafe_logs_table()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cafe-quick-rate/16-CONTEXT.md
@.planning/phases/16-cafe-quick-rate/16-RESEARCH.md
@.planning/phases/16-cafe-quick-rate/16-PATTERNS.md
@.planning/phases/16-cafe-quick-rate/16-VALIDATION.md
@.planning/phases/16-cafe-quick-rate/16-01-PLAN.md
@app/schemas/brew_session.py
@app/services/brew_sessions.py
@app/services/photos.py
@app/services/flavor_notes.py
@app/routers/brew.py
@app/routers/bags.py
@app/routers/coffees.py
@app/main.py
@app/models/cafe_log.py
@app/models/coffee_origin.py
@tests/routers/test_brew_router.py
@tests/services/test_brew_sessions_service.py

<interfaces>
Key references the executor needs (do not re-derive — these are the source of truth):

From `app/routers/brew.py`:
- `_NON_SCHEMA_FORM_KEYS` (line 123): the set of form keys the autocomplete/chip widgets emit but the schema doesn't accept (CSRF token, *_query, layout, _method)
- `_parse_form_payload` (line 156): converts FormData → (raw_view, schema_input); strips _NON_SCHEMA_FORM_KEYS; coerces empty strings to None for _EMPTY_TO_NONE_FIELDS; coerces _INT_FIELDS to int
- `_hydrate_form_context` (line 276): builds the form-page context dict; layout="desktop" + edit mode → target="#brew-form-mount"/swap="innerHTML"; the cafe-logs analog routes to "#cafe-form-mount"
- `new_brew_form` (line 657): GET handler; calls _hydrate_form_context with mode="create"
- `create_brew` (line 815): POST handler; calls _parse_form_payload then schema validation; on ValidationError → _render_form_error (HTTP 200); on success → Response(status_code=204, headers={"HX-Redirect": _LIST_URL})
- `edit_brew_form` (line 865): GET handler; 404 on cross-user (raise HTTPException(status_code=404) — non-leak)
- `update_brew` (line 935): POST handler; branches on form._method == "DELETE" (HTMX 2.x convention)

From `app/schemas/brew_session.py`:
- model_config = ConfigDict(extra="forbid")
- rating: Decimal | None = Field(None, ge=0, le=5, multiple_of=Decimal("0.25"))
- list[int] FK ids use Field(default_factory=list)
- user_id and photo_filename are absent by design (mass-assignment defense)

From `app/services/photos.py`:
- `process_and_save(raw_bytes: bytes) -> str` (line 170) — magic-byte + Pillow verify + re-encode + EXIF strip + UUID4 filename; raises PhotoRejected on bad input

From `app/routers/bags.py:367-418`:
- Photo upload pattern: read UploadFile, await .read() into raw bytes, pass to photos.process_and_save(), store returned filename

From `app/main.py:258-278`:
- Router registration block; add `from app.routers import cafe_logs as cafe_logs_router` near other router imports, and `app.include_router(cafe_logs_router.router)` in the registration block (placement: alongside brew_router for symmetry)

From `app/models/coffee_origin.py`:
- `coffee_origins.country` is a plain Text column (not CITEXT); query distinct values with `select(CoffeeOrigin.country).where(...).distinct().order_by(...)` for the origin-country autocomplete source per D-03
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CafeLogCreate/Update Pydantic schemas + cafe_logs service layer</name>
  <files>app/schemas/cafe_log.py, app/services/cafe_logs.py</files>
  <read_first>
    - app/schemas/brew_session.py (the canonical extra="forbid" + Decimal + list[int] pattern)
    - app/services/brew_sessions.py (kwargs-after-`*` CRUD signature; by_user_id; IDOR sentinel returns None on cross-user; single-commit per write)
    - app/models/cafe_log.py (the model shipped in plan 16-01)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "2. app/schemas/cafe_log.py" + § "3. app/services/cafe_logs.py"
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md Pattern 3 (schema) + "Cafe log service module — list / create stub" code example
    - .planning/phases/16-cafe-quick-rate/16-CONTEXT.md § "Claude's discretion — column shapes" + § "AI integration mechanics" (audit log decision)
  </read_first>
  <behavior>
    - Posting a payload that includes `user_id` or `photo_filename` (mass-assignment attempt) raises Pydantic ValidationError per extra="forbid".
    - `rating=Decimal("4.25")` validates; `rating=Decimal("4.30")` (not a 0.25 step) raises ValidationError.
    - `service.create_cafe_log(db, by_user_id=1, cafe_name="Onyx", rating=None)` succeeds with all enrichment fields defaulting to None / "" / [].
    - `service.get_cafe_log(db, cafe_log_id=row.id, by_user_id=OTHER_USER_ID)` returns None (IDOR sentinel — router maps to 404).
    - `service.update_cafe_log(db, cafe_log_id=row.id, by_user_id=OTHER_USER_ID, cafe_name="X")` returns None and does NOT mutate the row.
    - `service.delete_cafe_log(db, cafe_log_id=row.id, by_user_id=OTHER_USER_ID)` returns False and does NOT delete; with the owner returns True and the row is gone.
    - `service.list_cafe_logs(db, by_user_id=user.id, rating_min=Decimal("4.0"))` filters to rows with rating >= 4.0; date_from/date_to filter on logged_at; default sort is logged_at DESC.
  </behavior>
  <action>
    Create `app/schemas/cafe_log.py` with:
    - `class CafeLogCreate(BaseModel)` declaring `model_config = ConfigDict(extra="forbid")` and these fields with EXACT validation: `cafe_name: str = Field(..., min_length=1, max_length=200)`, `rating: Decimal | None = Field(None, ge=0, le=5, multiple_of=Decimal("0.25"))`, `roaster_id: int | None = Field(None, ge=1)`, `origin_country: str | None = Field(None, max_length=100)`, `brew_method: str | None = Field(None, max_length=100)`, `flavor_note_ids: list[int] = Field(default_factory=list)`, `notes: str = Field("", max_length=5000)`, `logged_at: datetime | None = None`.
    - `class CafeLogUpdate(CafeLogCreate)` with a docstring "Same shape today — split lets a future Update path diverge." (mirrors brew_session pattern at app/schemas/brew_session.py:128-134).
    - DO NOT declare `user_id` or `photo_filename` fields (per CONTEXT § Claude's discretion / RESEARCH.md Pattern 3 / ASVS V5 mass-assignment defense T-05-16).

    Create `app/services/cafe_logs.py` exporting CRUD functions with kwargs-after-`*` signatures (mirror app/services/brew_sessions.py:188-209 verbatim, removing brew-specific equipment-counter logic):
    - `create_cafe_log(db, *, by_user_id, cafe_name, rating, roaster_id, origin_country, brew_method, flavor_note_ids, notes, photo_filename, logged_at) -> CafeLog` — `db.add(row); db.commit(); db.refresh(row)`; single transaction; sets `logged_at=datetime.now(UTC)` server-side if the kwarg is None.
    - `get_cafe_log(db, *, cafe_log_id, by_user_id) -> CafeLog | None` — `select(CafeLog).where(CafeLog.id == cafe_log_id, CafeLog.user_id == by_user_id)`; returns scalar_one_or_none() (IDOR sentinel; router maps None → 404).
    - `list_cafe_logs(db, *, by_user_id, rating_min=None, rating_max=None, date_from=None, date_to=None) -> list[CafeLog]` — filters per RESEARCH.md "Cafe log service module" snippet; `order_by(CafeLog.logged_at.desc())`.
    - `update_cafe_log(db, *, cafe_log_id, by_user_id, **fields) -> CafeLog | None` — looks up via get_cafe_log; if None return None; else `setattr(row, key, value)` for known keys, `db.commit(); db.refresh(row)`; return row.
    - `delete_cafe_log(db, *, cafe_log_id, by_user_id) -> bool` — looks up via get_cafe_log; if None return False; else `db.delete(row); db.commit()`; return True. NO equipment counter logic, NO _sync_coffee_flavor_notes (cafe has no parent coffee).

    Audit-log decision: per RESEARCH.md § "Open Implementation Tactics for the Planner" item 1 and CONTEXT.md § "Claude's discretion" recommended default ("No at v1"), DO NOT emit structlog audit events from the cafe_logs service. No CAFE_LOG_CREATED/UPDATED/DELETED constants in app/events.py. Cafe log mutations don't justify the audit-log channel at household scale; if a future maintainer wants it, they can add it cheaply.

    Do NOT add structlog calls inside Pydantic schema files. Do NOT import the model in the schema file. Do NOT add a `coffee_id` kwarg anywhere (CONTEXT D-01). Do NOT modify app/events.py.
  </action>
  <verify>
    <automated>docker compose cp app/schemas/cafe_log.py coffee-snobbery:/app/app/schemas/cafe_log.py &amp;&amp; docker compose cp app/services/cafe_logs.py coffee-snobbery:/app/app/services/cafe_logs.py &amp;&amp; docker compose exec coffee-snobbery python -c "from app.schemas.cafe_log import CafeLogCreate; from app.services.cafe_logs import create_cafe_log, get_cafe_log, list_cafe_logs, update_cafe_log, delete_cafe_log; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/schemas/cafe_log.py` declares `model_config = ConfigDict(extra="forbid")` on CafeLogCreate
    - The schema does NOT declare `user_id` or `photo_filename` fields (grep returns no match)
    - `from app.schemas.cafe_log import CafeLogCreate; CafeLogCreate(cafe_name="X", user_id=1)` raises ValidationError (mass-assignment defense)
    - `CafeLogCreate(cafe_name="", rating=None)` raises ValidationError (min_length=1)
    - `CafeLogCreate(cafe_name="X", rating=Decimal("4.30"))` raises ValidationError (multiple_of=0.25)
    - All five CRUD functions in app/services/cafe_logs.py take `by_user_id` as a keyword-only arg (signature inspection confirms `*` before by_user_id)
    - `get_cafe_log`/`update_cafe_log`/`delete_cafe_log` return None/False on cross-user (verified by a unit test in Task 3)
    - app/events.py is UNCHANGED (grep for CAFE_LOG_ returns no matches in app/events.py — no audit logging added per CONTEXT Claude's-discretion recommended default)
  </acceptance_criteria>
  <done>Schema + service modules import cleanly; the kwargs-after-`*` invariant and the extra="forbid" + missing-user_id/photo_filename invariants are intact; no audit-log event names added (project lands on the CONTEXT-recommended No-at-v1 default).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /cafe-logs router (5 page routes + origin-country autocomplete) + register in main.py</name>
  <files>app/routers/cafe_logs.py, app/main.py</files>
  <read_first>
    - app/routers/brew.py (lines 74-127 for module-level constants; 156-200 for _parse_form_payload; 276-356 for _hydrate_form_context; 480-522 for list_sessions branching shape; 657-727 for new_brew_form; 814-856 for create_brew; 864-921 for edit_brew_form; 935-1005 for update_brew + _render_form_error)
    - app/routers/bags.py:367-418 (the photo upload pattern: read UploadFile, await .read(), pass to photos.process_and_save)
    - app/routers/coffees.py (for autocomplete endpoint shape — varietal-autocomplete; the fragments/autocomplete_list.html context dict)
    - app/services/photos.py (process_and_save signature + PhotoRejected exception type)
    - app/models/coffee_origin.py (the CoffeeOrigin model used for the origin-country distinct query)
    - app/main.py:258-278 (existing include_router block to mirror)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "4. app/routers/cafe_logs.py" + Shared Patterns ("Authentication / per-user scoping", "CSRF on state-changing forms", "Error handling", "Structured logging", "Photo upload — single call into the existing pipeline")
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md Pattern 4 (router structure) + Pattern 5 (autocomplete) + Pitfall 2 ("layout" must be in _NON_SCHEMA_FORM_KEYS) + Pitfall 11 (cafe_logs must be registered in main.py)
  </read_first>
  <behavior>
    - GET /cafe-logs/new returns HTTP 200 + renders pages/cafe_log_form.html with autofocus on cafe_name (template lands in Plan 16-03; this plan ships the route + context dict only).
    - POST /cafe-logs with valid payload {cafe_name, rating} returns 204 + `HX-Redirect: /brew?tab=cafe` (per RESEARCH.md Pattern 4 success-redirect snippet).
    - POST /cafe-logs with `cafe_name=""` returns HTTP 200 + re-renders form with `errors.cafe_name = "Coffee name is required."` (per UI-SPEC.md copy; NEVER 422 — HTMX swaps only 2xx).
    - POST /cafe-logs with `user_id=99` in form data returns HTTP 200 + re-renders form with the form-level error (extra="forbid" trips, normalized to `_form`).
    - POST /cafe-logs with an oversize/bad-magic photo blob returns HTTP 200 + re-renders form with `errors.photo`.
    - GET /cafe-logs/{id}/edit for another user's cafe log returns 404 (NOT 403 — existence non-leak per RESEARCH.md Pattern 4 + ASVS V4).
    - GET /cafe-logs/{id}/edit?layout=desktop returns HTTP 200 + renders the form fragment (innerHTML target #cafe-form-mount, no full base.html shell).
    - POST /cafe-logs/{id} with `_method=DELETE` for an owned row deletes it and returns the appropriate HTMX swap response (mirror brew session delete shape — empty body or OOB swap).
    - POST /cafe-logs/{id} with `_method=DELETE` for another user's row returns 404.
    - GET /cafe-logs/origin-country-autocomplete?q=eth returns the autocomplete_list.html fragment with `items=[{"id": "Ethiopia", "name": "Ethiopia"}]` (or similar entries from coffee_origins.country distinct + seeded list); the response context has `create_new_endpoint=""` so the dropdown does NOT show a "+ Create new" affordance (per D-03).
    - Every state-changing route enforces CSRF via the existing global middleware (no per-route bypass).
    - Every route has `Depends(require_user)` (ASVS V2/V4 inherited posture).
  </behavior>
  <action>
    Create `app/routers/cafe_logs.py` with the structure mirrored from `app/routers/brew.py` (PATTERNS.md § 4 lays out the file-level shape). The module declares:
    - `router = APIRouter(prefix="/cafe-logs")`
    - `_LIST_URL = "/brew?tab=cafe"` (per CONTEXT D-11 success redirect)
    - `_FORM_FIELDS = {"cafe_name", "rating", "roaster_id", "origin_country", "brew_method", "flavor_note_ids", "notes", "logged_at"}`
    - `_EMPTY_TO_NONE_FIELDS = {"rating", "roaster_id", "origin_country", "brew_method", "logged_at"}`
    - `_INT_FIELDS = {"roaster_id"}`
    - `_NON_SCHEMA_FORM_KEYS = {"X-CSRF-Token", "roaster_query", "flavor_note_query", "origin_country_query", "layout", "_method"}` — `"layout"` is MANDATORY here per Pitfall 2 (without it, ?layout=desktop driver field trips extra="forbid")
    - `_SEEDED_COUNTRIES = ("Ethiopia", "Kenya", "Colombia", "Brazil", "Guatemala", "Costa Rica", "Honduras", "Panama", "Peru", "Mexico", "Indonesia", "Yemen", "Rwanda", "Burundi", "Tanzania", "El Salvador", "Nicaragua", "Ecuador", "Bolivia")` per RESEARCH.md Pattern 5

    Implement these handlers (every handler depends on `require_user` + `get_session` — mirror brew.py signatures exactly):
    1. `@router.get("/new", response_class=HTMLResponse)` `new_cafe_log_form(...)` — calls `_hydrate_form_context(db, user=user, values={}, errors={}, mode="create", layout=None)` and renders pages/cafe_log_form.html (autofocus is a template concern, lands in 16-03).
    2. `@router.post("", response_class=HTMLResponse)` `async def create_cafe_log(...)` — reads `form_data = await request.form()`; calls `_parse_form_payload(form_data)`; tries `CafeLogCreate(**schema_input)`; on ValidationError → `_render_form_error(...)` returning HTTP 200 with the form re-rendered. On schema success, handles the optional photo: if `photo` in form_data and is a non-empty UploadFile, `raw_bytes = await photo.read()`; try `photo_filename = photos.process_and_save(raw_bytes)`; on `photos.PhotoRejected` → re-render with `errors["photo"] = str(exc) or "Photo must be JPEG, PNG, or WebP under 10 MB."` (UI-SPEC copy). Then calls `cafe_logs_service.create_cafe_log(db, by_user_id=user.id, cafe_name=..., photo_filename=photo_filename, ...)`. Returns `Response(status_code=204, headers={"HX-Redirect": _LIST_URL})`.
    3. `@router.get("/{cafe_log_id}/edit", response_class=HTMLResponse)` `edit_cafe_log_form(cafe_log_id: int, request: Request, ...)` — `row = cafe_logs_service.get_cafe_log(db, cafe_log_id=cafe_log_id, by_user_id=user.id)`; if row is None `raise HTTPException(status_code=404)`; populate values from row (cafe_name, rating, roaster_id, origin_country, brew_method, flavor_note_ids, notes, logged_at); read `qp.get("layout")` and pass to `_hydrate_form_context(..., mode="edit", cafe_log_id=cafe_log_id, layout=...)`. Render pages/cafe_log_form.html (template branches on layout to suppress `{% extends "base.html" %}` per UI-SPEC § Cafe form mount).
    4. `@router.post("/{cafe_log_id}", response_class=HTMLResponse)` `async def update_cafe_log_handler(cafe_log_id: int, request: Request, ...)` — first reads `form_data`; if `form_data.get("_method") == "DELETE"`, branches to delete path: `ok = cafe_logs_service.delete_cafe_log(db, cafe_log_id=cafe_log_id, by_user_id=user.id)`; if ok False `raise HTTPException(status_code=404)`; else return an empty HTMLResponse with `hx-swap-oob`-compatible body (mirror brew session delete response shape from app/routers/brew.py). Otherwise (normal update): same flow as create but service call is `update_cafe_log(db, cafe_log_id=..., by_user_id=user.id, **schema.model_dump(exclude_none=False))`; on None return raise HTTPException(404); on success return 204 + `HX-Redirect: _LIST_URL`.
    5. `@router.get("/origin-country-autocomplete", response_class=HTMLResponse)` `origin_country_autocomplete(request: Request, user: User = Depends(require_user), db: Session = Depends(get_session))` — implements RESEARCH.md Pattern 5 origin_country_autocomplete snippet: read `query = (request.query_params.get("q") or "").strip()`; if `len(query) < 2` return autocomplete_list.html with empty items; else `db_countries = db.execute(select(CoffeeOrigin.country).where(CoffeeOrigin.country.ilike(f"{query}%")).distinct().order_by(CoffeeOrigin.country).limit(50)).scalars().all()`; merge with prefix-matching seeded countries; render fragments/autocomplete_list.html with `context = {"items": [...], "query": query, "entity": "country", "exact_match": ..., "create_new_endpoint": ""}` — empty create_new_endpoint disables the "+ Create new" affordance per D-03.

    Implement `_parse_form_payload(form_data)` verbatim from app/routers/brew.py:156-200 (PATTERNS.md § 4), renaming the chip-list key handling from `flavor_note_ids_observed` to `flavor_note_ids`. Implement `_hydrate_form_context(db, *, user, values, errors, mode, cafe_log_id=None, layout=None)` per PATTERNS.md § 4 (D-21 dispatch: layout="desktop" + edit → "#cafe-form-mount" / innerHTML; edit without layout → "closest [data-row]" / outerHTML; create → "#cafe-form-mount" / innerHTML). Implement `_resolve_flavor_notes(db, ids)` and `_resolve_roaster_name(db, roaster_id)` helpers (mirror the equivalent from brew.py for chip + roaster pre-hydration). Implement `_normalize_errors(errors_by_field)` + `_render_form_error(...)` mirroring app/routers/brew.py:981-1005 exactly.

    Modify `app/main.py`: add `from app.routers import cafe_logs as cafe_logs_router` near the other router imports, and add `app.include_router(cafe_logs_router.router)` in the registration block (place it adjacent to `brew_router` registrations for symmetry — between lines 272-274 of the current file).

    Do NOT use `@router.delete(...)` (HTMX 2.x convention per CLAUDE.md § 3.2 is POST + hidden `_method=DELETE`; matches existing brew session delete). Do NOT add a "+ Create new" path to the origin-country autocomplete (D-03 rejects a countries lookup table). Do NOT skip CSRF on any state-changing route. Do NOT pass `user_id` into the Pydantic schema (mass-assignment defense — schema doesn't declare the field anyway, but defensive: the service call sets `by_user_id=user.id` from the dependency, never from form data). Do NOT emit audit-log events from the router (per Task 1 — No-at-v1 default).
  </action>
  <verify>
    <automated>docker compose cp app/routers/cafe_logs.py coffee-snobbery:/app/app/routers/cafe_logs.py &amp;&amp; docker compose cp app/main.py coffee-snobbery:/app/app/main.py &amp;&amp; docker compose restart coffee-snobbery &amp;&amp; sleep 3 &amp;&amp; docker compose exec coffee-snobbery python -c "from app.main import create_app; app = create_app(); routes = [r.path for r in app.routes]; assert '/cafe-logs/new' in routes, routes; assert '/cafe-logs' in routes, routes; assert '/cafe-logs/{cafe_log_id}/edit' in routes, routes; assert '/cafe-logs/{cafe_log_id}' in routes, routes; assert '/cafe-logs/origin-country-autocomplete' in routes, routes; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `/openapi.json` lists the five routes: GET /cafe-logs/new, POST /cafe-logs, GET /cafe-logs/{cafe_log_id}/edit, POST /cafe-logs/{cafe_log_id}, GET /cafe-logs/origin-country-autocomplete (Pitfall 11 negative test — none return 404)
    - `_NON_SCHEMA_FORM_KEYS` in app/routers/cafe_logs.py contains the literal string "layout" (Pitfall 2)
    - `_NON_SCHEMA_FORM_KEYS` in app/routers/cafe_logs.py contains the literal string "_method"
    - `_LIST_URL = "/brew?tab=cafe"` appears as a module-level constant
    - The POST handlers do NOT use `@router.delete` (grep for `@router\.delete` returns no match — HTMX 2.x POST + _method=DELETE convention)
    - Every route handler has `user: User = Depends(require_user)` in its signature (grep confirms ≥5 occurrences of `Depends(require_user)` in the new file)
    - app/main.py contains both `from app.routers import cafe_logs as cafe_logs_router` (or equivalent) AND `app.include_router(cafe_logs_router.router)` (grep confirms both)
    - The origin-country autocomplete handler renders fragments/autocomplete_list.html with `create_new_endpoint=""` in the context dict (grep on the file finds the literal empty string assignment)
  </acceptance_criteria>
  <done>The five routes are registered and importable; main.py wiring is in place; CSRF + IDOR + mass-assignment defenses are coded; "layout" is in _NON_SCHEMA_FORM_KEYS so the Plan 16-03 desktop edit form will not trip extra="forbid".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service + router test pair (CRUD, IDOR, CSRF, mass-assignment, layout, photo)</name>
  <files>tests/services/test_cafe_logs.py, tests/routers/test_cafe_logs.py</files>
  <read_first>
    - tests/services/test_brew_sessions_service.py (the per-user CRUD test shape)
    - tests/routers/test_brew_router.py (TestClient + login + form-post pattern; CSRF token extraction; multipart upload pattern)
    - tests/conftest.py (the `_require_cafe_logs_table()` helper added in plan 16-01)
    - app/routers/cafe_logs.py (the file you just wrote in Task 2)
    - app/schemas/cafe_log.py + app/services/cafe_logs.py (the schema + service you wrote in Task 1)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "10. tests/services/test_cafe_logs.py" + § "11. tests/routers/test_cafe_logs.py"
    - .planning/phases/16-cafe-quick-rate/16-VALIDATION.md § "Per-Task Verification Map" (the canonical list of test names this task creates)
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md § "Phase Requirements → Test Map" (lines 1480-1507)
  </read_first>
  <behavior>
    - Each test function in `tests/services/test_cafe_logs.py` calls `_require_postgres()` + `_require_cafe_logs_table()` at the top before any DB I/O (project memory `tests-pass-by-skip-mask-green`).
    - Each test function in `tests/routers/test_cafe_logs.py` uses the existing TestClient + login-as-user fixture pattern from tests/routers/test_brew_router.py (do NOT roll a new fixture).
    - Mass-assignment defense test: posting `user_id=99` in the form body produces ValidationError → 200 + form re-render (extra="forbid" trips).
    - IDOR test: a logged-in user A cannot GET, POST/update, or POST/delete a cafe_log owned by user B (every cross-user attempt returns 404 NOT 403).
    - Photo rejection test: posting an oversized/bad-magic byte sequence triggers PhotoRejected and returns 200 + form re-render with `errors.photo` populated.
    - Layout test: GET /cafe-logs/{id}/edit?layout=desktop returns a response body that does NOT contain `<html` (it's the form fragment, not the full page); GET without ?layout=desktop returns a body that DOES contain `<html`.
    - Origin-country autocomplete test: GET /cafe-logs/origin-country-autocomplete?q=eth returns a body containing "Ethiopia" (seeded) and the rendered HTML does NOT contain `+ Create new` (D-03).
  </behavior>
  <action>
    Create `tests/services/test_cafe_logs.py` with the following test functions (every function calls `_require_postgres()` + `_require_cafe_logs_table()` at the top):
    - `test_create_cafe_log_minimal` — creates a row with only cafe_name + rating; asserts row.id is not None and row.user_id == fixture user id.
    - `test_create_cafe_log_full_enrichment` — creates a row with all fields; asserts the row has the posted values.
    - `test_get_cafe_log_owner_returns_row` — owner read returns the row.
    - `test_get_cafe_log_cross_user_returns_none` — IDOR sentinel — cross-user read returns None.
    - `test_list_cafe_logs_rating_filter` — three rows with ratings 3.0 / 4.0 / 5.0; `rating_min=Decimal("4.0")` returns two rows.
    - `test_list_cafe_logs_date_filter` — date_from/date_to filters narrow logged_at range correctly.
    - `test_list_cafe_logs_default_sort_desc` — three rows with monotonically increasing logged_at; assert returned order is DESC.
    - `test_update_cafe_log_owner_updates` — owner update mutates the row and returns it.
    - `test_update_cafe_log_cross_user_returns_none` — cross-user update returns None and the original row is unmodified.
    - `test_delete_cafe_log_owner_returns_true` — owner delete returns True; subsequent get_cafe_log returns None.
    - `test_delete_cafe_log_cross_user_returns_false` — cross-user delete returns False; the row still exists.

    Create `tests/routers/test_cafe_logs.py` with the following 13 test functions owned by THIS plan (using the existing login-as-user fixture from test_brew_router.py):
    - `test_new_form_renders` — GET /cafe-logs/new logged-in returns 200 + body contains `<input` for `name="cafe_name"`. (CAFE-01 per VALIDATION.md.)
    - `test_create_minimal_payload` — POST /cafe-logs with form data {cafe_name=Onyx, rating=4.0, X-CSRF-Token=...} returns 204 + `HX-Redirect: /brew?tab=cafe`. (CAFE-01.)
    - `test_create_full_enrichment` — POST with all fields incl. flavor_note_ids list returns 204; subsequent GET /brew?tab=cafe contains "Onyx" (smoke; full list rendering lands in plan 16-04). (CAFE-02.)
    - `test_create_mass_assignment_rejected` — POST with `user_id=99` returns 200 (form re-render); response body contains form-level error copy. (T-05-16.)
    - `test_create_rating_out_of_range` — POST with rating=Decimal("4.30") returns 200; body contains UI-SPEC copy "Rating must be between 0 and 5 in 0.25 steps." (CAFE-01.)
    - `test_photo_rejection_paths` — POST with an oversize photo (or a polyglot magic-bytes payload) returns 200; body contains UI-SPEC copy "Photo must be JPEG, PNG, or WebP under 10 MB." (CAFE-02 / V12.)
    - `test_cross_user_returns_404` — GET /cafe-logs/{other_user_id}/edit returns 404 not 403. (CAFE-03 / V4.)
    - `test_edit_form_renders` — GET /cafe-logs/{own_id}/edit returns 200 + body contains the stored cafe_name. (CAFE-06.)
    - `test_edit_form_desktop_layout` — GET /cafe-logs/{own_id}/edit?layout=desktop returns 200 + body does NOT contain `<html` (fragment only) AND body DOES contain the literal string `id="cafe-flavor-chips"` (the chip-grid marker introduced by plan 16-03 Task 1 — hard-coded here so plans stay in lockstep). (CAFE-06 / D-21.)
    - `test_update_own_succeeds` — POST /cafe-logs/{own_id} with mutation returns 204 + HX-Redirect /brew?tab=cafe; subsequent get returns updated row. (CAFE-06.)
    - `test_delete_own_succeeds` — POST /cafe-logs/{own_id} with `_method=DELETE` returns 200 and row is gone. (CAFE-06.)
    - `test_delete_cross_user_404` — POST /cafe-logs/{other_user_id} with `_method=DELETE` returns 404. (CAFE-06 / V4.)
    - `test_origin_country_autocomplete` — GET /cafe-logs/origin-country-autocomplete?q=eth returns 200 + body contains "Ethiopia"; body does NOT contain `Create new` (D-03 — no + Create new affordance).

    Test naming convention MUST match the function names listed in 16-VALIDATION.md § "Per-Task Verification Map" lines 45-67 EXACTLY (the verifier scans for those literal test names). The two router tests `test_tab_cafe_renders_list` and `test_empty_state_is_blank` are owned by plan 16-04 (which writes them and the router branch that satisfies them in the same wave) — DO NOT author them here.

    The photo test seeds a deliberately-bad payload (e.g. plain text bytes, or a 11 MB random blob) and asserts the form re-render path; mock or stub `photos.process_and_save` only if the real implementation cannot be exercised against the test container. Read app/services/photos.py lines 170-256 to confirm PhotoRejected exception class name before writing the test.
  </action>
  <verify>
    <automated>docker compose cp tests/ coffee-snobbery:/app/tests/ &amp;&amp; docker compose exec coffee-snobbery python -m pytest tests/services/test_cafe_logs.py tests/routers/test_cafe_logs.py -x -v -rs 2>&amp;1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `tests/services/test_cafe_logs.py` declares at least 10 test functions; every one calls `_require_cafe_logs_table()` at the top
    - `tests/routers/test_cafe_logs.py` declares the 13 test functions listed above (the verifier scans for these literal names — project memory `validation-md-vacuous-k-filters`); the file does NOT declare `test_tab_cafe_renders_list` or `test_empty_state_is_blank` (those are plan 16-04's to author)
    - `pytest --collect-only -k "test_cross_user_returns_404 or test_delete_cross_user_404"` collects exactly 2 tests (IDOR coverage)
    - `pytest --collect-only -k "test_create_mass_assignment_rejected"` collects exactly 1 test (mass-assignment coverage)
    - Running the full test pair against the dev container with all plan-02 code in place returns PASSED for every test (zero SKIPPED with `-rs`; project memory `tests-pass-by-skip-mask-green`)
    - `pytest --collect-only -k "cafe" tests/services/test_cafe_logs.py tests/routers/test_cafe_logs.py` collects at least 23 tests across the two files (≥10 service + 13 router; Pitfall 13 / project memory `validation-md-vacuous-k-filters` defense)
  </acceptance_criteria>
  <done>Both test files exist with the canonical test names this plan owns; tests run green against the dev container; IDOR + CSRF + mass-assignment + photo rejection + layout dispatching all covered by passing assertions (not pytest.skip); plan 16-04's two tab/empty-state tests are deliberately NOT in this file.</done>
</task>

<task type="auto">
  <name>Task 4: Lint clean + commit gate (ruff format + ruff check)</name>
  <files>app/schemas/cafe_log.py, app/services/cafe_logs.py, app/routers/cafe_logs.py, app/main.py, tests/services/test_cafe_logs.py, tests/routers/test_cafe_logs.py</files>
  <read_first>
    - CLAUDE.md § "Code conventions" (ruff format + ruff check; warnings as errors)
    - project memory `executors-skip-ruff-ci-gates-both` (CI gates on BOTH ruff format --check and ruff check)
  </read_first>
  <action>
    Run `docker compose exec coffee-snobbery ruff format` against the six files listed above. Run `docker compose exec coffee-snobbery ruff check --fix` against the same files. Resolve any remaining warnings/errors (do NOT add `# noqa` for real issues; the existing repo uses `noqa: B008` ONLY for FastAPI `Depends(...)` defaults — mirror that convention for any new B008 hits).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery ruff format --check app/schemas/cafe_log.py app/services/cafe_logs.py app/routers/cafe_logs.py app/main.py tests/services/test_cafe_logs.py tests/routers/test_cafe_logs.py &amp;&amp; docker compose exec coffee-snobbery ruff check app/schemas/cafe_log.py app/services/cafe_logs.py app/routers/cafe_logs.py app/main.py tests/services/test_cafe_logs.py tests/routers/test_cafe_logs.py</automated>
  </verify>
  <acceptance_criteria>
    - `ruff format --check` exits 0 on the six files
    - `ruff check` exits 0 on the six files (no W or F warnings)
    - The only `# noqa` directives added are `# noqa: B008` on FastAPI Depends defaults (mirror existing brew.py convention; grep brew.py for the precedent)
  </acceptance_criteria>
  <done>Both ruff gates pass cleanly; CI will not regress after the wave-2 merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → router (POST /cafe-logs, POST /cafe-logs/{id}) | Untrusted form input crosses here; CSRF + extra="forbid" + IDOR-404 are the three layered defenses |
| Browser → router (GET /cafe-logs/origin-country-autocomplete) | Untrusted query string; bound :user_id / parameterized select() prevents SQLi |
| Router → service (cafe_logs_service.*) | Trust boundary for by_user_id scoping — service NEVER trusts a user_id from form data, only from request.state.user.id |
| Service → photos.process_and_save | Trust boundary for photo bytes; existing pipeline does magic-byte check + Pillow verify + re-encode + EXIF strip + decompression-bomb cap (inherited from app/services/photos.py per RESEARCH.md Security Domain V12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering | POST /cafe-logs body | mitigate | model_config = ConfigDict(extra="forbid") on CafeLogCreate; user_id and photo_filename are absent fields (mass-assignment T-05-16 / ASVS V5) |
| T-16-02-02 | Tampering | POST /cafe-logs CSRF | mitigate | Global starlette-csrf middleware enforces X-CSRF-Token header against the csrftoken cookie; hidden input `<input name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken', '') }}">` in form templates (V13) |
| T-16-02-03 | Information Disclosure | GET /cafe-logs/{id}/edit cross-user | mitigate | Service returns None → router raises HTTPException(404). NEVER 403 — existence non-leak per RESEARCH.md Pattern 4 + ASVS V4 |
| T-16-02-04 | Information Disclosure | DELETE /cafe-logs/{id} cross-user | mitigate | Same as T-16-02-03 — 404 on cross-user; service returns False → router raises 404 |
| T-16-02-05 | Tampering | POST /cafe-logs photo bytes | mitigate | photos.process_and_save() reused VERBATIM — magic-byte check, Pillow verify, re-encode, EXIF strip, decompression-bomb cap, UUID4 filename (V12 — inherited from app/services/photos.py) |
| T-16-02-06 | Tampering | GET /cafe-logs/origin-country-autocomplete?q=... | mitigate | SQLAlchemy 2.0 parameterized `select(CoffeeOrigin.country).where(CoffeeOrigin.country.ilike(...))` — no raw SQL; `:q` parameter binding prevents SQLi |
| T-16-02-07 | Information Disclosure | XSS via user notes / cafe_name | mitigate | Jinja2 autoescape ON globally; cafe form templates (plan 16-03) render all user strings via `{{ value }}` (escaped) — V8 / V12 |
| T-16-02-08 | Denial of Service | Rapid form posts | accept | Existing slowapi rate-limit on /login covers session creation; post-auth household-scale concurrency does not warrant a per-route limiter (RESEARCH.md Security Domain) |
| T-16-02-09 | Information Disclosure | Cross-user data via shared autocomplete | accept | Roaster autocomplete + origin-country distinct values are shared-catalog by design (household-scale, intentional); the per-user invariant applies only to cafe_logs ROWS not to catalog lookups (V4 reasoning) |
| T-16-02-10 | Tampering | Spoofed _method=DELETE on POST | mitigate | _method is in _NON_SCHEMA_FORM_KEYS (not validated as a schema field) and is only honored when CSRF token validates; cross-user delete still 404s via IDOR sentinel |
</threat_model>

<verification>
- `python -c "from app.main import create_app; app = create_app(); ..." asserts all 5 routes registered (Pitfall 11 negative test)
- `pytest tests/services/test_cafe_logs.py tests/routers/test_cafe_logs.py -q -rs` passes with zero SKIPs against a dev container that has plan 16-01 merged
- `grep -E "user_id|photo_filename" app/schemas/cafe_log.py` returns no matches (mass-assignment defense — fields absent by design)
- `grep "layout" app/routers/cafe_logs.py` shows the literal string inside _NON_SCHEMA_FORM_KEYS (Pitfall 2 defense)
- `pytest --collect-only -k "cross_user or mass_assignment"` collects ≥3 tests (IDOR + extra="forbid" coverage)
- `ruff format --check` and `ruff check` both exit 0 on every file touched (project memory `executors-skip-ruff-ci-gates-both`)
- `grep -E "CAFE_LOG_(CREATED|UPDATED|DELETED)" app/events.py` returns no matches (audit-log decision: No-at-v1 default per CONTEXT Claude's-discretion recommendation)
</verification>

<success_criteria>
- Five routes registered and discoverable in /openapi.json
- CafeLogCreate/Update schemas declare extra="forbid" and exclude user_id + photo_filename
- Service layer's CRUD all take by_user_id kwarg; cross-user reads/writes return None/False
- Router converts service None → HTTPException(404) (existence non-leak)
- main.py contains the include_router line; restarting the container picks up the new routes
- Test pair (services + routers) runs green with zero SKIPs (Wave 0 covered) — exactly the 23+ tests this plan owns (10 service + 13 router); plan 16-04's two router tests are deliberately absent from this file
- app/events.py UNCHANGED (No-at-v1 audit log per CONTEXT Claude's-discretion default)
- ruff format --check + ruff check green on every file touched
</success_criteria>

<output>
After completion, create `.planning/phases/16-cafe-quick-rate/16-02-SUMMARY.md` documenting:
- Final route list (path + method + name) from /openapi.json
- Test pass count for tests/services/test_cafe_logs.py and tests/routers/test_cafe_logs.py (and the rs-flag output for any SKIPs — should be zero)
- Confirmation that "layout" is in _NON_SCHEMA_FORM_KEYS (Pitfall 2)
- Audit-log decision: chose NO (No-at-v1 per CONTEXT Claude's-discretion default + RESEARCH.md § Open Implementation Tactics #1) — note for future maintainers that adding CAFE_LOG_* events is a two-line delta if business need arises
- Confirmation that plan 16-04's two router tests (`test_tab_cafe_renders_list`, `test_empty_state_is_blank`) are NOT in this plan's test files
- Any deviation from PATTERNS.md § 2-4 / 10-11 with justification
</output>
