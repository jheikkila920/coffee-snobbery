---
phase: 16-cafe-quick-rate
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - app/templates/pages/cafe_log_form.html
  - tests/routers/test_cafe_logs.py
autonomous: true
requirements: [CAFE-01, CAFE-02, CAFE-06]
tags: [snobbery, jinja2, htmx, alpine, tailwind-v3, csp-nonce, mobile-first, autofocus]

must_haves:
  truths:
    - "Per D-10: pages/cafe_log_form.html is a full-page render extending base.html (NOT a modal, NOT inline)"
    - "Per D-11: required fields (cafe_name autofocused + rating + Save button) are visible without scroll at 375x667 viewport; optional fields below the fold"
    - "Per D-11: no server-side draft autosave (no brewDraft scope on this page); localStorage form-restore is acceptable but not required"
    - "Per D-02: roaster field uses the existing /roasters/list autocomplete (+ Create new modal); the chosen roaster_id is the hidden FK input value"
    - "Per D-03: origin_country field uses the new /cafe-logs/origin-country-autocomplete endpoint with NO + Create new affordance — free text plus suggestions"
    - "Per D-04: flavor_note_ids field uses the existing observedFlavorNotes Alpine chip scope (or a renamed mirror); chip widget reuses /flavor-notes/datalist + flavor-note-created HX-Trigger"
    - "Per CAFE-02: photo input is reused via the existing app/static/js/photo-upload.js Canvas downscale handler + app/services/photos.py pipeline; never call PIL.Image directly"
    - "Per CAFE-06 / D-21: when rendered with layout=\"desktop\" + mode=\"edit\", the template suppresses {% extends \"base.html\" %} and the page H1 so the form fragment lands cleanly into #cafe-form-mount via innerHTML"
    - "Per ASVS V13: a hidden CSRF input <input type=\"hidden\" name=\"X-CSRF-Token\" value=\"{{ request.cookies.get('csrftoken', '') }}\"> is present in the form"
    - "Per project memory tojson-attr-quoting-and-live-browser-repro: every data attribute that interpolates |tojson uses SINGLE quotes around the attr value"
  artifacts:
    - path: "app/templates/pages/cafe_log_form.html"
      provides: "Full-page form template + desktop-mount fragment branch + sticky Save bar + all interaction contracts from UI-SPEC §"
      contains: "{% extends \"base.html\" %}"
      min_lines: 100
  key_links:
    - from: "app/templates/pages/cafe_log_form.html"
      to: "/cafe-logs (POST)"
      via: "form action attribute (create mode) or /cafe-logs/{id} (edit mode)"
      pattern: "form_action|/cafe-logs"
    - from: "app/templates/pages/cafe_log_form.html"
      to: "/roasters/list"
      via: "hx-get on the roaster autocomplete input"
      pattern: "hx-get=\"/roasters/list\""
    - from: "app/templates/pages/cafe_log_form.html"
      to: "/cafe-logs/origin-country-autocomplete"
      via: "hx-get on the origin country autocomplete input"
      pattern: "hx-get=\"/cafe-logs/origin-country-autocomplete\""
    - from: "app/templates/pages/cafe_log_form.html"
      to: "/flavor-notes/datalist"
      via: "hx-get on the flavor-note chip-input search; reuses existing observedFlavorNotes Alpine scope"
      pattern: "hx-get=\"/flavor-notes/datalist\""
---

<objective>
Ship the dedicated `/cafe-logs/new` and `/cafe-logs/{id}/edit` form page. Mirrors the brew_form architecture verbatim (single-scroll, required fields on top, sticky Save bar, reused autocomplete + chip + photo widgets) — diverging only where CONTEXT.md says so (no brewDraft autosave scope; origin-country has no + Create new; chip widget docstring clarifies cafe semantics; layout="desktop" branch suppresses the page shell for the D-21 mount).

Purpose: Without the form template, the routes from plan 16-02 have no UI; without the desktop-fragment branch, plan 16-04's Edit-button on the desktop row has nowhere to land. Lands D-10, D-11, the chip + roaster + origin-country autocomplete UX, photo upload reuse, and the dual-Edit form fragment in one template.
Output: A page template that renders correctly at both 375×667 (sticky Save in viewport) and ≥768px (form fragment mounts into #cafe-form-mount via innerHTML); a Playwright assertion in the existing router test file that confirms the sticky-save viewport invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cafe-quick-rate/16-CONTEXT.md
@.planning/phases/16-cafe-quick-rate/16-RESEARCH.md
@.planning/phases/16-cafe-quick-rate/16-PATTERNS.md
@.planning/phases/16-cafe-quick-rate/16-UI-SPEC.md
@.planning/phases/16-cafe-quick-rate/16-VALIDATION.md
@app/templates/pages/brew_form.html
@app/templates/fragments/coffee_form.html
@app/templates/fragments/autocomplete_list.html
@app/templates/base.html
@app/static/css/tailwind.src.css
@app/static/js/photo-upload.js
@app/routers/cafe_logs.py

<interfaces>
Key references the executor needs:

From `app/templates/pages/brew_form.html` (the page template analog):
- lines 1-59: extends base.html + page H1 + outer `<main class="mx-auto max-w-2xl px-6 py-12">`
- lines 112-164: `ratingStars` Alpine scope mount — reuse verbatim, no rename
- lines 285-327: sticky Save bar pattern — `sticky bottom-16 md:bottom-0` + `style="padding-bottom: calc(0.75rem + env(safe-area-inset-bottom))"` (the one allowed inline style — D-20 precedent)

From `app/templates/fragments/coffee_form.html`:
- lines 83-112: roaster autocomplete pattern — `x-data="autocomplete"`, hx-get /roasters/list, hidden input `roaster_id`, dropdown id `roaster-dropdown`
- lines 179-184: chip widget data-initial-chips SINGLE-quoted attr (project memory `tojson-attr-quoting-and-live-browser-repro`)
- lines 263-339: observedFlavorNotes chip widget reuse pattern + /flavor-notes/datalist hx-get + flavor-note-created HX-Trigger

From `app/templates/fragments/autocomplete_list.html`:
- context dict shape: items, query, entity, exact_match, create_new_endpoint — the dropdown hides "+ Create new" when create_new_endpoint is "" (origin-country case per D-03)

From `app/static/css/tailwind.src.css`:
- @layer base input/select/textarea pinned to 16px (iOS zoom-on-focus floor) — inherited automatically
- .htmx-indicator rule (project memory `strict-csp-blocks-htmx-indicator` — confirm the rule was added in Phase 9; if not, this plan adds it)

From `app/static/js/photo-upload.js`:
- The existing Canvas downscale handler; cafe form reuses verbatim by mounting the same `<input type="file" name="photo" accept="image/jpeg,image/png,image/webp">` with the appropriate hooks

From `app/routers/cafe_logs.py` (shipped in plan 16-02):
- context dict keys passed to the template: values, errors, mode, cafe_log_id, form_action, form_target, form_swap, layout, selected_flavor_notes, roaster_name
- The desktop-fragment branch tests against `layout == "desktop"` AND `mode == "edit"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cafe form page template — base structure + autocomplete + chip + photo + sticky Save</name>
  <files>app/templates/pages/cafe_log_form.html</files>
  <read_first>
    - app/templates/pages/brew_form.html (the page template analog — read in full, but lines 1-59 + 112-164 + 285-327 are load-bearing)
    - app/templates/fragments/coffee_form.html (lines 83-112 for roaster autocomplete; lines 179-184 for SINGLE-quoted data-initial-chips attr; lines 263-339 for observedFlavorNotes chip widget reuse)
    - app/templates/fragments/autocomplete_list.html (the context dict shape; the create_new_endpoint="" path that hides + Create new)
    - app/templates/base.html (block names: page_title, content, scripts; confirm how `{% extends "base.html" %}` interacts with `{% block content %}`)
    - app/static/css/tailwind.src.css (confirm input font floor is pinned; verify .htmx-indicator rule exists — if not, raise via Task 2)
    - app/static/js/photo-upload.js (confirm the photo input markup needed to opt into Canvas downscale)
    - app/routers/cafe_logs.py (the context dict keys shipped in plan 16-02 that this template MUST consume)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "5. app/templates/pages/cafe_log_form.html"
    - .planning/phases/16-cafe-quick-rate/16-UI-SPEC.md § "Component Inventory — Cafe form page" + § "Visual Distinction Spec" + § "Copywriting Contract" + § "Interaction Contracts" + § "Accessibility Contract"
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md Pitfall 5 (|tojson attr quoting) + Pitfall 7 (strict CSP blocks htmx-indicator)
  </read_first>
  <behavior>
    - GET /cafe-logs/new renders the form with the cafe_name input first (autofocus attr + Alpine init() .focus() backup), then the ratingStars widget, then a sticky-bottom Save bar. At 375×667 portrait, the Save button's bottom edge falls within the first viewport.
    - GET /cafe-logs/{id}/edit (no layout query param) renders the same page shell with stored values prefilled.
    - GET /cafe-logs/{id}/edit?layout=desktop renders ONLY the form card — no `{% extends "base.html" %}` shell, no page H1, no `<main>` wrapper — so the response body can be swapped into `#cafe-form-mount` via innerHTML.
    - Roaster autocomplete: typing 2+ chars hits `hx-get /roasters/list?q=...`; selecting a result sets hidden `roaster_id`; the existing "+ Create new" modal flow works.
    - Origin country autocomplete: typing 2+ chars hits `hx-get /cafe-logs/origin-country-autocomplete?q=...`; the dropdown shows up to 20 matches and NO "+ Create new" affordance (free text — if user typed something not in the list, the free text submits as-is).
    - Flavor notes chip input: reuses observedFlavorNotes scope verbatim per UI-SPEC § Open Question 1 Option A; data-initial-chips uses SINGLE-quoted attr (Pitfall 5).
    - Photo input: `<input type="file" name="photo" accept="image/jpeg,image/png,image/webp">`; opts into photo-upload.js Canvas downscale via the same hooks as the bag form.
    - CSRF: hidden input `<input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken', '') }}">` present.
    - Server-rendered validation errors appear in `<p class="text-sm text-red-700">` immediately after the offending control AND `aria-describedby` on the input points at the error paragraph id (UI-SPEC § Accessibility).
    - Touch target floor: every tappable control declares `min-h-[44px] min-w-[44px]` on mobile.
    - Sticky Save bar: `sticky bottom-16 md:bottom-0` with `style="padding-bottom: calc(0.75rem + env(safe-area-inset-bottom))"` (the one allowed inline style — D-20 precedent from brew_form).
    - Dark mode: uses `.dark:*` selectors only (NEVER `@custom-variant` — project memory `tailwind-v3-not-v4`).
  </behavior>
  <action>
    Create `app/templates/pages/cafe_log_form.html` mirroring the structure of `app/templates/pages/brew_form.html` (PATTERNS.md § 5 + UI-SPEC § Component Inventory pin every field's identity).

    Top-level branching (UI-SPEC § Cafe form mount + § Open Question 3):
    - If `layout == "desktop"` AND `mode == "edit"`: render the form card ONLY — no `{% extends "base.html" %}`, no page H1, no `<main>` wrapper. Output exactly the inner form `<form>` element + its sibling sticky Save bar (the Save bar at the bottom of the page becomes the bottom of the fragment in this branch).
    - Otherwise (create mode OR edit mode without `layout=desktop`): full-page extend pattern. Set `{% set is_edit = mode == "edit" %}` near the top. `{% block page_title %}` reads "Snobbery — Quick rate" (create) or "Snobbery — Edit cafe tasting" (edit) per UI-SPEC § Copywriting. `{% block content %}` opens `<main class="mx-auto max-w-2xl px-6 py-12">` and renders the page H1 ("Quick rate a coffee" create / "Edit cafe tasting" edit) inside `<h1 class="text-2xl font-semibold mb-6">`.

    Form element:
    - `<form method="post" action="{{ form_action }}" enctype="multipart/form-data" class="...">` — note `enctype="multipart/form-data"` is mandatory for the photo input.
    - Hidden CSRF input: `<input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken', '') }}">` (matches every other state-changing form).
    - Hidden layout input on the desktop edit branch: `{% if layout == "desktop" %}<input type="hidden" name="layout" value="desktop">{% endif %}` — Pitfall 2 already added "layout" to _NON_SCHEMA_FORM_KEYS in plan 16-02, so this is safe to post.

    Field order (UI-SPEC § Component Inventory — "Cafe form page" row gives the exact sequence — single-scroll, required on top per D-11):
    1. Cafe name — `<input name="cafe_name" type="text" required autofocus placeholder="e.g. Onyx Geometry" minlength="1" maxlength="200" value="{{ values.get('cafe_name', '') }}" class="...text-base...min-h-[44px]" aria-describedby="cafe_name-error">`; label "Coffee name" (`<span class="text-sm font-semibold">`); error `<p id="cafe_name-error" class="text-sm text-red-700">{{ errors.get('cafe_name', '') }}</p>`. Add an `<x-data>` mount or page-level Alpine init that calls `$el.focus()` on init() so Safari ignoring the autofocus attr is backstopped (UI-SPEC § Interaction Contracts — autofocus).
    2. Rating — reuse the `ratingStars` Alpine scope from brew_form.html lines 112-164 VERBATIM. Pass `data-initial-rating='{{ values.get('rating') or '' }}'` if needed (mirror brew_form's prop wiring). Output a hidden input `name="rating"`. Star size `w-14 h-14`, touch zone `min-h-[56px]`, half-step zones, `amber-400` fill (per UI-SPEC § Typography + § Color).
    3. Save button — primary CTA inside the sticky Save bar at the bottom of the form (NOT inline here). UI-SPEC § Open Question 2 says recommend the sticky pattern; do not place an inline Save above the optional fields.
    4. Roaster — reuse the autocomplete pattern from coffee_form.html lines 83-112: `<div class="field flex flex-col gap-1" x-data="autocomplete" data-entity-key="roaster" data-hidden-input-name="roaster_id" data-initial-id="{{ values.get('roaster_id') or '' }}" data-initial-name="{{ roaster_name or '' }}">` containing the search `<input name="roaster_query" hx-get="/roasters/list" hx-trigger="..." hx-target="#roaster-dropdown" hx-swap="innerHTML" placeholder="Type 2+ chars; pick or '+ Create new'">`, the hidden `<input type="hidden" name="roaster_id">`, and `<div id="roaster-dropdown" ...></div>`. Label: "Roaster (optional)".
    5. Origin country — NEW autocomplete pattern. Same `autocomplete` Alpine scope but `data-entity-key="country"` and the hidden input is `name="origin_country"` (NOT an id — it's a string), with `data-initial-id` and `data-initial-name` both set to `{{ values.get('origin_country', '') }}`. Search input `hx-get="/cafe-logs/origin-country-autocomplete"`, placeholder "Type 2+ chars for suggestions, or enter your own". The dropdown div id is `origin-country-dropdown`. Per D-03 there is NO "+ Create new" affordance — the autocomplete_list.html fragment from plan 16-02 already renders with `create_new_endpoint=""`. CRITICAL: the input MUST also fall back to the typed free text if the user does not select from the dropdown — read the existing `autocomplete` Alpine scope to confirm if free-text submit works; if not, add `name="origin_country"` directly on the visible input (mirror coffee_form varietal pattern). Label: "Origin country (optional)".
    6. Brew method — plain `<input type="text" name="brew_method" maxlength="100" placeholder="e.g. V60, Aeropress, espresso" value="{{ values.get('brew_method', '') }}" class="...">`. Label: "Brew method (optional)". No autocomplete (D-05 free text).
    7. Flavor notes — reuse `observedFlavorNotes` Alpine scope from coffee_form.html lines 263-339 VERBATIM. Per UI-SPEC § Open Question 1 Option A, do NOT rename — accept the slight semantic stretch. The chip widget mount: `<div id="cafe-flavor-chips" class="field flavor-note-chips-field flex flex-col gap-1" x-data="observedFlavorNotes" data-initial-chips='{{ selected_flavor_notes|tojson }}'>` — SINGLE-quoted attr per Pitfall 5 / project memory `tojson-attr-quoting-and-live-browser-repro`. Inside, the search input fires `hx-get="/flavor-notes/datalist?q=..."`; selecting a chip emits a hidden input `<input type="hidden" name="flavor_note_ids" value="...">` per chip. Label: "Flavor notes (optional)". Add an inline code comment "// Reusing observedFlavorNotes scope per UI-SPEC Q1 Option A — semantic stretch is intentional; the chip widget logic is identical, only the variable name differs from the cafe domain."
    8. Notes — `<textarea name="notes" rows="3" maxlength="5000" class="...">{{ values.get('notes', '') }}</textarea>`. Label: "Notes (optional)".
    9. Photo — `<input type="file" name="photo" accept="image/jpeg,image/png,image/webp" class="...">` — the existing photo-upload.js Canvas downscale handler picks this up automatically if the input shape matches the bag form precedent (read app/static/js/photo-upload.js to confirm). Label: "Photo (optional)". Error: `<p id="photo-error" class="text-sm text-red-700">{{ errors.get('photo', '') }}</p>`.
    10. Tasted at — `<input type="datetime-local" name="logged_at" value="{{ values.get('logged_at')|datetime_local_value if values.get('logged_at') else '' }}" class="...">`. Label: "Tasted at"; helper `<p class="text-sm text-espresso-700 dark:text-cream-200">Defaults to now; edit to backfill a previous tasting.</p>`.

    Sticky Save bar at the bottom of the form (UI-SPEC § Interaction Contracts — Form Save / D-11):
    ```
    <div class="sticky bottom-16 md:bottom-0 inset-x-0 z-20 border-t border-espresso-200 bg-cream-50/95 backdrop-blur px-4 py-3 flex flex-col gap-2 dark:bg-espresso-950/95 dark:border-espresso-800" style="padding-bottom: calc(0.75rem + env(safe-area-inset-bottom))">
      <div class="flex items-center gap-3">
        <a href="/brew?tab=cafe" class="flex-1 inline-flex items-center justify-center rounded border border-espresso-300 px-4 py-2 text-base text-espresso-800 hover:bg-espresso-50 dark:border-espresso-700 dark:text-cream-200">Cancel</a>
        <button type="submit" class="flex-1 inline-flex items-center justify-center rounded bg-espresso-700 px-4 py-2 text-base font-semibold text-cream-50 hover:bg-espresso-800">{% if is_edit %}Save changes{% else %}Save{% endif %}</button>
      </div>
    </div>
    ```
    (The inline `style="padding-bottom: calc(...)"` is the one allowed exception — D-20 brew_form precedent; UI-SPEC § Design System line 26 documents this.)

    Form-level error (extra="forbid" / 500 / unknown): `<p class="text-sm text-red-700">{{ errors.get('_form', '') }}</p>` rendered near the top of the form so a user sees it without scrolling past optional fields.

    Tailwind v3 invariants: `darkMode:'selector'` is already in tailwind.config.js — use `.dark:bg-*`, `.dark:text-*`, `.dark:border-*` selectors. NEVER `@custom-variant`. NEVER inline `<style>` blocks. Compile any new utility class via tailwind.src.css (it already covers the entire utility universe; verify there is no new selector required).

    Do NOT add a brewDraft scope to this page (D-11 rejects autosave). Do NOT introduce a modal pattern (D-10 — full-page only). Do NOT use `|tojson` inside a double-quoted attr (Pitfall 5; project memory). Do NOT use `<style>` blocks or inline `style=` except for the one calc(... env(safe-area-inset-bottom) ...) precedent.

    Verify `.htmx-indicator` rule exists in `app/static/css/tailwind.src.css` (project memory `strict-csp-blocks-htmx-indicator` — Phase 9 may already have added it). If absent, add the three-line rule documented in RESEARCH.md Pitfall 7:
    `.htmx-indicator { display: none; }`
    `.htmx-request .htmx-indicator { display: inline; }`
    `.htmx-request.htmx-indicator { display: inline; }`
  </action>
  <verify>
    <automated>docker compose cp app/templates/ coffee-snobbery:/app/app/templates/ &amp;&amp; docker compose cp app/static/css/ coffee-snobbery:/app/app/static/css/ &amp;&amp; docker compose restart coffee-snobbery &amp;&amp; sleep 3 &amp;&amp; docker compose exec coffee-snobbery python -m pytest tests/routers/test_cafe_logs.py::test_new_form_renders tests/routers/test_cafe_logs.py::test_edit_form_renders tests/routers/test_cafe_logs.py::test_edit_form_desktop_layout -x -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/pages/cafe_log_form.html` exists and contains `{% extends "base.html" %}` (full-page branch)
    - File contains a branch like `{% if layout == "desktop" and mode == "edit" %}` that suppresses the page shell (form-fragment branch)
    - File contains `<input` tags for: name="cafe_name", name="rating" (hidden), name="roaster_id" (hidden), name="roaster_query", name="origin_country", name="origin_country_query", name="brew_method", name="flavor_note_ids" (hidden, per chip), name="notes" (textarea), name="photo" (file), name="logged_at" (datetime-local)
    - The cafe_name input has BOTH the literal `autofocus` attribute AND `required` attribute
    - The CSRF hidden input is present: grep finds `name="X-CSRF-Token"` exactly once in the form
    - The chip widget data-initial-chips uses SINGLE quotes around the attr value (Pitfall 5): grep `data-initial-chips='` succeeds and `data-initial-chips="` returns no match
    - The origin-country autocomplete points at `/cafe-logs/origin-country-autocomplete` (grep finds the hx-get)
    - The roaster autocomplete points at `/roasters/list` (grep finds the hx-get)
    - The sticky Save bar uses `sticky bottom-16 md:bottom-0` AND the `style="padding-bottom: calc(0.75rem + env(safe-area-inset-bottom))"` inline (D-20 precedent)
    - No `@custom-variant` directives in the file (Tailwind v3 invariant; project memory `tailwind-v3-not-v4`)
    - No inline `<style>` blocks; the only `style=` attr is the one calc() exception
    - `pytest tests/routers/test_cafe_logs.py::test_new_form_renders -x` passes (CAFE-01)
    - `pytest tests/routers/test_cafe_logs.py::test_edit_form_renders -x` passes (CAFE-06)
    - `pytest tests/routers/test_cafe_logs.py::test_edit_form_desktop_layout -x` passes (D-21 — the body does NOT contain `<html`)
  </acceptance_criteria>
  <done>The form template renders both full-page and desktop-fragment modes; all 10 required fields are present with correct names; autofocus, CSRF, sticky Save, and single-quoted |tojson attrs are in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright sticky-Save viewport assertion at 375×667</name>
  <files>tests/routers/test_cafe_logs.py</files>
  <read_first>
    - tests/routers/test_cafe_logs.py (the file shipped in plan 16-02 — add the new test function alongside the existing ones)
    - tests/ (search for any existing Playwright test scaffold — Phase 12 baked Playwright at /ms-playwright per RESEARCH.md § Environment Availability; if a Playwright fixture pattern exists, reuse it)
    - .planning/phases/16-cafe-quick-rate/16-UI-SPEC.md § "Interaction Contracts — Form Save (D-11 sticky)" + § "Risks and Open Questions" item 2 (Save button visibility assertion)
    - .planning/phases/16-cafe-quick-rate/16-VALIDATION.md § "Manual-Only Verifications" (the row "Cafe form first viewport: name + rating + Save without scrolling at 375px")
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md § "Environment Availability" (Playwright at /ms-playwright + RESEARCH.md test framework block)
  </read_first>
  <behavior>
    - The new test `test_cafe_form_save_visible_at_375x667` opens a Playwright browser context at viewport 375×667, navigates to /cafe-logs/new (after authenticating), and asserts that the Save button's bounding rect bottom edge falls within `<= 667`. (UI-SPEC § Open Question 2 — sticky save visibility assertion.)
    - The test is decorated with `@pytest.mark.playwright` (or whatever marker the existing Playwright tests use) so it can be skipped via `-m "not playwright"` when Playwright is unavailable.
    - The test does NOT require a live VPS — it runs against the dev container's local FastAPI app (mirroring the existing Phase 12 Playwright responsive smoke).
    - If no Playwright fixture exists yet in the repo, the test gates on a pytest.importorskip("playwright.sync_api") + connectivity check; on missing dependency it pytest.skips with a clear message (NOT a silent pass per project memory `tests-pass-by-skip-mask-green`).
  </behavior>
  <action>
    Inspect the existing test layout for Playwright usage. If the Phase 12 smoke suite uses `playwright.sync_api` with a `browser_context` fixture, reuse the same fixture. If no Playwright tests yet live in tests/routers/ or tests/e2e/, add the test as a regular function-level test in `tests/routers/test_cafe_logs.py` and gate it with `pytest.importorskip("playwright.sync_api")` at the top of the test body.

    Add `test_cafe_form_save_visible_at_375x667` to tests/routers/test_cafe_logs.py:
    - Authenticates as the existing test user fixture.
    - Launches a Chromium context at viewport `{"width": 375, "height": 667}`.
    - Navigates to `http://localhost:8000/cafe-logs/new` (or the existing FastAPI base URL fixture).
    - Locates the Save button via `page.get_by_role("button", name="Save")` (UI-SPEC copy locked).
    - Calls `bbox = save_button.bounding_box()` and asserts `bbox["y"] + bbox["height"] <= 667` with a useful message ("Save button must be visible without scroll at 375x667 per UI-SPEC D-11 — got y={bbox['y']} h={bbox['height']}").
    - Also asserts the cafe_name input is the active element (autofocus): `assert page.evaluate('document.activeElement.name') == "cafe_name"`.
    - Closes the browser context.

    If Playwright is not installed in the test container, the test calls `pytest.importorskip("playwright.sync_api")` which emits a SKIPPED that `pytest -rs` will surface. Do NOT use a bare `try/except` that silently passes — the skip must be visible per project memory `tests-pass-by-skip-mask-green`.

    Do NOT change any other test in the file. Do NOT add a duplicate of test_new_form_renders or similar — those already exist from plan 16-02.
  </action>
  <verify>
    <automated>docker compose cp tests/ coffee-snobbery:/app/tests/ &amp;&amp; docker compose exec coffee-snobbery python -m pytest tests/routers/test_cafe_logs.py::test_cafe_form_save_visible_at_375x667 -x -v -rs 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/routers/test_cafe_logs.py` contains a `test_cafe_form_save_visible_at_375x667` function
    - The test uses Playwright (`playwright.sync_api`) and either reuses an existing fixture or calls `pytest.importorskip("playwright.sync_api")` at top of body
    - The test asserts `bbox["y"] + bbox["height"] <= 667` for the Save button
    - The test asserts `document.activeElement.name == "cafe_name"` (autofocus invariant per UI-SPEC § Interaction Contracts)
    - `pytest --collect-only tests/routers/test_cafe_logs.py::test_cafe_form_save_visible_at_375x667` collects exactly 1 test (project memory `validation-md-vacuous-k-filters` defense)
    - If Playwright is unavailable, the test SKIPS with a message containing "playwright" (visible under -rs)
    - If Playwright is available, the test PASSES (Save button visible at 375×667)
  </acceptance_criteria>
  <done>The Playwright assertion exists and either passes against the dev container (Playwright available) or skips visibly with a clear reason.</done>
</task>

<task type="auto">
  <name>Task 3: Lint clean + commit gate (ruff format + ruff check)</name>
  <files>app/templates/pages/cafe_log_form.html, app/static/css/tailwind.src.css, tests/routers/test_cafe_logs.py</files>
  <read_first>
    - CLAUDE.md § "Code conventions"
    - project memory `executors-skip-ruff-ci-gates-both`
  </read_first>
  <action>
    Run `docker compose exec coffee-snobbery ruff format tests/routers/test_cafe_logs.py` and `docker compose exec coffee-snobbery ruff check --fix tests/routers/test_cafe_logs.py`. The Jinja template (cafe_log_form.html) and the CSS file (tailwind.src.css) are not Python and are not linted by ruff — they pass through. Resolve any new ruff warnings/errors in the test file (do NOT add `# noqa` for real issues).

    If tailwind.src.css was modified to add the .htmx-indicator rule, rebuild Tailwind so the compiled css is in the image: `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`. (Per CLAUDE.md: any template/static change requires a rebuild OR `docker compose cp` for runtime visibility. The compose rebuild is the canonical path.)
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery ruff format --check tests/routers/test_cafe_logs.py &amp;&amp; docker compose exec coffee-snobbery ruff check tests/routers/test_cafe_logs.py</automated>
  </verify>
  <acceptance_criteria>
    - `ruff format --check tests/routers/test_cafe_logs.py` exits 0
    - `ruff check tests/routers/test_cafe_logs.py` exits 0
    - If tailwind.src.css was modified, the image was rebuilt and the served page (curl http://localhost:8000/cafe-logs/new with auth) contains the new .htmx-indicator rule reference (smoke check)
  </acceptance_criteria>
  <done>ruff gates pass; the form page is rebuild-served if static CSS changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → form submit | User-typed strings cross here; Jinja autoescape + Pydantic validation (in plan 16-02) are the two layered defenses |
| Browser → /cafe-logs/origin-country-autocomplete | Untrusted query string; the autocomplete endpoint in plan 16-02 binds via parameterized select() — this plan inherits |
| Browser → /flavor-notes/datalist (reused) | Existing Phase 4 hardened endpoint; reused VERBATIM, no new attack surface |
| Browser → /roasters/list (reused) | Same — existing Phase 4 hardened endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Information Disclosure | XSS via cafe_name / notes / origin_country re-render after validation error | mitigate | Jinja2 autoescape ON globally; the form renders user strings via `{{ value }}` (escaped) — V8 / V12 — never `|safe`, never inline HTML interpolation |
| T-16-03-02 | Tampering | Photo upload | mitigate | Reuses `app/services/photos.py:process_and_save` — magic-byte check, Pillow verify, EXIF strip, re-encode, decompression-bomb cap. NEVER calls PIL.Image directly from template or client JS. The Canvas downscale in photo-upload.js is a UX optimization; the server-side pipeline is the authority (V12) |
| T-16-03-03 | Tampering | CSRF token re-use across HTMX swaps | mitigate | Double-submit-cookie pattern via global starlette-csrf middleware; hidden `<input name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken', '') }}">` re-rendered on every form load; token rotation only on session regen (login), not per request (CLAUDE.md § Key Decisions / V13) |
| T-16-03-04 | Information Disclosure | Mass-assignment via hidden input injection | mitigate | extra="forbid" on CafeLogCreate (plan 16-02) rejects any field not in the schema; the form template does NOT render hidden inputs for `user_id` or `photo_filename` (V5) |
| T-16-03-05 | Tampering | |tojson HTML-attr context escape | mitigate | data-initial-chips MUST use SINGLE quotes around the attr (project memory `tojson-attr-quoting-and-live-browser-repro` — the failure mode is HTML parser confusion, not direct XSS, but the symptom is broken Alpine hydration that masks input bugs) |
| T-16-03-06 | Information Disclosure | autocomplete dropdown leak | accept | Roaster + origin-country dropdowns serve shared-catalog data by design (household-scale, intentional); per-user privacy applies only to the cafe_log row (V4 — RESEARCH.md Security Domain) |
| T-16-03-07 | Denial of Service | Decompression-bomb / polyglot photo | mitigate | Inherited from photos.py (V12); Image.MAX_IMAGE_PIXELS cap + magic-byte gate + re-encode strips trailing bytes |
</threat_model>

<verification>
- GET /cafe-logs/new logged-in renders 200 + body contains all 10 field markers from the field-order list above
- `grep -n "data-initial-chips=" app/templates/pages/cafe_log_form.html` finds the SINGLE-quoted form ONLY (project memory `tojson-attr-quoting`)
- `grep -n "@custom-variant" app/templates/pages/cafe_log_form.html` returns no match (Tailwind v3 invariant)
- `grep -n "<style>" app/templates/pages/cafe_log_form.html` returns no match (CSP nonce posture)
- `pytest tests/routers/test_cafe_logs.py::test_cafe_form_save_visible_at_375x667 -x -rs` passes (Playwright available) or SKIPs visibly with "playwright" in the message
- `pytest tests/routers/test_cafe_logs.py -q -rs` overall: every test from plan 16-02 still passes + the new sticky-save test passes/skips
- ruff format --check + ruff check exit 0 on the test file
</verification>

<success_criteria>
- pages/cafe_log_form.html renders both create and edit modes correctly
- layout="desktop" + edit branch suppresses the page shell so the response can mount via innerHTML into #cafe-form-mount
- All 10 form fields are present with the canonical name attributes (the router in plan 16-02 expects exactly these names)
- Autofocus, CSRF, sticky Save bar, |tojson single-quoting, dark-mode .dark: selectors all present and correct
- Playwright assertion confirms Save button visible at 375×667 (or skips visibly)
- No regressions in tests/routers/test_cafe_logs.py from plan 16-02
- ruff format --check + ruff check green
</success_criteria>

<output>
After completion, create `.planning/phases/16-cafe-quick-rate/16-03-SUMMARY.md` documenting:
- A screenshot or wkhtmltoimage capture of /cafe-logs/new at 375×667 showing the autofocused cafe_name input + rating + Save in the first viewport (deliverable for the manual UAT row in VALIDATION.md if a screenshot tool is available; otherwise document the Playwright assertion result)
- Confirmation that observedFlavorNotes scope was reused (UI-SPEC Q1 Option A) vs renamed (Option B) — capture the decision so future contributors don't re-litigate
- Whether tailwind.src.css was modified for .htmx-indicator (if not, confirm Phase 9 already shipped it)
- Test results: pass count + any SKIPs (and why for each SKIP)
- Any deviation from PATTERNS.md § 5 / UI-SPEC § Component Inventory / RESEARCH.md Pattern 5
</output>
</content>
</invoke>