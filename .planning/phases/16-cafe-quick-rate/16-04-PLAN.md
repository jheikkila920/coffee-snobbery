---
phase: 16-cafe-quick-rate
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - app/templates/pages/sessions.html
  - app/templates/fragments/cafe_log_card.html
  - app/templates/fragments/cafe_log_row.html
  - app/templates/fragments/cafe_log_list.html
  - app/routers/brew.py
  - tests/routers/test_cafe_logs.py
autonomous: true
requirements: [CAFE-01, CAFE-03, CAFE-06]
tags: [snobbery, jinja2, htmx, tailwind-v3, mobile-first, tabs, idor]

must_haves:
  truths:
    - "Per D-06: GET /brew?tab=cafe renders the cafe-tastings tab; tabs are server-side (?tab=brew default | ?tab=cafe) with hx-get + hx-push-url + hx-target=#session-list — NO Alpine.js tab swap"
    - "Per D-06: filters on the cafe tab are tab-scoped — only rating_min/rating_max/date_from/date_to are exposed; brew filters do NOT echo onto the cafe tab even if present in the URL"
    - "Per D-07: cafe cards + rows carry border-l-2 border-l-amber-500 (light) / dark:border-l-amber-400 (dark) accent AND a Heroicons Coffee outline SVG icon (aria-label=\"Cafe tasting\") top-left"
    - "Per D-08 LOCKED: the cafe tab no-data empty state renders BLANK — no heading, no body, no illustration, no CTA. The filtered-zero state still shows copy (UI-SPEC § Empty States — carve-out)"
    - "Per D-09: pages/sessions.html header gains a THIRD button \"Quick rate\" with identical Tailwind utility set to the existing \"Log session\" primary CTA; href=\"/cafe-logs/new\""
    - "Per CAFE-06 / D-21: cafe_log_row.html implements the dual Edit button (md:hidden mobile inline → hx-target=closest [data-row] outerHTML; hidden md:inline-flex desktop → ?layout=desktop + hx-target=#cafe-form-mount innerHTML)"
    - "Per CAFE-06: Delete uses POST + hidden _method=DELETE + hx-confirm (HTMX 2.x convention per CLAUDE.md § 3.2); cross-user delete returns 404 (plan 16-02 enforces; this plan exercises via tests)"
    - "Per UI-SPEC § Visual Distinction Spec: the cafe card differs from brew session card by EXACTLY three changes (left border + cup icon + nothing else); brew session cards stay unchanged"
  artifacts:
    - path: "app/templates/fragments/cafe_log_card.html"
      provides: "Mobile-card render of one cafe log (border-l-2 amber accent + cup icon + dual Edit + Delete)"
      contains: "border-l-2 border-l-amber-500"
    - path: "app/templates/fragments/cafe_log_row.html"
      provides: "Desktop-table-row render of one cafe log (same accent + dual Edit + Delete + OOB form clear)"
      contains: "hx-target=\"#cafe-form-mount\""
    - path: "app/templates/fragments/cafe_log_list.html"
      provides: "HTMX swap-target fragment with md+ table / <md cards split + filtered-zero / blank empty-state branches"
      contains: "id=\"session-list\""
    - path: "app/templates/pages/sessions.html"
      provides: "Third Quick rate button + tab toggle + tab-scoped filter form + cafe-form-mount div"
      contains: "/cafe-logs/new"
    - path: "app/routers/brew.py"
      provides: "list_sessions branched on ?tab=cafe — dispatches to cafe_logs_service.list_cafe_logs"
      contains: "tab == \"cafe\""
  key_links:
    - from: "app/routers/brew.py"
      to: "app/services/cafe_logs.py"
      via: "cafe_logs_service.list_cafe_logs call in the ?tab=cafe branch"
      pattern: "list_cafe_logs"
    - from: "app/templates/pages/sessions.html"
      to: "app/templates/fragments/cafe_log_list.html"
      via: "{% include %} on the active_tab == 'cafe' branch"
      pattern: "include.*cafe_log_list"
    - from: "app/templates/fragments/cafe_log_card.html"
      to: "/cafe-logs/{id}/edit and /cafe-logs/{id}"
      via: "hx-get on Edit button + hx-post on Delete form"
      pattern: "/cafe-logs/\\{\\{ log\\.id \\}\\}"
---

<objective>
Wire the cafe-tab UX onto the existing /brew Sessions page. Ships the third "Quick rate" header button, the server-side `?tab=brew | ?tab=cafe` toggle, the cafe list fragment (md+ table / <md cards), and the per-row Edit + Delete dual-button pattern. Also extends `app/routers/brew.py:list_sessions` to dispatch on the new query param.

Purpose: Without the tab toggle and list fragment, completed cafe logs are not viewable; without the dual Edit button, CAFE-06 has no entry point; without the cafe-form-mount div, the desktop edit path from plan 16-03 has nowhere to render. Lands D-06, D-07, D-08, D-09, and the visible UAT surface for CAFE-03 in one plan.
Output: A working Cafe tastings tab on /brew with visually-distinguished cards and rows, blank no-data empty state, filtered-zero copy, and Edit/Delete affordances; plus passing router tests that confirm the tab branch and the empty-state behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cafe-quick-rate/16-CONTEXT.md
@.planning/phases/16-cafe-quick-rate/16-RESEARCH.md
@.planning/phases/16-cafe-quick-rate/16-PATTERNS.md
@.planning/phases/16-cafe-quick-rate/16-UI-SPEC.md
@.planning/phases/16-cafe-quick-rate/16-VALIDATION.md
@.planning/phases/16-cafe-quick-rate/16-02-PLAN.md
@app/templates/pages/sessions.html
@app/templates/fragments/session_list.html
@app/templates/fragments/coffee_row.html
@app/templates/fragments/session_row.html
@app/templates/fragments/autocomplete_list.html
@app/routers/brew.py
@app/services/cafe_logs.py

<interfaces>
Key references the executor needs:

From `app/templates/pages/sessions.html`:
- lines 13-88: existing header flex-wrap row (Guided Brew + Log session anchors); the third Quick rate button inserts after Log session, BEFORE the Filters details
- lines 90 (approx): the `{% include "fragments/session_list.html" %}` line; the tab toggle `<nav>` inserts ABOVE this line; the cafe-form-mount div inserts ABOVE the tab toggle (UI-SPEC § Component Inventory — "Cafe form mount (desktop edit)")
- lines 30-86: existing filter `<details>` block; cafe-tab variant exposes only rating + date inputs (D-06 — filters are tab-scoped)

From `app/templates/fragments/coffee_row.html`:
- lines 23-99: mode="card" branch — the mobile card structure with `pr-24` for top-right button cluster
- lines 30-46: the dual Edit button (md:hidden mobile inline + hidden md:inline-flex desktop with ?layout=desktop + hx-target=#cafe-form-mount)
- lines 100-184: mode="row" branch — desktop table row with the same dual Edit button pattern
- lines 147-160: the desktop Edit + Delete cluster in the row's last `<td>`
- lines 175-183: OOB form-mount clear pattern (`<div id="cafe-form-mount" hx-swap-oob="innerHTML"></div>`)

From `app/templates/fragments/session_list.html`:
- The outer wrapper `<div id="session-list">` — Plan 16-04 reuses this exact id so the existing filter-bar's hx-target="#session-list" covers BOTH tabs without changing the filter bar wiring
- md+ table branch + <md cards branch
- Filtered-zero empty state structure (UI-SPEC § Empty States — same structure for cafe tab when filters are active)

From `app/templates/fragments/session_row.html`:
- The desktop table row column layout (Date / Coffee / Rating / Roaster / etc.); cafe row mirrors structure with Origin replacing some brew-specific columns

From `app/routers/brew.py:480`:
- `list_sessions(request, user, db)` handler — the existing brew-only list handler that gets branched on `?tab=cafe`

From `app/services/cafe_logs.py` (plan 16-02):
- `list_cafe_logs(db, *, by_user_id, rating_min, rating_max, date_from, date_to) -> list[CafeLog]`

From `.planning/phases/16-cafe-quick-rate/16-UI-SPEC.md`:
- § "Visual Distinction Spec" — the exact Heroicons "Coffee" outline path string
- § "Copywriting Contract" — every label and empty-state string
- § "Color" — espresso-700 for Sessions tab underline, amber-500 for Cafe tastings tab underline, amber-500/amber-400 for the border-l accent
- § "Component Inventory" — the third Quick rate button utility set must match "Log session" exactly
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cafe_log_card, cafe_log_row, cafe_log_list fragments (D-07 + D-08 + D-21)</name>
  <files>app/templates/fragments/cafe_log_card.html, app/templates/fragments/cafe_log_row.html, app/templates/fragments/cafe_log_list.html</files>
  <read_first>
    - app/templates/fragments/coffee_row.html (lines 23-99 mode="card"; lines 30-46 dual Edit; lines 100-184 mode="row"; lines 175-183 OOB clear)
    - app/templates/fragments/session_list.html (outer wrapper id="session-list"; md+ table / <md cards branch; filtered-zero copy)
    - app/templates/fragments/session_row.html (desktop column layout structure)
    - app/templates/fragments/autocomplete_list.html (NOT needed for these fragments — read only to confirm shared chip pill rendering pattern if used)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "6. app/templates/fragments/cafe_log_card.html" + § "7. app/templates/fragments/cafe_log_row.html" + § "8. app/templates/fragments/cafe_log_list.html"
    - .planning/phases/16-cafe-quick-rate/16-UI-SPEC.md § "Visual Distinction Spec — D-07 expanded" (the exact Heroicons path string) + § "Empty States" + § "Copywriting Contract" (filtered-zero copy) + § "Accessibility Contract" (aria-label, aria-current)
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md Pattern 12 (D-21 dual Edit) + Pitfall 7 (htmx-indicator)
  </read_first>
  <behavior>
    - cafe_log_card.html renders the mobile card with: `border-l-2 border-l-amber-500 dark:border-l-amber-400` left accent, Heroicons Coffee outline SVG top-left (`absolute top-3 left-3 w-4 h-4 text-amber-600 dark:text-amber-400` + `aria-label="Cafe tasting"`), cafe_name semibold first line, rating stars, roaster line, origin · brew_method line, flavor pills (max 3 + "+N more"), top-right Edit (dual button) + Delete cluster.
    - cafe_log_row.html renders the desktop table row with the same accent + dual Edit + Delete + OOB form-mount clear pattern.
    - cafe_log_list.html outer wrapper is `<div id="session-list">` so the existing filter bar's `hx-target="#session-list"` works on both tabs.
    - cafe_log_list.html branches: rows present → md+ table / <md cards (mirror session_list.html); rows empty + filters active → filtered-zero state with UI-SPEC copy + Clear filters link; rows empty + no filters → BLANK (D-08 LOCKED — no heading, no body, no illustration, no CTA).
    - The dual Edit button mobile branch has `hx-target="closest [data-row]"` + `hx-swap="outerHTML"`; the desktop branch has `hx-target="#cafe-form-mount"` + `hx-swap="innerHTML"` + the `?layout=desktop` query string.
    - The Delete form uses POST + hidden `_method=DELETE` + hidden `X-CSRF-Token` + `hx-confirm="Delete this cafe tasting? This cannot be undone."` (UI-SPEC copy + HTMX 2.x convention per CLAUDE.md § 3.2).
    - Brew session cards/rows stay UNCHANGED — Phase 16 does NOT retro-add a kettle icon to brew session cards (UI-SPEC § Visual Distinction Spec).
  </behavior>
  <action>
    Create `app/templates/fragments/cafe_log_card.html` (mirror PATTERNS.md § 6 + coffee_row.html mode="card" structure):
    - Outer wrapper: `<div id="cafe-log-{{ log.id }}" data-row class="relative rounded-lg border border-espresso-200 border-l-2 border-l-amber-500 bg-cream-100 p-4 pr-24 dark:bg-espresso-900 dark:border-espresso-800 dark:border-l-amber-400">`
    - Heroicons Coffee outline SVG top-left (UI-SPEC § Visual Distinction Spec — exact path string): `<svg xmlns="http://www.w3.org/2000/svg" class="absolute top-3 left-3 w-4 h-4 text-amber-600 dark:text-amber-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-label="Cafe tasting"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8h13v6a4 4 0 01-4 4H7a4 4 0 01-4-4V8zm13 1h2a2 2 0 010 4h-2"/></svg>`
    - Top-right Edit/Delete cluster (`<div class="absolute top-3 right-3 flex gap-1">`):
      - Mobile Edit button: `<button type="button" hx-get="/cafe-logs/{{ log.id }}/edit" hx-target="closest [data-row]" hx-swap="outerHTML" class="md:hidden inline-flex items-center justify-center rounded border border-espresso-300 px-2 py-1 text-sm text-espresso-800 hover:bg-espresso-50 dark:border-espresso-700 dark:text-cream-200 dark:hover:bg-espresso-900 min-h-[44px] min-w-[44px]">Edit</button>`
      - Desktop Edit button (D-21): `<button type="button" hx-get="/cafe-logs/{{ log.id }}/edit?layout=desktop" hx-target="#cafe-form-mount" hx-swap="innerHTML" class="hidden md:inline-flex ...">Edit</button>`
      - Delete form: `<form hx-post="/cafe-logs/{{ log.id }}" hx-target="closest [data-row]" hx-swap="outerHTML" hx-confirm="Delete this cafe tasting? This cannot be undone." class="inline"><input type="hidden" name="_method" value="DELETE"><input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken', '') }}"><button type="submit" class="... min-h-[44px] min-w-[44px]">Delete</button></form>`
    - Body content (between icon and button cluster): `<p class="text-base font-semibold">{{ log.cafe_name }}</p>` then rating star render (mirror session_row.html star-block markup), then `<p class="text-sm">{{ roaster_name or "—" }}</p>`, then `<p class="text-sm">{{ log.origin_country or "—" }}{% if log.brew_method %} · {{ log.brew_method }}{% endif %}</p>`, then flavor pills (max 3 + "+N more" per UI-SPEC § Component Inventory — mirror coffee_row.html lines 83-98 verbatim, renaming `coffee.advertised_flavor_note_ids` → `log.flavor_note_ids` and joining against the resolved flavor_notes lookup the router context provides).

    Create `app/templates/fragments/cafe_log_row.html` (mirror PATTERNS.md § 7 + coffee_row.html mode="row" structure):
    - `<tr id="cafe-log-{{ log.id }}" data-row {% if include_desktop_oob %}hx-swap-oob="outerHTML"{% endif %} class="border-b border-espresso-100 border-l-2 border-l-amber-500 dark:border-l-amber-400">`
    - Cells: `<td class="py-2">{{ log.logged_at|local_date }}</td>` (or whatever date filter the existing session_row uses — read session_row.html to copy the filter name), `<td class="py-2"><a href="/cafe-logs/{{ log.id }}" class="hover:underline">{{ log.cafe_name }}</a></td>`, `<td class="py-2">{{ log.rating or "—" }}</td>`, `<td class="py-2">{{ roaster_name or "—" }}</td>`, `<td class="py-2">{{ log.origin_country or "—" }}</td>`, then a final `<td class="py-2 text-right">` containing the dual Edit + Delete cluster (mirror coffee_row.html:147-160 structure verbatim, swapping routes).
    - OOB form-mount clear at the very end of the file (mirror coffee_row.html:175-183):
      ```
      {% if include_oob_form_clear %}
        <div id="cafe-form-mount" hx-swap-oob="innerHTML"></div>
      {% endif %}
      {% if include_desktop_oob %}
        <div id="cafe-form-mount" hx-swap-oob="innerHTML"></div>
      {% endif %}
      ```

    Create `app/templates/fragments/cafe_log_list.html` (mirror PATTERNS.md § 8 + session_list.html):
    - Outer wrapper `<div id="session-list">` — reuse the EXACT id so the existing filter-bar `hx-target="#session-list"` covers both tabs without changing sessions.html beyond the conditional include.
    - If `rows`: `<div class="hidden md:block">` containing `<table class="w-full text-base">` with header `<tr>` for Date / Coffee name / Rating / Roaster / Origin / Actions; tbody loops `{% for row in rows %}{% with mode = "row" %}{% include "fragments/cafe_log_row.html" %}{% endwith %}{% endfor %}`. Then `<div class="md:hidden space-y-3">` with the same loop but `{% with mode = "card" %}{% include "fragments/cafe_log_card.html" %}`. NOTE: Pass the resolved `roaster_name` for each row via the router context as a dict keyed by log.id, or include a `roaster_name` attribute on the CafeLog row tuple — the router code in plan 16-02's list handler needs to provide this (TASK 3 below extends the router).
    - elif `active_filter_count`: render filtered-zero state with UI-SPEC § Empty States copy: `<h2 class="text-lg font-semibold">No cafe tastings match these filters.</h2><p class="text-base text-espresso-700 dark:text-cream-200">Try widening the rating or date range.</p><a href="/brew?tab=cafe" class="...">Clear filters</a>`.
    - else: D-08 LOCKED — render the wrapper `<div id="session-list">` with NOTHING inside. Add a Jinja COMMENT explaining the divergence so future contributors do not add a hint copy: `{# D-08 LOCKED — blank no-data empty state. Do NOT add a heading or CTA. See .planning/phases/16-cafe-quick-rate/16-CONTEXT.md D-08. #}`

    Tailwind v3 invariants (every fragment): use `.dark:*` selectors, NEVER `@custom-variant`; no inline `<style>` blocks; no `style=` attrs (these fragments do not need the safe-area-inset exception). Touch-target floor `min-h-[44px] min-w-[44px]` on every mobile-tappable control.

    Brew session cards/rows STAY UNCHANGED (UI-SPEC § Visual Distinction Spec — do not retro-add a kettle icon).
  </action>
  <verify>
    <automated>docker compose cp app/templates/ coffee-snobbery:/app/app/templates/ &amp;&amp; docker compose restart coffee-snobbery &amp;&amp; sleep 3 &amp;&amp; docker compose exec coffee-snobbery python -c "import jinja2, os; env = jinja2.Environment(loader=jinja2.FileSystemLoader('app/templates'), autoescape=True); env.get_template('fragments/cafe_log_card.html'); env.get_template('fragments/cafe_log_row.html'); env.get_template('fragments/cafe_log_list.html'); print('parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Three new files exist: cafe_log_card.html, cafe_log_row.html, cafe_log_list.html
    - cafe_log_card.html contains the literal `border-l-2 border-l-amber-500` and `dark:border-l-amber-400` AND the Heroicons Coffee outline path string `M3 8h13v6a4 4 0 01-4 4H7a4 4 0 01-4-4V8zm13 1h2a2 2 0 010 4h-2` AND `aria-label="Cafe tasting"`
    - cafe_log_card.html contains BOTH `md:hidden` Edit button (mobile inline with `hx-target="closest [data-row]"`) AND `hidden md:inline-flex` Edit button (desktop with `hx-target="#cafe-form-mount"` + `?layout=desktop`)
    - cafe_log_card.html contains a Delete form with `hx-confirm="Delete this cafe tasting? This cannot be undone."` AND a hidden `_method=DELETE` input AND a hidden `X-CSRF-Token` input
    - cafe_log_row.html contains the OOB clear pattern `<div id="cafe-form-mount" hx-swap-oob="innerHTML"></div>` guarded by `{% if include_oob_form_clear %}` and `{% if include_desktop_oob %}`
    - cafe_log_list.html outer wrapper is exactly `<div id="session-list">` (grep confirms; same id as session_list.html so the existing filter bar reaches both tabs)
    - cafe_log_list.html no-data branch contains a Jinja comment referencing "D-08 LOCKED" and NO heading/copy/CTA (grep `<h2|<p|<a` finds only matches inside the filtered-zero branch; the no-data branch has none)
    - cafe_log_list.html filtered-zero branch contains the exact UI-SPEC copy "No cafe tastings match these filters." and "Try widening the rating or date range." and "Clear filters" anchor to `/brew?tab=cafe`
    - No `@custom-variant` directives anywhere in the three new files (Tailwind v3 invariant)
    - All three templates parse cleanly under Jinja2 with autoescape ON
  </acceptance_criteria>
  <done>The three cafe-list fragments exist and parse cleanly; D-07 accent + cup icon present; D-08 blank empty state present with the lock comment; dual Edit + Delete + OOB form-mount clear are in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modify pages/sessions.html — Quick rate header button + tab toggle + cafe-form-mount + tab-scoped filter</name>
  <files>app/templates/pages/sessions.html</files>
  <read_first>
    - app/templates/pages/sessions.html (lines 13-88 existing header flex row; line 90 approx the {% include %} insertion point; lines 30-86 existing filter <details>)
    - app/templates/fragments/cafe_log_list.html (the file shipped in Task 1 — confirm the wrapper id matches)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "app/templates/pages/sessions.html (modify — header + tab toggle + cafe filter variant)"
    - .planning/phases/16-cafe-quick-rate/16-UI-SPEC.md § "Component Inventory" rows "/brew page header" + "Tab toggle" + "Cafe form mount" + "Cafe list filter bar"
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md Pattern 7 (tab routing template snippet)
  </read_first>
  <behavior>
    - The /brew page header has a third "Quick rate" button after "Log session", with identical Tailwind utility classes (espresso-700 primary CTA).
    - Below the header (above the list region) a `<nav>` tab toggle has two anchors: Sessions (`/brew?tab=brew`) and Cafe tastings (`/brew?tab=cafe`), each with `hx-get` + `hx-push-url="true"` + `hx-target="#session-list"` + `hx-swap="outerHTML"`.
    - The active tab's anchor carries `border-b-2 border-espresso-700 font-semibold` (Sessions) or `border-b-2 border-amber-500 font-semibold` (Cafe tastings); the inactive tab uses muted text.
    - The active tab anchor also carries `aria-current="page"` for accessibility (UI-SPEC § Accessibility).
    - Above the tab toggle on md+ a `<div class="hidden md:block" id="cafe-form-mount"></div>` exists (visible only ≥768px; mobile uses inline edit per D-21).
    - The filter `<details>` block branches on `active_tab`: if cafe, render ONLY rating_min/rating_max + date_from/date_to inputs; if brew, the existing filter form stays unchanged.
    - The list include branches on `active_tab`: cafe → `{% include "fragments/cafe_log_list.html" %}`; brew → existing `{% include "fragments/session_list.html" %}`.
  </behavior>
  <action>
    Modify `app/templates/pages/sessions.html`:

    1. Header — insert a third button AFTER the existing "Log session" anchor (current line ~24-27, BEFORE the Filters `<details>`):
       `<a href="/cafe-logs/new" class="rounded bg-espresso-700 px-4 py-2 text-base font-semibold text-cream-50 hover:bg-espresso-800 dark:text-cream-50">Quick rate</a>`
       The class list MUST match the "Log session" anchor exactly so visual parity holds (UI-SPEC § Component Inventory; grep the existing Log session line to copy verbatim).

    2. Above the `{% include "fragments/session_list.html" %}` line (insert ABOVE the existing include), add the cafe-form-mount div + the tab toggle + a branch on `active_tab`:
       ```
       <div id="cafe-form-mount" class="hidden md:block"></div>
       <nav class="flex border-b border-espresso-200 dark:border-espresso-800 mb-4">
         <a href="/brew?tab=brew"
            hx-get="/brew?tab=brew"
            hx-push-url="true"
            hx-target="#session-list"
            hx-swap="outerHTML"
            {% if active_tab != 'cafe' %}aria-current="page"{% endif %}
            class="px-4 py-2 text-base {% if active_tab != 'cafe' %}border-b-2 border-espresso-700 font-semibold{% else %}text-espresso-600 dark:text-cream-300{% endif %}">Sessions</a>
         <a href="/brew?tab=cafe"
            hx-get="/brew?tab=cafe"
            hx-push-url="true"
            hx-target="#session-list"
            hx-swap="outerHTML"
            {% if active_tab == 'cafe' %}aria-current="page"{% endif %}
            class="px-4 py-2 text-base {% if active_tab == 'cafe' %}border-b-2 border-amber-500 font-semibold{% else %}text-espresso-600 dark:text-cream-300{% endif %}">Cafe tastings</a>
       </nav>
       ```

    3. Branch the existing list include on `active_tab`:
       ```
       {% if active_tab == 'cafe' %}
         {% include "fragments/cafe_log_list.html" %}
       {% else %}
         {% include "fragments/session_list.html" %}
       {% endif %}
       ```

    4. Filter `<details>` block — branch the existing body on `active_tab`. When `active_tab == 'cafe'`, render ONLY: a `rating_min` input, a `rating_max` input, a `date_from` input, a `date_to` input — with the same Tailwind classes used by the existing brew filter inputs (read the existing filter form to copy class strings exactly). Do NOT render the coffee select, brewer select, recipe select, etc. on the cafe tab (D-06 — tab-scoped filters). Document the carve-out with a Jinja comment so future contributors don't sync brew filters back into cafe.

    Do NOT add a `#brew-form-mount` div on the brew tab (UI-SPEC Open Question 3 — brew uses its own dedicated `/brew/new` page; no mid-page edit pattern). Do NOT use Alpine.js client-side tab swap (UI-SPEC § Interaction Contracts; project memory `strict-csp-blocks-htmx-indicator` underscores keeping client-side JS minimal). Do NOT change any existing brew-tab markup or filter behavior.

    Tailwind v3 invariants: `.dark:*` selectors only; no `@custom-variant`.
  </action>
  <verify>
    <automated>docker compose cp app/templates/pages/sessions.html coffee-snobbery:/app/app/templates/pages/sessions.html &amp;&amp; docker compose restart coffee-snobbery &amp;&amp; sleep 3 &amp;&amp; docker compose exec coffee-snobbery python -m pytest tests/routers/test_cafe_logs.py::test_tab_cafe_renders_list tests/routers/test_cafe_logs.py::test_empty_state_is_blank -x -v -rs 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/pages/sessions.html` contains `<a href="/cafe-logs/new"` exactly once (the third Quick rate button)
    - The Quick rate anchor class list matches the existing "Log session" anchor class list character-for-character (visual parity invariant — diff the two anchors to confirm)
    - The file contains the `<nav>` tab toggle with both `hx-get="/brew?tab=brew"` and `hx-get="/brew?tab=cafe"`
    - Both tab anchors have `hx-push-url="true"` AND `hx-target="#session-list"`
    - The active tab's anchor carries `aria-current="page"`
    - The file contains `<div id="cafe-form-mount" class="hidden md:block"></div>` exactly once
    - The list include is wrapped in `{% if active_tab == 'cafe' %}` / `{% else %}` branching
    - The cafe-tab filter branch does NOT contain any of the brew-only filter input names (grep for `name="coffee_id"`, `name="brewer_id"`, `name="recipe_id"` returns no matches inside the `active_tab == 'cafe'` branch)
    - No `@custom-variant` directives added (Tailwind v3 invariant)
    - The two pre-existing tests `test_tab_cafe_renders_list` and `test_empty_state_is_blank` from plan 16-02's test scaffold now pass after Task 3 wires the router branch (the test pass requirement gates on Task 3 completion; this task's acceptance is template-shape only)
  </acceptance_criteria>
  <done>sessions.html carries the third Quick rate button + tab toggle + cafe-form-mount + tab-scoped filter; all wiring points at the new templates and routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Branch app/routers/brew.py:list_sessions on ?tab=cafe + parse cafe filters + render fragment</name>
  <files>app/routers/brew.py</files>
  <read_first>
    - app/routers/brew.py (lines 409-429 _parse_list_filters + _raw_filters helpers; lines 479-522 list_sessions handler — the canonical structure)
    - app/services/cafe_logs.py (list_cafe_logs signature from plan 16-02)
    - app/services/roasters.py (or wherever the roaster lookup helper lives — needed to resolve roaster_name for each cafe_log row before rendering)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "app/routers/brew.py (modify list_sessions at l. 479)"
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md Pattern 7 (the full list_sessions branch snippet)
    - .planning/phases/16-cafe-quick-rate/16-UI-SPEC.md § Risks and Open Questions item 4 (filter state on tab switch — do NOT echo non-cafe-filter query params onto the cafe form)
  </read_first>
  <behavior>
    - GET /brew with no ?tab param defaults to `tab="brew"` (existing brew behavior unchanged).
    - GET /brew?tab=cafe with no other params returns the cafe list (empty if user has no logs) + the Cafe tastings tab marked active.
    - GET /brew?tab=cafe&rating_min=4.0 filters cafe_logs to rating >= 4.0 (rating_max + date_from + date_to behave symmetrically).
    - GET /brew?tab=cafe&coffee_id=42 IGNORES the coffee_id param (D-06 — filters are tab-scoped; do NOT echo brew filters onto cafe).
    - GET /brew?tab=cafe with `HX-Request: true` returns ONLY the `fragments/cafe_log_list.html` fragment (innerHTML target #session-list). Without the HTMX header returns the full `pages/sessions.html` shell with the cafe fragment included.
    - The context dict for the cafe branch includes `active_tab="cafe"`, `rows=[CafeLog rows pre-resolved with roaster_name]`, `filters={raw form values}`, `active_filter_count=int`.
    - Cross-user data leak: every cafe row in the response belongs to `request.state.user.id` (the service layer's by_user_id kwarg enforces this — plan 16-02 already covered).
  </behavior>
  <action>
    Modify `app/routers/brew.py:list_sessions` (line ~479):

    Near the top of the handler, read `tab = request.query_params.get("tab", "brew")`. If `tab == "cafe"`, branch into the cafe path; otherwise continue with the existing brew-only logic untouched.

    Add a module-level helper `_parse_cafe_list_filters(qp) -> dict` that reads rating_min, rating_max, date_from, date_to from `qp` and returns a dict of typed values (Decimal for ratings, datetime for dates, None for missing/empty). Mirror the structure of `_parse_list_filters` at line 409 — same coercion conventions, same "empty string → None" rule. IGNORE any other query params (specifically: coffee_id, brewer_id, recipe_id — D-06 carve-out).

    Add a module-level helper `_raw_cafe_filters(qp) -> dict` that echoes the four cafe-filter values back as the raw form-display dict (mirror `_raw_filters`). Do NOT include any brew-only filter keys in the returned dict — this defends UI-SPEC § Risks item 4.

    In the cafe branch:
    ```
    cafe_filters = _parse_cafe_list_filters(qp)
    cafe_rows = cafe_logs_service.list_cafe_logs(db, by_user_id=user.id, **cafe_filters)

    # Resolve roaster_name for each row (rendering reads roaster_name via the
    # row dict; the service returns CafeLog ORM objects without the join).
    roaster_ids = {row.roaster_id for row in cafe_rows if row.roaster_id is not None}
    roaster_names = {}
    if roaster_ids:
        roaster_names = {
            r.id: r.name
            for r in db.execute(
                select(Roaster).where(Roaster.id.in_(roaster_ids))
            ).scalars().all()
        }

    # Build the template-friendly row tuples
    rendered_rows = []
    for row in cafe_rows:
        rendered_rows.append({
            "id": row.id,
            "cafe_name": row.cafe_name,
            "rating": row.rating,
            "roaster_id": row.roaster_id,
            "roaster_name": roaster_names.get(row.roaster_id),
            "origin_country": row.origin_country,
            "brew_method": row.brew_method,
            "flavor_note_ids": row.flavor_note_ids,
            "logged_at": row.logged_at,
            "notes": row.notes,
            "photo_filename": row.photo_filename,
        })

    raw_filters = _raw_cafe_filters(qp)
    context = {
        "active_tab": "cafe",
        "rows": rendered_rows,
        "filters": raw_filters,
        "active_filter_count": sum(1 for v in raw_filters.values() if v),
    }

    if request.headers.get("HX-Request") == "true":
        return templates.TemplateResponse(
            request=request,
            name="fragments/cafe_log_list.html",
            context=context,
        )
    return templates.TemplateResponse(
        request=request,
        name="pages/sessions.html",
        context=context,
    )
    ```

    In the existing brew branch, add `context["active_tab"] = "brew"` to whatever context dict the handler already passes to the template (the template now reads `active_tab` to drive the tab toggle's active state).

    Add the necessary imports at the top of brew.py: `from app.services import cafe_logs as cafe_logs_service` and `from app.models.roaster import Roaster` if not already present. (Read the file to confirm whether Roaster is already imported; many of the existing brew handlers use it.)

    Do NOT change any other handler behavior. Do NOT add filter inputs that brew-tab requests honor and cafe-tab requests would echo onto the cafe filter form (UI-SPEC § Risks item 4).
  </action>
  <verify>
    <automated>docker compose cp app/routers/brew.py coffee-snobbery:/app/app/routers/brew.py &amp;&amp; docker compose cp tests/ coffee-snobbery:/app/tests/ &amp;&amp; docker compose restart coffee-snobbery &amp;&amp; sleep 3 &amp;&amp; docker compose exec coffee-snobbery python -m pytest tests/routers/test_cafe_logs.py -x -v -rs 2>&amp;1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/brew.py:list_sessions` reads `request.query_params.get("tab", "brew")` and branches when value == "cafe"
    - The cafe branch calls `cafe_logs_service.list_cafe_logs(db, by_user_id=user.id, **cafe_filters)`
    - The cafe branch returns `fragments/cafe_log_list.html` for HX-Request and `pages/sessions.html` otherwise
    - `_parse_cafe_list_filters` ignores `coffee_id`, `brewer_id`, `recipe_id` (grep the helper body — those keys must not appear)
    - `_raw_cafe_filters` returns only the four cafe filter keys (rating_min, rating_max, date_from, date_to)
    - The existing brew branch's context dict now includes `active_tab = "brew"` (so the template's active-tab styling works for both branches)
    - `pytest tests/routers/test_cafe_logs.py::test_tab_cafe_renders_list -x` passes
    - `pytest tests/routers/test_cafe_logs.py::test_empty_state_is_blank -x` passes
    - `pytest tests/routers/test_cafe_logs.py -q -rs` overall: all 13+ tests pass with zero SKIPs (project memory `tests-pass-by-skip-mask-green`)
    - `pytest tests/routers/test_brew_router.py -q` overall: no regressions (the existing brew router tests still pass)
  </acceptance_criteria>
  <done>The list_sessions handler dispatches correctly on ?tab=cafe; HTMX fragment vs full-page rendering both work; brew filters do not leak into the cafe filter form; existing brew tests still pass.</done>
</task>

<task type="auto">
  <name>Task 4: Lint clean + commit gate (ruff format + ruff check)</name>
  <files>app/routers/brew.py, app/templates/pages/sessions.html, app/templates/fragments/cafe_log_card.html, app/templates/fragments/cafe_log_row.html, app/templates/fragments/cafe_log_list.html</files>
  <read_first>
    - CLAUDE.md § "Code conventions"
    - project memory `executors-skip-ruff-ci-gates-both`
  </read_first>
  <action>
    Run `docker compose exec coffee-snobbery ruff format app/routers/brew.py` and `docker compose exec coffee-snobbery ruff check --fix app/routers/brew.py`. Jinja templates pass through. Resolve any new ruff warnings/errors (do NOT add `# noqa` to suppress real issues; mirror the existing brew.py `# noqa: B008` convention for FastAPI Depends defaults only).

    Rebuild the container so the static templates land in the BAKED image (CLAUDE.md: any template/static change requires a rebuild — the runtime path used `docker compose cp` for fast iteration above, but commit gating requires the rebuild path to confirm the image is shippable): `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`. Confirm the served /brew?tab=cafe page contains the third Quick rate button + the tab toggle nav after rebuild.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery &amp;&amp; docker compose up -d coffee-snobbery &amp;&amp; sleep 3 &amp;&amp; docker compose exec coffee-snobbery ruff format --check app/routers/brew.py &amp;&amp; docker compose exec coffee-snobbery ruff check app/routers/brew.py