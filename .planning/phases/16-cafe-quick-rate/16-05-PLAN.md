---
phase: 16-cafe-quick-rate
plan: 05
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - app/services/analytics.py
  - tests/services/test_analytics.py
autonomous: true
requirements: [CAFE-04, CAFE-05]
tags: [snobbery, sqlalchemy, postgres, analytics, ai-signature, cold-start, union-all]

must_haves:
  truths:
    - "Per D-12: compute_input_signature SHA256 payload becomes [brew_list, cafe_list] — a two-element top-level list, NOT a flat concatenation. Cafe row tuple shape is (cafe_log_id, float(rating), sorted flavor_note_ids, roaster_id, origin_country). Cafe rows ordered by CafeLog.id ASC. Unrated cafe rows excluded by CafeLog.rating.is_not(None). _EMPTY_SIGNATURE only when BOTH brew_rows and cafe_rows are empty (Pitfall 3 + Pitfall 9)."
    - "Per D-13: get_preference_profile.origin dim UNIONs brew_origin (CoffeeOrigin.country JOINed via BrewSession→Coffee) with cafe_origin (CafeLog.origin_country IS NOT NULL); roaster dim UNIONs brew_roaster (Roaster JOINed via BrewSession→Coffee) with cafe_roaster (Roaster JOINed via CafeLog.roaster_id IS NOT NULL). UNION-ALL → subquery → GROUP BY → HAVING count() >= 2 (min ≥2 applies across the union). process and roast_level dims stay brew-only via the existing _dim_query helper."
    - "Per D-13: get_flavor_descriptors raw SQL extends its FROM clause to a UNION ALL of two unnest blocks — brew_sessions.flavor_note_ids_observed (rating >= 4.0, per-user) and cafe_logs.flavor_note_ids (rating >= 4.0, per-user). Top-10 by appearance count across union; HAVING count(*) >= 2 preserved. Bound :user_id parameter (SQLi defense)."
    - "Per D-14: get_top_coffees query body is UNCHANGED. A new one-line code comment is added inside the function docstring or above the SELECT: 'CAFE-04 not applicable: cafe coffees have no row in coffees table by design (D-14).' Future contributors must NOT UNION cafe data into this query."
    - "Per D-15: get_cold_start_counts returns dict with the SAME keys (sessions, distinct_notes, gate_open, sessions_needed, notes_needed) so app/templates/fragments/home/_cold_start.html requires NO template change. New arithmetic: sessions = brew_count + cafe_count (scalar count from each table); distinct_notes = single raw SQL with UNION ALL of two unnest blocks JOINed to flavor_notes (only LIVE notes count, dangling IDs filtered, mirroring the existing brew-only block at analytics.py:326-336). gate_open = (sessions >= 3) AND (distinct_notes >= 5). UI copy stays 'brews' per UI-SPEC § Cold-Start Carve-Out — math input changes only; non-breaking dict shape."
    - "Per D-16: get_sweet_spots query body is UNCHANGED. A new one-line code comment is added inside the function docstring or above the SELECT: 'Cafe logs are intentionally excluded — they have no brew-parameter fields (CAFE-05 / D-16). Do not UNION cafe data into this query.' Future contributors must NOT UNION cafe data."
    - "Per RESEARCH.md Pitfall 9 (signature shape one-time AI regen): the signature shape transition from flat brew payload to [brew_list, cafe_list] forces a one-time regen for every existing user on first nightly run post-deploy. This is acknowledged in the plan output and SUMMARY — recommended Option (a), not avoided via conditional payload."
    - "Per RESEARCH.md Pitfall 6 (tests-pass-by-skip-mask-green): every new cafe-aware analytics test calls the _require_cafe_logs_table() skip-gate added by plan 16-01; pytest run includes -rs to surface skips during validate-phase."
    - "Per RESEARCH.md § Phase Requirements → Test Map: every named pytest test from VALIDATION.md per-task verification map exists and is green when run via the BAKED-image gate (project memory snobbery-test-gate-runtime)."
  artifacts:
    - path: "app/services/analytics.py"
      provides: "Extended compute_input_signature (brew + cafe payload list); get_preference_profile (origin + roaster UNIONs); get_flavor_descriptors (UNION ALL of unnest blocks); get_cold_start_counts (brew + cafe combined math with same dict keys); get_top_coffees + get_sweet_spots guard comments (D-14, D-16)."
      contains: "from app.models.cafe_log import CafeLog"
    - path: "tests/services/test_analytics.py"
      provides: "Extended _seed_analytics_scenario (or a new _seed_cafe_into_scenario helper) seeding cafe_logs rows; new test functions covering D-12 / D-13 / D-14 / D-15 / D-16 per VALIDATION.md per-task map."
      contains: "def test_signature_includes_cafe_logs"
  key_links:
    - from: "app/services/analytics.py:compute_input_signature"
      to: "app/models/cafe_log.CafeLog"
      via: "select(CafeLog.id, CafeLog.rating, CafeLog.flavor_note_ids, CafeLog.roaster_id, CafeLog.origin_country).where(CafeLog.user_id == user_id, CafeLog.rating.is_not(None)).order_by(CafeLog.id)"
      pattern: "select\\(\\s*CafeLog\\.id"
    - from: "app/services/analytics.py:get_preference_profile"
      to: "app/models/cafe_log.CafeLog"
      via: "brew_origin.union_all(cafe_origin).subquery() and brew_roaster.union_all(cafe_roaster).subquery()"
      pattern: "union_all\\("
    - from: "app/services/analytics.py:get_flavor_descriptors"
      to: "cafe_logs.flavor_note_ids"
      via: "raw SQL UNION ALL of two unnest blocks inside a subquery joined to flavor_notes"
      pattern: "FROM cafe_logs cl, unnest\\(cl\\.flavor_note_ids\\)"
    - from: "app/services/analytics.py:get_cold_start_counts"
      to: "app/models/cafe_log.CafeLog + raw SQL UNION ALL distinct-notes query"
      via: "scalar count(CafeLog.id) for cafe_count + UNION ALL distinct note_id raw SQL"
      pattern: "func\\.count\\(CafeLog\\.id\\)"
---

<objective>
Plan 16-05 closes the analytics + AI + cold-start gate extensions for Phase 16 — the cross-cutting cafe-data wiring into the existing analytics module. The cafe vertical slice (model, schema, service, router, templates, migration) is delivered by plans 16-01..16-04; this plan is the surgical edit-set against app/services/analytics.py that wires cafe data into the AI signature, the preference-derivation queries, and the cold-start gate — while explicitly fencing get_top_coffees and get_sweet_spots from accidental cafe UNIONs (D-14, D-16). Every change has an analog in the existing analytics.py module shape; no new architectural patterns are introduced. The photo orphan sweep extension (Pitfall 8) is split into a separate plan 16-06 to keep this plan's scope tight.

Purpose: implement CAFE-04 (cafe ratings/flavor/origin/roaster feed preference derivation AND the AI input signature, so subsequent AI runs reflect cafe taste data) and CAFE-05 (cafe logs excluded from brew-parameter sweet-spots analytics) — the two requirements in Phase 16 that aren't covered by the cafe CRUD vertical. The signature shape change forces a one-time AI regen for every user on first nightly run post-deploy — acknowledged + documented in SUMMARY per Pitfall 9 Option (a).

Output: surgical edits to analytics.py (five functions touched: two trivial guard comments, three substantive query extensions, plus signature payload extension) + a per-task test set covering every named analytics pytest case from VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-cafe-quick-rate/16-CONTEXT.md
@.planning/phases/16-cafe-quick-rate/16-RESEARCH.md
@.planning/phases/16-cafe-quick-rate/16-PATTERNS.md
@.planning/phases/16-cafe-quick-rate/16-VALIDATION.md
@.planning/phases/16-cafe-quick-rate/16-01-PLAN.md
@.planning/phases/16-cafe-quick-rate/16-02-PLAN.md
@./CLAUDE.md

# Production code the executor extends
@app/services/analytics.py

# Existing test scaffolding the executor extends
@tests/services/test_analytics.py

# Required reading — UI consumer of get_cold_start_counts (dict-key contract preserved)
@app/templates/fragments/home/_cold_start.html

# Required reading — produced by 16-01 (model + skip gate)
@app/models/cafe_log.py
@tests/conftest.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from analytics.py + cafe_log model. -->
<!-- The executor uses these directly — no codebase exploration needed. -->

From app/services/analytics.py (current signatures — UNCHANGED for this plan; only bodies are edited):
```python
def get_top_coffees(db: Session, user_id: int) -> list[Row]: ...
def get_preference_profile(db: Session, user_id: int) -> dict[str, list[Row]]: ...
def get_flavor_descriptors(db: Session, user_id: int) -> list[Row]: ...
def get_sweet_spots(db: Session, user_id: int) -> list[Row]: ...
def get_cold_start_counts(db: Session, user_id: int) -> dict[str, Any]: ...
def compute_input_signature(db: Session, user_id: int) -> str: ...
```

From app/models/cafe_log.py (created by plan 16-01 — read but DO NOT modify):
```python
class CafeLog(Base):
    __tablename__ = "cafe_logs"
    id: Mapped[int]                                # BigInteger Identity PK
    user_id: Mapped[int]                            # FK users.id RESTRICT
    roaster_id: Mapped[int | None]                  # FK roasters.id SET NULL
    cafe_name: Mapped[str]                          # Text NOT NULL
    origin_country: Mapped[str | None]              # Text NULL
    brew_method: Mapped[str | None]                 # Text NULL
    rating: Mapped[Decimal | None]                  # Numeric(3, 2) NULL
    flavor_note_ids: Mapped[list[int]]              # BIGINT[] NOT NULL DEFAULT '{}'
    notes: Mapped[str]                              # Text NOT NULL DEFAULT ''
    photo_filename: Mapped[str | None]              # Text NULL
    logged_at: Mapped[datetime]                     # TIMESTAMPTZ NOT NULL
```

From tests/conftest.py (added by plan 16-01):
```python
def _require_cafe_logs_table() -> None:
    """Skip if the cafe_logs table is not present (Pitfall 6 — mirrors _require_analytics_tables)."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _seed_analytics_scenario with cafe_logs fixtures + author analytics test scaffolds</name>
  <files>tests/services/test_analytics.py</files>
  <read_first>
    - tests/services/test_analytics.py:56-300 — the existing _seed_analytics_scenario shape (1 user, 1 roaster, 4 coffees, 6 sessions, 6 notes); follow its naming convention and return-tuple style. The cafe seed must extend this fixture so the existing brew assertions stay valid.
    - app/models/cafe_log.py — column names from interfaces block above (created by plan 16-01).
    - tests/conftest.py — _require_cafe_logs_table() skip-gate; every new cafe-aware test MUST call this gate (Pitfall 6 / project memory tests-pass-by-skip-mask-green).
    - 16-RESEARCH.md § Pattern 9 lines 707-805 — the signature test obligations enumerated; this task seeds the data those tests will consume in Task 2-5.
  </read_first>
  <behavior>
    - Test scaffolds (RED at end of this task — Tasks 2-6 turn them green one by one):
        * test_signature_includes_cafe_logs: seed brew-only user, capture signature_before; insert one rated CafeLog; assert compute_input_signature(db, user_id) != signature_before.
        * test_signature_excludes_unrated_cafe: seed brew-only user, capture signature_before; insert one UNRATED CafeLog (rating=None); assert signature unchanged.
        * test_preference_profile_origin_unions_cafe: seed scenario; insert 2 rated CafeLogs both origin_country='Costa Rica' (a country absent from brew-side scenario); assert preference_profile['origin'] now contains a 'Costa Rica' row with session_count==2 and avg_rating equal to the mean of the two cafe ratings.
        * test_preference_profile_roaster_unions_cafe: insert 2 rated CafeLogs both roaster_id pointing at the seeded roaster; assert preference_profile['roaster'] count for that roaster increases by 2 across the union.
        * test_preference_profile_process_brew_only: insert a rated CafeLog; assert preference_profile['process'] and ['roast_level'] are UNCHANGED vs the brew-only baseline (cafe contributes nothing to these two dims).
        * test_flavor_descriptors_unions_cafe: seed scenario; insert a rated-4.5 CafeLog whose flavor_note_ids contains a known note id present in brew-side and a new id present only in cafe-side; assert both notes appear in get_flavor_descriptors output with counts that reflect both arrays.
        * test_cold_start_brew_only: seed a fresh user with 3 brew sessions covering 5 distinct notes, ZERO cafe_logs; assert gate_open == True and sessions == 3.
        * test_cold_start_cafe_only: seed a fresh user with 0 brew sessions and 3 rated cafe logs covering 5 distinct flavor_note_ids; assert gate_open == True and sessions == 3.
        * test_cold_start_mixed: 1 brew + 2 cafe logs together cover 5 distinct notes; assert sessions == 3 and gate_open == True.
        * test_sweet_spots_excludes_cafe: seed scenario with brew sweet-spot already cleared; insert a CafeLog whose origin_country + roaster mirror the brew sweet-spot key; assert get_sweet_spots output is BIT-IDENTICAL to the brew-only baseline (cafe contributed nothing).
        * test_top_coffees_excludes_cafe: insert a high-rated CafeLog; assert get_top_coffees output is UNCHANGED (cafe coffees have no coffees.id; the JOIN excludes them).
    - All new tests call _require_postgres() + _require_analytics_tables() + _require_cafe_logs_table() at the top of the test body so a missing migration produces a visible skip (Pitfall 6).
    - At THIS task's point in the wave: all the listed tests SHOULD FAIL (the cafe seed helper exists but the analytics.py extensions are not yet written). Tasks 2-6 turn them green, one function at a time.
  </behavior>
  <action>
    Extend tests/services/test_analytics.py's _seed_analytics_scenario helper to seed CafeLog rows in addition to BrewSession rows (or factor a sibling helper _seed_cafe_into_scenario(db, *, user_id, roaster_id, flavor_note_ids, count, rated) — planner's call, the simpler shape is preferred). The fixture must keep all existing brew assertions valid (no change to the brew side; the cafe rows are PURELY additive). Add the imports for CafeLog at the top of the file. Add every test function listed in the behavior block above as a top-level def in test_analytics.py, each one calling the three skip-gates (_require_postgres, _require_analytics_tables, _require_cafe_logs_table). Use the same SessionLocal context-manager + clean_analytics fixture pattern as the existing tests. For cleanup, extend (or mirror) the clean_analytics fixture so it DELETE FROM cafe_logs WHERE user_id IN (seeded ids) before/after each test — see project memory full-suite-test-isolation-gaps for the rationale (cross-module pollution from un-cleaned cafe rows would break the next test module). Do NOT touch any other test. Run `docker compose cp tests/services/test_analytics.py coffee-snobbery:/app/tests/services/test_analytics.py` after editing so the running container picks up the new tests (per CLAUDE.md fast-iteration recipe). All tests should be RED at the end of this task — that's the TDD signal.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py --collect-only -q -k "cafe or cold_start_brew_only or cold_start_cafe_only or cold_start_mixed or sweet_spots_excludes or top_coffees_excludes or preference_profile or flavor_descriptors_unions or signature_includes or signature_excludes" | grep -E "test_(signature_includes_cafe_logs|signature_excludes_unrated_cafe|preference_profile_origin_unions_cafe|preference_profile_roaster_unions_cafe|preference_profile_process_brew_only|flavor_descriptors_unions_cafe|cold_start_brew_only|cold_start_cafe_only|cold_start_mixed|sweet_spots_excludes_cafe|top_coffees_excludes_cafe)" | wc -l</automated>
  </verify>
  <done>Collect-only output names all 11 new analytics test functions (count >= 11); the named tests fail RED with assertion errors when actually run (compute_input_signature still ignores cafe rows, etc.) — RED is the desired state going into Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend compute_input_signature with [brew_list, cafe_list] payload (D-12, Pitfall 3, Pitfall 9)</name>
  <files>app/services/analytics.py</files>
  <read_first>
    - app/services/analytics.py:353-399 — the current compute_input_signature implementation; the executor preserves brew-row ordering by BrewSession.id ASC and adds a parallel SELECT over CafeLog ordered by CafeLog.id ASC.
    - 16-RESEARCH.md § Pattern 9 lines 707-805 — full code template + payload-shape rationale. Critical: payload is [brew_list, cafe_list] — a list of TWO lists, NOT a flat concatenation. Pitfall 3 (cafe_log_id / coffee_id namespace collision defense). Pitfall 9 (one-time AI regen accepted).
    - 16-PATTERNS.md § "Modified-file pattern assignments — analytics.py compute_input_signature" line 869-870 — anchors on the existing _serialize_row lambda style.
    - app/models/cafe_log.py — column names (id, rating, flavor_note_ids, roaster_id, origin_country).
  </read_first>
  <behavior>
    - test_signature_includes_cafe_logs turns GREEN: rated cafe insert mutates signature.
    - test_signature_excludes_unrated_cafe turns GREEN: unrated cafe insert leaves signature unchanged (CafeLog.rating.is_not(None) filter present).
    - The _EMPTY_SIGNATURE branch is preserved: returns _EMPTY_SIGNATURE only when BOTH brew_rows AND cafe_rows are empty.
    - Per-user scoping preserved: CafeLog.user_id == user_id WHERE clause on the new SELECT.
    - The cafe_rows SELECT orders by CafeLog.id ASC (determinism — analytics module's existing Pitfall 5 invariant).
  </behavior>
  <action>
    Edit app/services/analytics.py:compute_input_signature. Add `from app.models.cafe_log import CafeLog` at the top of the file (alongside the other model imports at lines 28-33). Inside compute_input_signature, after the existing brew_stmt + brew_rows = db.execute(...).all() block, add a parallel cafe_stmt = select(CafeLog.id, CafeLog.rating, CafeLog.flavor_note_ids, CafeLog.roaster_id, CafeLog.origin_country).where(CafeLog.user_id == user_id, CafeLog.rating.is_not(None)).order_by(CafeLog.id). Capture cafe_rows = db.execute(cafe_stmt).all(). Change the empty-signature short-circuit from `if not rows: return _EMPTY_SIGNATURE` to `if not brew_rows and not cafe_rows: return _EMPTY_SIGNATURE`. Rename the existing local `rows` to `brew_rows` for clarity. Add a sibling lambda `_serialize_cafe(row)` mirroring _serialize_brew but emitting [row.id, float(row.rating), sorted(row.flavor_note_ids or []), row.roaster_id, row.origin_country]. Build payload = [[_serialize_brew(r) for r in brew_rows], [_serialize_cafe(r) for r in cafe_rows]] — a list of two lists. Keep json.dumps(payload, sort_keys=True, separators=(",", ":")) unchanged. Update the function docstring to document the new payload shape, the Pitfall 3 namespace defense, and the Pitfall 9 one-time-regen acceptance. Do NOT touch any other function. Run `docker compose cp app/services/analytics.py coffee-snobbery:/app/app/services/analytics.py` after editing.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py::test_signature_includes_cafe_logs tests/services/test_analytics.py::test_signature_excludes_unrated_cafe -x -q</automated>
  </verify>
  <done>Both signature tests pass. Re-running the full existing test_compute_signature_* tests in test_analytics.py confirms they STILL PASS (the brew-only payload shape changed from [...brews] to [[...brews], []] — Pitfall 9 one-time signature value churn is accepted; the EXISTING tests compare signatures pre/post mutation rather than against pre-computed hex, so they remain valid).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UNION cafe into get_preference_profile origin + roaster dims; keep process + roast_level brew-only (D-13)</name>
  <files>app/services/analytics.py</files>
  <read_first>
    - app/services/analytics.py:78-150 — the current get_preference_profile body. Three of the four dims (origin, roaster, process+roast_level via _dim_query) live here. Origin already uses an explicit `origin_stmt` (lines 128-143) and roaster uses an explicit `roaster_stmt` (lines 107-122). Process + roast_level use the inline _dim_query helper.
    - 16-RESEARCH.md § Pattern 10 lines 806-912 — the canonical per-dimension UNION subquery shape; both `brew_origin.union_all(cafe_origin).subquery()` and `brew_roaster.union_all(cafe_roaster).subquery()` shown in full.
    - 16-CONTEXT.md § Decisions D-13 — origin YES, roaster YES, process NO, roast_level NO. The cafe form does not capture process or roast_level (user typically doesn't know "washed/natural/honey" or roast level at a cafe).
    - 16-PATTERNS.md § "Modified-file pattern assignments — analytics.py get_preference_profile" lines 871-872.
  </read_first>
  <behavior>
    - test_preference_profile_origin_unions_cafe turns GREEN: a Costa Rica row appears in preference_profile['origin'] with session_count and avg_rating reflecting both brew + cafe.
    - test_preference_profile_roaster_unions_cafe turns GREEN: shared roaster's row reflects union counts.
    - test_preference_profile_process_brew_only turns GREEN: cafe insert leaves preference_profile['process'] and ['roast_level'] keys UNCHANGED (no cafe contribution).
    - Existing brew-only tests in test_analytics.py covering get_preference_profile (test_preference_profile_returns_*) stay green — the union extends but does not break brew-only aggregation.
    - min ≥2 HAVING clause applies ACROSS the union (a country with 1 brew + 1 cafe row counts as 2 — satisfies the floor).
  </behavior>
  <action>
    Edit app/services/analytics.py:get_preference_profile. Replace origin_stmt (lines 128-143) with the UNION-subquery shape: build `brew_origin = select(CoffeeOrigin.country.label("country"), BrewSession.rating.label("rating")).join(Coffee, BrewSession.coffee_id == Coffee.id).join(CoffeeOrigin, CoffeeOrigin.coffee_id == Coffee.id).where(BrewSession.user_id == user_id, BrewSession.rating.is_not(None))`. Build `cafe_origin = select(CafeLog.origin_country.label("country"), CafeLog.rating.label("rating")).where(CafeLog.user_id == user_id, CafeLog.rating.is_not(None), CafeLog.origin_country.is_not(None))`. Compose `origin_union = brew_origin.union_all(cafe_origin).subquery()`. Build origin_stmt = select(origin_union.c.country.label("label"), func.avg(origin_union.c.rating).label("avg_rating"), func.count().label("session_count")).group_by(origin_union.c.country).having(func.count() >= 2).order_by(func.avg(origin_union.c.rating).desc(), func.count().desc()). Apply the same UNION-subquery shape to roaster_stmt (lines 107-122): brew_roaster = the current JOINed select; cafe_roaster = select(Roaster.name.label("name"), CafeLog.rating.label("rating")).join(Roaster, CafeLog.roaster_id == Roaster.id).where(CafeLog.user_id == user_id, CafeLog.rating.is_not(None)); roaster_union = brew_roaster.union_all(cafe_roaster).subquery(); roaster_stmt = aggregate over roaster_union. Leave the `_dim_query` helper UNTOUCHED — process and roast_level continue to call it as today. The returned dict shape is unchanged. Update the function docstring to note "origin + roaster dims UNION cafe data per D-13; process + roast_level stay brew-only because the cafe form doesn't capture them." Run `docker compose cp app/services/analytics.py coffee-snobbery:/app/app/services/analytics.py`.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py::test_preference_profile_origin_unions_cafe tests/services/test_analytics.py::test_preference_profile_roaster_unions_cafe tests/services/test_analytics.py::test_preference_profile_process_brew_only -x -q</automated>
  </verify>
  <done>The three named preference_profile tests pass; the existing brew-only preference_profile tests (test_preference_profile_returns_*, test_preference_profile_origin_uses_coffee_origins, etc.) also still pass — verified by running `pytest tests/services/test_analytics.py -k preference_profile -q`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: UNION cafe into get_flavor_descriptors raw SQL (D-13)</name>
  <files>app/services/analytics.py</files>
  <read_first>
    - app/services/analytics.py:158-183 — the current get_flavor_descriptors body. Raw SQL with FROM brew_sessions bs, unnest(bs.flavor_note_ids_observed) AS note_id + bound :user_id parameter (SQLi defense T-06-03). Bound parameter pattern MUST be preserved.
    - 16-RESEARCH.md § Pattern 10 lines 914-956 — full raw SQL template extending the FROM clause with a UNION ALL subquery of two unnest blocks.
    - 16-CONTEXT.md § Decisions D-13 — flavor descriptors YES; UNION rated-4+ brew_sessions.flavor_note_ids_observed with rated-4+ cafe_logs.flavor_note_ids; top-10 by total appearance count.
    - 16-RESEARCH.md line 956 — "A flavor note appearing in both a 4.0+ brew row AND a 4.0+ cafe row counts TWICE (once per source). This is the desired behavior — it weights notes that recur across taste contexts."
  </read_first>
  <behavior>
    - test_flavor_descriptors_unions_cafe turns GREEN: a flavor note appearing in a rated-4+ cafe log surfaces in get_flavor_descriptors output; a note appearing in both arrays has its count incremented by both sources.
    - Existing get_flavor_descriptors brew-only tests stay green — only rated-4+ rows count, only LIVE notes (JOIN to flavor_notes filters dangling IDs, mirroring the brew-side).
    - Bound :user_id parameter preserved (single bind, both sides of the union read it).
    - HAVING count(*) >= 2 and LIMIT 10 preserved.
  </behavior>
  <action>
    Edit app/services/analytics.py:get_flavor_descriptors. Replace the SQL body with the UNION-of-unnests template from 16-RESEARCH.md § Pattern 10 (lines 930-952). Concretely, change the inner FROM clause from `FROM brew_sessions bs, unnest(bs.flavor_note_ids_observed) AS note_id` to a derived-table subquery `FROM (SELECT note_id FROM brew_sessions bs, unnest(bs.flavor_note_ids_observed) AS note_id WHERE bs.user_id = :user_id AND bs.rating IS NOT NULL AND bs.rating >= 4.0 UNION ALL SELECT note_id FROM cafe_logs cl, unnest(cl.flavor_note_ids) AS note_id WHERE cl.user_id = :user_id AND cl.rating IS NOT NULL AND cl.rating >= 4.0) AS notes`. Keep the outer SELECT fn.id, fn.name, count(*) AS session_count FROM notes JOIN flavor_notes fn ON fn.id = notes.note_id GROUP BY fn.id, fn.name HAVING count(*) >= 2 ORDER BY session_count DESC LIMIT 10. Keep db.execute(stmt, {"user_id": user_id}).all() — the bound parameter is referenced twice but the dict has only one key (psycopg supports this). Update the docstring to note "UNIONs rated-4+ brew + cafe arrays per D-13/CAFE-04." Run `docker compose cp app/services/analytics.py coffee-snobbery:/app/app/services/analytics.py`.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py::test_flavor_descriptors_unions_cafe tests/services/test_analytics.py -k flavor_descriptors -q</automated>
  </verify>
  <done>test_flavor_descriptors_unions_cafe passes and every other -k flavor_descriptors test (the existing brew-only ones from Phase 6) stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Extend get_cold_start_counts with combined brew+cafe math; preserve dict-key contract (D-15)</name>
  <files>app/services/analytics.py</files>
  <read_first>
    - app/services/analytics.py:309-345 — the current get_cold_start_counts: two queries (one scalar count of BrewSession.id + one raw-SQL count DISTINCT note_id from unnest with JOIN to flavor_notes for live-only counting). The returned dict shape (sessions, distinct_notes, gate_open, sessions_needed, notes_needed) is consumed by app/templates/fragments/home/_cold_start.html — preserve verbatim.
    - 16-RESEARCH.md § Pattern 11 lines 958-1025 — full code template (two-queries-summed-in-Python recommended).
    - app/templates/fragments/home/_cold_start.html:1-50 — confirms the template reads exactly gate.sessions, gate.distinct_notes, gate.sessions_needed, gate.notes_needed. NO template change required (UI copy stays "brews" per UI-SPEC § Cold-Start Carve-Out).
    - 16-CONTEXT.md § Decisions D-15 — (brew_count + cafe_count) >= 3 AND distinct flavor_notes across both >= 5.
    - 16-RESEARCH.md § Open Questions 1 + Assumption A2 lines 1428-1442 — UI copy imprecision accepted; Phase 17 owns home-copy polish.
  </read_first>
  <behavior>
    - test_cold_start_brew_only turns GREEN: 3 brews + 5 notes + 0 cafe logs → gate_open == True.
    - test_cold_start_cafe_only turns GREEN: 0 brews + 3 rated cafe logs + 5 distinct cafe-array notes → gate_open == True.
    - test_cold_start_mixed turns GREEN: 1 brew + 2 cafe logs spanning 5 distinct notes total → gate_open == True.
    - Existing brew-only cold-start tests (test_cold_start_returns_*) stay green when no cafe rows exist.
    - Dict keys unchanged: sessions, distinct_notes, gate_open, sessions_needed, notes_needed (template reads these names). Optional new keys brew_count + cafe_count MAY be added (non-breaking additions for future UI/debugging) — leave them out unless needed.
  </behavior>
  <action>
    Edit app/services/analytics.py:get_cold_start_counts. Add a second scalar count: `cafe_count: int = db.scalar(select(func.count(CafeLog.id)).where(CafeLog.user_id == user_id)) or 0`. Compute `total = brew_count + cafe_count` (rename the existing `session_count` local to `brew_count` for clarity, or keep session_count and add a new combined `total`). Change the distinct-notes raw SQL FROM clause to a UNION-ALL subquery of two unnest blocks (mirroring the get_flavor_descriptors pattern from Task 4 but WITHOUT the rating >= 4.0 filter — cold-start counts ALL sessions including unrated per D-02): `SELECT count(DISTINCT all_notes.note_id) AS cnt FROM (SELECT note_id FROM brew_sessions bs, unnest(bs.flavor_note_ids_observed) AS note_id WHERE bs.user_id = :user_id UNION ALL SELECT note_id FROM cafe_logs cl, unnest(cl.flavor_note_ids) AS note_id WHERE cl.user_id = :user_id) AS all_notes JOIN flavor_notes fn ON fn.id = all_notes.note_id`. Return dict: sessions=total, distinct_notes=note_count, gate_open=total>=3 and note_count>=5, sessions_needed=max(0, 3-total), notes_needed=max(0, 5-note_count). DO NOT modify _cold_start.html (UI copy stays "brews" per UI-SPEC carve-out; only the math input changes). Update the docstring to document D-15 + CAFE-04 + the Assumption A2 / UI-copy-imprecision-accepted note. Run `docker compose cp app/services/analytics.py coffee-snobbery:/app/app/services/analytics.py`.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py::test_cold_start_brew_only tests/services/test_analytics.py::test_cold_start_cafe_only tests/services/test_analytics.py::test_cold_start_mixed tests/services/test_analytics.py -k cold_start -q</automated>
  </verify>
  <done>The three D-15 tests pass; every other -k cold_start test (existing Phase 6 brew-only ones) stays green.</done>
</task>

<task type="auto">
  <name>Task 6: Add one-line guard comments to get_top_coffees + get_sweet_spots (D-14, D-16); confirm exclusion tests pass</name>
  <files>app/services/analytics.py</files>
  <read_first>
    - app/services/analytics.py:47-71 — get_top_coffees body. UNCHANGED; only a docstring/inline-comment edit.
    - app/services/analytics.py:191-230 — get_sweet_spots body. UNCHANGED; only a docstring/inline-comment edit.
    - 16-CONTEXT.md § Decisions D-14 + D-16 — explicit "stays brew-only" with the rationale (cafe coffees have no `coffees` row identity for top_coffees; cafe logs have no brew-parameter fields for sweet_spots).
    - 16-RESEARCH.md § Anti-Patterns to Avoid line 1139 — "Do NOT UNION cafe data into `get_sweet_spots` / `get_top_coffees`."
    - 16-RESEARCH.md § "Sweet-spots guard comment (D-16)" lines 1267-1287 — the canonical comment text.
    - 16-PATTERNS.md § "Modified-file pattern assignments — analytics.py" lines 877-885 — concrete one-line comment text for both functions.
  </read_first>
  <behavior>
    - test_sweet_spots_excludes_cafe turns GREEN: inserting a CafeLog whose data would (if accidentally UNIONed) shift sweet-spots leaves the output BIT-IDENTICAL.
    - test_top_coffees_excludes_cafe turns GREEN: inserting a high-rated CafeLog does not affect get_top_coffees output (the JOIN to Coffee filters cafe out — cafe coffees have no coffees row).
    - These two tests verify the AFTER-state of D-14 + D-16: the bodies never UNION cafe data; the new comments are a guard against future contributors "helpfully" adding UNIONs.
  </behavior>
  <action>
    Edit app/services/analytics.py at the get_top_coffees function. Add a one-line comment immediately after the docstring closing triple-quote: `# CAFE-04 not applicable: cafe coffees have no row in coffees table by design (D-14). Do not UNION cafe data into this query — a future "Top cafe tastings" widget belongs in Phase 17 (IA restructure) or Phase 19 (AI page), not here.` Edit get_sweet_spots similarly: add `# NOTE (CAFE-05 / D-16): Cafe logs are intentionally excluded — they have no brew-parameter fields (no recipe_id, brewer_id, dose, yield, temp, or grind setting). Do not UNION cafe data into this query.` Do NOT touch the query bodies. Run `docker compose cp app/services/analytics.py coffee-snobbery:/app/app/services/analytics.py`. Verify the two exclusion tests pass — they should pass even before adding the comments (the bodies were never broken); this task is primarily about adding the comment guards as a forward-looking defense.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py::test_sweet_spots_excludes_cafe tests/services/test_analytics.py::test_top_coffees_excludes_cafe -x -q &amp;&amp; docker compose exec coffee-snobbery python -c "src = open('/app/app/services/analytics.py').read(); assert 'CAFE-04 not applicable' in src and 'CAFE-05 / D-16' in src, 'guard comments missing'"</automated>
  </verify>
  <done>Both exclusion tests pass; the two guard comments exist verbatim in analytics.py (verified by Python assertion on the file source).</done>
</task>

<task type="auto">
  <name>Task 7: Ruff format + check + plan-scope pytest gate against BAKED image</name>
  <files>app/services/analytics.py, tests/services/test_analytics.py</files>
  <read_first>
    - CLAUDE.md § "Code conventions" — ruff format before commit; ruff check warnings-as-errors.
    - project memory `executors-skip-ruff-ci-gates-both` — CI gates on BOTH `ruff format --check` AND `ruff check`; run both before declaring the plan done.
    - project memory `snobbery-test-gate-runtime` — BAKED tree (no source bind-mount); rebuild the image before the full gate.
    - project memory `tests-pass-by-skip-mask-green` — run with `-rs` so any silent skip is visible.
  </read_first>
  <behavior>
    - `ruff format --check app/services/analytics.py tests/services/test_analytics.py` exits 0.
    - `ruff check app/services/analytics.py tests/services/test_analytics.py` exits 0 (zero warnings).
    - After rebuilding the coffee-snobbery image, `pytest tests/services/test_analytics.py -q -rs` is green with NO skips except the explicit human/UAT-tagged ones (none in this plan's scope).
    - Every named test from VALIDATION.md § "Per-Task Verification Map" rows tagged 16-05 passes (all 11 analytics tests listed in Task 1's behavior block).
  </behavior>
  <action>
    On the host, run `ruff format app/services/analytics.py tests/services/test_analytics.py` (apply formatting) then `ruff format --check app/services/analytics.py tests/services/test_analytics.py` (verify). Run `ruff check app/services/analytics.py tests/services/test_analytics.py` and resolve any warning (NOT --fix on tests — fix manually so behavior never changes silently). Rebuild the image: `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery`. Drop the test database (project memory full-suite-test-isolation-gaps): `docker compose exec coffee-snobbery-db psql -U $POSTGRES_USER -d postgres -c "DROP DATABASE IF EXISTS snobbery_test"`. Run the gate: `docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py -q -rs`. Commit nothing in this task — committing is the plan-orchestrator's responsibility after gsd-verify-phase.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_analytics.py -q -rs 2>&amp;1 | tail -10</automated>
  </verify>
  <done>ruff format --check + ruff check both exit 0; full plan-scope pytest run against the rebuilt image shows N passed, 0 failed, 0 skipped (or skips ONLY from explicit human-UAT gates documented as such). The output bears "X passed" with X >= 11 new tests + the pre-existing Phase 6 analytics tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /cafe-logs/* (multipart form) | Untrusted user input crosses here — origin_country (free text), notes (free text), photo bytes. Covered by plan 16-02 / 16-03. This plan (16-05) operates on data ALREADY persisted via those routes. |
| nightly scheduler → analytics functions | Trusted intra-process boundary — APScheduler runs `compute_input_signature` against the database. Per-user scoping is the only access-control surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Tampering | analytics.py raw SQL (get_flavor_descriptors + get_cold_start_counts UNION ALL of unnest blocks) | mitigate | Bound `:user_id` parameter on every raw SQL block (referenced twice on each side of the UNION ALL; psycopg supports single-key dict with multiple references). No string interpolation, no f-string concatenation of user-controlled values. Mirrors the existing brew-only pattern at analytics.py:170-183 + :326-336. ASVS V5 / V13. |
| T-16-05-02 | Information Disclosure | per-user scoping on the new cafe-side WHERE clauses | mitigate | Every new SELECT (compute_input_signature cafe_stmt, get_preference_profile cafe_origin + cafe_roaster, get_flavor_descriptors UNION inner cafe block, get_cold_start_counts cafe scalar + distinct-notes UNION inner cafe block) carries `CafeLog.user_id == user_id` or `cl.user_id = :user_id`. user_id is a typed function arg, never a global. T-06-01 IDOR-defense invariant from analytics.py module docstring lines 6-9 extended to cafe-side queries verbatim. ASVS V4. |
| T-16-05-03 | Tampering | signature shape change forces one-time AI regen (Pitfall 9) | accept | One-time churn at deploy is the chosen disposition (Option (a) recommended in 16-RESEARCH.md). Cost: ~6 users × 1 extra AI call. Documented in SUMMARY for operator awareness. NOT a security bug — semantically correct + idempotent. |
| T-16-05-04 | Information Disclosure | flavor_descriptors / cold_start UNION ALL revealing notes across a user's brew + cafe history | accept | Per-user scope on both sides of the UNION ensures no cross-user leakage. Within a user's own data, the union is the desired behavior (CAFE-04). Not a leak vector. |
| T-16-05-05 | Tampering | future contributor "helpfully" UNIONing cafe data into get_top_coffees or get_sweet_spots | mitigate | One-line guard comments added to both functions (D-14 + D-16) per Task 6. Plus 16-RESEARCH.md § Anti-Patterns to Avoid documents the rule. Plus the existing test_top_coffees_excludes_cafe + test_sweet_spots_excludes_cafe assert the AFTER-state. Defense-in-depth: code comment + research doc + automated test. |
| T-16-05-06 | Spoofing | cafe_log_id / coffee_id namespace collision in signature payload (Pitfall 3) | mitigate | Payload shape is `[brew_list, cafe_list]` — two SUBLISTS, not a flat concatenation. The sublist position is the namespace. A brew row with coffee_id=N and a cafe row with id=N cannot produce identical 5-tuples in the payload because they are in different outer-list positions. Documented in compute_input_signature docstring; covered by test_signature_includes_cafe_logs which asserts adding a rated cafe row mutates the signature. |
</threat_model>

<verification>
## Plan-Level Checks

After all 7 tasks complete:

1. **All 11 named analytics tests from 16-VALIDATION.md per-task map (plan 16-05 rows) pass**:
   - test_signature_includes_cafe_logs
   - test_signature_excludes_unrated_cafe
   - test_preference_profile_origin_unions_cafe
   - test_preference_profile_roaster_unions_cafe
   - test_preference_profile_process_brew_only
   - test_flavor_descriptors_unions_cafe
   - test_cold_start_brew_only
   - test_cold_start_cafe_only
   - test_cold_start_mixed
   - test_sweet_spots_excludes_cafe
   - test_top_coffees_excludes_cafe

2. **No silent skips** — full plan-scope pytest run includes `-rs`; the only acceptable skips are unrelated human-UAT items (none in this plan).

3. **Guard comments present** — `grep -E "CAFE-04 not applicable|CAFE-05 / D-16" app/services/analytics.py` returns both matches.

4. **Bound `:user_id` parameter only** — no f-string SQL or string interpolation in analytics.py raw SQL blocks.

5. **CafeLog import added** — `grep -n "from app.models.cafe_log import CafeLog" app/services/analytics.py` returns one line.

6. **Dict shape preserved for cold-start UI consumer** — `grep -E "gate\\.(sessions|distinct_notes|sessions_needed|notes_needed)" app/templates/fragments/home/_cold_start.html` returns 4+ lines (unchanged); template file is NOT in files_modified, NOT edited.

7. **ruff format --check + ruff check both green** on the two edited files.

8. **Pre-existing Phase 6 analytics tests still green** — running `pytest tests/services/test_analytics.py -q -rs` shows >50 passing tests (the existing Phase 6 brew-only coverage + the 11 new tests; no regressions).

9. **D-coverage gate satisfied** — every D-NN in the requirements scope (D-12, D-13, D-14, D-15, D-16) appears in must_haves.truths above (project memory `decision-coverage-gate-scans-musthaves`).
</verification>

<success_criteria>
- CAFE-04 satisfied: cafe rated rows + flavor notes + origin/roaster contribute to compute_input_signature, get_preference_profile (origin + roaster + flavor_descriptors), and the cold-start gate. process and roast_level dims stay brew-only per D-13.
- CAFE-05 satisfied: get_sweet_spots and get_top_coffees query bodies are UNCHANGED. Both gain one-line guard comments documenting WHY cafe data is intentionally excluded. Automated tests assert the AFTER-state.
- Pitfall 9 acknowledged: signature shape change documented in SUMMARY as "expected one-time AI regen on first nightly cadence post-deploy"; NOT avoided via conditional payload.
- Test-gate green: ruff format/check + plan-scope pytest run pass against the BAKED image, with no silent skips.
- All 16-VALIDATION.md plan 16-05 analytics rows tick to ✅ (the photo orphan sweep row moves to plan 16-06 for traceability).
</success_criteria>

<output>
After completion:

1. Create `.planning/phases/16-cafe-quick-rate/16-05-SUMMARY.md` covering: files touched, functions edited, the Pitfall 9 one-time-regen acknowledgement, the test coverage table mapping every CAFE-04 / CAFE-05 requirement to its pytest name, and any deviations from the plan (expected: none).
2. Confirm the 16-VERIFICATION-AUTO.md ledger (project memory `verifier-overwrites-human-verification-ledger` — write to `*-VERIFICATION-AUTO.md`, NOT `*-VERIFICATION.md`) updates the 11 plan-16-05 analytics rows from ❌ to ✅. The photo orphan sweep row is owned by plan 16-06.
3. Note in SUMMARY: the first nightly AI cadence post-deploy will regenerate every user's AI recommendation due to the signature payload shape change. Cost at household scale: <$0.10 total. Operator-facing visibility via the existing audit log on ai_recommendations.model_used.
</output>
