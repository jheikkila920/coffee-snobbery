---
phase: 16-cafe-quick-rate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/cafe_log.py
  - app/migrations/versions/p16_cafe_logs.py
  - tests/conftest.py
  - tests/migrations/test_cafe_logs_migration.py
autonomous: true
requirements: [CAFE-01, CAFE-02, CAFE-03, CAFE-04, CAFE-05, CAFE-06]
tags: [snobbery, postgres, sqlalchemy, alembic, gin-index, migration]

must_haves:
  truths:
    - "Per D-01: a new cafe_logs table exists (separate from brew_sessions, additive-only)"
    - "Per D-02: cafe_logs.roaster_id is a nullable FK to roasters.id with ondelete=SET NULL"
    - "Per D-03: cafe_logs.origin_country is a plain TEXT nullable column (no FK, no countries lookup table)"
    - "Per D-04: cafe_logs.flavor_note_ids is BIGINT[] NOT NULL DEFAULT '{}' with a GIN index emitted via op.execute()"
    - "Per D-05: cafe_logs.brew_method is a plain nullable TEXT column (no enum, no FK)"
    - "alembic upgrade head succeeds on top of p15_1_varietal_m2m and \\d cafe_logs shows both the DESC b-tree index and the GIN index"
    - "_require_cafe_logs_table() skip-gate exists in tests so downstream test files do not silently mask a missing migration (project memory tests-pass-by-skip-mask-green)"
  artifacts:
    - path: "app/models/cafe_log.py"
      provides: "CafeLog SQLAlchemy 2.0 model with Mapped[...] columns mirroring brew_session.py FK directionality"
      contains: "class CafeLog(Base)"
    - path: "app/migrations/versions/p16_cafe_logs.py"
      provides: "Alembic revision creating cafe_logs table + DESC b-tree index + GIN index via op.execute()"
      contains: "revision: str = \"p16_cafe_logs\""
    - path: "tests/conftest.py"
      provides: "_require_cafe_logs_table() skip-gate helper used by every cafe-related test file"
      contains: "def _require_cafe_logs_table"
    - path: "tests/migrations/test_cafe_logs_migration.py"
      provides: "smoke test that exercises upgrade + downgrade chain on top of p15_1_varietal_m2m"
      contains: "def test_cafe_logs_migration"
  key_links:
    - from: "app/models/cafe_log.py"
      to: "app/models/base.py"
      via: "from app.models.base import Base"
      pattern: "from app.models.base import Base"
    - from: "app/migrations/versions/p16_cafe_logs.py"
      to: "app/migrations/versions/p15_1_varietal_m2m.py"
      via: "down_revision string"
      pattern: "down_revision.*p15_1_varietal_m2m"
    - from: "tests/migrations/test_cafe_logs_migration.py"
      to: "app/migrations/versions/p16_cafe_logs.py"
      via: "alembic upgrade + downgrade calls under a sandbox engine"
      pattern: "alembic|upgrade|downgrade"
---

<objective>
Create the `cafe_logs` table — the data foundation for every other Phase 16 plan. Adds one SQLAlchemy 2.0 model, one Alembic migration with hand-edited GIN + DESC B-tree indices, and the `_require_cafe_logs_table()` test skip-gate that downstream plans will reuse.

Purpose: Without the table, no other Phase 16 work can run. Lands the locked column shapes (D-01..D-05) once so 16-02..16-05 can build against them without re-litigating types.
Output: A schema migration that applies cleanly on top of `p15_1_varietal_m2m`, a CafeLog model that mirrors `BrewSession`, and a test scaffold so downstream tests don't silently skip when the migration hasn't run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cafe-quick-rate/16-CONTEXT.md
@.planning/phases/16-cafe-quick-rate/16-RESEARCH.md
@.planning/phases/16-cafe-quick-rate/16-PATTERNS.md
@.planning/phases/16-cafe-quick-rate/16-VALIDATION.md
@app/models/brew_session.py
@app/models/coffee.py
@app/models/coffee_origin.py
@app/migrations/versions/p5_brew_sessions.py
@app/migrations/versions/p15_1_varietal_m2m.py
@app/migrations/versions/p15_1_multi_origin.py
@tests/services/test_analytics.py

<interfaces>
Key references the executor needs (do not re-derive — these are the source of truth):

From `app/models/brew_session.py` (lines 73-110, 127-131, 149-161):
- ondelete="RESTRICT" on user_id (history is precious)
- ondelete="SET NULL" on optional shared-catalog FKs
- ARRAY(BigInteger) + server_default=text("'{}'::bigint[]") for flavor_note_ids_observed
- `__table_args__` declares DESC b-tree via Index(..., text("logged_at DESC")); GIN is NOT declared in the model

From `app/migrations/versions/p5_brew_sessions.py`:
- "Alembic-safe convention": migration body does NOT import from `app.models`; schema is described inline with `sa.Column` / `sa.ForeignKey`
- `op.execute("CREATE INDEX ... ON ... USING GIN (...)")` is the hand-edit idiom for GIN
- `op.execute("CREATE INDEX ... ON ... (col, other_col DESC)")` is the hand-edit idiom for DESC sort direction
- `downgrade()` drops indexes (IF EXISTS) before dropping the table

From `app/migrations/versions/p15_1_varietal_m2m.py`:
- Confirmed current head revision id is `p15_1_varietal_m2m`

From `tests/services/test_analytics.py` (lines 24-49, approximate):
- `_require_postgres()` / `_require_analytics_tables()` use `pytest.skip(...)` based on `to_regclass(...)` lookups
- New gate must call `pytest.skip(...)` (not raise) so the test session continues
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CafeLog model + Alembic migration with hand-edited GIN</name>
  <files>app/models/cafe_log.py, app/migrations/versions/p16_cafe_logs.py</files>
  <read_first>
    - app/models/brew_session.py (FK directionality lines 73-110; ARRAY column lines 127-131; __table_args__ lines 149-161)
    - app/models/coffee_origin.py (plain Text country pattern — confirms cafe_name + origin_country are plain TEXT, NOT CITEXT)
    - app/models/base.py (Base import path)
    - app/migrations/versions/p5_brew_sessions.py (Alembic-safe convention; inline sa.Column shape; op.execute for GIN + DESC b-tree; downgrade ordering)
    - app/migrations/versions/p15_1_varietal_m2m.py (confirms current head revision string)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "1. app/models/cafe_log.py" and "9. app/migrations/versions/p16_cafe_logs.py"
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md Pattern 1 (model), Pattern 2 (migration), Pitfall 1 (autogen cannot emit USING GIN), Pitfall 4 (down_revision)
  </read_first>
  <behavior>
    - The cafe_logs table exists post-migration with EXACTLY the columns and types listed in the action below (per D-01..D-05 + Claude's discretion column shapes locked in CONTEXT.md).
    - alembic upgrade head succeeds on top of p15_1_varietal_m2m and `\\d cafe_logs` in psql shows BOTH ix_cafe_logs_user_logged_at (DESC b-tree on user_id, logged_at) AND ix_cafe_logs_flavor_note_ids (GIN on flavor_note_ids).
    - alembic downgrade -1 cleanly removes the table and both indexes (DROP INDEX IF EXISTS before DROP TABLE).
    - The CafeLog Mapped[...] model imports cleanly with `from app.models.cafe_log import CafeLog` and is discoverable through `app.models.__init__` if that module re-exports models (mirror brew_session import discoverability).
  </behavior>
  <action>
    Create `app/models/cafe_log.py` defining `class CafeLog(Base)` with `__tablename__ = "cafe_logs"` and these typed Mapped columns (column types and nullability per CONTEXT.md D-01..D-05 + locked Claude's-discretion shapes):
    - id: BigInteger Identity(always=False) primary_key
    - user_id: BigInteger ForeignKey("users.id", ondelete="RESTRICT") nullable=False (per D-01 / CONTEXT § Claude's discretion)
    - roaster_id: BigInteger ForeignKey("roasters.id", ondelete="SET NULL") nullable=True (per D-02)
    - cafe_name: Text nullable=False (plain TEXT, NOT CITEXT — mirrors coffee_origins.country precedent; per CONTEXT § Claude's discretion)
    - origin_country: Text nullable=True (per D-03 — plain TEXT, no FK)
    - brew_method: Text nullable=True (per D-05 — free-text, no enum)
    - rating: Numeric(3, 2) nullable=True (mirrors brew_sessions.rating; 0-5 in 0.25 steps; per CONTEXT § Claude's discretion)
    - flavor_note_ids: ARRAY(BigInteger) nullable=False server_default=text("'{}'::bigint[]") (per D-04)
    - notes: Text nullable=False server_default="" (per CONTEXT § Claude's discretion)
    - photo_filename: Text nullable=True (per CONTEXT § Claude's discretion)
    - logged_at: TIMESTAMP(timezone=True) nullable=False server_default=func.now() (per CONTEXT § Claude's discretion — editable via form to backfill)
    - created_at: TIMESTAMP(timezone=True) nullable=False server_default=func.now()
    - updated_at: TIMESTAMP(timezone=True) nullable=False server_default=func.now()
    Declare `__table_args__ = (Index("ix_cafe_logs_user_logged_at", "user_id", text("logged_at DESC")),)` and add the explicit comment "GIN on flavor_note_ids is NOT declared here — see migration p16_cafe_logs.py" exactly as shown in PATTERNS.md § 1.

    Create `app/migrations/versions/p16_cafe_logs.py` following the Alembic-safe convention (no imports from `app.models`):
    - revision: str = "p16_cafe_logs"
    - down_revision: str | Sequence[str] | None = "p15_1_varietal_m2m" (current head — verified)
    - branch_labels = None; depends_on = None
    - upgrade(): op.create_table("cafe_logs", ...) with the same columns/types as the model (declared inline via sa.Column / sa.ForeignKey / postgresql.ARRAY / sa.TIMESTAMP); then `op.execute("CREATE INDEX ix_cafe_logs_user_logged_at ON cafe_logs (user_id, logged_at DESC)")`; then `op.execute("CREATE INDEX ix_cafe_logs_flavor_note_ids ON cafe_logs USING GIN (flavor_note_ids)")`. The GIN line is hand-edited per Pitfall 1 — alembic autogenerate cannot emit USING GIN for ARRAY columns.
    - downgrade(): `op.execute("DROP INDEX IF EXISTS ix_cafe_logs_flavor_note_ids")` then `op.execute("DROP INDEX IF EXISTS ix_cafe_logs_user_logged_at")` then `op.drop_table("cafe_logs")` (mirror p5_brew_sessions.py downgrade ordering).

    If `app/models/__init__.py` re-exports brew_session / coffee / etc., add a parallel `from app.models.cafe_log import CafeLog` line — do not re-export selectively, mirror the existing pattern verbatim.

    Do NOT add a `coffee_id` FK (D-01 rejection). Do NOT add CITEXT to cafe_name or origin_country (PATTERNS.md § 1 — explicit). Do NOT introduce a `countries` lookup table (D-03 rejection).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery alembic upgrade head &amp;&amp; docker compose exec coffee-snobbery-db psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "\\d cafe_logs" | grep -E "ix_cafe_logs_(user_logged_at|flavor_note_ids)"</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/cafe_log.py` contains `class CafeLog(Base):` and `__tablename__ = "cafe_logs"`
    - The model declares all 13 columns with the exact types and nullable settings listed in the action above
    - `app/migrations/versions/p16_cafe_logs.py` declares `revision = "p16_cafe_logs"` and `down_revision = "p15_1_varietal_m2m"` (single head; no fork)
    - The migration body contains `op.execute(...USING GIN...)` for the flavor_note_ids index (Pitfall 1 fix)
    - `docker compose exec coffee-snobbery alembic upgrade head` exits 0 with no "Multiple head revisions" or "Can't locate revision" errors (Pitfall 4 negative test)
    - After upgrade, `\\d cafe_logs` in psql lists both `ix_cafe_logs_user_logged_at` (b-tree) AND `ix_cafe_logs_flavor_note_ids` (gin) — grep matches both index names
    - `alembic downgrade -1` followed by `alembic upgrade head` is idempotent (round-trip green)
    - The migration body contains no `from app.models` imports (Alembic-safe convention)
  </acceptance_criteria>
  <done>The cafe_logs table exists in the dev container with both indexes present and visible to psql; the migration round-trips cleanly; the CafeLog model imports without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _require_cafe_logs_table() skip-gate + migration smoke test</name>
  <files>tests/conftest.py, tests/migrations/test_cafe_logs_migration.py</files>
  <read_first>
    - tests/services/test_analytics.py (lines 24-49 — `_require_postgres()` + `_require_analytics_tables()` skip-gate helpers; the pytest.skip(...) idiom)
    - tests/conftest.py (current root conftest — confirm where helpers live and how engine fixtures expose the test DB)
    - app/migrations/versions/p16_cafe_logs.py (the file you just created in Task 1)
    - .planning/phases/16-cafe-quick-rate/16-PATTERNS.md § "10. tests/services/test_cafe_logs.py" (skip-gate text + project memory `tests-pass-by-skip-mask-green`)
    - .planning/phases/16-cafe-quick-rate/16-VALIDATION.md § "Wave 0 Requirements"
    - .planning/phases/16-cafe-quick-rate/16-RESEARCH.md Pitfall 6 (tests-pass-by-skip-mask-green)
  </read_first>
  <behavior>
    - `_require_cafe_logs_table()` is callable from any test module via `from tests.conftest import _require_cafe_logs_table` (or whatever the existing import path for `_require_analytics_tables` is — mirror it exactly).
    - When the cafe_logs table is absent, _require_cafe_logs_table() calls `pytest.skip(...)` with a message that names the migration revision (`p16_cafe_logs`) so a failed pytest -rs surfaces "cafe_logs table not present — migration p16_cafe_logs not applied".
    - When the table is present, _require_cafe_logs_table() returns None silently and tests proceed.
    - `pytest tests/migrations/test_cafe_logs_migration.py -x -v` collects at least one test and that test exercises the upgrade path on top of p15_1_varietal_m2m. (Direct rebuke against Pitfall 13 / project memory `validation-md-vacuous-k-filters`: the file must actually contain a passing test, not just exist.)
  </behavior>
  <action>
    Add `_require_cafe_logs_table()` helper to `tests/conftest.py` (or to the same module that holds `_require_analytics_tables()` — mirror placement). The helper imports `text` from sqlalchemy and `engine` from `app.db`, opens a connection, runs `SELECT to_regclass('public.cafe_logs')`, and calls `pytest.skip("cafe_logs table not present — migration p16_cafe_logs not applied")` if the result is None. The helper wraps the import + connect block in try/except so import or DB-connection failures degrade to `pytest.skip(...)` with the underlying exception class name, exactly mirroring the existing `_require_analytics_tables()` pattern at tests/services/test_analytics.py lines 24-49.

    Create `tests/migrations/test_cafe_logs_migration.py` with a `test_cafe_logs_migration_upgrade_downgrade()` function that:
    - Calls `_require_postgres()` + `_require_cafe_logs_table()` at the top (gating).
    - Runs `SELECT to_regclass('public.cafe_logs')` and asserts not-None (upgrade success proof).
    - Runs `SELECT indexname FROM pg_indexes WHERE tablename = 'cafe_logs'` and asserts both `ix_cafe_logs_user_logged_at` and `ix_cafe_logs_flavor_note_ids` appear in the result set.
    - Runs `SELECT amname FROM pg_indexes JOIN pg_class ON pg_class.relname = indexname JOIN pg_am ON pg_am.oid = pg_class.relam WHERE indexname = 'ix_cafe_logs_flavor_note_ids'` (or equivalent pg_class lookup) and asserts the GIN access method (`gin`) — proves Pitfall 1 fix landed and we didn't ship a silent b-tree.
    - Asserts `SELECT current_setting('server_version_num')::int >= 160000` (gate on Postgres 16+ matching the stack pin) — skip if older.
    - Use `with engine.connect() as conn:` block (mirror the analytics test pattern); do not open transactions you do not close.

    Do NOT add a test that actually runs `alembic downgrade -1` then `upgrade head` mid-suite — that mutates DB state for parallel tests. The round-trip check happened manually in Task 1's verify step; the migration-smoke test only inspects the post-upgrade state.
  </action>
  <verify>
    <automated>docker compose cp tests/ coffee-snobbery:/app/tests/ &amp;&amp; docker compose exec coffee-snobbery python -m pytest tests/migrations/test_cafe_logs_migration.py -x -v -rs 2>&amp;1 | grep -E "(PASSED|FAILED|SKIPPED|ERROR)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` (or the same module hosting `_require_analytics_tables`) exports `_require_cafe_logs_table` as a callable
    - Calling `_require_cafe_logs_table()` with the cafe_logs table missing emits a pytest.skip with the literal substring "p16_cafe_logs" in the message (Pitfall 6 / project memory `tests-pass-by-skip-mask-green` — name the migration revision so -rs output is actionable)
    - `pytest --collect-only tests/migrations/test_cafe_logs_migration.py` collects ≥1 test (project memory `validation-md-vacuous-k-filters` — non-empty filter)
    - Running the test against the current dev container (where Task 1 already applied) returns PASSED, not SKIPPED, with the GIN-access-method assertion exercised
    - The smoke test does NOT execute `alembic downgrade -1` (avoids cross-test DB state mutation)
    - `pytest tests/migrations/test_cafe_logs_migration.py -q -rs` shows zero skips on a healthy stack
  </acceptance_criteria>
  <done>The skip-gate helper exists and downstream test files can import it; the migration smoke test passes and asserts both indexes plus the GIN access method.</done>
</task>

<task type="auto">
  <name>Task 3: Lint clean + commit gate (ruff format + ruff check)</name>
  <files>app/models/cafe_log.py, app/migrations/versions/p16_cafe_logs.py, tests/conftest.py, tests/migrations/test_cafe_logs_migration.py</files>
  <read_first>
    - .planning/STATE.md (project memory `executors-skip-ruff-ci-gates-both` — CI gates on BOTH ruff format --check AND ruff check)
    - CLAUDE.md § "Code conventions" (ruff format / ruff check before commit; treat warnings as errors)
  </read_first>
  <action>
    Run `docker compose exec coffee-snobbery ruff format app/models/cafe_log.py app/migrations/versions/p16_cafe_logs.py tests/conftest.py tests/migrations/test_cafe_logs_migration.py` and `docker compose exec coffee-snobbery ruff check --fix app/models/cafe_log.py app/migrations/versions/p16_cafe_logs.py tests/conftest.py tests/migrations/test_cafe_logs_migration.py`. Resolve any remaining warnings or errors (do NOT add `# noqa` to suppress real issues; fix them). Per project memory `executors-skip-ruff-ci-gates-both`, CI runs `ruff format --check` AND `ruff check` — both must be green or CI fails after merge.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery ruff format --check app/models/cafe_log.py app/migrations/versions/p16_cafe_logs.py tests/conftest.py tests/migrations/test_cafe_logs_migration.py &amp;&amp; docker compose exec coffee-snobbery ruff check app/models/cafe_log.py app/migrations/versions/p16_cafe_logs.py tests/conftest.py tests/migrations/test_cafe_logs_migration.py</automated>
  </verify>
  <acceptance_criteria>
    - `ruff format --check` exits 0 on the four files touched by this plan
    - `ruff check` exits 0 on the same four files
    - No new `# noqa` comments added (the existing repo uses noqa sparingly and only for B008-style FastAPI Depends defaults — read app/routers/brew.py to see the established pattern)
  </acceptance_criteria>
  <done>Both ruff gates pass cleanly so the wave-1 merge will not regress CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External user → DB row | A future POST /cafe-logs handler (Plan 16-02) will INSERT rows here; this plan declares the schema invariants those handlers must respect |
| Migration runtime → live DB | entrypoint.sh runs alembic upgrade head as root before dropping to UID 1000 (per DEBT-01 / G-01 fix); this plan's migration must be idempotent and safe under that exec posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | cafe_logs.user_id FK | mitigate | Declare ondelete="RESTRICT" so a user delete cannot orphan or silently rewrite cafe history (mirrors brew_sessions invariant per RESEARCH.md Security Domain V4) |
| T-16-01-02 | Information Disclosure | cafe_logs row visibility | accept (defer to 16-02) | The schema itself has no per-user filter; per-user IDOR scoping is enforced at the service/router tier in plan 16-02. Recording disposition here so 16-02's threat model picks it up. |
| T-16-01-03 | Denial of Service | flavor_note_ids ARRAY queries | mitigate | GIN index on flavor_note_ids (declared via op.execute per Pitfall 1) so containment queries from analytics.py UNIONs in plan 16-05 stay sub-linear |
| T-16-01-04 | Tampering | migration chain integrity | mitigate | down_revision pinned to verified head `p15_1_varietal_m2m`; migration body uses Alembic-safe inline schema (no `from app.models` import) so a future model rename cannot retroactively break this revision (Pitfall 4) |
| T-16-01-05 | Information Disclosure | photo_filename field | accept | photo_filename is a server-set UUID-hex string (decided in plan 16-02 / 16-03 router); the column itself is plain Text and carries no auth signal |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery alembic heads` returns exactly one head, equal to `p16_cafe_logs`
- `\d cafe_logs` in psql lists both indexes (ix_cafe_logs_user_logged_at + ix_cafe_logs_flavor_note_ids)
- `SELECT amname FROM pg_am JOIN pg_class ON pg_am.oid = pg_class.relam WHERE pg_class.relname = 'ix_cafe_logs_flavor_note_ids'` returns `gin` (proves Pitfall 1 fix)
- `pytest tests/migrations/test_cafe_logs_migration.py -q -rs` passes with zero skips on a healthy stack
- `pytest tests/ -q -rs --collect-only -k cafe_logs` collects ≥1 test (project memory `validation-md-vacuous-k-filters` defense)
</verification>

<success_criteria>
- New table `cafe_logs` exists with all 13 columns at the locked types/nullability (D-01..D-05 + locked Claude's-discretion shapes)
- Both indexes (DESC b-tree + GIN) created via op.execute() (Pitfall 1 / 4 defended)
- CafeLog model imports without errors
- `_require_cafe_logs_table()` helper exists and downstream tests can import it
- Migration smoke test passes and asserts both index names + GIN access method
- ruff format --check + ruff check both green on the four files
</success_criteria>

<output>
After completion, create `.planning/phases/16-cafe-quick-rate/16-01-SUMMARY.md` documenting:
- Final column types/nullable settings as shipped
- Alembic revision id + down_revision (proof of single-head chain)
- Output of `\d cafe_logs` (or equivalent) showing both indexes
- Output of `pytest tests/migrations/test_cafe_logs_migration.py -q -rs`
- Any deviation from PATTERNS.md § 1 / § 9 with justification
</output>
</content>
</invoke>