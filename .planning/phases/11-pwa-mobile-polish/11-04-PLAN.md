---
phase: 11-pwa-mobile-polish
plan: 04
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified:
  - app/routers/brew_guided.py
  - app/templates/pages/brew_guided.html
  - app/static/js/alpine-components/guided-brew-mode.js
  - app/static/js/vendor/NoSleep.min.js
  - app/templates/pages/brew_form.html
  - app/templates/fragments/recipe_row.html
  - app/routers/brew.py
  - app/main.py
  - tests/test_gbm.py
autonomous: false
requirements: [BREW-12, BREW-13]
user_setup: []

must_haves:
  truths:
    - "GET /brew/guided?recipe_id=N renders a full-screen GBM page with the recipe steps; the bottom nav is hidden"
    - "A recipe with zero steps disables the launch with a 'no steps' message linking to the recipe builder"
    - "GBM auto-advances steps on elapsed time and supports a manual Next-step skip, with chime + vibration cues gated by localStorage toggles"
    - "Wake lock is requested on start, re-acquired on visibilitychange, with a visible 'screen stays on' indicator and an iOS NoSleep.js fallback"
    - "Done brewing returns to the session form prefilled: form-entry prefills coffee+recipe, recipe-entry prefills recipe only, both carrying brew_time"
    - "The brew form renders an editable brew_time_seconds field and reads ?brew_time= on the GBM completion path"
    - "Entry points exist: 'Brew with timer' on the brew form (coffee+recipe selected) and 'Start guided brew' on the recipe row"
  artifacts:
    - path: "app/routers/brew_guided.py"
      provides: "GET /brew/guided page route (require_user, 404 on missing recipe)"
      contains: "guided"
    - path: "app/templates/pages/brew_guided.html"
      provides: "Full-screen GBM page (start/timer/completion screens), NoSleep.js via head_extra"
      contains: "guidedBrewMode"
    - path: "app/static/js/alpine-components/guided-brew-mode.js"
      provides: "guidedBrewMode component: timer, audio, vibration, wake lock + fallback, cue prefs"
      contains: "Alpine.data('guidedBrewMode'"
    - path: "app/static/js/vendor/NoSleep.min.js"
      provides: "Self-hosted iOS wake-lock fallback (no CDN, satisfies connect-src 'self')"
  key_links:
    - from: "app/templates/pages/brew_guided.html"
      to: "guided-brew-mode.js"
      via: "x-data + data-steps JSON"
      pattern: "data-steps"
    - from: "guided-brew-mode.js"
      to: "/brew/new"
      via: "completion redirect with prefill params"
      pattern: "/brew/new"
    - from: "app/routers/brew.py:/brew/new"
      to: "brew_form.html brew_time_seconds field"
      via: "reads ?brew_time= and seeds the form value"
      pattern: "brew_time"
    - from: "app/main.py"
      to: "app.routers.brew_guided"
      via: "include_router before brew_router to avoid /brew/{id} shadowing"
      pattern: "include_router\\(brew_guided"
---

<objective>
Ship Guided Brew Mode (BREW-12, BREW-13): the full-screen, recipe-driven timer with a large countdown, current-step highlight (cumulative water target + elapsed time), audio chime + vibration cues at step transitions (each configurable + live-toggleable), pause/resume, manual next-step skip alongside auto-advance, cancel-without-logging, wake lock with iOS fallback (re-acquired on visibilitychange, with a visible indicator), and a "Done brewing" flow that returns to the session form with the recipe/coffee and elapsed brew time prefilled. It wires the two entry points (D-07) and renders the editable `brew_time_seconds` field on the brew form, reading it on the GBM completion path.

Purpose: This is the largest net-new surface of the phase and the carried multi-phase research flag (iOS Wake Lock fallback). It owns ALL brew_form.html edits in this phase (the "Brew with timer" button + the brew_time field + the D-20 sticky-actions-above-nav offset) so it does not collide with the polish plan. It depends on Plan 02 (the brew_time_seconds column/schema) and Plan 03 (the `{% block head_extra %}` in base.html for NoSleep.js, and the nav frame whose hide-rule GBM relies on).
Output: brew_guided router + full-screen page, the guidedBrewMode Alpine component, self-hosted NoSleep.js, brew-form integration (button + field + sticky offset), the recipe-row entry point, main.py registration, GBM smoke tests, and a manual iOS wake-lock validation gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pwa-mobile-polish/11-CONTEXT.md
@.planning/phases/11-pwa-mobile-polish/11-RESEARCH.md
@.planning/phases/11-pwa-mobile-polish/11-PATTERNS.md
@.planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md

<interfaces>
<!-- Contracts the executor must replicate. Extracted from codebase + research. -->

Recipe steps shape (drives the timer): `recipe.steps` is a JSONB array of pour steps carrying cumulative water (grams) and time offsets (seconds). Render to the component as `data-steps="{{ recipe.steps | tojson }}"` and JSON.parse it in init() (NO eval). A recipe with an empty/zero-length steps array CANNOT launch GBM (disable + message).

Brew router prefill (app/routers/brew.py GET /brew/new, lines 630-678): already reads `?coffee_id=`, `?recipe_id=`, and `?from=` query params and resolves prefill via `brew_sessions_service.resolve_prefill`. It does NOT yet read `?brew_time=`. The GBM completion redirect target is `/brew/new?recipe_id=N[&coffee_id=M]&brew_time=T&gbm=1`. This plan adds reading `?brew_time=` and seeding it into the form context value for the new brew_time_seconds field. Reuse the existing `_int_or_none` helper for parsing.

Routing collision risk: brew.py owns prefix `/brew` with `GET /brew/{session_id}/edit` and `POST /brew/{session_id}`. The new `/brew/guided` is a STATIC segment. To guarantee it is not captured by a `/brew/{session_id}`-style match, register brew_guided_router BEFORE brew_router in main.py (FastAPI matches in registration order). Declare `APIRouter(prefix="/brew")` with `GET /guided`, included before brew_router.

Brew form (app/templates/pages/brew_form.html): sticky actions at `sticky bottom-0 inset-x-0 z-20 ...` (line 287). D-20 requires these stack ABOVE the persistent bottom nav on mobile — change the mobile offset to `bottom-16` (64px = nav height) + `pb-[env(safe-area-inset-bottom)]` so Save/Cancel sit above the nav; keep `bottom-0` at >=768px (no bottom nav there). The brew_time_seconds field renders in the form body near the other numeric fields (inputmode="numeric", min 0, max 86400), is editable (manual entry per D-10) and is seeded from the prefill value on the gbm path. The "Brew with timer" secondary button (D-07a) appears when a coffee AND recipe are selected — `border border-espresso-700 text-espresso-700 bg-transparent` adjacent to the primary Save CTA — linking to `/brew/guided?recipe_id=<selected>&coffee_id=<selected>`.

Recipe row (app/templates/fragments/recipe_row.html): has Edit links to `/recipes/{{ recipe.id }}/edit` (lines 36-40, 75-79) in both desktop and card layouts. Add a "Start guided brew" primary button `bg-espresso-700 text-cream-50` next to Edit, linking `/brew/guided?recipe_id={{ recipe.id }}` (recipe-only entry, D-07b). If the recipe has no steps, render the disabled "no steps" state instead (UX-04 copy "Recipe has no steps." + link to the recipe builder).

head_extra block (added by Plan 03 in base.html): the GBM template populates `{% block head_extra %}` with a nonce-tagged `<script defer src="/static/js/vendor/NoSleep.min.js" nonce="{{ csp_nonce(request) }}">` — NoSleep.js loads ONLY on the GBM page, not globally (RESEARCH Pattern 3, Pitfall 5).

Wake lock / audio / vibration / cue-prefs patterns: RESEARCH Patterns 3,4,5 and PATTERNS §guided-brew-mode.js give the exact requestWakeLock/releaseWakeLock/_setupVisibilityReacquire, unlockAudio/playChime, triggerVibration, _loadCuePrefs/_saveCuePrefs ('snobbery:gbm:cues' → {chime:true,vibrate:true}), and cancelWithoutLogging (window.confirm) bodies.

NoSleep.js: self-host the v0.12.0 UMD build at app/static/js/vendor/NoSleep.min.js (RESEARCH: self-hosting satisfies connect-src 'self' and removes the CDN as a runtime dependency). The UMD build must contain no eval/new Function (verify — Assumption A5).

GBM route auth + 404 (PATTERNS §brew_guided.py): `Depends(require_user)` on the handler; if the recipe is not found raise HTTPException 404; the zero-steps case is handled in the TEMPLATE (disabled launch + message), so the route still 200s for a stepless recipe and the template renders the no-steps state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: guidedBrewMode Alpine component + self-hosted NoSleep.js</name>
  <read_first>
    - app/static/js/alpine-components/brew-draft.js (init()/destroy() lifecycle, localStorage try/catch, data-* config — PATTERNS §guided-brew-mode.js)
    - app/static/js/alpine-components/autocomplete.js (data-* attribute config idiom, lines 43-53)
    - app/static/js/alpine-components/mini-modal.js (window.confirm cancel pattern, line 53)
    - .planning/phases/11-pwa-mobile-polish/11-RESEARCH.md (Patterns 3,4,5 — wake lock, audio chime, vibration; Pitfalls 3,4,5,9; Assumption A5 NoSleep no-eval)
    - .planning/phases/11-pwa-mobile-polish/11-PATTERNS.md (§guided-brew-mode.js — exact component bodies)
    - docs/decisions/0001 (Alpine CSP eval-free contract; or the relevant ADR under docs/decisions/)
  </read_first>
  <action>
    Self-host NoSleep.js: download the v0.12.0 UMD build (https://cdn.jsdelivr.net/npm/nosleep.js@0.12.0/dist/NoSleep.min.js) to app/static/js/vendor/NoSleep.min.js and commit it. Verify it contains no eval( / new Function; if it does, instead self-host the @zakj/no-sleep ESM build per RESEARCH alternatives and note the swap in the SUMMARY. Create app/static/js/alpine-components/guided-brew-mode.js registering Alpine.data('guidedBrewMode', () => ({...})) inside an alpine:init listener. init() reads data-steps (JSON.parse, no eval), data-recipe-id, data-coffee-id from this.$root.dataset, then calls _loadCuePrefs() and _setupVisibilityReacquire(); destroy() stops the timer, releases the wake lock, removes the visibilitychange listener. State: steps, currentStepIndex, isRunning, isPaused, remainingSeconds, elapsedTotalSeconds, cuePrefs ({chime,vibrate}), wakeLockState ('held'|'fallback'|'none'). Timer: a setInterval that decrements the current step's remaining time and increments elapsedTotalSeconds; when a step's time offset elapses, play chime (if cuePrefs.chime) + vibrate (if cuePrefs.vibrate) + advance to the next step; "Next step" tap advances early with the same cues; pause()/resume() stop/restart the interval. start(): the user-gesture handler — call unlockAudio() AND requestWakeLock() here (both require a gesture on iOS), set isRunning. unlockAudio()/playChime() per RESEARCH Pattern 4 (AudioContext OscillatorNode 880Hz sine; resume() inside the gesture and also call resume() on each step advance per Pitfall 4 re-suspension). triggerVibration() per Pattern 5 (guarded by navigator.vibrate existence — silent skip on iOS, no error/UI). requestWakeLock()/releaseWakeLock()/_setupVisibilityReacquire() per Pattern 3 (try native navigator.wakeLock.request('screen'); on throw fall through to NoSleep.js if window.NoSleep; re-acquire on visibilitychange→visible while isRunning). _loadCuePrefs()/_saveCuePrefs() read/write localStorage 'snobbery:gbm:cues' (default {chime:true,vibrate:true}); the in-brew mute toggles flip cuePrefs and call _saveCuePrefs(). cancelWithoutLogging(): if (window.confirm('Cancel brew? This cannot be undone.')) window.location.assign('/brew'). finishBrewing(): on "Log this brew", navigate to /brew/new?gbm=1&recipe_id=<id>[&coffee_id=<id>]&brew_time=<elapsedTotalSeconds>; "Done without logging" navigates to /. No eval, no new Function anywhere.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery sh -c "test -f app/static/js/vendor/NoSleep.min.js && grep -q \"Alpine.data('guidedBrewMode'\" app/static/js/alpine-components/guided-brew-mode.js && grep -q 'snobbery:gbm:cues' app/static/js/alpine-components/guided-brew-mode.js && grep -q '/brew/new' app/static/js/alpine-components/guided-brew-mode.js && ! grep -nE 'eval\\(|new Function' app/static/js/alpine-components/guided-brew-mode.js && echo GBM_JS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/static/js/vendor/NoSleep.min.js exists (self-hosted) and contains no eval(/new Function (or the @zakj/no-sleep swap is documented in the SUMMARY)
    - guided-brew-mode.js registers Alpine.data('guidedBrewMode') with timer, start (gesture-unlocks audio + requests wake lock), auto-advance + manual next-step, pause/resume, chime/vibration gated by cuePrefs
    - Wake lock tries native then NoSleep.js fallback and re-acquires on visibilitychange; wakeLockState tracks held/fallback/none
    - localStorage key 'snobbery:gbm:cues' read/written; cancelWithoutLogging uses window.confirm; finishBrewing builds the /brew/new prefill URL with brew_time
    - No eval/new Function in guided-brew-mode.js
  </acceptance_criteria>
  <done>The guidedBrewMode CSP-safe component and self-hosted NoSleep.js exist with full timer/cue/wake-lock behavior.</done>
</task>

<task type="auto">
  <name>Task 2: GBM router + full-screen page + main.py registration</name>
  <read_first>
    - app/routers/home.py (require_user GET-page render analog — PATTERNS §brew_guided.py)
    - app/routers/brew.py (the existing /brew prefix routes; HTTPException 404 idiom; confirm /brew/guided does not collide)
    - app/templates/pages/home.html (extends base.html + page_title block)
    - app/templates/base.html (the search full-screen-sheet markup `fixed inset-0 z-50` to mirror for the GBM full-screen layout; the head_extra block from Plan 03)
    - .planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md (§4 Guided Brew Mode — 4a entry points, 4b start screen, 4c timer screen layout, 4d done flow, 4e wake-lock indicator, 4f cues; §Copywriting Contract CTAs/empty states)
    - .planning/phases/11-pwa-mobile-polish/11-PATTERNS.md (§brew_guided.py, §brew_guided.html)
  </read_first>
  <action>
    Create app/routers/brew_guided.py: APIRouter(prefix="/brew"), GET /guided gated by Depends(require_user) and Depends(get_session); read recipe_id (required int) + coffee_id (optional int) query params; load the recipe (404 if not found); optionally load the coffee for display; render pages/brew_guided.html with {recipe, coffee}. Register in app/main.py BEFORE brew_router: add `from app.routers import brew_guided as brew_guided_router` and `app.include_router(brew_guided_router.router)` placed immediately before the existing `app.include_router(brew_router.router)` line. Create app/templates/pages/brew_guided.html extending base.html with `{% block page_title %}Guided brew{% endblock %}` and a `{% block head_extra %}` that loads the nonce-tagged NoSleep.js (`<script defer src="/static/js/vendor/NoSleep.min.js" nonce="{{ csp_nonce(request) }}"></script>`). The content block is a full-screen `<div x-data="guidedBrewMode" data-recipe-id="{{ recipe.id }}" data-coffee-id="{{ coffee.id if coffee else '' }}" data-steps="{{ recipe.steps | tojson }}" class="fixed inset-0 z-50 flex flex-col bg-cream-50 dark:bg-espresso-950">` so it COVERS the bottom nav (D-20: GBM full-screen does not include/relies-not-on the nav; z-50 over the z-40 nav). Build three screens toggled by Alpine state per UI-SPEC §4b/4c/4d: (1) START screen — recipe name, coffee name (if set), recipe summary (total water/dose/yield/step count), the two cue toggles bound to cuePrefs (role="switch"), and the "Start guided brew" primary CTA (`bg-espresso-700 text-cream-50 min-h-[56px] w-full`) calling start(); if `recipe.steps` is empty render instead the "Recipe has no steps." message + link to the recipe builder (disabled launch). (2) TIMER screen — Cancel link top-left; wake-lock indicator top-right ("● Screen stays on" green when held / "● Screen kept on (fallback)" yellow when fallback / nothing when none) bound to wakeLockState; the countdown `text-7xl font-semibold tabular-nums`; "Step N of M"; current-step card `bg-espresso-700 text-cream-50` with step name + cumulative water ("40g / 250g total"); upcoming-step preview `text-sm`; a thin progress bar; Pause/Resume + Next step buttons (`min-h-[56px]`); the in-brew mute icon-buttons (each `min-h-[44px] min-w-[44px]` with an aria-label that toggles by state); a "× Cancel without logging" control calling cancelWithoutLogging(). (3) DONE screen — "Brew complete! Time: MM:SS" + "Log this brew" (calls finishBrewing → /brew/new prefill) + "Done without logging" (→ /). Use the locked CTA + empty-state copy from the Copywriting Contract. No `|safe`, no inline `hx-on:`; all interactivity is Alpine `@`/`x-` declarative bindings.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery sh -c "grep -q 'include_router(brew_guided' app/main.py && grep -q 'guidedBrewMode' app/templates/pages/brew_guided.html && grep -q 'data-steps' app/templates/pages/brew_guided.html && grep -q 'NoSleep.min.js' app/templates/pages/brew_guided.html && ! grep -n '|safe' app/templates/pages/brew_guided.html && echo GBM_PAGE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - GET /brew/guided?recipe_id=N returns 200 for an authenticated user (404 for a missing recipe; 401 anonymous)
    - brew_guided_router is registered BEFORE brew_router in main.py
    - brew_guided.html extends base.html, loads NoSleep.js via head_extra (nonce-tagged), and renders the full-screen guidedBrewMode div with data-steps JSON
    - The page COVERS the bottom nav via `fixed inset-0 z-50` (D-20)
    - A zero-steps recipe renders the "Recipe has no steps." state with a recipe-builder link
    - Start/timer/done screens present with the locked copy; no `|safe`/inline hx-on:
  </acceptance_criteria>
  <done>GBM route + full-screen page render with the three screens, NoSleep loaded only here, nav covered, registered correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Brew-form integration (button + brew_time field + sticky offset) + recipe-row entry + brew_time prefill</name>
  <read_first>
    - app/templates/pages/brew_form.html (the numeric field block, the coffee/recipe selects, the sticky-actions div line 287, the brewDraft/x-data wiring)
    - app/routers/brew.py (new_brew_form lines 630-678 — add ?brew_time= reading + seeding; _int_or_none helper; _stringify_prefill / _hydrate_form_context flow)
    - app/templates/fragments/recipe_row.html (the Edit links + desktop/card dual layout to add the "Start guided brew" button)
    - .planning/phases/11-pwa-mobile-polish/11-UI-SPEC.md (§4a entry points exact button styles; §5d sticky-nav offset bottom-16; §Color accent reserved list)
    - .planning/phases/11-pwa-mobile-polish/11-PATTERNS.md (§brew_form / §recipe_row guidance)
  </read_first>
  <action>
    In app/routers/brew.py new_brew_form, parse `brew_time = _int_or_none(qp.get("brew_time"))` and seed it into the form values so the brew_time_seconds field renders prefilled on the gbm completion path (add it into the `values` dict / context the same way coffee_id etc. flow; do not break the existing prefill ordering). In app/templates/pages/brew_form.html: (a) add an editable brew_time_seconds field near the other numeric inputs — `inputmode="numeric"`, `min="0"`, `max="86400"`, name "brew_time_seconds", value seeded from the form value (blank when absent) — labeled e.g. "Brew time (seconds)"; (b) add a "Brew with timer" secondary button (D-07a) shown when a coffee AND recipe are selected, styled `border border-espresso-700 text-espresso-700 bg-transparent`, linking to `/brew/guided?recipe_id=<selected recipe>&coffee_id=<selected coffee>` (use the existing selected values; if the selects are Alpine-bound, build the href via an Alpine `:href` binding to the selected ids, falling back to disabled when either is unset); (c) change the sticky-actions div (line 287) so on mobile it offsets ABOVE the bottom nav: `bottom-16` + `pb-[env(safe-area-inset-bottom)]` at <768px, `md:bottom-0` at >=768px (Pitfall 7 / D-20). In app/templates/fragments/recipe_row.html add, next to each Edit link (both desktop and card layouts), a "Start guided brew" primary button `bg-espresso-700 text-cream-50 min-h-[44px]` linking `/brew/guided?recipe_id={{ recipe.id }}`; if `recipe.steps` is empty/zero-length, render a disabled control with title/text indicating the recipe has no steps instead.
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery sh -c "grep -q 'brew_time' app/routers/brew.py && grep -q 'brew_time_seconds' app/templates/pages/brew_form.html && grep -q 'Brew with timer' app/templates/pages/brew_form.html && grep -q '/brew/guided' app/templates/fragments/recipe_row.html && grep -q 'bottom-16' app/templates/pages/brew_form.html && echo BREW_INTEGRATION_OK"</automated>
  </verify>
  <acceptance_criteria>
    - GET /brew/new?recipe_id=N&coffee_id=M&brew_time=T&gbm=1 renders the form with the brew_time_seconds field pre-seeded to T and coffee/recipe prefilled
    - brew_form.html has an editable brew_time_seconds numeric input (inputmode numeric, min 0, max 86400)
    - "Brew with timer" button appears when coffee+recipe are selected and links to /brew/guided with both ids
    - The sticky Save/Cancel actions offset to bottom-16 on mobile and md:bottom-0 on desktop (above the nav, Pitfall 7)
    - recipe_row.html has a "Start guided brew" button to /brew/guided?recipe_id={{recipe.id}} (disabled/no-steps state when the recipe has no steps)
  </acceptance_criteria>
  <done>Both GBM entry points wired; brew_time_seconds field editable + seeded from the gbm path; sticky actions clear the bottom nav.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: GBM smoke tests</name>
  <read_first>
    - tests/test_phase02_smoke.py (authenticated-client login flow to reuse)
    - tests/conftest.py (fixtures; how to seed a recipe with steps and one without)
    - app/routers/brew_guided.py (the route under test)
    - .planning/phases/11-pwa-mobile-polish/11-RESEARCH.md (Validation Architecture — GBM behaviors are mostly manual; the automatable parts are route auth/404, page render, brew_time round-trip)
  </read_first>
  <behavior>
    - GET /brew/guided?recipe_id=N (recipe with steps) → 200, body contains guidedBrewMode + data-steps
    - GET /brew/guided?recipe_id=<missing> → 404
    - GET /brew/guided anonymous → 401
    - GET /brew/guided?recipe_id=<stepless> → 200 with the "no steps" message
    - POST /brew with brew_time_seconds persists the value on the created session
  </behavior>
  <action>
    Create tests/test_gbm.py. Using the authenticated client + fixtures, seed a recipe WITH steps and one WITHOUT. Assert: GET /brew/guided?recipe_id=<with steps> → 200 and the HTML contains x-data="guidedBrewMode" and data-steps; GET /brew/guided?recipe_id=<nonexistent> → 404; GET /brew/guided?recipe_id=<with steps> WITHOUT a session cookie → 401; GET /brew/guided?recipe_id=<stepless> → 200 and contains the "Recipe has no steps." copy. Add a brew_time round-trip test: POST a valid brew create payload (mirroring the existing brew create tests) including brew_time_seconds=300 and assert the persisted session has brew_time_seconds == 300; and assert a payload with brew_time_seconds=86401 is rejected (422/validation error). Do NOT pytest.skip on missing seed data — create the fixtures (project memory: skip-as-green is a gap).
  </action>
  <verify>
    <automated>docker compose cp tests/ coffee-snobbery:/app/tests/ && docker compose exec coffee-snobbery python -m pytest tests/test_gbm.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_gbm.py asserts: /brew/guided 200 (with steps) + guidedBrewMode/data-steps in body; 404 (missing recipe); 401 (anonymous); 200 + "no steps" message (stepless recipe)
    - A brew_time_seconds=300 create round-trips to the persisted session; 86401 is rejected
    - No pytest.skip in the file; pytest tests/test_gbm.py exits 0
  </acceptance_criteria>
  <done>GBM route/auth/404/no-steps and brew_time persistence are covered by green, skip-free tests.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Guided Brew Mode wake lock + iOS fallback (BREW-13) — the carried multi-phase research flag. The code requests native wakeLock, re-acquires on visibilitychange, and falls back to self-hosted NoSleep.js (silent audio loop) on iOS, with audio chime (AudioContext) + vibration cues.</what-built>
  <how-to-verify>
    This step cannot be automated (Wake Lock / AudioContext / standalone-PWA behavior require a real device — RESEARCH Validation Architecture marks it manual-only). On a REAL iPhone:
    1. Note the iOS version (Settings → General → About). iOS 18.4+ has native PWA wake lock; 16.4–18.3 needs the NoSleep.js fallback.
    2. Install Snobbery to the Home Screen (use the iOS install banner instructions: Share → Add to Home Screen) and open it standalone.
    3. Start a guided brew (recipe with steps). Confirm: the screen does NOT dim/lock for the full brew duration; the "Screen stays on" / "(fallback)" indicator shows; the chime plays at a step transition (volume up; AudioContext unlocked by the Start tap); on 18.4+ the indicator is green (native), on older iOS it is yellow (fallback).
    4. Background the app (home button) for ~10s then reopen — confirm the wake lock re-acquires (screen stays on again).
    5. Vibration is expected to be SILENT on iOS (unsupported) — confirm there is no error and the brew continues normally.
    6. On Android Chrome (installed): confirm native wake lock (green indicator) + chime + vibration all work, and that cancel-without-logging confirmation appears (Pitfall 9 — if window.confirm is suppressed in standalone, note it for Phase 12).
    Record the device iOS/Android versions tested and which wake-lock path (native vs fallback) engaged in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" with the device versions tested + which wake-lock path engaged, or describe failures (screen dimmed, chime silent on Android, fallback not engaging) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → /brew/guided | GBM page must require authentication |
| client → /brew/new?brew_time= | User-supplied brew_time crosses into the form/persistence |
| GBM page → CSP | NoSleep.js + the component scripts must be nonce-tagged, eval-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-12 | Elevation of privilege | /brew/guided accessed unauthenticated | mitigate | `Depends(require_user)` on the GET /brew/guided handler returns 401 for anonymous (verified by tests/test_gbm.py). |
| T-11-13 | Tampering | brew_time injected via ?brew_time= query param | mitigate | Parsed with `_int_or_none` and persisted only through BrewSessionCreate, which enforces ge=0/le=86400 (Plan 02); an out-of-range value is rejected at validation (verified by the 86401 test). |
| T-11-14 | Tampering (XSS/CSP) | NoSleep.js + guidedBrewMode scripts | mitigate | NoSleep.js self-hosted under /static (satisfies `script-src 'self'` + connect-src 'self'; no CDN), loaded nonce-tagged via head_extra; component is Alpine CSP build (no eval, no new Function — grep-gated in Task 1); no inline hx-on:/`|safe` in the page (grep-gated in Task 2). |
| T-11-15 | Information disclosure | GBM cue prefs localStorage | accept | 'snobbery:gbm:cues' stores only {chime,vibrate} booleans — no user-identifying data. |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery python -m pytest tests/test_gbm.py -q -rs` → green, no skips.
- Manual iOS/Android wake-lock checkpoint approved (real-device, records versions + path engaged).
</verification>

<success_criteria>
- GBM launches full-screen from both entry points, runs the recipe-step timer with auto-advance + manual skip + cues + pause/resume + cancel, and completes back to a prefilled session form carrying brew_time.
- Wake lock holds on a real device (native on iOS 18.4+/Android, NoSleep.js fallback otherwise), re-acquires on visibilitychange, with a visible indicator.
- brew_time_seconds is editable on the form and round-trips through validation; entry points wired; tests green; CSP clean.
</success_criteria>

<output>
After completion, create `.planning/phases/11-pwa-mobile-polish/11-04-SUMMARY.md`
</output>
