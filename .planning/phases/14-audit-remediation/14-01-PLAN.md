---
phase: 14-audit-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routers/admin/users.py
  - tests/phase_09/test_admin_users.py
autonomous: true
requirements: [B1, B4]
must_haves:
  truths:
    - "Admin A demoting admin B with 2+ admins present succeeds (no 500) — D-01, D-02"
    - "Admin A deactivating admin B with 2+ admins present succeeds — D-02"
    - "Admin A deleting admin B (no brews) with 2+ admins present succeeds — D-02"
    - "Demote-to-last-admin and self-demote are still refused (existing guard tests pass) — D-01"
    - "_count_active_admins issues a subquery COUNT, never FOR UPDATE on an aggregate — D-01"
    - "The FOR UPDATE row lock is retained on the inner subquery (not dropped) — D-01"
    - "The unreachable duplicate self-demote guard at users.py:298-300 is deleted — D-09"
    - "B1 and B4 land together on a feature branch (not direct-to-main) — D-10"
  artifacts:
    - path: "app/routers/admin/users.py"
      provides: "Fixed _count_active_admins subquery + dead-guard removal"
      contains: "with_for_update"
    - path: "tests/phase_09/test_admin_users.py"
      provides: "Multi-admin regression tests for all 4 call sites"
      contains: "demote_other_admin"
  key_links:
    - from: "app/routers/admin/users.py::_count_active_admins"
      to: "users table (SELECT COUNT FROM locked subquery)"
      via: "select(func.count()).select_from(locked_subq)"
      pattern: "select\\(func\\.count\\(\\)\\)\\.select_from\\("
    - from: "tests/phase_09/test_admin_users.py"
      to: "POST /admin/users/{id} demote/deactivate/delete + toggle-admin"
      via: "two_admins fixture + client POST"
      pattern: "two_admins"
---

<objective>
Fix B1 (CRITICAL) and B4 (LOW), both in `app/routers/admin/users.py`. B1: `_count_active_admins` issues `SELECT COUNT(*) ... FOR UPDATE`, which PostgreSQL rejects (`ERROR: FOR UPDATE is not allowed with aggregate functions`), 500ing every last-admin guard evaluation against another admin. B4: an unreachable duplicate self-demote guard sits at lines 298-300. Both items edit the same file, so they ship in one plan to avoid a same-wave file conflict (per planning guidance + D-10 coupling note).

Purpose: Restore the last-admin guard to a working state (it currently crashes on the only path that matters — demoting/deactivating/deleting *another* admin) while keeping the `FOR UPDATE` row lock that serializes the demote-to-zero-admins race (D-01). Remove dead code (D-09).
Output: Corrected `_count_active_admins`, removed dead guard, and the first-ever regression tests for the multi-admin path covering all 4 call sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-remediation/14-CONTEXT.md
@.planning/phases/14-audit-remediation/14-RESEARCH.md
@.planning/phases/14-audit-remediation/14-PATTERNS.md
@.planning/phases/14-audit-remediation/14-VALIDATION.md

<interfaces>
<!-- Current buggy implementation — app/routers/admin/users.py:64-68 -->
<!-- def _count_active_admins(db: Session) -> int: -->
<!--     return db.execute(text("SELECT COUNT(*) FROM users WHERE is_admin = true AND is_active = true FOR UPDATE")).scalar_one() -->

<!-- In-file correct count analog — users.py:489 -->
<!-- db.execute(select(func.count()).select_from(BrewSession).where(...)).scalar_one() -->

<!-- Imports at users.py:39-40 (text is used ONLY at line 67 — verified by grep) -->
<!-- from sqlalchemy import delete as sql_delete -->
<!-- from sqlalchemy import func, select, text -->

<!-- two_admins fixture — tests/phase_09/conftest.py:171-185 -->
<!-- returns {admin1_id, admin1_cookie, admin2_id, admin2_cookie} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Wave 0 multi-admin regression tests (B1)</name>
  <files>tests/phase_09/test_admin_users.py</files>
  <read_first>
    - tests/phase_09/test_admin_users.py (existing guard test shapes, _prime_csrf helper, TestDeactivateUser/TestDeleteUser classes)
    - tests/phase_09/conftest.py:171-198 (two_admins + single_admin fixtures)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "tests/phase_09/test_admin_users.py — B1 multi-admin tests" (test shape + 4 call-site POST routes)
    - app/routers/admin/users.py (the 4 call sites and their POST routes/response shapes)
  </read_first>
  <action>
    Add a `TestMultiAdminOperations` class (or equivalent) to `tests/phase_09/test_admin_users.py` using the existing `two_admins` fixture + `client` fixture + `_prime_csrf`. Author exactly these tests, each operating on admin2 (NOT admin1, so the self path does not short-circuit) and asserting the operation SUCCEEDS (DB state changed), covering all 4 call sites:
    - `test_demote_other_admin_succeeds` — POST `/admin/users/{admin2_id}` updating `is_admin=false` (update_user, ~line 292); assert admin2.is_admin is False afterward.
    - `test_toggle_admin_other_admin_succeeds` — POST `/admin/users/{admin2_id}/toggle-admin` (toggle_admin, ~line 365); assert admin2.is_admin flipped.
    - `test_deactivate_other_admin_succeeds` — POST `/admin/users/{admin2_id}/deactivate` (deactivate_user, ~line 416); assert admin2.is_active is False.
    - `test_delete_other_admin_succeeds` — POST `/admin/users/{admin2_id}/delete` (delete_user, ~line 484); admin2 has no brew_sessions so D-15 allows it; assert admin2 row gone.
    Use the in-file DB-verification pattern (`SessionLocal()` + `select(User).where(...)`) shown in the existing guard tests. These tests MUST FAIL (500) against the current buggy `_count_active_admins` before Task 2 — confirm RED before fixing. Do NOT modify the existing `last_admin`/`single_admin`/self-path guard tests; they must keep passing after Task 2.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/phase_09/test_admin_users.py" -k "other_admin" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/phase_09/test_admin_users.py` contains `test_demote_other_admin_succeeds`, `test_toggle_admin_other_admin_succeeds`, `test_deactivate_other_admin_succeeds`, and `test_delete_other_admin_succeeds`.
    - Each new test uses the `two_admins` fixture and targets `admin2_id`.
    - Run BEFORE Task 2 the four tests FAIL with the FOR-UPDATE-on-aggregate 500 (RED confirmed); this confirmation is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Four multi-admin regression tests authored against the `two_admins` fixture, confirmed RED on the current code.</done>
</task>

<task type="auto">
  <name>Task 2: Fix _count_active_admins subquery COUNT (B1)</name>
  <files>app/routers/admin/users.py</files>
  <read_first>
    - app/routers/admin/users.py:39-40 (sqlalchemy imports — confirm `text` is used ONLY at line 67 before removing it)
    - app/routers/admin/users.py:64-68 (buggy _count_active_admins)
    - app/routers/admin/users.py:489 (in-file `select(func.count()).select_from(...)` analog)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "B1 — Fix _count_active_admins" (verbatim replacement)
    - .planning/phases/14-audit-remediation/14-RESEARCH.md § "B1: SQLAlchemy 2.0 Subquery COUNT with FOR UPDATE"
  </read_first>
  <action>
    Replace the body of `_count_active_admins` (users.py:64-68) so it: builds a locked subquery `select(User.id).where(User.is_admin.is_(True), User.is_active.is_(True)).with_for_update().subquery()`, then returns `db.execute(select(func.count()).select_from(locked_subq)).scalar_one()`. Keep the docstring's intent (FOR UPDATE row lock retained — D-01, NOT decorative). Do NOT use `text(...)` and do NOT drop the lock. After the change, `text` is no longer referenced anywhere in `users.py` (grep-confirmed: only use was line 67) — remove `text` from the import at line 40, changing `from sqlalchemy import func, select, text` to `from sqlalchemy import func, select`. Run `ruff check app/routers/admin/users.py` to confirm no unused-import warning remains.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/phase_09/test_admin_users.py" -k "other_admin or last_admin or single_admin or admin_count or active_admins" -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/admin/users.py` contains `with_for_update()` and `select(func.count()).select_from(` inside `_count_active_admins`.
    - `app/routers/admin/users.py` contains NO `FOR UPDATE` inside any `text(` call: `grep -c "FOR UPDATE" app/routers/admin/users.py` returns 0.
    - `from sqlalchemy import func, select` present at line ~40; `text` removed from that import.
    - `pytest tests/phase_09/test_admin_users.py -k "other_admin" -x` exits 0 (the four Task 1 tests now PASS).
    - `pytest tests/phase_09/test_admin_users.py -k "last_admin or single_admin" -x` exits 0 (existing guard tests still pass).
    - `ruff check app/routers/admin/users.py` exits 0.
  </acceptance_criteria>
  <done>`_count_active_admins` uses a subquery COUNT with the FOR UPDATE lock retained; multi-admin tests green, existing guard tests green, no FOR UPDATE inside a text() call, `text` import removed.</done>
</task>

<task type="auto">
  <name>Task 3: Delete dead duplicate self-demote guard (B4)</name>
  <files>app/routers/admin/users.py</files>
  <read_first>
    - app/routers/admin/users.py:289-302 (the D-16 guard block at 290-296 and the unreachable duplicate at 298-300)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "B4 — Delete dead code (lines 298-300)"
  </read_first>
  <action>
    Delete the unreachable block at lines 298-300 (the `# Self-lockout guard on is_admin demotion` comment plus the `if target.is_admin and not new_is_admin_raw and target_id == admin_user.id:` statement and its `return _render_error_fragment(request, "Cannot demote yourself.", 409)`). Its condition is already fully covered at lines 290-296 (the `admin_count <= 1` check and the `target_id == admin_user.id` check both run first). Pure deletion, no behavioral change. NOTE: this deletion shifts all downstream line numbers up by 3 — apply AFTER Task 2 to keep Task 2's line references valid, and re-locate by content (not line number) at edit time. Exactly one "Cannot demote yourself." string must remain in the file (the live guard at line ~296).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/phase_09/test_admin_users.py" -k "self_demote or demote or other_admin or last_admin or single_admin" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "demote yourself" app/routers/admin/users.py` returns 1.
    - The remaining "Cannot demote yourself." guard is the one inside the `admin_count > 1` branch (lines 290-296 region).
    - All self-demote and multi-admin tests still pass: `pytest tests/phase_09/test_admin_users.py -k "self_demote or other_admin" -x` exits 0.
  </acceptance_criteria>
  <done>The duplicate guard is removed; exactly one self-demote guard remains; all admin-user tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin user → admin user-management endpoints | Authenticated admin issues state-changing POSTs that mutate other users' privilege/active state |
| concurrent admin requests → users table | Two admin mutations in separate threadpool transactions can interleave the read-modify-write on admin count |

## STRIDE Threat Register (ASVS V4 Access Control)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V4-01 | Denial of Service | `_count_active_admins` (`users.py:64`) | mitigate | Subquery COUNT replaces invalid `FOR UPDATE`-on-aggregate so the last-admin guard is evaluable instead of 500ing every demote/deactivate/delete of another admin. Verified by `test_*_other_admin_succeeds` (maps to VALIDATION rows B1 demote/deactivate/delete-other). |
| T-V4-02 | Elevation of Privilege / Lockout | last-admin guard race across the 4 call sites | mitigate | `with_for_update()` lock retained on the inner subquery serializes two concurrent admin mutations at the DB level (sync handlers run in a threadpool → real, if rare, race) so the system can never be demoted to zero active admins. Existing `last_admin`/`single_admin` guard tests prove demote-to-zero stays refused. |
| T-V4-03 | (none — dead code) | duplicate guard `users.py:298-300` | accept→remove | Unreachable duplicate guard removed (D-09); no behavioral change, eliminates maintenance-confusion risk. `grep -c "demote yourself"` → 1. |
</threat_model>

<verification>
- Quick run (the 4 phase-touched test files) green after the plan:
  `docker compose exec coffee-snobbery python -m pytest tests/test_scheduler.py tests/phase_09/test_admin_users.py tests/services/test_ai_service.py tests/test_search.py -q`
- `ruff check app/ tests/` exits 0 AND `ruff format --check app/ tests/` exits 0 (CI gates BOTH).
- `grep -c "FOR UPDATE" app/routers/admin/users.py` returns 0; `grep -c "demote yourself" app/routers/admin/users.py` returns 1.
</verification>

<success_criteria>
- ROADMAP Phase 14 success criterion #1 (last-admin guard no longer crashes; all 4 call sites work; multi-admin path tested; existing guard tests pass) is satisfied.
- ROADMAP Phase 14 success criterion #5 (dead duplicate self-demote guard removed) is satisfied.
- `ruff check` and `ruff format --check` both clean on `app/` and `tests/`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-remediation/14-01-SUMMARY.md`. Record the RED→GREEN confirmation for the multi-admin tests and the `text`-import removal.
</output>
