---
phase: 14-audit-remediation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/rate_limit.py
  - app/routers/search.py
  - tests/test_search.py
autonomous: true
requirements: [S4]
must_haves:
  truths:
    - "/search caps q at 100 chars BEFORE strip; over-long input short-circuits to an empty 200 — D-07"
    - "The 100-char cap operates on raw q (so a 101-char all-spaces string is also rejected) — D-07"
    - "A SEARCH_LIMIT = '60/minute' constant exists in app/rate_limit.py (mirrors LOGIN_LIMIT pattern) — D-08"
    - "search_results is decorated with @limiter.limit(SEARCH_LIMIT), keyed by get_remote_address — D-08"
    - "The existing <2-char empty-200 guard and normal search behavior are unchanged — D-07"
  artifacts:
    - path: "app/rate_limit.py"
      provides: "SEARCH_LIMIT rate-limit constant"
      contains: "SEARCH_LIMIT"
    - path: "app/routers/search.py"
      provides: "100-char cap + @limiter.limit decoration on search_results"
      contains: "limiter.limit(SEARCH_LIMIT)"
    - path: "tests/test_search.py"
      provides: "over-long query empty-200 test (+ optional rate-limit test or manual-verify note)"
      contains: "long_query"
  key_links:
    - from: "app/routers/search.py::search_results"
      to: "SEARCH_LIMIT in app/rate_limit.py"
      via: "from app.rate_limit import SEARCH_LIMIT, limiter + @limiter.limit(SEARCH_LIMIT)"
      pattern: "limiter\\.limit\\(SEARCH_LIMIT\\)"
    - from: "app/routers/search.py::search_results"
      to: "empty-200 short-circuit"
      via: "if len(q) > 100: return HTMLResponse('', status_code=200)"
      pattern: "len\\(q\\) > 100"
---

<objective>
Fix S4 (LOW) — `/search` has no length cap and no rate limit. Per D-07, cap raw `q` at 100 chars (before `.strip()`) so over-long input short-circuits to an empty `200` (same shape as the existing `<2`-char guard). Per D-08, add a `SEARCH_LIMIT = "60/minute"` constant to `app/rate_limit.py` and decorate `search_results` with `@limiter.limit(SEARCH_LIMIT)` (mirror the `LOGIN_LIMIT` pattern; `get_remote_address` keying; the handler already takes `request: Request`).

Purpose: Cap abuse/scraping of the search endpoint without affecting legitimate debounced live-search (250ms debounce + `hx-sync` cancels in-flight; 60/min sits comfortably above real bursts). No legitimate search is longer than 100 chars.
Output: The new constant, the cap + decorator, and the over-long-query test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-audit-remediation/14-CONTEXT.md
@.planning/phases/14-audit-remediation/14-RESEARCH.md
@.planning/phases/14-audit-remediation/14-PATTERNS.md
@.planning/phases/14-audit-remediation/14-VALIDATION.md

<interfaces>
<!-- app/rate_limit.py:40-47 — existing constants + module-level limiter -->
<!-- LOGIN_LIMIT = "5/15minutes"; SETUP_LIMIT = "5/15minutes"; CSP_REPORT_LIMIT = "30/minute" -->
<!-- limiter = Limiter(key_func=get_remote_address, default_limits=[]) -->

<!-- Decorator-order analog — app/routers/auth.py:229-231 (route decorator OUTERMOST, limiter NEXT line) -->
<!-- @router.post("/login", response_class=HTMLResponse) -->
<!-- @limiter.limit(LOGIN_LIMIT) -->

<!-- Current handler — app/routers/search.py:30-49 (sync def, already takes request: Request first) -->
<!-- @router.get("", response_class=HTMLResponse) -->
<!-- def search_results(request: Request, q: str = "", user=Depends(require_user), db=Depends(get_session)) -> Response -->
<!--     if len(q.strip()) < 2: return HTMLResponse("", status_code=200) -->

<!-- Empty-200 analog test — tests/test_search.py::test_short_query_empty (lines ~437-453) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEARCH_LIMIT constant + cap + decorator on /search (S4)</name>
  <files>app/rate_limit.py, app/routers/search.py</files>
  <read_first>
    - app/rate_limit.py:38-47 (constant block + module-level limiter)
    - app/routers/search.py:1-49 (imports + the full search_results handler)
    - app/routers/auth.py:229-231 (decorator-order analog: @router first, @limiter.limit next)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "S4 — app/rate_limit.py" and § "S4 — app/routers/search.py" (verbatim constant + modified handler)
    - .planning/phases/14-audit-remediation/14-RESEARCH.md § "S4: Search hardening" + Pitfall 4 (slowapi decorator order)
  </read_first>
  <action>
    In `app/rate_limit.py`, add the constant `SEARCH_LIMIT: str = "60/minute"` immediately after `CSP_REPORT_LIMIT` (same typed-string style; do not change `limiter` or the existing constants).
    In `app/routers/search.py`: add `from app.rate_limit import SEARCH_LIMIT, limiter` to the imports; decorate `search_results` with `@limiter.limit(SEARCH_LIMIT)` placed on the line immediately BELOW `@router.get("", response_class=HTMLResponse)` (route decorator outermost, limiter next — Pitfall 4; slowapi requires the existing `request: Request` first param, which is already present). Add the length cap as the FIRST statement in the body, BEFORE the existing `if len(q.strip()) < 2:` check: `if len(q) > 100: return HTMLResponse("", status_code=200)` (operate on RAW `q`, not `q.strip()`, so a 101-char all-spaces string is also short-circuited — D-07). Do not change the existing `<2` guard, the `run_search` call, or the template response.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/test_search.py" -k "short_query or long_query" -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/rate_limit.py` contains `SEARCH_LIMIT: str = "60/minute"`.
    - `app/routers/search.py` contains `from app.rate_limit import SEARCH_LIMIT, limiter`, `@limiter.limit(SEARCH_LIMIT)`, and `if len(q) > 100:`.
    - The `@limiter.limit(SEARCH_LIMIT)` line sits directly below `@router.get(...)` (route decorator outermost).
    - The cap operates on raw `q` (the `len(q) > 100` check appears before `len(q.strip()) < 2`).
    - Existing search behavior preserved: `pytest tests/test_search.py -k "short_query" -x` exits 0.
    - `ruff check app/rate_limit.py app/routers/search.py` exits 0.
  </acceptance_criteria>
  <done>SEARCH_LIMIT constant added; search_results carries the cap and the rate-limit decorator in the correct order; existing search tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author over-long-query test (+ rate-limit test or manual-verify note) (S4)</name>
  <files>tests/test_search.py</files>
  <read_first>
    - tests/test_search.py:437-453 (test_short_query_empty — the empty-200 shape to clone, incl. cookie/seed helper)
    - .planning/phases/14-audit-remediation/14-PATTERNS.md § "tests/test_search.py — S4 length cap test" (101-char shape)
    - .planning/phases/14-audit-remediation/14-VALIDATION.md § "Per-Task Verification Map" (S4 rows) + § "Manual-Only Verifications" (the rate-limit flakiness escape hatch)
  </read_first>
  <action>
    Add `test_long_query_returns_empty` to `tests/test_search.py` mirroring `test_short_query_empty`: build a 101-char `q` (e.g. `"a" * 101`), GET `/search?q=<that>` with an authenticated cookie, assert `status_code == 200` and `resp.text.strip() == ""`.
    For the rate-limit assertion: ATTEMPT an automated `test_search_rate_limit` (fire `>60` GETs to `/search?q=ab` from one client and assert a `429` appears). slowapi uses in-memory buckets that can reset between TestClient requests — if the test proves flaky/non-deterministic in-process, do NOT ship a flaky test: instead remove it and record the manual-verify instruction from 14-VALIDATION.md § Manual-Only ("hit `/search?q=ab` >60×/min from one IP and confirm a 429") in the SUMMARY and as a brief comment in the test module. Document in the SUMMARY which path was taken (automated vs manual-verify).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest "tests/test_search.py" -k "long_query" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_search.py` contains `test_long_query_returns_empty`; `pytest tests/test_search.py -k "long_query" -x` exits 0.
    - EITHER an automated `test_search_rate_limit` passes deterministically, OR it is omitted and the manual-verify instruction is recorded (per 14-VALIDATION.md Manual-Only) — no flaky test is committed.
    - `ruff check tests/test_search.py` exits 0 AND `ruff format --check tests/test_search.py` exits 0.
  </acceptance_criteria>
  <done>Over-long-query test green; rate-limit coverage is either a deterministic automated test or a documented manual-verify per VALIDATION; no flaky test shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → GET /search | Logged-in user supplies an arbitrary `q` string at arbitrary frequency |

## STRIDE Threat Register (ASVS V5 Input Validation, V10 Web Services)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-V5-01 | Denial of Service | `search_results` `q` length | mitigate | 100-char cap on raw `q` short-circuits over-long input to an empty 200 before any DB query (no legitimate search exceeds 100 chars). Tested by `test_long_query_returns_empty` (VALIDATION S4 q_length). |
| T-V10-rl | Denial of Service / scraping | `search_results` request rate | mitigate | `@limiter.limit("60/minute")` per IP (get_remote_address keying) caps a scripted abuse loop while staying above debounced live-search bursts. Verified by automated `test_search_rate_limit` if deterministic, else manual-verify per VALIDATION Manual-Only (slowapi in-memory buckets are flaky under TestClient). |
</threat_model>

<verification>
- Quick run (the 4 phase-touched test files) green after the plan:
  `docker compose exec coffee-snobbery python -m pytest tests/test_scheduler.py tests/phase_09/test_admin_users.py tests/services/test_ai_service.py tests/test_search.py -q`
- `ruff check app/ tests/` exits 0 AND `ruff format --check app/ tests/` exits 0 (CI gates BOTH).
- `register_rate_limiter(app)` already wires `app.state.limiter` in `app/main.py`; no main.py change needed (the decorator resolves `limiter` at import time and 429s route through the existing structured handler).
</verification>

<success_criteria>
- ROADMAP Phase 14 success criterion #4 (`/search` caps over-long `q` to an empty 200 and carries a slowapi rate limit via a new constant matching the existing per-route pattern) is satisfied.
- `ruff check` and `ruff format --check` both clean on `app/` and `tests/`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-remediation/14-04-SUMMARY.md`. Record whether the rate-limit check shipped as an automated test or a manual-verify (and why).
</output>
