---
phase: 05-brew-sessions
plan: 05
type: execute
wave: 4
depends_on: [4]
files_modified:
  - app/static/js/alpine-components/rating-stars.js
  - app/static/js/alpine-components/flavor-tag-input.js
  - app/static/js/alpine-components/brew-ratio.js
  - app/static/js/alpine-components/brew-draft.js
  - app/templates/base.html
  - app/templates/pages/brew_form.html
autonomous: false
requirements: [BREW-02, BREW-03, BREW-04, BREW-05, BREW-06, BREW-07, BREW-08, MOB-05, MOB-06]
user_setup: []

must_haves:
  truths:
    - "The add/edit form renders all fields in the UI-SPEC order with prefill pills, sticky save bar, and the Advanced disclosure"
    - "Tap-on-stars sets 0.5-step values into a hidden rating input; the live 1:N.NN ratio updates as dose/water change"
    - "The observed-flavor-note tag input commits chips, auto-creates on no-match (D-09 'new' badge), and shows advertised quick-add chips (D-11) bound to flavor_note_ids_observed (never advertised)"
    - "Drafts persist to localStorage namespaced by user_id, autosave to /brew/draft on blur, and restore localStorage-primary / server-fallback"
    - "Every brew input is >=16px font-size; correct inputmode/type per MOB-05; no |safe, no hx-on:, no x-model"
  artifacts:
    - path: "app/static/js/alpine-components/rating-stars.js"
      provides: "Alpine.data('ratingStars') half-step tap zones + hidden input"
      contains: "ratingStars"
    - path: "app/static/js/alpine-components/flavor-tag-input.js"
      provides: "Alpine.data('observedFlavorNotes') bound to flavor_note_ids_observed + D-09 + D-11"
      contains: "observedFlavorNotes"
    - path: "app/static/js/alpine-components/brew-ratio.js"
      provides: "Alpine.data('brewRatio') live 1:N.NN"
      contains: "brewRatio"
    - path: "app/static/js/alpine-components/brew-draft.js"
      provides: "Alpine.data('brewDraft') localStorage + autosave + reconciliation + touched-state"
      contains: "snobbery:draft:brew:"
    - path: "app/templates/pages/brew_form.html"
      provides: "the dedicated add/edit page mounting all four scopes"
      contains: "X-CSRF-Token"
  key_links:
    - from: "app/templates/base.html"
      to: "the four new Alpine component scripts"
      via: "<script defer nonce> BEFORE the Alpine core"
      pattern: "rating-stars.js"
    - from: "app/templates/pages/brew_form.html"
      to: "flavor_note_ids_observed"
      via: "parallel x-for hidden inputs (observed, NOT advertised)"
      pattern: "flavor_note_ids_observed"
---

<objective>
Build the brew form UI: the four CSP-build Alpine components (rating stars, observed-flavor tag input, live ratio, draft persistence), wire them into `base.html`, and render the dedicated `pages/brew_form.html` add/edit page per the locked UI-SPEC (prefill pills, sticky save bar, Advanced disclosure, the 16px no-zoom inputs). Verify the <30s logging flow with a human checkpoint.

Purpose: This is the daily-use surface — the form a returning user logs N+1 in under 30 seconds. It renders against the Plan 04 route context.
Output: four Alpine components, `base.html` wiring, `pages/brew_form.html`, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-brew-sessions/05-CONTEXT.md
@.planning/phases/05-brew-sessions/05-RESEARCH.md
@.planning/phases/05-brew-sessions/05-PATTERNS.md
@.planning/phases/05-brew-sessions/05-UI-SPEC.md
@.planning/phases/05-brew-sessions/05-04-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 04 route context + the analog components/templates. Use directly. -->

Route context keys from Plan 04 (see 05-04-SUMMARY.md for the exact shape):
  mode ("create"|"edit"), values (echo), errors (by-field), prefill (dict), touched (per-field map),
  advertised_chips (list of {id,name} for the selected coffee), selected_flavor_notes (seed chips),
  server_draft (create mode only), water_type seed list, selectable coffees/bags/recipes/equipment.

CSP-build Alpine registration (app/static/js/alpine-components/recipe-step-builder.js):
  document.addEventListener('alpine:init', () => Alpine.data('name', () => ({...})))
  config via data-* read in init() from this.$root.dataset (lines 24-44); CSP rules lines 11-22:
  NO x-model (use :value + x-on:input/x-on:click/x-on:keydown), no inline expressions, only method
  calls + simple member access; precompute Math in JS (barStyle line 164).
flavorNoteChips factory to CLONE+RENAME (app/static/js/alpine-components/autocomplete.js lines 167-318):
  selectedChips[], init() reads data-initial-chips JSON, commitItem($el) reads data-item-id/name,
  removeChip, onInput/onKeydown (Up/Down/Enter/Esc + Backspace-removes-last), parallel x-for hidden inputs.
base.html script block (app/templates/base.html lines 13-25): add four <script defer nonce> tags
  BEFORE the @alpinejs/csp core script (line 25). csp_nonce(request) in templates; csrf-token meta line 10.
coffee_form.html analogs: CSRF hidden field lines 60-61; label/input/error pattern lines 63-73;
  native <select> lines 137-149; chip-builder seed markup lines 206-282; autocomplete input wiring
  lines 261-278 (hx-get on focus, delay:350ms[length>=2], hx-sync="this:replace").
autocomplete_list.html (REUSE unchanged): <ul role="listbox">, min-h-[44px] rows, server <strong>
  match highlight, "+ Create new" only when not exact_match (D-10 encoded already).

MOB-06: 16px input rule ALREADY ships in tailwind.src.css @layer base. Do NOT create custom.css.
  Obligation: no brew input may set font-size below 16px (no text-xs on inputs; text-base default).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four Alpine components + base.html wiring</name>
  <files>app/static/js/alpine-components/rating-stars.js, app/static/js/alpine-components/flavor-tag-input.js, app/static/js/alpine-components/brew-ratio.js, app/static/js/alpine-components/brew-draft.js, app/templates/base.html</files>
  <read_first>
    - app/static/js/alpine-components/recipe-step-builder.js (init/data-* seed lines 24-44; CSP rules lines 11-22; computed getters lines 118-139; precompute Math line 164)
    - app/static/js/alpine-components/autocomplete.js (flavorNoteChips factory lines 167-318 — clone+rename)
    - app/static/js/alpine-components/__init.js (Alpine.data registration convention lines 1-63)
    - app/static/js/htmx-listeners.js (global X-CSRF-Token injection lines 34-39)
    - app/templates/base.html (component script block lines 13-25; csrf-token meta line 10)
    - .planning/phases/05-brew-sessions/05-UI-SPEC.md (Tap-on-Stars; Flavor-Note Tag Input incl. D-09 "new" badge + D-11 quick-add; Live Brew-Ratio; Draft Persistence UX)
    - .planning/phases/05-brew-sessions/05-PATTERNS.md (sections: rating-stars.js, flavor-tag-input.js, brew-ratio.js, brew-draft.js, base.html)
  </read_first>
  <action>
    Create four CSP-build Alpine components (each its own file; each `document.addEventListener('alpine:init', () => Alpine.data(...))`; config via `data-*` read in `init()`; `:value`+`x-on:*` only, NO `x-model`, no inline expressions — subject to the existing CSP grep tests):

    `rating-stars.js` -> `Alpine.data('ratingStars', ...)`: seed `value` from `data-initial-rating` in `init()`; mirror a hidden `<input type="hidden" name="rating" :value="value">`; left half of star i = (i+0.5), right half = (i+1.0) via `x-on:click` on per-half `<button type="button">` tap-zones; idempotent re-tap; a "Clear" affordance resets to blank; arrow-key parity (`x-on:keydown` Left/Right ±0.5); group `role="group" aria-label="Rating, 0 to 5 stars"`, each zone `aria-label="Set rating to {value}"`; numeric echo "{value} / 5". Half-fill rendered via SVG clipPath/two paths (filled = fill-espresso-700; empty = text-espresso-300).

    `flavor-tag-input.js` -> `Alpine.data('observedFlavorNotes', ...)`: CLONE the flavorNoteChips factory and bind the hidden inputs to `name="flavor_note_ids_observed"` (per-session observed — NEVER `advertised_flavor_note_ids`; ANTI-PATTERN: do not conflate). Add D-09: when commit (comma/Enter) matches nothing, POST to the existing flavor-note create endpoint (category="other") and push the returned chip with an `isNew=true` flag that renders the "new" badge (text-sm, neutral — text-xs PROHIBITED). Add D-11: render `advertised_chips` (from context) as one-tap quick-add chips above the input; tapping adds to observed and dims the suggestion. Reuse the autocomplete wiring (hx-get on focus, delay:350ms[length>=2], hx-sync="this:replace") targeting autocomplete_list.html. Keep Up/Down/Enter/Esc/Backspace-removes-last.

    `brew-ratio.js` -> `Alpine.data('brewRatio', ...)`: dose + water inputs report in via `x-on:input`; `get ratio()` returns `water/dose` to 2 decimals; dose 0/empty -> "—" (never NaN/Infinity). Display `1:` + `x-text="ratio"` with tabular-nums.

    `brew-draft.js` -> `Alpine.data('brewDraft', ...)`: localStorage key `snobbery:draft:brew:<user_id>` (user_id from `data-user-id`); write on every input change (BREW-06); per-field touched-state map driving the prefill pills; on field blur issue an HTMX/`fetch` POST to `/brew/draft` with the serialized payload (incl. disclosure-open flag, D-02) — if `fetch`, read `X-CSRF-Token` from the csrf-token meta and set the header (HTMX requests get it from htmx-listeners.js automatically; prefer HTMX). On `init()` reconcile (BREW-07): restore from localStorage if present; else fall back to `server_draft` from context. /brew/new ONLY. Clear the namespaced localStorage key on successful submit. Restore notice + Discard handled in the template (Task 2) reading this component's state.

    Wire base.html: add four `<script defer src="/static/js/alpine-components/{rating-stars,flavor-tag-input,brew-ratio,brew-draft}.js" nonce="{{ csp_nonce(request) }}"></script>` tags BEFORE the @alpinejs/csp core script (matching the existing three component tags).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest -q tests/ci -k "csp or safe or hx_on or x_model or alpine" 2>&1 | tail -20; docker compose exec coffee-snobbery sh -c "grep -rln 'x-model\|hx-on:' app/static/js/alpine-components/rating-stars.js app/static/js/alpine-components/flavor-tag-input.js app/static/js/alpine-components/brew-ratio.js app/static/js/alpine-components/brew-draft.js || echo CSP_CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - Four files exist, each registering its named factory via `Alpine.data` on `alpine:init`.
    - flavor-tag-input.js binds hidden inputs to `name="flavor_note_ids_observed"` (grep confirms; NOT advertised_flavor_note_ids).
    - brew-draft.js uses the literal key prefix `snobbery:draft:brew:` and reconciles localStorage-primary / server-fallback.
    - No `x-model`, no inline `hx-on:` in any of the four files (grep prints CSP_CLEAN).
    - base.html loads all four scripts with a nonce, before the Alpine core.
    - The existing CSP/grep CI tests still pass.
  </acceptance_criteria>
  <done>Four CSP-compliant components exist and are loaded; observed notes are bound correctly; drafts namespaced + reconciled.</done>
</task>

<task type="auto">
  <name>Task 2: brew_form.html dedicated add/edit page</name>
  <files>app/templates/pages/brew_form.html</files>
  <read_first>
    - app/templates/fragments/coffee_form.html (CSRF hidden field 60-61; label/input/error 63-73; native select 137-149; chip-builder seed 206-282; autocomplete wiring 261-278)
    - app/templates/base.html (the block this page extends; modal-mount, csrf meta)
    - app/templates/pages/coffees.html (page-shell that extends base.html)
    - .planning/phases/05-brew-sessions/05-UI-SPEC.md (Brew Session Form layout diagram + mechanics; Prefill Indicators; Advanced disclosure; Sticky bar; Draft restore notice; Copywriting Contract; Tap-Target table)
    - .planning/phases/05-brew-sessions/05-PATTERNS.md (section: brew_form.html)
    - .planning/phases/05-brew-sessions/05-04-SUMMARY.md (exact context keys)
  </read_first>
  <action>
    Create app/templates/pages/brew_form.html extending base.html (a real page, not a fragment — D-01). Render the UI-SPEC field order: Coffee, Bag (+ "open new bag" link when none, D-06), Recipe, Brewer, Grinder, Kettle, Water type (native select + Other free-text, D-07), Dose/Water (+ live ratio readout via brew-ratio scope), Temp/Grind, Rating (ratingStars scope, hidden `name="rating"`), Advertised quick-add chips (D-11) + Flavor notes tag input (observedFlavorNotes scope, hidden `name="flavor_note_ids_observed"`), Notes textarea, the closed-by-default "Refractometer / advanced" `<details>`/Alpine disclosure (Yield, TDS, read-only Extraction yield rendered from the GENERATED value — never an input, never submitted), Brewed at (`type="datetime-local"`, default now), and the sticky Save/Discard bar (BREW-08). Mount all four Alpine scopes via string `x-data` refs + `data-*` config (initial-rating, initial-chips, user-id, server-draft, advertised chips). Mandatory CSRF hidden field (coffee_form.html lines 60-61). Field/label/error pattern verbatim from coffee_form.html (red-700 on error, autoescaped). MOB-05: `inputmode="decimal"` on dose/water/temp/yield/tds, `type="datetime-local"` on brewed_at, `inputmode="numeric"` where integer-only. MOB-06: every input `text-base` (>=16px) — NO text-xs/font-size override on any input. Prefill pills (UI-SPEC neutral pill, captions "from last brew"/"from recipe"/"from this brew") rendered from the `touched` map; pills disappear on edit. Edit mode shows actual stored values, no pills, disclosure auto-open when yield/tds non-null. Draft-restore inline notice + "Discard" ghost (create mode, reading brew-draft state). CSP locks: NO `|safe`, NO inline `hx-on:`, NO `hx-vals='js:'`, NO `x-model`; all dynamic strings autoescaped. Use only the locked spacing tokens (the 12px gap-3 inherited exception is allowed; no text-xs; 4-size type scale; 2 weights).
  </action>
  <verify>
    <automated>docker compose build coffee-snobbery && docker compose up -d coffee-snobbery && docker compose exec coffee-snobbery python -m pytest -q tests/ci -k "safe or csp" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `app/templates/pages/brew_form.html` extends base.html and contains the CSRF hidden field.
    - The observed tag input's hidden inputs use `name="flavor_note_ids_observed"`; the advertised quick-add chips READ advertised_chips but never write advertised_flavor_note_ids.
    - The Advanced disclosure renders Extraction yield read-only (no input element, not in the submitted form data).
    - No `|safe`, no `hx-on:`, no `x-model` anywhere in the file (existing CI grep tests pass).
    - Numeric inputs carry inputmode/type per MOB-05; no input sets font-size below 16px (no text-xs on inputs).
    - The page builds and the app serves GET /brew/new (authed) returning 200 with the form.
  </acceptance_criteria>
  <done>The dedicated add/edit form renders per the UI-SPEC with prefill pills, disclosure, sticky bar, and all four Alpine scopes; CSP-clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the <30s logging flow on a phone-sized viewport</name>
  <what-built>The dedicated brew add/edit form at /brew/new and /brew/{id}/edit: aggressive prefill with neutral pills, tap-on-stars rating (56px stars, 0.5 steps), observed-flavor tag input with D-09 auto-create + D-11 advertised quick-add, live 1:N.NN ratio, the closed-by-default Refractometer/advanced disclosure, the sticky Save/Discard bar, and localStorage + server draft persistence. Rebuilt into the container.</what-built>
  <how-to-verify>
    Prereq: at least one prior brew session exists for the test user (log one first if needed). Open the app at a 375px-wide viewport (browser devtools responsive mode, iPhone SE).
    1. Visit /brew/new. Confirm coffee, recipe, brewer, grinder, kettle, water type, dose, water, temp, grind are PREFILLED with neutral "from last brew" pills; rating, flavor notes, and notes are blank.
    2. Tap a star's left then right half — confirm the value sets to N.5 then N.0 and the numeric echo updates; tap "Clear" to blank it.
    3. Change Dose or Water — confirm the 1:N.NN ratio updates live; set dose to 0 and confirm it shows "1:—" (no NaN/Infinity).
    4. In Flavor notes, type a known note and commit with Enter (links it); type a brand-new word and commit (auto-creates with a "new" badge). Tap an "Advertised:" quick-add chip and confirm it adds to the observed list.
    5. Open "Refractometer / advanced", enter Yield + TDS, confirm Extraction yield shows a computed read-only value (and "—" when one is blank).
    6. Confirm NO input zooms the viewport on focus at 375px (16px rule). Confirm the Save/Discard bar stays stuck to the bottom and clears the home indicator.
    7. Select a recipe and confirm dose/water/temp/grind switch to the recipe's targets with "from recipe" pills (D-05 recipe-wins).
    8. Reload the page mid-edit — confirm the draft restores with a "Restored your unsaved draft." notice; submit a valid session and confirm it saves and redirects to the list, and the draft is gone.
    9. Open the session in /brew/{id}/edit — confirm it shows the actual stored values with NO prefill pills.
  </how-to-verify>
  <resume-signal>Type "approved" if all nine checks pass, or describe what's off (which step, what you saw vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template-rendered strings -> DOM | Prefill values, chip names, refused reasons must autoescape (no |safe) — stored-XSS defense |
| Alpine component config -> behavior | Config via data-* only (CSP build); no inline JS expressions |
| draft autosave (client) -> /brew/draft | Must carry CSRF (HTMX header or meta-read fetch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Tampering (stored XSS) | brew_form.html rendering notes/chip/prefill/error strings | mitigate | Jinja autoescape ON; no `|safe` (existing SEC-05 grep test covers the new template) (Task 2) |
| T-05-20 | Code injection (CSP bypass) | the four Alpine components + template | mitigate | CSP-build only: no x-model, no inline hx-on:, no hx-vals='js:'; data-* config; nonce'd scripts (Tasks 1,2) |
| T-05-21 | Spoofing (CSRF) | brew-draft.js autosave POST | mitigate | X-CSRF-Token via global htmx-listeners.js (HTMX) or meta-read header (fetch) (Task 1) |
| T-05-22 | Info disclosure | shared-device draft leak | mitigate | localStorage key namespaced `snobbery:draft:brew:<user_id>`; cleared on submit (Task 1, MX-5) |
| T-05-23 | Data integrity | EY editable from the form | mitigate | Extraction yield rendered read-only, no input element, never submitted (Task 2) |
</threat_model>

<verification>
- `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery` healthy; GET /brew/new returns 200 with the form for an authed user.
- Existing CSP/`|safe` grep CI tests pass against the new template + components.
- The human-verify checkpoint (Task 3) confirms the <30s prefill flow, rating, ratio, tag input, disclosure, no-zoom, sticky bar, draft restore, and edit-mode-no-pills.
</verification>

<success_criteria>
- The dedicated add/edit form renders per the locked UI-SPEC; CSP-clean; 16px no-zoom; MOB-05 inputmodes.
- Tap-stars (0.5 steps), live ratio, observed tag input (D-09/D-11), Advanced disclosure (read-only EY), sticky bar, and draft persistence all function.
- Human verification of the <30s flow passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-brew-sessions/05-05-SUMMARY.md`.
</output>
