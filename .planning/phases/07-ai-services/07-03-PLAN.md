---
phase: 07-ai-services
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/services/ai_service.py
  - tests/services/test_ai_service.py
autonomous: true
requirements: [AI-01, AI-03, AI-06, AI-07, AI-10, AI-12, AI-13, AI-16, HOME-06]
must_haves:
  truths:
    - "regenerate(user_id, generated_by, db=, force=) returns a status string and is the SCHED-02 entry point Phase 8 will call"
    - "The coffee-rec flow runs the three-tier web-search fallback (primary → broadened → characteristics-only) and the response records which tier produced it"
    - "Anthropic is tried first; a non-retryable or persistent-529 error falls back to OpenAI; no provider enabled returns 'not_configured'"
    - "Recipe suggestion picks from existing recipes by historical avg rating (never invents); alt-brewer callout fires only at >=0.5 rating delta"
    - "Sweet-spots prose is generated in the same regen transaction and stored as its own sweet_spots row, regenerated/expired together with the coffee row"
    - "An unchanged signature skips regeneration unless force=True; in-memory + advisory lock prevent concurrent runs (returns 'locked')"
  artifacts:
    - path: "app/services/ai_service.py"
      provides: "regenerate(), coffee-rec 3-tier flow, sweet-spots prose, recipe-suggestion SQL, alt-brewer SQL, anthropic+openai callers, get_latest_recommendation, is_stale"
      contains: "async def regenerate"
    - path: "tests/services/test_ai_service.py"
      provides: "Tests for tier fallback, provider fallback, sig-skip, locked, not_configured, recipe/alt-brewer SQL"
      contains: "def test_three_tier_fallback"
  key_links:
    - from: "app/services/ai_service.py::regenerate"
      to: "analytics.compute_input_signature"
      via: "signature compare for skip decision"
      pattern: "compute_input_signature"
    - from: "app/services/ai_service.py"
      to: "credentials.get_provider_credential"
      via: "anthropic-then-openai provider lookup"
      pattern: "get_provider_credential"
---

<objective>
Implement the cached nightly bundle (D-06): the `regenerate()` entry point plus the coffee-rec
composite flow (three-tier web search, Anthropic→OpenAI provider fallback, citation projection,
Pydantic validation), the recipe-suggestion and alt-brewer SQL sub-flows (no LLM), and the
sweet-spots prose flow (HOME-06) — all written in one regeneration transaction and persisted as
two `ai_recommendations` rows (`coffee` + `sweet_spots`) that regenerate/expire together. Includes
signature-skip, the in-memory + advisory lock, and the `not_configured` / `try_again` status paths.

Purpose: This is Snobbery's differentiator and the SCHED-02 contract Phase 8 calls directly. The
`regenerate()` signature is frozen here: `regenerate(user_id, generated_by, *, db, force=False)`.

Output: the flow logic appended to `app/services/ai_service.py` (built on 07-01's helpers) + tests.

DISCRETION DECISIONS RESOLVED (noted per CONTEXT):
- sweet_spots row shape: SEPARATE row `recommendation_type="sweet_spots"` (cleaner telemetry), written
  in the same transaction as the `coffee` row so they stay regenerated/expired together (AI-10).
- url verify timing: NOT done inside regenerate(); the coffee row is written with `url_verified=None`
  and verification runs as a background task triggered by the router/poll (07-05, AI-05). This keeps
  regenerate() free of the post-write async fetch and matches the "render then verify via poll" UX.
- recommendation_type set: {coffee, equipment, paste_rank, sweet_spots}.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-services/07-RESEARCH.md
@.planning/phases/07-ai-services/07-PATTERNS.md
@.planning/phases/07-ai-services/07-CONTEXT.md
@.planning/phases/07-ai-services/07-01-SUMMARY.md

<interfaces>
<!-- From 07-01 (already in app/services/ai_service.py): -->
_project_tool_use_input(content, tool_name) -> dict           # keeps named tool_use only
_verify_buy_url(url, roaster_name, coffee_name) -> bool       # async; SSRF-hardened (used by 07-05, not here)
_advisory_key(user_id, rec_type) -> int ;  _try_advisory_lock(db, user_id, rec_type) -> bool
_get_lock(user_id, rec_type) -> asyncio.Lock ; _evict_stale_throttle(*, now, window_secs)
_build_anthropic_client(cred) / _build_openai_client(cred)    # max_retries=1
_is_anthropic_fallback_error(exc) -> bool                     # non-retryable + 529/overloaded
_write_recommendation_row(db, *, user_id, rec_type, input_signature, response_json, cred,
                          tool_version, usage, web_search_count, duration_ms, generated_by,
                          url_verified=None, error_status=None) -> AIRecommendation
ai_schemas: CoffeeRecSchema (incl. search_tier, recipe_suggestion, alt_brewer), SweetSpotsProseSchema,
            RecipeSuggestionSchema, AltBrewerSchema

<!-- Reusable services (verified on disk): -->
analytics.compute_input_signature(db, user_id) -> str (SHA256 hex; _EMPTY_SIGNATURE for zero rated)
analytics.get_cold_start_counts(db, user_id) -> {sessions, distinct_notes, gate_open, sessions_needed, notes_needed}
analytics.get_sweet_spots(db, user_id) -> list[Row(origin, process, brewer_name, recipe_name, avg_rating, session_count)]
analytics.get_preference_profile(db, user_id) -> dict (origin/process/roaster/roast_level)
credentials.get_provider_credential(db, "anthropic"|"openai") -> ProviderCredential | None
settings.get_int("ai_primary_max_searches")=5, get_int("ai_broadened_max_searches")=3,
settings.get_str("ai_tool_version_anthropic")="web_search_20250305", get_str("ai_tool_version_openai"),
settings.get_str("recommendation_region")="US", get_str("ai_provider_default")="anthropic"

<!-- Models for SQL sub-flows (NOTE: recipes + equipment are SHARED catalog — NO user_id column;
     scope by joining brew_sessions which IS user-scoped): -->
Recipe: id, name, dose_grams, water_grams, water_temp_c, grind_setting, steps, archived
BrewSession: id, user_id, coffee_id, recipe_id, brewer_id, rating (Numeric, nullable), brewed_at
Coffee: id, name, origin, process, roast_level, roaster_id
Equipment: id, type ("brewer"|"grinder"|...), brand, model
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recipe-suggestion + alt-brewer SQL sub-flows (no LLM)</name>
  <files>app/services/ai_service.py, tests/services/test_ai_service.py</files>
  <read_first>
    - app/services/ai_service.py (07-01 helpers)
    - app/services/analytics.py (lines 47-265 — select() join conventions over BrewSession/Coffee/Recipe/Equipment; func.avg/count + HAVING patterns)
    - app/models/brew_session.py, app/models/recipe.py, app/models/equipment.py, app/models/coffee.py (column sets)
    - .planning/phases/07-ai-services/07-RESEARCH.md (AI-06 line 65; AI-07 line 66; phase_requirements table)
  </read_first>
  <behavior>
    - suggest_recipe returns the user's existing recipe with the highest avg rating for sessions matching the bean style (origin+process+roast_level); never fabricates a recipe.
    - suggest_recipe returns a no-match result (no_match=True, recipe_id=None) when no rated session for that style used a recipe.
    - alt_brewer_callout returns an AltBrewerSchema only when a different brewer shows >=0.5 higher avg rating than the current/best brewer for the bean style; returns None below the threshold.
  </behavior>
  <action>
    Add two pure-SQL helpers to `app/services/ai_service.py` (no LLM call):
    - `suggest_recipe(db, *, user_id, origin, process, roast_level) -> RecipeSuggestionSchema` (AI-06): join
      BrewSession→Coffee (match origin/process/roast_level, case-insensitive) and BrewSession→Recipe, filter
      user_id and rating not null and recipe_id not null, GROUP BY recipe, ORDER BY avg(rating) desc, take top 1.
      If a row exists, return RecipeSuggestionSchema(recipe_id, recipe_name, summary="...", no_match=False); else
      RecipeSuggestionSchema(recipe_id=None, recipe_name=None, summary="No matching recipe yet — try the recipe builder.", no_match=True).
      Picks from existing recipes only — NEVER invents (D-06/AI-06).
    - `alt_brewer_callout(db, *, user_id, origin, process, roast_level, exclude_brewer_id=None) -> AltBrewerSchema | None` (AI-07):
      join BrewSession→Coffee→Equipment(brewer), filter matching style + user_id + rating not null, GROUP BY brewer,
      compute avg(rating) per brewer; compare the best alternate brewer's avg to the baseline; return an AltBrewerSchema
      with rating_delta only when delta >= 0.5; else None. Use the >=0.5 threshold exactly (AI-07).
    Match analytics.py join + aggregation idioms (func.avg, func.count, having). These produce data the coffee-rec
    flow attaches to CoffeeRecSchema.recipe_suggestion / .alt_brewer.
    Add tests: test_suggest_recipe_picks_highest_rated, test_suggest_recipe_no_match,
    test_alt_brewer_fires_at_half_point_delta, test_alt_brewer_below_threshold_none. Seed brew sessions inline.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_ai_service.py -x -q -k "suggest_recipe or alt_brewer"</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/ai_service.py` defines `suggest_recipe` and `alt_brewer_callout`.
    - `suggest_recipe` only ever returns recipe_ids that exist in the `recipes` table (never a synthesized id) — test asserts the returned recipe_id is one of the seeded ids or None.
    - `test_alt_brewer_fires_at_half_point_delta` passes (delta exactly 0.5 → callout returned).
    - `test_alt_brewer_below_threshold_none` passes (delta 0.4 → None).
    - Named pytest selection exits 0.
  </acceptance_criteria>
  <done>Recipe suggestion (existing-recipes-only) and alt-brewer (>=0.5 delta) SQL sub-flows implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coffee-rec composite flow — 3-tier search + provider fallback + sweet-spots prose</name>
  <files>app/services/ai_service.py, tests/services/test_ai_service.py</files>
  <read_first>
    - app/services/ai_service.py (07-01 helpers + Task 1 sub-flows)
    - app/services/ai_schemas.py (CoffeeRecSchema, SweetSpotsProseSchema)
    - .planning/phases/07-ai-services/07-RESEARCH.md (Pattern 1 projector lines 232-277; Pattern 2 tool def lines 279-311; Pattern 3 OpenAI fallback lines 313-348; Pattern 4 anthropic errors lines 351-380; three-tier AI-03 lines 118-119 of CONTEXT; token accounting lines 305-311 & 345-350)
    - .planning/phases/07-ai-services/07-PATTERNS.md (anthropic tool def lines 155-173; pydantic-after-projection lines 175-184; OpenAI fallback lines 207-228; telemetry write lines 254-276; structlog emit lines 278-294)
    - .planning/phases/07-ai-services/07-CONTEXT.md (D-03/D-04 voice; D-06 composite; locked provider/citation/openai decisions)
  </read_first>
  <behavior>
    - The flow tries tier 1 (primary: origin+process+roast_level, max_uses from ai_primary_max_searches); on empty/invalid result advances to tier 2 (broadened, max_uses from ai_broadened_max_searches) then tier 3 (characteristics-only, no web_search, no URL); the returned CoffeeRecSchema.search_tier records which tier produced it.
    - Anthropic is the primary provider; a non-retryable / persistent-529 anthropic error routes to the OpenAI caller (prompt-based JSON, NOT json_schema, web_search_preview).
    - A ValidationError after projection at all tiers yields a "try_again" outcome (no garbled JSON surfaced).
    - Sweet-spots prose is produced from the user's get_sweet_spots data via a small LLM call returning SweetSpotsProseSchema.
    - The system prompt encodes the D-03 voice (confident expert, lightly wry) and D-04 tight prose (1-2 sentences).
  </behavior>
  <action>
    Add the LLM callers and the composite assembler to `app/services/ai_service.py`:
    - `_anthropic_coffee_call(client, *, model, tool_version, max_uses, region, prompt) -> tuple[dict, usage, web_search_count]`:
      build tools per 07-PATTERNS lines 155-173 — the web_search server tool (type=tool_version, name="web_search",
      max_uses, user_location US) PLUS a client tool name="structure_output" with input_schema=CoffeeRecSchema.model_json_schema();
      call client.messages.create; project via _project_tool_use_input(resp.content, "structure_output"); return raw dict + usage + web_search_count.
    - `_openai_coffee_call(client, *, model, prompt) -> tuple[dict, usage, web_search_count]` per Pattern 3: responses.create
      with tools=[{"type":"web_search_preview"}] and a prompt that instructs JSON matching the schema; extract output_text;
      json.loads. Do NOT use text.format.json_schema (silently disables web_search — Pitfall 2).
    - `_generate_coffee_rec(db, *, user_id, generated_by) -> tuple[str, AIRecommendation | None]` — the three-tier driver:
      build the primary prompt from the user's preference profile + sweet spots (grounding); pick provider
      (anthropic via get_provider_credential; if None, openai; if both None return "not_configured"); run tier 1, on
      failure broaden to tier 2, then tier 3 (characteristics-only, no web_search/no buy_url). Catch anthropic errors with
      _is_anthropic_fallback_error → switch to OpenAI caller. Validate each tier output with CoffeeRecSchema.model_validate;
      on ValidationError advance tier; if all tiers fail validation, write a row with error_status="pydantic_error" and
      return "try_again". Set CoffeeRecSchema.search_tier to the producing tier (AI-03). Attach suggest_recipe(...) and
      alt_brewer_callout(...) results to the validated schema before persisting. Write the row via _write_recommendation_row
      (rec_type="coffee", url_verified=None — verification deferred to 07-05). Emit AI_TIER_FALLBACK on each broaden and
      AI_FALLBACK_TRIGGERED on provider switch.
    - `_generate_sweet_spots_prose(db, *, user_id, generated_by, cred, signature) -> AIRecommendation | None` (HOME-06, AI-10):
      read analytics.get_sweet_spots; if empty, skip (return None); else one small LLM call (no web_search) returning
      SweetSpotsProseSchema; write a SEPARATE row rec_type="sweet_spots" via _write_recommendation_row in the same regen flow.
    Define a module-level SYSTEM_PROMPT_VOICE string encoding D-03/D-04 (confident expert, lightly wry, 1-2 sentences;
    NOT full-snob persona). All prose stays plain text (rendered escaped + <br> later — never |safe).
    Add tests with MagicMock SDK clients (no live network): test_three_tier_fallback (tier1 empty → tier2 → tier3, assert
    search_tier), test_provider_fallback_anthropic_to_openai (anthropic raises AuthenticationError → openai caller invoked),
    test_pydantic_validation_error_try_again (all tiers return invalid dict → "try_again"), test_sweet_spots_prose_skipped_when_empty.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_ai_service.py -x -q -k "three_tier or provider_fallback or validation_error_try_again or sweet_spots_prose"</automated>
  </verify>
  <acceptance_criteria>
    - `_generate_coffee_rec` advances primary→broadened→characteristics-only and sets `search_tier` accordingly (AI-03); test_three_tier_fallback asserts the final search_tier value.
    - `test_provider_fallback_anthropic_to_openai` passes: a non-retryable anthropic error invokes `_openai_coffee_call`.
    - The OpenAI caller does NOT pass `text.format.json_schema` (grep `json_schema` absent from `_openai_coffee_call`); uses `tools=[{"type": "web_search_preview"}]` (Pitfall 2).
    - `_generate_sweet_spots_prose` writes a row with `recommendation_type="sweet_spots"` distinct from the coffee row (AI-10).
    - All prose generation uses the D-03/D-04 SYSTEM_PROMPT_VOICE constant; no `|safe` introduced anywhere.
    - Named pytest selection exits 0.
  </acceptance_criteria>
  <done>Coffee-rec composite flow with 3-tier search, provider fallback (prompt-JSON OpenAI), projection+validation→try_again, and sweet-spots prose all implemented and tested with mocked SDKs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: regenerate() entry point + signature skip + locking + read helpers</name>
  <files>app/services/ai_service.py, tests/services/test_ai_service.py</files>
  <read_first>
    - app/services/ai_service.py (Tasks 1-2 + 07-01 helpers)
    - .planning/phases/07-ai-services/07-RESEARCH.md (Pattern 7 advisory + in-memory lock lines 451-489; Pattern 9 regenerate signature lines 510-523; Pitfall 5 sync-DB-in-async lines 614-617; Open Question 2 advisory+async lines 803-806)
    - .planning/phases/07-ai-services/07-PATTERNS.md (regenerate signature lines 124-134; SQLAlchemy select lines 864-877)
    - app/services/analytics.py (compute_input_signature lines 370-419; get_cold_start_counts lines 323-362)
  </read_first>
  <behavior>
    - regenerate with an unchanged signature and force=False returns "skipped" (emits AI_REGEN_SKIPPED) without an LLM call.
    - regenerate with force=True regenerates even when the signature is unchanged (manual refresh path).
    - regenerate returns "not_configured" when no provider credential is enabled.
    - A second concurrent regenerate for the same (user_id,"coffee") cannot run while the first holds the lock — the second returns "locked".
    - regenerate returns "locked" when the advisory lock cannot be acquired.
    - get_latest_recommendation returns the newest row for (user_id, rec_type) scoped to the user.
    - is_stale(db, user_id) is True when the current signature differs from the stored coffee-row signature (AI-15).
  </behavior>
  <action>
    Implement the public surface on `app/services/ai_service.py`:
    - `async def regenerate(user_id: int, generated_by: str, *, db: Session, force: bool = False) -> str` per Pattern 9.
      Returns one of "generated"|"skipped"|"locked"|"try_again"|"not_configured"|"error". Sequence:
      (1) gate check via get_cold_start_counts — if not gate_open return "skipped" (no AI for cold-start, AI-11).
      (2) acquire the in-memory `_get_lock(user_id, "coffee")` non-blockingly: if `lock.locked()` return "locked";
          else `async with lock:` for the body. (3) Inside, do SYNC DB work bracketing the awaited LLM call to avoid
          blocking the event loop (Pitfall 5 / Open Question 2): acquire advisory lock with _try_advisory_lock; if False
          return "locked". (4) compute current signature; read stored signature from the latest coffee row; if equal and
          not force → emit AI_REGEN_SKIPPED, return "skipped". (5) call _generate_coffee_rec; if "try_again"/"not_configured"
          return that. (6) call _generate_sweet_spots_prose so coffee + sweet_spots are written together (AI-10).
          (7) emit AI_GENERATION_SUCCESS; return "generated". Wrap unexpected exceptions → emit AI_GENERATION_ERROR,
          write a row with error_status, return "error". CRITICAL: do not hold a sync Session open across `await llm_call()`
          on the event loop — structure DB reads/writes as discrete sync bracketing calls around the awaited LLM call
          (document the chosen approach in the docstring; run_in_executor or pre-read/post-write per Open Question 2).
    - `get_latest_recommendation(db, *, user_id, rec_type) -> AIRecommendation | None` — select latest by generated_at desc, user-scoped, error_status is null.
    - `is_stale(db, *, user_id) -> bool` (AI-15) — compare compute_input_signature to the latest coffee row's input_signature.
    - `in_flight(user_id, rec_type="coffee") -> bool` — `_get_lock(...).locked()` so the router/poll can detect an active run.
    Add tests (mock _generate_coffee_rec / _generate_sweet_spots_prose so no SDK is needed): test_sig_skip (unchanged sig →
    "skipped"), test_force_regenerates (force=True ignores sig), test_not_configured (no cred → "not_configured"),
    test_advisory_lock_concurrent (advisory returns False → "locked"), test_cold_start_skips (gate closed → "skipped"),
    test_is_stale_true_when_sig_changed.
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/services/test_ai_service.py -x -q -k "sig_skip or force_regenerates or not_configured or advisory_lock_concurrent or cold_start_skips or is_stale"</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/ai_service.py` defines `async def regenerate(user_id, generated_by, *, db, force=False)` exactly matching the SCHED-02 contract (Phase 8 calls `regenerate(uid, "scheduler", db=db)`).
    - `test_sig_skip` passes (unchanged signature, force=False → "skipped", no generation called).
    - `test_advisory_lock_concurrent` passes (advisory lock unavailable → "locked").
    - `test_not_configured` passes (no provider → "not_configured").
    - `is_stale` and `get_latest_recommendation` are user-scoped (queries filter `user_id ==`).
    - regenerate does not hold a sync Session open across the awaited LLM call (docstring states the bracketing approach; reviewed in code).
    - Named pytest selection exits 0; full file `python -m pytest tests/services/test_ai_service.py -q` exits 0.
  </acceptance_criteria>
  <done>regenerate() entry point complete: cold-start gate, in-memory + advisory locks, signature-skip/force, not_configured/try_again/locked statuses, plus get_latest_recommendation/is_stale/in_flight read helpers — all tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM + web-search output → structured parse | Untrusted tool output crosses into Pydantic validation |
| AI provider credentials → SDK | Decrypted key consumed only at client construction |
| concurrent callers (manual + future scheduler) → regenerate | Race on the same user's coffee bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering / Elevation (prompt injection) | `_generate_coffee_rec` projection + validation | mitigate | Keep-only-tool_use projection + CoffeeRecSchema(extra="forbid") validation; ValidationError at all tiers → "try_again" (no garbled JSON). Test: test_pydantic_validation_error_try_again. |
| T-07-03 | Information Disclosure (secret leakage) | provider callers + structlog emit | mitigate | cred.key only at `_build_*_client`; AI_GENERATION_* logs carry provider/model/tier/tokens only — never key or raw response embedding the key (07-01 helper enforces). |
| T-07-07 | Denial of Service / cost abuse (concurrent regen) | `regenerate` locks | mitigate | in-memory `_get_lock` + Postgres `pg_try_advisory_xact_lock`; second concurrent run returns "locked" (AI-13). Test: test_advisory_lock_concurrent. |
| T-07-08 | Denial of Service / cost (needless regen) | signature skip | mitigate | unchanged signature + force=False → "skipped" (COST-4 signature = user's own rated sessions). Test: test_sig_skip. |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery python -m pytest tests/services/test_ai_service.py -q` green (all 07-01 + 07-03 tests).
- `ruff check app/services/ai_service.py` passes; type hints on every new signature.
- grep confirms `_openai_coffee_call` contains `web_search_preview` and NOT `json_schema`.
- grep confirms no `|safe` and no `cred.key` outside `_build_*_client`.
</verification>

<success_criteria>
- regenerate() is the frozen SCHED-02 entry point; coffee + sweet_spots written together; 3-tier search + provider fallback work; signature skip + locking + not_configured/try_again statuses all covered by tests with mocked SDKs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-services/07-03-SUMMARY.md`
</output>
