---
phase: 07-ai-services
plan: 07
type: execute
wave: 6
depends_on: ["07-05", "07-06"]
files_modified:
  - app/routers/ai.py
  - app/templates/pages/paste_rank.html
  - app/templates/pages/wishlist.html
  - app/templates/pages/home.html
  - app/templates/fragments/ai/paste_rank_results.html
  - app/templates/fragments/home/equipment_rec.html
  - tests/routers/test_ai_router.py
autonomous: false
requirements: [AI-08, AI-09, HOME-06]
must_haves:
  truths:
    - "GET /ai/paste-rank renders the dedicated 'Rank these for me' page with a paste box accepting text or URLs (D-07/D-08)"
    - "Submitting paste-rank returns the top-3 ranked coffees, each with one-sentence reasoning (AI-09)"
    - "GET /ai/wishlist renders the minimal wishlist view (list / mark-purchased / remove), user-scoped (D-09)"
    - "The home page links to the paste-rank page and the wishlist; the equipment-rec 'analyze my setup' button renders its result on click (D-05)"
  artifacts:
    - path: "app/templates/pages/paste_rank.html"
      provides: "Dedicated paste-and-rank page (paste box + results)"
      contains: "/ai/paste-rank"
    - path: "app/templates/pages/wishlist.html"
      provides: "Minimal wishlist view"
      contains: "/ai/wishlist"
    - path: "app/routers/ai.py"
      provides: "GET /ai/paste-rank, GET /ai/wishlist page handlers"
      contains: "GET"
  key_links:
    - from: "app/templates/pages/home.html"
      to: "/ai/paste-rank and /ai/wishlist"
      via: "links from the home page to the dedicated pages"
      pattern: "/ai/paste-rank"
    - from: "app/templates/pages/home.html"
      to: "/ai/equipment"
      via: "the on-demand 'analyze my setup' equipment button"
      pattern: "/ai/equipment"
---

<objective>
Ship the two dedicated pages (paste-and-rank, wishlist) and the home-page entry points that complete
the AI surface: the "Rank these for me" page (D-07/D-08), the minimal wishlist view (D-09), the
on-demand equipment-rec "analyze my setup" button + result fragment (D-05), and links from the home
page to the paste-rank and wishlist pages. Add the GET page handlers to the AI router.

Purpose: Without these, the paste-rank flow, wishlist loop, and equipment rec built in 07-04/07-05
have no UI entry point. There is no persistent nav yet (Phase 11 owns that), so the home page is the
hub: it links to /ai/paste-rank and /ai/wishlist and hosts the equipment button.

Output: paste_rank.html + wishlist.html pages, the paste-rank results fragment, the equipment-rec result
fragment, the GET handlers in ai.py, home.html links + equipment button, tests, and a human-verify checkpoint.

This plan has a human-verify checkpoint, so `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-services/07-PATTERNS.md
@.planning/phases/07-ai-services/07-CONTEXT.md
@.planning/phases/07-ai-services/07-05-SUMMARY.md
@.planning/phases/07-ai-services/07-06-SUMMARY.md

<interfaces>
<!-- ai_service / wishlist (07-04/07-02): -->
async rank_pasted_coffees(user_id, generated_by, *, db, raw_input) -> tuple[str, PasteRankSchema|None]  # PasteRankSchema.ranked[<=3], summary_prose
async generate_equipment_rec(user_id, generated_by, *, db) -> tuple[str, AIRecommendation|None]
list_wishlist(db, *, by_user_id) -> list[WishlistEntry]   # WishlistEntry: coffee_name, roaster_name, source_url, purchased_at, added_at

<!-- POST routes already exist (07-05): /ai/paste-rank, /ai/equipment, /ai/wishlist/add, /ai/wishlist/{id}/purchase, /ai/wishlist/{id}/remove -->

<!-- Conventions: -->
Pages extend base.html: {% block page_title %} + {% block content %}. base.html has NO persistent nav yet (Phase 11).
templates from app.templates_setup; require_user; user.id only; get_session (SYNC).
CSRF: forms carry <input type="hidden" name="X-CSRF-Token" value="{{ request.cookies.get('csrftoken','') }}">.
HX-Request dual render: htmx submit → return the results fragment; non-HTMX → full page (brew.py pattern lines 484-516).
No |safe on AI prose / reasoning. Mobile-first 375px (max-w container + px-6 like home.html).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET page handlers + paste-rank page/results fragment + equipment result fragment</name>
  <files>app/routers/ai.py, app/templates/pages/paste_rank.html, app/templates/fragments/ai/paste_rank_results.html, app/templates/fragments/home/equipment_rec.html, tests/routers/test_ai_router.py</files>
  <read_first>
    - app/routers/ai.py (POST routes from 07-05; add GET handlers)
    - app/templates/pages/home.html (extends base.html; container/layout classes; CSP-clean form pattern)
    - app/routers/brew.py (lines 484-516 HX-Request dual-render; form parsing)
    - .planning/phases/07-ai-services/07-PATTERNS.md (paste_rank page lines 669-694; HX dual render lines 401-415)
    - .planning/phases/07-ai-services/07-CONTEXT.md (D-07 dedicated page top-3 one-sentence; D-08 text+URL one box; D-05 equipment on-demand)
  </read_first>
  <behavior>
    - GET /ai/paste-rank renders pages/paste_rank.html with an empty paste box (no results).
    - POST /ai/paste-rank (already exists) returns the paste_rank_results fragment on HX-Request, full page otherwise.
    - The equipment "analyze my setup" POST returns the equipment_rec result fragment.
    - GET handlers are gated by require_user.
  </behavior>
  <action>
    Extend `app/routers/ai.py`:
    - Add `GET /ai/paste-rank` -> render `pages/paste_rank.html` (empty form). (POST /ai/paste-rank from 07-05 now returns
      `fragments/ai/paste_rank_results.html` on HX-Request, full `pages/paste_rank.html` with results otherwise.)
    - Ensure the POST /ai/equipment handler (07-05) renders `fragments/home/equipment_rec.html` with the result.
    Create templates:
    - `app/templates/pages/paste_rank.html` (extends base.html, page_title "Rank these coffees"): a single textarea
      `name="input_text"` accepting pasted text OR URLs (D-08) with helper copy explaining both are accepted; a hidden
      X-CSRF-Token field; submit posts to /ai/paste-rank (hx-post, hx-target the results region). A `{% if results %}`
      block including the results fragment for the non-HTMX full-page render. Mobile-first container (max-w + px-6).
    - `app/templates/fragments/ai/paste_rank_results.html`: render results.ranked (top 3) — each item rank, name, reasoning
      (escaped, NEVER |safe), plus results.summary_prose. Handle the try_again/not_configured message states.
    - `app/templates/fragments/home/equipment_rec.html`: render the EquipmentRecSchema result — summary_prose, and
      weakest_link/recommendation when present, or the "no changes recommended" copy (D-05). Escaped text, no |safe.
    Add tests to `tests/routers/test_ai_router.py`: test_paste_rank_page_renders (GET 200, contains the textarea),
    test_paste_rank_submit_returns_results (POST with mocked rank_pasted_coffees → results fragment with 3 items),
    test_equipment_button_returns_fragment (POST with mocked generate_equipment_rec → equipment_rec fragment).
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py -x -q -k "paste_rank_page or paste_rank_submit or equipment_button"</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/ai.py` defines `GET /ai/paste-rank` gated by require_user.
    - `pages/paste_rank.html` contains a `name="input_text"` textarea and posts to `/ai/paste-rank` with a hidden X-CSRF-Token.
    - `fragments/ai/paste_rank_results.html` renders up to 3 ranked items with reasoning (AI-09), no |safe.
    - `fragments/home/equipment_rec.html` renders the equipment result incl. the "no changes recommended" path (D-05/AI-08).
    - `test_paste_rank_submit_returns_results` passes (3-item results fragment).
    - Named pytest selection exits 0.
  </acceptance_criteria>
  <done>Paste-rank page + results fragment, equipment result fragment, and GET handlers shipped and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wishlist page + GET handler + home-page links + equipment button</name>
  <files>app/routers/ai.py, app/templates/pages/wishlist.html, app/templates/pages/home.html, tests/routers/test_ai_router.py, tests/routers/test_home.py</files>
  <read_first>
    - app/routers/ai.py (Task 1 GET handlers)
    - app/services/wishlist.py (list_wishlist)
    - app/templates/pages/home.html (where to add the links + equipment button — within the gate-open content block)
    - .planning/phases/07-ai-services/07-PATTERNS.md (wishlist page lines 698-723; wishlist POST routes lines 456-470)
    - .planning/phases/07-ai-services/07-CONTEXT.md (D-09 minimal view; D-05 equipment button on home)
  </read_first>
  <behavior>
    - GET /ai/wishlist lists the calling user's entries (newest first), each with mark-purchased and remove actions.
    - Purchased entries are visually distinguished from open ones.
    - The home page links to /ai/paste-rank and /ai/wishlist and shows an "analyze my setup" equipment button (hx-post /ai/equipment).
  </behavior>
  <action>
    Extend `app/routers/ai.py`: add `GET /ai/wishlist` -> load list_wishlist(db, by_user_id=user.id) and render
    `pages/wishlist.html`.
    Create `app/templates/pages/wishlist.html` (extends base.html, page_title "Wishlist") per 07-PATTERNS lines 698-723:
    list entries; each row shows coffee_name + roaster_name + (source_url as a link if present), a "Mark purchased" form
    posting to /ai/wishlist/{id}/purchase and a "Remove" form posting to /ai/wishlist/{id}/remove (both with hidden
    X-CSRF-Token, hx-post + hx-target the row/list). Purchased entries (purchased_at not None) shown with a "purchased"
    badge / dimmed style. Empty state copy. Mobile-first container.
    Edit `app/templates/pages/home.html`: inside the gate-open content, add (a) a link/button to /ai/paste-rank
    ("Rank these for me"), (b) a link to /ai/wishlist ("Wishlist"), and (c) an "Analyze my setup" equipment section:
    a button with hx-post="/ai/equipment" hx-target="#equipment-rec-result" hx-swap="innerHTML" + a hidden X-CSRF-Token
    field + an empty `<div id="equipment-rec-result">` that the equipment_rec fragment swaps into (D-05 generate-on-click,
    never auto-loaded). Keep all CSP-clean (no hx-on:, no |safe).
    Add tests: test_wishlist_page_lists_user_entries (GET 200, only the user's entries shown), test_wishlist_page_requires_auth,
    test_home_links_to_ai_pages (home GET contains /ai/paste-rank and /ai/wishlist), test_home_has_equipment_button
    (home GET contains hx-post="/ai/equipment").
  </action>
  <verify>
    <automated>docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py tests/routers/test_home.py -x -q -k "wishlist_page or home_links or equipment_button or home_has_equipment"</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/ai.py` defines `GET /ai/wishlist` gated by require_user, scoped via list_wishlist(by_user_id=user.id).
    - `pages/wishlist.html` renders mark-purchased + remove forms (CSRF hidden field) and distinguishes purchased entries (D-09).
    - `app/templates/pages/home.html` contains `/ai/paste-rank`, `/ai/wishlist`, and `hx-post="/ai/equipment"` with an `#equipment-rec-result` target (D-05).
    - `test_wishlist_page_lists_user_entries` and `test_home_links_to_ai_pages` pass.
    - Named pytest selection exits 0.
  </acceptance_criteria>
  <done>Wishlist page + GET handler + home links to paste-rank/wishlist + on-demand equipment button shipped and tested.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — paste-rank, wishlist, equipment button at 375px</name>
  <what-built>
    The dedicated "Rank these for me" page (text + URL input, top-3 results), the minimal wishlist view
    (list / mark-purchased / remove), the on-demand "Analyze my setup" equipment button + result on the
    home page, and the home-page links to both pages.
  </what-built>
  <how-to-verify>
    1. `docker compose build coffee-snobbery && docker compose up -d coffee-snobbery` (templates baked; rebuild required).
    2. At 375px: from home, follow the "Rank these for me" link. Paste a few coffee descriptions (and optionally a URL),
       submit, and confirm a top-3 ranked list with one-sentence reasoning renders without horizontal scroll.
    3. From a hero card (or directly), add a coffee to the wishlist, then open /ai/wishlist via the home link.
       Confirm it lists the entry; mark it purchased (badge/dim appears); remove an entry (row disappears).
    4. On home, click "Analyze my setup": confirm the equipment recommendation (or "no changes recommended") renders
       in place on click — and that it is NOT shown automatically before clicking (D-05).
    5. Confirm wishlist + paste-rank only ever show YOUR entries/results (per-user scoping).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /ai page GETs + POSTs | Untrusted client requests (paste input, entry ids) |
| paste-rank/equipment/wishlist results → rendered HTML | LLM/user text rendered in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering / XSS | paste-rank results, equipment result, wishlist render | mitigate | Autoescape; NEVER |safe on reasoning/summary_prose/coffee names. Acceptance: Jinja-safety CI grep passes on the new templates. |
| T-07-05 | Information Disclosure (IDOR) | wishlist page + purchase/remove | mitigate | list_wishlist + purchase/remove all scoped by user.id; cross-user id → 404 (07-05). Test: test_wishlist_page_lists_user_entries (only own entries). |
| T-07-11 | Cross-Site Request Forgery | mark-purchase/remove/paste-rank/equipment POSTs | mitigate | All forms carry the X-CSRF-Token field; starlette-csrf enforces; no route is csrf-exempt. |
| T-07-12 | Spoofing / Access Control | GET /ai/paste-rank, GET /ai/wishlist | mitigate | Both gated by Depends(require_user); user_id from request.state.user.id. Test: test_wishlist_page_requires_auth. |
</threat_model>

<verification>
- `docker compose exec coffee-snobbery python -m pytest tests/routers/test_ai_router.py tests/routers/test_home.py -q` green.
- Jinja-safety CI grep test passes (no |safe in the new pages/fragments).
- Human-verify checkpoint approved at 375px.
</verification>

<success_criteria>
- Paste-rank page (text+URL → top-3), wishlist minimal view (list/purchase/remove), on-demand equipment button, and home-page links all live, user-scoped, CSP-clean, and verified at 375px.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-services/07-07-SUMMARY.md`
</output>
